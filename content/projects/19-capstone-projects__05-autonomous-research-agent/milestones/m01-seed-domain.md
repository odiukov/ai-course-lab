Начни с исследовательского контракта, а не с формулировки общего интереса. Выбери узкую seed-идею, например исследование паттернов разреженности attention в трансформерах до 1B параметров, и опиши, какой вопрос должен разрешить агент. Зафиксируй предметную область достаточно узко, чтобы сравнивать гипотезы в одном дереве, но не своди задачу к локальной имитации или заранее известному ответу.

Определи допустимые модели, датасеты и вычислительный бюджет. Отдельно зафиксируй общий потолок $30 на статью и то, какие расходы входят в него. Поиск должен иметь конечные границы: планировщик вправе выбирать только из объявленных конфигурационных измерений, а каждый дочерний узел должен менять ровно одно из них. Запиши исходные baseline, обязательные seed, ожидаемые метрики и условия, при которых эксперимент считается осмысленным.

Согласуй область с доступной облачной инфраструктурой. Здесь не требуется запускать исследования, но нужно убедиться, что выбранные модели и данные совместимы с будущими лимитами E2B или Daytona и с PyTorch 2.5. Типичные ошибки — сформулировать тему без проверяемого вопроса, оставить пространство поиска бесконечным, забыть baseline или зарезервировать весь бюджет на единственный дорогой прогон. Результатом этапа должен стать стабильный входной контракт для литературы, планировщика, verifier и финальной статьи.
