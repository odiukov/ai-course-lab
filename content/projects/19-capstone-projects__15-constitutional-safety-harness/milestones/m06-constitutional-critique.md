Сформируй набор из 1 000 harmful-attempt промптов для training-time интервенции и отдельно выдели held-out eval, который не попадёт в дообучение. Для каждого обучающего примера целевая модель сначала создаёт черновой ответ. Затем critic LLM оценивает его по письменной конституции: не причинять вред, приводить доказательства и отказывать в незаконных запросах. Если critic возражает, ответ переписывается с учётом критики. Сохраняй исходный draft, решение critic и улучшенную версию как связанную запись.

Из critique-improved пар подготовь данные для SFT и дообучи ту же целевую модель, чья исходная версия была зафиксирована на первом этапе. Не подменяй этот шаг дополнительным serving-фильтром: конституционная самокритика является вмешательством во время обучения, тогда как classifier gate остаётся независимым контуром инференса. Также не оценивай обновлённую модель на тех же примерах, которые использовались для SFT.

Запусти held-out eval до и после обучения в сопоставимых условиях и рассчитай harmlessness delta. Сохрани конфигурацию конституции, связи между примерами и результаты обеих версий модели, чтобы изменение можно было воспроизвести и проверить. Этап считается законченным только после настоящего training run и внешнего evaluation run; локальный scaffold вообще не реализует critic или fine-tuning и не является заменой этой части capstone.
