- concept: повтор действия свидетельствует о петле только при совпадении действия и входных условий и отсутствии нового результата между попытками
  question: В журнале агента одна и та же команда запуска тестов встречается подряд несколько раз. Какое условие превращает этот повтор в признак петли, а не в нормальный retry?
  explanation: 'Ретрай после сбоя или повторный прогон тестов после правки кода — прогресс: изменились входные условия. Петля опознаётся по неизменности входов и отсутствию нового результата между попытками.'
  kind: choice
  options:
    - Действие и входные условия совпадают, а между попытками не появилось ни нового результата, ни изменения, ни решения
    - Действие встретилось в журнале больше одного раза за сессию
    - Каждая следующая попытка выполняется дольше предыдущей
    - Повтор произошёл после ошибки сети или таймаута инструмента
  correct: 0
  id: 013-povtory-v-trasse-1
  fingerprint: a5078cb2
- concept: 'локализация дефекта по повтору: незаписанный результат указывает на состояние, неизвестный исход — на обратную связь'
  question: 'Прогон A: команда завершилась, её вывод вернулся в цикл, но в состоянии нет отметки о выполнении. Прогон B: в состоянии записано «команда запущена», а исход команды в цикл не попал. Чего не хватает в каждом случае?'
  explanation: Внешне симптом один — повторяющийся шаг. Но в A агент не помнит, что действие сделано, а в B помнит попытку и не знает её исхода. Это разные поверхности, и лечатся разными правками окружения.
  kind: choice
  options:
    - A — записи результата в состояние, B — обратной связи об исходе
    - A — обратной связи об исходе, B — записи результата в состояние
    - В обоих случаях — обратной связи об исходе
    - В обоих случаях — контракта области изменений
  correct: 0
  id: 013-povtory-v-trasse-2
  fingerprint: 26a1370f
- concept: обнаружение петли — функция окружения, потому что внутри цикла у исполнителя нет признака, по которому петля отличима от продолжения работы
  question: Объясни своими словами, почему детекцию повторяющихся шагов размещают в окружении агента, а не полагаются на то, что агент сам заметит зацикливание.
  explanation: Петля — свойство трассы, а не отдельного шага. Наблюдатель с журналом обнаруживает её дешевле, чем исполнитель, у которого каждый ход выглядит обоснованным.
  kind: open
  reference: 'Агент внутри петли видит на каждом ходу одну и ту же непротиворечивую картину: план не отмечен как выполненный либо исход неизвестен, значит действие уместно. Признак петли лежит не в отдельном ходу, а в последовательности ходов — его видно только тому, кто хранит журнал и сравнивает действия с входными условиями. Поэтому проверку ставят снаружи: она сравнивает записи журнала и выдаёт места для разбора, экономя вызовы инструментов, которые иначе тратятся без продвижения задачи.'
  id: 013-povtory-v-trasse-3
  fingerprint: 142f36f3
