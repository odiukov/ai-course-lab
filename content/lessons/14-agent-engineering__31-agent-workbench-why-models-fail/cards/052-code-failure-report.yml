- concept: сводный отчёт вызывает уже написанные анализаторы вместо повторной реализации анализа, иначе выводы отчёта расходятся с выводами функций
  question: Сводный отчёт собирается из уже готовых функций анализа прогона. Заполни пропуск так, чтобы у каждого вывода остался единственный источник.
  explanation: 'Повторная реализация выбора слабой поверхности внутри отчёта создаёт второй источник истины: при правке правила один из двух ответов молча устареет, и отчёт начнёт противоречить самому аудиту.'
  kind: cloze
  template: |-
    report = {
        "missing": missing_surfaces(audit),
        "weakest": ___(audit),
        "failures": classify_failures(run),
    }
  answer: weakest_surface
  accept:
    - weakest_surface(audit)
    - 'weakest_surface '
  id: 052-code-failure-report-1
  fingerprint: cb71f734
