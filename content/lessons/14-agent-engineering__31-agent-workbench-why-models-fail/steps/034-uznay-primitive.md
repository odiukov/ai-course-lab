---
id: 034-uznay-primitive
type: check
title: 'Чем являются доска задач, MCP-вызов и список разрешённых файлов?'
source_anchor: '### Reasoning from primitives, not from vendor taxonomies'
check:
  - question: >-
      Ты видишь доску задач, запрос к MCP-серверу и список файлов, которые
      агенту разрешено менять. Какая тройка системных примитивов им
      соответствует?
    options:
      - 'Очередь, HTTP/RPC, политика авторизации'
      - 'Хранилище сессии, воркер, триггер'
      - 'Рантайм, функция, очередь'
      - 'Функция, очередь, воркер'
    correct: 0
    explanation: >-
      Доска удерживает ожидающую работу до свободного исполнителя — это очередь.
      Запрос к MCP-серверу идёт по проводу вызова — HTTP/RPC. Список разрешённых
      файлов задаёт, кто что может трогать, — политика авторизации.
  - question: >-
      MCP-сервер выполнил инструмент после полученного запроса. Чем в этой сцене
      является именно запрос, а не сервер и не действие инструмента?
    options:
      - Воркером
      - HTTP/RPC-вызовом
      - Политикой авторизации
      - Хранилищем сессии
    correct: 1
    explanation: >-
      В одной сцене сразу три разных примитива: сервер — долгоживущий воркер,
      инструмент — функция с входом и выходом, а запрос между ними — провод
      вызова, то есть HTTP/RPC. Классифицировать нужно конкретный элемент, а не
      сцену целиком.
---

Ты открываешь workbench и видишь три разных элемента интерфейса. Проверка здесь — суметь посмотреть сквозь их названия: доска удерживает работу до обработки, MCP-запрос связывает вызывающего с сервером, список файлов ограничивает допустимые действия. Обычно спотыкаются на MCP: сам сервер — воркер, инструмент — функция, но передаваемый ему запрос — HTTP/RPC из [шага 32](#step-32).

> 🎒 **На пальцах.** Представь пункт выдачи заказов: стеллаж с ожидающими коробками работает как очередь, звонок сотруднику склада — как удалённый вызов, а отметка «выдавать только владельцу паспорта» — как политика авторизации. Предметы разные, но роль каждого определяется не внешним видом, а тем, что он делает в системе.
