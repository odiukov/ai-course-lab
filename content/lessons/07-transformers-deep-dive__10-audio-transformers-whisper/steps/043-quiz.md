---
id: 043-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: Ты подаёшь Whisper запись длиной 10 секунд. Что увидит энкодер?
    options:
      - Ровно 1000 кадров — вход сжимается под длительность записи
      - >-
        Все те же 3000 кадров: остаток добьётся тишиной, и на этой тишине модель
        иногда начинает выдумывать текст
      - >-
        3000 кадров, но последние 2000 модель помечает как пустые и полностью
        игнорирует
      - 'Ошибку: запись короче 30 секунд не принимается'
    correct: 1
    explanation: >-
      Вход у Whisper фиксированный: 30 секунд, 3000 кадров. Короткая запись
      дополняется тишиной, и именно на этой тишине берутся галлюцинации.
  - question: >-
      У тебя японская речь, а на выходе нужен английский текст. Каким должен
      быть префикс декодера?
    options:
      - Токен языка ja и токен translate
      - Токен языка en и токен transcribe
      - 'Токен языка ja и токен transcribe, а перевод делается отдельной моделью'
      - >-
        Только токен translate: язык модель определит сама и укажет его на
        выходе
    correct: 0
    explanation: >-
      Язык в префиксе — это язык звука, а translate означает «выведи
      по-английски». Транскрипция и перевод отличаются одним токеном, отдельная
      модель не нужна.
  - question: >-
      Почему для одного 30-секундного куска энкодер запускается один раз, а
      декодер — десятки раз?
    options:
      - 'Энкодер быстрее, поэтому его успевают прогнать за один проход'
      - 'Декодер перезапускает энкодер на каждом слове, но результат кэшируется'
      - >-
        Звук не меняется, поэтому его представление считается один раз; текст же
        рождается по одному токену, и каждый новый токен требует нового прохода
        декодера
      - >-
        Энкодер обрабатывает весь звук сразу, а декодер обрабатывает по секунде
        звука за раз
    correct: 2
    explanation: >-
      Аудио фиксировано — его представление из 1500 шагов считается однажды.
      Авторегрессия же означает, что каждый следующий токен зависит от уже
      написанных, отсюда повторные проходы декодера.
  - question: >-
      У turbo энкодер взят от large-v3, а декодер урезан до 4 слоёв. Что это
      даёт на практике?
    options:
      - 'Всё работает вдвое быстрее, потому что параметров примерно вдвое меньше'
      - >-
        Экономия идёт там, где работа повторяется — в декодере; разовый проход
        энкодера остаётся таким же тяжёлым
      - 'Качество распознавания падает вдвое, зато память тоже вдвое меньше'
      - 'Экономится в первую очередь энкодер, потому что он самый большой'
    correct: 1
    explanation: >-
      Энкодер отрабатывает один раз на кусок и остаётся полноразмерным. Декодер
      же крутится на каждый токен, поэтому именно его урезание даёт заметное
      ускорение — но не пропорционально числу параметров.
---

Дальше в курсе тебе понадобится не список терминов, а четыре вещи из этого урока. Первая: звук у Whisper всегда приходит одной и той же картинкой — 30 секунд, 3000 кадров, и короткое становится длинным за счёт тишины ([шаг 38](#step-38)). Вторая: задача задаётся префиксом, а не другой моделью ([шаг 15](#step-15)). Третья: энкодер считает один раз, декодер — на каждый токен, и вся арифметика скорости растёт отсюда ([шаг 13](#step-13)). Четвёртая: у окна нет памяти о соседях, поэтому подкаст — это 120 независимых кусков ([шаг 28](#step-28)).

Спотыкаются обычно на двух местах: считают, что Whisper слушает поток вживую, и что перевод — это отдельная модель. Ни то, ни другое.
