---
id: 022-check-020
type: check
title: 'Проверка: откуда взялись 20 миллисекунд'
source_anchor: '### Step 6 — output'
check:
  - question: Откуда берётся шаг таймстемпа 0.02 секунды?
    options:
      - 30 секунд окна делятся на 1500 позиций энкодера
      - 30 секунд окна делятся на 3000 кадров мел-спектрограммы
      - 'Это округление до сотых, выбранное вручную при обучении'
    correct: 0
    explanation: >-
      Свёрточный стем ужимает 3000 кадров вдвое, и энкодер работает с 1500
      позициями. 30 / 1500 = 0.02 секунды на позицию.
  - question: >-
      Представь, что второй свёрточный слой оставили с шагом 1 и на выходе
      энкодера получилось 3000 позиций вместо 1500. Что стало бы с шагом
      таймстемпа?
    options:
      - 'Остался бы 0.02 секунды: шаг задаётся отдельно от длины'
      - 'Стал бы 0.01 секунды: то же окно делится на вдвое большее число позиций'
      - 'Стал бы 0.04 секунды: позиций больше, значит каждая длиннее'
    correct: 1
    explanation: >-
      30 секунд делятся на 3000 позиций, получается 0.01 секунды. Шаг таймстемпа
      — прямое следствие того, сколько позиций пришлось на окно.
---

Число 0.02 легко принять за круглую константу, которую кто-то выбрал руками. Оно не выбрано — оно посчитано, и посчитать его умеешь ты сам.

Тебе нужны ровно две вещи из уже пройденного: длина окна из [шага 5](#step-5) и то, во что превратились 3000 кадров после свёрточного стема ([шаг 8](#step-8)). Дальше одно деление.

Спотыкаются обычно в одном месте: берут 3000 вместо 1500 и получают 0.01 — то есть забывают, что стем ужал дорожку вдвое. Второе типичное место — считать, что таймстемпы предсказываются всегда: они появляются, только если в префиксе нет `<|notimestamps|>` ([шаг 20](#step-20)).

Проверь себя на двух вопросах ниже.
