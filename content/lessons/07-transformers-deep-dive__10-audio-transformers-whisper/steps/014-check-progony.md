---
id: 014-check-progony
type: check
title: 'Проверка: сколько раз запускается энкодер и декодер'
source_anchor: '### Step 4 — decoder'
check:
  - question: >-
      Whisper расшифровывает 30-секундный фрагмент и выдаёт 40 токенов текста.
      Сколько раз запустится энкодер и сколько раз декодер?
    options:
      - 'Энкодер 40 раз, декодер 40 раз'
      - 'Энкодер 1 раз, декодер 40 раз'
      - 'Энкодер 1500 раз, декодер 40 раз'
      - 'Энкодер 1 раз, декодер 1 раз'
    correct: 1
    explanation: >-
      Энкодер обрабатывает все 1500 шагов за один проход и больше к аудио не
      возвращается, а декодер выдаёт по одному токену за проход — значит, ровно
      столько проходов, сколько токенов.
  - question: >-
      Ты подаёшь на вход два фрагмента по 30 секунд: на первом человек молчит и
      говорит одно слово, на втором тараторит длинную фразу. Что будет заметно
      медленнее и почему?
    options:
      - 'Оба одинаково: длина окна одна и та же — 30 секунд'
      - >-
        Второй, потому что в нём получается больше токенов, и декодер
        запускается больше раз
      - 'Первый, потому что тишину модели труднее разобрать'
      - 'Второй, потому что энкодеру приходится обработать больше кадров'
    correct: 1
    explanation: >-
      Оба фрагмента дают одинаковые 3000 кадров и один проход энкодера, но
      текста во втором больше — а каждый лишний токен это лишний запуск
      декодера.
---

Из [шага 13](#step-13) ты знаешь, что декодер низает токены по одному. Но у этого есть неочевидное следствие, которое стоит проверить на себе, прежде чем идти дальше: если попросить Whisper расшифровать одну и ту же тридцатисекундку дважды — сначала коротким ответом, потом длинным, — время работы окажется разным, хотя аудио не менялось.

Здесь обычно спотыкаются в двух местах. Первое: кажется, что энкодер тоже «дорабатывает» на каждом токене — нет, он отрабатывает 1500 шагов один раз, и дальше декодер просто заглядывает в готовый результат. Второе: кажется, что длина аудио решает скорость — но окно всегда 30 секунд из [шага 5](#step-5), а вот текст бывает и в слово, и в двести.
