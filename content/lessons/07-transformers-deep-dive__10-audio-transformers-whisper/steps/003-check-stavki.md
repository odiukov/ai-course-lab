---
id: 003-check-stavki
type: check
title: 'Проверка: чем Whisper отличался от wav2vec 2.0'
source_anchor: '## The Problem'
check:
  - question: >-
      У тебя есть записи на языке, для которого чистого размеченного корпуса
      просто нет. Почему старый подход тут упирается, а Whisper — нет?
    options:
      - 'Старый подход не умеет читать спектрограммы, а Whisper умеет'
      - >-
        Старому подходу нужна голова, дообученная на размеченных данных именно
        этого языка, а Whisper учился сразу на 680 000 часов слабо размеченного
        аудио на 97 языках
      - >-
        Старый подход работает только с записями студийного качества, а Whisper
        — с любыми
      - 'Разницы нет, обе модели требуют одинаковой разметки'
    correct: 1
    explanation: >-
      Дорогое звено старого конвейера — размеченные данные под конкретный язык и
      домен. Whisper обошёл это, взяв огромный грязный корпус вместо маленького
      чистого.
  - question: >-
      Тебе нужно от одной записи получить и текст, и перевод, и метку языка. Что
      делает Whisper?
    options:
      - 'Запускает три отдельные модели, по одной на задачу'
      - >-
        Использует одну модель, а нужную задачу задаёт служебный токен на входе
        декодера
      - >-
        Использует одну модель, но её приходится дообучать заново под каждую
        задачу
      - Определяет задачу автоматически по длине аудио
    correct: 1
    explanation: >-
      Второй ставкой был один декодер на все задачи: транскрипция, перевод,
      детекция речи, язык и таймстемпы различаются служебным токеном, а не
      отдельной моделью.
---

Дальше пойдёт разбор конвейера по деталям, и там будет легко потерять из виду, зачем всё это устроено именно так. Поэтому проверь себя сейчас: сможешь ли ты, глядя на конкретную задачу — записи с плохого микрофона на языке, для которого нет чистого корпуса, — объяснить, почему подход из [шага 1](#step-1) тут буксует, а три ставки из [шага 2](#step-2) его вытаскивают.

Чаще всего спотыкаются на двух местах. Первое: путают «много данных» с «хороших данных» — у Whisper корпус как раз грязный, ставка была на объём. Второе: думают, что перевод и определение языка делают разные модели. Модель одна, задачу выбирает служебный токен.
