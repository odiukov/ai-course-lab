---
id: 010-quiz
type: quiz
title: >-
  Разбираем границы повторов, неизвестный исход таймаута и семантику ключа
  идемпотентности
source_anchor: '## Going further'
check:
  - question: >-
      Какие структурированные события полезно выдавать непосредственно при
      выполнении и повторе вызова?
    options:
      - dispatch.attempt и dispatch.retry
      - schema.created и schema.deleted
      - cache.sorted и cache.reversed
    correct: 0
    explanation: >-
      События dispatch.attempt и dispatch.retry отражают ключевые переходы цикла
      исполнения и позволяют наблюдать историю попыток.
  - question: Какое условие обычно переводит circuit breaker в открытое состояние?
    options:
      - Достижение порога сбоев за заданное окно времени
      - Любой успешный ответ обработчика
      - Наличие необязательного аргумента в вызове
      - Освобождение места в семафоре
    correct: 0
    explanation: >-
      Предохранитель агрегирует сбои во временном окне. При достижении порога он
      открывается и даёт нестабильному инструменту время на восстановление.
  - question: 'Что должен сделать диспетчер, пока circuit breaker инструмента открыт?'
    options:
      - 'Сразу вернуть ошибку kind=circuit_open, не запуская обработчик'
      - Запускать обработчик без таймаута
      - Считать каждый вызов успешным и возвращать пустой результат
      - Создавать несколько параллельных пробных вызовов
    correct: 0
    explanation: >-
      Открытый предохранитель прекращает бесполезные обращения к неработающему
      инструменту и немедленно возвращает нормализованную ошибку circuit_open.
  - question: >-
      Почему добавление circuit breaker не требует менять внешний контракт
      диспетчера?
    options:
      - >-
        Точка входа остаётся прежней, а отказ выражается новым kind внутри
        DispatchError
      - Предохранитель скрывает все ошибки и всегда возвращает DispatchOk
      - >-
        После открытия предохранителя вызывающая сторона обращается к
        обработчику напрямую
      - Предохранитель применяется только во время регистрации инструмента
    correct: 0
    explanation: >-
      Вызывающая сторона по-прежнему использует dispatch и получает
      DispatchError. Расширяется классификация ошибки, а не форма взаимодействия
      с диспетчером.
---

Дальше диспетчер станет частью агента, поэтому тебе важно удерживать весь контракт целиком: порядок входных заслонов из [шага 8](#step-8), единый вызов обработчиков, общий исход дедупликации и границы повторов из [шагов 6](#step-6)–[7](#step-7). Чаще всего путают число повторов с числом попыток и принимают таймаут за доказанный провал. Но таймаут оставляет исход неизвестным, а ключ идемпотентности обозначает логическую операцию, не отдельный сетевой запрос.
