---
id: 010-check-modes
type: check
title: 'Проверка: PDF можно, картинку нет'
source_anchor: '### Agent Card'
check:
  - question: >-
      У агента-исследователя один skill: inputModes — text и file, outputModes —
      text и artifact. Ты хочешь отправить ему скриншот графика и попросить
      описать его. Что произойдёт?
    options:
      - 'Отправишь и подождёшь ответа: пока не попробуешь, узнать нельзя'
      - >-
        Ещё до запроса видно, что этот skill такое не обещает, и звать его
        смысла нет
      - >-
        Отправишь картинку, а агент сам переведёт её в text, раз text у него в
        inputModes
    correct: 1
    explanation: >-
      Карточка перечисляет режимы заранее, поэтому несовпадение видно до первого
      запроса — из описания skill следует, что file здесь про PDF статьи, а не
      про любой файл.
  - question: >-
      Тот же skill. Ты просишь у него ответ в виде готового файла-вложения. Куда
      смотреть, чтобы понять, получится ли?
    options:
      - 'В inputModes: они описывают весь обмен целиком'
      - >-
        В outputModes: там text и artifact, то есть файл-результат он вернуть
        может
      - >-
        В поле capabilities: streaming и pushNotifications отвечают за формат
        ответа
    correct: 1
    explanation: >-
      inputModes говорят, что можно прислать, outputModes — что придёт назад;
      artifact в outputModes и есть обещание вернуть результат отдельным
      объектом.
---

После этого куска ты должен уметь взять чужую карточку и до первого запроса сказать: мои данные этому skill подойдут или нет. Практически это значит — не путать две стороны обмена. `inputModes` из [шага 6](#step-6) описывают только то, что ты отправляешь; `outputModes` — только то, что вернётся.

Спотыкаются обычно на двух вещах. Первая: `file` читают как «любой файл», хотя описание skill прямо говорит про PDF статьи, и картинка туда не попадает. Вторая: пытаются проверить совместимость запросом — отправить и посмотреть на ошибку. Смысл карточки ровно в обратном: несовпадение видно заранее, ещё до того как `select_skill` из [шага 9](#step-9) вообще что-то выберет.
