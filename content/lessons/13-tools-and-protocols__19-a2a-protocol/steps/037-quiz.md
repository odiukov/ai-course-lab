---
id: 037-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      Твоему агенту нужно вызвать внутреннюю функцию расчёта НДС и передать
      целую задачу по написанию письма внешнему агенту. Как распределить эти
      вызовы?
    options:
      - 'Оба выполнить через A2A: характер вызываемой стороны не имеет значения'
      - >-
        Расчёт НДС вызвать как инструмент через MCP, а письмо делегировать
        агенту через A2A
      - >-
        Расчёт НДС передать через A2A, а письмо — через MCP, поскольку его
        результат является текстом
      - 'Оба выполнить через MCP: A2A применяется только при платежах'
    correct: 1
    explanation: >-
      MCP подходит для вызова конкретного доступного инструмента, а A2A — для
      делегирования самостоятельной задачи другому агенту без доступа к его
      внутренней реализации.
  - question: >-
      После получения Signed Agent Card проверка её канонического представления
      доверенным ключом завершилась неуспешно. Как следует поступить?
    options:
      - 'Продолжить работу, считая карточку лишь устаревшей'
      - 'Довериться карточке, если имя агента и адрес выглядят правдоподобно'
      - >-
        Не доверять карточке, поскольку подпись не подтверждает полученные
        данные
      - Исключить необязательные поля и принять карточку по оставшейся подписи
    correct: 2
    explanation: >-
      Криптографическая подпись подтверждает конкретную последовательность
      байтов. Если проверка не прошла, подлинность и целостность полученной
      карточки не установлены.
  - question: >-
      Задача A2A перешла в состояние input-required. Что должен сделать
      вызывающий агент?
    options:
      - Считать выполнение проваленным и создать новую задачу
      - Передать запрошенные данные в существующую задачу
      - 'Ничего не делать: выполнение обязательно продолжится автоматически'
      - 'Забрать итоговый артефакт, поскольку задача уже завершена'
    correct: 1
    explanation: >-
      Input-required — нефинальная пауза: агенту не хватает входных данных.
      После их передачи та же задача может вернуться к выполнению.
  - question: >-
      Почему итоговый результат задачи полезно возвращать как Artifact с именем
      и MIME-типом, а не как неразмеченную строку?
    options:
      - A2A обязательно сжимает Artifact сильнее обычного текста
      - Artifact нужен только для совместимости со старыми клиентами
      - Только содержимое Artifact может быть защищено подписью
      - Получатель может определить назначение и формат результата по метаданным
    correct: 3
    explanation: >-
      Имя сообщает назначение результата, а MIME-тип — его формат и способ
      обработки. Получателю не приходится угадывать структуру по содержимому
      строки.
---

Урок закончился, но почти всё из него ты ещё будешь трогать руками.

Дальше в курсе агенты перестанут быть одиночками: один зовёт другого, тот третьего, и вопрос «а этому вообще можно верить» встанет уже не в теории. Тогда пригодится связка из [шагов 12](#step-12)–[13](#step-13) и [17](#step-17): каноническая форма, подпись, сверка байт в байт.

Где обычно спотыкаются. Первое — граница MCP и A2A из [шага 35](#step-35): её тянет размыть, и тогда протокол выбирают по привычке, а не по тому, чужой на другом конце агент или свой инструмент. Второе — состояния Task: `input-required` читают как ошибку и начинают задачу заново, хотя это пауза ([шаг 19](#step-19)). Третье — непрозрачность из [шага 32](#step-32): попытка «заглянуть внутрь» чужого агента вместо работы с его артефактом.

Проверь себя на вопросах выше — они по всему уроку, не по последнему экрану.
