---
id: 033-check-opacity
type: check
title: 'Проверка: что видит вызывающий'
source_anchor: '### Opacity preservation'
check:
  - question: >-
      Агент поддержки отдал задачу агенту-писателю. Тот сходил в свою базу
      знаний, несколько раз обратился к модели и позвал под-агента за версткой.
      Что из этого увидит вызывающий?
    options:
      - Всю последовательность внутренних операций
      - Только смену состояний задачи и переданные артефакты
      - 'Обращения к базе и под-агенту, но не работу модели'
      - 'Ничего до завершения задачи, включая её состояния'
    correct: 1
    explanation: >-
      Вызывающему доступны состояния задачи и артефакты. Обращения к базе,
      модели и под-агентам остаются внутренними деталями вызванного агента.
  - question: Чем A2A отличается от MCP с точки зрения видимости выполняемой работы?
    options:
      - >-
        В MCP host видит вызовы инструментов и их аргументы, а A2A скрывает
        внутреннюю работу вызванного агента
      - 'MCP скрывает всю работу, а A2A раскрывает цепочку рассуждений'
      - >-
        Оба протокола раскрывают вызовы инструментов; отличаются только
        транспорты
      - A2A раскрывает внутреннюю работу только владельцу Agent Card
    correct: 0
    explanation: >-
      MCP позволяет host наблюдать вызовы подконтрольных ему инструментов. При
      взаимодействии через A2A другой агент остаётся непрозрачным и
      предоставляет только состояния задачи и артефакты.
---

После [шага 32](#step-32) ты должен уметь провести границу: что из работы вызванного агента доходит до вызывающего, а что остаётся за дверью. Наружу — состояния задачи из [шага 19](#step-19) и артефакты. Внутрь — рассуждения, вызовы инструментов, собственные под-агенты.

Спотыкаются обычно на двух вещах. Первая: непрозрачность путают с молчанием — кажется, будто вызывающий сидит в темноте до самого конца, хотя состояния он видит всё время. Вторая: непрозрачность принимают за недостаток, тогда как это условие, без которого два банка-конкурента вообще не соединили бы своих агентов.
