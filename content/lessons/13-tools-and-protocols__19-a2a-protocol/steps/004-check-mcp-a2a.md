---
id: 004-check-mcp-a2a
type: check
title: 'Проверка: инструмент или агент'
source_anchor: '## The Problem'
check:
  - question: >-
      У тебя есть сервис погоды: он принимает город и возвращает температуру.
      Также есть агент-аналитик: он сам решает, какие данные собрать и как
      построить вывод. Как их правильно подключить к агенту поддержки?
    options:
      - 'Оба сервиса через A2A, потому что они внешние'
      - >-
        Сервис погоды через MCP как инструмент, агента-аналитика через A2A как
        агента
      - 'Оба сервиса через MCP, потому что в итоге они возвращают результат'
      - 'Сервис погоды через A2A, агента-аналитика через MCP'
    correct: 1
    explanation: >-
      Сервис погоды предоставляет конкретную операцию с заранее определённым
      интерфейсом, поэтому подходит MCP. Аналитик самостоятельно выбирает шаги и
      взаимодействует через задачу с состояниями и результатом, поэтому подходит
      A2A.
  - question: >-
      Почему собственный REST API между агентом поддержки и агентом-писателем
      плохо масштабируется при добавлении новых агентов?
    options:
      - REST слишком медленно передаёт текстовые данные
      - REST принципиально не позволяет сообщать состояние задачи
      - >-
        Для каждой новой пары агентов приходится отдельно согласовывать и
        реализовывать интеграцию
      - >-
        REST требует, чтобы все агенты были написаны на одном языке
        программирования
    correct: 2
    explanation: >-
      REST способен передавать и текст, и состояния, но сам по себе не задаёт
      общего контракта взаимодействия агентов. Без такого контракта каждая пара
      отдельно согласовывает форматы запросов, ответов, ошибок и статусов.
---

Прежде чем идти дальше, проверь одну вещь: умеешь ли ты сам, глядя на чужой сервис, сказать — это инструмент или это агент. Инструмент делает ровно то, что ты попросил, и ты заранее знаешь форму ответа; такому нужен MCP. Агент сам выбирает шаги, и его внутренние рассуждения остаются для тебя закрытыми — видны только состояния задачи и результат; такому нужен A2A.

Спотыкаются обычно на двух местах. Первое: «A2A вместо MCP» — нет, они про разные вещи и живут рядом. Второе: считают признаком агента сложность кода. Признак другой — кто решает, что делать дальше.
