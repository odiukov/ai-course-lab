- concept: если попарное сравнение завершилось без расхождения, длина общего префикса равна длине более короткого списка
  question: Заполни возврат для случая, когда цикл не обнаружил различающихся токенов.
  explanation: '`zip` прекращает обход при завершении более короткого списка. Если до этого расхождения не было, весь короткий список является общим префиксом.'
  kind: cloze
  template: |-
    def common_prefix_len(left, right):
        for index, (a, b) in enumerate(zip(left, right)):
            if a != b:
                return index
        return ___
  answer: min(len(left), len(right))
  accept:
    - min(len(right), len(left))
  id: 005-common-prefix-1
  fingerprint: 77b8a302
