---
id: 009-block-savings
type: check
title: >-
  Почему запрос Carol вычисляет 4 блока вместо 160 и получает ускорение prefill
  около 40x?
source_anchor: '### The radix tree as a KV index'
check:
  - question: >-
      Почему для запроса Carol после прохода по совпавшим ветвям вычисляется
      только новый хвост?
    options:
      - Совпавший префикс удаляется из контекста запроса
      - >-
        KV-блоки совпавшего префикса уже сохранены и не требуют повторного
        prefill
      - Модель заменяет совпавший префикс кратким текстовым описанием
      - Планировщик пропускает префикс независимо от наличия его KV-блоков
    correct: 1
    explanation: >-
      Для системного префикса и doc A уже доступны 155 готовых KV-блоков,
      поэтому заново вычисляются только 4 блока вопроса Carol.
  - question: >-
      Почему ускорение prefill оценивают отношением работы без кэша к новой
      работе, а не разностью числа блоков?
    options:
      - >-
        Коэффициент ускорения показывает, во сколько раз один объём вычислений
        больше другого
      - 'Разность применима только к числу токенов, но не к KV-блокам'
      - RadixAttention всегда сообщает ускорение только в процентах
      - Отношение учитывает размер всего сохранённого дерева
    correct: 0
    explanation: >-
      Фраза «во сколько раз» требует отношения сравниваемых объёмов работы.
      Поэтому 160 блоков без переиспользования делят на 4 вычисляемых блока и
      получают ускорение около 40×.
---

Здесь легко перепутать размер всего запроса с объёмом новой работы. После прохода по дереву из [шага 8](#step-8) Carol не вычисляет заново совпавшие 124 и 31 блок: их готовые KV-блоки просто подключаются. На GPU отправляется только новый хвост из 4 блоков.

Для ускорения сравнивай именно вычисляемую работу: прежние 160 блоков против новых 4. Отсюда около 40 раз. Не вычитай 159 из 160: число 159 описывает части сохранённого пути и хвоста на схеме, а не объём работы без кэша. Главный вопрос всегда один: сколько блоков нужно посчитать сейчас?
