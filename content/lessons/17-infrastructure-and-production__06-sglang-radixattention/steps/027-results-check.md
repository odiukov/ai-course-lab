---
id: 027-results-check
type: check
title: >-
  Разница возникла в prefill, decode или ожидании — и почему перестановка частей
  меняет результат?
source_anchor: '## Exercises'
check:
  - question: >-
      После cache-aware планирования новых prefill-токенов стало меньше, число
      сгенерированных токенов не изменилось, а некоторые запросы дольше ждали
      запуска. Откуда взялась разница?
    options:
      - Из ускорения decode и сокращения ожидания
      - 'Из экономии на prefill и изменения ожидания, но не из экономии на decode'
      - Только из уменьшения числа decode-токенов
    correct: 1
    explanation: >-
      Кэш устраняет повторный prefill, а перестановка очереди меняет ожидание
      отдельных запросов. Если длина ответа не изменилась, объём decode не
      сокращается.
  - question: >-
      Ты случайно переставил части system, tools и context в каждом запросе. Что
      вероятнее всего произойдёт?
    options:
      - 'Доля попаданий вырастет, потому что содержимое частей осталось прежним'
      - 'Ускорится только decode, а доступный общий префикс не изменится'
      - >-
        Доля попаданий упадёт, потому что точное совпадение токенов оборвётся
        раньше
    correct: 2
    explanation: >-
      RadixAttention сопоставляет токены последовательно от начала промпта. Те
      же части в другом порядке образуют другой путь в дереве и раньше разводят
      запросы по ветвям.
---

Здесь легко приписать всё ускорение «кэшу». Но разделяй три места: повторная работа исчезает именно в prefill; decode остаётся прежним, если модель генерирует столько же токенов; ожидание меняется потому, что cache-aware очередь обслуживает запросы не в порядке прихода. Поэтому один запрос может стартовать позже, хотя вся нагрузка завершится быстрее.

Перестановка частей бьёт по другой причине: дерево сравнивает точную последовательность токенов, как в [шаге 12](#step-12). Одинаковое содержимое в другом порядке — уже другой префикс.
