---
id: 015-adain-check
type: check
title: Вычислите среднее и разброс после AdaIN и сравните два положения epsilon
source_anchor: '### Step 2: adaptive instance normalization'
check:
  - question: >-
      После нормализации значения имеют среднее 0 и стандартное отклонение 1.
      Какими станут среднее и стандартное отклонение после масштаба -3 и
      смещения 4?
    options:
      - 'Среднее 4, стандартное отклонение 3'
      - 'Среднее 4, стандартное отклонение -3'
      - 'Среднее 1, стандартное отклонение 4'
    correct: 0
    explanation: >-
      Смещение задаёт новое среднее, а модуль масштаба — новое стандартное
      отклонение. Отрицательный масштаб переворачивает порядок значений, но
      стандартное отклонение не бывает отрицательным.
  - question: >-
      Как различаются нижние границы знаменателя при почти нулевой дисперсии,
      если epsilon поставить под корнем или прибавить после него?
    options:
      - 'Под корнем граница равна √epsilon, а после корня — epsilon'
      - 'Под корнем граница равна epsilon, а после корня — √epsilon'
      - В обоих вариантах граница равна epsilon
    correct: 0
    explanation: >-
      При epsilon = 1e-8 вариант под корнем даёт знаменатель не меньше 1e-4, а
      прибавление после корня — лишь 1e-8. Поэтому второй вариант гораздо слабее
      ограничивает усиление малых отклонений.
---

Здесь тебе нужно связать два результата из [шага 12](#step-12): после нормализации среднее близко к нулю, а стандартное отклонение — к единице. Масштаб превращает разброс в модуль своего значения, затем смещение задаёт новое среднее. Отрицательный масштаб не создаёт отрицательный разброс — он лишь переворачивает порядок активаций.

Отдельно проверь защиту из [шага 14](#step-14): положение epsilon почти незаметно для обычной карты, но радикально меняет результат при крошечной дисперсии.

> 🎒 **На пальцах.** Представь пружинные весы с ограничителем. Масштаб растягивает или переворачивает шкалу, а смещение переносит её нулевую отметку. Epsilon задаёт упор: под корнем он останавливает движение раньше, а после корня позволяет стрелке приблизиться к опасной границе в десять тысяч раз сильнее.
