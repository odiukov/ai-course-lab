---
id: 060-quiz
type: quiz
title: Итоговая проверка
source_anchor: '## Key Terms'
check:
  - question: >-
      Один и тот же вопрос ты задаёшь двумя способами: в первом случае
      вываливаешь в окно 100 000 токенов всей документации проекта, во втором —
      10 000 токенов отобранных кусков, где ответ точно есть. Что получится?
    options:
      - >-
        Первый вариант надёжнее: чем больше материала, тем выше шанс, что нужное
        попало в окно
      - >-
        Второй вариант надёжнее: внимание модели не растёт вместе с объёмом, и
        нужный кусок тонет среди лишнего
      - 'Разницы не будет, пока оба варианта помещаются в окно'
    correct: 1
    explanation: >-
      Ёмкость окна и внимание — разные вещи. Лишний контекст может размывать
      сигнал: нужный фрагмент оказывается среди большого количества
      нерелевантных данных.
  - question: >-
      Окно 200 000 токенов. Системный промпт, инструменты, история и найденные
      документы вместе заняли 199 000. Что произойдёт дальше?
    options:
      - 'Модель ответит короче, но нормально — она сама ужмёт ответ под остаток'
      - 'Модель отбросит часть входа, чтобы освободить место под ответ'
      - >-
        Модели почти негде отвечать: ответ и вход делят одно окно, и резерв под
        генерацию надо было вычесть заранее
    correct: 2
    explanation: >-
      Вход и выход живут в одном окне. Поэтому сначала резервируют место для
      генерации, а затем распределяют оставшуюся ёмкость между компонентами
      входа.
  - question: >-
      У тебя 20 найденных документов и один из них — самый релевантный. Куда его
      положить?
    options:
      - В середину — так он окружён контекстом с обеих сторон
      - >-
        В начало или в конец — на краях точность держится около 85–90%, в
        середине падает до 60–70%
      - >-
        Порядок неважен: модель всё равно читает весь контекст целиком за один
        проход
    correct: 1
    explanation: >-
      Модель обрабатывает весь контекст, но извлекает информацию с разной
      точностью. Начало и конец обычно воспринимаются лучше, поэтому наиболее
      важные сведения размещают на краях.
  - question: >-
      Почему при запросе об ошибке в auth.py выгодно включить в контекст только
      определения инструментов, относящиеся к диагностике кода?
    options:
      - >-
        Нерелевантные определения инструментов занимают токены и отнимают место
        у истории и найденного кода
      - >-
        Модель не умеет работать более чем с несколькими инструментами
        одновременно
      - 'Инструменты, не связанные с диагностикой, всегда вызывают ошибку API'
    correct: 0
    explanation: >-
      Определения инструментов передаются модели вместе с остальным контекстом и
      оплачиваются токенами на каждом вызове. Отбор релевантных инструментов
      освобождает место для данных, нужных именно этому запросу.
---

Дальше в курсе ты будешь собирать окно руками — под retrieval, агентов, длинные диалоги. Из этого урока туда переезжают четыре привычки: считать токены до отправки, а не после ([шаг 22](#step-22)); вычитать резерв генерации первым делом ([шаг 25](#step-25)); класть важное по краям ([шаг 12](#step-12)); включать только те инструменты, которые нужны этому запросу ([шаг 45](#step-45)).

Спотыкаются обычно на трёх вещах. Считают, что большое окно снимает вопрос — не снимает, внимание в нём распределено неровно. Путают промпт и контекст: строка видна, остальные 22 000 токенов — нет. И забывают, что сжатие истории теряет детали намеренно, поэтому последние ходы оставляют дословно.
