- concept: число столбцов матрицы задаёт размерность входного вектора, число строк — размерность результата, поэтому преобразование может менять размерность пространства
  question: Матрица имеет семь строк и девять столбцов. Какой вектор она может принять на вход и что выдаст?
  explanation: Каждая строка перемножается с вектором поэлементно, значит длина строки (число столбцов, девять) обязана совпасть с длиной вектора. Каждая строка даёт ровно одно число результата, значит на выходе столько координат, сколько строк — семь. Отсюда матрица 7×9 переводит девятимерное пространство в семимерное.
  kind: choice
  options:
    - Принимает девятимерный вектор, выдаёт семимерный
    - Принимает семимерный вектор, выдаёт девятимерный
    - Принимает девятимерный вектор, выдаёт девятимерный
    - Принимает любой вектор, размерность результата зависит от его значений
  correct: 0
  id: 048-matrica-na-vektor-1
  fingerprint: 6adc4588
- concept: k-я координата результата матрично-векторного умножения получается из k-й строки матрицы и всего входного вектора целиком
  question: Вторая строка матрицы равна (4, 6, 8), входной вектор равен (9, 7, 4). Чему равна вторая координата результата умножения матрицы на этот вектор?
  explanation: 'Вторая координата ответа зависит только от второй строки, но от всего вектора: 4·9 + 6·7 + 8·4 = 36 + 42 + 32 = 110. Остальные строки на эту координату не влияют — каждая строка отвечает за свою позицию в результате.'
  kind: numeric
  answer: 110
  tolerance: 0.01
  id: 048-matrica-na-vektor-2
  fingerprint: 46a7dca2
- concept: в сумме для одной координаты результата индекс строки зафиксирован, а суммирование идёт по общему индексу, который одновременно нумерует элементы строки и координаты вектора
  question: Заполни пропуск в вычислении i-й координаты произведения матрицы на вектор.
  explanation: Индекс i фиксирует строку и задаёт номер координаты в ответе, а j бежит по длине строки. Один и тот же j нумерует и элемент строки, и координату вектора — они спариваются по позиции. Подстановка i вместо j сломала бы соответствие и потребовала бы квадратной матрицы, чтобы вообще не выйти за границы.
  kind: cloze
  template: result[i] = sum(M.rows[i][j] * v.components[___] for j in range(n_cols))
  answer: j
  accept:
    - j
  id: 048-matrica-na-vektor-3
  fingerprint: 76fddae6
