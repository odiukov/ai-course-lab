---
id: 044-check-proekciya
type: check
title: 'Проверка: проекция [1, 2, 3] на [1, 1, 1]'
source_anchor: '## Exercises'
check:
  - question: >-
      Почему коэффициент проекции делят на квадрат длины опорного вектора, а не
      на саму длину?
    options:
      - >-
        Скалярное произведение уже несёт в себе одну длину опорного вектора, а
        вторая появляется при умножении коэффициента на этот вектор — делить
        надо на обе
      - >-
        Разницы нет: деление на длину даёт тот же вектор, просто формулу принято
        писать через квадрат
      - >-
        Квадрат длины всегда положителен, а длина может выйти отрицательной и
        перевернуть проекцию
      - 'Так гарантируется, что проекция всегда короче исходного вектора'
    correct: 0
    explanation: >-
      a·b растёт пропорционально длине b, и коэффициент потом ещё раз умножается
      на b — итого два множителя |b|. Поэтому делитель b·b = |b|². Если поделить
      на |b|, проекция окажется длиннее нужной ровно в |b| раз: для [1, 2, 3] и
      [1, 1, 1] получилось бы 6/√3 ≈ 3.46 вместо 2. Длина неотрицательна по
      определению, так что дело не в знаке.
  - question: >-
      Из [1, 2, 3] вычли его проекцию на [1, 1, 1]. По какому признаку сразу
      видно, что полученный остаток перпендикулярен направлению проецирования?
    options:
      - >-
        Сумма координат остатка равна нулю, а сумма координат — это и есть
        скалярное произведение с [1, 1, 1]
      - Все координаты остатка по модулю меньше координат исходного вектора
      - >-
        У остатка есть нулевая координата, а ноль на любой позиции даёт
        перпендикулярность
      - >-
        Остаток короче проекции, а более короткий вектор всегда перпендикулярен
        более длинному
    correct: 0
    explanation: >-
      Скалярное произведение с вектором из единиц — это просто сумма координат.
      Проекция забирает всю часть вдоль диагонали, поэтому у остатка сумма
      координат обнуляется, и он в это направление вклада не даёт. Ни наличие
      нулевой координаты, ни сравнение длин перпендикулярность не доказывают.
---

Проекция — не абстрактное упражнение, а рабочий инструмент: именно так модель отвечает на вопрос «сколько в этих данных вот этого направления, а сколько — всего остального». Поэтому важно уметь посчитать её руками на маленьком примере, где всё видно.

После этого куска ты должен уметь взять два конкретных вектора, получить проекцию одного на другой числом-коэффициентом и вектором, а потом вычесть её и проверить, что остаток действительно перпендикулярен.

Спотыкаются обычно на двух вещах. Первая: делят на длину, а не на квадрат длины — тогда масштаб уезжает. Вторая: путают коэффициент и саму проекцию. Коэффициент здесь 2 — это «сколько раз укладывается направление». Проекция — это вектор [2, 2, 2]. И маленькая приятность: 2 — это среднее координат 1, 2, 3. Проекция на диагональ [1, 1, 1] и есть усреднение.
