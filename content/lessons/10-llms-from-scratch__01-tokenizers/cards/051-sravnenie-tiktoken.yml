- concept: разница в числе токенов между двумя byte-level BPE-токенизаторами объясняется объёмом корпуса и числом merges, а не различием алгоритмов
  question: Токенизатор A обучен byte-level BPE на одном учебном тексте, 300 склеиваний. Токенизатор B — тот же byte-level BPE, обучен на большом разнородном корпусе, словарь 32 000 токенов. На строке `def fibonacci(n):` A выдаёт 17 токенов, B — 6. Что корректно заключить?
  explanation: 'Оба выполняют одну процедуру: старт с байтов, слияние частых пар. При 300 склеиваниях длинные последовательности вроде `def ` и `return` просто не успевают стать отдельными токенами. Разрыв — результат разного опыта обучения, а не разного алгоритма. Вывод об ошибке в реализации требует отдельной проверки — roundtrip, а не счёта токенов.'
  kind: choice
  options:
    - 'Алгоритм B устроен умнее: он использует другое правило выбора пары'
    - Алгоритм у обоих один, различаются обучающие данные и число выполненных склеиваний
    - B применяет предварительную токенизацию по словам, а A работает с байтами
    - A содержит ошибку в реализации, иначе разрыв был бы меньше
  correct: 1
  id: 051-sravnenie-tiktoken-1
  fingerprint: 4d21990f
- concept: профиль разрыва по типам строк указывает на состав обучающего корпуса конкурента
  question: Свой BPE и промышленный токенизатор прогнали на четырёх строках. На обычной английской фразе разрыв 1.3x, на редком немецком составном слове — 1.6x, на фрагменте Python-кода — 3.1x. Что этот профиль сообщает о корпусе промышленного токенизатора?
  explanation: Наибольший выигрыш появляется там, где у токенизатора есть выученные куски. Скачок именно на коде означает, что склеивания вроде `def`, `return`, `):` окупились статистикой корпуса. Кодовая ASCII-строка не даёт лишних байтов на символ, а семейство алгоритма из одного счёта токенов не выводится.
  kind: choice
  options:
    - В его обучающем корпусе была заметная доля кода, поэтому конструкции языка программирования получили собственные токены
    - Он оптимизирован под английский, а на остальном работает случайно
    - Он использует Unigram, а не BPE, поэтому лучше режет код
    - Разрыв на коде вызван тем, что код содержит больше байтов на символ
  correct: 0
  id: 051-sravnenie-tiktoken-2
  fingerprint: 13ac8d67
- concept: 'порядок контролируемого сравнения двух токенизаторов: одинаковый вход, затем счёт, затем объяснение разницы'
  question: Расставь шаги честного сравнения самодельного BPE с промышленным токенизатором.
  explanation: 'Разный вход или разная подготовка текста подменяют измеряемую величину. Построчный разбор нужен, чтобы увидеть, где именно возникает разрыв: усреднение скроет, что на коде отставание втрое, а на простой фразе почти нет. Последний шаг превращает число в вывод: причина — опыт обучения, а не качество алгоритма.'
  kind: order
  items:
    - 'Зафиксировать один набор строк, покрывающий разные типы текста: обычная фраза, редкое слово, код, другой язык'
    - Прогнать обе реализации ровно на этих строках без нормализации, отличающейся между ними
    - Сравнить число токенов по каждой строке отдельно, а не одно усреднённое число
    - Объяснить наблюдаемый разрыв через различия обучающих корпусов и число выполненных склеиваний
  id: 051-sravnenie-tiktoken-3
  fingerprint: db7199f6
