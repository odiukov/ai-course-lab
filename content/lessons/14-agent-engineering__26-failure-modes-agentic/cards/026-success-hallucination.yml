- concept: 'детектор галлюцинации успеха срабатывает только при конъюнкции двух независимых признаков: заявленного успеха и неизменности целевого состояния'
  question: Детектор галлюцинации успеха разбирает трейсы агента. В каком из случаев он обязан вернуть положительный флаг?
  explanation: 'Флаг ставится только при одновременном выполнении обоих условий: агент заявил успех И состояние среды осталось прежним. Ни одно условие само по себе меткой не является — успех с реальным изменением состояния это нормальный результат, а неизменное состояние без заявления об успехе это обычный отказ.'
  kind: choice
  options:
    - Финальный отчёт агента — успех, снимок целевого состояния до и после прогона идентичен
    - Финальный отчёт агента — успех, целевая запись в базе изменилась после прогона
    - Финальный отчёт агента — неудача, целевое состояние не изменилось
    - Финальный отчёт агента — неудача, целевое состояние изменилось
  correct: 0
  id: 026-success-hallucination-1
  fingerprint: 767b34d5
- concept: признанная агентом неудача при неизменном состоянии не является галлюцинацией успеха, потому что расхождения между отчётом и средой нет
  question: 'Агент из 9 попыток создать заказ во всех получил ошибку, заказ не создан, финальный отчёт: «выполнить задачу не удалось». Как это классифицирует детектор галлюцинации успеха?'
  explanation: 'Галлюцинация успеха — это разрыв между словами агента и фактом в среде. Здесь разрыва нет: агент честно сообщил о провале, состояние действительно не изменилось. Это отказ другого рода, его ловят другие детекторы, а не этот.'
  kind: choice
  options:
    - 'Метку не ставит: отчёт совпадает с фактическим состоянием среды'
    - 'Ставит метку: целевое состояние не изменилось'
    - 'Ставит метку: было 9 неудачных вызовов инструмента'
    - Ставит метку с половинным весом, так как одно из условий выполнено
  correct: 0
  id: 026-success-hallucination-2
  fingerprint: 0e2ee242
- concept: объём активности в трейсе (число вызовов, длина цепочки, уверенность финального текста) не является свидетельством достижения цели
  question: Трейс агента содержит 17 вызовов инструментов, все со статусом 200, и уверенный финальный ответ. Объясни своими словами, почему это не доказывает выполнение задачи и какие именно данные читает детектор галлюцинации успеха.
  explanation: Главная ловушка — считать активность результатом. Проверяется не то, сколько агент сделал шагов, а изменилось ли конкретное целевое состояние; поэтому в детекторе два вычисления считаются отдельно и только потом объединяются.
  kind: open
  reference: 'Активность не равна результату: длина трейса, количество вызовов и тон финального ответа описывают усилия агента, а не наблюдаемое целевое состояние. Вызовы могли уходить не туда, писать в черновик, читать вместо записи или отменять друг друга. Детектор читает два независимых источника: флаг заявленного успеха из финального отчёта и снимки целевого состояния до и после прогона (появился ли файл, изменилась ли запись, отправилось ли сообщение). Метка ставится, когда успех заявлен, а снимки совпадают.'
  id: 026-success-hallucination-3
  fingerprint: 319cd919
