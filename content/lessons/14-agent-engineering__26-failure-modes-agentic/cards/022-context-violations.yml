- concept: нарушение раннего ограничения фиксируется только при явно конфликтующем значении поля, а отсутствие поля уликой не является
  question: 'В начале трейса задано ограничение: платёж проводить в валюте `EUR`. Дальше агент делает два вызова инструмента `charge`: вызов A — с аргументом `currency="USD"`, вызов B — вообще без аргумента `currency`. Какие вызовы сигнатурный детектор нарушений контекста должен пометить?'
  explanation: 'Помечается только явное противоречие. `currency="USD"` при требовании `EUR` — наблюдаемый конфликт значений. Пропущенное поле не доказывает нарушения: инструмент мог подставить значение по умолчанию, и трейс не даёт основания утверждать, что оно неверное. Детектор, помечающий B, будет генерировать ложные срабатывания на каждом вызове с неполными аргументами.'
  kind: choice
  options:
    - Только A
    - Только B
    - И A, и B
    - 'Ни один: детектор смотрит на ответ инструмента, а не на аргументы'
  correct: 0
  id: 022-context-violations-1
  fingerprint: ac522770
- concept: 'порядок работы детектора: сначала извлечение ограничений из трейса, потом сопоставление последующих действий по полям, потом запись пары ограничение-действие'
  question: Расставь шаги сигнатурного детектора противоречий контексту в порядке выполнения.
  explanation: Детектор работает не по одному событию, а по паре. Сначала нужно множество ограничений с их позициями, иначе не с чем сравнивать; затем перебор поздних действий; на выходе — не булев флаг, а привязка конкретного действия к конкретному нарушенному ограничению, иначе результат нельзя проверить вручную.
  kind: order
  items:
    - Извлечь из событий трейса заданные ограничения и факты (поле, требуемое значение, позиция в трейсе)
    - Для каждого действия, идущего позже ограничения, взять значения полей, упомянутых в ограничении
    - Сравнить значение поля с требуемым и при явном расхождении записать пару «ограничение — нарушившее действие»
  id: 022-context-violations-2
  fingerprint: ec3594fd
- concept: ограничение должно предшествовать действию в трейсе; действие раньше ограничения нарушением не считается
  question: Заполни пропуск в условии детектора так, чтобы нарушением считалось только действие, совершённое после того, как ограничение прозвучало в трейсе.
  explanation: 'Агента нельзя обвинять в нарушении правила, которого он ещё не получил. Действие с позицией меньше позиции ограничения — обычное исполнение по старым данным, а не противоречие. Без сравнения позиций детектор пометит весь трейс, как только ограничение появится где угодно, и потеряет главное свойство: локализацию отказа в позднем действии.'
  kind: cloze
  template: |-
    for constraint in constraints:
        for action in actions:
            if action["index"] ___ constraint["index"] and conflicts(action, constraint):
                violations.append((constraint, action))
  answer: '>'
  accept:
    - '>'
    - '> '
    - action["index"] > constraint["index"]
  id: 022-context-violations-3
  fingerprint: c4a6f2f8
