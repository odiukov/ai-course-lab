---
id: 036-check-lambda
type: check
title: 'Проверка: считаем 0.5 и 0.3 при λ = 0.6'
source_anchor: '### Step 4: differential attention'
check:
  - question: >-
      Обе карты дали токену 0 вес 0.5, коэффициент λ равен 0.6. Какой вес
      останется после вычитания?
    options:
      - '0.2'
      - '0.5 минус 0.6, то есть минус 0.1'
      - 0.3 — второй вес просто умножается на λ и становится итогом
      - 0.5 — вычитание сток не трогает
    correct: 0
    explanation: >-
      Вычитается не сам λ, а λ, умноженный на вес второй карты: 0.5 минус 0.6
      умножить на 0.5 равно 0.5 минус 0.3, то есть 0.2. Сток просел больше чем
      вдвое.
  - question: >-
      Первая карта дала токену 0.3, вторая — всего 0.05, λ по-прежнему 0.6.
      Почему этот токен почти не пострадал?
    options:
      - 'Потому что 0.3 меньше 0.5, а маленькие веса вычитание не задевает'
      - >-
        Потому что вторая карта дала мало, и вычитается только 0.6 умножить на
        0.05, то есть 0.03 — остаётся 0.27
      - Потому что λ применяется только к самому большому весу в строке
      - Потому что после вычитания строка нормируется обратно к единице
    correct: 1
    explanation: >-
      Размер вычета задаёт вторая карта: 0.6 умножить на 0.05 равно 0.03, и от
      0.3 остаётся 0.27. Схлопывается то, что обе карты оценили одинаково
      высоко.
---

После [шага 33](#step-33) и [шага 34](#step-34) ты должен уметь взять две строки весов и посчитать результат руками, не запуская код: берёшь вес из первой карты, вычитаешь из него вес из второй, умноженный на λ.

Спотыкаются обычно на двух вещах. Первая — вычитают сам λ, а не λ, умноженный на второй вес; тогда получаются отрицательные числа там, где их быть не должно. Вторая — забывают, что вычет зависит от второй карты: одинаковый вес в обеих картах срезается сильно, а вес, который вторая карта почти не заметила, остаётся почти целым. Ровно на этой разнице и держится весь приём.
