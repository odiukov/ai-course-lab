---
id: 010-check-granicy
type: check
title: 'Проверка: window=n и window=1'
source_anchor: '### Step 2: sliding window causal mask'
check:
  - question: 'Ты вызвал swa_mask(6, 1). Что получится в маске?'
    options:
      - Каждый токен видит себя и предыдущий токен
      - >-
        Каждый токен видит только себя: открыта одна диагональная клетка в
        каждой строке
      - 'Все клетки закрыты, потому что окно слишком узкое'
    correct: 1
    explanation: >-
      При window = 1 нижняя граница lo = max(0, i - 1 + 1) равна i. Цикл
      открывает единственную колонку j = i, поэтому остаётся только диагональ.
  - question: 'Что вернёт swa_mask(6, 20)?'
    options:
      - 'Ошибка: window не может превышать длину последовательности'
      - Обычная нижнетреугольная причинная маска
      - 'Полностью открытую маску, разрешающую смотреть на будущие токены'
    correct: 1
    explanation: >-
      Для каждой строки нижняя граница lo прижимается к 0, а верхней остаётся
      текущая позиция i. Поэтому доступны все прошлые позиции и текущая, но не
      будущие.
---

После [шага 9](#step-9) ты должен уметь прокрутить `swa_mask` в голове на крайних значениях `window`, не запуская код. Проверь два конца шкалы: очень широкое окно и окно шириной один.

Спотыкаются обычно на единице. Кажется, что `window = 1` — это «сам плюс один назад», и в строке должны открыться две клетки. Но `lo = max(0, i - window + 1)` при `window = 1` даёт ровно `i`, а цикл идёт от `lo` до `i` включительно — открыта одна клетка. Второе место — большое окно: тут нет никакой проверки на «слишком много», просто `max(0, ...)` прижимает границу к нулю, и ты получаешь обратно причинную маску.
