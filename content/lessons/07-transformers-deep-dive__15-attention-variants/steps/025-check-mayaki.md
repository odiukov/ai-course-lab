---
id: 025-check-mayaki
type: check
title: 'Проверка: сколько колонок откроет stride=8 при i=100'
source_anchor: '### Step 3: local + strided sparse mask'
check:
  - question: >-
      stride = 8, строка i = 100. Сколько колонок открывает второй цикл, который
      идёт по позициям 0, 8, 16 и так далее до i включительно?
    options:
      - 12 колонок
      - 13 колонок
      - 8 колонок
      - 100 колонок
    correct: 1
    explanation: >-
      Кратные восьми от 0 до 100: 0, 8, 16 ... 96. Это 96 делить на 8 плюс один,
      то есть 13 — единицу добавляет нулевая позиция, которую легко потерять при
      делении 100 на 8.
  - question: Почему в такой маске колонка 0 открыта у каждой строки без исключения?
    options:
      - >-
        Потому что нуль делится на любой stride, и цикл по маячкам всегда
        начинается с позиции 0
      - >-
        Потому что локальное окно всегда дотягивается до начала
        последовательности
      - Потому что первый токен по правилам всегда считается глобальным
      - Потому что причинная маска запрещает закрывать самую левую колонку
    correct: 0
    explanation: >-
      Перебор кратных stride стартует с нуля в любой строке, поэтому позиция 0
      попадает в список маячков всегда — это следствие устройства цикла, а не
      отдельного правила.
---

Ты уже написал `strided_mask` в [шаге 23](#step-23) и заметил залипшую нулевую колонку в [шаге 24](#step-24). Теперь проверь, умеешь ли ты считать цену маячков в конкретной строке, а не вообще.

Здесь спотыкаются на двух вещах. Первая — арифметика: делишь 100 на 8, получаешь 12 с хвостом и называешь 12, забыв, что счёт начинается с нулевой позиции, а не с восьмой. Вторая — путаница между «сколько маячков» и «докуда они дотягиваются»: последний маячок стоит на 96, а сама позиция 100 попадает в строку через локальное окно, а не через второй цикл.

Держи в голове: маячки — это редкая сетка до самого начала, а не продолжение окна.
