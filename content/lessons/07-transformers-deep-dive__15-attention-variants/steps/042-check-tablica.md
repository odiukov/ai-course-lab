---
id: 042-check-tablica
type: check
title: 'Проверка: кто платит дважды и за что'
source_anchor: '### Variant Comparison'
check:
  - question: >-
      Какой вариант attention увеличивает примерно вдвое сразу обе статьи
      затрат: вычисления и KV cache?
    options:
      - Sliding window attention
      - Differential attention
      - Local + strided sparse attention
      - Native Sparse Attention
    correct: 1
    explanation: >-
      Differential attention строит две независимые карты внимания и затем
      вычитает одну из другой. По сравнению с одной плотной ветвью это даёт
      примерно O(2·N²) вычислений и O(2N) KV cache.
  - question: >-
      Ты выбираешь между SWA с окном 1024 и local + strided sparse. Чем
      различается их KV cache?
    options:
      - У обоих кэш ограничивается размером локального окна
      - >-
        У SWA кэш ограничен окном, а у strided-варианта остаётся смешанным из-за
        обращений к дальним токенам
      - >-
        У strided-варианта кэш всегда меньше, поскольку его маска содержит
        меньше разрешённых связей
      - >-
        У обоих вариантов сохраняется полный KV cache, а экономятся только
        вычисления
    correct: 1
    explanation: >-
      SWA больше не обращается к токенам старше окна, поэтому их ключи и
      значения можно удалить. Strided-связи продолжают обращаться к части
      дальних позиций, так что соответствующие записи KV cache необходимо
      сохранять.
---

Таблица вариантов из [шага 41](#step-41) выглядит как меню, но читать её надо по двум колонкам сразу: сколько платишь за арифметику и сколько за KV cache. После этого куска ты должен уметь взять любую строку и сказать, где именно она экономит, а где нет.

Спотыкаются обычно на двух вещах. Первая: кажется, что если маска разреженная, то и кэш автоматически меньше — но у strided и BigBird дальние токены остаются нужны, и держать их приходится. Вторая: differential воспринимают как «улучшение качества бесплатно», хотя это единственная строка, где обе колонки удваиваются.

Бесплатных строк в таблице нет — вопрос только в том, чем ты готов заплатить.
