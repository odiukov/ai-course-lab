- concept: начальный шум и батч времён должны находиться на том же устройстве, что и состояние модели
  question: Модель ожидает отдельное значение времени для каждого элемента батча, а её параметры находятся на устройстве `device`. Как корректно создать начальное состояние и общий для батча момент `tau`?
  explanation: Шум создаётся непосредственно на устройстве модели, а время оформляется как батч значений на устройстве текущего состояния. Это предотвращает конфликт устройств и даёт модели по одному значению времени на каждый образец.
  kind: choice
  options:
    - |-
      `x = torch.randn(sample_shape, device=device)
      t = torch.full((len(x),), tau, device=x.device)`
    - |-
      `x = torch.randn(sample_shape)
      t = torch.full((len(x),), tau, device=device)`
    - |-
      `x = torch.randn(sample_shape, device=device)
      t = torch.full((len(x),), tau)`
    - |-
      `x = torch.randn(sample_shape, device=device)
      t = torch.tensor(tau, device=x.device)`
  correct: 0
  id: 015-euler-sample-1
  fingerprint: 6b5c68c6
- concept: отключение autograd при сэмплировании предотвращает построение ненужного графа вычислений
  question: Заполни контекстный менеджер PyTorch для генерации без вычисления градиентов.
  explanation: Во время сэмплирования параметры модели не обновляются, поэтому граф для обратного прохода не нужен. `no_grad` уменьшает расход памяти; `inference_mode` также подходит для чистого инференса.
  kind: cloze
  template: |-
    with torch.___():
        samples = euler_sample(model, sample_shape)
  answer: no_grad
  accept:
    - inference_mode
  id: 015-euler-sample-2
  fingerprint: f6d17cdb
