---
id: 013-check-request
type: check
title: 'Проверка: чьё в запросе какое поле'
source_anchor: '### `sampling/createMessage` request'
check:
  - question: >-
      В запросе sampling/createMessage лежат messages, systemPrompt, maxTokens и
      modelPreferences. Кто формирует эти поля?
    options:
      - 'Клиент, потому что он предоставляет модель'
      - 'Сервер, который собирает промпт, задаёт лимит и описывает предпочтения'
      - Пользователь вручную перед каждым запросом
      - 'messages формирует сервер, а остальные поля всегда добавляет клиент'
    correct: 1
    explanation: >-
      Сервер формирует весь объект params и тем самым описывает задачу для
      модели. Клиент принимает запрос и исполняет его в пределах своих
      возможностей и настроек.
  - question: Что означают поля model и stopReason в результате sampling/createMessage?
    options:
      - >-
        Модель, которую сервер потребовал использовать, и установленный им
        способ остановки
      - Фактически выбранную клиентом модель и причину завершения генерации
      - Копию modelPreferences из запроса
      - Версию протокола и состояние соединения
    correct: 1
    explanation: >-
      Клиент может выбрать модель, отличную от указанной в hints. Поэтому в
      результате он сообщает фактическую модель и причину остановки, например
      endTurn.
---

После [шагов 7](#step-7)–[12](#step-12) ты должен уметь взять любой кусок этого JSON и не задумываясь сказать, чья это часть: сервер собирает `params` целиком — текст, системную инструкцию, лимит, веса и подсказки; клиент возвращает `content` плюс два честных факта о том, как всё прошло.

Спотыкаются обычно на двух вещах. Первая: `id: 42` кажется клиентским просто потому, что «id всегда у того, кто запускает диалог» — но запустил-то здесь сервер. Вторая: `modelPreferences` читают как приказ и ждут, что в ответе придёт ровно та модель, что в `hints`. Не придёт. Поэтому поле `model` в ответе и существует.
