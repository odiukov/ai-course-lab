- concept: тип элементов exclude должен совпадать с индексами строк матрицы embeddings
  question: Функция перебирает результат np.argsort по массиву сходств и проверяет условие `i in exclude`. Что должно храниться в `exclude`?
  explanation: '`np.argsort` возвращает индексы элементов массива сходств, совпадающие с индексами строк `W`. Поэтому `exclude` должен содержать такие же индексы; строки со словами при сравнении с `i` не сработают.'
  kind: choice
  options:
    - Слова из словаря
    - Пары из слова и его сходства
    - Индексы строк матрицы embeddings
    - Векторы исключаемых слов
  correct: 2
  id: 030-nearest-1
  fingerprint: ce6ff8c4
- concept: исключённые кандидаты отбрасываются до заполнения результата top-k
  question: Заполни пропуск так, чтобы запрещённый кандидат не попал в результат и не занял место в квоте `topk`.
  explanation: '`continue` сразу переходит к следующему кандидату. Добавление в `out` и проверка заполнения `topk` выполняются только для разрешённых индексов, поэтому исключённые слова не сокращают итоговую выдачу.'
  kind: cloze
  template: |-
    for i in order:
        if i in exclude:
            ___
        out.append((inv_vocab[i], float(sims[i])))
  answer: continue
  accept:
    - continue
  id: 030-nearest-2
  fingerprint: 7f693885
