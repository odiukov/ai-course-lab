- concept: 'направление «лучше» зависит от метрики: задержка и стоимость минимизируются, точность и отлаживаемость максимизируются'
  question: Функция ищет лучший профиль по задержке (меньше — лучше) одним проходом. Какой оператор сравнения нужен в условии обновления лидера?
  explanation: 'Один компаратор на все метрики применять нельзя: для задержки и стоимости лидер обновляется при строго меньшем значении, для точности и отлаживаемости — при строго большем. Перепутанное направление даёт «победителя» с худшим значением.'
  kind: cloze
  template: |-
    for p in profiles:
        if p.latency_ms ___ best["latency"]["value"]:
            best["latency"] = {"pattern": p.name, "value": p.latency_ms}
  answer: <
  accept:
    - <
  id: 027-code-compare-patterns-1
  fingerprint: a8feb7ee
- concept: строгое сравнение при обновлении лидера делает разрешение ничьей детерминированным в пользу первого встреченного профиля
  question: В списке профилей две топологии имеют одинаковую задержку 640 мс. Что изменится, если в условии обновления лидера заменить строгое сравнение на нестрогое (`<` на `<=`)?
  explanation: Нестрогое сравнение перезаписывает лидера при равенстве, поэтому побеждает последний равный. Строгое сравнение фиксирует первого встреченного — правило предсказуемое и не зависящее от порядка сортировки, которой здесь нет.
  kind: choice
  options:
    - 'Ничего: при равных значениях результат один и тот же'
    - При ничьей победителем станет последний из равных профилей, а не первый
    - Функция начнёт возвращать оба профиля как победителей по этой метрике
  correct: 1
  id: 027-code-compare-patterns-2
  fingerprint: c152cba0
- concept: сравнение возвращает победителей по каждой метрике отдельно, а приоритеты применяет вызывающий код
  question: Почему функция сравнения топологий возвращает победителя по каждой метрике отдельно, а не одну «лучшую» топологию?
  explanation: 'Свёртка в один балл возможна, но требует весов, которых у функции сравнения нет. Разные победители по разным метрикам — нормальный результат, а не ошибка: приоритет задаёт тот, кто знает ограничения потока.'
  kind: choice
  options:
    - Потому что свести четыре метрики в одну оценку невозможно технически
    - 'Потому что веса метрик определяются ограничениями конкретного потока: вызывающий код сам отсеет варианты, не проходящие SLA, и выберет из оставшихся самый точный'
    - Потому что порядок профилей в списке заранее неизвестен
  correct: 1
  id: 027-code-compare-patterns-3
  fingerprint: bd77dd78
