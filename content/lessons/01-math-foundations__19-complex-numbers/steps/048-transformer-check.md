---
id: 048-transformer-check
type: check
title: Сравнить роль sin/cos в обычной кодировке и RoPE
source_anchor: '### Connection to transformers'
check:
  - question: Как точнее всего различается роль sin и cos в двух позиционных кодировках?
    options:
      - >-
        В синусоидальной кодировке они образуют числовое представление позиции,
        а в RoPE задают поворот пар координат query и key
      - >-
        В синусоидальной кодировке используется только sin, а в RoPE — только
        cos
      - >-
        В синусоидальной кодировке они преобразуют query, а в RoPE — только
        значения value
    correct: 0
    explanation: >-
      Синусоидальная кодировка передаёт модели готовый частотный отпечаток
      позиции. В RoPE значения sin и cos служат коэффициентами поворота query и
      key.
  - question: >-
      В какой кодировке позиционная информация влияет на внимание через
      сравнение предварительно повёрнутых query и key?
    options:
      - В RoPE
      - В синусоидальной позиционной кодировке
      - 'Ни в одной: позиция изменяет только value'
    correct: 0
    explanation: >-
      RoPE применяет зависящие от позиции повороты к query и key, после чего
      внимание вычисляется по преобразованным векторам. В синусоидальной
      кодировке позиция передаётся как готовый набор значений sin и cos.
---

Сравни две роли знакомой пары sin/cos. В синусоидальной кодировке из [шага 46](#step-46) её значения становятся частью числового «адреса» позиции: модель получает готовый частотный отпечаток места токена. В RoPE из [шага 47](#step-47) sin и cos работают не как отдельная подпись, а как коэффициенты поворота пар координат query и key.

Частая ловушка — считать RoPE просто другим способом приписать токену числа. Его ключевой эффект проявляется при сравнении повёрнутых векторов: общий сдвиг поворачивает оба одинаково, а угол между ними сохраняет именно расстояние позиций. Значит, инструменты те же, но работа разная: записать позицию или встроить её разницу во внимание.
