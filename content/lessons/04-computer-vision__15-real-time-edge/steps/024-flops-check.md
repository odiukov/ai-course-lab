---
id: 024-flops-check
type: check
title: 'Проверка: параметры, FLOPs и реальное время — разные величины'
source_anchor: '### FLOPs as a proxy'
check:
  - question: >-
      У модели A меньше параметров и FLOPs, чем у модели B; веса обеих моделей
      хранятся в одном числовом формате. Что можно уверенно заключить до запуска
      на целевом устройстве?
    options:
      - Модель A обязательно быстрее и использует меньше пиковой памяти
      - 'Модель A обязательно быстрее, хотя её пиковая память неизвестна'
      - >-
        У модели A меньше объём весов и вычислительная оценка, но latency и
        пиковую память нужно измерить
    correct: 2
    explanation: >-
      При одинаковом числовом формате меньшее число параметров означает меньший
      объём весов, а FLOPs оценивают вычислительную работу. Ни то, ни другое не
      является временем: latency зависит от того, насколько операции модели
      дружественны железу и его памяти. Пиковая память тоже не выводится из
      весов — её задают ещё и промежуточные активации.
  - question: >-
      Замер на целевом устройстве показал: кандидат с большим числом FLOPs
      работает заметно быстрее второго кандидата. Как поступить при выборе
      модели для деплоя?
    options:
      - >-
        Предпочесть более быструю модель, если корректный benchmark стабильно
        подтверждает её преимущество
      - >-
        Отвергнуть замеры как ошибочные: модель с большими FLOPs не может быть
        быстрее
      - 'Выбирать по числу параметров, поскольку оно точнее определяет скорость'
    correct: 0
    explanation: >-
      FLOPs — аппаратно-независимая оценка числа операций, а не времени их
      выполнения. Разные операции по-разному ложатся на железо, поэтому
      небольшой перевес по FLOPs спокойно сочетается с кратным выигрышем по
      latency. Решает замер на целевом устройстве; число параметров тем более
      скоростью не является.
---

Ты сравниваешь две модели и видишь три числа: параметры, FLOPs и миллисекунды. Не подменяй одно другим. Параметры помогают оценить объём весов, как в [шаге 16](#step-16); FLOPs — вычислительную работу, как в [шаге 17](#step-17). Но ответ «успевает ли модель вовремя» даёт только benchmark в условиях из [шага 7](#step-7). Меньше по первым двум метрикам не означает автоматически быстрее на часах.

> 🎒 **На пальцах.** Представь театральную сцену. Число декораций показывает, сколько места нужно за кулисами. Число действий в сценарии — сколько работы предстоит выполнить. Но длительность сцены зависит ещё и от механики сцены и слаженности команды. Узнать реальное время можно только на репетиции именно в этом театре.
