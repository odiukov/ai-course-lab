---
id: 002-contract-check
type: check
title: Проверяем контракт синхронизации
source_anchor: '### The three operations DDP needs'
check:
  - question: >-
      Два ранга должны выполнить один шаг DDP. Какой порядок действий
      соответствует контракту синхронизации?
    options:
      - >-
        Сначала согласовать параметры, после backward объединить градиенты, а
        при необходимости рассылать буферы
      - >-
        Сначала объединить градиенты, затем согласовать параметры, а буферы
        оставить локальными
      - >-
        Согласовать только параметры перед стартом: дальше ранги полностью
        независимы
    correct: 0
    explanation: >-
      Ранги должны одинаково стартовать, согласовывать градиенты после backward
      и отдельно синхронизировать буферы, если модель их изменяет.
  - question: >-
      Обучение завершилось без ошибок, но одна и та же модель на разных рангах
      выдаёт разные ответы при инференсе. Градиенты были синхронизированы. Что
      стоит проверить первым?
    options:
      - Одинаков ли размер локального батча
      - >-
        Синхронизировались ли буферы модели, например скользящие статистики
        батчнорма
      - Вызывался ли backward дважды на каждом ранге
    correct: 1
    explanation: >-
      Буферы не имеют градиентов, поэтому их не затрагивает объединение
      градиентов. Несогласованные статистики могут дать разные ответы на
      инференсе.
---

После шва из [шага 1](#step-1) ты должен уметь проверить не отдельный вызов, а весь контракт: одинаковый старт, согласование градиентов после backward и отдельное внимание к буферам. Чаще всего спотыкаются именно на последнем пункте: успешное обучение ещё не доказывает, что всё состояние модели совпадает.

> 🎒 **На пальцах.** Представь два оркестра, играющих одну запись. Одинаковые ноты — это параметры, сверка исполнения после репетиции — синхронизация градиентов. Но у каждого дирижёра ещё есть пометки о темпе — буферы. Если их не разослать, репетиции пройдут гладко, а финальные записи зазвучат по-разному.
