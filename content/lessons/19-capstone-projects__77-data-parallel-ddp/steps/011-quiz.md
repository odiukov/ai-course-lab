---
id: 011-quiz
type: quiz
title: Закрепляем контракт DDP
source_anchor: '## Production patterns in the wild'
check:
  - question: >-
      Два ранга начинают обучение с разными параметрами, но затем усредняют
      градиенты. Что произойдёт?
    options:
      - Параметры автоматически сравняются после первого шага оптимизатора
      - >-
        Ранги могут остаться разными, потому что усреднение градиентов не
        синхронизирует начальные параметры
      - Обучение станет эквивалентно одному процессу с удвоенной скоростью
    correct: 1
    explanation: >-
      Общий градиент задаёт одинаковую поправку, но не устраняет уже
      существующее различие параметров. Поэтому начальное состояние нужно
      синхронизировать отдельно.
  - question: >-
      На двух рангах градиенты равны 2 и 6. Allreduce сложил их. Какое значение
      должно попасть в оптимизатор каждого ранга?
    options:
      - '8'
      - '6'
      - '4'
      - '2'
    correct: 2
    explanation: >-
      Сеть возвращает сумму 8, после чего её делят на число рангов. Каждый
      оптимизатор получает средний градиент 4.
  - question: >-
      Как организовать данные и коллективные операции, чтобы ранги вместе
      изображали один общий батч?
    options:
      - >-
        Каждому рангу дать свой непересекающийся шард и сохранить одинаковый
        порядок коллективных операций
      - >-
        Каждому рангу дать весь датасет и разрешить пропускать ненужные
        коллективные операции
      - >-
        Раздать шарды случайно на каждом шаге, даже если один пример попадёт
        нескольким рангам
    correct: 0
    explanation: >-
      Непересекающиеся шарды собираются в общий батч без дублирования, а
      одинаковая последовательность коллективных операций не даёт рангам ждать
      разные обмены.
  - question: >-
      Ты накапливаешь градиенты по восьми микробатчам. Как избежать восьми
      сетевых обменов перед одним шагом оптимизатора?
    options:
      - Отключить синхронизацию на всех восьми микробатчах
      - >-
        Синхронизировать каждый микробатч, но выполнять оптимизатор только после
        восьмого
      - >-
        Использовать no_sync для первых семи микробатчей и синхронизировать
        восьмой
    correct: 2
    explanation: >-
      Первые семь обратных проходов только накапливают локальные градиенты.
      Последний запускает единственный обмен перед шагом оптимизатора.
---

Дальше тебе пригодится не набор флагов, а проверяемый контракт: одинаковый старт из [шага 3](#step-3), средние градиенты из [шагов 4](#step-4)–[5](#step-5), отдельный шард на ранг и согласованный порядок обменов из [шагов 6](#step-6)–[7](#step-7). Если распределённый прогон вызывает сомнения, возвращайся к независимому эталону из [шагов 8](#step-8)–[10](#step-10).

Чаще всего ошибку маскирует то, что программа не падает: сумма вместо среднего просто меняет масштаб шага, повторённые данные искажают батч, а лишняя синхронизация лишь замедляет обучение. Поэтому совпадение с эталоном важнее ощущения, что «всё запустилось».
