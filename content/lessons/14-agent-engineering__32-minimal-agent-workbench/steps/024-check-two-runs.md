---
id: 024-check-two-runs
type: check
title: 'Проверка: чем должны различаться первый и второй запуск?'
source_anchor: '## Build It'
check:
  - question: >-
      После первого запуска в agent_state.json уже записана текущая задача. Что
      должен сделать второй запуск?
    options:
      - Снова выбрать первую задачу со статусом todo
      - Удалить workdir и заново создать три файла
      - Прочитать сохранённое состояние и продолжить текущую задачу
      - 'Остановиться, потому что состояние больше не пустое'
    correct: 2
    explanation: >-
      Сохранённая текущая задача связывает запуски: второй запуск продолжает её,
      а не повторяет начальный выбор.
  - question: >-
      Какое различие между diff первого и второго запуска подтверждает
      правильную работу?
    options:
      - >-
        Второй diff отражает следующий ход сохранённой работы, а не повтор
        первого
      - Оба diff должны полностью совпадать
      - Во втором diff обязательно меняется только task_board.json
      - Второй запуск вообще не должен печатать diff
    correct: 0
    explanation: >-
      Каждый запуск делает один ход, поэтому второй diff должен показывать
      продолжение из записанного состояния.
---

Здесь легко спутать повторяемый запуск с перезапуском с нуля. После первого хода в `agent_state.json` уже есть след работы, описанный в [шаге 20](#step-20). Поэтому во втором запуске важен не конкретный текст diff, а причинная связь: скрипт сначала читает сохранённое состояние и продолжает текущую работу. Если он снова выбирает исходную задачу как новую, связь между сессиями потеряна.

> 🎒 **На пальцах.** Представь раскраску по номерам, которую ты оставил вечером на столе вместе с закладкой. Утром ты открываешь её на отмеченном месте и закрашиваешь следующую область. Если же каждый раз начинать с участка номер один, движения будут похожи на работу, но картина никогда не продвинется дальше первого шага.
