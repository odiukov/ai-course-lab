- concept: отсутствие подходящей задачи — штатный пустой результат функции выбора, а не ошибка
  question: Функция выбора обошла всю доску и не нашла ни одной задачи, подходящей исполнителю. Заполни пропуск так, чтобы вызывающий мог отличить «кандидата нет» от «кандидат найден» без обработки исключения.
  explanation: Пустая очередь — нормальное состояние работы, а не сбой. Пустое значение вызывающий проверяет одним условием и спокойно завершает ход; исключение заставляло бы оборачивать каждый вызов в обработчик ради обычной ситуации.
  kind: cloze
  template: |-
    def next_task(board, role):
        for task in board:
            if task["status"] == "todo" and task["owner"] == role:
                return task
        return ___
  answer: None
  accept:
    - None
    - 'null'
    - nil
    - undefined
  id: 014-code-next-task-1
  fingerprint: 67d53e8c
- concept: результат выбора определяется парой «доска + роль исполнителя», а не одной доской
  question: Доска не менялась. Функция выбора вызвана сначала для роли builder, затем для роли reviewer и вернула разные задачи. Что это означает?
  explanation: Выбор — чистая функция от двух аргументов. Зашитая внутрь роль сделала бы функцию пригодной только для одного исполнителя, и для второй роли пришлось бы копировать код с теми же правилами.
  kind: choice
  options:
    - 'Нормальное поведение: роль — такой же вход функции, как доска, и при другой роли множество допустимых задач другое'
    - 'Ошибку: при неизменной доске функция обязана возвращать одну и ту же задачу'
    - 'Ошибку: функция где-то поменяла порядок задач между вызовами'
    - Нормальное поведение только если доска содержит задачи со статусом blocked
  correct: 0
  id: 014-code-next-task-2
  fingerprint: 96ec0174
- concept: функция выбора не меняет доску, поэтому повторный вызов на неизменной доске даёт тот же ответ
  question: Разработчик добавил внутрь функции выбора перевод найденной задачи в рабочий статус. Что ломается в первую очередь?
  explanation: 'Скрытый побочный эффект в запросе убивает возможность просто спросить доску: любой диагностический или пробный вызов молча захватывает работу. Изменение статуса — отдельная операция захвата.'
  kind: choice
  options:
    - 'Вопрос «что можно взять следующим?» перестаёт быть безопасным: повторный вызов на той же доске вернёт уже другую задачу, а планирование и предпросмотр становятся невозможны'
    - Функция начинает возвращать задачи в обратном порядке
    - Пустой результат перестаёт отличаться от найденной задачи
    - Задачи с чужим владельцем начинают проходить фильтр
  correct: 0
  id: 014-code-next-task-3
  fingerprint: '99605178'
