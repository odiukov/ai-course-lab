- concept: размер модели при заданном бюджете и отношении токенов к параметрам находится обращением зависимости C = 6rN²
  question: Заполните вычисление числа параметров. Переменная FLOPS_PER_PARAM_TOKEN хранит коэффициент из оценки стоимости обучения, а ratio — желаемое число токенов на параметр.
  explanation: При D = rN оценка C = 6ND превращается в C = 6rN². Поэтому N = √(C/(6r)), после чего число токенов вычисляется как D = rN.
  kind: cloze
  template: |-
    def tokens_for_budget(compute, ratio):
        params = ___
        tokens = ratio * params
        return params, tokens
  answer: math.sqrt(compute / (FLOPS_PER_PARAM_TOKEN * ratio))
  accept:
    - (compute / (FLOPS_PER_PARAM_TOKEN * ratio)) ** 0.5
    - pow(compute / (FLOPS_PER_PARAM_TOKEN * ratio), 0.5)
  id: 005-tokens-for-budget-1
  fingerprint: f4a99499
