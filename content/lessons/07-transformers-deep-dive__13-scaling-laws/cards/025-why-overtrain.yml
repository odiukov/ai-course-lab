- concept: число обучающих токенов не определяет стоимость инференса после фиксации размера модели
  question: Две модели имеют одинаковое число параметров и одинаковую архитектуру, но одну обучали на гораздо большем датасете. Как это само по себе повлияет на вычислительную стоимость генерации одного токена?
  explanation: После обучения датасет не участвует в обычном прямом проходе. При одинаковых размере и архитектуре модели вычислительная стоимость одного сгенерированного токена примерно одинакова, даже если объёмы обучения сильно различались.
  kind: choice
  options:
    - 'Практически не повлияет: при инференсе используются веса модели, а не обучающий датасет'
    - Увеличит стоимость пропорционально размеру обучающего датасета
    - Уменьшит стоимость, потому что дольше обученная модель выполняет меньше операций
    - Заставит загружать часть обучающего датасета при генерации каждого токена
  correct: 0
  id: 025-why-overtrain-1
  fingerprint: a0ffb7e8
- concept: объём памяти для весов равен числу параметров, умноженному на число байтов на параметр
  question: Модель содержит 13 млрд параметров, а каждый вес хранится в формате fp16. Сколько примерно гигабайт занимают только веса модели?
  explanation: В fp16 один параметр занимает 2 байта. Поэтому веса требуют примерно 13 млрд × 2 байта = 26 ГБ. Служебные буферы и KV-кэш потребуют дополнительной памяти.
  kind: numeric
  answer: 26
  tolerance: 0.5
  id: 025-why-overtrain-2
  fingerprint: cd620a6c
