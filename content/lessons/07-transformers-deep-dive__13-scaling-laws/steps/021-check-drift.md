---
id: 021-check-drift
type: check
title: 'Проверка: солвер печатает ровно 20 — что сломано'
source_anchor: '### Step 2: compute-optimal frontier'
check:
  - question: >-
      Ты запустил перебор по бюджетам от 1e18 до 1e25, и колонка отношения
      токенов к параметрам во всех строках показывает ровно 20. Какой вывод
      наиболее обоснован?
    options:
      - Перебор подтвердил универсальное правило для любого compute-бюджета
      - >-
        Солвер, вероятно, не минимизирует loss, а использует зафиксированное
        отношение
      - Закон Хоффмана перестаёт учитывать размер датасета на больших бюджетах
      - >-
        Compute-ограничение автоматически округляет любое отношение до одного
        значения
    correct: 1
    explanation: >-
      Настоящая минимизация должна давать меняющееся D/N: примерно от 25.7 при
      1e18 FLOPs до 121.9 при 1e25 FLOPs. Ровная колонка двадцаток указывает на
      жёстко заданную эвристику или другой дефект солвера.
  - question: >-
      Почему рост compute-оптимального отношения D/N при увеличении бюджета
      совместим с законом Хоффмана?
    options:
      - >-
        Оптимальный объём данных растёт с бюджетом быстрее оптимального размера
        модели, поэтому их отношение увеличивается
      - Коэффициент 6 в ограничении 6ND = C сам растёт вместе с бюджетом
      - Неустранимый вклад E превращается в дополнительные обучающие токены
      - При больших бюджетах численная погрешность неизбежно завышает D
    correct: 0
    explanation: >-
      Оптимальные D и N имеют разные показатели масштабирования: около 0.548 и
      0.452. Поэтому отношение D/N растёт как C^0.097, что и создаёт наблюдаемый
      дрейф примерно от 26 до 122.
---

Дальше в лабораторной ты сам прогонишь бюджеты от `1e18` до `1e25` и распечатаешь таблицу. И вот тут срабатывает ловушка: правило «20 токенов на параметр» звучит так уверенно, что ровная колонка двадцаток выглядит как успех, а не как поломка. На самом деле всё наоборот — это первый признак, что минимизация не запустилась и число подставлено вручную.

После этого куска ты должен уметь читать собственный вывод критически: сверять его не с запомненным правилом, а с тем, что предсказывает [шаг 19](#step-19). Спотыкаются обычно двое: тот, кто увидел 122 и полез искать баг, и тот, кто увидел 20 и обрадовался. Правильная реакция — ровно противоположная в обоих случаях.
