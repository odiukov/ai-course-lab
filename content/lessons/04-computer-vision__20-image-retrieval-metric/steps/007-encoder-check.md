---
id: 007-encoder-check
type: check
title: 'Проверка: выбрать энкодер для текста, категории и конкретного экземпляра'
source_anchor: '## Use It'
check:
  - question: >-
      Ты проектируешь три поиска: по текстовому описанию, по визуальной
      категории и по конкретному экземпляру товара. Какой набор энкодеров
      подходит лучше всего?
    options:
      - 'CLIP для текста, DINOv2 для категории, дообученный DINOv2 для экземпляра'
      - 'DINOv2 для текста, CLIP для категории, обычный DINOv2 для экземпляра'
      - Один обычный DINOv2 для всех трёх задач
    correct: 0
    explanation: >-
      Текст и изображения умеет сопоставлять CLIP. Для визуальной похожести
      подходит DINOv2, а различать конкретные экземпляры он начинает после
      дообучения на размеченных парах.
  - question: >-
      Поиск должен находить именно ту же модель кроссовок, но обычный DINOv2
      возвращает просто похожую обувь. Что разумнее изменить?
    options:
      - Заменить FAISS на другой индекс
      - Дообучить DINOv2 на примерах с разметкой по экземплярам
      - Добавить в каталог больше случайных изображений
    correct: 1
    explanation: >-
      Индекс ускоряет поиск, но не учит различать экземпляры. Для этого энкодер
      дообучают так, чтобы снимки одного предмета сближались, а разных —
      расходились.
---

Здесь ты проверяешь главный выбор из [шага 6](#step-6): модель определяется не модным названием, а видом запроса и требуемой точностью совпадения. Текстовое описание ведёт к CLIP, визуальный поиск по категории — к DINOv2, а поиск конкретной вещи — к DINOv2, дообученному на размеченных экземплярах.

Частая ловушка — пытаться исправить плохую выдачу заменой FAISS или HNSW. Но индекс лишь быстро перебирает уже созданные эмбеддинги. Если энкодер считает две разные футболки почти одинаковыми, более быстрый индекс не научит его видеть различие. Сначала выбираешь представление смысла, потом — способ быстро искать по нему.
