---
id: 014-metric-equivalence
type: check
title: 'Проверка: когда cosine и L2 дают одинаковый порядок соседей'
source_anchor: '### Cosine similarity vs L2'
check:
  - question: >-
      Запрос и все кандидаты L2-нормализованы. У кандидата A cosine равен 0,9, а
      у кандидата B — 0,8. Кто окажется ближе по евклидову расстоянию?
    options:
      - Кандидат A
      - Кандидат B
      - Оба окажутся на одинаковом расстоянии
    correct: 0
    explanation: >-
      Для единичных векторов квадрат расстояния равен 2 − 2 cos(a, b). Поэтому
      большему cosine соответствует меньшее L2-расстояние: обе метрики поставят
      A выше B.
  - question: >-
      Ты получил одинаковый порядок соседей по cosine и L2. Можно ли
      использовать порог cosine 0,9 как порог L2, равный 0,9?
    options:
      - 'Да, потому что одинаковое ранжирование означает одинаковую шкалу'
      - 'Да, если все эмбеддинги L2-нормализованы'
      - 'Нет, одинаковым будет порядок, но не значения метрик'
    correct: 2
    explanation: >-
      На нормализованных эмбеддингах cosine 0,9 соответствует квадрату
      L2-расстояния 0,2 и L2-расстоянию примерно 0,45. Порог нужно преобразовать
      или откалибровать заново.
---

Здесь важно отличить одинаковое ранжирование от одинаковых значений. Если запрос и кандидаты прошли L2-нормализацию из [шага 8](#step-8), рост cosine всегда означает уменьшение L2-расстояния. Поэтому сортировка даст один порядок: самый большой cosine соответствует самому маленькому расстоянию. Корень в формуле из [шага 12](#step-12) порядок тоже не меняет.

Обычно спотыкаются в двух местах: забывают проверить нормализацию или переносят числовой порог между метриками. На сырых эмбеддингах длины векторов вмешиваются в L2, а одинаковый порядок соседей больше не гарантирован. Даже после нормализации совпадают места в выдаче, но не сами числа.
