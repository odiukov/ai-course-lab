---
id: 007-check-modalnost
type: check
title: 'Проверка: вход и выход из разных миров'
source_anchor: '## The Problem'
check:
  - question: В какой задаче отдельный энкодер обычно даёт меньше всего пользы?
    options:
      - 'Распознавание речи: спектрограмма преобразуется в текст'
      - Чат-бот продолжает текстовый диалог следующей репликой
      - Перевод текста с английского на французский
      - Из свободного текста извлекается JSON с заданными полями
    correct: 1
    explanation: >-
      В обычном текстовом чате вход и выход принадлежат одной текстовой
      последовательности, поэтому decoder-only естественно продолжает контекст.
      В остальных задачах источник и результат различаются по представлению,
      языку или структуре, что делает отдельный энкодер полезнее.
  - question: >-
      Коллега утверждает: «Decoder-only модели полностью вытеснили
      encoder-decoder». Какой ответ точнее всего?
    options:
      - 'Верно: encoder-decoder больше не используют в прикладных системах'
      - >-
        Неверно: decoder-only модели почти не применяют за пределами
        исследований
      - >-
        Неверно: encoder-decoder сохранился в распознавании речи, переводе и
        структурированных преобразованиях
      - >-
        Верно: encoder-decoder остался только способом объяснять устройство
        Transformer
    correct: 2
    explanation: >-
      Decoder-only модели получили больше внимания, но encoder-decoder
      продолжает использоваться там, где источник нужно отдельно представить
      перед генерацией результата. К таким областям относятся распознавание
      речи, машинный перевод и некоторые задачи структурированного
      преобразования.
---

После этого куска ты должен уметь смотреть на новую задачу и быстро прикидывать: вход и выход у неё из одного мира или из разных. Это и есть рабочий критерий, по которому encoder-decoder либо оправдан, либо оказывается лишней половиной.

Спотыкаются обычно на двух вещах. Первая — путают «разные миры» с «длинным входом»: длина сама по себе ничего не решает, решает то, что источник и результат устроены по-разному. Вторая — слышат «decoder-only победил» и делают вывод, что encoder-decoder исчез; из [шага 6](#step-6) видно, что он просто ушёл в свою нишу и там прекрасно себя чувствует.
