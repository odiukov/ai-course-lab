---
id: 038-check-vybor
type: check
title: 'Проверка: чат и распознавание речи — какая архитектура'
source_anchor: '### When to pick each variant in 2026'
check:
  - question: >-
      Ты проектируешь чат-ассистента, которому нужно продолжать историю из
      реплик пользователя и модели. Какая архитектура здесь естественнее?
    options:
      - >-
        Encoder-decoder, потому что каждую реплику пользователя нужно кодировать
        отдельно
      - >-
        Decoder-only, потому что вся история диалога и новый ответ образуют одну
        авторегрессивно растущую последовательность
      - 'Encoder-decoder, потому что энкодер обязательно сжимает длинную историю'
      - 'Decoder-only, потому что encoder-decoder не способен обрабатывать текст'
    correct: 1
    explanation: >-
      В чате история реплик и генерируемый ответ представляются как единая
      последовательность токенов. Поэтому отдельный неподвижный источник,
      который требовал бы собственного энкодера, обычно не выделяют.
  - question: >-
      Почему в системе распознавания речи вроде Whisper уместна архитектура
      encoder-decoder?
    options:
      - 'Энкодер нужен только потому, что аудиозапись обычно длиннее расшифровки'
      - >-
        Энкодер преобразует аудио-признаки в представления, на которые текстовый
        декодер может опираться при генерации токенов
      - Декодер не способен генерировать текст авторегрессивно
      - Без энкодера невозможно применять beam search
    correct: 1
    explanation: >-
      На вход поступают аудио-признаки, а на выходе требуются текстовые токены.
      Энкодер обрабатывает вход другой модальности и формирует представления
      источника для декодера; длина записи и beam search сами по себе не
      определяют необходимость энкодера.
---

Посмотри на таблицу из [шага 36](#step-36) ещё раз, но не читай её построчно — читай столбец «да/нет» и спрашивай себя, что общего у строк с «да». Перевод, речь-в-текст: «да». Чат, дописывание кода: «нет».

Первый рефлекс — сказать «да там, где задача сложнее». Но перевод не сложнее диалога, скорее наоборот. Работает другой признак: пришёл ли вход извне и лежит ли он неподвижно, пока модель говорит. Аудиофайл не меняется, пока Whisper печатает расшифровку. А в чате твой вопрос и ответ модели — одна лента, которая растёт с обеих сторон; резать её на «вход» и «выход» некуда.

На этой развилке спотыкаются двумя способами. Первый — тянуть энкодер туда, где входа как отдельной сущности нет, просто потому что «вопрос ведь есть». Второй — забывать про модальность: в речи дело не в длине аудио и не в сложности, а в том, что декодер работает с токенами текста, и кто-то должен привести аудио-признаки к тому виду, откуда декодер возьмёт K и V ([шаг 9](#step-9)).

Два вопроса ниже — ровно про эту развилку.
