- concept: совпадающие уникальные sentinel-токены связывают удалённые спаны с соответствующими пропусками
  question: При span corruption во входе каждый удалённый фрагмент заменён уникальным sentinel-токеном, а в цели тот же токен стоит перед восстановленным фрагментом. Какую информацию кодирует такое совпадение меток?
  explanation: 'Одинаковая уникальная метка во входе и цели задаёт однозначное соответствие: модель видит, содержимое какого пропуска следует после неё. Следующая метка также отделяет один восстановленный спан от другого.'
  kind: open
  reference: Каждый sentinel-токен связывает конкретный пропуск во входе с соответствующим удалённым спаном в цели. Одновременно метки обозначают границы между восстанавливаемыми спанами.
  id: 021-sentinel-format-1
  fingerprint: bb60077d
- concept: цель T5 при span corruption содержит только удалённые спаны с sentinel-префиксами
  question: Как формируется целевая последовательность при предобучении T5 методом span corruption?
  explanation: Декодер не восстанавливает полную последовательность. Цель состоит только из выброшенных спанов, причём перед каждым стоит совпадающий с пропуском во входе sentinel-токен.
  kind: choice
  options:
    - В неё копируется весь исходный текст без изменений
    - В неё копируется испорченный вход вместе с оставшимися словами
    - В неё записываются только удалённые спаны без каких-либо меток
    - В неё записываются только sentinel-токены без удалённого содержимого
    - В неё записываются удалённые спаны, каждый после соответствующего sentinel-токена
  correct: 4
  id: 021-sentinel-format-2
  fingerprint: cbdddea0
