---
id: 012-check-gradient
type: check
title: 'Проверка: чему равен градиент в точке (1, 1)'
source_anchor: '### Step 1: Define a test function'
check:
  - question: >-
      Подставь x = 1 и y = 1 в обе частные производные Розенброка. Чему равен
      градиент в этой точке?
    options:
      - Ноль и ноль
      - Минус два и ноль
      - Ноль и двести
      - Один и один
    correct: 0
    explanation: >-
      При x = 1 и y = 1 выражение y минус x в квадрате равно нулю, поэтому
      вторая производная равна нулю; в первой обнуляются оба слагаемых, потому
      что 1 минус x тоже ноль.
  - question: 'Что сделает градиентный спуск, если запустить его ровно из точки (1, 1)?'
    options:
      - 'Останется на месте: шаг равен learning rate, умноженному на ноль'
      - 'Сделает один большой шаг, потому что долина узкая'
      - Начнёт колебаться поперёк долины
      - Уйдёт вверх по стенке долины
    correct: 0
    explanation: >-
      Из формулы шага видно: вычитается learning rate, умноженный на градиент.
      Градиент нулевой, значит вычитается ноль, и точка не двигается — это и
      означает «мы на дне».
---

Формулы двух частных производных из шага 10 уже написаны — теперь стоит проверить их на одной точке, которую ты знаешь заранее. В шаге 8 сказано, что минимум Розенброка лежит в (1, 1). Если формулы верны, градиент там должен быть нулевым: в точке минимума спускаться некуда, и правило шага из шага 3 перестаёт что-либо менять.

Подставь единицы в обе производные руками, до запуска кода. Именно эта подстановка обычно и спотыкает: люди считают вторую производную, видят ноль, и на первой останавливаются — а в ней два слагаемых, и обнулиться должны оба. Второе слагаемое содержит тот же множитель, что и вторая производная, поэтому гаснет вместе с ней; первое гаснет само.
