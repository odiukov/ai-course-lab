- concept: счётчик итераций Adam должен изменяться вместе с состоянием оптимизатора
  question: Почему при последовательных обновлениях Adam нельзя каждый раз передавать один и тот же счётчик итераций, даже если моменты m и v обновляются правильно?
  explanation: Коррекция смещения зависит не только от m и v, но и от возраста этих скользящих средних. Поэтому номер обновления должен увеличиваться синхронно с переходом к новому состоянию оптимизатора.
  kind: open
  reference: Счётчик входит в формулы коррекции смещения через степени beta. Если он не растёт, Adam на каждой итерации применяет коррекцию как для одного и того же этапа обучения, поэтому оценки моментов и величина обновления искажаются.
  id: 030-code-minimize-adam-1
  fingerprint: 2669d037
- concept: история оптимизации должна содержать независимые снимки изменяемых параметров
  question: Заполни пропуск так, чтобы последующие изменения массива point не меняли уже сохранённые точки траектории.
  explanation: Сохранение самого изменяемого массива добавляет в историю ссылку на общий объект. Копия фиксирует значения параметров на текущей итерации и сохраняет настоящую траекторию.
  kind: cloze
  template: history.append(___)
  answer: point.copy()
  accept:
    - np.copy(point)
    - numpy.copy(point)
  id: 030-code-minimize-adam-2
  fingerprint: 6be1d552
