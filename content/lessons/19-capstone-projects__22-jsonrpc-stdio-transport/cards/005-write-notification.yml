- concept: write_notification делегирует построение конверта и запись специализированным помощникам
  question: Заполни тело метода, используя готовые помощники построения конверта и записи.
  explanation: _notification_envelope отвечает за единую форму уведомления, а _write — за сериализацию и вывод через общий транспортный путь.
  kind: cloze
  template: |-
    def write_notification(self, method, params=None):
        ___
  answer: self._write(_notification_envelope(method, params))
  accept:
    - self._write(_notification_envelope(method, params))
  id: 005-write-notification-1
  fingerprint: bb882a6f
