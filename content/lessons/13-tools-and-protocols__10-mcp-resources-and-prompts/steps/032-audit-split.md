---
id: 032-audit-split
type: check
title: 'Проверка архитектуры: что оставить инструментом, а что разделить'
source_anchor: '## Ship It'
check:
  - question: >-
      summarize_pr загружает данные pull request и задаёт модели порядок
      анализа. Как лучше разделить эту возможность?
    options:
      - >-
        Оставить одним tool, потому что данные и инструкция относятся к одной
        задаче
      - >-
        Вынести данные pull request в resource, а повторяемый порядок анализа —
        в prompt
      - 'Сделать prompt, который сам загружает данные и изменяет pull request'
    correct: 1
    explanation: >-
      Данные служат контекстом, а порядок анализа — повторяемым сценарием. Их
      независимое использование важнее общего названия задачи.
  - question: >-
      release_notes читает изменения, готовит черновик и публикует его. Какая
      граница архитектуры надёжнее?
    options:
      - 'Всё оформить как resource, поскольку работа начинается с чтения'
      - 'Всё оформить как prompt, поскольку итог создаёт модель'
      - 'Отделить контекст и сценарий подготовки, а публикацию оставить tool'
    correct: 2
    explanation: >-
      Публикация изменяет внешнее состояние и остаётся действием, а исходные
      данные и сценарий подготовки можно использовать отдельно.
---

Теперь проверь проекты из [шага 31](#step-31) правилом из [шага 2](#step-2). Частая ловушка — считать одну пользовательскую цель одной MCP-возможностью. Но `triage_issue` может одновременно читать обращение, вести модель по сценарию разбора и менять метки. Если эти части нужны независимо, раздели их: контекст — в resource, порядок рассуждения — в prompt, изменение состояния — в tool. Оставляй единый tool только там, где ценность действительно состоит в неделимом действии.

> 🎒 **На пальцах.** Представь редакцию журнала: папка с материалами, памятка редактору и кнопка «опубликовать» участвуют в выпуске одной статьи. Но склеивать их в один автомат неудобно: папку можно прочитать отдельно, памятку применить к другому тексту, а кнопка совершает необратимое действие. Одна цель не означает одну примитивную операцию.
