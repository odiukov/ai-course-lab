---
id: 012-quiz
type: quiz
title: Выбираем N и защищаем контракт реранкера
source_anchor: '## Use It'
check:
  - question: Как cross-encoder должен оценить пару запроса и документа?
    options:
      - Получить два независимых вектора и сравнить расстояние между ними
      - >-
        Прочитать упакованную пару совместно и вернуть один балл, не учитывая
        padding
      - 'Оценить только документ, используя запрос после вычисления балла'
      - Вернуть отдельный балл для каждого токена документа
    correct: 1
    explanation: >-
      Совместное внимание позволяет словам запроса влиять на чтение документа.
      На выходе нужен один балл пары, причём padding не должен менять результат.
  - question: 'Что произойдёт, если перед шагом оптимизатора не обнулять градиенты?'
    options:
      - Градиенты разных батчей начнут непреднамеренно складываться
      - Модель автоматически перейдёт в режим оценки
      - Loss будет записываться только один раз за всё обучение
      - Батч из одной пары потеряет размерность
    correct: 0
    explanation: >-
      По умолчанию градиенты накапливаются. Без обнуления новый батч повлияет на
      обновление вместе с остатками от предыдущих батчей.
  - question: >-
      Релевантный документ оказался на позиции N плюс 1 у retriever. Что сможет
      сделать reranker, получающий ровно top-N?
    options:
      - Восстановить документ по высокому баллу похожего кандидата
      - Добавить документ из корпуса после сортировки
      - 'Ничего: документ не попал во вторую стадию'
      - 'Вернуть больше top-K, чтобы документ появился'
    correct: 2
    explanation: >-
      Реранкер переставляет только переданные кандидаты. Если нужного документа
      нет в top-N, вторая стадия не может его вернуть.
  - question: >-
      Какая практика надёжнее защищает продакшен-оценку и обработку сомнительных
      запросов?
    options:
      - 'Менять N без новой оценки, а низкий top-1-балл заменять средним'
      - >-
        Фиксировать bi-encoder, cross-encoder и N вместе, а низкий top-1-балл
        использовать как сигнал неуверенности
      - Оценивать cross-encoder на всём корпусе и всегда доверять первому месту
      - 'Кешировать только итоговый текст ответа, не учитывая запрос и документ'
    correct: 1
    explanation: >-
      Качество относится ко всей связке двух моделей и N. Необычно низкий лучший
      балл предупреждает, что подходящего ответа в корпусе может не быть.
---

Дальше тебе понадобится не просто реранкер, а устойчивый контракт всей связки. Упаковка пары и батчинг из [шагов 3](#step-3)–[5](#step-5) сохраняют корректный балл, а [шаги 10](#step-10)–[11](#step-11) связывают качество с N и top-K. Частая ошибка — заменить модель или N, но считать старую оценку действующей. В продакшене фиксируй всю тройку, кешируй балл по запросу и идентификатору документа, а подозрительно низкий top-1 передавай дальше как «я не уверен».

> 🎒 **На пальцах.** Представь сортировочный центр: первый сотрудник кладёт в мешок N писем, второй лишь расставляет их по срочности. Его нельзя винить за письмо, оставшееся на складе. На каждом проверенном конверте сохраняют штамп, чтобы не проверять повторно, а необычно слабая отметка лучшего письма означает: подходящего адресата, вероятно, нет.
