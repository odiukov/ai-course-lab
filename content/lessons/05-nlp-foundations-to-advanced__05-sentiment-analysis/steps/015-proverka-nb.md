---
id: 015-proverka-nb
type: check
title: 'Проверка: нулевая вероятность, сглаживание и log-score'
source_anchor: '### Step 2: multinomial Naive Bayes from scratch'
check:
  - question: >-
      Слово есть в словаре, но ни разу не встречалось в негативном классе. Что
      произойдёт при alpha равном нулю?
    options:
      - Слово просто не повлияет на оценку негативного класса
      - 'Его вероятность станет нулевой, а вычисление логарифма сломается'
      - Его вероятность автоматически станет равной априорной вероятности класса
    correct: 1
    explanation: >-
      Без сглаживания нулевой счётчик даёт нулевую вероятность. Логарифм нуля
      вычислить нельзя, поэтому train_nb заранее запрещает alpha равное нулю.
  - question: >-
      После сглаживания оба класса получили конечные log-score: минус 18 и минус
      23. Какой класс выберет predict_nb?
    options:
      - 'Класс со score минус 23, потому что модуль числа больше'
      - 'Класс со score минус 18, потому что это большее число'
      - 'Ни один: отрицательные score нельзя сравнивать'
    correct: 1
    explanation: >-
      predict_nb выбирает максимальный score. Среди отрицательных чисел минус 18
      больше минус 23; превращать score обратно в вероятность для сравнения не
      требуется.
---

Здесь легко споткнуться дважды: принять нулевую вероятность за «слабый голос» и решить, что более отрицательный log-score лучше. Проверь, что ты связываешь запрет `alpha=0` из [шага 12](#step-12) с `math.log` в функции из [шага 14](#step-14). Сглаживание оставляет каждому классу конечный шанс, а log-score сравниваются как обычные числа: побеждает больший, даже если оба ниже нуля.

> 🎒 **На пальцах.** Представь турнир с денежными штрафами. Один забытый пропуск без страховки означает дисквалификацию — это нулевая вероятность. Сглаживание заменяет дисквалификацию небольшим штрафом, и участник остаётся в игре. После подсчёта выигрывает тот, у кого итог ближе к нулю: долг в 18 монет лучше долга в 23.
