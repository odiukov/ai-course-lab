- concept: третья стадия ZeRO и FSDP — одна и та же техника, разработанная независимо в двух фреймворках
  question: 'Команда сравнивает две конфигурации обучения: PyTorch FSDP с полным шардированием и DeepSpeed ZeRO на стадии, где шардируются параметры, градиенты и состояния оптимизатора. Что верно про их механику?'
  explanation: 'DeepSpeed ZeRO-3 и PyTorch FSDP описывают один механизм: постоянно хранится шард, полный набор параметров слоя собирается на время вычисления и освобождается. Реализации создавались независимо (Microsoft и PyTorch), отсюда две терминологии для одного и того же.'
  kind: choice
  options:
    - Это математически и механически эквивалентные подходы, различаются только названиями и реализациями
    - FSDP шардирует параметры, а ZeRO на этой стадии только реплицирует их с ленивой синхронизацией
    - FSDP даёт приближённый результат, а ZeRO — точный, поэтому у них разная сходимость
    - ZeRO на этой стадии эквивалентен обычному data parallel, а FSDP — нет
  correct: 0
  id: 051-zero-stages-map-1
  fingerprint: 2ef61a9c
- concept: экономия от шардирования только состояний оптимизатора и градиентов ограничена постоянным множителем и не растёт с числом устройств, а шардирование параметров масштабируется вместе с размером группы
  question: Инженер запускает шардирование состояний оптимизатора и градиентов (без шардирования параметров) и увеличивает группу с 16 GPU до 64. Память состояния модели на карту почти не изменилась. Почему?
  explanation: 'Экономия делением статьи на N быстро насыщается: делимая часть стремится к нулю, и остаток бюджета определяется тем, что не шардируется. Поэтому стадии без шардирования параметров дают примерно постоянный выигрыш (около 4× и 8×), а масштабирование ~N появляется только когда на устройства делятся и сами параметры.'
  kind: choice
  options:
    - 'Экономия этих стадий упирается в постоянный потолок: делимые статьи уже почти обнулены, а нешардированные параметры остаются полной копией на каждой карте и не зависят от размера группы'
    - Потому что коллективные операции при большем числе GPU занимают дополнительную память, ровно компенсируя экономию
    - Потому что состояния оптимизатора делятся только внутри узла и не делятся между узлами
    - Потому что при росте группы фреймворк автоматически откатывается к обычному data parallel
  correct: 0
  id: 051-zero-stages-map-2
  fingerprint: 01978ac8
