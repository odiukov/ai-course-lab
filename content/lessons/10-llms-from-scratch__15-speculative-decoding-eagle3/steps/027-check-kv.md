---
id: 027-check-kv
type: check
title: 'Проверка: посчитайте kv_length для двух сценариев'
source_anchor: '### Step 4: KV rollback bookkeeping'
check:
  - question: >-
      Префикс длиной 20 токенов, черновик предложил 5 токенов, отказ случился на
      самой первой позиции. Чему равна логическая длина kv_length после
      добавления токена-поправки?
    options:
      - '20 — принятых черновиков нет, поэтому длина не меняется'
      - '21 — принятых черновиков нет, но добавлен один токен-поправка'
      - 25 — нужно учитывать все физически записанные черновые позиции
      - 26 — к черновику дополнительно прибавляется поправка
    correct: 1
    explanation: >-
      При отказе на первой позиции принято ноль черновых токенов, но
      токен-поправка входит в текст: 20 + 0 + 1 = 21.
  - question: >-
      Префикс содержит 12 токенов, черновик — 6 токенов, а первый отказ
      произошёл на позиции 2. Логическую длину установили в 15, но физически
      записанный хвост кеша не очистили. Что произойдёт?
    options:
      - >-
        Хвост будет проигнорирован, поскольку последующие чтения ограничены
        логической длиной
      - Verifier обязательно прочитает все 18 физических записей
      - Возникнет исключение из-за наличия данных за логической границей
    correct: 0
    explanation: >-
      Длина 15 учитывает 12 токенов префикса, два принятых черновика и одну
      поправку. Физические записи после этой границы не участвуют в чтении и
      впоследствии могут быть перезаписаны.
---

После [шага 26](#step-26) у тебя есть формула `prefix + j + 1`, и на бумаге она выглядит безобидно. Проблема в том, что ошибка здесь не падает с исключением — она просто добавляет в текст токен, который verifier отверг, и генерация поедет вбок.

Поэтому сейчас проверь себя на двух разных ситуациях: когда отказ случился в середине черновика и когда он случился на самой первой позиции. Второй случай спотыкает чаще всего: принятых черновиков ноль, и рука тянется оставить длину прежней. Но поправка — тоже настоящий токен, и единица прибавляется всегда.

Второй частый промах — путать физическую длину кеша с логической: в кеше записей больше, и это нормально.
