---
id: 005-check-problema
type: check
title: 'Проверка: что именно экономит спекуляция'
source_anchor: '## The Problem'
check:
  - question: >-
      Спекуляция заставляет большую модель считать не меньше, а больше
      арифметики: она обрабатывает сразу пять черновых токенов вместо одного.
      Откуда тогда берётся ускорение?
    options:
      - >-
        Арифметика на GPU почти бесплатна по времени, а дорогое — вычитывание
        всех весов модели из памяти, и его теперь делают один раз на пять
        токенов
      - >-
        Черновая модель берёт часть арифметики большой модели на себя, поэтому
        большой остаётся меньше работы
      - 'Пять токенов считаются с меньшей точностью, и каждый обходится дешевле'
      - >-
        Ускорение берётся из того, что черновик угадывает конец фразы и большую
        модель можно не запускать вовсе
    correct: 0
    explanation: >-
      Время уходит на перекачку 140 ГБ весов из памяти, а не на счёт. Веса
      подняли один раз — а проверить на них можно и один токен, и пять, почти за
      те же миллисекунды.
  - question: >-
      Ты заменил черновик на более умный: он стал угадывать вдвое чаще, а
      работать так же быстро. Что вырастет?
    options:
      - Скорость одного forward pass большой модели
      - >-
        Число принятых токенов за один проход большой модели, значит проходов на
        тот же текст нужно меньше
      - Пропускная способность памяти GPU
      - 'Ничего: скорость определяет только размер большой модели'
    correct: 1
    explanation: >-
      Один проход verifier стоит столько же, но выносит из него больше готовых
      токенов. Именно поэтому качество черновика — не мелочь, а второй множитель
      ускорения.
---

Прежде чем идти дальше, проверь одну вещь на себе: что именно спекуляция сокращает. Не арифметику — её становится больше. Не размер модели — он тот же 70B. Сокращается число раз, когда приходится тащить эти веса из памяти, потому что каждый такой поход теперь приносит не один токен, а несколько ([шаг 4](#step-4)).

Здесь обычно спотыкаются дважды. Первое: кажется, что черновик «делает часть работы за» большую модель — нет, большая модель проверяет всё сама, просто одним заходом ([шаг 3](#step-3)). Второе: кажется, что качество черновика — деталь. Оно множитель: чем чаще он угадывает, тем полнее корзина за тот же поход.
