- concept: обучаемый подмодуль регистрируется в nn.Module при присваивании атрибуту self
  question: Заполните пропуск так, чтобы линейный слой зарегистрировался как часть модуля и использовался в forward.
  explanation: Присваивание self.proj регистрирует слой и его параметры внутри nn.Module. Локальная переменная proj не стала бы частью модуля, поэтому оптимизатор не получил бы её веса через model.parameters().
  kind: cloze
  template: |-
    class SpeechAdapter(nn.Module):
        def __init__(self, in_dim, out_dim):
            super().__init__()
            ___ = nn.Linear(in_dim, out_dim)

        def forward(self, x):
            return self.proj(x)
  answer: self.proj
  accept:
    - self.proj =
  id: 015-project-1
  fingerprint: dc675530
- concept: линейный слой изменяет последнюю ось тензора и сохраняет ведущие оси
  question: Projector получает тензор формы (6, 240, 1536) и преобразует каждый вектор последней оси из ширины 1536 в ширину 3584. Какой будет форма результата?
  explanation: 'nn.Linear независимо обрабатывает каждый вектор по последней оси. Размеры батча и последовательности сохраняются, поэтому меняется только ширина признаков: (6, 240, 1536) превращается в (6, 240, 3584).'
  kind: choice
  options:
    - (6, 3584, 240)
    - (6, 240, 3584)
    - (3584, 240, 1536)
    - (6, 240, 1536)
  correct: 1
  id: 015-project-2
  fingerprint: 221b2c18
