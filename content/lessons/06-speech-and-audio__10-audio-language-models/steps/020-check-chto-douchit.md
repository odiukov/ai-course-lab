---
id: 020-check-chto-douchit
type: check
title: 'Проверка: что реально дообучают в чужой LALM'
source_anchor: '### The three-component template'
check:
  - question: >-
      У тебя одна GPU и 40 часов записей колл-центра. Ты хочешь, чтобы чужая
      готовая LALM отвечала на вопросы про эти звонки. С чего реально начинают в
      такой ситуации?
    options:
      - >-
        Переучивают весь декодер LLM на своих данных — иначе модель не увидит
        новую предметную область
      - >-
        Замораживают энкодер и LLM, учат только projector — это те самые пара
        миллионов параметров
      - >-
        Меняют аудио-энкодер на другой, с шириной побольше, чтобы в него влезло
        больше деталей
      - 'Добавляют речевой декодер, чтобы модель отвечала голосом'
    correct: 1
    explanation: >-
      Stage 1 трогает только projector — около 22 млн параметров против 7 млрд у
      LLM, и это влезает в одну GPU. Полный или LoRA-файнтюн LLM — это уже Stage
      2 и кластер.
  - question: >-
      Чем Stage 2 отличается от Stage 1 по тому, что физически меняется в
      модели?
    options:
      - >-
        Stage 2 обучает projector на других данных — на инструкциях вместо ASR,
        но веса меняются те же
      - >-
        Stage 2 размораживает LLM (полностью или через LoRA), поэтому меняются
        веса самой языковой модели
      - 'Stage 2 добавляет второй аудио-энкодер, как в SALMONN'
      - Stage 2 меняет ширину projector с 4096 на другую
    correct: 1
    explanation: >-
      Stage 1 — только мост между энкодером и LLM. Stage 2 идёт внутрь LLM:
      полный или LoRA-файнтюн на аудио-инструкциях, отсюда и скачок цены с одной
      GPU до кластера.
---

Тебе прислали чужую LALM и попросили «дообучить её под наши звонки». Первый порыв — открыть модель и запустить обучение целиком. Здесь и стоит остановиться и вспомнить числа из [шага 17](#step-17) и три стадии из [шага 19](#step-19): projector — это доли процента модели, LLM — всё остальное.

После этого куска ты должен уметь по описанию задачи сказать, какая стадия имеется в виду и во что она обойдётся по железу.

Спотыкаются обычно на двух вещах. Первая — путают «обучили на аудио-инструкциях» с «обучили projector»: данные поменялись, а размороженные веса — совсем другие. Вторая — считают Stage 3 обязательной; она нужна только там, где ответ должен звучать голосом.
