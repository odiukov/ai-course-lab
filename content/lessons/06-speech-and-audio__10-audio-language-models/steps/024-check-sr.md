---
id: 024-check-sr
type: check
title: 'Проверка: что будет от 44.1 кГц без ресемплинга'
source_anchor: '### Step 1: query Qwen2.5-Omni'
check:
  - question: >-
      Ты загрузил clip.wav как есть, с частотой 44.1 кГц, и передал в
      Qwen2.5-Omni. Что произойдёт?
    options:
      - >-
        Модель упадёт с ошибкой про несовпадение частоты — и это подскажет, что
        надо ресемплить
      - 'Процессор сам приведёт запись к 16 кГц, разницы в ответе не будет'
      - >-
        Ошибки не будет: модель уверенно выдаст текст, который не имеет
        отношения к записи
      - 'Ответ станет чуть менее точным, но по смыслу останется правильным'
    correct: 2
    explanation: >-
      Энкодер обучен на 16 кГц и молча считает, что ему дали 16 кГц. Те же
      сэмплы он читает как другой звук — и генерация идёт по мусорным
      аудио-токенам, без единого предупреждения.
  - question: >-
      В вызове Qwen2.5-Omni поле content — это список из двух элементов: аудио и
      текст. Что это значит для модели?
    options:
      - >-
        Аудио и вопрос идут двумя отдельными запросами, модель обрабатывает их
        независимо
      - >-
        Аудио и текст попадают в LLM одной последовательностью токенов, как один
        ход диалога
      - >-
        Порядок элементов не важен, потому что аудио всё равно обрабатывается
        первым
      - >-
        Текст нужен только как подпись к файлу, отвечать модель будет по одному
        аудио
    correct: 1
    explanation: >-
      Аудио превращается в токены той же ширины, что и текстовые, и склеивается
      с ними в один ряд — это ровно та схема, что была на шаге 21.
---

После этого куска ты должен уметь прочитать вызов Qwen2.5-Omni из [шага 23](#step-23) и сказать, что в нём держит всю конструкцию: список `content` из аудио и текста как один ход диалога, `sr=16000` под частоту, на которой обучался энкодер, и `max_new_tokens=200` — примерно абзац ответа.

Спотыкаются обычно на частоте. Кажется, что 44.1 кГц — это «лучше, чем 16», и что библиотека при необходимости сама подстроится или хотя бы предупредит. Ни того, ни другого: сэмплы уйдут в энкодер как есть, он прочитает их по своей мерке, и ты получишь гладкий уверенный текст — просто про другой звук. Тихая ошибка страшнее падения: падение видно сразу, а этот ответ выглядит нормально.
