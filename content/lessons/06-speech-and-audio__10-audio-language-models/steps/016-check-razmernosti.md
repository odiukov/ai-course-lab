---
id: 016-check-razmernosti
type: check
title: 'Проверка: что сломается при llm_dim = 2048'
source_anchor: '### Step 2: the projector pattern'
check:
  - question: >-
      Ты поставил в projector llm_dim = 2048, а LLM осталась прежней, с шириной
      токенных эмбеддингов 4096. Что произойдёт при передаче аудио-векторов в
      модель?
    options:
      - LLM автоматически дополнит каждый аудио-вектор нулями
      - >-
        Возникнет ошибка размерности: выход projector нельзя объединить с
        токенными эмбеддингами LLM
      - 'Модель продолжит работать, но обработка аудио станет медленнее'
      - >-
        Сломается только fc1, потому что его входная ширина должна делиться на
        выходную
    correct: 1
    explanation: >-
      Выход projector должен иметь ту же ширину, что и токенные эмбеддинги LLM.
      Векторы шириной 2048 нельзя подать вместо ожидаемых векторов шириной 4096
      без дополнительного преобразования.
  - question: >-
      Ты заменяешь Whisper-large на аудиоэнкодер с шириной выхода 768, сохраняя
      LLM с шириной эмбеддингов 4096. Как нужно изменить projector?
    options:
      - 'Установить audio_dim = 768, а llm_dim оставить равным 4096'
      - 'Установить и audio_dim, и llm_dim равными 768'
      - 'Оставить audio_dim = 1280, а llm_dim уменьшить до 768'
      - 'Ничего не менять: nn.Linear определит ширину входа при первом вызове'
    correct: 0
    explanation: >-
      audio_dim должен совпадать с шириной выхода аудиоэнкодера, поэтому он
      меняется на 768. llm_dim по-прежнему задаётся шириной токенных эмбеддингов
      LLM и остаётся равным 4096.
---

После [шагов 9](#step-9)–[15](#step-15) ты должен уметь отвечать на один практический вопрос: какое из двух чисел в projector твоё, а какое продиктовано снаружи. `audio_dim` ты переставляешь свободно — сменил энкодер, поправил вход. `llm_dim` тебе не принадлежит: он задан той LLM, к которой ты пристыковываешься, и уменьшить его «чтобы модель была легче» нельзя.

Спотыкаются обычно здесь: кажется, что размерности внутри projector — это просто настройки, которые модель как-нибудь переварит. Не переварит. Векторы, выходящие из fc2, встают в один ряд с токенными эмбеддингами, а ряд из элементов разной длины не соберётся.
