---
id: 008-check-lai
type: check
title: 'Проверка: кто услышит лай, а кто примет его за слово'
source_anchor: '### The 2026 model map'
check:
  - question: >-
      Ты собираешь модель, которая должна отвечать на вопрос «что за звук на
      фоне речи». Аудио-энкодер взял только Whisper. Что скорее всего получится?
    options:
      - >-
        Модель опишет фоновый звук точнее, чем при любом другом энкодере, потому
        что Whisper учили на реальных записях
      - >-
        Модель попробует натянуть на лай что-то речевое и выдаст слово или
        обрывок фразы вместо описания звука
      - 'Модель откажется отвечать, потому что во входе нет распознаваемой речи'
      - >-
        Ничего не изменится: энкодер отвечает только за длину карточек, а смысл
        достаёт LLM
    correct: 1
    explanation: >-
      Whisper обучали на речи, и он описывает вход в терминах речи — лай он
      представит как что-то похожее на слово, а не как звук собаки.
  - question: >-
      Зачем SALMONN держит сразу два энкодера — Whisper и BEATs, — вместо
      одного?
    options:
      - >-
        Чтобы удвоить скорость: два энкодера обрабатывают запись параллельно,
        каждый свою половину
      - Чтобы один страховал другой на случай ошибки распознавания
      - >-
        Потому что один энкодер хорошо слышит либо речь, либо остальные звуки, и
        два набора карточек дают LLM обе стороны записи
      - >-
        Потому что LLM не принимает вход от одного энкодера — ей всегда нужны
        минимум два источника
    correct: 2
    explanation: >-
      Каждый энкодер силён в том, на чём его учили; два энкодера — плата за то,
      чтобы модель одинаково честно слышала и «stop!», и собаку.
---

Здесь проверяется одна практичная вещь: умеешь ли ты по вопросу к записи предсказать, какой энкодер её вытянет, а какой соврёт. Скелет из [шага 4](#step-4) остаётся тем же — меняется только то, чьи карточки попадают в LLM.

Спотыкаются обычно на двух местах. Первое: кажется, что энкодер «просто оцифровывает звук», а смысл целиком достаёт LLM — нет, энкодер уже решает, в каких терминах записан вход, и LLM других данных не увидит. Второе: два энкодера читают как запас прочности, дубль на всякий случай. Это не дубль — это два разных слуха, и в [шаге 7](#step-7) видно, зачем оба.
