---
id: 013-check-hann
type: check
title: 'Проверка: чему равно hann(1) и почему не деление на ноль'
source_anchor: '### Step 2: Hann window'
check:
  - question: 'Что произойдёт, если убрать из hann отдельную ветку для N равного 1?'
    options:
      - 'Функция вернёт пустой список, потому что range(1) не даст ни одного шага'
      - >-
        Python бросит ZeroDivisionError, потому что знаменатель N минус 1
        обратится в ноль
      - 'Функция вернёт [0.0], потому что при n равном 0 косинус равен единице'
    correct: 1
    explanation: >-
      При N равном 1 в знаменателе стоит 1 минус 1, то есть ноль, и деление
      падает с ZeroDivisionError — поэтому этот случай перехватывают до
      вычислений.
  - question: 'Почему для окна из одного сэмпла честный ответ — это [1.0], а не [0.0]?'
    options:
      - >-
        Ноль на краях получается только у окна из одного элемента, так что 0.0
        был бы неверным типом
      - >-
        Единственный сэмпл сглаживать нечем и незачем, а множитель 1.0 оставляет
        его как есть; 0.0 просто стёр бы данные в ноль
      - 1.0 — это среднее между нулями на краях и единицей в середине
    correct: 1
    explanation: >-
      Окно из одного элемента не имеет ни краёв, ни середины, сглаживать нечего;
      умножение на 1.0 сохраняет сэмпл, а на 0.0 — уничтожило бы его.
---

После [шага 12](#step-12) ты должен уметь читать не только основную строку с формулой, но и защитную ветку перед ней — и отвечать, что именно она ловит.

Разбери на числах: у формулы из [шага 11](#step-11) в знаменателе стоит `N - 1`. При `N = 400` это 399, всё считается. При `N = 1` это ноль, и Python не вернёт ни `inf`, ни `nan` — он остановит программу с `ZeroDivisionError`.

Чаще всего спотыкаются на двух вещах. Первая: путают «окно нулевое на краях» с «ответ должен быть нулём» — и ждут `[0.0]`. Вторая: считают такую ветку украшением для аккуратности, а не единственным способом не уронить код на вырожденном входе.
