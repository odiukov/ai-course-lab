---
id: 040-itogovaya-proverka
type: quiz
title: Итоговая проверка
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты меняешь замороженную LLM в готовом BLIP-2 на другую, с другой
      размерностью эмбеддингов. Что придётся переделать?
    options:
      - 'Всё обучение с нуля: и этап представлений, и этап с LLM'
      - >-
        Только этап 2 — проекцию и подгонку Q-Former под новую LLM; этап 1
        переиспользуется
      - 'Ничего: 32 запроса подойдут любой LLM как есть'
      - 'Только визуальный энкодер, потому что он даёт признаки для запросов'
    correct: 1
    explanation: >-
      Этап 1 учит запросы вытягивать содержание картинки и про LLM ничего не
      знает, поэтому его результат переносится; заново нужен только этап 2, где
      выход подгоняется под конкретные эмбеддинги.
  - question: В cross-attention внутри Q-Former откуда берутся Q и откуда K и V?
    options:
      - 'Q, K и V — все из патчей ViT, запросы только складывают результат'
      - 'Q из патчей ViT, K и V из 32 запросов'
      - 'Q из 32 обучаемых запросов, K и V из признаков патчей ViT'
      - 'Q из запросов, K из патчей, V из текста'
    correct: 2
    explanation: >-
      Запросы задают вопросы, поэтому они сторона Q; отвечает картинка, поэтому
      её патчи дают K и V. Матрица внимания выходит 32 на 256, и каждая её
      строка суммируется в единицу.
  - question: >-
      Видео 60 кадров. Мост BLIP-2 даёт 32 tokens на кадр, LLaVA — 576. Что это
      меняет на практике?
    options:
      - >-
        1920 против 34560 tokens: у BLIP-2 видео влезает в контекст, у LLaVA
        почти наверняка нет
      - 'Разница только в качестве, длина контекста от числа tokens не зависит'
      - '1920 против 3456 tokens — разница есть, но некритичная'
      - 'У LLaVA меньше, потому что MLP-проектор сжимает сильнее'
    correct: 0
    explanation: >-
      60 умножить на 32 = 1920, 60 умножить на 576 = 34560. Именно поэтому там,
      где картинок много, выигрывает сжатие в фиксированные 32 вектора.
  - question: 'Почему нельзя сказать «первый запрос отвечает за объекты, второй за цвет»?'
    options:
      - Потому что смысл запросов задаётся вручную при инициализации
      - >-
        Потому что запросы меняются от картинки к картинке и общего смысла у них
        нет
      - 'Потому что их всего 32, а признаков в картинке больше'
      - >-
        Потому что они настроились так, чтобы минимизировать лосс, а не поделить
        смыслы по-человечески
    correct: 3
    explanation: >-
      Запросы — обычные параметры: они одинаковы для всех входов и выучены под
      лосс. Разделение труда между ними складывается само и человеческим
      категориям не соответствует.
---

Это конец урока — и дальше в курсе тебе понадобится не столько название «Q-Former», сколько привычка считать. Сколько векторов картинка отдаёт в контекст, кто в cross-attention сторона Q, а кто K/V, и что именно ломается, когда меняешь один блок из трёх.

Спотыкаются обычно в двух местах. Первое: путают, где чей вход в cross-attention, — а от этого зависит форма матрицы внимания из [шага 13](#step-13). Второе: думают, что смена LLM обнуляет весь труд, хотя двухэтапная схема из [шага 22](#step-22) и [шага 27](#step-27) как раз затем и придумана, чтобы первый этап пережил замену второго.

Если эти два места держатся — четыре ответа на вопрос «сколько tokens отдать картинке» из [шага 35](#step-35) складываются в один выбор, а не в четыре имени.
