---
id: 004-tsena-256-tokenov
type: check
title: 'Проверка: сколько tokens съедает батч картинок'
source_anchor: '## The Problem'
check:
  - question: >-
      Замороженный ViT выдаёт фиксированное число patch-tokens для каждого
      изображения. Что произойдёт с общим числом визуальных tokens, если размер
      батча увеличить вдвое, не меняя представление одной картинки?
    options:
      - Общее число визуальных tokens удвоится
      - Общее число визуальных tokens уменьшится вдвое
      - Общее число визуальных tokens останется прежним
      - >-
        Изменится только размерность каждого token, а общее число будет
        непредсказуемым
    correct: 0
    explanation: >-
      Каждая картинка приносит одинаковое число patch-tokens, поэтому при
      удвоении количества картинок суммарное число визуальных tokens также
      удваивается.
  - question: >-
      Линейный слой преобразует каждый patch-token из пространства признаков ViT
      в пространство эмбеддингов LLM. Какое утверждение о последовательности
      верно?
    options:
      - >-
        Он меняет ширину каждого token, но сохраняет число позиций в
        последовательности
      - Он объединяет все patch-tokens в один token
      - Он удаляет половину patch-tokens независимо от их содержимого
      - Он добавляет по одному token за каждую новую координату признака
    correct: 0
    explanation: >-
      Линейная проекция применяется к каждому вектору отдельно: размерность
      признака меняется, а длина последовательности остаётся прежней.
---

После этого куска ты должен уметь превращать «256 patch-tokens» в реальный счёт: взять размер батча, умножить и увидеть, сколько контекста ушло ещё до текста. И объяснить, почему мост из [шага 3](#step-3) эту цифру не трогает.

Спотыкаются обычно на двух местах. Первое — путают 1408 и 4096 с количеством tokens: это ширина одного вектора, а не их число. Второе — считают, что стыковка размерностей и есть решение проблемы: она снимает несовместимость форм, но 256 остаётся 256, и на батче из 32 картинок это те самые 8192 tokens, которых больше нет под вопрос пользователя.
