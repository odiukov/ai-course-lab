---
id: 013-gde-teryaetsya-tochnost
type: check
title: На каких двух операциях roundtrip теряет информацию?
source_anchor: '## The Concept'
check:
  - question: На каких двух операциях roundtrip может необратимо потерять информацию?
    options:
      - На делении на масштаб и обратном умножении
      - На округлении и clipping
      - На вычислении границ кодов и деквантизации
    correct: 1
    explanation: >-
      Округление объединяет близкие значения в один код, а clipping прижимает
      значения за пределами диапазона к его краям.
  - question: >-
      Два разных исходных числа превратились в один код, хотя оба находились
      внутри допустимого диапазона. Где потерялось различие?
    options:
      - При округлении
      - При clipping
      - При обратном умножении на масштаб
    correct: 0
    explanation: >-
      Clipping не меняет значения внутри допустимого диапазона. После
      масштабирования оба числа попали в одну область округления и получили
      одинаковый код.
---

Ты запускаешь `roundtrip` из [шага 12](#step-12) и замечаешь: разные числа иногда возвращаются одинаковыми. Найди две точки невозврата из [шага 7](#step-7). Округление стирает различия между близкими значениями, отправляя их в один целый код. Clipping стирает различия между значениями за границей: все слишком большие получают верхний код, все слишком маленькие — нижний.

Не путай потерю с деквантизацией: она лишь переводит уже выбранный код обратно в приближённое число.

> 🎒 **На пальцах.** Представь сортировку писем по почтовым ячейкам. Близкие адреса округлили до одного района — первое различие исчезло. Письма из районов за пределами карты сложили в крайнюю ячейку — исчезло второе. Доставая письма, ты уже не восстановишь точные адреса.
