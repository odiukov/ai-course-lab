---
id: 036-itogovyi-quiz
type: quiz
title: 'Итоговая проверка: выбираем продакшн-квантизацию и защищаем решение числами'
source_anchor: '## Exercises'
check:
  - question: >-
      Модель 70B обслуживает 128 одновременных запросов с контекстом 2k, а
      KV-кэш занимает 20 ГБ независимо от формата весов. Какой вывод верен для
      H100 на 80 ГБ?
    options:
      - 'Влезет только INT4, поэтому остальные форматы можно не тестировать'
      - >-
        Влезут почти все форматы, кроме FP16, а формат повлияет прежде всего на
        запас для параллельных запросов
      - >-
        Влезет только FP8, потому что KV-кэш хранится в том же формате, что и
        веса
      - 'Влезет даже FP16, потому что при расчёте HBM учитывают только веса'
    correct: 1
    explanation: >-
      KV-кэш добавляет одинаковые 20 ГБ к каждому варианту. Поэтому почти все
      сжатые форматы помещаются, но оставляют разный запас HBM для роста
      нагрузки.
  - question: >-
      На GPU нужно обслуживать одну базовую модель с десятками LoRA-адаптеров.
      Какая отправная точка лучше соответствует этой нагрузке?
    options:
      - >-
        GGUF Q4_K_M, потому что любой четырёхбитный формат одинаково эффективен
        на GPU
      - 'FP16, потому что LoRA исключает необходимость экономить память весов'
      - >-
        GPTQ с Marlin, после чего нужно проверить поддержку движком и измерить
        реальную пропускную способность
      - 'MXFP4 на любой видеокарте, даже без аппаратной поддержки Blackwell'
    correct: 2
    explanation: >-
      GPTQ с Marlin подходит для GPU-сценария с множеством LoRA. Но название
      формата само по себе не гарантирует результат: важны совместимость движка
      и замер на целевом железе.
  - question: >-
      Ты квантизуешь медицинскую модель в AWQ. Какой план калибровки и проверки
      надёжнее?
    options:
      - >-
        Взять несколько сотен примеров из целевого медицинского домена и затем
        провести доменный eval
      - >-
        Взять максимально большой набор WikiText: объём важнее соответствия
        рабочим запросам
      - 'Не использовать калибровочные данные, если средняя ошибка весов мала'
      - 'Проверить только скорость генерации: она показывает и качество ответов'
    correct: 0
    explanation: >-
      Небольшая репрезентативная доменная выборка лучше показывает типичные
      активации модели. После калибровки отдельный доменный eval проверяет уже
      качество на задачах.
  - question: >-
      INT4-вариант помещается в HBM и показывает малую среднюю ошибку весов, но
      теряет четыре пункта на ключевом reasoning-тесте. Что делать?
    options:
      - 'Одобрить INT4: численная ошибка важнее прикладной метрики'
      - >-
        Увеличить число одновременных запросов, чтобы компенсировать потерю
        качества
      - Считать падение нормальным для любого сжатия и убрать тест из проверки
      - >-
        Не выпускать этот вариант при строгом допуске и откатиться к FP8 или
        исходной точности
    correct: 3
    explanation: >-
      Малая ошибка тензора не гарантирует сохранение качества модели. Если
      доменная метрика нарушила заранее заданный допуск, должен сработать путь
      отката.
---

Перед тобой два кандидата: один экономит ещё 12 ГБ, другой теряет меньше баллов на доменном тесте. Здесь пригодится весь путь урока: сначала совместимость из [шага 4](#step-4), затем расчёт памяти из [шага 29](#step-29), измерение скорости, доменный eval и путь отката из [шага 35](#step-35). Частая ошибка — защищать формат одним красивым числом. Продакшн-решение выдерживает сразу ограничения железа, нагрузки и качества.

> 🎒 **На пальцах.** Ты выбираешь мост для грузовиков. Цена строительства важна, но акт приёмки не подпишут только по цене: нужны допустимая нагрузка, результат испытаний и запасной маршрут. Формат — это конструкция моста, бенчмарки — испытания, а откат — маршрут, который не даст остановить движение.
