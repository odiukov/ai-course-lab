- concept: общий курсор KV-кэша сдвигается после записи одного и того же блока всеми слоями
  question: Заполните аргумент, которым нужно сдвинуть общий курсор после завершения записей во всех слоях.
  explanation: Все слои записывают данные одних и тех же токенов в одинаковый диапазон позиций. Поэтому общий курсор сдвигают на длину нового блока лишь после завершения всех послойных записей.
  kind: cloze
  template: |-
    for layer in layers:
        cache.update(layer, new_keys[layer], new_values[layer])
    cache.advance(___)
  answer: num_new_tokens
  accept:
    - block_length
    - длина нового блока
    - число новых токенов
  id: 023-with-cache-algorithm-1
  fingerprint: b3edd440
