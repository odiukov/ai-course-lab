- concept: предельное число одновременных диалогов определяется остатком памяти после размещения весов и накладных расходов
  question: Заполни формулу оценки числа полных диалогов, которые помещаются на GPU.
  explanation: Сначала из памяти GPU вычитают объём весов и накладные расходы. Оставшийся бюджет делят на требуемый KV-кэш одного диалога, а результат округляют вниз.
  kind: cloze
  template: N_max = floor((M_GPU - M_weights - M_overhead) / ___)
  answer: M_KV_per_dialog
  accept:
    - M_KV_per_request
    - размер KV-кэша одного диалога
    - KV cache per request
  id: 032-memory-check-1
  fingerprint: f6186ba1
