- concept: 'критерий выбора между агентным харнессом и графом переходов — кто определяет следующий шаг: модель на каждом витке или заранее нарисованная схема'
  question: 'Регламент требует: маршрут обработки заявки фиксирован, каждая ветка согласована с комплаенсом заранее, прогон обязан повторяться шаг в шаг. Какая архитектура подходит и почему?'
  explanation: 'Развилка одна: кто выбирает следующий шаг. Харнесс отдаёт этот выбор модели на каждом витке цикла, поэтому маршрут недетерминирован даже при узком наборе инструментов и подробном промпте. Конечный автомат в форме графа фиксирует переходы в коде — модель влияет на содержимое узла, но не на топологию.'
  kind: choice
  options:
    - 'Агентный харнесс: модель выберет тот же маршрут, если дать ей подробный системный промпт'
    - 'Граф с явными узлами и переходами: маршрут задан кодом, модель работает внутри узлов, а не решает, куда идти'
    - 'Агентный харнесс с ограниченным набором инструментов: сузив набор, получаем фиксированный порядок вызовов'
    - 'Любая из двух: разница только в удобстве API, поведение прогона одинаковое'
  correct: 1
  id: 033-check-vybor-instrumenta-1
  fingerprint: bb0c5647
- concept: агентные SDK разных вендоров реализуют один и тот же подход «харнесс вокруг модели», поэтому выбор между ними — вопрос вендора и экосистемы, а не архитектуры
  question: Команда сравнивает Claude Agent SDK и OpenAI Agents SDK. Что здесь на самом деле выбирается?
  explanation: 'Это один и тот же подход в исполнении двух вендоров: агентный цикл, встроенные инструменты, делегирование. Архитектурная развилка (модель решает против графа) проходит не между ними, а между обоими и LangGraph.'
  kind: choice
  options:
    - 'Модель управления прогоном: один даёт цикл под управлением модели, другой — граф переходов'
    - 'Вендор и экосистема при одинаковом подходе: оба — харнесс с агентным циклом, инструментами и делегированием'
    - 'Степень предсказуемости: у OpenAI прогон детерминирован, у Anthropic — нет'
    - 'Способ размещения: один только self-hosted, другой только хостируемый'
  correct: 1
  id: 033-check-vybor-instrumenta-2
  fingerprint: 08ae91e4
- concept: 'ось «где исполняется прогон» независима от оси «кто решает, что делать дальше»: хостируемый вариант снимает инфраструктуру, но не превращает прогон в заданный маршрут'
  question: 'Инженер говорит: «Возьмём хостируемых управляемых агентов — тогда прогон станет предсказуемым, потому что всё исполняется на стороне сервиса». В чём ошибка рассуждения?'
  explanation: Типовая подмена «хочу удобно» на «хочу управляемо». Управляемый хостинг меняет операционную модель, а не механику принятия решений внутри прогона.
  kind: open
  reference: 'Он смешал две независимые оси. Первая — где живёт процесс, владеющий циклом: у себя или у провайдера. Хостируемый вариант снимает аптайм, кэширование промптов, сжатие истории. Вторая — кто решает, что делать на следующем шаге. В управляемых агентах это по-прежнему модель: цикл тот же агентный, просто чужой. Предсказуемость маршрута даёт только явно заданный граф переходов; хостинг её не добавляет, а контроля над вмешательством в цикл, наоборот, оставляет меньше.'
  id: 033-check-vybor-instrumenta-3
  fingerprint: dbd8c018
