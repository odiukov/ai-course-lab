- concept: плоский перечень ключей сессий не несёт информации о родстве, поэтому детей конкретного запуска находят отдельной операцией по родительскому ключу
  question: В хранилище сессий 50 записей. Вчера оркестратор запустил четырёх субагентов, каждый вёл свою сессию. Почему `list_sessions()` не решает задачу «найти сессии этих четверых»?
  explanation: '`list_sessions()` перечисляет всё, что есть, одним уровнем: сорок-пятьдесят идентификаторов без структуры. Родство — отдельное измерение, за него отвечает `list_subkeys(session_id)`: передаёшь ключ родителя, получаешь ключи его субагентов. Сортировка по времени не спасает: между запуском и разбором в базу попадают чужие сессии.'
  kind: choice
  options:
    - Он возвращает плоский список всех ключей без указания, кто чей родитель — нужен запрос детей по ключу родительской сессии
    - Он возвращает только сессии оркестраторов, сессии субагентов в него не попадают
    - Он возвращает ключи, но стенограммы субагентов хранятся не в сессионном хранилище
    - Он возвращает список, отсортированный по времени, поэтому четыре последних ключа и есть искомые
  correct: 0
  id: 025-derevo-subklyuchej-1
  fingerprint: d0496629
- concept: связь родитель–субагент фиксируется в хранилище в момент порождения субагента, а не реконструируется постфактум эвристиками
  question: Откуда хранилище сессий знает, что сессия субагента принадлежит именно этому оркестратору?
  explanation: 'Отметка о субключе кладётся при порождении субагента — это факт, записанный в момент, когда он ещё известен точно. Восстановление постфактум (по времени, по префиксам, по тексту стенограммы) — это догадка: параллельные запуски и перезапуски её ломают.'
  kind: choice
  options:
    - Родитель записывает субключ рядом со своей сессией в момент запуска субагента
    - Связь вычисляется при чтении по совпадению временных меток начала сессий
    - 'Связь выводится из префикса идентификатора: у детей он наследуется от родителя'
    - Связь восстанавливается разбором стенограммы родителя на упоминания субагентов
  correct: 0
  id: 025-derevo-subklyuchej-2
  fingerprint: 35960e5e
- concept: аргумент запроса субключей — идентификатор родительской сессии, а не сессии субагента
  question: 'Оркестратор работает в сессии `parent_id`, его субагенты — в сессиях `kid_a` и `kid_b`. Заполни пропуск: какой идентификатор передают, чтобы получить ключи субагентов?'
  explanation: Операция отвечает на вопрос «кто мои дети», значит на вход идёт ключ родителя и наружу выходит список детей. Передать `kid_a` — значит спросить про детей самого субагента (если он никого не порождал, список пуст).
  kind: cloze
  template: subkeys = store.list_subkeys(___)
  answer: parent_id
  accept:
    - parent_id
    - parent
    - ключ родительской сессии
    - session_id родителя
  id: 025-derevo-subklyuchej-3
  fingerprint: eaeedae4
