- concept: подставная функция модели обязана воспроизводить оба вида ответа API — текстовый блок и tool_use — потому что цикл распаковывает ответ одинаково для заглушки и для реального клиента
  question: Разработчик пишет подставную функцию модели для агентного цикла. Она принимает историю сообщений и что-то возвращает. Какое требование к возвращаемому значению обязательно, чтобы цикл работал без правок при замене заглушки на настоящий клиент?
  explanation: 'Взаимозаменяемость держится на контракте формы ответа, а не на содержании. Цикл читает у ответа одни и те же поля: есть tool_use — исполнить инструмент и дописать результат в историю, есть только текст — закончить. Если заглушка отдаёт результат исполнения или голую строку, цикл придётся ветвить под неё, и проверка перестаёт быть честной.'
  kind: choice
  options:
    - 'Возвращать те же формы ответа, что и API: либо блок с текстом, либо блок tool_use с именем инструмента и словарём аргументов'
    - Возвращать готовый результат исполнения инструмента, чтобы циклу не пришлось его вызывать
    - Возвращать строку с именем инструмента, а аргументы цикл достроит сам по схеме
    - Возвращать исключение особого типа, когда нужен вызов инструмента
  correct: 0
  id: 006-code-stub-model-1
  fingerprint: c42b2a62
- concept: сценарий подставной модели должен зависеть от состояния истории, иначе агентный цикл не получает сигнала завершения и упирается в лимит шагов
  question: Подставная модель написана так, что на любую историю сообщений возвращает один и тот же блок tool_use с вызовом `list_dir`. Лимит шагов цикла — 12. Что произойдёт при запуске?
  explanation: 'Завершение цикла — это ответ модели без tool_use. Заглушка, не смотрящая на историю, такой ответ никогда не выдаст, поэтому единственный ограничитель — лимит шагов. Дедупликации вызовов в цикле нет, история растёт от шага к шагу, а схема при повторе валидна. Сценарий обязан ветвиться по состоянию: пустая история, история с результатом первого инструмента, история с прочитанными данными.'
  kind: choice
  options:
    - Цикл выполнит `list_dir` 12 раз и оборвётся по лимиту, так и не получив финальный текстовый блок
    - 'Цикл выполнит `list_dir` один раз: повторный одинаковый вызов будет отброшен как дубликат'
    - Цикл завершится сразу, потому что история после первого шага перестанет меняться
    - Цикл упадёт с ошибкой валидации схемы инструмента на втором шаге
  correct: 0
  id: 006-code-stub-model-2
  fingerprint: d6a75246
- concept: трасса, оборвавшаяся по лимиту шагов с повтором одного инструмента, локализует неисправность в сценарии подставной модели, а не в реализации агентного цикла
  question: 'Трасса тестового прогона: 40 шагов подряд с вызовом `read_file` по одному и тому же пути, затем обрыв по лимиту. Инструменты отработали, результаты дописались в историю. Где искать причину в первую очередь?'
  explanation: 'Цикл сделал ровно то, о чём его попросили: исполнил инструмент и вернул результат, столько раз, сколько заглушка просила. Раз инструмент отрабатывал и результаты попадали в историю, механика цикла исправна — а вот условие «пора заканчивать» в сценарии не выполнилось ни разу. Такая трасса — диагноз сценарию, и чинят её в ветвлении заглушки.'
  kind: choice
  options:
    - 'В ветвлении подставной модели: условие перехода к финальному текстовому ответу не срабатывает'
    - 'В реализации инструмента `read_file`: он возвращает результат в неправильном формате'
    - 'В диспетчере инструментов: он не удаляет отработавший инструмент из реестра'
    - 'В хранилище сессий: история сохраняется не полностью, поэтому шаги повторяются'
  correct: 0
  id: 006-code-stub-model-3
  fingerprint: 9e553e84
