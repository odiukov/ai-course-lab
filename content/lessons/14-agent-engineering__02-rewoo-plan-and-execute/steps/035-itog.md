---
id: 035-itog
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      Планировщик выдал узел: #E2 = Search[столица #E1]. В какой момент вместо
      #E1 появится настоящий текст?
    options:
      - 'Сразу, пока планировщик пишет план: он подставляет результат по ходу'
      - >-
        В момент отправки узла воркеру, когда результат первого узла уже
        посчитан
      - 'В самом конце, когда Solver собирает ответ из всех свидетельств'
      - 'Никогда: воркер сам понимает, что #E1 надо поискать в переписке'
    correct: 1
    explanation: >-
      План остаётся статичным текстом, каким его написал планировщик.
      Подстановка — детерминированная замена подстроки в копии аргументов прямо
      перед вызовом инструмента, когда нужный результат уже лежит в хранилище
      свидетельств.
  - question: >-
      Почему ReWOO тратит меньше токенов, чем ReAct, на задаче с одинаковым
      набором вызовов инструментов?
    options:
      - Потому что ReWOO делает меньше вызовов инструментов
      - Потому что промпты в ReWOO короче за счёт более сжатых формулировок
      - >-
        Потому что рассуждение и наблюдения не накапливаются в одном промпте:
        планировщик вызывается один раз без наблюдений, а воркер видит только
        свой узел
      - Потому что Solver сжимает историю перед последним вызовом модели
    correct: 2
    explanation: >-
      Дорого не количество вызовов, а повторная отправка растущей истории на
      каждом шаге. У ReWOO истории нет: планировщик работает один раз, воркер
      видит одну строку, Solver — план и свидетельства.
  - question: >-
      Один из узлов плана упал, от него зависят два более поздних узла. Что даёт
      явный план по сравнению с перемешанным циклом ReAct?
    options:
      - >-
        Видно ровно одно место отказа и ровно то, что от него зависит:
        независимые свидетельства остаются годными, перезапускать надо упавший
        узел и его потомков
      - 'Ничего: как и в ReAct, придётся начинать всю задачу с нуля'
      - >-
        Можно просто пропустить упавший узел, Solver достроит ответ по остальным
        свидетельствам
      - >-
        Отказ исправляется сам: диспетчер подбирает другой инструмент для того
        же узла
    correct: 0
    explanation: >-
      Зависимости записаны до исполнения, поэтому деградация идёт по узлам, а не
      по шагам: исключение инструмента становится значением свидетельства,
      независимые ветки доезжают, повторить нужно только повреждённую.
  - question: >-
      Задача такая, что после первых результатов может выясниться: искать надо
      совсем не там. Что тут уместнее?
    options:
      - 'Чистый ReWOO: план всё равно валидируется перед запуском'
      - >-
        Plan-and-Execute: тот же скелет плюс узел-replanner, который после
        исполнения переписывает остаток плана поверх собранных свидетельств
      - 'ReAct: только он умеет менять направление'
      - 'Увеличить число узлов в плане, чтобы покрыть все варианты заранее'
    correct: 1
    explanation: >-
      Валидация проверяет исполнимость графа, а не пригодность плана; чистый
      ReWOO держит план неизменным. Replanner возвращает наблюдения в
      планирование, но обращается к планирующей модели по событию, а не на
      каждом шаге.
---

Урок кончился, и стоит проверить не термины, а сцепку. Дальше в курсе тебе понадобится ровно то, что мы собрали руками: план как DAG с зависимостями ([шаг 7](#step-7)), ссылки `#E1` и момент их подстановки ([шаг 13](#step-13)), порядок запуска по зависимостям ([шаг 18](#step-18)) и границы применимости ([шаг 33](#step-33)). Тот же скелет лежит внутри LangGraph и CrewAI Flows — там ты будешь узнавать его, а не изучать заново.

Спотыкаются обычно на двух местах. Первое: кажется, что планировщик уже знает результаты и подставляет их сам — нет, он пишет текст с дырками. Второе: экономию токенов приписывают числу вызовов, а не отсутствию растущей истории в промпте.

Четыре вопроса ниже бьют именно туда.
