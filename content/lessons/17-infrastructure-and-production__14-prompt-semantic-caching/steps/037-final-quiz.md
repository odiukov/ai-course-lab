---
id: 037-final-quiz
type: quiz
title: 'Итоговая проверка: проектируем двухуровневый кэш и защищаем экономию числами'
source_anchor: '## Exercises'
check:
  - question: >-
      Семантический кэш нашёл безопасное совпадение. Что должно произойти
      дальше?
    options:
      - Ответ возвращается из L1 без вызова LLM
      - 'LLM вызывается с provider cache, чтобы подтвердить ответ'
      - Запрос сразу записывается в provider cache
      - Порог сходства временно снижается
    correct: 0
    explanation: >-
      Безопасное попадание L1 останавливает путь до LLM. Поэтому provider cache
      в этом запросе вообще не участвует.
  - question: >-
      Порог 0.85 даёт 50% попаданий, но иногда возвращает неверные ответы; при
      0.95 попаданий 20%, зато ошибки не обнаружены. Что выбрать для поддержки
      клиентов?
    options:
      - '0.85, потому что доля попаданий важнее точности'
      - Среднее значение 0.90 без дополнительной проверки
      - '0.95, пока измеренная цена неверного попадания выше экономии'
      - '0.85, но учитывать неверные ответы как промахи'
    correct: 2
    explanation: >-
      L1 возвращает ответ без проверки моделью, поэтому ложное попадание
      особенно опасно. Порог защищают ценой ошибки, а не максимальной долей
      попаданий.
  - question: >-
      Дата пользователя стояла в начале промпта и менялась в каждом запросе. Как
      улучшить работу provider cache?
    options:
      - Перенести дату после общего стабильного префикса
      - Продублировать дату в системной инструкции
      - Сократить общий префикс до одного предложения
      - Понизить порог семантического кэша
    correct: 0
    explanation: >-
      Provider cache переиспользует точный общий префикс. Ранняя динамика
      разрывает совпадение, а перенос даты вниз сохраняет стабильную часть.
  - question: >-
      Есть 100 запросов по 1000 входных токенов. L1 останавливает 20 запросов. В
      остальных 80 запросах 250 токенов свежие, а 750 читаются из L2 по
      коэффициенту 0.1. Сколько оплачиваемых эквивалентных входных токенов
      останется, если не учитывать эмбеддинги и выход?
    options:
      - 26 000
      - 32 500
      - 80 000
      - 100 000
    correct: 0
    explanation: >-
      После L1 остаётся 80 вызовов. Каждый стоит как 250 свежих плюс 75
      кэшированных токенов, то есть 325; вместе получается 26 000.
---

Теперь тебе предстоит соединять два вида экономии в одном расчёте: L1 убирает часть вызовов, а L2 удешевляет оставшиеся. Не смешивай их показатели: считай путь запроса из [шага 5](#step-5), отдельно проверяй качество L1 по [шагу 20](#step-20), а стоимость L2 — по [шагу 28](#step-28). Затем проверь расположение динамики, TTL и прогрев параллельной волны. Так обещание «кэш экономит» превращается в проверяемую сумму.

> 🎒 **На пальцах.** В мастерской готовая подходящая вывеска на полке — это L1: станки не включаются. Если готовой нет, сохранённый трафарет ускоряет изготовление — это L2. Но похожая вывеска с чужим адресом не экономия, а брак. Поэтому ты считаешь отдельно готовые работы, повторное использование трафарета и цену ошибки.
