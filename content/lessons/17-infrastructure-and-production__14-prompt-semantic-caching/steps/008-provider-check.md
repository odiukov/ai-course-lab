---
id: 008-provider-check
type: check
title: Выбираем способ включения и проверки provider cache
source_anchor: '### L2 — provider prompt/prefix caching'
check:
  - question: >-
      Ты дважды отправил OpenAI одинаковый промпт длиной 800 токенов, но
      cached_tokens остался равен нулю. Что стоит изменить для проверки?
    options:
      - Добавить явный cache_control к системному сообщению
      - >-
        Увеличить стабильный префикс минимум до 1024 токенов и снова проверить
        cached_tokens
      - Выбрать часовой TTL вместо пятиминутного
    correct: 1
    explanation: >-
      OpenAI включает кэш автоматически только для промптов от 1024 токенов;
      результат проверки виден в usage.cached_tokens.
  - question: >-
      Ты работаешь с Anthropic и хочешь сам выбрать границу кэшируемого
      префикса. Какой способ подходит?
    options:
      - >-
        Поставить cache_control на нужном блоке и повторить запрос с тем же
        префиксом
      - 'Ничего не указывать: Anthropic всегда сам выбирает границу'
      - Проверять только сокращение времени ответа
    correct: 0
    explanation: >-
      У Anthropic граница задаётся явным маркером cache_control, а попадание
      подтверждается данными usage, не ощущением скорости.
---

Теперь тебе нужно связать способ включения с конкретным провайдером: у Anthropic поставить маркер, у OpenAI обеспечить достаточно длинный стабильный префикс, у Gemini использовать отдельный API. Различия разобраны на [шаге 6](#step-6) и [шаге 7](#step-7).

Главная ловушка — принять повторный запрос или быстрый ответ за доказательство попадания. Проверка заканчивается не отправкой двух одинаковых промптов, а просмотром счётчиков `usage`, как на [шаге 3](#step-3). Если нужные кэшированные токены не появились, сначала проверь порог длины, неизменность префикса и способ включения. Кэш считается включённым только тогда, когда это подтвердил ответ провайдера.
