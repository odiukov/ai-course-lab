---
id: 016-proverka-uslovlivaniya
type: check
title: 'Проверка: VAE, текстовые эмбеддинги и cross-attention'
source_anchor: '### The pipeline'
check:
  - question: >-
      Ты хочешь изменить существующее изображение через его латенты. Какой
      маршрут правильно соединяет компоненты?
    options:
      - >-
        Изображение проходит через VAE-энкодер, после чего с полученными
        латентами работает шумоподавитель
      - >-
        Изображение проходит через текстовый энкодер, после чего его эмбеддинги
        поступают в VAE-декодер
      - >-
        К изображению напрямую применяется cross-attention, который превращает
        его в текстовые эмбеддинги
    correct: 0
    explanation: >-
      VAE-энкодер переводит исходное изображение в латентное пространство, где
      работает шумоподавитель. Текстовый энкодер обрабатывает промпт, а не
      изображение.
  - question: >-
      В одной позиции латента модели нужно сильнее учесть токен «красный», чем
      остальные токены промпта. Какой механизм устанавливает эту связь?
    options:
      - VAE-декодер выбирает нужный токен и изменяет его эмбеддинг
      - >-
        Cross-attention сопоставляет позицию латента с текстовыми эмбеддингами и
        распределяет внимание между ними
      - >-
        Текстовый энкодер заранее закрепляет каждый токен за постоянной областью
        изображения
    correct: 1
    explanation: >-
      Cross-attention позволяет разным позициям латента по-разному учитывать
      токены промпта. Текстовый энкодер создаёт смысловые представления токенов,
      но не назначает им области изображения.
---

Проверь, не перепутались ли у тебя направления: VAE связывает изображение с латентами, текстовый энкодер превращает токены в эмбеддинги, а cross-attention даёт позициям латента обращаться к этим эмбеддингам. Частая ошибка — приписать текстовому энкодеру выбор места для объекта. Он лишь подготавливает смысловое представление; связь с конкретными позициями возникает внутри шумоподавителя, как разобрано на [шаге 14](#step-14).

> 🎒 **На пальцах.** Представь сборщика мозаики с коробкой деталей и письменным заданием. Упаковщик VAE переводит большую картину в компактный набор деталей, переводчик готовит понятные карточки задания, а cross-attention помогает сборщику у каждой ячейки сверяться именно с подходящими карточками. У каждого своя работа — поэтому их нельзя безболезненно поменять местами.
