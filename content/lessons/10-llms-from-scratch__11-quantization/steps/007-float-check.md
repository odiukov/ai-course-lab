---
id: 007-float-check
type: check
title: Выбираем между FP16 и BF16 для весов и градиентов
source_anchor: '### Number Formats: What Each Bit Does'
check:
  - question: >-
      Веса держатся в узком коридоре около нуля, а градиенты то проваливаются до
      крошечных величин, то резко растут. Как раздать два формата одинаковой
      ширины — один с длинной мантиссой, другой с длинной экспонентой?
    options:
      - 'Обоим тензорам формат с длинной мантиссой: точность важнее'
      - 'Весам — формат с длинной мантиссой, градиентам — с длинной экспонентой'
      - 'Весам — формат с длинной экспонентой, градиентам — с длинной мантиссой'
    correct: 1
    explanation: >-
      FP16 (длинная мантисса) даёт весам более точные значения в их узком
      диапазоне, а широкая экспонента BF16 не даёт градиентам обнулиться или
      переполниться.
  - question: >-
      Почему перевод градиентов в формат с более широкой экспонентой, но той же
      общей шириной, не уменьшит занимаемую ими память?
    options:
      - >-
        Ширина обоих форматов одинакова, меняется только раздача битов между
        экспонентой и мантиссой
      - Формат с широкой экспонентой хранит дополнительную копию экспоненты
      - 'Память зависит только от диапазона значений, а не от формата'
    correct: 0
    explanation: >-
      FP16 и BF16 весят одинаково — по два байта на параметр. Отличается только
      распределение битов внутри числа: диапазон против точности.
---

Здесь важно не спутать две разные задачи. Для весов, которые обычно находятся в сравнительно узком диапазоне, FP16 сохраняет больше точности. Для градиентов опаснее потерять очень малое или слишком большое значение, поэтому BF16 с широким диапазоном надёжнее. Это тот же компромисс из [шага 6](#step-6), применённый к конкретным данным модели. При этом экономии памяти между форматами нет: оба занимают 16 бит.
