---
id: 026-channel-check
type: check
title: Проверяем формы scales и восстановленного тензора для обеих осей
source_anchor: '### Step 2: Symmetric Quantization (Per-Tensor and Per-Channel)'
check:
  - question: Матрица имеет 2 строки и 3 столбца. Какие формы получатся при axis=0?
    options:
      - 'scales: 2 числа; восстановленный тензор: 2 строки на 3 столбца'
      - 'scales: 3 числа; восстановленный тензор: 2 строки на 3 столбца'
      - 'scales: 2 числа; восстановленный тензор: 2 строки на 1 столбец'
    correct: 0
    explanation: >-
      При axis=0 отдельный scale назначается каждой строке, поэтому scales
      содержит 2 числа. Broadcasting возвращает результат к исходной форме 2 на
      3.
  - question: >-
      Для той же матрицы выбрали axis=1. Какая форма scales подходит для
      восстановления?
    options:
      - Одно число
      - 2 числа
      - 3 числа
    correct: 2
    explanation: >-
      При axis=1 масштаб назначается каждому из трёх столбцов, поэтому нужны 3
      значения scales.
---

Здесь ты проверяешь стыковку функций из [шагов 23](#step-23)–[25](#step-25). Для матрицы из 2 строк и 3 столбцов `axis=0` даёт два масштаба — по одному на строку, а `axis=1` даёт три — по одному на столбец. После восстановления форма всегда остаётся исходной: 2 на 3. Частая ловушка — ожидать, что номер оси совпадёт с длиной `scales`; в нашей функции он обозначает каналы, а максимум берётся по другой оси.

> 🎒 **На пальцах.** Представь два стеллажа по три коробки. Построчный режим выдаёт по одному ценнику каждому стеллажу — всего два. Постолбцовый выдаёт ценник каждой позиции слева направо — всего три. Ценников меньше, чем коробок, но после их применения все шесть коробок остаются на месте.
