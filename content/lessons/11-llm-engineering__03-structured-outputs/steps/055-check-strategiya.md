---
id: 055-check-strategiya
type: check
title: 'Проверка: подбери стратегию под задачу'
source_anchor: '## Ship It'
check:
  - question: >-
      У тебя десять разных схем: заявка на возврат, жалоба, запрос счёта и так
      далее. Модель должна сама определить, какая подходит к письму, и заполнить
      её. Что выбрать?
    options:
      - Одну огромную схему со всеми полями сразу и oneOf между вариантами
      - Набор инструментов с разными input_schema и дать модели выбрать вызов
      - JSON mode плюс просьба в промпте указать название схемы в поле type
    correct: 1
    explanation: >-
      Когда выбор схемы — часть задачи, инструменты решают её штатно: модель
      называет функцию и сразу заполняет только её аргументы.
  - question: >-
      Провайдер даёт только JSON mode — гарантию, что придёт разбираемый JSON, и
      ничего больше. Схема простая: три поля и цена от нуля. Что обязательно
      нужно в коде?
    options:
      - 'Ничего, JSON mode уже покрывает эти требования'
      - Только повторный запрос при ошибке разбора
      - Свой валидатор схемы и цикл повторов с текстом ошибки
    correct: 2
    explanation: >-
      JSON mode отвечает за синтаксис, а не за ключи, типы и minimum — эту часть
      контракта проверяешь ты сам и возвращаешь жалобу модели.
---

Ты прошёл весь спектр — от таблички «отвечай в JSON» до маскировки токенов ([шаг 7](#step-7)) — и теперь у тебя не один правильный ответ, а вилка. Проверь, умеешь ли ты по описанию задачи назвать стратегию: что даёт провайдер, насколько сложна схема, входит ли выбор схемы в саму задачу и что произойдёт при отказе.

Спотыкаются обычно на двух местах. Первое: считают, что раз провайдер что-то гарантирует, валидатор и цикл повторов ([шаг 39](#step-39)) больше не нужны — а гарантия почти всегда уже́, чем контракт. Второе: тянут одну гигантскую схему туда, где задача — выбрать одну из десяти, и удивляются, что модель путается.
