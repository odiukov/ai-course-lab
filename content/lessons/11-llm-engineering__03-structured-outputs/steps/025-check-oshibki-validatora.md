---
id: 025-check-oshibki-validatora
type: check
title: 'Проверка: что скажет валидатор'
source_anchor: '### Step 1: JSON Schema Validator'
check:
  - question: >-
      Схема требует поле count типа integer. Приходит объект, где count равен
      true. Что вернёт валидатор?
    options:
      - >-
        Пустой список: в Python логическое значение считается разновидностью
        целого
      - 'Жалобу вида: count — ожидалось integer, получено bool'
      - 'Жалобу про minimum: true будет проверено как единица'
      - Исключение TypeError при проверке типа
    correct: 1
    explanation: >-
      В ветке integer логические значения исключаются отдельной проверкой.
      Поэтому true не проходит как целое число и добавляется ошибка типа.
  - question: >-
      В объекте одновременно нет обязательного поля email, а значение age меньше
      заданного minimum. Что окажется в результате валидации?
    options:
      - Только первая обнаруженная жалоба — об отсутствии email
      - 'Обе жалобы: об отсутствии email и о значении age ниже minimum'
      - >-
        Только жалоба про age, потому что пропущенные поля проверяет другая
        функция
      - 'Пустой список, потому что пропуск поля отменяет остальные проверки'
    correct: 1
    explanation: >-
      Валидатор складывает найденные несоответствия в общий список и продолжает
      обход, поэтому возвращает обе жалобы.
---

Ты дописал валидатор и прогнал его на пяти случаях из [шага 24](#step-24). Теперь проверим главное: умеешь ли ты, глядя на пару «объект + схема», заранее сказать, что окажется в списке жалоб — до запуска.

Спотыкаются обычно на трёх вещах. Первая: ждут исключения, а функция возвращает список, и пустой список — это и есть «всё хорошо». Вторая: думают, что проверка останавливается на первой ошибке, — она не останавливается, обход дерева идёт до конца. Третья: забывают про `True` из [шага 22](#step-22), где логическое значение маскируется под целое.

Ответь на два вопроса, не подглядывая в код.
