---
id: 058-itog
type: quiz
title: Итоговая проверка
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты добавил в промпт «Отвечай строго в JSON по схеме» и получил валидный
      JSON десять раз подряд. Что это доказывает?
    options:
      - 'Схема соблюдается, промпт работает как контракт'
      - >-
        Ничего про гарантию: на каждом шаге генерации словарь по-прежнему
        открыт, и одиннадцатый ответ может уехать в преамбулу или обрезаться
      - Модель запомнила формат и дальше будет его держать
    correct: 1
    explanation: >-
      Промпт влияет на вероятности, но не убирает невалидные токены из словаря.
      Десять удач подряд — статистика, а не забор.
  - question: 'Ты включил JSON mode и получил ответ. Что гарантировано, а что нет?'
    options:
      - >-
        Гарантировано, что текст разберётся парсером; ключи, типы и ограничения
        не гарантированы
      - 'Гарантировано, что все обязательные поля на месте и типы верные'
      - >-
        Гарантировано, что нет заборчика из тройных кавычек, но JSON может быть
        битым
    correct: 0
    explanation: >-
      JSON mode отвечает за синтаксис. Форму — ключи, типы, minimum, enum —
      задаёт только схема.
  - question: >-
      Валидатор пропустил объект: все поля на месте, типы верные, цена больше
      нуля. Товар при этом называется «Товар 1», а цена — 9999. Что произошло?
    options:
      - 'Валидатор написан с ошибкой: такие значения он должен отсекать'
      - >-
        Схема проверяет форму, а не правду — заглушки и выдуманные значения
        проходят её насквозь
      - 'Нужен более строгий тип для строки, и заглушки перестанут проходить'
    correct: 1
    explanation: >-
      Ограничения схемы говорят про структуру и границы. Осмысленность значений
      ловят размеченные примеры и флаг на ручную проверку.
  - question: >-
      У тебя десять разных схем, и заранее неизвестно, какая нужна под запрос
      пользователя. Что тут подходит лучше?
    options:
      - Одна большая схема с oneOf и constrained decoding
      - 'Tool use: дать модели десять инструментов и позволить ей выбрать вызов'
      - 'Retry loop: гонять по кругу, пока одна из схем не сойдётся'
    correct: 1
    explanation: >-
      Когда выбор схемы сам является решением, его логичнее отдать модели как
      выбор инструмента, а не зашивать в один контракт.
---

Урок начался с одного разрыва: модель отдаёт текст, а приложению нужно поле `price` как число. Дальше ты видел, что промпт этот разрыв не закрывает — [шаг 6](#step-6), — потому что проблема живёт на этапе декодирования, где словарь открыт целиком.

Дальше в курсе всё это работает как воронка: контракт (JSON Schema), забор на входе (JSON mode, schema mode, constrained decoding), парсер и валидатор на выходе, повторы как последний рубеж. Когда будешь строить агентов и пайплайны, именно это решает, падает система на битом ответе или продолжает работать.

Два места, где спотыкаются чаще всего. Первое: путают синтаксис и форму — «JSON валидный, значит схема соблюдена». Второе: ждут от схемы правды. Валидная цена `9999` у «Товара 1» пройдёт любой валидатор — форма идеальна, смысла нет.

> 🎒 **На пальцах.** Замок на двери, охрана на входе, камера в коридоре, журнал на выходе. Ни один из четырёх не заменяет остальные: замок держит форму, охрана проверяет пропуск, журнал ловит то, что уже прошло. Ты не выбираешь один — ты решаешь, какой рубеж дешевле поставить в твоей задаче.
