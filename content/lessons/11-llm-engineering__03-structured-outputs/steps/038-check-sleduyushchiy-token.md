---
id: 038-check-sleduyushchiy-token
type: check
title: 'Проверка: что допустимо после этого куска'
source_anchor: '### Step 3: Constrained Token Filter'
check:
  - question: >-
      Модель уже выдала строку {"product": "Sony", и делает следующий шаг. Какой
      набор продолжений здесь допустим?
    options:
      - >-
        Пробел, кавычка, открывающая фигурная скобка, открывающая квадратная
        скобка
      - 'Пробел, кавычка, закрывающая фигурная скобка — объект можно и завершить'
      - 'Только кавычка: после запятой обязано идти имя следующего ключа'
      - >-
        Любой токен: запятая ничего не ограничивает, ограничения начнутся после
        двоеточия
    correct: 0
    explanation: >-
      Запятая — обещание, что дальше будет ещё один элемент, поэтому закрыть
      объект нельзя. Остаются пробел, кавычка ключа и скобки для вложенного
      объекта или массива.
  - question: >-
      На вход пришла строка {"product": "Sony", "price": 348}. Почему фильтр
      возвращает EOS, а не варианты для последнего символа — закрывающей
      фигурной скобки?
    options:
      - Потому что закрывающая фигурная скобка всегда означает конец генерации
      - Потому что схема из двух полей исчерпана и больше писать нечего
      - >-
        Потому что строка целиком разобралась как JSON, и проверка на
        завершённость идёт раньше разбора по последнему символу
      - >-
        Потому что после закрывающей фигурной скобки других допустимых символов
        не существует
    correct: 2
    explanation: >-
      Сначала делается попытка разобрать всю строку. Она удалась — значит JSON
      завершён, и остаётся только остановиться. До веток по последнему символу
      дело не доходит.
---

После этого куска ты должен уметь взять любую недописанную строку JSON, посмотреть на её хвост и назвать, что физически может идти дальше, — а заодно объяснить, почему остальные сто тысяч токенов в этот момент запрещены.

Спотыкаются обычно на двух вещах. Первая: проверка «а не готов ли JSON целиком» идёт до разбора хвоста, и одна и та же закрывающая скобка означает разное в зависимости от того, закрылись ли все скобки до неё. Вторая: запятая и закрывающая скобка — не взаимозаменяемые концовки. После запятой объект закрыть уже нельзя, обещание нового элемента придётся выполнить.
