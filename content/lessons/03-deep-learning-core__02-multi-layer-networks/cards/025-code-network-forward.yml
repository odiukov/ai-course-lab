- concept: результат сети возвращается только после обработки всех слоёв
  question: Заполни пропуск так, чтобы функция вернула результат всей цепочки слоёв.
  explanation: '`return current` расположен после цикла, поэтому функция сначала пропускает данные через все слои. Если поместить `return` в тело цикла, выполнение завершится после первого слоя.'
  kind: cloze
  template: |-
    def network_forward(inputs, layers):
        current = inputs
        for layer in layers:
            current = layer.forward(current)
        ___
  answer: return current
  accept:
    - return current
  id: 025-code-network-forward-1
  fingerprint: 7101b7b7
- concept: пустая последовательность слоёв задаёт тождественное преобразование
  question: Что вернёт прямой проход сети, если список её слоёв пуст?
  explanation: '`current` изначально равен входу. Для пустого списка тело цикла не выполняется, поэтому функция возвращает исходные данные.'
  kind: choice
  options:
    - Исходный вход без изменений
    - Значение None
    - Возникнет ошибка из-за отсутствия слоёв
  correct: 0
  id: 025-code-network-forward-2
  fingerprint: 18a64e0f
- concept: Network.forward отвечает за композицию слоёв, а вычисления отдельного преобразования инкапсулированы в Layer.forward
  question: Почему метод прямого прохода сети может не содержать формул для матричных вычислений и функций активации?
  explanation: 'Разделение обязанностей позволяет сети одинаково соединять разные виды слоёв: внутренняя математика остаётся в слоях, а сеть управляет только потоком данных между ними.'
  kind: open
  reference: Каждый слой сам реализует своё преобразование в методе forward. Сеть лишь последовательно вызывает эти методы и передаёт результат одного вызова следующему слою.
  id: 025-code-network-forward-3
  fingerprint: a3cd161f
