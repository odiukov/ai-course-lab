---
id: 042-random-width
type: check
title: 'Проверка: что меняется при ширине скрытого слоя 2, 8 и 32'
source_anchor: '## Exercises'
check:
  - question: >-
      В одном запуске сеть с шириной скрытого слоя 32 показала больший разброс
      выходов, чем сеть с шириной 8. Какой вывод обоснован?
    options:
      - Ширина 32 при любой инициализации даёт больший разброс
      - >-
        Наблюдение совместимо с влиянием ширины, но одного случайного запуска
        недостаточно для общего вывода
      - 'Больший разброс доказывает, что сеть уже научилась распознавать круг'
    correct: 1
    explanation: >-
      Выходной нейрон смешивает разное число скрытых сигналов, поэтому ширина
      может влиять на распределение. Однако конкретные значения определяются и
      случайными весами, а прямой проход без обучения не подтверждает качество
      классификации.
  - question: >-
      Что непосредственно меняется в архитектуре при увеличении ширины скрытого
      слоя с 2 до 32 нейронов?
    options:
      - 'Выходной слой получает больше скрытых сигналов, которые может объединить'
      - Порог классификации автоматически подстраивается под данные
      - Диапазон значений сигмоиды расширяется за пределы от 0 до 1
    correct: 0
    explanation: >-
      Каждый скрытый нейрон формирует отдельный сигнал для выходного слоя.
      Увеличение ширины добавляет такие сигналы, но не меняет диапазон сигмоиды
      и само по себе не настраивает порог или параметры.
---

Возьми точки задачи с кругом из [шага 39](#step-39) и трижды запусти прямой проход: со скрытой шириной 2, 8 и 32. Диапазон и распределение выходов могут различаться, потому что выходной нейрон смешивает разное число скрытых сигналов.

Здесь легко принять случайность за свойство архитектуры. Начальные числа выбираются случайно, как разобрано в [шаге 40](#step-40), поэтому ширина 32 не обязана в каждом запуске давать больший разброс или лучшие ответы. Сравни несколько инициализаций: ширина задаёт возможности сети, а конкретный случайный запуск показывает лишь одну их реализацию.

> 🎒 **На пальцах.** Представь витраж из 2, 8 или 32 случайно окрашенных стёкол. Больше стёкол позволяет собрать более сложный оттенок, но не гарантирует, что он окажется ярче или красивее. Итог зависит и от числа стёкол, и от цветов, которые случайно попались.
