---
id: 022-shape-bug
type: check
title: 'Проверка: почему матрица (2, 3) не подходит входу из двух чисел'
source_anchor: '### Matrix Dimensions'
check:
  - question: 'Почему матрицу весов формы (2, 3) нельзя умножить на вход формы (2,)?'
    options:
      - Число строк матрицы должно совпадать с длиной входа
      - Число столбцов матрицы не совпадает с длиной входа
      - Результат матрично-векторного умножения обязан быть скаляром
    correct: 1
    explanation: >-
      В произведении W x число столбцов W должно совпадать с длиной x. Здесь
      матрица формы (2, 3) ожидает три входных компонента, а вектор содержит
      две, поэтому возникает ошибка размерности.
  - question: >-
      Как интерпретируются строки и столбцы матрицы весов слоя, который
      преобразует вход из двух компонентов в три выхода?
    options:
      - 'Строки соответствуют выходам слоя, а столбцы — компонентам входа'
      - 'Строки соответствуют компонентам входа, а столбцы — выходам слоя'
      - 'И строки, и столбцы соответствуют только выходам слоя'
    correct: 0
    explanation: >-
      Каждая строка содержит веса одного выходного нейрона, а каждый столбец
      соответствует одной компоненте входа. Поэтому для трёх выходов и двух
      входов нужна матрица формы (3, 2).
---

Здесь легко принять две строки за два входа. Но по правилу из [шага 16](#step-16) входам соответствуют столбцы. В матрице формы (2, 3) каждая строка содержит три веса и ждёт три числа. Во входе их только два, поэтому третьему весу не находится пары и вычисление останавливается с ошибкой размерности.

Для трёх нейронов и двух входов нужна форма (3, 2): три строки создают три результата, два столбца принимают два числа.

> 🎒 **На пальцах.** Представь два бланка, и в каждом по три пустых поля. Ты принёс только два значения. Заполнить оба бланка не получится: в каждом останется пустым третье поле. Если же взять три бланка по два поля, твои два значения подойдут каждому, а на выходе получатся три заполненных бланка.
