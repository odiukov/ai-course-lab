---
id: 002-xor-predel
type: check
title: 'Проверка: почему настройка весов не спасает один нейрон на XOR'
source_anchor: '## The Problem'
check:
  - question: >-
      Ты много раз меняешь веса и смещение линейного порогового нейрона, но XOR
      всё равно распознаётся с ошибками. Что лучше всего объясняет результат?
    options:
      - >-
        Настройка параметров выбирает разные линейные решения, но ни одно из них
        не реализует XOR полностью
      - Веса для XOR обязательно должны быть целыми и положительными
      - 'Нужно продолжать обучение на тех же примерах, пока ошибка не исчезнет'
    correct: 0
    explanation: >-
      Проблема не в точности настройки: веса и смещение меняют конкретную
      линейную границу, но не возможности самого нейрона. В пределах этой
      архитектуры безошибочного решения XOR нет.
  - question: >-
      После настройки линейный пороговый нейрон правильно классифицирует три
      случая из таблицы XOR, а в одном ошибается. Что следует из этого
      результата?
    options:
      - >-
        Параметры уже почти найдены, поэтому малая поправка обязательно устранит
        последнюю ошибку
      - >-
        Частичный успех не означает существования безошибочных параметров:
        исправление оставшегося случая приведёт к ошибке в другом
      - 'Один из целевых ответов XOR, вероятно, указан неверно'
    correct: 1
    explanation: >-
      Линейный нейрон может правильно классифицировать часть комбинаций XOR, но
      не все одновременно. Поэтому результат с тремя верными случаями не
      является промежуточной точкой на пути к идеальному решению.
---

На [шаге 1](#step-1) ты увидел ограничение одного нейрона. Теперь важно не спутать невозможность с неудачной настройкой: веса и смещение действительно можно крутить, но они лишь поворачивают и передвигают ту же границу. Если три случая XOR распознаны верно, возникает соблазн ещё чуть-чуть поправить числа. Однако исправление четвёртого случая заставит прямую пересечь уже правильно разделённую пару.

Здесь мешают не плохие веса, а возможности самого инструмента.

> 🎒 **На пальцах.** Представь шлагбаум на парковке: ты можешь развернуть его или перенести, но он останется одной прямой перекладиной. Если разрешённые и запрещённые места чередуются, никакая особенно точная установка шлагбаума не разделит их правильно. Нужна не более старательная настройка, а другой способ разделения.
