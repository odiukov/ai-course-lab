---
id: 034-xor-11
type: check
title: 'Проверка: считаем прохождение входа [1, 1] через XOR'
source_anchor: '### Step 4: XOR with Hand-Tuned Weights'
check:
  - question: >-
      Для входа [1, 1] первый скрытый нейрон активирован почти полностью, а
      второй сохраняет лишь малую положительную активацию. Почему выход сети всё
      равно остаётся ниже порога?
    options:
      - Выходная сигмоида полностью отбрасывает малые положительные входы
      - >-
        Отрицательное смещение выходного нейрона перевешивает взвешенные скрытые
        активации
      - Выходной нейрон повторно применяет веса скрытого слоя
    correct: 1
    explanation: >-
      Выходная предактивация равна 20×h₁ + 20×h₂ − 30. При h₁ почти 1 и h₂ почти
      0 отрицательное смещение оставляет её около −10, поэтому сигмоида даёт
      значение ниже 0.5.
  - question: >-
      Что произойдёт, если перед выходным слоем округлить скрытые активации для
      входа [1, 1] до точных логических значений?
    options:
      - >-
        Немного изменится мягкий выход, но итоговая метка в этом примере
        сохранится
      - Мягкий выход и итоговая метка обязательно останутся в точности прежними
      - >-
        Сеть выдаст противоположную метку, потому что второй скрытый выход
        станет нулём
    correct: 0
    explanation: >-
      Без округления второй скрытый нейрон вносит малую положительную добавку,
      поэтому мягкий результат немного отличается от расчёта с точными 1 и 0.
      Однако оба результата остаются значительно ниже порога 0.5 и дают одну
      метку.
---

Проверь, можешь ли ты вручную проследить весь путь входа `[1, 1]` через веса из [шага 31](#step-31): сначала получить два скрытых значения, затем передать именно их выходному нейрону и только после этого применить порог из [шага 28](#step-28).

Чаще всего здесь спотыкаются дважды: подставляют в выходной слой исходные единицы вместо результатов скрытого слоя или округляют скрытые значения слишком рано. Считай их как «почти 1» и «почти 0», но помни: до порога сеть всё ещё работает с мягкими числами.
