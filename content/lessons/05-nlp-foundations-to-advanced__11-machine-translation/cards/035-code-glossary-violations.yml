- concept: глоссарное правило применимо только при наличии исходного термина и нарушено при отсутствии обязательного перевода
  question: 'Дан глоссарий `{ "Checkout": "Paiement", "Cart": "Panier" }`. Исходный текст: `Open cart before checkout`. Перевод: `Ouvrez le panier avant de régler`. Какой результат должна вернуть проверка без учёта регистра?'
  explanation: Оба исходных термина присутствуют. Обязательный вариант `Panier` есть в переводе, а `Paiement` отсутствует, поэтому нарушено только требование для `Checkout`.
  kind: choice
  options:
    - '[["Checkout", "Paiement"]]'
    - '[["Cart", "Panier"]]'
    - '[["Checkout", "Paiement"], ["Cart", "Panier"]]'
    - '[]'
  correct: 0
  id: 035-code-glossary-violations-1
  fingerprint: 8c72a263
- concept: регистронезависимое сравнение через нормализованные копии строк
  question: Заполни пропуск так, чтобы подготовить нормализованные копии строк для регистронезависимого поиска, не изменяя аргументы функции.
  explanation: '`casefold()` создаёт нормализованные строки и подходит для регистронезависимого сравнения Unicode-текста. Исходные `source` и `translation` при этом остаются неизменными; `lower()` также удовлетворяет базовому требованию шага.'
  kind: cloze
  template: source_norm, translation_norm = (text.___ for text in (source, translation))
  answer: casefold()
  accept:
    - lower()
  id: 035-code-glossary-violations-2
  fingerprint: 9675549c
