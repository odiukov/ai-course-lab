---
id: 011-generation-check
type: check
title: 'Проверка: токенизатор, forced BOS и параметры генерации'
source_anchor: '### Step 1: a pretrained MT call'
check:
  - question: >-
      Ты переводишь английское предложение на французский. Какая конфигурация
      NLLB согласована правильно?
    options:
      - >-
        src_lang задан как eng_Latn, а forced_bos_token_id получен из токена
        fra_Latn
      - >-
        src_lang задан как fra_Latn, а forced_bos_token_id получен из токена
        eng_Latn
      - 'И src_lang, и forced_bos_token_id соответствуют fra_Latn'
    correct: 0
    explanation: >-
      src_lang описывает язык и письменность исходного текста, а
      forced_bos_token_id задаёт целевой язык декодирования.
  - question: >-
      Ты уменьшил num_beams с 5 до 1, не меняя forced_bos_token_id. Какой
      результат ожидается?
    options:
      - Модель обязательно начнёт отвечать на языке исходного текста
      - >-
        Целевой язык сохранится, но декодер перестанет сравнивать несколько
        лучей поиска
      - Токенизатор начнёт применять правила целевого языка к исходному тексту
    correct: 1
    explanation: >-
      num_beams управляет шириной поиска, тогда как целевой язык фиксируется
      forced_bos_token_id. Изменение ширины поиска не меняет языковую настройку.
---

Во вызове из [шага 10](#step-10) легко перепутать две языковые настройки: `src_lang` описывает вход, а `forced_bos_token_id` фиксирует язык выхода. Параметры генерации не исправляют ошибочный код языка: `num_beams` и `length_penalty` меняют выбор перевода, как разобрано в [шаге 9](#step-9), а `max_new_tokens` лишь ограничивает его длину.

Главная ловушка — пытаться одной ручкой управлять всем сразу.

> 🎒 **На пальцах.** Представь диспетчера поезда: одна табличка сообщает, с какой станции прибыл состав, другая фиксирует станцию назначения. Число проверяемых маршрутов и лимит вагонов влияют на поездку, но не заменяют ни отправление, ни пункт назначения.
