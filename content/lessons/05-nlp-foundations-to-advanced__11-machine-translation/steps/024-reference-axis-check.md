---
id: 024-reference-axis-check
type: check
title: 'Проверка: не перепутаны ли оси hypotheses и references'
source_anchor: '### Step 2: BLEU and chrF'
check:
  - question: >-
      Ты оцениваешь две гипотезы, для каждой из которых подготовлен один эталон.
      Как правильно передать данные в sacrebleu?
    options:
      - >-
        hypotheses — список из двух переводов; references — внешний список с
        одним внутренним списком из двух эталонов
      - >-
        hypotheses — внешний список с одним внутренним списком из двух
        переводов; references — список из двух эталонов
      - >-
        hypotheses — список из двух переводов; references — два внутренних
        списка, каждый с одним эталоном
    correct: 0
    explanation: >-
      hypotheses представляет один поток переводов. Внешний список references
      перечисляет потоки эталонов, поэтому единственный поток должен содержать
      два эталона, выровненных с гипотезами.
  - question: >-
      Для каждой из двух гипотез есть по два варианта эталона. Какая структура
      references сохраняет правильные оси?
    options:
      - >-
        Два внутренних списка: в первом оба эталона первой гипотезы, во втором
        оба эталона второй
      - Один внутренний список из всех четырёх эталонов
      - >-
        Два внутренних списка-потока: каждый содержит сначала эталон первой
        гипотезы, затем эталон второй
    correct: 2
    explanation: >-
      Внешняя ось references перечисляет варианты эталона, а внутренняя —
      позиции корпуса. Поэтому каждый внутренний список должен быть отдельным
      потоком, выровненным с hypotheses.
---

Перед запуском метрики посмотри не только на количество строк, но и на смысл каждой оси. В `hypotheses` одна позиция соответствует одному переведённому фрагменту. В `references`, как показано на [шаге 23](#step-23), внешний список перебирает варианты эталона, а внутренний — фрагменты корпуса в том же порядке, что и гипотезы.

Частая ловушка — сгруппировать эталоны по фрагментам: форма данных выглядит аккуратно, но оси уже поменялись местами. Проверяй их словами: «поток эталонов, затем позиции».

> 🎒 **На пальцах.** Представь несколько стопок контрольных листов. В каждой стопке лежит один вариант правильных ответов для всей группы учеников. Если вместо этого собрать в стопку все варианты ответа одного ученика, число листов не изменится, но проверка сопоставит не те строки.
