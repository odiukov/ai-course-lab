---
id: 035-scale-check
type: check
title: Выберите правильное направление scaling factor при encode и decode
source_anchor: '## Pitfalls'
check:
  - question: >-
      Как правильно применить scaling factor при переходе между пространством
      VAE и рабочим пространством диффузионной модели?
    options:
      - >-
        После encode умножить латент на scaling factor, а перед decode разделить
        на него
      - >-
        После encode разделить латент на scaling factor, а перед decode умножить
        на него
      - 'И после encode, и перед decode умножить латент на scaling factor'
      - Применять scaling factor только к предсказанному U-Net шуму
    correct: 0
    explanation: >-
      После кодирования выполняют z = E(x) × scaling_factor. Перед
      декодированием применяют обратную операцию: x = D(z / scaling_factor).
  - question: >-
      Почему деление результата encode на scaling factor меньше единицы вместо
      умножения нарушает работу диффузионной модели?
    options:
      - >-
        Модули значений и разброс латентов увеличиваются, поэтому их масштаб не
        соответствует обучению U-Net
      - >-
        Число каналов латента уменьшается, поэтому U-Net получает тензор другой
        формы
      - >-
        Латент теряет пространственные позиции, поэтому декодер не может
        восстановить разрешение
      - Добавленный диффузионный шум автоматически становится равен нулю
    correct: 0
    explanation: >-
      Деление на 0.18215 увеличивает значения примерно в 5.5 раза, тогда как
      после encode их следовало уменьшить умножением. В результате U-Net
      получает латенты с сильно завышенным разбросом.
---

Тебе нужно без подсказки восстановить направление двух операций из [шага 34](#step-34). Частая ловушка — увидеть число меньше единицы и решить, что деление «уменьшает» латент. Наоборот: деление на такое число увеличивает значения. Смотри не на название операции, а на маршрут: после encode латент приводят к рабочему масштабу диффузии, перед decode возвращают в масштаб VAE обратной операцией.

> 🎒 **На пальцах.** Представь пропускной пункт с жетонами. На входе в рабочую зону кассир пересчитывает номинал по фиксированному коэффициенту — умножает. На выходе нужно восстановить исходный номинал, поэтому кассир делает обратное — делит. Если перепутать окошки, жетоны станут не того масштаба, хотя их количество не изменится.
