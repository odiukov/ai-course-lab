- concept: поэлементное умножение тензора на скаляр сохраняет его форму и порядок элементов
  question: Декодер умножает каждый элемент латентного тензора на один и тот же скаляр. Что при этом происходит со структурой тензора?
  explanation: 'Операция z * 2.0 применяется поэлементно: она меняет значения, но не индексы, число осей или размеры тензора.'
  kind: choice
  options:
    - Форма и порядок элементов сохраняются, изменяются только значения
    - Добавляется новая ось
    - Элементы переставляются в обратном порядке
    - Все оси объединяются в одну
  correct: 0
  id: 008-decode-1
  fingerprint: ed56abae
- concept: умножение на скаляр является линейным преобразованием
  question: Заполни пропуск в равенстве, которое выражает линейность декодера.
  explanation: Для decode(z) = 2.0 * z умножение распределяется по сумме и совместимо со скалярными коэффициентами, поэтому такое отображение линейно.
  kind: cloze
  template: decode(alpha * u + beta * v) = ___
  answer: alpha * decode(u) + beta * decode(v)
  accept:
    - alpha*decode(u)+beta*decode(v)
    - beta * decode(v) + alpha * decode(u)
  id: 008-decode-2
  fingerprint: 1199d9da
