---
id: 023-normalizovannye-vektory
type: check
title: 'Проверка: когда dot и cosine дают одинаковый рейтинг'
source_anchor: '### Dot Product Similarity vs Cosine Similarity'
check:
  - question: >-
      Запрос и все объекты имеют L2-норму 1. Как dot и cosine упорядочат
      объекты?
    options:
      - 'Одинаково, и оценки тоже совпадут'
      - 'Одинаково, но оценки обязательно будут разными'
      - Dot всегда поставит первым самый популярный объект
    correct: 0
    explanation: >-
      При единичных длинах множители длины исчезают, поэтому dot и cosine дают
      одинаковые оценки и один рейтинг.
  - question: >-
      Все объекты имеют одинаковую длину, а запрос не нормирован. Что произойдёт
      с рейтингом?
    options:
      - Cosine полностью изменит порядок объектов
      - 'Порядок совпадёт, хотя сами оценки могут различаться'
      - Dot не сможет сравнить объекты
    correct: 1
    explanation: >-
      Длина запроса одинакова для всех сравнений, а равные длины объектов не
      дают никому преимущества. Общий множитель меняет оценки, но не порядок.
---

Ты уже видел в [шаге 22](#step-22), что dot учитывает длину, а cosine её убирает. Теперь проверь важное исключение: если сравниваемые векторы имеют одинаковую длину, преимущество исчезает и рейтинг совпадает. При L2-норме 1 совпадают даже сами оценки.

Обычно здесь путают равенство чисел и равенство порядка. Ненормированный запрос может умножить все dot-оценки на один общий коэффициент: числа изменятся, но места объектов — нет.

> 🎒 **На пальцах.** Представь забег, где секундомер каждого участника по ошибке показывает время вдвое больше. Значения неверны, но порядок финиша сохраняется: один и тот же множитель применён ко всем. Так же общая длина запроса может изменить оценки dot, не меняя рейтинг относительно cosine.
