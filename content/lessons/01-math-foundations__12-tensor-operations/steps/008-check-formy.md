---
id: 008-check-formy
type: check
title: 'Проверка: прочитать форму (16, 12, 128, 64)'
source_anchor: '### Tensor shapes in deep learning'
check:
  - question: >-
      Почему вторую ось тензора (16, 12, 128, 64) следует интерпретировать как
      головы внимания, а не как высоту изображения?
    options:
      - 'Потому что смысл оси задаёт раскладка (B, H, T, D) для тензора внимания'
      - >-
        Потому что вторая ось любого четырёхмерного тензора всегда обозначает
        головы
      - Потому что по величине второй оси можно однозначно определить тип данных
      - Потому что высота изображения всегда записывается последней
    correct: 0
    explanation: >-
      Числа сами по себе не раскрывают смысл осей. Для тензора внимания
      используется раскладка (B, H, T, D), поэтому вторая ось H обозначает
      головы; в раскладке изображения H означала бы высоту.
  - question: >-
      Что происходит с осями тензора внимания (B, H, T, D), когда выходы всех
      голов склеивают в один вектор для каждой позиции?
    options:
      - 'Оси H и D объединяются, а B и T сохраняются'
      - 'Оси B и H объединяются, а T и D сохраняются'
      - 'Оси T и D объединяются, а B и H сохраняются'
      - Удаляется только ось D без изменения остальных осей
    correct: 0
    explanation: >-
      При склейке голов их векторы объединяются: размер новой последней оси
      равен H·D. Для формы (16, 12, 128, 64) это даёт текстовую раскладку (16,
      128, 768).
---

Сейчас проверим одно умение: увидеть четыре числа в скобках и вслух сказать, что внутри — не подглядывая в код. Именно это спасает от ошибок формы из первого шага: ты замечаешь несостыковку раньше, чем запустишь обучение.

Тензор `(16, 12, 128, 64)` — из раскладки внимания `(B, H, T, D)`, которую мы разбирали вместе с остальными каноническими формами.

Где обычно спотыкаются: буква H в двух раскладках значит разное. В картинках `(B, C, H, W)` это высота в пикселях, во внимании `(B, H, T, D)` — количество голов. Одна и та же буква, разный смысл, и по числам их не различить — различает только тип данных, к которому относится тензор. Второй частый промах — перепутать T и D местами: 128 здесь длина текста, 64 — длина вектора на одну голову.
