---
id: 008-check-formy
type: check
title: 'Проверка: прочитать форму (16, 12, 128, 64)'
source_anchor: '### Tensor shapes in deep learning'
check:
  - question: >-
      Почему вторую ось тензора (16, 12, 128, 64) следует интерпретировать как
      головы внимания, а не как высоту изображения?
    options:
      - 'Тензор задан в раскладке внимания (B, H, T, D), где H обозначает головы'
      - Вторая ось любого четырёхмерного тензора всегда обозначает головы
      - Тип данных можно однозначно определить только по величине второй оси
      - Высота изображения всегда располагается на последней оси
    correct: 0
    explanation: >-
      Смысл оси задаётся раскладкой тензора. В (B, H, T, D) буква H означает
      головы внимания, тогда как в раскладке изображения (B, C, H, W) она
      означает высоту.
  - question: 'Как различаются роли осей T и D в тензоре внимания (16, 12, 128, 64)?'
    options:
      - >-
        T перечисляет позиции токенов, а D — компоненты представления токена
        внутри одной головы
      - 'T перечисляет компоненты представления, а D — головы внимания'
      - 'T задаёт размер batch, а D — число позиций токенов'
      - T и D обозначают высоту и ширину изображения
    correct: 0
    explanation: >-
      В раскладке (B, H, T, D) ось T отвечает за последовательность: здесь это
      128 позиций. Ось D хранит признаки для одной позиции внутри одной головы:
      здесь их 64.
---

Сейчас проверим одно умение: увидеть четыре числа в скобках и вслух сказать, что внутри — не подглядывая в код. Именно это спасает от ошибок формы из первого шага: ты замечаешь несостыковку раньше, чем запустишь обучение.

Тензор `(16, 12, 128, 64)` — из раскладки внимания `(B, H, T, D)`, которую мы разбирали вместе с остальными каноническими формами.

Где обычно спотыкаются: буква H в двух раскладках значит разное. В картинках `(B, C, H, W)` это высота в пикселях, во внимании `(B, H, T, D)` — количество голов. Одна и та же буква, разный смысл, и по числам их не различить — различает только тип данных, к которому относится тензор. Второй частый промах — перепутать T и D местами: 128 здесь длина текста, 64 — длина вектора на одну голову.
