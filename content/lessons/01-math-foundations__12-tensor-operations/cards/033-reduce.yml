- concept: свёртка по одной оси удаляет её размер из формы тензора
  question: Заполни форму результата после свёртки по средней оси.
  explanation: Свёртка схлопывает выбранную ось Q, поэтому она исчезает из формы. Размеры остальных осей P и R сохраняются.
  kind: cloze
  template: shape(X) = (P, Q, R); X.sum(по оси Q).shape = ___
  answer: (P, R)
  accept:
    - (P,R)
    - P × R
    - P×R
  id: 033-reduce-1
  fingerprint: 0ac8b9d5
- concept: при свёртке остальные индексы фиксируются, а значения вдоль выбранной оси агрегируются
  question: Матрицу X сворачивают суммированием по оси строк. Как вычисляется элемент результата с индексом j?
  explanation: Для каждого столбца j его индекс остаётся фиксированным, а индекс строки i перебирается. Поэтому на выходе получается по одной сумме на каждый столбец.
  kind: choice
  options:
    - Как сумма X[i, j] по всем строкам i
    - Как сумма X[i, j] по всем столбцам j
    - Как сумма всех элементов матрицы X
    - Он просто равен X[i, j] для произвольной строки i
  correct: 0
  id: 033-reduce-2
  fingerprint: 30ad683d
