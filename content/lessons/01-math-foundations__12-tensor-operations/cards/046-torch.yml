- concept: is_contiguous проверяет, соответствует ли размещение тензора последовательному обходу в порядке текущих осей
  question: Какой метод PyTorch проверяет, является ли размещение тензора сплошным для текущего порядка осей?
  explanation: '`is_contiguous()` проверяет совместимость текущих strides со сплошным размещением. Например, исходный `t` возвращает `True`, а представление после `transpose(0, 1)` — `False`.'
  kind: cloze
  template: Чтобы проверить, является ли размещение тензора `x` сплошным для текущего порядка осей, вызовите `x.___()`.
  answer: is_contiguous
  accept:
    - is_contiguous()
  id: 046-torch-1
  fingerprint: 6f986d8c
