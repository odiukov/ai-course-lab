- concept: ограничение вероятностей перед вычислением обоих логарифмов сохраняет конечность BCE на границах
  question: Как корректно предотвратить бесконечную binary cross-entropy для предсказаний на границе вероятностного диапазона?
  explanation: Логарифм на границе диапазона не определён как конечное число. Ограничение вероятности перед обоими логарифмами сохраняет большой штраф за уверенную ошибку, но не допускает бесконечности и последующего появления nan.
  kind: choice
  options:
    - Заменить все нечисловые значения итоговой потери нулём
    - Ограничить только правильные метки открытым интервалом
    - Прибавить малую константу к уже усреднённой потере
    - Ограничить предсказанные вероятности интервалом от eps до единицы минус eps перед вычислением обоих логарифмов
  correct: 3
  id: 008-code-bce-1
  fingerprint: 5d192bdb
- concept: безопасная BCE сначала стабилизирует вероятности, затем вычисляет поэлементные потери и только потом усредняет их
  question: Расположи операции реализации binary_cross_entropy в правильном порядке.
  explanation: Защита должна применяться до логарифмов. После неё BCE вычисляется поэлементно, а усреднение превращает набор штрафов в один скалярный loss, сопоставимый для батчей разного размера.
  kind: order
  items:
    - Ограничить предсказанные вероятности безопасным открытым интервалом
    - Вычислить BCE отдельно для каждого предсказания и соответствующей метки
    - Усреднить поэлементные потери
    - Вернуть получившийся скаляр
  id: 008-code-bce-2
  fingerprint: c1622ca0
