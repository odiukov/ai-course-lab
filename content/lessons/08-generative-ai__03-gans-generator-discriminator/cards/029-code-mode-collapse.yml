- concept: симметричная проверка коллапса сравнивает с порогом счётчик наименее наполненной моды
  question: Диагностика двухмодового GAN считает коллапсом ситуацию, когда хотя бы одна мода получила меньше заданного порога образцов. Заполни пропуск.
  explanation: Минимум выбирает менее наполненную моду независимо от того, A это или B. Затем её счётчик сравнивается с порогом.
  kind: cloze
  template: |-
    def is_mode_collapse(mode_a, mode_b, threshold):
        return ___ < threshold
  answer: min(mode_a, mode_b)
  accept:
    - min([mode_a, mode_b])
    - min((mode_a, mode_b))
  id: 029-code-mode-collapse-1
  fingerprint: f7b70902
- concept: строгое неравенство не считает коллапсом наполнение моды, в точности равное порогу
  question: Коллапс определяется условием min(mode_a, mode_b) < 64. При каких счётчиках функция вернёт False?
  explanation: Во втором случае минимальный счётчик равен 64. Проверка 64 < 64 ложна, потому что порог строгий. В остальных случаях минимум меньше порога.
  kind: choice
  options:
    - mode_a = 63, mode_b = 937
    - mode_a = 936, mode_b = 64
    - mode_a = 12, mode_b = 988
  correct: 1
  id: 029-code-mode-collapse-2
  fingerprint: 77ef3144
