---
id: 012-quiz
type: quiz
title: Проверяем весь шов фузии
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты дошёл до cross-attention в декодере. Из каких потоков должны поступить
      Q, K и V?
    options:
      - 'Q, K и V поступают из текста'
      - 'Q поступает из текста, а K и V — из токенов изображения'
      - 'Q поступает из изображения, а K и V — из текста'
      - 'Q и K поступают из текста, а V — из изображения'
    correct: 1
    explanation: >-
      Запросы представляют позиции текста, для которых нужен визуальный
      контекст. Ключи и значения поступают из одной памяти изображения: ключи
      участвуют в выборе патчей, а значения несут извлекаемую информацию.
  - question: >-
      Что произойдёт при обучении авторегрессионного декодера, если убрать
      причинную маску из self-attention?
    options:
      - >-
        Текущая позиция сможет использовать будущие токены целевой
        последовательности
      - Текстовые запросы перестанут видеть токены изображения
      - Все веса внимания автоматически станут одинаковыми
      - Модель сможет обрабатывать только первый токен
    correct: 0
    explanation: >-
      Без причинной маски self-attention видит будущие позиции целевого текста.
      При обучении возникает утечка правильного продолжения, которой не будет во
      время авторегрессионной генерации.
  - question: >-
      Для новой картинки по ошибке переиспользовали KV-кеш, построенный для
      предыдущей. Каков наиболее вероятный результат?
    options:
      - Декодер будет извлекать визуальный контекст из предыдущей картинки
      - Кеш автоматически обновится при вычислении текстовых запросов
      - Изменится только причинная маска текста
      - Cross-attention превратится в self-attention
    correct: 0
    explanation: >-
      KV-кеш привязан к конкретной визуальной памяти. Текстовые запросы не
      пересчитывают его, поэтому устаревшие K и V заставят декодер обращаться к
      представлению предыдущего изображения.
  - question: Как точнее всего различить memory tokens и KV-кеш изображения?
    options:
      - >-
        Memory tokens — выход визуального энкодера, а KV-кеш — их
        спроецированные ключи и значения для cross-attention
      - 'Memory tokens содержат только ключи, а KV-кеш — только значения'
      - 'Memory tokens относятся к тексту, а KV-кеш — к изображению'
      - Это два названия одного и того же тензора без преобразований
    correct: 0
    explanation: >-
      Memory tokens — базовое представление изображения после визуального
      энкодера. Каждый блок cross-attention применяет к нему собственные
      проекции и получает отдельные K и V, которые можно сохранить в кеше.
---

Урок закончился, но именно этот шов ты дальше будешь встречать постоянно: любая модель, которая смотрит на картинку и пишет текст, собрана из тех же трёх подслоёв. Полезнее всего унести две вещи — правило источников (Q из потока, который спрашивает; K и V из потока, который отвечает) и правило масок (треугольник только там, где есть время, то есть в тексте).

Спотыкаются обычно на двух местах. Первое — маску вешают после softmax, и запрещённые позиции всё равно утаскивают часть вероятности. Второе — путают, где кэшировать: пересчитывают картинку на каждом сгенерированном слове и удивляются, почему генерация в десятки раз медленнее, чем должна быть.

Проверь себя на вопросах выше — они по всему уроку, не только по последнему экрану.
