---
id: 012-quiz
type: quiz
title: Проверяем весь шов фузии
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты дошёл до второго подслоя блока декодера — до cross-attention. Что
      подать в качестве Q, K и V?
    options:
      - 'Q, K и V — все три от текста, а картинка добавляется потом сложением'
      - 'Q от текста, K и V от токенов картинки'
      - 'Q от картинки, K и V от текста'
      - 'Q и K от текста, V от картинки'
    correct: 1
    explanation: >-
      Текст задаёт вопрос — значит, Q идёт от него. Картинка отвечает, поэтому
      от неё берутся и ключи, и значения: они всегда приходят из одного
      источника.
  - question: >-
      Кто-то на всякий случай навесил нижнетреугольную маску на матрицу
      совместимости в cross-attention. Что получится?
    options:
      - 'Ничего не изменится: маска из нулей и единиц не влияет на чужой поток'
      - >-
        Первый токен текста увидит только первый участок картинки, второй — два,
        и часть картинки окажется недоступна началу ответа
      - Модель начнёт подглядывать в будущее текста
      - 'Softmax вернёт NaN, потому что маска не той формы'
    correct: 1
    explanation: >-
      Маска запрещает смотреть вперёд по времени, но у токенов картинки времени
      нет — они все доступны сразу. Нижний треугольник просто отрежет ранним
      словам большую часть изображения.
  - question: >-
      Ты генерируешь ответ длиной 40 токенов по одной картинке. Сколько раз
      стоит прогнать энкодер картинки и посчитать её K и V?
    options:
      - 'Один раз до генерации, дальше переиспользовать готовые K и V'
      - '40 раз, по разу на каждый шаг декодирования'
      - 'Один раз на каждый блок декодера, то есть по числу блоков'
      - '40 раз, иначе значения устареют и ответ рассинхронизируется'
    correct: 0
    explanation: >-
      Картинка не меняется, пока пишется ответ, поэтому её ключи и значения
      считаются один раз и подаются в каждый шаг готовыми — это и есть смысл
      KV-кэша.
  - question: >-
      Сколько LayerNorm и residual-связок в одном блоке декодера с
      cross-attention?
    options:
      - Один LayerNorm на весь блок и один residual в конце
      - Три LayerNorm и три residual — по паре на каждый подслой
      - >-
        Два: маскированный self-attention и feed-forward, а cross-attention
        обходится без них
      - 'Три LayerNorm, но residual только вокруг feed-forward'
    correct: 1
    explanation: >-
      Подслоя три — маскированный self-attention, cross-attention и feed-forward
      — и каждый обёрнут своей парой: нормализация до, сложение со входом после.
---

Урок закончился, но именно этот шов ты дальше будешь встречать постоянно: любая модель, которая смотрит на картинку и пишет текст, собрана из тех же трёх подслоёв. Полезнее всего унести две вещи — правило источников (Q из потока, который спрашивает; K и V из потока, который отвечает) и правило масок (треугольник только там, где есть время, то есть в тексте).

Спотыкаются обычно на двух местах. Первое — маску вешают после softmax, и запрещённые позиции всё равно утаскивают часть вероятности. Второе — путают, где кэшировать: пересчитывают картинку на каждом сгенерированном слове и удивляются, почему генерация в десятки раз медленнее, чем должна быть.

Проверь себя на вопросах выше — они по всему уроку, не только по последнему экрану.
