- concept: условие остановки рекурсии проверяется до агрегатных операций над текущей подвыборкой
  question: Почему в build_isolation_tree условие остановки нужно проверить до вычисления минимума и максимума по выбранному признаку?
  explanation: Сначала проверяют глубину и размер текущей подвыборки. Если строить разбиение уже нельзя, функция возвращает лист, не обращаясь к минимуму и максимуму.
  kind: open
  reference: Терминальная подвыборка должна сразу превратиться в лист. Это предотвращает вызов min и max на пустых данных и исключает ненужные вычисления для подвыборки, которую уже нельзя делить.
  id: 031-code-build-tree-1
  fingerprint: c3f1ed33
- concept: лист isolation-дерева хранит размер своей текущей подвыборки
  question: Рекурсивный вызов build_isolation_tree завершился созданием листа. Какое значение следует записать в поле size этого листа?
  explanation: Каждый узел работает со своей подвыборкой, поэтому лист сохраняет именно её размер. Это значение позднее учитывается при оценке длины пути.
  kind: choice
  options:
    - Количество строк в текущей подвыборке
    - Количество строк в исходном обучающем наборе
    - Текущую глубину узла
    - Общее количество признаков
  correct: 0
  id: 031-code-build-tree-2
  fingerprint: 2627b8f1
- concept: наблюдения, равные порогу, направляются в правую ветвь
  question: Заполни оператор сравнения для маски левой ветви isolation-дерева.
  explanation: В левую ветвь попадают значения строго меньше порога. Равные порогу значения не входят в left_mask и вместе с остальными направляются вправо.
  kind: cloze
  template: left_mask = X[:, feature] ___ threshold
  answer: <
  accept:
    - <
  id: 031-code-build-tree-3
  fingerprint: 5484051c
