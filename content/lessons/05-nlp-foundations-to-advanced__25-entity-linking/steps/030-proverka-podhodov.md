---
id: 030-proverka-podhodov
type: check
title: 'Когда выбрать prior, embeddings или ограниченную генерацию?'
source_anchor: '## The Concept'
check:
  - question: >-
      У тебя нет обучающей выборки, систему нужно запустить быстро, а частоты
      сущностей и простой контекст уже доступны. Что разумнее выбрать первым?
    options:
      - Prior плюс контекст
      - Сравнение embeddings
      - Неограниченную генерацию названия
    correct: 0
    explanation: >-
      Prior плюс контекст даёт быстрый базовый вариант без обучения: частота
      предлагает вероятную сущность, а контекст помогает исправлять перекос
      популярности.
  - question: >-
      Система обязана всегда возвращать только существующий ID из KB. Какой
      подход обеспечивает это самим способом выбора ответа?
    options:
      - Prior с большим весом популярности
      - Embeddings с максимальным косинусным сходством
      - 'Генерация, ограниченная допустимыми именами KB'
    correct: 2
    explanation: >-
      Ограниченное декодирование не позволяет составить имя вне белого списка
      KB, поэтому результат можно однозначно сопоставить с допустимым ID.
---

Перед тобой три инструмента, и выбирать нужно по ограничению задачи. Если важны скорость и отсутствие обучения, начинай с prior и контекста из [шага 18](#step-18). Если смысл контекста сложный и есть описания сущностей, embeddings из [шага 27](#step-27) позволяют сравнивать их содержательно. Если критично не получить несуществующий ID, выбирай ограниченную генерацию из [шага 29](#step-29).

> 🎒 **На пальцах.** Ты подбираешь актёра на роль. Prior зовёт самого известного, embeddings сравнивают сценарий с опытом каждого кандидата, а ограниченная генерация разрешает назвать только человека из утверждённого списка. Лучший способ зависит не от моды, а от того, что для постановки нельзя нарушить.
