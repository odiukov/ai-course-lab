---
id: 004-check-problem
type: check
title: 'Проверка: кто говорит, кто делает'
source_anchor: '## The Problem'
check:
  - question: >-
      Модель сообщила текущую погоду, хотя хост не объявлял ей никаких
      инструментов. Что произошло?
    options:
      - Модель самостоятельно обратилась к погодному API
      - >-
        Модель сгенерировала правдоподобный текст, который не гарантирует
        актуальность данных
      - JSON Schema автоматически получила погоду из сети
    correct: 1
    explanation: >-
      Без объявленного и исполненного хостом инструмента модель только
      продолжает последовательность токенов. Её ответ может звучать убедительно
      и даже случайно совпасть с реальностью.
  - question: >-
      Модель вернула структурированный блок с именем погодного инструмента и
      названием города. Кто должен выполнить сетевой запрос?
    options:
      - Сама модель сразу после генерации блока
      - 'Программа-хост, которая разбирает блок и запускает исполнитель'
      - 'JSON Schema, по которой описаны аргументы'
    correct: 1
    explanation: >-
      Блок модели выражает запрос на действие. Хост валидирует аргументы,
      вызывает реальный сервис и добавляет полученный результат в диалог.
---

После этих трёх экранов ты должен уметь разводить две роли, которые в тексте выглядят похоже: модель, которая только произносит просьбу, и хост, который единственный что-то делает руками. Проверь себя на этом: если модель «сходила в интернет» — значит, инструмент ей объявил и вызвал хост, а не она сама.

Спотыкаются обычно на двух местах. Первое: блок `tool_use` принимают за уже случившееся действие, хотя это ещё просьба — ничего не произошло, пока хост её не разобрал. Второе: думают, что смена названия что-то меняет по существу. Названия разные — `functions`, `tool_use`, `functionDeclarations`, MCP, A2A, — а роли внутри те же, как в [шаге 3](#step-3).
