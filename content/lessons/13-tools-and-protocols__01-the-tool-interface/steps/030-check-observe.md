---
id: 030-check-observe
type: check
title: 'Проверка: что будет, если убрать tool-сообщение'
source_anchor: '### Step four: observe'
check:
  - question: >-
      Модель на прошлом ходу вызвала get_weather, хост вернул результат, модель
      ответила «в Киеве 24 °C». Ты отправляешь следующий вопрос, но вычищаешь из
      истории сообщение с ролью tool. Что произойдёт?
    options:
      - 'Ничего, модель уже запомнила число 24 внутри себя'
      - 'Модель снова не знает погоду и, скорее всего, запросит вызов заново'
      - 'Хост сам подставит последний результат, потому что помнит id вызова'
      - 'Запрос упадёт с ошибкой: без tool-сообщения модель не может ответить'
    correct: 1
    explanation: >-
      Результат инструмента живёт только как сообщение в истории. Убрал
      сообщение — данных в контексте нет, и модели придётся запрашивать вызов
      заново.
  - question: >-
      Хост получил результат инструмента и добавил его в историю. Что должно
      совпасть, чтобы модель поняла, к какому вызову относится этот результат?
    options:
      - Порядок сообщений в истории — этого достаточно
      - Имя инструмента в поле name
      - id из полезной нагрузки вызова
      - 'Текст аргументов, с которыми вызывали инструмент'
    correct: 2
    explanation: >-
      id — единственная надёжная сцепка между вызовом и его результатом,
      особенно когда за один ход ушло несколько параллельных вызовов одного
      инструмента.
---

После этого куска ты должен уметь ответить на два вопроса про шаг observe: что именно хост кладёт в историю и что случится, если этого сообщения там не окажется.

Спотыкаются обычно на слове «помнит». Кажется, что модель, один раз получив 24 °C, держит это число где-то у себя — ведь она же ответила по делу. Но результат жил ровно один ход, в виде сообщения с ролью `tool`, и на следующем ходу хост отправляет всю переписку заново. Что не положил в неё — того для модели не существует.

Вторая ловушка — id из [шага 18](#step-18). Пока вызов один, порядок сообщений кажется достаточным. Два параллельных вызова одного инструмента ([шаг 17](#step-17)) — и без id результаты уже не различить.
