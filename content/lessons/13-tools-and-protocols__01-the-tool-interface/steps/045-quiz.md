---
id: 045-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      Хост получил от модели вызов get_weather с аргументом city равным 42. Что
      происходит дальше по правильному порядку?
    options:
      - 'Исполнитель запускается, а ошибку разбирают после его падения'
      - >-
        Хост сначала проверяет аргументы по схеме и не запускает исполнитель,
        пока они не пройдут валидацию
      - >-
        Модель сама обнаруживает несоответствие типа и автоматически исправляет
        вызов
    correct: 1
    explanation: >-
      Перед запуском исполнителя хост проверяет аргументы по объявленной схеме.
      Это не позволяет ошибочному запросу дойти до кода, способного
      воздействовать на внешний мир.
  - question: >-
      Модель вернула два вызова за один ход, хост оба выполнил, но результат
      одного вызова не добавил в передаваемую историю. Что получит модель на
      следующем ходу?
    options:
      - 'Оба результата, потому что модель помнит собственные запросы'
      - >-
        Только добавленный в историю результат; второй вызов останется для неё
        без результата
      - 'Автоматическую ошибку протокола, которая обязательно прервёт цикл'
    correct: 1
    explanation: >-
      На следующем ходу модель видит только переданную ей историю. Если
      результат инструмента в неё не добавлен, модель не получает этот
      результат, даже если хост успешно выполнил вызов.
  - question: >-
      Инструмент send_email отправляет письмо клиенту по адресу из аргументов.
      Почему с ним нужно обращаться осторожнее, чем с get_weather?
    options:
      - Он обычно работает медленнее и расходует больше токенов
      - >-
        Он меняет внешнее состояние, поэтому может требовать шлюза, аудита или
        подтверждения пользователя
      - У него неизбежно более сложная схема аргументов
    correct: 1
    explanation: >-
      Отправка письма создаёт внешний эффект, который нельзя безопасно отменить
      простым повторным вызовом. Поэтому запуск такого инструмента требует
      дополнительных мер контроля.
  - question: >-
      Один MCP-сервер погоды работает с несколькими совместимыми клиентами без
      изменения реализации. Что обеспечивает такую переносимость?
    options:
      - Все клиенты обязательно используют одну и ту же языковую модель
      - >-
        Клиенты и сервер следуют общему протоколу описания, вызова и исполнения
        инструментов
      - MCP заменяет выбранную клиентом модель собственной моделью
    correct: 1
    explanation: >-
      MCP задаёт общий протокольный интерфейс между клиентом и сервером. Поэтому
      один сервер может подключаться к разным клиентам, которые поддерживают
      этот контракт, независимо от используемой ими модели.
---

Ты прошёл урок целиком — и, если посмотреть назад, всё держится на одном простом разделении: модель говорит, хост делает. Дальше в курсе будет 22 урока, и в каждом ты встретишь тот же цикл из [шага 5](#step-5) под новыми названиями — MCP-серверы, subagents, browser use, память, оркестрация. Форма не поменяется.

Спотыкаются обычно на трёх местах. Первое: кажется, что модель «выполняет» инструмент — нет, она только выдаёт JSON-вызов. Второе: `id` воспринимают как декорацию, а без него параллельные результаты из [шага 17](#step-17) не сопоставить с вызовами. Третье: валидацию из [шага 21](#step-21) хочется отложить «на потом», но именно она стоит между галлюцинацией и настоящим действием.

Проверь, держится ли это без подсказок.
