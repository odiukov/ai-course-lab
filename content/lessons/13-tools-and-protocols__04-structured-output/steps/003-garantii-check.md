---
id: 003-garantii-check
type: check
title: 'Проверка: промпт, поствалидация или constrained decoding'
source_anchor: '## The Problem'
check:
  - question: >-
      Ты обрабатываешь тысячи заказов и должен гарантировать, что каждый
      успешный ответ разбирается и соответствует схеме. Какой подход выбрать?
    options:
      - Подробно попросить модель вернуть только JSON
      - Проверять ответ после генерации и повторять запрос при ошибке
      - Использовать constrained decoding и отдельно обрабатывать отказ
    correct: 2
    explanation: >-
      Constrained decoding не позволяет модели сгенерировать структуру вне
      схемы. При этом отказ остаётся отдельным результатом, который должен
      заметить твой код.
  - question: >-
      Ответ, полученный через constrained decoding, прошёл схему, но total_usd
      не совпадает с суммой позиций. Что это означает?
    options:
      - 'Схема гарантирует форму ответа, но не правильность извлечённых данных'
      - Constrained decoding не был включён
      - Такой ответ обязательно считается отказом
    correct: 0
    explanation: >-
      Ограничение гарантирует допустимые поля и типы, но модель всё ещё может
      неверно понять исходное письмо или извлечь ошибочное значение.
---

Теперь ты должен различать три обещания: промпт лишь просит соблюдать формат, поствалидация обнаруживает нарушение после генерации, а constrained decoding не даёт создать запрещённую структуру. Главная ловушка — принять корректную форму за достоверное содержание. Даже идеально разобранный ответ может содержать неверную сумму. Связь подходов с уровнями гарантии можно освежить в [шаге 2](#step-2).

> 🎒 **На пальцах.** Представь автомат для фасовки таблеток. Трафарет гарантирует, что в блистере будет ровно десять ячеек и каждая окажется заполнена. Но он не проверяет, то ли лекарство положили внутрь. Constrained decoding — такой трафарет: форма под контролем, смысл всё ещё нужно проверять отдельно.
