---
id: 052-quiz
type: quiz
title: 'Итоговая проверка: общий кэш, эмерджентная координация, арифметика p, c и N'
source_anchor: '## Key Terms'
check:
  - question: >-
      Четыре воркера используют один общий KV-кэш без отдельного канала
      сообщений. Как они могут избегать выполнения одной и той же работы?
    options:
      - Координатор раздаёт подзадачи и отслеживает дубли
      - >-
        Каждый воркер читает записи остальных в общем кэше и сам выбирает
        дальнейшую работу
      - Заранее обученный протокол закрепляет за воркерами постоянные роли
      - Кэш блокируется на всё время рассуждения одного воркера
    correct: 1
    explanation: >-
      Общий кэш служит средой обмена контекстом: воркеры видят добавленные
      другими токены и корректируют свои действия. Отдельный координатор или
      заранее выученный протокол для этого не требуется.
  - question: >-
      Задача требует 1000 токенов работы, её параллелизуемая доля p = 0.3,
      координационные расходы c = 200, а число воркеров N = 4. Что произойдёт по
      сравнению с последовательным выполнением?
    options:
      - >-
        Получится ускорение около 1.5 раза, поскольку параллельная часть делится
        на четыре
      - >-
        Получится небольшое ускорение, поскольку любой ненулевой p сокращает
        итоговое время
      - >-
        Возникнет замедление: последовательный хвост велик, а координационные
        расходы превышают выигрыш
      - >-
        Время не изменится, поскольку выигрыш и накладные расходы всегда
        компенсируют друг друга
    correct: 2
    explanation: >-
      Оценка времени равна 1000 × (0.7 + 0.3 / 4) + 200 × 4 = 1575. Это больше
      исходных 1000 шагов, поэтому запуск четырёх воркеров замедляет выполнение.
  - question: >-
      Воркеры перестали учитывать записи друг друга, но по-прежнему параллельно
      генерируют токены в общий кэш. Что произойдёт с эффективным ускорением?
    options:
      - >-
        Оно может упасть ниже единицы из-за дублирования работы и оставшихся
        накладных расходов
      - >-
        Оно станет ровно единицей, поскольку отсутствие координации нейтрализует
        параллелизм
      - >-
        Оно останется высоким, поскольку число активных воркеров важнее
        полезности их результатов
      - >-
        Оно обязательно вырастет, поскольку чтение общего контекста больше не
        требуется
    correct: 0
    explanation: >-
      Без учёта чужих записей воркеры часто исследуют одно и то же. Повторные
      токены не увеличивают объём полезной работы, но вычисления и нагрузка на
      общий кэш сохраняются.
  - question: >-
      Почему добавление новых записей в общий KV-кэш при использовании RoPE не
      требует пересчитывать представления уже записанных токенов?
    options:
      - 'RoPE хранит для каждого токена обучаемый вектор, не связанный с позицией'
      - Воркеры всегда вычисляют токены строго по очереди
      - >-
        Позиционная информация задаётся поворотами, а внимание использует
        разность углов между запросом и ключом
      - KV-кэш не содержит позиционной информации
    correct: 2
    explanation: >-
      RoPE поворачивает запросы и ключи в зависимости от позиции, а их
      взаимодействие выражает относительное смещение через разность углов.
      Добавление токенов в конец поэтому не меняет уже вычисленные ключи и
      значения.
---

Урок кончился, а дальше в курсе тебе понадобится не столько название «Hogwild!», сколько три привычки мышления, которые он тренировал.

Первая: смотреть на топологию, а не на количество копий. N воркеров можно связать голосованием, деревом или координатором из [шага 3](#step-3) — а можно посадить за один общий KV-кэш, и это меняет всё, потому что среда координации становится тем же буфером, из которого модель читает контекст.

Вторая: не верить в ускорение на слово. У тебя есть арифметика из [шагов 16](#step-16)–[19](#step-19): последовательный хвост $(1-p)$ не сжимается никаким N, а координация $c$ растёт с числом воркеров. Здесь чаще всего и спотыкаются — держат в голове «четыре воркера, значит вчетверо быстрее» и удивляются числу 1.8 из [шага 23](#step-23).

Третья: помнить, что параллелизм без координации хуже, чем его отсутствие ([шаг 46](#step-46)).

Проверь себя — и обрати внимание, где ты угадывал, а где считал.
