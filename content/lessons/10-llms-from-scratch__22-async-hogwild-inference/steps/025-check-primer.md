---
id: 025-check-primer
type: check
title: 'Проверка: почему на 50 000 токенов ускорение выше'
source_anchor: '### Concrete example'
check:
  - question: >-
      Возьми ту же задачу, но на 50 000 токенов: p всё те же 0.7, воркеров 4, c
      = 200. Что изменится в расчёте по сравнению с примером на 10 000?
    options:
      - >-
        Слагаемое координации так и останется 800 шагов, а множитель 0.475
        применится к вдесятеро большему числу — координация станет весить 1.6
        процента вместо 8
      - >-
        Слагаемое координации вырастет вместе с длиной, примерно до 4000 шагов,
        и его доля не изменится
      - >-
        Множитель 0.475 уменьшится, потому что за 50 000 токенов воркеры лучше
        приноровятся друг к другу
      - >-
        Изменятся оба слагаемых, но ускорение останется ровно 1.8, потому что
        оно от длины не зависит
    correct: 0
    explanation: >-
      c умножается на число воркеров, а не на длину цепочки: 200 на 4 — это 800
      шагов и на короткой задаче, и на длинной. Растёт только та часть, что
      умножается на 0.475, поэтому фиксированные 800 шагов размазываются.
  - question: >-
      Продолжим удлинять задачу при тех же p = 0.7 и N = 4: 100 000 токенов, 200
      000. К чему стремится ускорение?
    options:
      - >-
        Растёт без предела: чем длиннее цепочка, тем меньше весит координация и
        тем больше выигрыш
      - 'Упирается в потолок, заданный множителем 0.475, — примерно в 2.1 раза'
      - 'Возвращается к 1, потому что общий кэш становится слишком большим'
    correct: 1
    explanation: >-
      Координация размазывается до нуля, но множитель 0.475 никуда не девается:
      в нём сидит последовательный хвост 0.3. Единица, делённая на 0.475, — это
      около 2.1, дальше при этих p и N не уйти.
---

К этому месту у тебя на руках готовый счёт из [шага 23](#step-23) и разбор его по частям из [шага 24](#step-24). Проверь, что ты умеешь не просто прочитать 5550, а сказать, какое слагаемое сдвинется, если покрутить длину задачи.

Обычно спотыкаются на двух вещах. Первая: кажется, будто координация тоже растёт вместе с цепочкой — тогда её доля была бы одинаковой на любой длине и разговор про «длинные задачи выигрывают» терял бы смысл. Вторая: если координация размазывается, хочется решить, что ускорение растёт бесконечно. Оба слагаемых ведут себя по-разному, и именно из этой разницы получается ответ.
