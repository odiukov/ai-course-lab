---
id: 013-check-perehvat
type: check
title: 'Проверка: вор перехватил code — что дальше'
source_anchor: '### Authorization code + PKCE'
check:
  - question: >-
      Вор перехватил code на шаге 3 и сам отправляет POST на /token. Что
      произойдёт?
    options:
      - 'Токен выдадут: code — это и есть пропуск, больше ничего не нужно'
      - >-
        Запрос отклонят: без правильного code_verifier сервер не получит
        совпадения с сохранённым challenge
      - >-
        Сервер попросит вора заново пройти согласие пользователя и на этом всё
        закончится
      - >-
        Сервер выдаст токен, но с урезанным scope, потому что вор не подтвердил
        личность
    correct: 1
    explanation: >-
      На шаге 4 сервер хеширует присланный verifier и сверяет с challenge из
      шага 2. У вора verifier'а нет, подставить нечего — обмен не проходит.
  - question: >-
      code_challenge летит в открытом URL на шаге 2, так что вор мог увидеть и
      его. Помогает ли это ему?
    options:
      - 'Да: challenge и есть тот секрет, который проверяют на шаге 4'
      - >-
        Да, если он пришлёт challenge вместо verifier — сервер сверит их и
        увидит совпадение
      - >-
        Нет: challenge — это отпечаток SHA256, обратно из него verifier не
        восстановить
      - 'Нет, потому что challenge действует только одну секунду после отправки'
    correct: 2
    explanation: >-
      Сервер хеширует то, что ему прислали, и сравнивает с challenge. Пришлёшь
      сам challenge — сервер сравнит с ним хеш от challenge, а это другое число.
      Восстановить verifier из хеша нельзя — это и показал [шаг 12](#step-12).
---

Здесь проверяется одна связка: ты должен уметь пройти по [шести шагам потока](#step-8) и показать пальцем, где именно вор ломается. Не «PKCE защищает», а: код перехвачен на шаге 3, обмен происходит на шаге 4, и на шаге 4 требуется вещь, которая по сети вообще не летела.

Спотыкаются обычно на двух местах. Первое — думают, что PKCE прячет сам `code`. Не прячет: код по-прежнему уходит в редиректе, и перехватить его можно. Второе — путают направление проверки: verifier хешируют и сравнивают с challenge, а не наоборот, поэтому знание challenge вору ничего не даёт.
