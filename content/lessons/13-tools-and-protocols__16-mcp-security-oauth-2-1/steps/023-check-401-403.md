---
id: 023-check-401-403
type: check
title: 'Проверка: чужой aud и нехватка scope — разные ответы'
source_anchor: '### Token audience validation'
check:
  - question: >-
      Токен пришёл с валидной подписью, не истёк, в scopes есть всё нужное — но
      в aud стоит адрес соседнего MCP-сервера. Что должен ответить твой сервер?
    options:
      - 'Пропустить запрос: подпись настоящая, прав хватает'
      - 'Отказать как неаутентифицированному: токен выписан не для этого сервера'
      - Отказать из-за нехватки прав и попросить добавить scope
      - 'Пропустить, но записать в лог предупреждение'
    correct: 1
    explanation: >-
      Чужой aud означает, что этот токен вообще не является пропуском к твоему
      ресурсу — права тут проверять уже не из чего, отказ идёт по линии
      аутентификации.
  - question: >-
      Другой случай: aud совпадает с твоим URL, срок в порядке, но в токене
      только files:read, а вызванный инструмент требует files:write. Чем этот
      отказ отличается от предыдущего?
    options:
      - 'Ничем: любой отказ по токену выглядит одинаково'
      - >-
        Тут токен признан своим и валидным, не хватает именно разрешения — отказ
        по линии прав
      - Тут сервер должен молча вернуть пустой результат
      - 'Тут нужно выдать новый токен самому, добавив в него files:write'
    correct: 1
    explanation: >-
      Токен принят как свой — проблема не в том, кто его предъявил, а в том, что
      запрошенное действие в его список прав не входит; это отказ другого сорта.
---

После [шага 21](#step-21) и [шага 22](#step-22) у тебя в руках две разные проверки, и обе заканчиваются словом «отказать» — поэтому их легко слепить в одну. Здесь надо расцепить.

Умение простое на словах и коварное на практике: по описанию ситуации сказать, споткнулся сервер на «этот токен не мой» или на «токен мой, но прав мало». Спотыкаются обычно там, где всё остальное в токене идеально: подпись настоящая, срок свежий, scopes щедрые — и рука сама тянется пропустить запрос. И наоборот: видят слово «scope» и пишут отказ по аутентификации, хотя предъявитель уже опознан.

Разница не косметическая: от неё зависит, что клиенту делать дальше — идти за токеном для правильной аудитории или просить больше прав у того же authorization server.
