---
id: 007-check-roli
type: check
title: 'Проверка: кто выдаёт, кто проверяет'
source_anchor: '### Roles'
check:
  - question: >-
      Ты поднял MCP-сервер с заметками, и токены он выдаёт сам — тот же процесс,
      тот же хост. Что из этого верно?
    options:
      - >-
        Раз хост один, роль тоже одна: authorization server тут не нужен, есть
        просто сервер с токенами
      - >-
        Ролей всё равно две — одна выдаёт токен, другая его проверяет, — и их
        следует развести по разным URL
      - >-
        Так делать нельзя: профиль MCP требует отдельный IdP вроде Auth0 или
        Keycloak
      - >-
        Роли две, но URL обязан быть один, иначе клиент не поймёт, куда идти за
        токеном
    correct: 1
    explanation: >-
      Профиль MCP разрешает одному хосту совмещать обе роли, но различать их по
      URL следует: токен должен быть привязан к конкретному ресурсу, а не «к
      серверу вообще».
  - question: >-
      Claude Desktop подключается к MCP-серверу GitHub, а токены выдаёт
      отдельный IdP. Кто здесь resource server?
    options:
      - Claude Desktop — он держит токен и предъявляет его
      - 'IdP — он решает, выдавать токен или нет'
      - MCP-сервер GitHub — он отдаёт данные и проверяет предъявленный токен
      - 'И IdP, и MCP-сервер сразу: оба работают с токеном'
    correct: 2
    explanation: >-
      Resource server — тот, у кого лежат данные и кто проверяет токен на входе.
      Claude Desktop — клиент, IdP — authorization server.
---

После [шага 6](#step-6) ты должен уметь разложить любую реальную связку на три роли и не сбиться, когда две из них живут в одном процессе. Проверь себя на паре вопросов ниже.

Спотыкаются обычно на двух местах. Первое: если authorization server и resource server — один и тот же хост, кажется, что роль тоже одна, и разделение — формальность. Оно не формальность: выдача права и проверка права — разные действия, и разные URL нужны, чтобы токен был привязан к конкретному ресурсу.

Второе: клиента путают с authorization server, потому что токен физически лежит у клиента. Но хранить — не значит выдавать.
