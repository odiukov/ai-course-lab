- concept: scope-параметр разбирается как пробельно-разделённая строка отдельных разрешений
  question: Заполните пропуск в Python-коде, который превращает строковое значение scope в множество отдельных разрешений.
  explanation: Scopes передаются одной строкой с разделением пробелами. split() без явного разделителя разбивает её на отдельные непустые элементы, после чего множество удобно использовать для проверки наличия разрешений.
  kind: cloze
  template: granted_scopes = set(scope_value.___)
  answer: split()
  accept:
    - split()
    - split(None)
  id: 018-scopes-satisfied-1
  fingerprint: 25d24f2f
