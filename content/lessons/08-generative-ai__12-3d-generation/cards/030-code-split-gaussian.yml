- concept: симметричные смещения потомков сохраняют среднее положение исходной гауссианы
  question: При разделении гауссового примитива центр родителя равен μ, а вектор смещения — δ. Какая пара центров не вызывает общего сдвига представления?
  explanation: У смещений +δ и −δ противоположные знаки, поэтому при усреднении они сокращаются. Среднее положение потомков совпадает с центром μ родителя.
  kind: choice
  options:
    - μ + δ и μ − δ
    - μ + δ и μ + δ
    - μ − δ и μ − δ
    - μ и μ + δ
  correct: 0
  id: 030-code-split-gaussian-1
  fingerprint: d3e88e1d
- concept: независимое копирование изменяемых полей предотвращает совместное изменение потомков
  question: Заполни пропуск так, чтобы данные потомков и родителя не ссылались на один изменяемый объект.
  explanation: 'Отдельное глубокое копирование исключает aliasing: изменение массива или списка одного потомка не затронет другого потомка либо исходную гауссиану.'
  kind: cloze
  template: |-
    left.data = copy.deepcopy(parent.data)
    right.data = ___
  answer: copy.deepcopy(parent.data)
  accept:
    - deepcopy(parent.data)
  id: 030-code-split-gaussian-2
  fingerprint: 86804f83
