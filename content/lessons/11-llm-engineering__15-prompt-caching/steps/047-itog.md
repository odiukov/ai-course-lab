---
id: 047-itog
type: quiz
title: Итоговая проверка
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты добавил в самое начало системного промпта строку с текущим временем
      запроса. Промпт длинный, всё остальное не меняется. Что будет со
      стоимостью?
    options:
      - >-
        Кэш перестанет попадать, поэтому каждый запрос будет заново записывать
        префикс с надбавкой
      - >-
        Кэш пропустит только строку со временем, а остальной префикс прочитается
        со скидкой
      - >-
        Кэш попадёт, потому что провайдер учитывает смысл, а не точное
        содержимое
      - 'Кэш сработает, но скидка автоматически станет меньше'
    correct: 0
    explanation: >-
      Изменение в начале кэшируемого префикса нарушает совпадение всей
      последующей последовательности токенов. Каждый запрос становится новой
      записью и несёт премию за запись.
  - question: >-
      Ты закэшировал системный блок и обращаешься к модели один раз в час. TTL
      по умолчанию — пять минут. Что происходит?
    options:
      - >-
        Каждый запрос приходит после истечения TTL и создаёт новую запись,
        поэтому экономии нет
      - 'Первый запрос создаёт кэш, а все последующие читают его со скидкой'
      - 'Кэш сохраняется бессрочно, пока системный промпт не изменится'
      - Маркер кэширования автоматически продлевает TTL без новых попаданий
    correct: 0
    explanation: >-
      Кэш остаётся тёплым ограниченное время. При интервале в час пятиминутное
      окно истекает до следующего запроса, поэтому каждый запрос записывает
      префикс заново.
  - question: >-
      У тебя гигантский корпус документов, который команда переиспользует
      несколько дней подряд. Какой режим кэша уместнее?
    options:
      - Именованный объект кэша с оплатой за время хранения
      - Автоматический кэш без настроек
      - Эфемерный блок с пятиминутным TTL
      - Отказ от кэширования независимо от объёма корпуса
    correct: 0
    explanation: >-
      Для многодневного переиспользования лучше подходит явный CachedContent
      Gemini: объект сохраняется дольше эфемерного TTL, а стоимость учитывает
      хранение.
  - question: >-
      Два промпта выглядят одинаково, но в одном после двоеточия два пробела, а
      в другом один. Попадёт ли второй запрос в кэш первого?
    options:
      - >-
        Нет: изменение содержимого нарушает совпадение последовательности
        токенов
      - 'Да: пробельные различия всегда игнорируются'
      - 'Да, но скидка будет пропорциональна совпавшей части'
      - Это зависит только от наличия cache_control
    correct: 0
    explanation: >-
      Prompt cache проверяет точное совпадение токенов в префиксе. Даже
      небольшое изменение форматирования может нарушить совпадение с этого
      места.
---

Этот урок дальше в курсе работает не как отдельная тема, а как привычка при сборке любого запроса. Как только появятся агенты с длинным системным промптом, наборами инструментов и растущей историей диалога, ты будешь автоматически спрашивать: что здесь стабильно, а что меняется от вызова к вызову — и в каком порядке это положить.

Спотыкаются обычно на трёх местах. Первое: кажется, что кэш ищет похожее — а он сравнивает токены от начала, побайтово, как в [шаге 45](#step-45). Второе: забывают про надбавку за запись и радуются кэшу там, где чтений слишком мало ([шаг 20](#step-20)). Третье: не смотрят в поля `usage` — и поломка живёт неделями молча.

Проверь себя на четырёх вопросах.
