---
id: 038-final-quiz
type: quiz
title: >-
  Итоговая проверка: спроектировать восстановление, очередь и effective-once
  побочные эффекты
source_anchor: '### The canonical production checklist'
check:
  - question: >-
      В чекпоинте сохранён завершённый super-step 7. Воркер начал шаг 8 и упал
      до следующего чекпоинта. Как должен продолжить другой воркер?
    options:
      - 'Начать шаг 9, потому что шаг 8 уже запускался'
      - >-
        Восстановить состояние после шага 7 и повторить шаг 8, защитив побочные
        эффекты от дублей
      - 'Начать весь прогон заново, чтобы гарантировать согласованность'
      - 'Ждать возвращения прежнего воркера, потому что только он знает состояние'
    correct: 1
    explanation: >-
      Подтверждено только состояние после шага 7, поэтому шаг 8 может
      выполниться повторно. Из-за этого восстановление необходимо дополнять
      идемпотентными побочными эффектами.
  - question: >-
      Два воркера одновременно видят одну задачу в очереди. Какой механизм
      позволяет одному начать работу, а другому позже подхватить её после
      падения владельца?
    options:
      - 'Оба выполняют задачу, а система сохраняет самый быстрый результат'
      - Задачу навсегда закрепляют за первым увидевшим её воркером
      - >-
        Атомарный claim назначает одного владельца, а истёкший lease разрешает
        повторную выдачу
      - Воркеры договариваются через случайную задержку перед запуском
    correct: 2
    explanation: >-
      Атомарный захват исключает одновременное владение задачей, а ограниченный
      по времени lease позволяет вернуть её в очередь, если владелец перестал
      продлевать аренду.
  - question: >-
      Платёж нужно выполнить effective-once, хотя доставка задачи допускает
      повторы. Какой путь выдержит сбой между фиксацией состояния и вызовом
      платёжного сервиса?
    options:
      - >-
        Записать новое состояние и намерение в outbox одной транзакцией, затем
        повторять отправку с тем же ключом дедупликации
      - >-
        Сначала вызвать платёжный сервис, а затем при возможности сохранить
        чекпоинт
      - Отправить платёж один раз без повторов и считать тайм-аут отказом
      - После каждого тайм-аута создавать новый ключ операции
    correct: 0
    explanation: >-
      Outbox атомарно сохраняет бизнес-изменение и намерение отправки, а
      постоянный ключ дедупликации не позволяет повторным доставкам превратиться
      в несколько платежей.
  - question: >-
      У тебя 10 000 ожидающих LLM-вызовов и долгие прогоны, которые переживают
      деплой. Какой набор решений наиболее согласован?
    options:
      - >-
        Поток на каждый вызов, перезапуск всех прогонов при деплое и только
        общий счётчик ошибок
      - >-
        Асинхронный I/O, закрепление прогона за версией рантайма и наблюдаемость
        по super-step, повторам, задержкам и стоимости
      - >-
        Один последовательный воркер, обновление рантайма посреди прогона и
        ручная проверка логов
      - >-
        Отдельный процесс на каждый ожидающий вызов и удаление старых чекпоинтов
        перед деплоем
    correct: 1
    explanation: >-
      Асинхронный I/O экономно обслуживает множество ожидающих запросов,
      rainbow-деплой сохраняет совместимость долгих прогонов, а детальная
      наблюдаемость выявляет причины и цену повторов.
---

Дальше тебе понадобится собирать эти решения в одну систему, а не выбирать их по отдельности. Главная ловушка — принять чекпоинт за гарантию единственного выполнения: после сбоя super-step может повториться, очередь доставит задачу снова, а внешний сервис уже успеет принять запрос. Сверяй свой проект с цепочкой из [шага 3](#step-3), [шага 19](#step-19), [шага 27](#step-27) и [шага 29](#step-29): восстановление, владение, дедупликация и outbox должны состыковаться.
