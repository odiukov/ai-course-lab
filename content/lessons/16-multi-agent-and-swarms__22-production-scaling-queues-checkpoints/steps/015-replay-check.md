---
id: 015-replay-check
type: check
title: С какого шага продолжит второй воркер и какой шаг может выполниться повторно?
source_anchor: '### A checkpoint-per-step runtime'
check:
  - question: >-
      Воркер выполнил внешний запрос в текущем super-step, но упал до записи
      чекпоинта этого шага. Что возможно после передачи прогона другому воркеру?
    options:
      - >-
        Новый воркер гарантированно пропустит внешний запрос, поскольку сервис
        уже его получил
      - >-
        Новый воркер может повторить весь неподтверждённый super-step вместе с
        внешним запросом
      - >-
        Рантайм восстановит оперативную память упавшего процесса и продолжит со
        следующей инструкции
      - Прогон автоматически считается завершённым
    correct: 1
    explanation: >-
      Для рантайма выполнение подтверждается записью чекпоинта. Если падение
      произошло раньше, super-step может быть запущен повторно, даже когда его
      внешний эффект уже состоялся.
---

Проверь, различаешь ли ты фактическое выполнение шага и его подтверждение чекпоинтом. После смены воркера продолжение начинается не с места падения и не со следующего по догадке шага, а с шага после последнего сохранённого состояния — как в [шаге 14](#step-14).

Главная ловушка находится между внешним эффектом и записью чекпоинта: действие уже могло произойти, но рантайм этого не видит. Тогда весь неподтверждённый super-step выполняется повторно. Именно поэтому внешним действиям нужна идемпотентность из [шага 8](#step-8): повторный запуск допустим, повторный результат — нет.
