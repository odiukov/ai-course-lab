---
id: 011-check-branches
type: check
title: 'Проверка: какая ветка отвечает за что'
source_anchor: '### Three parallel branches'
check:
  - question: >-
      Модель дописывает закрывающую скобку в коде: открылась она четыре токена
      назад. Какой вес гейта, скорее всего, будет самым большим?
    options:
      - g_cmp — сжатая ветка видит всю последовательность целиком
      - g_win — нужная скобка лежит внутри последних W токенов
      - g_sel — top-k блоки всегда точнее остальных веток
      - 'Все веса будут примерно равны, потому что гейт делит вклад поровну'
    correct: 1
    explanation: >-
      Связь короткая и локальная, поэтому нужные данные доступны оконной ветке
      без сжатия и отбора. Сжатое представление может не сохранить одиночную
      скобку, а выбранной ветке незачем искать её среди дальних блоков.
  - question: 'Что случится с выбранной веткой, если сжатую ветку выключить совсем?'
    options:
      - 'Ничего: выбранная ветка вычисляет top-k блоков самостоятельно'
      - >-
        Выбранная ветка потеряет сигнал маршрутизации, потому что оценки сжатой
        ветки определяют загружаемые блоки
      - 'Выбранная ветка загрузит все блоки, сохранив прежнюю разреженность'
      - Вес g_sel автоматически станет нулевым
    correct: 1
    explanation: >-
      Оценки attention по сжатым токенам служат сигналом для выбора top-k
      блоков. Без этих оценок у выбранной ветки нет предусмотренного NSA способа
      определить, какие несжатые K/V нужно загрузить.
---

Три ветки уже разобраны по отдельности — сжатая из [шага 7](#step-7), выбранная из [шага 8](#step-8), оконная из [шага 9](#step-9), и гейт из [шага 10](#step-10). Но по отдельности их держит в голове почти каждый; спотыкаются на другом.

Первое место, где обычно едет: сжатая и выбранная кажутся двумя способами сделать одно и то же. Они не параллельны — первая кормит вторую, её оценки и есть маршрут.

Второе: соблазн выбрать ветку «по важности вообще», а не по конкретному токену. Гейт решает заново на каждой позиции.

Проверь оба места на вопросах ниже.
