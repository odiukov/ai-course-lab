---
id: 051-quiz
type: quiz
title: 'Итоговая проверка: три ветки, гейт, поток градиента и бюджет вычислений'
source_anchor: '## Key Terms'
check:
  - question: >-
      Для поздней позиции длина доступного контекста равна 48 000 токенов. В NSA
      используются блоки по 48 токенов, выбираются 12 блоков, а ширина окна
      равна 384. Сколько операций сопоставления запроса с ключом выполнят три
      ветки суммарно при оценке бюджета без вычитания пересечений?
    options:
      - '1960'
      - 48 000
      - '1384'
      - '960'
    correct: 0
    explanation: >-
      Сжатая ветка обрабатывает 48 000 / 48 = 1000 ключей, выбранная — 12 × 48 =
      576, оконная — 384. Итого 1000 + 576 + 384 = 1960 операций.
  - question: >-
      Как корректно описать обучение оценок, по которым NSA выбирает top-k
      блоков?
    options:
      - >-
        Операция top-k дифференцируема, поэтому градиент проходит
        непосредственно через выбранные индексы
      - >-
        Потери обучают оценки через дифференцируемый выход сжатой ветки, а top-k
        использует те же оценки для дискретной адресации блоков
      - Для индексов top-k всегда применяется straight-through estimator
      - 'Оценки блоков не обучаются: их заменяет выход гейта веток'
    correct: 1
    explanation: >-
      Дискретные индексы top-k сами не передают полезный градиент. Обучающий
      сигнал получают оценки обычного attention сжатой ветки, а затем эти же
      оценки используются для маршрутизации выбранной ветки.
  - question: >-
      Какое последствие наиболее вероятно, если из NSA удалить оконную ветку,
      сохранив сжатую и выбранную?
    options:
      - Модель полностью потеряет доступ к глобальному контексту
      - Несжатый KV-кэш автоматически станет значительно меньше
      - >-
        Исчезнет гарантированный мелкозернистый доступ к ближайшему контексту,
        что может ухудшить моделирование локальных зависимостей
      - >-
        Гейт перестанет вычисляться, поскольку его веса обязаны быть
        вероятностями
    correct: 2
    explanation: >-
      Оконная ветка гарантированно читает недавние токены в полном разрешении.
      Сжатая ветка сохраняет глобальный обзор, а выбранная может случайно
      захватить соседние блоки, но не обеспечивает их постоянное локальное
      покрытие.
  - question: >-
      Запрос NSA прочитал лишь небольшую долю блоков выбранной ветки. Что
      следует сделать с несжатыми ключами и значениями остальных блоков перед
      поступлением следующего запроса?
    options:
      - 'Удалить их: следующий запрос обязан использовать тот же top-k'
      - >-
        Оставить только локальное окно, поскольку дальние блоки уже представлены
        средними значениями
      - >-
        Сохранить их: другой запрос может выбрать иной набор блоков и
        потребовать исходные ключи и значения
      - Выгрузить их окончательно после первого пропуска через сжатую ветку
    correct: 2
    explanation: >-
      Маршрутизация зависит от запроса, поэтому любой дальний блок может
      понадобиться позднее. NSA сокращает число чтений и вычислений, но
      выбранная ветка по-прежнему требует полного несжатого KV-кэша.
---

Урок дал тебе четыре вещи, и дальше в курсе они будут работать вместе. Первое: три ветки делят обязанности — общая картина, детали важных блоков, ближайшие соседи, — а гейт из [шага 10](#step-10) решает, чей голос сейчас громче. Второе: разреженность можно выучивать, если дискретный выбор оставить адресом, а оценки считать дифференцируемо. Третье: бюджет считается по формуле из [шага 18](#step-18), и выигрыш растёт с длиной контекста.

Чаще всего спотыкаются на двух местах. Путают «градиент течёт через top-k» с «градиент течёт в оценки, а top-k их читает» — это разные утверждения. И решают, что срезанные вычисления автоматически означают срезанный KV-кэш; [шаг 49](#step-49) как раз про то, почему нет.
