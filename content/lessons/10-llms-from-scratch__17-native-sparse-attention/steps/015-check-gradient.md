---
id: 015-check-gradient
type: check
title: 'Проверка: куда доходит градиент в NSA и в обрезке KV'
source_anchor: '### Why this is "natively trainable"'
check:
  - question: >-
      В обрезке KV-кэша токен, который не попал в отобранные, выбрасывается. Что
      происходит с градиентом по нему на шаге обучения?
    options:
      - >-
        Градиент доходит до него ослабленным, пропорционально его оценке
        важности
      - >-
        Градиент до него не доходит вообще: его нет в выходе, значит нечему течь
        назад
      - 'Градиент доходит через оценку, по которой его выбросили'
      - >-
        Градиент доходит, но только на первых шагах обучения, пока веса не
        устоялись
    correct: 1
    explanation: >-
      Выброшенный токен не участвует в выходе, поэтому производная по нему равна
      нулю — учиться на этом решении нечему. Именно поэтому обрезка остаётся
      приёмом инференса.
  - question: В NSA операция top_k тоже дискретна. Почему она при этом не рвёт обучение?
    options:
      - >-
        Её заменяют гладкой softmax-релаксацией, а жёсткий отбор включают только
        на инференсе
      - Через неё пропускают приближённый градиент вручную заданной формулой
      - >-
        Она не входит в формулу выхода: выбирает адрес блоков, а градиент идёт
        через оценки сжатой ветки и через сам вклад выбранных блоков
      - >-
        Число k подбирают заранее, поэтому выбор оказывается константой и
        градиент не нужен
    correct: 2
    explanation: >-
      top_k только указывает, какие блоки поднять из памяти. Оценки, по которым
      он решал, живут в сжатой ветке и дифференцируемы, а вклад поднятых блоков
      в выход дифференцируем тоже — обучаемая цепочка нигде не разрывается.
---

Тут стоит остановиться и проверить одну вещь: ты должен уметь показать пальцем, где именно проходит граница обучаемого. Не «NSA обучаемо, а обрезка нет» на словах, а по цепочке: что попало в выход, то получает градиент; что не попало — не получает ничего.

Спотыкаются обычно на двух местах. Первое: кажется, что раз оценка важности в обрезке KV посчитана, то через неё что-то и обучится — но выброшенный токен в выходе не участвует, и обучать нечего. Второе: кажется, что дискретность top_k надо как-то «чинить» — сгладить, приблизить градиент. В NSA её не чинят: её просто выносят из формулы выхода, как разбирали в [шаге 14](#step-14).
