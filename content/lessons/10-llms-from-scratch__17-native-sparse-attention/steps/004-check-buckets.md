---
id: 004-check-buckets
type: check
title: 'Проверка: почему обе корзины проигрывают'
source_anchor: '## The Problem'
check:
  - question: >-
      Модель обучили с плотным attention, а на инференсе включили обрезку
      KV-кэша. Почему ускорение выходит меньше ожидаемого?
    options:
      - 'Обрезка работает только на коротких контекстах, а на 64k отключается'
      - >-
        Модель училась опираться на все токены, поэтому выкинуть удаётся
        немного, и экономия появляется только на этапе декодинга
      - 'KV-кэш занимает мало памяти, поэтому его сокращение ничего не даёт'
      - Обрезка требует пересчёта всех слоёв заново и съедает выигрыш
    correct: 1
    explanation: >-
      Обученная на плотном attention модель рассчитывает на все токены:
      агрессивно резать нельзя без потери качества, а обучение и предзаполнение
      остаются плотными — отыгрывается лишь доля потенциального ускорения.
  - question: >-
      Чем принципиально различаются провалы фиксированного шаблона и обрезки
      KV-кэша?
    options:
      - 'Оба проваливаются по одной причине: слишком мало токенов остаётся в окне'
      - >-
        Фиксированный шаблон теряет качество на дальнем поиске, потому что
        решает заранее и вслепую, а обрезка теряет ускорение, потому что модель
        к разреженности не готовили
      - 'Фиксированный шаблон медленный, а обрезка неточная'
      - >-
        Фиксированный шаблон работает только на инференсе, а обрезка — только на
        обучении
    correct: 1
    explanation: >-
      Это две разные болезни: у шаблона проблема в качестве — он выбрасывает
      информацию по правилу, не глядя на текст; у обрезки проблема в выигрыше —
      плотно обученную модель нельзя резать достаточно сильно.
---

Останови себя на секунду и попробуй одной фразой сказать, почему провалились обе корзины. Если получается «в обеих выбрасывают токены» — это ещё не понимание: выбрасывают обе, а ломается у них разное.

После [шагов 2](#step-2)–[3](#step-3) ты должен уметь развести два провала. У фиксированного шаблона страдает качество: он решает, что читать, до того, как увидел текст, и на дальнем поиске нужный фрагмент оказывается за прорезью. У обрезки KV страдает сам выигрыш: модель обучали на плотном attention, она привыкла опираться на всё, поэтому резать сильно нельзя, а обучение и предзаполнение всё равно остаются плотными.

Типичная ошибка — считать, что обрезку можно просто сделать агрессивнее и получить ту же экономию. Нельзя: качество падает раньше, чем приходит ускорение.
