---
id: 016-check-two-classes
type: check
title: 'Проверка: какой класс агента под эту задачу'
source_anchor: '### LiveKit Agents (livekit/agents)'
check:
  - question: >-
      Ты делаешь банковского агента: нужно сохранять распознанные слова клиента
      и проверять текст до ответа. Какой класс выберешь?
    options:
      - 'MultimodalAgent, потому что он передаёт аудио напрямую'
      - 'VoicePipelineAgent, потому что между входом и выходом доступен текст'
      - >-
        MultimodalAgent, потому что он автоматически добавляет текстовый
        контроль
    correct: 1
    explanation: >-
      VoicePipelineAgent разделяет обработку на STT, LLM и TTS, поэтому
      распознанный текст можно журналировать, проверять и фильтровать.
  - question: >-
      Для разговорного персонажа важнее минимальная задержка, а сохранять и
      проверять распознанный текст не требуется. Что подходит лучше?
    options:
      - VoicePipelineAgent с обязательным текстовым промежуточным этапом
      - Оба класса всегда дают одинаковую задержку и контроль
      - MultimodalAgent с прямой обработкой аудио
    correct: 2
    explanation: >-
      MultimodalAgent работает с аудио напрямую и убирает отдельный текстовый
      этап, поэтому подходит, когда скорость важнее контроля над расшифровкой.
---

Здесь ты проверяешь не названия классов, а умение заметить главное ограничение задачи. Как разобрано в [шаге 15](#step-15), прямой путь аудио обычно выбирают ради скорости, а каскад — ради доступа к тексту между этапами.

Чаще всего спотыкаются на слове «голосовой»: оба варианта действительно голосовые. Решает другое. Если тебе нужны журналирование, фильтрация или проверка распознанной реплики, нужен `VoicePipelineAgent`. Если промежуточный текст не важен, а задержку хочется сократить, подходит `MultimodalAgent`. Сначала найди обязательное требование — и выбор класса перестанет быть угадыванием.
