- concept: прямая формула 1/(1+exp(-x)) переполняется на больших отрицательных аргументах, потому что показатель экспоненты становится большим положительным
  question: Сигмоида вычисляется как `1.0 / (1.0 + np.exp(-x))`. На каком входе float64 даст переполнение и предупреждение?
  explanation: 'В этой формуле в экспоненту идёт -x. При x = -750 это exp(750) ≈ 10^325 — за пределом float64 (максимум ~1.8·10^308, порог переполнения около x = -709). При x = 750 считается exp(-750), что безопасно уходит в 0 и даёт результат 1.0. Ограниченность самого результата отрезком (0, 1) ничего не гарантирует: переполняется промежуточное значение.'
  kind: choice
  options:
    - x = -750, потому что считается exp(750)
    - x = 750, потому что считается exp(750)
    - x = 750, потому что знаменатель обращается в ноль
    - 'переполнения не будет ни при каком x: результат всегда лежит между 0 и 1'
  correct: 0
  id: 016-write-sigmoid-1
  fingerprint: b5f2ef05
- concept: устойчивая ветка для отрицательного аргумента получается умножением числителя и знаменателя на exp(x), чтобы показатель экспоненты остался неположительным
  question: Заполни ветку численно устойчивой сигмоиды для случая x < 0, чтобы в экспоненту попадал только неположительный показатель.
  explanation: 'Умножаем числитель и знаменатель исходной дроби 1/(1+exp(-x)) на exp(x): получаем exp(x)/(exp(x)+1). Выражение алгебраически тождественно, но при x < 0 показатель отрицателен, exp(x) лежит в (0, 1), и переполнения нет. Симметрично для x >= 0 берут 1/(1+exp(-x)), где показатель -x неположителен.'
  kind: cloze
  template: 'sigmoid(x) = np.exp(x) / (1.0 + ___)   # ветка x < 0'
  answer: np.exp(x)
  accept:
    - exp(x)
    - np.exp(x)
    - math.exp(x)
  id: 016-write-sigmoid-2
  fingerprint: 475ad5f4
- concept: np.where выбирает элементы уже после того, как обе ветки вычислены целиком, поэтому не предотвращает само переполнение
  question: Реализация `np.where(x >= 0, 1/(1+np.exp(-x)), np.exp(x)/(1+np.exp(x)))` вызвана на массиве, где есть элемент -900. Что произойдёт?
  explanation: 'np.where — обычная функция: оба её аргумента-массива вычисляются полностью ещё до выбора элементов. На элементе -900 первая ветка считает exp(900) — переполнение и RuntimeWarning, значение inf. Итоговый ответ корректен, потому что для этого элемента берётся вторая ветка, но предупреждение уже выдано. Чтобы переполнения не было даже внутри вычисления, элементы делят булевой маской и обрабатывают ветки на разных подмассивах.'
  kind: choice
  options:
    - 'Результат верный, предупреждений нет: np.where вычисляет только нужную ветку для каждого элемента'
    - 'Результат верный, но RuntimeWarning об overflow всё равно возникнет: обе ветки считаются целиком на всём массиве'
    - Функция упадёт с исключением OverflowError на элементе -900
    - Для элемента -900 вернётся NaN, потому что выбранная ветка переполнилась
  correct: 1
  id: 016-write-sigmoid-3
  fingerprint: 5d0af0be
