---
id: 054-check-keysy
type: check
title: 'Проверка: общая ошибка всех трёх кейсов'
source_anchor: '### Real Attack Case Studies'
check:
  - question: >-
      Bing с промптом Sydney, плагин ChatGPT, читающий вредоносный сайт, и
      письмо со скрытым текстом. Что во всех трёх случаях сломалось одинаково?
    options:
      - Модель была слишком слабая и не поняла запрос
      - >-
        Система не отличала текст, который надо прочитать, от команды, которую
        надо выполнить
      - 'Не стоял лимит длины входа, и атака прошла длинным сообщением'
      - Пользователь получил доступ к настройкам модели напрямую
    correct: 1
    explanation: >-
      Во всех трёх кейсах вредоносные слова лежали в контенте — в сообщении, на
      странице, в письме — а система прочитала их как приказ. Отсюда обе защиты:
      системный уровень не перебивается снизу, а найденный контент остаётся
      данными.
  - question: >-
      В кейсе с плагинами данные утекли через markdown-тег картинки. Почему это
      важная деталь?
    options:
      - 'Картинка обходит фильтр длины, потому что занимает мало символов'
      - >-
        Взлома не было вовсе: браузер сам отправил запрос на адрес атакующего,
        просто отрендерив ссылку
      - >-
        Модель специально кодирует данные в изображение, чтобы их не увидели
        фильтры
      - 'Плагины умеют отправлять файлы, а обычный чат — нет'
    correct: 1
    explanation: >-
      Достаточно было вывести ссылку на картинку с адресом атакующего и данными
      в нём — рендер сработал сам. Утечка случилась не в модели, а в доверчивом
      отображении её ответа.
---

Три громкие истории — Sydney из [шага 35](#step-35), плагины и письмо из [шага 53](#step-53) — выглядят как три разные дыры. После этого куска ты должен уметь свести их к одной: система не отличала текст, который надо прочитать, от команды, которую надо выполнить. Отсюда обе защиты из [шага 52](#step-52): иерархия инструкций и изоляция найденного контента.

Спотыкаются обычно на двух местах. Первое — ищут отдельное лекарство под каждый кейс, хотя корень один. Второе — думают, что утечка через картинку требует взлома: там сработал сам рендер ссылки, без единой строчки эксплойта.
