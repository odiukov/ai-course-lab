---
id: 002-sboy-parsera
type: check
title: Как содержательно верный ответ обнуляет результат конвейера
source_anchor: '## The Problem'
check:
  - question: >-
      Классификатор должен вернуть positive, но отвечает: «Настроение
      положительное, потому что покупатель доволен». По смыслу ответ верен. Что
      произойдёт, если парсер принимает только точное значение positive?
    options:
      - Парсер сам извлечёт нужный смысл
      - 'Ответ не пройдёт разбор, и конвейер потеряет верное предсказание'
      - Ответ засчитается как neutral
      - Модель автоматически повторит запрос
    correct: 1
    explanation: >-
      Парсер проверяет допустимую форму, а не понимает рассуждение модели.
      Лишний текст превращает содержательно верный ответ в ошибку конвейера.
  - question: >-
      Модель правильно определила тональность всех отзывов, но каждый раз
      добавляла пояснение, из-за чего парсер отклонил все ответы. Какой
      результат увидит система?
    options:
      - 'Идеальную оценку, потому что смысл ответов верен'
      - Небольшое снижение оценки за лишние слова
      - 'Нулевой результат, потому что ни одно предсказание не дошло до проверки'
    correct: 2
    explanation: >-
      Для системы отклонённый ответ не становится предсказанием. Если парсер
      отверг всё, содержательная точность модели уже не спасает итоговую
      метрику.
---

На [шаге 1](#step-1) ты увидел, почему просьба вернуть JSON ещё не создаёт контракт. Теперь проверь более коварную ситуацию: модель рассуждает правильно, но нарушает форму ответа.

Здесь легко оценивать только смысл и решить, что «почти всё сработало». Однако конвейер видит не намерение модели, а строку, которую должен разобрать парсер. Если ожидается ровно `positive`, пояснение после него может сделать весь ответ непригодным. При массовом отказе разбора F1 падает до 0,0, даже когда каждое решение по смыслу верно.

> 🎒 **На пальцах.** Ты верно решил тест, но вписал ответы на полях вместо клеток бланка. Преподаватель поймёт тебя, сканер — нет. Если проверяет сканер, правильные мысли превращаются в ноль баллов.
