---
id: 021-proverka-cikla
type: check
title: 'Когда цикл завершается успешно, а когда аварийно'
source_anchor: '### Step 1: regex-constrained generation from scratch'
check:
  - question: >-
      Автомат достиг принимающего состояния до исчерпания лимита. Как
      завершилась генерация?
    options:
      - 'Успешно: требуемая структура полностью сформирована'
      - 'Аварийно: цикл обязан выполнить все предусмотренные итерации'
      - 'Неопределённо: успех зависит только от вероятности последнего токена'
    correct: 0
    explanation: >-
      Принимающее состояние подтверждает полное совпадение вывода с
      ограничением. Выполнять оставшиеся итерации не требуется.
  - question: >-
      Лимит токенов исчерпан, но состояние автомата осталось непринимающим. Как
      следует обработать возвращённую строку?
    options:
      - 'Принять как готовую, поскольку все выбранные токены были допустимыми'
      - >-
        Пометить как аварийно оборванную и не передавать дальше как готовый
        результат
      - Добавить наиболее вероятный токен без учёта ограничений
    correct: 1
    explanation: >-
      Допустимость отдельных продолжений не означает завершённости всей
      структуры. Если лимит закончился раньше достижения принимающего состояния,
      результат остаётся незавершённым.
---

В `generate_constrained` из [шага 20](#step-20) любой `break` приводит к возврату строки, но причины выхода неравнозначны. Успех — только состояние, для которого `is_accept` вернул `True`. Пустой `valid` и исчерпанный `max_tokens` — защитные аварийные остановки: строка может быть допустимым началом, но ещё не готовым результатом. Поэтому в рабочем конвейере важно сохранять причину завершения, а не судить об успехе по самому факту возврата.

> 🎒 **На пальцах.** Стиральная машина может остановиться после программы «Готово», из-за перекрытой воды или по аварийному таймеру. Во всех трёх случаях барабан больше не крутится, но только первый означает чистое бельё. Так же и здесь: остановка цикла ещё не успех; успех подтверждает только принимающее состояние.
