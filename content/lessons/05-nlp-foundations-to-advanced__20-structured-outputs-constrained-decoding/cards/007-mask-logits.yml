- concept: маска логитов создаётся как новый список той же длины, чтобы сохранить соответствие token_id позициям
  question: Заполни пропуск в реализации mask_logits.
  explanation: Маскированный список должен иметь ту же длину, что и исходный вектор логитов. Тогда token_id по-прежнему совпадает с индексом, а создание нового списка не изменяет входные logits.
  kind: cloze
  template: |-
    def mask_logits(logits, valid_token_ids):
        masked = [float("-inf")] * ___
        for token_id in valid_token_ids:
            masked[token_id] = logits[token_id]
        return masked
  answer: len(logits)
  accept:
    - logits.__len__()
  id: 007-mask-logits-1
  fingerprint: d6a4632e
- concept: пустой набор допустимых токенов требует остановки генерации до вызова сэмплера
  question: Что должна вернуть mask_logits при пустом valid_token_ids и почему вызывающий код не должен передавать такой результат сэмплеру?
  explanation: 'mask_logits только строит маску и потому закономерно возвращает одни -inf. Проверка пустого valid относится к управляющему циклу: он должен завершить генерацию до маскирования или сэмплирования.'
  kind: open
  reference: 'Функция должна вернуть вектор исходной длины, целиком заполненный -inf. Это означает, что допустимых продолжений нет, поэтому внешний цикл генерации должен остановиться: сэмплировать токен из распределения, в котором запрещены все варианты, нельзя.'
  id: 007-mask-logits-2
  fingerprint: 8855ddd9
