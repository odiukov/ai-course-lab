- concept: относительный порядок полей схемы проверяется по их позициям без сортировки ключей
  question: Заполни пропуск так, чтобы функция принимала схему, где reasoning предшествует answer, даже если между ними расположены другие поля.
  explanation: Сравнение индексов сохраняет исходный порядок ключей и не зависит от посторонних полей между reasoning и answer. Если одного из полей нет, функция заранее возвращает False.
  kind: cloze
  template: |-
    def check_field_order(schema):
        keys = list(schema.get("properties", {}).keys())
        if "reasoning" not in keys or "answer" not in keys:
            return False
        return ___
  answer: keys.index("reasoning") < keys.index("answer")
  accept:
    - keys.index('reasoning') < keys.index('answer')
    - keys.index("reasoning") < keys.index("answer")
  id: 025-check-field-order-1
  fingerprint: eb619620
