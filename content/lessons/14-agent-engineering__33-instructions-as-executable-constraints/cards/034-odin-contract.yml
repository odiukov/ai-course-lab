- concept: 'одно правило принуждается в разные моменты: постфактум по трейсу в CI, на границе входа/выхода агента в guardrail, во время выполнения узла в interrupt'
  question: Одно approval-правило подключено к CI, к guardrails агентского SDK и к прерываниям графа задач. Чем различаются эти подключения?
  explanation: Контракт один, точек принуждения три. CI ловит расхождение задним числом по трейсу, guardrail не пропускает опасный вход или выход, прерывание останавливает узел и показывает правило человеку. Формулировка и строгость при этом не размножаются по средам.
  kind: choice
  options:
    - 'Моментом проверки и реакцией: CI проверяет журнал уже завершённого прогона, guardrail — вход или выход агента до продолжения, прерывание — выполняющийся узел, с паузой на ответ человека'
    - 'Содержанием правила: каждая среда требует своей формулировки условия одобрения'
    - 'Строгостью: в CI правило обязательно, в guardrail и прерывании — рекомендательно'
    - Тем, что только прерывание проверяет правило, а остальные две среды его документируют
  correct: 0
  id: 034-odin-contract-1
  fingerprint: 89572c76
- concept: 'переносимость обеспечена составом записи: текст плюс имя функции-проверки, без объектов конкретного фреймворка'
  question: Почему набор правил переносится между разными инструментами и фреймворками без переписывания?
  explanation: 'Имя проверки работает как точка стыковки: контракт называет, что проверять, среда решает, когда и чем.'
  kind: open
  reference: Запись правила состоит из человекочитаемого текста (категория, описание, строгость) и имени функции-проверки — строки. В ней нет ни объектов SDK, ни конфигурации CI, ни узлов графа, то есть нет ничего, что привязано к одной среде. Переезд в другой инструмент сводится к тому, чтобы реализовать функции с теми же именами и подключить их к нужному моменту исполнения; сам контракт переносится файлом.
  id: 034-odin-contract-2
  fingerprint: 0234ff16
- concept: дублирование контракта в форматах фреймворков порождает расхождение строгости между средами
  question: Команда решила записать одно и то же ограничение трижды — отдельно в конфигурации CI, отдельно в guardrail и отдельно в обработчике прерывания. Какова главная цена такого решения?
  explanation: 'Три записи одного ограничения — три источника истины. Любая правка, забытая в одном месте, даёт тихое расхождение: в CI удаление запрещено, а прерывание всё ещё пропускает его без вопроса. Единственная запись плюс имя проверки убирают этот класс ошибок.'
  kind: choice
  options:
    - 'Версии со временем разойдутся: одна среда будет запрещать действие, другая — пропускать его, и изменение строгости придётся вносить в трёх местах'
    - Проверки станут выполняться медленнее из-за троекратного повторения
    - Правило нельзя будет пометить severity
    - Человек перестанет видеть правило в диффе ревью
  correct: 0
  id: 034-odin-contract-3
  fingerprint: 4e6e855f
