---
id: 023-check-response-index
type: check
title: >-
  С какой позиции targets начинается первый токен ответа и что произойдёт при
  полном обрезании ответа?
source_anchor: '### Step 2: Sequence Log-Probability'
check:
  - question: >-
      Промпт занимает 12 токенов. С какой позиции массива targets начинается
      первый токен ответа и почему?
    options:
      - >-
        С позиции 12, потому что первые 12 элементов targets заняты токенами
        промпта
      - >-
        С позиции 11, потому что targets — это последовательность без первого
        токена, и индексы сдвинуты на единицу влево
      - >-
        С позиции 13, потому что логит должен стоять после последнего токена
        промпта
    correct: 1
    explanation: >-
      input_ids — последовательность без последнего токена, target_ids — без
      первого. Из-за сдвига логит, предсказывающий первый токен ответа, стоит на
      позиции prompt_len - 1: при промпте из 12 токенов это 11.
  - question: >-
      Обрезка склеенной последовательности по max_seq_len отсекла все токены
      ответа. Почему функция возвращает сторожевое значение, а не сумму по
      оставшимся позициям?
    options:
      - Потому что суммировать логарифмы по токенам промпта запрещено численно
      - >-
        Потому что начало диапазона ответа оказалось не меньше его конца —
        целевых позиций ответа не осталось
      - >-
        Потому что модель не может сделать прямой проход по укороченной
        последовательности
    correct: 1
    explanation: >-
      response_start считается от длины промпта, response_end — от длины
      target_ids. Если обрезка съела ответ, первое становится не меньше второго,
      диапазон пуст, и функция выходит по защитному условию. Слагаемых нет —
      значит и вклада в потерю DPO нет, хотя внешне результат выглядит как
      обычное число.
---

Проверь, удерживаешь ли ты вместе две детали из [шага 21](#step-21): `targets` сдвинуты относительно полной последовательности, поэтому при промпте длины 5 первый токен ответа стоит не на позиции 5, а на позиции 4. Именно логит на этой позиции предсказывает следующий токен — начало ответа.

Частая ловушка при обрезании: нулевой результат не означает, что модель дала ответу вероятность 1. Это лишь значит, что токенов ответа для суммирования не осталось.

> 🎒 **На пальцах.** Представь нитку бусин: первые пять — промпт, следующая — начало ответа. Но подписи лежат не под самой бусиной, а под предыдущей, которая её предсказывает. Поэтому первая подпись ответа стоит под пятой бусиной, на позиции 4. Если ножницы отрезали все бусины ответа, складывать нечего — получается 0.
