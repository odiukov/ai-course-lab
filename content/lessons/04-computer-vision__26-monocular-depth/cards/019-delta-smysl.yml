- concept: максимум взаимно обратных отношений делает delta-критерий симметричным к завышению и занижению глубины
  question: Почему в delta accuracy используют максимум отношений предсказанной глубины к истинной и истинной к предсказанной?
  explanation: Если предсказание в несколько раз больше истины, большим будет первое отношение; если во столько же раз меньше — второе. Их максимум симметрично измеряет мультипликативное расхождение и всегда не меньше единицы.
  kind: choice
  options:
    - Чтобы одинаковое отклонение во столько же раз давало одинаковый результат при завышении и занижении глубины
    - Чтобы сильнее наказывать ошибки на далёких объектах
    - Чтобы преобразовать относительную глубину в метрическую
    - Чтобы усреднить два независимых предсказания модели
  correct: 0
  id: 019-delta-smysl-1
  fingerprint: df6a353b
- concept: delta accuracy является средней долей индикаторов строгого прохождения порога отношения глубин
  question: Заполни выражение для индикатора пикселя, который засчитывается в delta accuracy при пороге τ.
  explanation: Пиксель получает положительный индикатор только при строгом прохождении порога. Delta accuracy равна среднему таких индикаторов по всем допустимым пикселям, поэтому точное попадание на границу не засчитывается.
  kind: cloze
  template: I_i = ___
  answer: max(d_pred / d_gt, d_gt / d_pred) < τ
  accept:
    - max(d_i^pred / d_i^gt, d_i^gt / d_i^pred) < τ
    - max(dpred/dgt, dgt/dpred) < tau
  id: 019-delta-smysl-2
  fingerprint: 6ee584d5
