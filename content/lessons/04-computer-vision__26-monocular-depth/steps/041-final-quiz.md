---
id: 041-final-quiz
type: quiz
title: >-
  Итоговая проверка: шкала глубины, метрики, камера-обскура и подъём детекций в
  3D
source_anchor: '## Key Terms'
check:
  - question: >-
      После выравнивания масштаба и сдвига AbsRel модели резко улучшился. Можно
      ли теперь использовать её значения как расстояния в метрах?
    options:
      - >-
        Да, низкий AbsRel автоматически превращает относительную глубину в
        метрическую
      - >-
        Нет, выравнивание помогает сравнить форму глубины, но не доказывает
        метрическую шкалу на новых кадрах
      - 'Да, если delta accuracy тоже стала выше'
      - 'Нет, потому что относительную глубину вообще нельзя оценивать метриками'
    correct: 1
    explanation: >-
      Выравнивание подгоняет предсказание под известную разметку конкретной
      выборки. Оно не даёт модели устойчивой метрической шкалы для новых сцен.
  - question: >-
      Истинная глубина пикселя равна 2 м, а предсказанная — 2,4 м. Что верно для
      этого пикселя?
    options:
      - 'Вклад в AbsRel равен 0,2, и проверка delta < 1,25 пройдена'
      - 'Вклад в AbsRel равен 0,4, и проверка delta < 1,25 не пройдена'
      - 'Вклад в AbsRel равен 1,2, и проверка delta < 1,25 пройдена'
      - 'Вклад в AbsRel равен 0,2, но проверка delta < 1,25 не пройдена'
    correct: 0
    explanation: >-
      Ошибка составляет 0,4 м относительно 2 м, то есть 0,2. Отношение глубин
      равно 1,2, поэтому оно остаётся ниже порога 1,25.
  - question: >-
      Ты оставляешь пиксель и параметры камеры неизменными, но удваиваешь его
      глубину. Как изменится поднятая 3D-точка?
    options:
      - Удвоится только координата Z
      - 'Координаты X, Y и Z удвоятся'
      - 'X и Y уменьшатся вдвое, а Z удвоится'
      - 'Точка не изменится, потому что пиксель остался прежним'
    correct: 1
    explanation: >-
      При неизменных intrinsics все три координаты пропорциональны глубине.
      Точка движется дальше по тому же лучу от камеры.
  - question: >-
      Внутри рамки объекта есть несколько ошибочных глубин от бликов и кусок
      дальнего фона. Как надёжнее получить одну глубину для подъёма детекции в
      3D?
    options:
      - Взять максимальную глубину во всей рамке
      - 'Усреднить все пиксели, включая невалидные'
      - >-
        Взять медиану валидных глубин, по возможности ограничив пиксели маской
        объекта
      - Использовать яркость центрального пикселя как расстояние
    correct: 2
    explanation: >-
      Медиана слабее реагирует на отдельные выбросы, а маска уменьшает примесь
      фона. После этого выбранную глубину можно спроецировать через параметры
      камеры.
---

Дальше тебе понадобится не отдельная формула, а умение состыковать решения: распознать шкалу глубины из [шагов 3](#step-3) и [22](#step-22), честно прочитать метрики из [шагов 17](#step-17) и [19](#step-19), а затем применить проекцию из [шага 29](#step-29). Частая ловушка — получить правдоподобное облако точек и принять его за метрически точное. Красивый 3D-результат ещё не подтверждает ни масштаб, ни intrinsics.

> 🎒 **На пальцах.** Представь туристическую карту: цвет рельефа показывает, что вершина выше долины, но не сообщает высоту в метрах. Отметки высот задают шкалу, проверка маршрута оценивает ошибки, а координатная сетка переносит выбранную точку в пространство. Если перепутать хотя бы один слой, палатка окажется аккуратно отмечена — но не там.
