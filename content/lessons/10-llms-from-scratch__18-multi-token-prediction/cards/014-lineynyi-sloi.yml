- concept: каждая строка матрицы вычисляет одну координату результата как скалярное произведение со входом
  question: Заполни формулу одной координаты результата умножения матрицы A на вектор x.
  explanation: Строка с индексом j содержит коэффициенты для всех координат входа. Сумма их произведений с соответствующими координатами x образует одну выходную координату y_j.
  kind: cloze
  template: y_j = ___
  answer: sum_m A_{jm} x_m
  accept:
    - Σ_m A_{jm} x_m
    - скалярное произведение строки A_j и вектора x
    - dot(A[j], x)
  id: 014-lineynyi-sloi-1
  fingerprint: 9cd7541a
