---
id: 030-check-srednee
type: check
title: 'Проверка: что случится при сумме вместо среднего'
source_anchor: '### Step 5: per-depth loss'
check:
  - question: >-
      В коде вместо множителя lambda/D по ошибке написали просто lambda, то есть
      потери глубин сложили без деления. Что изменится при D = 4, если потери
      глубин имеют примерно одинаковый масштаб?
    options:
      - 'Ничего: величина loss не влияет на обучение, важен только его знак'
      - Вклад MTP станет примерно вчетверо больше относительно основного loss
      - Вклад MTP станет примерно вчетверо меньше
      - Каждая глубина начнёт получать одинаковый целевой токен
    correct: 1
    explanation: >-
      Без деления четыре слагаемых складываются в величину примерно в четыре
      раза больше их среднего. Из-за этого MTP может начать доминировать над
      основной задачей.
  - question: >-
      Ты сравниваешь два запуска: в первом D = 1, во втором D = 4, а lambda
      одинакова. Зачем в обоих случаях делить сумму потерь по глубинам на D?
    options:
      - >-
        Чтобы сохранить примерно одинаковый общий вес MTP и сравнивать влияние
        числа глубин
      - Чтобы итоговый loss обязательно находился между 0 и 1
      - 'Чтобы softmax на каждой глубине давал сумму вероятностей, равную единице'
      - Чтобы все глубины предсказывали один и тот же токен
    correct: 0
    explanation: >-
      Усреднение фиксирует масштаб вспомогательной задачи. Тогда изменение D
      меняет набор предсказываемых смещений, но не увеличивает автоматически
      силу MTP в полном loss.
---

После [шага 29](#step-29) ты должен уметь предсказать, что произойдёт с обучением, если тронуть множитель перед суммой по глубинам. Не «знать формулу», а именно прикинуть порядок: четыре слагаемых примерно одинакового размера дают сумму около четырёх средних — и добавка, задуманная как подсказка, начинает тянуть модель сильнее, чем основное предсказание следующего токена.

Спотыкаются обычно на двух вещах. Первая: кажется, что общий масштаб loss не важен, ведь оптимизатор всё равно подстроится, — но здесь важно соотношение двух слагаемых, а не абсолютная величина. Вторая: забывают, что без деления число D тайком становится вторым регулятором силы MTP, и два запуска с разным D перестают быть сравнимыми.
