---
id: 034-check-prichinnost
type: check
title: 'Проверка: что видит глубина k+1, чего не видит параллельная голова'
source_anchor: '### Why sequential, not parallel'
check:
  - question: >-
      Что получает модуль на глубине k+1, чего не получает параллельная голова
      номер k+1?
    options:
      - >-
        Более длинный контекст слева: он видит больше предыдущих токенов
        последовательности
      - >-
        Эмбеддинг токена t_{i+k} и состояние предыдущей глубины, то есть уже
        зафиксированный ответ про предыдущую позицию
      - >-
        Собственный словарь и собственную выходную голову, обученные под дальние
        позиции
      - >-
        Больше параметров: у него отдельный трансформерный блок, а у головы
        только матрица
    correct: 1
    explanation: >-
      Вход глубины k+1 склеен из состояния предыдущей глубины и эмбеддинга
      токена на позиции i+k. Параллельная голова смотрит только на общее
      состояние backbone и про соседние позиции ничего не знает.
  - question: >-
      Две схемы предсказывают токен на позиции i+2. Чем отличаются вероятности,
      которые они моделируют?
    options:
      - 'Обе моделируют одно и то же, разница только в скорости обучения'
      - >-
        Параллельная голова моделирует вероятность при условии контекста и
        t_{i+1}, а последовательный модуль — только при условии контекста
      - >-
        Последовательный модуль моделирует вероятность при условии контекста и
        токена t_{i+1}, а параллельная голова — только при условии контекста
      - >-
        Последовательный модуль вообще не моделирует вероятность, он только
        переносит состояние дальше
    correct: 2
    explanation: >-
      Последовательная схема добавляет во вход настоящий t_{i+1}, поэтому её
      распределение обусловлено ещё и им — как у обычного авторегрессивного
      декодера. У параллельной головы этого условия нет.
---

Здесь стоит остановиться и проверить одну вещь, потому что дальше на ней держится весь speculative decoding.

После [шагов 4](#step-4) и [7](#step-7) ты умеешь описывать обе схемы. Теперь нужно уметь другое: назвать ровно ту информацию, которая есть у глубины k+1 и отсутствует у параллельной головы, — и понимать, что из-за неё меняется само распределение, которое модель учит (сравнение двух вероятностей — в [шаге 33](#step-33)).

Спотыкаются обычно на двух вещах. Первая: путают «больше контекста слева» с «знает соседний будущий токен» — у обеих схем контекст один и тот же, разница только в будущем. Вторая: считают, что цепочка нужна ради качества обучения, а не ради того, чтобы модуль умел потреблять собственный выход.
