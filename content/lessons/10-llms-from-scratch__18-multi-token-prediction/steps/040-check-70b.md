---
id: 040-check-70b
type: check
title: 'Проверка: прибавка для плотной 70B с hidden 8192'
source_anchor: '## Exercises'
check:
  - question: >-
      Почему при подсчёте прибавки для плотной модели на 70B с hidden 8192 и
      одним MTP-модулем стоимость модуля не нужно умножать на 80 слоёв основной
      модели?
    options:
      - 'MTP-модуль добавляется один раз, а не после каждого слоя основной модели'
      - Параметры всех слоёв основной модели полностью общие с MTP-модулем
      - Число слоёв влияет только на размер словаря
      - Умножение на число слоёв требуется только для моделей с небольшим hidden
    correct: 0
    explanation: >-
      При D = 1 добавляется один отдельный MTP-модуль стоимостью примерно 14h².
      Восемьдесят слоёв уже относятся к основной модели и не означают, что этот
      модуль создаётся восемьдесят раз.
  - question: >-
      Почему плотная оценка около 940M для такого MTP-модуля не противоречит
      заявленным DeepSeek-V3 14B?
    options:
      - В оценке для плотной модели забыта вторая копия словаря
      - DeepSeek умножает стоимость модуля на число слоёв основной модели
      - >-
        MTP-блок DeepSeek наследует MoE-структуру и хранит веса множества
        экспертов
      - Плотная формула учитывает только параметры нормализации
    correct: 2
    explanation: >-
      Оценка 14h² предполагает плотный MLP. В DeepSeek MTP-блок содержит
      экспертные слои: на токене активируется лишь часть экспертов, но в число
      хранимых параметров входят веса каждого из них.
---

Ты подставил в формулу из [шага 37](#step-37) свои числа и получил цифру. Теперь надо уметь две вещи: довести арифметику до процента от модели и объяснить разрыв с заявленными 14B у DeepSeek.

Спотыкаются обычно на двух местах. Первое — тянут в подсчёт 80 слоёв основной модели: кажется, что модуль должен масштабироваться вместе с ней, но модуль один, и глубина основной сети в формулу не входит вовсе. Второе — считают формулу универсальной. Множитель 12 честен только для плотного MLP; там, где внутри блока эксперты, та же позиция набирает совсем другой вес.
