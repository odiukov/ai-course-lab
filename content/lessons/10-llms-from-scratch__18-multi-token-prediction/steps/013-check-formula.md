---
id: 013-check-formula
type: check
title: 'Проверка: какие формы у входов и выхода на каждой стадии'
source_anchor: '### The sequential MTP recipe'
check:
  - question: >-
      После конкатенации двух скрытых векторов ширины h вход проекции имеет
      ширину 2h. Как должна быть устроена матрица M_k, чтобы её результат можно
      было передать трансформерному блоку ширины h?
    options:
      - >-
        Число столбцов должно соответствовать ширине 2h, а число строк — ширине
        h
      - Число строк и столбцов должно соответствовать ширине 2h
      - >-
        Число столбцов должно соответствовать ширине h, а число строк — размеру
        словаря
      - Матрица должна возвращать по одному значению для каждого токена словаря
    correct: 0
    explanation: >-
      Матрица формы h × 2h принимает склеенный вектор длины 2h и возвращает
      вектор длины h. В числах урока это преобразование из 14336 координат
      обратно в 7168.
  - question: >-
      Почему общая голова Out возвращает вектор в пространстве словаря, а не ещё
      одно скрытое состояние?
    options:
      - Потому что каждому слою трансформера требуется отдельная координата
      - Потому что для кросс-энтропии нужна оценка каждого возможного токена
      - Потому что голова должна хранить эмбеддинг истинного токена
      - Потому что размер словаря всегда совпадает с шириной модели
    correct: 1
    explanation: >-
      Out преобразует скрытый вектор ширины 7168 в логиты по словарю: примерно
      по одному числу для каждого из 129 тысяч токенов. Затем эти логиты
      участвуют в вычислении кросс-энтропии.
---

Ты только что прошёл модуль насквозь — от склейки до головы ([шаг 11](#step-11) и [шаг 12](#step-12)). Теперь стоит проверить не смысл, а формы: сколько чисел лежит в векторе на каждой остановке. Это скучная на вид вещь, но именно на ней ломается понимание всей цепочки.

Спотыкаются обычно в двух местах. Первое: кажется, что после склейки вектор так и остаётся двойной длины — и тогда непонятно, почему модули можно ставить друг за другом. Второе: путают длину скрытого состояния с длиной словаря, а это два совершенно разных пространства — 7168 против 129 тысяч.

Держи в голове одно: вход модуля и выход модуля обязаны совпадать по форме.
