---
id: 047-quiz
type: quiz
title: 'Итоговая проверка: цепочка глубин, joint loss, параметры и черновик'
source_anchor: '## Key Terms'
check:
  - question: >-
      Модуль глубины 2 в последовательной схеме DeepSeek-V3 получает скрытое
      состояние глубины 1 и эмбеддинг токена t_(i+1). Что именно это даёт по
      сравнению с параллельной головой, которая предсказывает t_(i+2) прямо из
      состояния backbone?
    options:
      - >-
        Предсказание t_(i+2) обусловлено ещё и токеном на позиции i+1, а не
        только префиксом до i
      - >-
        Предсказание получается быстрее, потому что модулю не нужно учитывать
        префикс
      - >-
        Модуль тратит меньше параметров, поскольку эмбеддинг заменяет
        трансформерный блок
      - Модуль перестаёт зависеть от основной модели и может обучаться отдельно
    correct: 0
    explanation: >-
      Последовательная схема сохраняет причинную цепочку: следующая глубина
      использует информацию о промежуточном токене. Параллельная голова пытается
      предсказать дальний токен непосредственно из исходного состояния backbone.
  - question: >-
      В joint loss вместо среднего по D глубинам поставили обычную сумму, а
      lambda оставили равной 0.3. Что произойдёт при D = 4?
    options:
      - 'Ничего не изменится: масштаб loss не влияет на итоговые градиенты'
      - >-
        Вклад MTP вырастет примерно вчетверо и может начать перевешивать
        основной loss
      - >-
        Изменение повлияет только на начало обучения, после чего сумма и среднее
        сойдутся
      - >-
        Вклад MTP уменьшится, поскольку суммирование сглаживает потери отдельных
        глубин
    correct: 1
    explanation: >-
      Среднее делит сумму потерь на число глубин и тем самым удерживает масштаб
      MTP-сигнала. Без этого деления каждая новая глубина дополнительно
      увеличивает его вклад.
  - question: >-
      Плотная модель на 70B с hidden 8192 получает один MTP-модуль: проекция
      даёт 2h² параметров, трансформерный блок — около 12h². Насколько примерно
      вырастет модель?
    options:
      - 'Примерно на 940 миллионов параметров, то есть примерно на 1.3%'
      - >-
        Примерно на 134 миллиона параметров, поскольку учитывать нужно только
        проекцию
      - >-
        Примерно на 14 миллиардов параметров независимо от типа трансформерного
        блока
      - 'Примерно на 67 миллионов параметров, то есть на один квадрат hidden'
    correct: 0
    explanation: >-
      Квадрат hidden при 8192 составляет около 67 миллионов. Весь модуль
      содержит примерно 14 таких квадратов, что даёт около 940 миллионов
      параметров.
  - question: >-
      На обучении модуль очередной глубины получает эмбеддинг настоящего
      промежуточного токена из корпуса. Что подаётся вместо него на инференсе,
      когда MTP строит черновик?
    options:
      - Настоящий будущий токен из ещё не сгенерированной части ответа
      - Средний эмбеддинг всех токенов словаря
      - 'Собственный токен, предсказанный предыдущей глубиной'
      - 'Нулевой вектор, чтобы оставить только скрытое состояние'
    correct: 2
    explanation: >-
      На инференсе истинное продолжение неизвестно, поэтому последовательные
      MTP-модули используют собственные предыдущие предсказания. Затем основная
      модель проверяет получившийся черновик.
---

Этот урок дальше в курсе всплывёт дважды: когда речь пойдёт о спекулятивном декодировании целиком и когда будешь читать конфиги реальных чекпойнтов и упрёшься в поля вроде числа MTP-модулей.

Два места, где спотыкаются чаще всего. Первое — путают, чем последовательная схема отличается от параллельной: считают, что дело в количестве голов, хотя дело в том, кто на кого смотрит. Второе — арифметика joint loss: lambda и деление на D решают разные задачи, и подмена одного другим ломает баланс с основным loss.

Ответь на вопросы — и станет видно, какая из четырёх нитей урока держится слабее.
