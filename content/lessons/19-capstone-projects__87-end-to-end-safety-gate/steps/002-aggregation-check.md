---
id: 002-aggregation-check
type: check
title: Проверяем таблицу агрегации и роли точек
source_anchor: '## Concept'
check:
  - question: >-
      Почему при чистых severity-сигналах агрегатор иногда выбирает warn, а не
      allow?
    options:
      - >-
        У детектора есть отдельная ветка для промежуточной уверенности, даже
        если остальные сигналы отсутствуют
      - >-
        Любая ненулевая уверенность детектора автоматически считается high
        severity
      - Отсутствие сигналов всегда означает warn
      - >-
        Уверенность детектора автоматически преобразуется в срабатывание
        токенного фильтра
    correct: 0
    explanation: >-
      Если остальные сигналы равны none, решение зависит от уверенности
      детектора: ниже 0.5 получается allow, а диапазон 0.5–0.85 даёт warn. Это
      отдельная ветка таблицы, а не преобразование уверенности в severity.
  - question: >-
      Токенный фильтр оборвал поток на середине, все остальные сигналы чистые.
      Что будет на выходе?
    options:
      - 'allow: булево значение не severity, в таблицу оно не попадает'
      - 'warn: обрыв считают низким уровнем'
      - 'redact: досрочный обрыв приравнивают к medium, а medium даёт redact'
      - 'block: раз поток оборвали, ответ отдавать нельзя'
    correct: 2
    explanation: >-
      Булево срабатывание токенного фильтра приводят к общей шкале как medium
      severity. Поэтому первой подходящей строкой таблицы становится «any medium
      severity», которой соответствует redact.
---

Сейчас проверим главное умение этого куска: взять четыре сигнала и по таблице сказать, что гейт сделает с ответом. Не заучить строки, а именно прогнать конкретный случай сверху вниз до первого совпадения.

Спотыкаются обычно на двух местах. Первое — токенный фильтр: он отдаёт «да/нет», а не уровень, и легко решить, что в таблице ему места нет. Второе — две последние строки: при чистых severity исход решает одно число, порог 0.5, и allow с warn отличаются только им.

Разберись с этими двумя случаями — и остальная таблица читается сама.
