---
id: 054-check-l1-solution
type: check
title: Где окажется решение для (x−3)²+(y−2)² при |x|+|y|≤1 и почему оно разрежено
source_anchor: '## Exercises'
check:
  - question: >-
      Какой расчёт доказывает, что смещение из (1, 0) вдоль верхней стороны
      L1-ромба не улучшает решение?
    options:
      - 'В точке (1−ε, ε) значение цели больше на 2ε² при любом ненулевом ε'
      - 'В точке (1−ε, ε) значение цели уменьшается линейно с ростом ε'
      - 'На всей стороне от (1, 0) до (0, 1) значение цели постоянно'
    correct: 0
    explanation: >-
      Подстановка (1−ε, ε) даёт значение цели 8+2ε² вместо 8 в точке (1, 0).
      Поэтому любое ненулевое смещение вдоль этой стороны ухудшает результат.
  - question: Как L1-ограничение приводит к разреженности решения в этой задаче?
    options:
      - Квадратичная цель перестаёт зависеть от одной из координат
      - 'Оптимум попадает в угол L1-ромба, где одна координата равна нулю'
      - Каждая точка на границе L1-ромба имеет нулевую координату
    correct: 1
    explanation: >-
      У L1-области есть углы на координатных осях. Здесь ближайшей к (3, 2)
      допустимой точкой оказывается угол (1, 0), поэтому координата y
      обнуляется.
---

Посмотри на угол \((1,0)\). Если сдвинуться от него по верхней стороне ромба на величину \(\varepsilon\), получится \((1-\varepsilon,\varepsilon)\): ты приблизишься к желаемому \(y=2\), но одновременно сильнее отдалишься от более важного здесь \(x=3\). Эти изменения не компенсируются — значение цели вырастет на \(2\varepsilon^2\). Значит, минимум остаётся в углу \((1,0)\).

Именно углы L1-ромба из [шага 53](#step-53) делают решение разреженным: одна координата становится ровно нулевой.

> 🎒 **На пальцах.** Ты несёшь два пакета, но турникет пропускает суммарно только один килограмм. Первый пункт назначения заметно дальше второго, поэтому весь доступный вес выгоднее отдать первому пакету. Деление веса между ними лишь ухудшит результат. Второй пакет останется пустым — это и есть нулевая координата.
