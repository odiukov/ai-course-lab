- concept: двойственная SVM допускает kernel trick, потому что обучающие точки входят в неё только через попарные скалярные произведения
  question: Какое свойство двойственной формулировки SVM позволяет применить kernel trick?
  explanation: В двойственной SVM вектор весов исключён, а зависимость от данных сохраняется в выражениях вида x_i^T x_j. Поэтому каждое такое произведение можно заменить значением ядра K(x_i, x_j).
  kind: choice
  options:
    - После исключения вектора весов обучающие точки входят в задачу только через попарные скалярные произведения
    - Все двойственные коэффициенты становятся ненулевыми
    - Штраф за ошибку классификации исчезает из задачи
    - Исходные точки автоматически становятся линейно разделимыми
  correct: 0
  id: 056-svm-kernel-1
  fingerprint: a2008adf
- concept: допустимое ядро представляет скалярное произведение образов объектов в некотором пространстве признаков
  question: Заполните условие, связывающее ядро с отображением объектов в пространство признаков.
  explanation: Ядро должно возвращать скалярное произведение образов объектов после некоторого отображения φ. Тогда алгоритм использует геометрию нового пространства, не вычисляя координаты φ(x) явно.
  kind: cloze
  template: K(x, z) = ___
  answer: \phi(x)^\top\phi(z)
  accept:
    - φ(x)^Tφ(z)
    - φ(x)ᵀφ(z)
    - ⟨φ(x), φ(z)⟩
    - phi(x)^T phi(z)
  id: 056-svm-kernel-2
  fingerprint: baa3765f
