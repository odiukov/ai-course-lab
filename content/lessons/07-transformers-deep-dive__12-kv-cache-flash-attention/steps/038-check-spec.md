---
id: 038-check-spec
type: check
title: 'Проверка: приняли 4 токена из 5 — какое ускорение'
source_anchor: '### Speculative decoding — the other latency win'
check:
  - question: >-
      Почему длина предложенного черновика сама по себе не определяет ускорение
      спекулятивного декодирования?
    options:
      - >-
        Потому что готовым результатом становятся только токены, принятые
        проверяющей моделью
      - >-
        Потому что большая модель всегда проверяет только последний токен
        черновика
      - >-
        Потому что каждый предложенный токен требует отдельного прохода большой
        модели
      - Потому что скорость зависит только от размера словаря
    correct: 0
    explanation: >-
      Большая модель проверяет кандидатов параллельно, но полезную работу дают
      только принятые токены. Поэтому выигрыш считают по числу принятых токенов,
      а не по полной длине черновика.
  - question: >-
      Что может произойти, если проверяющая модель обычно отклоняет почти весь
      черновик?
    options:
      - 'Ускорение сохранится, поскольку длина черновика не изменилась'
      - >-
        Выигрыш почти исчезнет, а расходы на черновую модель могут сделать
        генерацию медленнее обычной
      - Качество обязательно ухудшится из-за отклонённых токенов
      - Проверка перестанет требовать прямого прохода большой модели
    correct: 1
    explanation: >-
      При низком принятии дорогой проход даёт мало готовых токенов, а время
      построения черновика всё равно расходуется. Поэтому суммарный темп может
      оказаться не выше, а даже ниже обычного декодирования.
---

Здесь проверяется одна вещь: умеешь ли ты пересчитать «принято k из N» в реальный выигрыш. Правило простое — платишь за один прямой проход большой модели, получаешь столько токенов, сколько она приняла.

Спотыкаются обычно на двух местах. Первое: считают по длине черновика, а не по числу принятых токенов — предложить можно хоть десять, засчитываются только те, что прошли проверку до первой ошибки. Второе: забывают, что черновая модель тоже работает и её время входит в счёт, поэтому при низком проценте принятия схема из выигрышной становится убыточной. Качество при этом не страдает никогда: отвергнутое просто не попадает в вывод.
