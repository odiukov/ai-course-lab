---
id: 003-check-pereschet
type: check
title: 'Проверка: почему пересчёт префикса — лишняя работа'
source_anchor: '## The Problem'
check:
  - question: >-
      Ты сгенерировал ответ длиной 4096 токенов наивным декодером. Почему
      большая часть работы считается лишней?
    options:
      - >-
        Модель на каждом шаге заново прогоняет весь префикс, хотя ключи и
        значения этих токенов уже посчитаны и больше не меняются
      - >-
        Модель случайно повторяет одни и те же токены в ответе, и их приходится
        отбрасывать
      - >-
        Модель считает внимание для токенов, которые ещё не сгенерированы, а
        потом стирает результат
      - >-
        Модель тратит время на перевод чисел из одного формата в другой на
        каждом шаге
    correct: 0
    explanation: >-
      Скрытое состояние префиксного токена детерминировано: посчитал один раз —
      оно уже не изменится. Наивный декодер всё равно пересчитывает его на
      каждом новом шаге.
  - question: >-
      Ответ вырос вдвое: не 4096 токенов, а 8192. Как примерно изменится
      суммарное число пар «запрос — ключ» у наивного декодера?
    options:
      - 'Вырастет примерно вдвое: работа на шаг та же, шагов вдвое больше'
      - >-
        Вырастет примерно вчетверо: удлиняется и число шагов, и префикс на
        каждом шаге
      - 'Останется тем же: длина ответа на объём внимания не влияет'
      - Вырастет примерно в восемь раз
    correct: 1
    explanation: >-
      Сумма 1+2+...+N растёт как квадрат N. Удвоил длину — получил примерно
      вчетверо больше пар: и шагов вдвое больше, и каждый шаг смотрит на вдвое
      более длинный префикс.
---

Прежде чем идти дальше, проверь себя на двух вещах, ради которых были [шаг 1](#step-1) и [шаг 2](#step-2).

Первая: ты должен уметь объяснить своими словами, почему пересчёт префикса — это работа впустую. Не «так медленнее», а конкретно: ключи и значения уже написанных токенов застыли, новый токен ничего в них не меняет.

Вторая: почувствовать, как растёт счёт. Тут спотыкаются чаще всего — кажется, что вдвое длиннее ответ означает вдвое больше работы. Это не так, и разница видна уже на цифрах из [шага 2](#step-2).
