---
id: 017-check-1000-tokens
type: check
title: 'Проверка: во сколько раз выигрыш на 1000 токенах'
source_anchor: '### Step 3: compare naive vs cached decoding on 100-token generation'
check:
  - question: >-
      Ответ длиной 1000 токенов. Во сколько раз меньше K/V-проекций делает
      декодер с кэшем по сравнению с наивным?
    options:
      - Примерно в 500 раз
      - Примерно в 1000 раз
      - 'Примерно в 50 раз, как и на 100 токенах'
      - Ровно в 2 раза
    correct: 0
    explanation: >-
      Наивный декодер проецирует 1 + 2 + ... + 1000 = 500500 раз, кэшированный —
      1000. Отношение равно 500500 делить на 1000, то есть 500,5 — примерно
      половина длины ответа.
  - question: Что кэш НЕ убирает на тысячном шаге генерации?
    options:
      - Перебор всех 1000 ключей при подсчёте оценок для текущего запроса
      - 'Проекцию K и V для нового, тысячного токена'
      - Хранение K и V префикса в памяти
      - Умножение вероятностей на матрицу значений
    correct: 0
    explanation: >-
      Запрос последнего токена всё равно смотрит на весь префикс — и с кэшем, и
      без. Кэш убирает повторный пересчёт K и V префикса, а не сам перебор
      ключей.
---

На 100 токенах выигрыш вышел 50,5 раза. Хочется решить, что это просто «полсотни» — такая уж у кэша прибавка. Но подставь 1000: наивный декодер проецирует 1 + 2 + ... + 1000 = 500500 раз, кэшированный — 1000. Выигрыш стал 500 раз, и он поедет дальше вместе с длиной ответа, потому что равен примерно половине N.

После этого куска ты должен уметь взять любую длину ответа и назвать оба числа — и объяснить, за что именно платят. Спотыкаются обычно на двух вещах: считают ускорение постоянным множителем и приписывают кэшу экономию на самом attention. Оценки перебираются по всем ключам в обоих случаях — разница только в том, лежат эти ключи готовыми или их каждый раз считают заново, как в [шаге 16](#step-16).
