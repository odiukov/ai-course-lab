---
id: 040-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      Клиент получил токен для шлюза и хочет обратиться напрямую к
      research-серверу. Почему такой вызов должен быть отклонён и какие гарантии
      исчезнут при обходе шлюза?
    options:
      - >-
        Токен подходит любому компоненту экосистемы, поэтому исчезнет только
        единая трассировка
      - >-
        Audience токена ограничен шлюзом, а обход шлюза также исключит его RBAC,
        сверку манифеста и аудит
      - >-
        Research-сервер автоматически обменяет токен на собственный и выполнит
        запрос
      - 'Вызов разрешён, если имя инструмента совпадает с записью в каталоге'
    correct: 1
    explanation: >-
      Resource indicator ограничивает токен получателем-шлюзом. Именно шлюз
      последовательно применяет авторизацию, RBAC, сверку прибитого манифеста и
      аудит, поэтому прямой вызов лишается всей этой цепочки гарантий.
  - question: >-
      При объединении каталогов шлюз обнаружил, что описание инструмента search
      на одном из серверов не совпадает с прибитым манифестом. Как он должен
      поступить?
    options:
      - 'Опубликовать инструмент, а расхождение только записать в аудит'
      - Добавить к имени префикс сервера и разрешить вызов
      - >-
        Заблокировать инструмент, потому что несовпадение хэша является отказом
        проверки
      - 'Передать решение субагенту, который будет использовать инструмент'
    correct: 2
    explanation: >-
      Префикс устраняет лишь коллизию имён. Он не подтверждает целостность
      описания: новый или изменившийся хэш должен блокировать инструмент, а не
      порождать предупреждение.
  - question: >-
      Оркестратор делегировал задачу A2A-субагенту. Какая информация доступна
      оркестратору с учётом границы непрозрачности?
    options:
      - Полная последовательность внутренних рассуждений субагента
      - 'Никакая: переход к субагенту не отражается в трассе'
      - >-
        Вызов, его состояние и публичный результат, но не внутренние рассуждения
        субагента
      - Только внутренние шаги без общего trace id
    correct: 2
    explanation: >-
      Переход к субагенту остаётся наблюдаемым узлом общей трассы, но граница
      непрозрачности скрывает его внутренние рассуждения. Наружу выходят
      состояние задачи и разрешённый публичный результат.
---

Дальше в курсе всё это перестаёт быть отдельными темами: ты будешь собирать системы, где шлюз, RBAC, прибитый манифест и трасса работают в одном вызове — так, как ты уже прошёл по шагам от [шага 8](#step-8) до [шага 31](#step-31).

Спотыкаются обычно на двух местах. Первое — путают слияние пространства имён и границу непрозрачности: одно склеивает инструменты в удобный плоский список, другое намеренно прячет чужие внутренности. Второе — считают проверки шлюза независимыми: авторизация прошла, значит вызов пойдёт. Не пойдёт, если хэш описания разошёлся.

Проверь себя на трёх вопросах — и особенно на том, что именно теряется при обходе шлюза.
