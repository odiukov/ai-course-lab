---
id: 034-check-rendering
type: check
title: 'Проверка: что отдать модели, а что хосту'
source_anchor: '### Rendering'
check:
  - question: >-
      generate_report вернул блоки контента и ресурс ui://report/current. Что из
      этого для кого?
    options:
      - >-
        Текстовые блоки идут модели в контекст, а ui://report/current забирает
        хост и рисует дашборд в песочнице
      - 'Оба результата идут модели, и она сама решает, что показать пользователю'
      - 'Оба результата получает только хост, а модель содержимое отчёта не видит'
      - >-
        ui://report/current передаётся модели как обычная ссылка, а текстовые
        блоки отрисовывает iframe
    correct: 0
    explanation: >-
      Текст нужен модели для рассуждений и продолжения разговора. Ресурс ui://
      предназначен хосту, который отображает интерактивный интерфейс внутри
      изолированного iframe.
  - question: >-
      Пользователь щёлкнул в дашборде по статье, и кнопка вызвала
      host.callTool('summarize_paper', {arxiv_id}). Что произойдёт дальше?
    options:
      - >-
        Хост направит вызов через шлюз, где снова проверят токен, RBAC и
        прибитый манифест
      - >-
        Iframe напрямую обратится к MCP-серверу, поскольку уже загружен
        доверенным хостом
      - 'Шлюз проверит вызов модели, но пропустит клик пользователя без RBAC'
      - >-
        Хост передаст iframe credentials провайдера, чтобы тот самостоятельно
        выполнил запрос
    correct: 0
    explanation: >-
      Кнопка лишь просит хост вызвать инструмент. Сам вызов проходит через
      обычную границу доверия, поэтому интерфейс не позволяет обойти
      аутентификацию, авторизацию или проверку манифеста.
---

После [шага 32](#step-32) и [шага 33](#step-33) ты должен уметь разделить один ответ инструмента на две адресованные части: текст — модели, `ui://report/current` — хосту, и сказать, что случится при клике по кнопке в дашборде.

Спотыкаются обычно в двух местах. Первое: кажется, что раз дашборд «наш» и уже нарисован, то его кнопки работают напрямую с сервером — а iframe сидит в песочнице и умеет только попросить хост. Второе: кажется, что проверки шлюза — про модель, а человек с мышкой их обходит. Наоборот: путь у клика ровно тот же, значит и токен, и RBAC, и прибитый манифест на месте.
