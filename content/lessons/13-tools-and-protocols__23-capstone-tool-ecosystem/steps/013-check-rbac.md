---
id: 013-check-rbac
type: check
title: 'Проверка: bob зовёт generate_report'
source_anchor: '### Security posture'
check:
  - question: >-
      bob успешно авторизовался и просит создать отчёт по найденным заметкам.
      Что произойдёт с вызовом generate_report?
    options:
      - 'Шлюз отклонит вызов при проверке прав, поэтому MCP-сервер его не получит'
      - 'MCP-сервер создаст отчёт, но исключит из него исходный текст заметок'
      - >-
        Вызов пройдёт, поскольку bob разрешено читать данные, из которых
        строится отчёт
      - Шлюз предложит bob повторить авторизацию с тем же набором прав
    correct: 0
    explanation: >-
      Шлюз проверяет права до передачи вызова серверу. У bob есть research:read,
      но создание отчёта требует research:write, поэтому запрос останавливается
      на шлюзе.
  - question: >-
      Токен bob действителен, корректно подписан и предназначен для нужного
      шлюза. Почему generate_report всё равно недоступен?
    options:
      - 'Для вызова не хватает права research:write'
      - PKCE разрешает только операции чтения
      - Любая долгая задача требует повторной авторизации
      - Правильно заданный audience запрещает обращаться к MCP-инструментам
    correct: 0
    explanation: >-
      Действительность токена подтверждает его подлинность, но не даёт
      дополнительных полномочий. Шлюз отдельно проверяет RBAC и отклоняет
      операцию из-за отсутствия research:write.
---

Один и тот же запрос, два пользователя — и разный финал. У alice есть `research:read` и `research:write`, она проходит всю цепочку до отчёта. bob с одним `research:read` доходит до вызова `generate_report` и упирается в стену.

После этого куска ты должен уметь сказать, **где именно** он упирается и **почему** — не «система не разрешила», а: проверка прав живёт в единой точке входа шлюза ([шаг 8](#step-8)), сервер про этот вызов не узнаёт вовсе.

Тут обычно спотыкаются дважды. Первое: думают, что токен bob «плохой» или просроченный — нет, он идеально валиден, не хватает ровно одного права. Второе: рассуждают «отчёт же собирается из прочитанного, значит `read` достаточно» — но `generate_report` создаёт новый артефакт, а создание — это `research:write`.
