---
id: 017-proverka-merge
type: check
title: 'Проверка: число placeholder-токенов и число патчей'
source_anchor: '### Step 2: Assemble ViT-MLP-LLM end-to-end'
check:
  - question: >-
      Vision encoder создал 196 патчей, а в промпте оказалось 195
      placeholder-токенов изображения. Что произойдёт при объединении?
    options:
      - 'Последний визуальный эмбеддинг будет отброшен, после чего запустится LLM'
      - Недостающий placeholder-токен будет добавлен автоматически
      - Объединение завершится ошибкой до запуска LLM
    correct: 2
    explanation: >-
      Метод _merge требует отдельную позицию для каждого визуального эмбеддинга.
      Поскольку 195 placeholder-токенов недостаточно для 196 патчей, он выбросит
      ValueError и не передаст последовательность в LLM.
  - question: >-
      В первом элементе батча есть 196 placeholder-токенов, а во втором — 200.
      Vision encoder вернул по 196 патчей для каждого элемента. Что произойдёт?
    options:
      - >-
        Оба элемента пройдут, поскольку общее число placeholder-токенов
        проверяется для всего батча
      - Второй элемент вызовет ошибку из-за четырёх лишних placeholder-позиций
      - >-
        Во втором элементе четыре лишние позиции сохранят исходные текстовые
        эмбеддинги
    correct: 1
    explanation: >-
      Число placeholder-токенов проверяется отдельно для каждого элемента батча.
      Первый элемент удовлетворяет условию, а во втором 200 позиций не
      соответствуют 196 визуальным эмбеддингам, поэтому объединение завершится
      ошибкой.
---

После функции из [шага 16](#step-16) ты должен уверенно проверять главное условие: один визуальный эмбеддинг заменяет ровно один placeholder-токен. Частая ловушка — считать `<image>` одним местом для всей картинки. На деле перед объединением промпт должен содержать столько зарезервированных позиций, сколько vision encoder создал патчей. Проверка выполняется для каждого элемента батча отдельно; любое несовпадение останавливает обработку.

> 🎒 **На пальцах.** Представь стеллаж с 196 пронумерованными ячейками и тележку со 196 фотокарточками. Ты кладёшь по одной карточке в каждую ячейку. Если ячеек 195, одна карточка останется в руке; если 200 — четыре места останутся пустыми. Поэтому кладовщик сначала сверяет числа и лишь затем начинает раскладку.
