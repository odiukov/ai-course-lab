---
id: 032-generation-check
type: check
title: >-
  Откуда берётся z без входного объекта и почему обычный AE не даёт такого
  ответа?
source_anchor: '## The Concept'
check:
  - question: 'Как VAE генерирует новый объект, если входного объекта нет?'
    options:
      - Берёт z из стандартного нормального приора и передаёт его декодировщику
      - Передаёт пустой объект кодировщику и использует полученный z
      - 'Восстанавливает z итеративно, начиная с выхода декодировщика'
    correct: 0
    explanation: >-
      KL-регуляризация приближает латентные распределения к стандартному
      нормальному приору. Поэтому при генерации можно сэмплировать z из N(0, I)
      и выполнить один проход через декодировщик.
  - question: >-
      Почему выборка z из стандартного нормального распределения сама по себе не
      гарантирует осмысленной генерации у обычного AE?
    options:
      - Обычный AE может декодировать только коды конкретных обучающих объектов
      - >-
        Его функция потерь не заставляет распределение кодов совпадать со
        стандартным нормальным приором
      - Его кодировщик обязательно выдаёт латентный вектор нулевой размерности
    correct: 1
    explanation: >-
      Обычный AE оптимизирует восстановление, но не согласует расположение кодов
      с N(0, I). Случайная выборка из этого распределения может попасть в
      область, на которой декодировщик не обучался.
---

Ты убираешь входной объект и оставляешь только декодировщик. У VAE следующий ход определён: взять `z` из приора, как в [шаге 28](#step-28), потому что KL при обучении подталкивал коды жить именно в этой области. У обычного AE такого соглашения нет: случайная координата может попасть между освоенными участками, что мы видели в [шаге 3](#step-3). Отдельную модель распределения кодов добавить можно, но сам обычный AE её не даёт.

> 🎒 **На пальцах.** Представь два склада. На первом коробки расставлены по заранее нанесённой координатной сетке: называешь случайный допустимый адрес и находишь коробку. На втором кладовщик ставил коробки только туда, где было свободно. Взять случайный адрес можно и там, но полка, скорее всего, окажется пустой.
