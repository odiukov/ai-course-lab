- concept: stride задаёт расстояние между соседними стартовыми позициями ядра
  question: Какой параметр определяет сдвиг ядра между двумя соседними вычислениями свёртки?
  explanation: Stride определяет, на сколько пикселей сдвигается ядро. При stride=1 проверяются соседние позиции, а при stride=2 ядро пропускает каждую вторую стартовую позицию.
  kind: cloze
  template: next_start = current_start + ___
  answer: stride
  accept:
    - шаг
    - шаг свёртки
  id: 017-stride-1
  fingerprint: be5f212f
- concept: увеличение stride снижает вычислительную нагрузку ценой пространственной детализации
  question: Объясни своими словами, какой компромисс возникает при увеличении stride свёртки.
  explanation: 'При stride=2 высота и ширина карты обычно уменьшаются примерно вдвое, а число её значений — примерно вчетверо. Ядро и выполняемая им операция не меняются: меняется плотность пространственного сканирования.'
  kind: open
  reference: Ядро вычисляет отклики реже, поэтому выходная карта содержит меньше значений и требует меньше последующих вычислений, но хуже сохраняет мелкие пространственные детали.
  id: 017-stride-2
  fingerprint: 33c58a31
