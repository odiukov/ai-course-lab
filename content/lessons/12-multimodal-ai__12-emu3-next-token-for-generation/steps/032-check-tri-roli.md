---
id: 032-check-tri-roli
type: check
title: 'Проверка: чем отличаются три «модели»'
source_anchor: '### Three roles, one model'
check:
  - question: >-
      Ты скачал Emu3-Gen, а потом захотел задавать вопросы по картинкам. Что
      придётся сделать?
    options:
      - 'Скачать отдельный чекпойнт Emu3-Chat: это другая модель со своими весами'
      - >-
        Ничего не докачивать: те же веса, меняется только шаблон промпта и
        порядок токенов
      - 'Дообучить Emu3-Gen на VQA, иначе она умеет только рисовать'
      - Прикрутить к модели вторую выходную голову под текст
    correct: 1
    explanation: >-
      Emu3-Gen, Emu3-Chat и Emu3-Stage2 — три способа обратиться к одному набору
      весов на 7B. Голов под задачи нет, есть разные шаблоны промптов.
  - question: >-
      Почему один и тот же чекпойнт вообще может и рисовать, и описывать
      картинку?
    options:
      - 'Внутри переключатель режимов, который активирует нужную половину сети'
      - >-
        Текстовые и картиночные токены живут в одном словаре, поэтому задача —
        просто предсказать следующий токен, а что именно предсказывать, задаёт
        порядок токенов в промпте
      - Модель определяет тип входа и вызывает подходящий декодер
      - 'Для картинок используется диффузия, для текста — авторегрессия'
    correct: 1
    explanation: >-
      Общий словарь и один лосс делают все задачи однотипными, а порядок токенов
      в промпте решает, генерация это или восприятие.
---

Ты только что дошёл до конца пути: токенизатор, общий словарь, один лосс, шаблоны промптов, сэмплирование. Теперь проверь, сложилось ли всё в одну картину.

После этого куска ты должен уметь объяснить, почему три названия — Emu3-Gen, Emu3-Chat, Emu3-Stage2 — это не три файла на диске, а три способа обратиться к одному. И уметь связать это с [шагом 21](#step-21): роль задаёт порядок токенов, а не отдельный модуль.

Обычно спотыкаются в двух местах. Первое — привычка из пайплайна 2023-го ([шаг 4](#step-4)): раз задач много, значит и моделей много. Второе — ищут «голову под задачу» или внутренний переключатель. Ни того, ни другого нет: словарь один, предсказание одно, меняется только строка промпта.
