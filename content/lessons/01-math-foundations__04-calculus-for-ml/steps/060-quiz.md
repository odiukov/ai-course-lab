---
id: 060-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: Пусть g(θ) = ∇L(θ). Что представляет собой якобиан векторной функции g?
    options:
      - Саму функцию потерь L
      - Гессиан функции потерь L
      - Интеграл функции потерь L
      - Learning rate для оптимизации L
    correct: 1
    explanation: >-
      Компоненты g — первые частные производные L. Дифференцируя каждую из них
      по всем параметрам, получают матрицу вторых частных производных — гессиан.
  - question: >-
      Чему должен быть равен интеграл плотности вероятности непрерывного
      распределения по всей области возможных значений?
    options:
      - Ровно единице
      - Нулю
      - Максимальному значению плотности
      - Размерности входного вектора
    correct: 0
    explanation: >-
      Интеграл плотности по области собирает всю вероятностную массу. Для
      нормированного распределения полная вероятность равна единице.
  - question: >-
      Как выглядит приближение Тейлора функции f(x + h) с учётом производных до
      второго порядка?
    options:
      - f(x) + f'(x)h + (1/2)f''(x)h²
      - f(x) + f'(x)h + f''(x)h
      - f'(x) + (1/2)f''(x)h
      - f(x) + f''(x)h²
    correct: 0
    explanation: >-
      Линейная поправка задаётся наклоном f'(x), а квадратичная — кривизной
      f''(x) с коэффициентом 1/2.
  - question: >-
      Скалярная функция loss(θ) и векторная функция activations(θ) зависят от
      одного вектора параметров θ. Как обычно организуют их первые производные?
    options:
      - 'Для loss используют градиент, для activations — якобиан'
      - 'Для loss используют гессиан, для activations — интеграл'
      - Для обеих функций производная всегда является одним числом
      - >-
        Для loss используют якобиан вторых производных, для activations —
        learning rate
    correct: 0
    explanation: >-
      У скалярного выхода частные производные образуют градиент. У векторного
      выхода свой набор производных есть у каждой выходной координаты, поэтому
      их собирают в якобиан.
---

Этот урок дал тебе язык, на котором дальше говорит весь курс. Каждый раз, когда модель обучается, внутри крутится одно и то же: посчитать потери, получить градиент, шагнуть против него на длину learning rate, повторить. Все остальные темы — слои, оптимизаторы, регуляризация — это варианты этой петли.

Три места, где обычно спотыкаются. Первое: знак. Градиент показывает, куда функция растёт, поэтому в обновлении стоит минус, а не плюс. Второе: градиент — не одно число, а по одной частной производной на каждый вес; для миллиона весов это вектор длиной миллион. Третье: нулевой градиент означает «здесь плоско», а не «здесь лучшее решение» — плоско бывает и на седле.

Дальше по курсу это перестанет быть отдельной темой и станет фоном: ты будешь читать про новый оптимизатор и сразу видеть, какую часть петли он меняет.
