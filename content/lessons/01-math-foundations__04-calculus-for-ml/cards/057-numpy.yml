- concept: скалярные параметры линейной модели применяются ко всему NumPy-массиву за одну векторизованную операцию
  question: Заполни выражение, которое без Python-цикла вычисляет предсказания линейной модели для всего массива x.
  explanation: NumPy поэлементно умножает массив x на скаляр w и прибавляет скаляр b к каждому результату. Поэтому pred содержит предсказание для каждого элемента x.
  kind: cloze
  template: pred = ___
  answer: w * x + b
  accept:
    - x * w + b
    - np.multiply(w, x) + b
    - np.multiply(x, w) + b
  id: 057-numpy-1
  fingerprint: e23a9de7
- concept: усреднение по объектам делает значение MSE и среднего градиента независимым от простого дублирования выборки
  question: Каждый объект обучающей выборки продублировали одинаковое число раз. Как при неизменных параметрах модели изменятся np.mean(error ** 2), dw и db?
  explanation: Дублирование повторяет одни и те же слагаемые, но np.mean одновременно делит их сумму на возросшее число элементов. Поэтому средняя ошибка и средние градиенты остаются прежними.
  kind: choice
  options:
    - Не изменятся
    - Увеличатся пропорционально числу копий
    - Уменьшатся пропорционально числу копий
    - Loss не изменится, но градиенты увеличатся
  correct: 0
  id: 057-numpy-2
  fingerprint: 9719722f
