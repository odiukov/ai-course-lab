---
id: 024-proverka-tracing
type: check
title: 'Проверка: почему X3 = 104 проверяет не тот же навык, что одна иголка'
source_anchor: '### Step 3: multi-hop variable tracing (RULER-style)'
check:
  - question: >-
      Модель безошибочно находит строку X1 = 42, но неверно отвечает на вопрос о
      X3. Что это показывает?
    options:
      - Она не умеет извлекать одиночный факт
      - >-
        Поиск сработал, но восстановление цепочки зависимостей или вычисление —
        нет
      - Контекст обязательно превысил заявленное окно
    correct: 1
    explanation: >-
      Найти одно присваивание недостаточно: для X3 нужно собрать связанные
      строки в правильном порядке и выполнить вычисления.
  - question: >-
      Почему правильный ответ X3 = 104 нельзя считать тем же успехом, что
      прохождение одной иголки?
    options:
      - Потому что значение 104 длиннее обычной иголки
      - Потому что трассировка проверяет только арифметику
      - >-
        Потому что трассировка сочетает поиск нескольких строк, связывание
        зависимостей и рассуждение
    correct: 2
    explanation: >-
      Одна иголка из шага 5 проверяет извлечение готового факта, а X3 требует
      восстановить разбросанную цепочку и получить новый ответ.
---

Здесь легко спутать одинаковый итог с одинаковым навыком. В задаче с иголкой из [шага 5](#step-5) ответ уже лежит в тексте. Для X3 модель должна найти несколько присваиваний, расположить зависимости в нужном порядке и провести вычисление — именно такую трассировку ты разбирал в [шаге 22](#step-22). Поэтому ошибка в X3 ещё не говорит, что модель не нашла строки: она могла потерять связь между ними.

> 🎒 **На пальцах.** Представь повара, которому в разных частях длинной книги оставили три инструкции: взять основу, добавить ингредиент, затем удвоить порцию. Найти запись про основу — одна иголка. Приготовить правильное количество — значит собрать все инструкции, соблюсти порядок и выполнить действия. Найденная первая запись ещё не гарантирует готовое блюдо.
