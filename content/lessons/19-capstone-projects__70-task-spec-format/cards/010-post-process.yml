- concept: extract_first_line пропускает пустые строки и очищает края первой содержательной строки
  question: Заполни ветку правила `extract_first_line`.
  explanation: Проверка пропускает пустые и состоящие из пробелов строки. `line.strip()` возвращает первую содержательную строку без пробельных символов по краям.
  kind: cloze
  template: |-
    for line in text.splitlines():
        if line.strip():
            return ___
  answer: line.strip()
  accept:
    - (line.strip())
  id: 010-post-process-1
  fingerprint: aa06be2b
