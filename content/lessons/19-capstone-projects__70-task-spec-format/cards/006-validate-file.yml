- concept: идентификатор добавляется в seen_ids только после успешной валидации и проверки на дубликат
  question: Заполни строку, которая фиксирует успешно принятой задачей её идентификатор.
  explanation: Если занести идентификатор раньше, невалидная строка сможет зарезервировать его и вызвать ложную ошибку у следующей корректной записи.
  kind: cloze
  template: |-
    line_errors = validate_task(record, line_number=i)
    if line_errors:
        errors.extend(line_errors)
        continue
    tid = record["task_id"]
    if tid in seen_ids:
        errors.append(duplicate_error)
        continue
    ___
    validated.append(TaskSpec(...))
  answer: seen_ids.add(tid)
  accept:
    - seen_ids.add(record["task_id"])
    - seen_ids.add(record['task_id'])
  id: 006-validate-file-1
  fingerprint: 89a4a4c5
