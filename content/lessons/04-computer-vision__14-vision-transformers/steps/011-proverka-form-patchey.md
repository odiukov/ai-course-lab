---
id: 011-proverka-form-patchey
type: check
title: 'Проверка: откуда берётся форма (N, 196, 768)'
source_anchor: '### Step 1: Patch embedding'
check:
  - question: >-
      После patch embedding форму записывают как (N, 196, 768), а не как (N ·
      196, 768). Почему важно не объединять первые две оси?
    options:
      - >-
        Чтобы сохранить для каждого изображения отдельную последовательность
        токенов и не смешивать изображения внутри attention
      - >-
        Чтобы число признаков токена обязательно совпадало с числом пикселей в
        патче
      - Чтобы свёртка использовала разные веса для каждого изображения
      - >-
        Чтобы трансформер мог восстановить исходные значения всех пикселей без
        потерь
    correct: 0
    explanation: >-
      N обозначает независимые элементы батча, а 196 — токены внутри каждого из
      них. Если слить эти оси, границы между изображениями исчезнут и их токены
      могут ошибочно взаимодействовать.
  - question: >-
      Размер батча увеличили с 4 до 8, оставив размер изображения, патча и
      patch-проекции прежними. Почему длина последовательности и число признаков
      токена не изменятся?
    options:
      - >-
        Размер батча задаёт только количество независимо обрабатываемых
        изображений; сетка патчей и выходная размерность проекции остались
        прежними
      - >-
        Patch embedding автоматически делит число токенов и признаков на размер
        батча
      - >-
        Трансформер всегда требует фиксировать все три оси независимо от входных
        данных
      - Увеличение батча компенсируется уменьшением числа каналов изображения
    correct: 0
    explanation: >-
      Переход от 4 к 8 меняет только первую ось: форма становится (8, 196, 768).
      Для каждого изображения по-прежнему создаются 196 токенов по 768
      признаков.
---

Разбери форму по трём независимым осям: \(N\) — сколько изображений обрабатывается вместе, 196 — сколько патчей получилось из одного изображения, 768 — сколько признаков дала каждому патчу проекция из [шага 9](#step-9).

Главная ловушка — принять последние 768 за размер исходного патча. У цветного патча \(16 \times 16\) действительно тоже 768 чисел, но это лишь совпадение параметров: после `patch_embed` из [шага 10](#step-10) последние 768 обозначают уже новые признаки токена. Число 196 берётся отдельно: сетка патчей из [шага 4](#step-4) имеет размер \(14 \times 14\).
