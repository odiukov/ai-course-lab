---
id: 022-proverka-attention
type: check
title: 'Проверка: формы матрицы внимания и сумма весов'
source_anchor: '### Transformer encoder block'
check:
  - question: >-
      У Q и K форма [B, N, D]. Если увеличить D, не меняя B и N, как изменится
      форма матрицы весов внимания?
    options:
      - 'Станет [B, D, D]'
      - 'Станет [B, N, D]'
      - 'Останется [B, N, N]'
      - 'Станет [N, N, D]'
    correct: 2
    explanation: >-
      Ось признаков D сворачивается при вычислении скалярных произведений. В
      матрице весов остаются ось запросов и ось ключей, поэтому её форма — [B,
      N, N].
  - question: >-
      Softmax применён к логитам внимания по оси ключей. Какое утверждение
      обязательно верно для полученной матрицы весов?
    options:
      - Сумма всех элементов матрицы равна единице
      - Сумма весов каждого запроса по всем ключам равна единице
      - Сумма весов каждого ключа по всем запросам равна единице
      - Все строки матрицы одинаковы
    correct: 1
    explanation: >-
      Каждая строка относится к одному запросу, а softmax распределяет его вес
      между ключами. Поэтому сумма строки равна единице; суммы столбцов и всей
      матрицы так нормироваться не обязаны.
---

Проверь, можешь ли ты проследить формы без вычисления самих чисел. После сравнений из [шага 20](#step-20) ось признаков исчезает: вместо неё появляются две оси токенов — запросы и ключи. Поэтому квадратная матрица весов не обязана иметь размерность признаков.

Частая ошибка — требовать сумму единица от всей матрицы или от столбца. Softmax из [шага 18](#step-18) применяется отдельно для каждого запроса: одна строка распределяет его внимание между всеми ключами. Если формы состыковались и каждая строка суммируется до единицы, основная проверка пройдена.
