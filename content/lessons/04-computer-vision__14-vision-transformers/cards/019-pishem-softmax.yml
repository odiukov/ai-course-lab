- concept: вычитание максимального логита предотвращает переполнение экспоненты
  question: Почему в численно устойчивой реализации softmax перед вычислением экспоненты из логитов вычитают их максимум?
  explanation: 'После вычитания максимума наибольший аргумент экспоненты равен 0, а остальные меньше 0. Поэтому exp не переполняется: её значения лежат между 0 и 1. Например, логиты [1000, 1001, 1002] превращаются в [-2, -1, 0].'
  kind: choice
  options:
    - Все аргументы экспоненты становятся неположительными, поэтому её значения не превышают единицу
    - Отрицательные логиты автоматически заменяются нулями
    - Результат softmax становится равномерным распределением
    - Экспоненту можно заменить линейной функцией
  correct: 0
  id: 019-pishem-softmax-1
  fingerprint: a1583d3e
- concept: сохранение размерности редуцируемой оси обеспечивает корректный broadcasting
  question: Заполни пропуск так, чтобы максимум можно было вычесть из многомерного массива без ручного изменения формы.
  explanation: Параметр keepdims=True оставляет редуцированную ось размером 1. Благодаря этому максимум корректно распространяется по соответствующей оси исходного массива при вычитании.
  kind: cloze
  template: row_max = np.max(logits, axis=axis, keepdims=___)
  answer: 'True'
  accept:
    - 'true'
    - 'True'
  id: 019-pishem-softmax-2
  fingerprint: e6117cb9
