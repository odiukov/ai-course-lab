- concept: в pre-LN нормализованный вход передаётся в подслой до остаточного сложения
  question: Заполни пропуск так, чтобы функция реализовывала pre-LayerNorm.
  explanation: 'В pre-LN сначала вычисляется LN(x), затем нормализованный тензор проходит через подслой: x + sublayer(LN(x)). Нормализацию не применяют к уже готовой сумме.'
  kind: cloze
  template: |-
    def prenorm_residual(x, sublayer, gamma, beta, eps):
        normalized = layer_norm(x, gamma, beta, eps)
        return x + ___
  answer: sublayer(normalized)
  accept:
    - sublayer(layer_norm(x, gamma, beta, eps))
  id: 027-pishem-prenorm-1
  fingerprint: 2da9241c
