- concept: padding_idx защищает embedding токена заполнения от обучения, но не маскирует временные шаги LSTM
  question: Что гарантирует параметр padding_idx в nn.Embedding?
  explanation: 'padding_idx фиксирует соответствующую строку матрицы embeddings. Он не маскирует выходы LSTM и не исключает их из pooling: для этого нужны отдельная маска или упаковка последовательностей.'
  kind: choice
  options:
    - Вектор токена заполнения не обновляется градиентом
    - LSTM полностью пропускает позиции заполнения
    - Позиции заполнения автоматически исключаются из max-pooling
    - Все тексты в батче обрезаются до одинаковой содержательной длины
  correct: 0
  id: 038-pytorch-lstm-1
  fingerprint: 9c37e0d1
- concept: batch_first задаёт порядок осей батч, последовательность, признаки
  question: Заполни аргумент LSTM, чтобы вход и выход использовали порядок осей «батч, длина последовательности, признаки».
  explanation: При batch_first=True LSTM принимает и возвращает тензоры, у которых первой осью идёт батч, второй — временные шаги, а последней — признаки.
  kind: cloze
  template: nn.LSTM(embed_dim, hidden_dim, ___, bidirectional=use_bidir)
  answer: batch_first=True
  accept:
    - batch_first = True
  id: 038-pytorch-lstm-2
  fingerprint: e18f80da
- concept: линейный классификатор возвращает логиты без явного softmax для CrossEntropyLoss
  question: Объясни своими словами, почему после последнего Linear в многоклассовом LSTM-классификаторе обычно не добавляют softmax, если модель обучается с nn.CrossEntropyLoss.
  explanation: Выход Linear — логиты классов. CrossEntropyLoss самостоятельно преобразует их в логарифмические вероятности и вычисляет отрицательное логарифмическое правдоподобие.
  kind: open
  reference: nn.CrossEntropyLoss ожидает необработанные логиты и сама вычисляет log-softmax в численно устойчивой форме. Явный softmax перед ней был бы лишним и изменил бы смысл входа функции потерь.
  id: 038-pytorch-lstm-3
  fingerprint: b6bdf676
