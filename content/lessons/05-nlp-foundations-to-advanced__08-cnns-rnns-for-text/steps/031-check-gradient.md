---
id: 031-check-gradient
type: check
title: 'Проверка: затухание при 0.9 и взрыв при 1.1'
source_anchor: '### Step 3: the vanishing gradient demo (intuition)'
check:
  - question: После 99 переходов какой результат ты ожидаешь при рекуррентном весе 0.9?
    options:
      - Сигнал уменьшится примерно до 0.00003 от исходного
      - Сигнал сохранится почти без изменений
      - Сигнал увеличится примерно в 99 раз
    correct: 0
    explanation: >-
      Каждое умножение на 0.9 немного уменьшает сигнал, а 99 повторений
      превращают это уменьшение в сильное затухание.
  - question: 'Что изменится, если поставить рекуррентный вес 1.1 вместо 0.9?'
    options:
      - 'Сигнал тоже затухнет, только медленнее'
      - Сигнал вырастет примерно до 12 500 исходных значений
      - Сигнал станет ровно в 1.1 раза больше
    correct: 1
    explanation: >-
      Увеличение повторяется на каждом переходе, поэтому эффект накапливается:
      после 99 умножений получается примерно 12 500.
---

Запусти функцию из [шага 30](#step-30) для 99 переходов с весами 0.9 и 1.1. Здесь легко смотреть только на близость обоих весов к единице и недооценить повторение: небольшое изменение на одном переходе превращается в огромную разницу на длинной цепочке.

> 🎒 **На пальцах.** Представь копировальный аппарат, который перед каждой новой копией меняет масштаб. При 90% надпись понемногу сжимается и через 99 копий почти исчезает. При 110% она каждый раз чуть растёт и вскоре перестаёт помещаться на листе. Дело не в одном изменении, а в том, сколько раз оно повторилось.
