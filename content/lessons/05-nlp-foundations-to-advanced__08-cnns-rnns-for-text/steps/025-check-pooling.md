---
id: 025-check-pooling
type: check
title: 'Проверка: последнее состояние, mean или max'
source_anchor: '### Step 2: LSTM classifier'
check:
  - question: >-
      Полезный признак умеренно выражен во многих позициях текста, а одиночные
      экстремальные активации считаются шумом. Какое объединение состояний лучше
      отражает такой распределённый сигнал?
    options:
      - Mean-pooling по всем состояниям
      - Max-pooling по каждому признаку
      - Только последнее состояние
      - Все способы одинаково устойчивы к одиночному всплеску
    correct: 0
    explanation: >-
      Mean-pooling учитывает все позиции и уменьшает влияние отдельного
      всплеска. Max-pooling, напротив, определяется самым сильным значением, а
      последнее состояние отбрасывает информацию с остальных позиций.
  - question: >-
      Для одного признака RNN выдала по тексту значения 0.1, 0.8 и 0.2. Какие
      результаты соответствуют последнему состоянию, mean-pooling и max-pooling?
    options:
      - '0.8, примерно 0.37 и 0.2'
      - '0.2, примерно 0.37 и 0.8'
      - '0.2, 0.8 и примерно 0.37'
      - 'Примерно 0.37, 0.2 и 0.8'
    correct: 1
    explanation: >-
      Последнее состояние равно 0.2, среднее трёх значений — примерно 0.37, а
      максимум — 0.8.
---

После [шага 23](#step-23) и функции из [шага 24](#step-24) тебе нужно различать не только форму результата, но и то, какая информация в него попадёт. Последнее состояние сильнее зависит от конца текста, mean учитывает все позиции, но размывает редкий всплеск, а max сохраняет самое сильное значение каждого признака. Все способы дают вектор одинакового размера, но собирают смысл по-разному.

> 🎒 **На пальцах.** Представь датчики шума в нескольких комнатах. Последнее состояние показывает только финальное измерение, mean сообщает средний шум за весь обход, а max запоминает самый громкий звук. Если один раз сработала сигнализация, среднее может приглушить событие, финальное измерение — пропустить, а максимум сохранит его.
