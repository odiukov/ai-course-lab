---
id: 049-final-quiz
type: quiz
title: 'Итоговая проверка: TextCNN, RNN, LSTM и границы их применимости'
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты классифицируешь короткие отзывы: решающая фраза может встретиться в
      любом месте, а скорость важнее дальнего контекста. Какую модель разумнее
      взять первой?
    options:
      - TextCNN с фильтрами нескольких ширин и global max-pooling
      - Однонаправленную RNN с последним hidden state
      - 'BiLSTM, потому что она всегда точнее'
      - Обычную RNN без pooling
    correct: 0
    explanation: >-
      TextCNN быстро ищет локальные шаблоны по всему тексту, а global
      max-pooling сохраняет самое сильное совпадение независимо от его позиции.
  - question: >-
      В обычной RNN обучающий сигнал к началу длинного текста почти исчезает.
      Какое свойство LSTM прямо помогает ослабить эту проблему?
    options:
      - Фильтры нескольких ширин
      - Аддитивное обновление cell state под управлением гейтов
      - Global max-pooling по токенам
      - Чтение последовательности только справа налево
    correct: 1
    explanation: >-
      Cell state создаёт более прямой путь для переноса памяти и градиента, а
      гейты регулируют сохранение и обновление информации.
  - question: >-
      Система должна размечать каждое слово сразу при его поступлении и не может
      ждать продолжения фразы. Почему BiLSTM здесь неудобна?
    options:
      - Она принимает только тексты фиксированной длины
      - Её обратному проходу нужны будущие токены
      - Она не создаёт представление каждого токена
      - Она работает только с мешком слов
    correct: 1
    explanation: >-
      Обратное направление читает последовательность с конца, поэтому для
      окончательной разметки текущего токена требуется ещё не поступивший правый
      контекст.
  - question: >-
      Две модели показали одинаковую общую точность, но TextCNN заметно хуже на
      самых длинных текстах. Какой вывод наиболее обоснован?
    options:
      - TextCNN обязательно неправильно реализована
      - 'Общей точности достаточно, поэтому модели эквивалентны'
      - Локальных признаков TextCNN может не хватать для дальних зависимостей
      - Нужно сравнивать модели только на коротких текстах
    correct: 2
    explanation: >-
      Разбивка качества по длине выявляет ограничение, скрытое общей метрикой.
      Результат согласуется с нехваткой дальнего контекста, хотя сам по себе ещё
      не доказывает эту причину.
---

Дальше тебе придётся выбирать архитектуру не по громкому названию, а по задаче. Опирайся на различие локального поиска и последовательной памяти из [шага 3](#step-3), ограничения моделей из [шагов 39](#step-39)–[42](#step-42) и честное сравнение из [шагов 47](#step-47)–[48](#step-48). Частая ошибка — считать LSTM универсальным победителем, TextCNN полностью нечувствительной к порядку, а двунаправленность бесплатным улучшением. Решают длина зависимости, допустимая задержка и способ проверки качества.

> 🎒 **На пальцах.** Представь пункт наблюдения: один сотрудник мгновенно замечает знакомые короткие жесты, другой ведёт журнал событий по порядку, а третий хранит важные записи в защищённых разделах. Ни один не лучший всегда: выбор зависит от того, ищешь ли ты сигнал, историю или связь через большой промежуток.
