---
id: 037-check-rnn-family
type: check
title: 'Проверка: обычная RNN, LSTM или GRU'
source_anchor: '## The Concept'
check:
  - question: >-
      В длинном отзыве важное отрицание находится в начале, а классификация
      выполняется после обработки всего текста. Какой выбор разумнее?
    options:
      - >-
        Обычная RNN, потому что увеличение длины цепочки улучшает сохранение
        ранней информации
      - >-
        LSTM или GRU, потому что гейты помогают сохранять информацию на длинных
        зависимостях
      - >-
        Только LSTM, потому что GRU не способна обрабатывать длинные
        последовательности
    correct: 1
    explanation: >-
      В обычной RNN градиент и сигнал от ранних токенов могут затухать. LSTM и
      GRU используют гейты, поэтому обе архитектуры лучше приспособлены к
      длинным зависимостям.
  - question: >-
      Для модели нужна гейтированная память, но также важно уменьшить число
      параметров. Какую архитектуру разумнее выбрать?
    options:
      - Обычную RNN
      - LSTM
      - GRU
    correct: 2
    explanation: >-
      GRU сохраняет механизм управляемой памяти, но имеет более простую
      архитектуру без отдельного cell state и обычно требует меньше параметров,
      чем LSTM.
---

Здесь важно выбирать не «самую продвинутую» модель, а подходящую. Обычная RNN годится как простой ориентир и для коротких зависимостей. Если ранняя информация должна пережить длинную цепочку, разумнее LSTM или GRU: причину ты видел в [шагах 28](#step-28)–[33](#step-33). Между ними нет вечного победителя: GRU компактнее, а качество проверяют на данных.

> 🎒 **На пальцах.** Представь три камеры хранения. В первой сотрудник после каждой посылки переписывает одну общую наклейку — ранняя запись быстро стирается. LSTM раскладывает сведения по ячейкам и отдельно управляет хранением и выдачей. GRU делает похожую работу на более компактной стойке. Поэтому для длинной истории нужны ячейки, а выбор между двумя стойками решает практика.
