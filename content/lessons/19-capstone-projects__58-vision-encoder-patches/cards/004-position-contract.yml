- concept: двумерное позиционное представление кодирует строку и столбец в раздельных частях вектора
  question: Заполни пропуск в схеме двумерного позиционного представления.
  explanation: 'Раздельное кодирование сохраняет структуру сетки: позиции в одной строке имеют общую строчную часть, а позиции в одном столбце — общую столбцовую.'
  kind: cloze
  template: position(row, col) = concat(___, column_code(col))
  answer: row_code(row)
  accept:
    - row_code(row)
    - код строки
    - row embedding
  id: 004-position-contract-1
  fingerprint: 0625c89d
