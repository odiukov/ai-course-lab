---
id: 028-probe-or-workflow
type: check
title: 'Проверка: отличите быструю пробу от сети, LLM-вызова и отдельного workflow'
source_anchor: '## Production patterns in the wild'
check:
  - question: >-
      Перед каждой сессией нужно выбрать одну проверку для горячего пути init.
      Какая подходит?
    options:
      - Локально сравнить версию Python с требуемой версией
      - Запросить у сервера актуальный статус лицензии
      - Попросить LLM объяснить найденную ошибку
      - Запустить внешний workflow и дождаться его отчёта
    correct: 0
    explanation: >-
      Сравнение версий выполняется локально, быстро и даёт одинаковый результат
      при одинаковых данных. Остальные варианты зависят от внешней системы или
      недетерминированного вызова, поэтому вердикт перестаёт описывать состояние
      стенда.
  - question: >-
      Локальная проба стабильно занимает десять секунд на каждом старте. Как это
      оценить?
    options:
      - >-
        Оставить как есть: локальные проверки допустимы в горячем пути по
        определению
      - 'Поднять общий тайм-аут init, чтобы проба гарантированно успевала'
      - Вынести её из горячего пути или кэшировать результат
      - 'Добавить вызов модели, чтобы быстрее интерпретировать её вывод'
    correct: 2
    explanation: >-
      Локальность не освобождает от бюджета: задержка умножается на число сессий
      за день. Результат пробы воспроизводим, поэтому его можно честно
      кэшировать и пропускать по свежему локу; если кэшировать нечего — проверке
      место в отдельном прогоне.
---

Здесь легко спутать важную проверку с проверкой, которую обязательно надо выполнять при каждом старте. Ориентируйся на три признака из [шага 27](#step-27): горячая проба работает локально, даёт повторяемый результат и укладывается примерно в три секунды. Сеть добавляет недоступность и задержку, LLM — ещё и изменчивость ответа. Такие действия могут быть полезны, но им нужен отдельный workflow, а не место у входной двери каждой сессии.

> 🎒 **На пальцах.** Перед выходом ты быстро проверяешь, взял ли ключи: карманы рядом, результат однозначен. Звонить управляющему, чтобы уточнить работу домофона, — уже отдельное дело. А просить знакомого по голосовому сообщению угадать, безопасно ли выходить, — тем более не проверка перед каждым открытием двери.
