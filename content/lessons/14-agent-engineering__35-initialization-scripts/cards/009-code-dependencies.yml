- concept: ранний выход на первой пропаже растягивает ремонт на столько прогонов, сколько пропусков; полный обход даёт их за один
  question: Из 7 обязательных зависимостей в окружении отсутствуют 4. Проба останавливается на первой ненайденной и возвращает ошибку; человек ставит названный пакет и запускает init заново. Сколько прогонов init понадобится, чтобы узнать обо всех пропусках?
  explanation: 'Каждый прогон вскрывает ровно одну пропажу, значит их будет 4 — по числу отсутствующих пакетов. Полный обход списка возвращает те же 4 имени за один прогон: цикл ремонта сокращается вчетверо, а стоимость обхода та же.'
  kind: numeric
  answer: 4
  tolerance: 0.5
  id: 009-code-dependencies-1
  fingerprint: 0994f59f
- concept: проба зависимостей агрегирует весь список в один результат, а не порождает по результату на пакет
  question: В манифесте 6 обязательных пакетов, 4 из них не находятся в окружении. Как это должно выглядеть в отчёте init?
  explanation: 'Одна функция-проба порождает один результат вида (name, status, detail). Список пакетов — её внутреннее дело: она обходит все имена, копит ненайденные и сворачивает их в одно объяснение. Так число строк отчёта не зависит от размера манифеста, а перечень ремонта остаётся полным.'
  kind: choice
  options:
    - 'Один результат пробы: имя пробы, блокирующий статус и объяснение, перечисляющее все 4 отсутствующих пакета'
    - Шесть результатов проб — по одному на каждый заявленный пакет
    - Четыре результата проб — по одному на каждый отсутствующий пакет
    - Один результат с блокирующим статусом и именем первого не найденного пакета
  correct: 0
  id: 009-code-dependencies-2
  fingerprint: 607006a9
