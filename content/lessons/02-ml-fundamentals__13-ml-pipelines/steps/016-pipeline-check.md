---
id: 016-pipeline-check
type: check
title: 'Проверка: что разрешено менять при предсказании'
source_anchor: '### Step 2: Pipeline from Scratch'
check:
  - question: >-
      Во время predict пайплайн заменил пропуски в копии новых данных
      сохранёнными медианами. Что здесь изменилось допустимо?
    options:
      - Параметры шага предобработки
      - 'Промежуточные данные, проходящие по пайплайну'
      - Веса обученной модели
    correct: 1
    explanation: >-
      При предсказании данные можно последовательно преобразовывать. Сохранённые
      медианы и веса модели при этом остаются неизменными.
  - question: >-
      В новых данных появились значения другого масштаба. Как должен поступить
      пайплайн при predict?
    options:
      - Заново вычислить параметры преобразования по новым данным
      - Объединить новые данные с обучающими и повторить fit
      - Применить сохранённые параметры преобразования и вызвать predict модели
    correct: 2
    explanation: >-
      Режим предсказания использует уже обученное состояние: только transform
      для преобразований, затем predict модели.
---

Тебе нужно уверенно различать две перемены. Во время предсказания может меняться копия входных данных: пропуски заполняются, значения преобразуются, результат передаётся дальше. Но состояние пайплайна — медианы, параметры следующих шагов и веса модели — остаётся прежним. Именно такой режим разобран в [шаге 13](#step-13).

Частая ловушка — принять необычные новые значения за повод снова вызвать `fit`. Это уже обучение, а не предсказание.

> 🎒 **На пальцах.** Представь типографию с настроенным печатным станком. Новый лист проходит через валики и меняется: на нём появляется изображение. Но под каждый лист ты не перенастраиваешь положение валиков и не вырезаешь новую печатную форму. На предсказании меняется обрабатываемый материал, а настройки остаются зафиксированными.
