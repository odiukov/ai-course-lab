- concept: имя шага служит ключом для доступа к компоненту через named_steps
  question: Пайплайн содержит шаг масштабирования с именем "normalizer". Дополните выражение, которое возвращает этот компонент.
  explanation: Pipeline сохраняет шаги под именами, указанными при его создании. Поэтому компонент масштабирования доступен как pipe.named_steps["normalizer"].
  kind: cloze
  template: pipe.named_steps[___]
  answer: '"normalizer"'
  accept:
    - '''normalizer'''
  id: 020-sklearn-pipeline-1
  fingerprint: dfb5a83d
- concept: в Pipeline с методом predict промежуточные шаги являются трансформерами, а последний шаг — моделью
  question: Какая последовательность шагов подходит для Pipeline, у которого затем будут вызывать predict?
  explanation: Все промежуточные шаги должны преобразовывать данные, а последний — строить предсказания. Поэтому заполнение пропусков ставят перед классификатором.
  kind: choice
  options:
    - SimpleImputer, затем RandomForestClassifier
    - RandomForestClassifier, затем SimpleImputer
    - LogisticRegression, затем RandomForestClassifier
  correct: 0
  id: 020-sklearn-pipeline-2
  fingerprint: 98fd0afc
