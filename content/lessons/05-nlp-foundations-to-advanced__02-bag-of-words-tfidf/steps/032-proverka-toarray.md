---
id: 032-proverka-toarray
type: check
title: 'Проверка: чем опасен преждевременный toarray'
source_anchor: '## Use It'
check:
  - question: >-
      У тебя разреженная матрица для 100 000 документов и 50 000 слов. Что
      произойдёт при вызове toarray()?
    options:
      - 'Матрица сохранит только ненулевые значения, но округлит их'
      - >-
        Создастся плотная матрица из 5 миллиардов ячеек, и памяти может не
        хватить
      - 'Удалятся слова, которые редко встречаются в документах'
    correct: 1
    explanation: >-
      toarray() материализует все ячейки, включая нули. При 8 байтах на значение
      такая матрица потребует около 40 ГБ памяти.
  - question: >-
      Тебе нужно быстро посмотреть несколько значений большой разреженной
      матрицы. Как поступить безопаснее?
    options:
      - 'Сначала превратить в плотную всю матрицу, затем выбрать нужные строки'
      - Преобразовать в плотный вид только небольшой выбранный фрагмент
      - Сделать копию матрицы и вызвать toarray() у копии
    correct: 1
    explanation: >-
      Небольшой срез требует памяти только для выбранных ячеек. Копирование не
      спасает: полное преобразование всё равно материализует все нули.
---

Ты уже видел в [шаге 31](#step-31), почему текстовые матрицы хранят разреженными. Теперь проверь главный практический риск: `.toarray()` не просто меняет удобство отображения, а создаёт в памяти каждую ячейку, включая миллиарды нулей. Поэтому код может работать на трёх учебных документах и внезапно завершиться из-за нехватки памяти на настоящем корпусе.

Для проверки смотри плотным только маленький срез. Всю матрицу оставляй разреженной до операции, которой действительно нужен другой формат.

> 🎒 **На пальцах.** Представь парковку на пять миллиардов мест, где занята лишь одна ячейка из тысячи. Разреженный журнал записывает только номера занятых мест. `.toarray()` требует составить отдельную отметку «свободно» или «занято» для каждого места. Машин не прибавилось, но один полный список способен занять весь архив.
