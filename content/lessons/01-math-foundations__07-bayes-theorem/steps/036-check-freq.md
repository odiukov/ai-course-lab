---
id: 036-check-freq
type: check
title: 'Проверка: какой подход выбрать при 30 примерах и цене ошибки'
source_anchor: '### Bayesian vs frequentist: the practical difference'
check:
  - question: >-
      У тебя всего 30 размеченных примеров, а ошибочное решение модели может
      привести к большим потерям. Какой подход разумнее выбрать?
    options:
      - >-
        Частотную точечную оценку, потому что скорость вычислений автоматически
        компенсирует риск ошибки
      - >-
        Байесовский подход, потому что априорное распределение помогает при
        малой выборке, а результат позволяет учитывать неопределённость
      - >-
        Любой подход без оценки неопределённости, потому что размер выборки не
        влияет на риск переобучения
    correct: 1
    explanation: >-
      На 30 примерах априорное распределение может играть роль регуляризации.
      При высокой цене ошибки также важно знать не только наиболее вероятную
      оценку, но и степень неопределённости вокруг неё.
  - question: >-
      Сервис рекомендаций обучается на миллионах кликов и обновляет модель
      каждый час. Почему для него часто практичнее частотный SGD, чем полный
      байесовский вывод?
    options:
      - Потому что байесовские методы нельзя применять к большим наборам данных
      - >-
        Потому что при большом объёме данных влияние фиксированного априорного
        обычно уменьшается, а расчёт полного распределения требует
        дополнительных вычислений
      - >-
        Потому что SGD без дополнительных вычислений возвращает полное
        распределение параметров
    correct: 1
    explanation: >-
      Миллионы наблюдений обычно делают вклад правдоподобия доминирующим над
      априорным. Если сервису достаточно точечной оценки, быстрый SGD может быть
      выгоднее полного байесовского вывода и связанного с ним сэмплирования.
---

Дальше в курсе тебе придётся выбирать подход не по красоте теории, а по двум вещам: сколько у тебя данных и что случится, если модель ошибётся. После этого шага ты должен уметь по описанию задачи сказать, где хватит одного числа, а где нужен разброс значений — и объяснить почему.

Спотыкаются обычно на двух местах. Первое: кажется, что байесовский подход «точнее». Он не точнее — он отвечает на другой вопрос и стоит дороже по вычислениям. Второе: забывают, что при большом объёме данных априорное почти перестаёт влиять, поэтому спор о нём теряет смысл именно там, где данных много, а не там, где их 30.

> 🎒 **На пальцах.** Фотограф на съёмке репортажа делает пятьсот кадров с одной средней настройкой: скорость важнее, промах не страшен, следующий кадр через секунду. Тот же фотограф на единственном кадре важного момента снимает брекетингом — несколько вариантов экспозиции, потому что второго шанса не будет. Мало попыток и высокая цена промаха — берёшь разброс. Много попыток и дёшево — берёшь одну настройку.
