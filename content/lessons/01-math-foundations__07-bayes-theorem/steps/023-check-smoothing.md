---
id: 023-check-smoothing
type: check
title: 'Проверка: что меняет smoothing=10 против smoothing=0.01'
source_anchor: '## Exercises'
check:
  - question: >-
      В небольшом обучающем корпусе слово часто встречается в спаме и ни разу —
      в ham. Как smoothing=10 повлияет на силу этого признака?
    options:
      - >-
        Вероятность слова в ham останется нулевой, поэтому итоговое произведение
        обнулится
      - >-
        Условные вероятности в spam и ham заметно сблизятся, поэтому слово
        станет более слабым свидетельством спама
      - Различие между классами обязательно возрастёт
      - Сглаживание изменит только априорные вероятности классов
    correct: 1
    explanation: >-
      При smoothing=10 к счётчику каждого слова добавляется крупная
      псевдочастота. Она сглаживает различия между классами и приближает оценки
      слов к общему фону.
  - question: >-
      Слово часто встречается в спаме, но отсутствует в ham. Что произойдёт с
      его условными вероятностями при smoothing=0.01?
    options:
      - Вероятность в ham останется строго нулевой
      - >-
        Вероятность в ham станет небольшой, но положительной, а различие со spam
        в основном сохранится
      - Вероятности в spam и ham станут практически одинаковыми
      - Вероятность в spam обязательно станет меньше вероятности в ham
    correct: 1
    explanation: >-
      Положительная добавка 0.01 устраняет нулевую вероятность в ham, но слабо
      меняет реальные частоты. Поэтому слово продолжает сильно поддерживать
      класс spam.
---

Смягчение — не переключатель «вкл/выкл», а ручка громкости, и её положение надо уметь выбирать. Единицу из формулы Лапласа можно заменить любым числом $\alpha$: $\frac{\text{count}(w,C)+\alpha}{\text{total}(C)+\alpha V}$, где $\alpha$ — сколько «выдуманных» встреч мы дописываем каждому слову, а $V$ — размер словаря.

При $\alpha = 0{,}01$ добавка почти не заметна: частоты остаются близки к настоящим, слова говорят громко, а невиданное слово получает крошечную, но не нулевую оценку. При $\alpha = 10$ выдуманные встречи перевешивают реальные, и оценки всех слов сползаются к одному фону — классификатор перестаёт различать улики.

Спотыкаются обычно здесь: думают, что большое смягчение «надёжнее». Оно безопаснее против нулей, но платит потерей чувствительности.

> 🎒 **На пальцах.** Это как ползунок размытия в фоторедакторе. На нуле видна каждая пылинка на объективе — включая случайную. Чуть-чуть размытия убирает пыль, а лица остаются узнаваемыми. Выкрути ползунок на максимум — пыли точно нет, но и лиц тоже: остаётся ровное серое пятно, по которому никого не опознать.
