---
id: 026-check-bins
type: check
title: 'Проверка: куда попадёт 440 Hz при N = 1024'
source_anchor: '### Step 4: find the dominant frequency'
check:
  - question: >-
      Расчёт позиции тона даёт 440 × 1024 / 16000 = 28.16. Как использовать этот
      результат для прогноза спектрального пика?
    options:
      - >-
        Выбрать ближайший целочисленный индекс, потому что дробных bin'ов в DFT
        нет
      - 'Искать отдельный дробный bin, сохранив индекс без изменений'
      - Считать дробную часть признаком aliasing
      - 'Увеличивать sample rate, пока позиция не станет целой'
    correct: 0
    explanation: >-
      Значение 28.16 показывает положение частоты относительно сетки, но индекс
      bin обязан быть целым. Поэтому максимум ожидается около ближайшего узла —
      bin 28.
  - question: >-
      После перевода bin 28 обратно в герцы получилось 437.5 Hz вместо исходных
      440 Hz. Как проверить, является ли расхождение ожидаемым?
    options:
      - Вычислить шаг сетки sr / N и сравнить с ним расхождение
      - 'Потребовать, чтобы обратная функция всегда возвращала исходную частоту'
      - Считать любое расхождение доказательством aliasing
      - 'Изменить значения сэмплов, не меняя sample rate'
    correct: 0
    explanation: >-
      При sr = 16000 и N = 1024 шаг сетки равен 15.625 Hz. Расхождение 2.5 Hz
      укладывается в разрешение сетки: bin 28 соответствует 437.5 Hz, а
      отдельного узла ровно на 440 Hz нет.
---

После этого куска ты должен уметь считать в обе стороны: из частоты — в номер bin, и из номера bin — обратно в герцы, той же формулой $f_k = \frac{k \cdot sr}{N}$ из [шага 24](#step-24), просто выраженной относительно нужного тебе символа.

Спотыкаются тут обычно на двух вещах. Первая — дробный результат. Когда получается 28.16, хочется округлить «как-нибудь» или решить, что расчёт неверен; на самом деле дробного bin просто нет, и пик садится в целый номер.

Вторая — обратный перевод. Он почти никогда не возвращает ровно ту частоту, с которой ты начал, и это пугает сильнее всего: кажется, что где-то потерялась точность в `bin_to_hz` из [шага 25](#step-25). Прежде чем лезть в код, сравни расхождение с шагом сетки — если оно меньше, искать нечего.
