- concept: предикат отбора — конъюнкция ветки, темы и статуса
  question: 'Заполни пропуск в фильтре `select_active_handoff`: отбираются записи нужной ветки и темы, но ещё не отсеяны замещённые и архивные.'
  explanation: 'Совпадения ветки и темы мало: `superseded` и `archived` тоже несут ту же пару ключей. Третье условие по статусу — то, что превращает похожий файл в единственную разрешённую точку входа.'
  kind: cloze
  template: matches = [h for h in handoffs if h["branch"] == branch and h["topic"] == topic and h["status"] == ___]
  answer: '"active"'
  accept:
    - '''active'''
    - active
    - '"active"'
  id: 026-select-active-1
  fingerprint: c04b9436
- concept: несколько подходящих активных записей — повреждённое состояние, требующее остановки, а не выбора
  question: '`select_active_handoff` нашла в списке более одной записи, у которой совпали ветка и тема и статус равен `active`. Как должна повести себя функция?'
  explanation: Несколько активных записей на одну пару — нарушение инварианта, то есть учёт уже сломан. Любой автоматический выбор скрывает поломку и может отправить сессию по устаревшему маршруту. Функция читает состояние и не должна его чинить сама — тем более молча.
  kind: choice
  options:
    - Вернуть запись с самым поздним `last_known_good_commit` как наиболее актуальную
    - 'Вернуть первую найденную: любая активная запись пригодна для продолжения'
    - Сообщить о конфликте и не возвращать пакет — лишние записи должен пометить человек
    - Самостоятельно перевести все записи, кроме одной, в `superseded` и вернуть оставшуюся
  correct: 2
  id: 026-select-active-2
  fingerprint: e6b43f57
- concept: отсутствие совпадения блокирует продолжение, а не разрешает откат на замещённую запись
  question: Для пары «ветка + тема» есть только записи со статусами `superseded` и `archived`, активной нет. Что должна вернуть функция отбора?
  explanation: 'Замещённая запись потому и замещена, что её план мог быть уже выполнен: продолжение по ней повторяет сделанное и рискует затереть свежий результат. Пустой пакет-заглушка ещё хуже — он выглядит валидным входом. Честный ответ — «активной передачи нет».'
  kind: choice
  options:
    - 'Самую свежую из `superseded`: она ближе всего к актуальному состоянию'
    - Любую запись с совпавшей темой из другой ветки, чтобы сессии было с чего начать
    - 'Признак отсутствия точки входа: продолжать не из чего, пока активная передача не создана'
    - Пустой пакет со всеми обязательными полями, заполненными значениями по умолчанию
  correct: 2
  id: 026-select-active-3
  fingerprint: c1f31f1f
