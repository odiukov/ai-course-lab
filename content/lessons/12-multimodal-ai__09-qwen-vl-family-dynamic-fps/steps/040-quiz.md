---
id: 040-quiz
type: quiz
title: Итоговая проверка
source_anchor: '## Key Terms'
check:
  - question: >-
      Видео длится 300 секунд, на него выделено 24300 tokens, один кадр стоит 81
      token. Какую частоту выборки ты можешь себе позволить?
    options:
      - 4 кадра в секунду
      - 1 кадр в секунду
      - 0.25 кадра в секунду
      - 'Частота от бюджета не зависит, её задаёт сама модель'
    correct: 1
    explanation: >-
      Делим бюджет на длительность и на цену кадра: 24300 разделить на 300 и на
      81 даёт ровно 1. Больше кадров в секунду просто не поместится в окно.
  - question: >-
      Два патча взяты из одного и того же кадра видео, но один сверху слева,
      другой снизу справа. Чем отличаются их позиции в M-RoPE?
    options:
      - 'Отличается только временная координата, пространственные совпадают'
      - 'Отличаются координаты высоты и ширины, временная координата у них общая'
      - >-
        Все три координаты обязаны отличаться, иначе позиции считаются
        одинаковыми
      - У патчей одного кадра позиция одна на всех
    correct: 1
    explanation: >-
      Время у патчей одного кадра одно и то же, а различают их высота и ширина —
      каждая координата крутит свою полосу скрытой размерности.
  - question: >-
      Один и тот же двенадцатый по счёту кадр модель видит в двух видео: одно
      снято с выборкой 4 кадра в секунду, другое с выборкой 1 кадр в секунду.
      Что даёт ей понять, что это разные моменты?
    options:
      - Номер кадра в последовательности — он и есть время
      - 'Ничего: для модели оба случая выглядят одинаково'
      - >-
        Вставленный token абсолютного времени, где стоят реальные секунды, а не
        индекс
      - Разное число tokens на кадр
    correct: 2
    explanation: >-
      Индекс 12 при 4 FPS — это 3 секунды, а при 1 FPS — 12 секунд. Отличает их
      только явный token с реальным временем.
  - question: >-
      Ты просишь модель кликнуть по кнопке на скриншоте и получаешь ответ:
      «Кнопка находится примерно в правом верхнем углу, чуть ниже строки
      поиска». Что не так для агента?
    options:
      - 'Ответ неверный по сути, координаты названы неправильно'
      - >-
        Ответ разбирается только человеком: агенту нужны имя инструмента и
        числовые координаты в разбираемом виде
      - Слишком мало tokens потрачено на описание
      - 'Нужно было увеличить max_pixels, чтобы модель ответила подробнее'
    correct: 1
    explanation: >-
      Проза может быть по смыслу верной, но клик по ней не сделаешь: агенту
      нужен структурированный вывод с координатами и именем инструмента.
---

Ты прошёл весь путь: от трёх разрывов первого Qwen-VL до ручек `min_pixels` и `max_pixels`, которые крутишь на каждый запрос. Дальше в курсе тебе понадобится не список версий, а три рабочих привычки: считать tokens руками, прежде чем закидывать видео в модель; понимать, что позиция — это адрес из трёх чисел, а не номер в очереди; и требовать от модели разбираемый вывод, если её ответ куда-то поедет автоматически.

Спотыкаются обычно на двух местах. Первое — путают индекс кадра с временем: кажется, что порядок кадров уже несёт секунды, а он их не несёт. Второе — забывают, что бюджет tokens жёсткий, и FPS выбирается под него, а не наоборот.
