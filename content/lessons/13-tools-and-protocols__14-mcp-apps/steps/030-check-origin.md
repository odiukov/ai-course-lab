---
id: 030-check-origin
type: check
title: 'Проверка: какое сообщение проходит фильтр'
source_anchor: '### postMessage protocol'
check:
  - question: >-
      Хост принимает сообщения только от https://ui.example.com и после проверки
      origin валидирует JSON-RPC-конверт. Какое сообщение следует обработать?
    options:
      - >-
        Сообщение с https://ui.example.com, содержащее объект с версией JSON-RPC
        2.0, id и строковым method
      - >-
        Сообщение с https://ui.example.com.attacker.net, содержащее корректный
        JSON-RPC-конверт
      - 'Сообщение с https://ui.example.com, у которого data является строкой'
      - 'Сообщение с неизвестного origin, у которого id совпадает с ожидаемым'
    correct: 0
    explanation: >-
      Сначала origin должен целиком совпасть с разрешённой строкой, затем
      проверяется форма данных. Похожий домен, совпавший id или внешне
      правильный конверт не заменяют проверку источника.
  - question: >-
      Iframe отправляет host.callTool в window.parent с targetOrigin, равным
      звёздочке, а хост строго проверяет origin входящих сообщений. Какой риск
      на стороне отправителя всё равно остаётся?
    options:
      - >-
        Если родительское окно перейдёт на чужой origin, браузер всё равно
        сможет доставить ему имя инструмента и аргументы
      - Сообщение автоматически разошлётся всем открытым страницам браузера
      - Хост не сможет прочитать сообщение из-за несовместимого формата JSON-RPC
      - 'Браузер заменит id запроса, поэтому ответ нельзя будет сопоставить'
    correct: 0
    explanation: >-
      postMessage отправляется конкретному объекту Window, а не всем страницам.
      Однако документ в этом окне может сменить origin; точный targetOrigin
      остановил бы доставку, а звёздочка разрешает её.
---

Ты дописал `accept_message` в [шаге 29](#step-29) — теперь проверим, что фильтр действительно фильтрует.

После этих экранов ты должен уметь глядя на одно сообщение сказать, обработает его хост или молча выйдет: сверить `event.origin` со списком разрешённых до того, как трогать `event.data`, и понимать, что `targetOrigin` на отправке и проверка на приёме — две разные защиты, а не одна.

Спотыкаются обычно на двух вещах. Первая — похожий домен: `ui.example.com.attacker.net` глазами читается как «свой», а сравнение строк говорит обратное. Вторая — соблазн сначала посмотреть на содержимое: «тут же правильный `jsonrpc` и знакомый `id`, значит от своих». Наоборот: и форма, и `id` подделываются бесплатно, а origin ставит браузер.
