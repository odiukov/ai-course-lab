---
id: 010-check-shema
type: check
title: 'Проверка: что вернул инструмент, а что resources/read'
source_anchor: '### The `ui://` resource scheme'
check:
  - question: >-
      Хост вызвал tools/call и получил ответ. Что в этом ответе относится к
      самой шкале заметок?
    options:
      - 'Готовый HTML-бандл шкалы, целиком, вместе с текстом для модели'
      - 'Только адрес ui://notes/timeline, по которому HTML можно будет запросить'
      - 'Ссылка на внешний сайт, откуда браузер сам подтянет страницу'
    correct: 1
    explanation: >-
      tools/call отдаёт указатель — строку ui://notes/timeline. Сам HTML
      приходит отдельным ответом на resources/read по этому адресу.
  - question: >-
      В ответе resources/read у contents стоит mimeType
      text/html;profile=mcp-app. Что это говорит хосту?
    options:
      - 'Что это не HTML, а особый формат, и разбирать его надо своим парсером'
      - >-
        Что перед ним обычный HTML, но с пометкой: показывать его как
        MCP-приложение
      - 'Что хост обязан запросить этот ресурс заново, уже с другим заголовком'
    correct: 1
    explanation: >-
      Профиль — приписка к знакомому text/html: парсер тот же, а пометка
      говорит, по каким правилам этот HTML запускать.
---

Проверь себя на одной практической вещи: тебе показывают ответ и ты должен сказать, из какого он вызова. Если внутри лежит строка `ui://notes/timeline` и текст «Here is your notes timeline:» — это `tools/call`, инструмент отдал адрес. Если внутри `contents` с `mimeType` и `text: "<!doctype html>..."` — это `resources/read`, приехал сам бандл.

Чаще всего спотыкаются на том, что `uri` встречается в обоих ответах, и кажется, будто это один и тот же шаг. Нет: в первом ответе `uri` — это обещание, во втором — подпись под доставленным содержимым. Разделение из [шага 8](#step-8) и есть то, что позволяет хосту не тянуть HTML второй раз.
