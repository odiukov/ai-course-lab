- concept: отрицание оценок позволяет получить индексы в порядке убывания через np.argsort
  question: Заполни пропуск так, чтобы первыми оказались дочерние чанки с наибольшей оценкой сходства.
  explanation: np.argsort сортирует по возрастанию. После смены знака наибольшие исходные оценки становятся наименьшими значениями, поэтому их индексы оказываются в начале массива.
  kind: cloze
  template: top = np.argsort(___)[:child_top_k]
  answer: -scores
  accept:
    - -scores
  id: 025-retrieve-parents-1
  fingerprint: '01679750'
- concept: матрично-векторное произведение создаёт по одной оценке релевантности для каждой строки матрицы дочерних эмбеддингов
  question: Строки матрицы child_embs содержат эмбеддинги дочерних чанков, а q_emb — эмбеддинг запроса. Что возвращает выражение child_embs @ q_emb?
  explanation: Каждая строка child_embs скалярно умножается на q_emb. Результат — одномерный массив оценок, расположенных в том же порядке, что и дочерние чанки в mapping.
  kind: choice
  options:
    - Вектор, где каждому дочернему чанку соответствует его оценка сходства с запросом
    - Единственный усреднённый эмбеддинг всех дочерних чанков
    - Матрицу попарного сходства всех дочерних чанков друг с другом
  correct: 0
  id: 025-retrieve-parents-2
  fingerprint: 2f8875b1
