- concept: 'реализация степени согласия: максимум по частотам ответов, делённый на длину списка'
  question: 'Заполни пропуск: функция должна вернуть долю агентов, давших самый частый ответ.'
  explanation: Counter даёт частоты ответов, max по значениям — размер крупнейшей группы. Деление на len(answers) переводит его в долю от 0 до 1.
  kind: cloze
  template: |-
    from collections import Counter

    def agreement_score(answers):
        return ___(Counter(answers).values()) / len(answers)
  answer: max
  accept:
    - max
  id: 018-agreement-score-1
  fingerprint: 7ce3fa43
