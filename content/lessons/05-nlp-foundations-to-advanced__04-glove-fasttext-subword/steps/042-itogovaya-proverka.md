---
id: 042-itogovaya-proverka
type: quiz
title: 'Итоговая проверка: GloVe, FastText, BPE и OOV'
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты обучаешь GloVe и замечаешь, что одна пара слов встречается намного чаще
      остальных. Почему она не должна полностью подчинить себе обучение?
    options:
      - Частые пары удаляются из матрицы совместной встречаемости
      - 'Вес частых пар ограничивается, а модель работает с логарифмом частоты'
      - Для каждой частой пары создаётся отдельный словарь
      - 'Обновляются только смещения, но не векторы'
    correct: 1
    explanation: >-
      Ограниченная весовая функция не даёт очень частым парам подавить
      остальные, а логарифм сжимает разницу между большими частотами.
  - question: >-
      В словаре нет слова сверхперепроверенный. Какая модель всё ещё может
      собрать для него вектор без добавления слова в словарь?
    options:
      - 'GloVe, потому что использует глобальные частоты'
      - 'TF-IDF, потому что учитывает редкость'
      - 'FastText, если известны символьные n-граммы слова'
      - 'Обычный Word2Vec, если увеличить окно'
    correct: 2
    explanation: >-
      FastText складывает векторы известных символьных фрагментов, поэтому может
      представить целиком незнакомое слово.
  - question: >-
      Ты применяешь BPE-слияния в порядке их частоты в новом тексте, а не в
      выученном порядке. Что может произойти?
    options:
      - >-
        Разбиение изменится и перестанет соответствовать токенам обученной
        модели
      - 'Ничего: порядок слияний не влияет на результат'
      - Каждое слово обязательно превратится в один токен
      - 'BPE начнёт возвращать вектор слова, как FastText'
    correct: 0
    explanation: >-
      Каждое слияние меняет доступные соседние пары, поэтому порядок является
      частью обученного токенизатора.
  - question: >-
      Чем принципиально различаются результаты FastText и BPE для незнакомого
      слова?
    options:
      - 'FastText выдаёт последовательность токенов, а BPE сразу один вектор'
      - Оба метода всегда выдают один и тот же набор символов
      - >-
        FastText собирает вектор из признаков, а BPE разбивает слово на
        последовательность токенов
      - >-
        FastText и BPE могут работать только со словами из фиксированного
        словаря
    correct: 2
    explanation: >-
      FastText использует кусочки как признаки для одного вектора слова, а BPE
      делает кусочки самостоятельными входными токенами модели.
---

Дальше тебе пригодится не перечень названий, а умение выбрать представление под задачу. Сверяй источник информации GloVe по [шагу 8](#step-8), механизм OOV в FastText по [шагу 19](#step-19), порядок BPE по [шагу 29](#step-29) и различие их результатов по [шагу 33](#step-33). Частая ловушка — считать любые subword-части взаимозаменяемыми: одинаковое слово может быть разобрано на кусочки с совершенно разной ролью.

> 🎒 **На пальцах.** Представь набор для ремонта: GloVe выбирает готовый инструмент из каталога, FastText собирает нужный инструмент из знакомых насадок, а BPE раскладывает работу на последовательность стандартных операций. Детали могут выглядеть похоже, но результат разный: готовый вектор, собранный вектор или цепочка токенов.
