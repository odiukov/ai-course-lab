---
id: 004-tri-podhoda
type: check
title: 'Проверка: какую проблему решают GloVe, FastText и BPE'
source_anchor: '## The Problem'
check:
  - question: >-
      Ты выбираешь подход для трёх задач: использовать глобальные счётчики
      контекстов, получить вектор незнакомого слова из его частей и разбить
      любой текст на единицы известного словаря. Какое соответствие верно?
    options:
      - 'GloVe — счётчики, FastText — вектор слова, BPE — разбиение текста'
      - 'FastText — счётчики, BPE — вектор слова, GloVe — разбиение текста'
      - 'BPE — счётчики, GloVe — вектор слова, FastText — разбиение текста'
    correct: 0
    explanation: >-
      GloVe использует глобальную совместную встречаемость, FastText складывает
      вектор из символьных частей, а BPE представляет текст последовательностью
      известных subword-единиц.
  - question: >-
      В тексте встретилось придуманное слово, которого не было при обучении. В
      чём главное различие между FastText и BPE?
    options:
      - >-
        FastText пытается собрать вектор целого слова из символьных фрагментов,
        а BPE разбивает слово на известные токены
      - 'FastText добавляет слово в словарь, а BPE удаляет его из текста'
      - >-
        FastText анализирует глобальные контексты, а BPE обучает отдельный
        вектор для каждого нового слова
    correct: 0
    explanation: >-
      Оба подхода используют части слова, но решают разные задачи: FastText
      восстанавливает представление слова, а BPE гарантирует, что текст можно
      записать известными токенами.
---

Здесь важно не смешать три разных затруднения. GloVe отвечает на вопрос о глобальных контекстах из [шага 1](#step-1). FastText подхватывает незнакомое слово, складывая его представление из символьных частей, — это проблема [шага 2](#step-2). BPE решает ограничение словаря из [шага 3](#step-3): любое слово можно разложить на известные subword-токены.

Чаще всего путают FastText и BPE, потому что оба работают с кусочками. Проверяй цель: нужен один вектор нового слова — смотри на FastText; нужно гарантированно превратить любой текст в последовательность токенов — на BPE. GloVe же чинит не словарь, а способ извлекать смысл из контекстных счётчиков.
