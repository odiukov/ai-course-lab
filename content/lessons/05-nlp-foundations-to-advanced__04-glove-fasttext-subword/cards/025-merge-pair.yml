- concept: одно применение BPE-слияния заменяет неперекрывающиеся вхождения пары слева направо
  question: Какой результат должно дать одно BPE-слияние для tokens = ("x", "x", "x", "x", "x") и pair = ("x", "x")?
  explanation: После совпадения обе исходные части считаются использованными. Поэтому проход последовательно создаёт два токена "xx", а последний "x" оставляет без изменений.
  kind: choice
  options:
    - ("xx", "xx", "x")
    - ("xxxxx",)
    - ("x", "xx", "xx")
  correct: 0
  id: 025-merge-pair-1
  fingerprint: 9d5ea0d0
- concept: маркер конца слова участвует в BPE-слияниях как обычная часть последовательности
  question: Подставь результат однократного слияния пары ("t", "</w>").
  explanation: 'Маркер </w> обрабатывается как обычный токен: соседние "t" и "</w>" заменяются их конкатенацией "t</w>", а остальные части сохраняют порядок.'
  kind: cloze
  template: merge_pair(("c", "a", "t", "</w>"), ("t", "</w>")) == ___
  answer: ("c", "a", "t</w>")
  accept:
    - ('c', 'a', 't</w>')
  id: 025-merge-pair-2
  fingerprint: b6999127
