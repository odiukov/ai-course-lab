- concept: сглаженный target encoding смешивает среднее категории с глобальным средним с весом, зависящим от размера категории
  question: В категории восемь объектов, параметр smoothing равен двенадцати, среднее целевой переменной для категории — 0.75, а глобальное среднее — 0.25. Какое закодированное значение получит категория?
  explanation: Вес категории равен 8 / (8 + 12) = 0.4. Поэтому encoding = 0.4 × 0.75 + 0.6 × 0.25 = 0.45.
  kind: numeric
  answer: 0.45
  tolerance: 0.001
  id: 030-target-encode-1
  fingerprint: 77f12310
- concept: закодированный список восстанавливается в исходном порядке посредством поиска значения каждой категории в словаре кодировки
  question: Заполни пропуск так, чтобы функция вернула закодированные значения в том же порядке, в котором категории находились во входном списке.
  explanation: Перебор feature_values сохраняет порядок исходных элементов, а обращение encoding[v] заменяет каждую категорию рассчитанным для неё числом. Поэтому повторяющиеся категории получают одинаковый код, оставаясь на своих позициях.
  kind: cloze
  template: return [encoding[___] for v in feature_values], encoding
  answer: v
  accept:
    - v
  id: 030-target-encode-2
  fingerprint: fd46659e
