- concept: ориентация матриц при вычислении сходства всех документов со всеми запросами
  question: Матрица документов имеет форму (N, D), а батч запросов — (Q, D). Какое выражение создаст матрицу оценок формы (N, Q), где строки соответствуют документам?
  explanation: Транспонирование превращает матрицу запросов из формы (Q, D) в (D, Q). Произведение (N, D) на (D, Q) даёт по одной оценке для каждой пары документа и запроса.
  kind: choice
  options:
    - embeddings @ q_emb.T
    - embeddings.T @ q_emb
    - q_emb @ embeddings.T
  correct: 0
  id: 024-dense-rank-1
  fingerprint: 8587a4e1
- concept: преобразование двумерного столбца оценок в одномерный массив
  question: Заполни пропуск методом NumPy, чтобы результат произведения для единственного запроса стал одномерным массивом оценок.
  explanation: Произведение с батчем из единственного запроса остаётся двумерным столбцом. `flatten()` превращает его в вектор, поэтому `sims[i]` возвращает отдельную оценку, а не вложенный массив.
  kind: cloze
  template: sims = (embeddings @ q_emb.T).___
  answer: flatten()
  accept:
    - ravel()
  id: 024-dense-rank-2
  fingerprint: 4c2d1487
