- concept: идентичность документа при накоплении оценки RRF определяется его индексом
  question: Элемент входного ранжирования имеет вид `(retriever_score, doc_idx)`. Какой ключ следует использовать в словаре накопленных оценок, чтобы объединить появления одного документа?
  explanation: '`doc_idx` однозначно обозначает документ во всех ранжированиях. Поэтому каждый вклад прибавляется к `scores[doc_idx]`, даже если исходные оценки ретриверов различаются.'
  kind: choice
  options:
    - '`doc_idx`'
    - '`retriever_score`'
    - Всю пару `(retriever_score, doc_idx)`
  correct: 0
  id: 028-reciprocal-rank-fusion-1
  fingerprint: c1528cfb
- concept: перестановка полей при преобразовании словаря оценок в формат ранжирования
  question: Словарь `scores` хранит соответствие `doc_idx → fused_score`, поэтому после сортировки элементы `fused` имеют вид `(doc_idx, fused_score)`. Какое выражение вернёт результат в формате `(score, doc_idx)`?
  explanation: '`scores.items()` выдаёт сначала ключ документа, затем накопленную оценку. В выходной паре поля нужно поменять местами, чтобы оценка стояла перед `doc_idx`.'
  kind: choice
  options:
    - '`[(score, doc_idx) for doc_idx, score in fused]`'
    - '`[(doc_idx, score) for doc_idx, score in fused]`'
    - '`[(score, doc_idx) for score, doc_idx in fused]`'
  correct: 0
  id: 028-reciprocal-rank-fusion-2
  fingerprint: 6bf183a1
