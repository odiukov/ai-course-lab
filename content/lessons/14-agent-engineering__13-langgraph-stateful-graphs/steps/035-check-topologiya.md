---
id: 035-check-topologiya
type: check
title: 'Проверка: какая топология под задачу'
source_anchor: '### Three topologies'
check:
  - question: >-
      Задача: восемь узких специалистов, и на каждом шаге нужно решать, кто из
      них берёт работу дальше, потому что порядок заранее неизвестен. Что будет,
      если выбрать swarm вместо supervisor?
    options:
      - >-
        Решения о передаче размажутся по агентам, и общей картины прогона не
        будет ни у кого
      - 'Граф вообще не соберётся: swarm не поддерживает больше четырёх агентов'
      - >-
        Каждый шаг станет дороже, потому что swarm тратит два вызова LLM вместо
        одного
    correct: 0
    explanation: >-
      В swarm центрального роутера нет, агенты передают работу друг другу сами
      через общий набор инструментов. Лишних вызовов на выбор следующего не
      тратится, но и никто не держит картину целиком — а когда порядок заранее
      неизвестен, именно эта картина и нужна.
  - question: >-
      Команда взяла supervisor «по умолчанию». Объём полезной работы прежний, а
      счёт за вызовы модели оказался примерно вдвое больше ожидаемого. Куда
      уходит разница?
    options:
      - >-
        Супервизор перезапускает субагента, если тот вернул слишком короткий
        ответ
      - >-
        Каждое решение «кто следующий» — это отдельный вызов LLM, и таких
        решений столько же, сколько шагов работы
      - >-
        Супервизор дублирует запрос сразу двум субагентам и выбирает лучший
        ответ
    correct: 1
    explanation: >-
      Supervisor — это роутер на LLM: на каждом шаге он тратит собственный вызов
      только на то, чтобы выбрать исполнителя. Полезная работа и маршрутизация
      делят бюджет примерно поровну. Если следующего исполнителя можно вывести
      из предметной области, за это решение платить не нужно — там уместнее
      swarm.
---

Три топологии из [шага 33](#step-33) и [шага 34](#step-34) выглядят как список на выбор, но выбирают их не по вкусу. Ты должен уметь по описанию задачи сказать, за что заплатишь: в supervisor — отдельным вызовом LLM на каждое решение «кто следующий», в swarm — отсутствием единой картины прогона, в иерархии — количеством роутеров, которое растёт быстрее, чем количество исполнителей.

Спотыкаются обычно на двух вещах. Первая: считают supervisor «правильным по умолчанию» и не замечают, что половина вызовов уходит не на работу, а на маршрутизацию. Вторая: в иерархии считают только исполнителей и забывают посчитать бригадиров.
