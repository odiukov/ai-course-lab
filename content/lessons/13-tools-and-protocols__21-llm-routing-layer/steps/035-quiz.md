---
id: 035-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: 'Шлюз вернул ошибку: у команды кончилась квота на ключ. Что делать коду?'
    options:
      - Перейти к следующему провайдеру из fallback-цепочки
      - >-
        Не ретраить и вернуть отказ, потому что смена провайдера не восстановит
        квоту
      - Несколько раз повторить тот же запрос с короткой задержкой
    correct: 1
    explanation: >-
      Квоту проверяет сам шлюз до выбора провайдера. Поэтому ни повтор запроса,
      ни переход к другому бэкенду не устранят причину отказа.
  - question: >-
      После переключения по fallback-цепочке успешный ответ вернула более
      дешёвая резервная модель. С какой моделью нужно связать токены этого
      ответа в учёте?
    options:
      - 'С моделью, которую приложение запросило первоначально'
      - >-
        С резервной моделью, которая фактически обработала запрос и вернула
        usage
      - 'С обеими моделями, разделив токены поровну'
    correct: 1
    explanation: >-
      Usage успешного ответа относится к фактическим провайдеру и модели. Если
      другие попытки тоже тарифицировались, их записывают отдельными событиями,
      а не приписывают их токены ответившей модели.
  - question: >-
      В коде используется алиас our_smart_model вместо точного идентификатора
      снапшота. Что нужно сделать при плановом переходе на новую версию модели?
    options:
      - Заменить алиас во всех клиентах на название нового снапшота
      - Изменить сопоставление алиаса в конфигурации шлюза
      - 'Ничего не менять: алиас всегда автоматически выбирает новейшую модель'
    correct: 1
    explanation: >-
      Конкретный идентификатор модели хранится за алиасом в конфигурации шлюза.
      Обновление этого сопоставления переводит клиентов на новую версию без
      изменения прикладного кода.
---

Ты прошёл путь от одного захардкоженного id модели до слоя, который сам выбирает, куда отправить запрос, что делать при падении и с кого списать деньги. Дальше в курсе это фундамент: агент из множества шагов без учёта трат и без fallback просто не доживает до продакшена.

Споткнуться легче всего на двух вещах. Первая — путать сбой провайдера и исчерпанный лимит ключа: внешне обе ситуации выглядят как ошибка, но ретрай помогает только в одной. Вторая — считать деньги по той модели, которую ты *просил*, а не по той, которая ответила: после срабатывания каскада из [шага 9](#step-9) это разные строки в счёте.

Проверь себя на трёх вопросах ниже.
