---
id: 005-check-prichiny
type: check
title: 'Проверка: какая это причина роутинга'
source_anchor: '## The Problem'
check:
  - question: >-
      Ночной суммаризатор разбирает десять тысяч писем, результат нужен к утру,
      и никто не смотрит на экран во время работы. Ты переводишь его на самую
      дешёвую модель. Какая причина роутинга тут работает?
    options:
      - >-
        Цена: задача простая и массовая, за скорость первого токена никто не
        платит
      - 'SLA на задержку: пакетной задаче тоже важно отвечать быстро'
      - 'Регион: письма нужно обрабатывать ближе к отправителю'
      - 'A/B-тест: ты сравниваешь две модели на одной нагрузке'
    correct: 0
    explanation: >-
      Ответ никто не ждёт вживую, поэтому быстрое время до первого токена
      бесполезно — выигрыш даёт разница в цене на большом объёме запросов.
  - question: >-
      Пользователь из ЕС шлёт запрос. Провайдер в США сейчас и дешевле, и
      отвечает быстрее. Куда уйдёт запрос при правильно настроенном роутинге?
    options:
      - 'К американскому провайдеру: цена и задержка важнее'
      - 'В регион ЕС: правило по региону перекрывает выгоду по цене и скорости'
      - 'К обоим сразу, чтобы сравнить ответы'
      - 'Никуда: такой запрос нужно отклонить'
    correct: 1
    explanation: >-
      Регион — это требование, а не оптимизация: его нельзя обменять на скидку
      или пару сотен миллисекунд.
---

Пять причин ты уже разобрал по отдельности: цену из [шага 2](#step-2), аварию и задержку из [шага 3](#step-3), регион и A/B-тест из [шага 4](#step-4). Теперь навык другой — увидеть живое описание задачи и назвать, какая причина в нём главная.

Спотыкаются обычно на двух местах. Первое: путают аварию и задержку. Провайдер, который отвечает медленно, и провайдер, который отвечает ошибкой, — разные ситуации и разные правила. Второе: пытаются торговать регионом. Цену и скорость можно взвешивать друг против друга, а требование по региону — нет, оно просто отсекает варианты.

И помни: в одном запросе причины часто накладываются. Вопрос не «какая единственная», а «какая здесь решает».
