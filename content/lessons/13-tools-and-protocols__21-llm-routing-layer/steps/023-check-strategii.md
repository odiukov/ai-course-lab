---
id: 023-check-strategii
type: check
title: 'Проверка: когда task-aware вредит'
source_anchor: '### Routing strategies'
check:
  - question: >-
      Сервис принимает почти одинаковые короткие запросы. После включения
      task-aware перед каждым запросом вызывается классификатор, и его средняя
      стоимость оказалась выше экономии от перенаправления. Что произойдёт с
      общим счётом?
    options:
      - >-
        Счёт вырастет из-за дополнительной стоимости классификации каждого
        запроса
      - >-
        Счёт уменьшится, потому что любое перенаправление в дешёвую модель даёт
        чистую экономию
      - >-
        Счёт не изменится, потому что вызов классификатора не относится к
        стоимости маршрутизации
    correct: 0
    explanation: >-
      Классификатор — отдельный платный вызов перед каждым основным запросом.
      Если его стоимость превышает экономию от выбора модели, task-aware
      стратегия увеличивает общий счёт.
  - question: При каком профиле нагрузки task-aware роутинг вероятнее всего окупится?
    options:
      - Все запросы однотипны и должны попадать в одну модель
      - >-
        Запросы заметно различаются по задачам, а экономия от выбора подходящих
        моделей превышает стоимость классификации
      - Главная цель — исключить любые дополнительные сетевые вызовы
    correct: 1
    explanation: >-
      Task-aware полезен на разнородном трафике, когда классификация
      действительно меняет выбор модели и полученная экономия перекрывает
      постоянную надбавку за классификатор.
---

Пять стратегий из [шага 22](#step-22) отличаются не только умом, но и ценой самого ума. Static priority не стоит ничего сверху. Task-aware требует классификатора — то есть ещё одного вызова модели перед каждым запросом, плюс примерно 100-200 токенов.

Здесь тебе нужно уметь делать одно: сравнивать две величины — сколько ты сэкономил, отправив запрос в дешёвую модель вместо дорогой, и сколько стоил сам факт выбора. Спотыкаются обычно на том, что цену классификатора считают разовой или «фоновой». Она берётся с каждого запроса, включая те, где выбор всё равно очевиден. Проверь себя на двух ситуациях ниже.
