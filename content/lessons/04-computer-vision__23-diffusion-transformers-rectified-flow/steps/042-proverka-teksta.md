---
id: 042-proverka-teksta
type: check
title: 'Проверка: какие текстовые башни используют SD3 и FLUX'
source_anchor: '### Text encoders in SD3 and FLUX'
check:
  - question: Какое различие в составе CLIP-башен отделяет SD-три от FLUX?
    options:
      - 'У SD-три есть дополнительная CLIP-башня, а FLUX использует CLIP-L'
      - 'FLUX полностью отказывается от CLIP, а SD-три сохраняет CLIP-L'
      - Обе системы используют одинаковый набор CLIP-башен
    correct: 0
    explanation: >-
      SD3 использует две модели CLIP вместе с T5-XXL. У FLUX текстовый стек
      состоит из одного CLIP-L и T5-XXL.
  - question: >-
      Почему общий T-пять-XXL не означает, что у SD-три и FLUX одинаковые
      текстовые стеки?
    options:
      - Помимо общего T-пять-XXL системы различаются набором CLIP-энкодеров
      - >-
        Один и тот же T-пять-XXL выдаёт эмбеддинги разных размеров в каждой
        системе
      - >-
        Во FLUX T-пять-XXL используется только при обучении и удаляется перед
        инференсом
    correct: 0
    explanation: >-
      T5-XXL используют обе модели, но SD3 добавляет две модели CLIP, а FLUX —
      один CLIP-L. Сравнивать нужно весь набор текстовых энкодеров.
---

Здесь важно не спутать размер текстового энкодера из [шага 41](#step-41) с количеством башен. Тебе нужно уверенно сопоставлять наборы: у SD3 — две модели CLIP и T5-XXL, у FLUX — одна CLIP-L и T5-XXL. Частая ошибка — заметить общий T5-XXL и решить, что системы используют одинаковый текстовый стек.

> 🎒 **На пальцах.** Представь стойку переводчиков на конференции. У SD3 за стеклом сидят три специалиста: два переводчика CLIP и один T5-XXL. У FLUX их двое: CLIP-L и T5-XXL. Если ты заметил только самого крупного переводчика, команды кажутся одинаковыми; различие видно, когда пересчитываешь всю смену.
