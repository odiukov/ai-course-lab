---
id: 020-proverka-arhitektur
type: check
title: 'Проверка: DiT, MMDiT и архитектура FLUX'
source_anchor: '### From U-Net to transformer'
check:
  - question: >-
      Текстовые и картиночные токены должны участвовать в общем attention, но
      обрабатываться разными наборами весов. Какую архитектуру ты выберешь?
    options:
      - Обычный DiT с одним потоком патчей
      - MMDiT с раздельными весами для двух потоков
      - Однопоточный блок со склеенными токенами и общими весами
      - Свёрточный U-Net без потоков токенов
    correct: 1
    explanation: >-
      В MMDiT текст и изображение сохраняют собственные веса, но встречаются в
      совместном attention.
  - question: Что произойдёт с потоками текста и изображения в поздних блоках FLUX?
    options:
      - Они останутся раздельными до самого выхода
      - 'Текстовый поток завершится, а картиночный продолжит работу'
      - Токены склеятся и будут обрабатываться общими весами
      - Оба потока превратятся обратно в свёрточные карты
    correct: 2
    explanation: >-
      FLUX начинает с двухпоточных блоков, а затем объединяет токены в один
      поток с общими весами.
---

Тебе нужно различать не названия моделей, а маршрут токенов и принадлежность весов. В DiT из [шага 11](#step-11) главный рабочий материал — патчи изображения. MMDiT добавляет два специализированных потока: текст и изображение используют разные веса, хотя встречаются в совместном attention, как разобрано в [шаге 16](#step-16). FLUX сначала сохраняет такое разделение, а позже склеивает токены и применяет общие веса ([шаг 17](#step-17)).

Частая ловушка — решить, что «совместное внимание» уже означает «одинаковые веса». Нет: токены могут общаться в одном attention, оставаясь по-разному обработанными.
