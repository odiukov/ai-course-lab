---
id: 010-proverka-patchey
type: check
title: 'Проверка: число токенов, размерность и roundtrip'
source_anchor: '### Step 2: A tiny DiT'
check:
  - question: >-
      Как правильно интерпретировать две оси последовательности после
      patch-проекции изображения?
    options:
      - >-
        Одна ось перечисляет пространственные патчи, а другая хранит признаки
        каждого патча
      - 'Одна ось перечисляет цветовые каналы, а другая — отдельные пиксели'
      - Обе оси определяются только количеством голов внимания
    correct: 0
    explanation: >-
      Сетка 8 на 8 даёт 64 позиции-токена, а проекция представляет каждый из них
      вектором длины 96. Количество токенов и размерность токена описывают
      разные свойства тензора.
  - question: >-
      Что определяет размерность выхода финальной головы TinyDiT перед
      unpatchify?
    options:
      - Количество пиксельных значений в одном патче с учётом всех каналов
      - Ширина внутреннего представления токена
      - Количество токенов в последовательности
      - Количество голов внимания
    correct: 0
    explanation: >-
      Патч 2 на 2 в трёх каналах содержит 12 значений, поэтому голова выдаёт 12
      чисел на токен. Внутренняя размерность 96 нужна для обработки признаков и
      не обязана совпадать с выходной.
---

Теперь проверь, не смешались ли три разных числа в маршруте из [шага 9](#step-9): 64 — количество токенов, 96 — длина внутреннего представления токена, 12 — содержимое одного восстановленного патча. Частая ошибка — ожидать от выхода тоже 96 чисел. Но 96 нужны модели для обработки, а при сборке изображению нужны исходные пиксельные значения. Roundtrip из [шагов 6](#step-6) и [8](#step-8) сходится лишь тогда, когда число элементов до разбиения и после сборки одинаково.

> 🎒 **На пальцах.** Представь 64 пассажира в экскурсионных наушниках с 96 настройками звука. Настройки помогают провести экскурсию, но на выходе каждый пассажир сдаёт не 96 вещей, а свой пакет из 12 фотографий. Если пассажиров осталось 64 и каждый вернул ровно 12 снимков, весь фотоархив можно собрать обратно без пропусков.
