---
id: 015-proverka-adaln
type: check
title: 'Проверка: scale, shift, gate и безопасный старт'
source_anchor: '### Step 1: A DiT block with AdaLN'
check:
  - question: >-
      При размерности dim = 192 линейный слой получил условие для одного
      изображения. Что он должен выдать перед chunk?
    options:
      - '192 числа: общие для scale, shift и gate'
      - '384 числа: отдельно для изменения масштаба и сдвига'
      - '576 чисел: по 192 для scale, shift и gate'
      - '576 чисел: по одному набору для каждого токена'
    correct: 2
    explanation: >-
      Слой выдаёт три набора размерности 192, а chunk делит 576 чисел на scale,
      shift и gate. Эти параметры создаются из условия, а не отдельно для
      каждого токена.
  - question: >-
      Веса и смещения управляющего линейного слоя равны нулю. Что произойдёт при
      первом проходе через весь DiT-блок?
    options:
      - Нормализация обнулит все токены
      - 'Attention и MLP изменят x, потому что их веса не нулевые'
      - >-
        Scale и shift сохранят нормализованный h, но нулевые gate не пропустят
        изменения в x
      - Блок умножит исходный x на ноль
    correct: 2
    explanation: >-
      Внутри ветвей вычисления могут выполняться, но gate равен нулю. Поэтому их
      результаты не добавляются к остаточному пути, и выход блока совпадает с
      входным x.
---

Проверь, что ты различаешь внутренний сигнал и выход всего блока. При нулевой инициализации `scale` и `shift` равны нулю, поэтому в ветвь поступает обычный результат LayerNorm. Это ещё не тождественное преобразование. Безопасный старт появляется позже: нулевой `gate` не позволяет результатам attention и MLP изменить остаточный путь. Именно этот механизм из [шага 13](#step-13) реализован в функции из [шага 14](#step-14).

> 🎒 **На пальцах.** Представь турникет перед двумя новыми аттракционами. Внутри уже работают моторы, лампы и автоматика, но створка турникета закрыта. Посетители продолжают идти по старой дорожке без изменений. Обучение понемногу открывает створку: `gate` начинает пропускать вклад новых ветвей, а `scale` и `shift` настраивают, какой сигнал они получат.
