---
id: 034-proverka-samplera
type: check
title: 'Проверка: направление шага, dt и число вызовов модели'
source_anchor: '### Step 4: Euler sampler'
check:
  - question: >-
      Почему при положительном `dt` обратный шаг rectified flow обновляет
      состояние как `x = x - dt * v`?
    options:
      - >-
        `dt` задаёт модуль шага, а движение от шума к данным идёт в сторону
        уменьшения времени
      - 'Скорость модели всегда отрицательна, поэтому её необходимо вычитать'
      - Знак минус нужен только для компенсации ошибок округления метода Эйлера
    correct: 0
    explanation: >-
      В сэмплере `dt = 0,05` — положительный размер шага, но фактическое
      изменение времени равно `-dt`: интегрирование идёт от `t = 1` к `t = 0`.
      Поэтому вклад скорости вычитается.
  - question: >-
      Почему число вызовов модели в цикле Эйлера равно числу переходов, а не
      числу временных точек?
    options:
      - >-
        Скорость вычисляется только для первого перехода, а затем остаётся
        постоянной
      - >-
        Каждый переход требует одного вычисления скорости в текущем состоянии, а
        конечная точка уже получается последним обновлением
      - >-
        Модель вызывается в каждой временной точке, включая конечную, но
        последний результат отбрасывается
    correct: 1
    explanation: >-
      При 20 итерациях выполняются 20 переходов и 20 вызовов модели. Хотя
      временных точек 21, состояние при `t = 0` создаётся последним обновлением,
      поэтому отдельное вычисление скорости там не требуется.
---

Здесь легко спутать положительное число `dt` с направлением движения. В сэмплере из [шага 33](#step-33) `dt = 0,05` — лишь размер отрезка. Само время идёт от 1 к 0, поэтому реальное изменение времени отрицательное, а скорость вычитается, как разобрано в [шаге 32](#step-32).

Вторая ловушка — насчитать 21 точку времени и решить, что вызовов модели тоже 21. Но скорость нужна для каждого перехода между соседними точками: 20 переходов означают 20 вызовов. Конечная точка получается последним обновлением, отдельный вызов в ней не нужен.
