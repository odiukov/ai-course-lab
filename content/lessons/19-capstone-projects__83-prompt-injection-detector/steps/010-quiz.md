---
id: 010-quiz
type: quiz
title: Проверяем весь контракт детектора
source_anchor: '## Key Terms'
check:
  - question: >-
      Детектор пометил как атаку все входящие сообщения без исключения. Что
      будет с его метриками?
    options:
      - 'И precision, и recall будут равны 1.0 — он же поймал всё'
      - >-
        recall будет 1.0, а precision упадёт тем ниже, чем больше в потоке
        безобидных сообщений
      - >-
        precision будет 1.0, а recall упадёт, потому что часть атак он всё равно
        не разобрал
      - >-
        Обе метрики останутся около 0.5, потому что срабатываний ровно столько
        же, сколько сообщений
    correct: 1
    explanation: >-
      Ни одна атака не пропущена, значит FN равен нулю и recall равен 1.0. Зато
      каждое безобидное сообщение становится FP, и precision тает по мере роста
      доли безобидного трафика.
  - question: >-
      Из входа убрали шаг нормализации, а набор правил оставили тем же. Что
      изменится в первую очередь?
    options:
      - 'Вырастет число FP: правила начнут срабатывать на закодированном мусоре'
      - >-
        Вырастет число FN: спрятанные в base64 и hex токены до правил просто не
        дойдут
      - 'Ничего не изменится, правила ищут подстроки и regex в любом виде текста'
      - 'Упадёт recall на безобидных сообщениях, а на атаках метрики сохранятся'
    correct: 1
    explanation: >-
      Правила видят только тот текст, который им подали. Без раскрытия base64 и
      hex атака остаётся невидимой, детектор молчит, и это ровно FN — пропуск.
  - question: >-
      На одном входе сработали три правила: два из категории injection и одно из
      категории exfiltration. Почему нельзя просто вернуть список этих трёх
      срабатываний как результат?
    options:
      - 'Список слишком длинный, его тяжело логировать'
      - >-
        Потому что дальше по конвейеру ждут один Verdict с одной категорией и
        одной уверенностью, иначе непонятно, что считать за одно решение при
        подсчёте TP и FP
      - >-
        Потому что срабатывания из разных категорий противоречат друг другу и
        вход надо отклонить целиком
      - Потому что правила должны срабатывать не больше одного раза на вход
    correct: 1
    explanation: >-
      Матрица ошибок строится по решениям, а не по срабатываниям. Пока три
      сработавших правила не схлопнуты в один Verdict, непонятно, что именно
      записать в клетку TP или FP.
  - question: >-
      Детектор поймал атаку, но отнёс её к категории exfiltration вместо
      injection. Как это считается по разбивке из этого урока?
    options:
      - 'Как TP: атака же обнаружена, категория тут не важна'
      - Как FP для exfiltration и одновременно FN для injection
      - 'Как FN для exfiltration, потому что там срабатывания быть не должно'
      - 'Как TN, потому что для injection детектор ничего не сказал'
    correct: 1
    explanation: >-
      Счёт ведётся по категориям: в exfiltration появилось срабатывание,
      которого там быть не должно, — это FP; в injection настоящая атака
      осталась неотмеченной — это FN.
---

Урок закончился, и дальше по курсу от него останутся три вещи, которыми ты будешь пользоваться постоянно.

Первая — порядок в конвейере: нормализация из [шагов 3](#step-3)–[5](#step-5) идёт до правил из [шага 6](#step-6), иначе правила ищут в тексте, которого ещё нет. Вторая — одно решение на один вход, тот самый Verdict из [шага 7](#step-7): без него нечего кладать в клетки матрицы. Третья — precision и recall всегда парой.

Спотыкаются обычно в двух местах. Путают, что попало в знаменатель: FP — цена ложной тревоги, FN — цена пропуска. И забывают, что неверная категория — это сразу две записи, FP и FN, а не мягкий полу-успех.
