---
id: 017-check-cosine
type: check
title: 'Проверка: нулевой эмбеддинг из тишины и деление на ноль'
source_anchor: '### Step 2: cosine similarity + threshold'
check:
  - question: >-
      Ты подаёшь на проверку запись, где полная тишина: все 26 чисел эмбеддинга
      вышли нулями. Что случится в косинусе без защитной проверки?
    options:
      - 'Оценка получится 1.0, потому что нулевой вектор совпадает сам с собой'
      - >-
        Длина этого вектора равна нулю, знаменатель обнулится и программа упадёт
        на делении на ноль
      - 'Оценка получится 0.0 сама собой, ничего защищать не нужно'
      - 'Косинус вернёт отрицательное число, потому что направление не определено'
    correct: 1
    explanation: >-
      Косинус делит на произведение длин обоих векторов. Нулевой вектор имеет
      длину 0, произведение длин тоже 0 — и деление на ноль ломает всё до
      порога.
  - question: >-
      Защита сработала и вернула 0.0 вместо оценки. Почему именно 0.0, а не,
      скажем, 1.0?
    options:
      - '0.0 — середина шкалы, решение остаётся нейтральным'
      - '1.0 сломало бы косинус, а 0.0 — допустимое значение'
      - >-
        0.0 гарантированно ниже любого разумного порога, поэтому тишина не
        пройдёт проверку как «свой»
    correct: 2
    explanation: >-
      Порог сравнивает оценку с чем-то около 0.75. Ответ 0.0 всегда ниже —
      тишина честно отклоняется. Значение 1.0 пропустило бы кого угодно с пустой
      записью.
---

После этого куска ты должен уметь посмотреть на `cosine_score` из [шага 16](#step-16) и сказать, при каком входе он сломается — не «в теории», а на конкретной записи: микрофон был выключен, в кадре тишина, все 13 средних и 13 std вышли нулями. Длина такого вектора — ноль, а на ноль делить нельзя.

Спотыкаются обычно на двух вещах. Первая: кажется, что нулевой вектор «просто даст 0.0» сам по себе — нет, он даст падение, ноль надо вернуть руками. Вторая: возврат 0.0 путают с «не знаю». Это не нейтральный ответ, а сознательное «не пропускать»: ноль ниже любого порога.
