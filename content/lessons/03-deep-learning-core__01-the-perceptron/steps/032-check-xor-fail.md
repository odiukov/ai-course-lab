---
id: 032-check-xor-fail
type: check
title: 'Проверка: почему обучение здесь не поможет'
source_anchor: '### Step 3: Watch XOR fail'
check:
  - question: >-
      Один перцептрон обучают на полной таблице XOR значительно дольше прежнего.
      Почему это не приведёт к безошибочной классификации?
    options:
      - >-
        После долгого обучения ступенчатая функция перестанет выдавать бинарные
        значения
      - >-
        Ни одно положение линейной границы не разделяет все строки XOR по их
        меткам
      - Перцептрон обязательно переобучится и начнёт запоминать отдельные строки
    correct: 1
    explanation: >-
      Для XOR не существует линейной границы, правильно классифицирующей все
      четыре строки. Поэтому увеличение лимита с 1000 до 100000 эпох лишь
      продолжит обновления параметров, но не создаст достижимого решения.
  - question: >-
      Ошибка на обучающей выборке не уменьшается даже после длительного
      обучения. Что следует проверить до дальнейшей настройки процесса обучения?
    options:
      - Способна ли архитектура модели представить требуемую зависимость
      - Достаточно ли велико случайное начальное значение каждого веса
      - Можно ли многократно продублировать уже имеющиеся примеры
    correct: 0
    explanation: >-
      Если требуемая зависимость не входит в класс функций, доступных модели,
      настройка скорости обучения, добавление эпох и повторение тех же данных не
      дадут безошибочного решения. Для XOR именно так ограничен одиночный
      перцептрон.
---

Этот шаг нужен, чтобы ты отличал две очень похожие снаружи ситуации: «модель ещё не доучилась» и «модель не может доучиться никогда». Снаружи они выглядят одинаково — ошибка не падает, но лечатся противоположно. В первом случае помогают эпохи, данные, шаг обучения. Во втором ничего из этого не поможет: у одной прямой просто нет положения, при котором два нуля XOR оказываются с одной стороны, а две единицы — с другой.

Обычно спотыкаются на том, что «1000 эпох» звучит как «мало старались». Отсюда рефлекс: добавить эпох, подкрутить настройки, дать больше примеров. Для XOR все три хода бесполезны — четыре примера и есть вся задача целиком, а прямая всё равно одна.
