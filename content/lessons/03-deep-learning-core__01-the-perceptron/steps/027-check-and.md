---
id: 027-check-and
type: check
title: 'Проверка: прогоняем выученные веса по таблице AND'
source_anchor: '### Step 2: Train on logic gates'
check:
  - question: >-
      Почему при весах [0.2, 0.1] и смещении −0.2 вход [0, 1] относится к
      отрицательному классу?
    options:
      - Положительный вес второго входа полностью игнорируется
      - >-
        Нулевой первый вход убирает вклад первого веса, а вклад второго не
        компенсирует отрицательное смещение
      - >-
        Любой вход, содержащий ноль, автоматически относится к отрицательному
        классу
      - При проверке учитывается только больший из двух весов
    correct: 1
    explanation: >-
      Первый вес даёт нулевой вклад, поэтому предактивация равна 0.1 − 0.2 =
      −0.1. Она ниже порога, и перцептрон выдаёт 0.
  - question: 'Что позволяет увидеть проверка обученного перцептрона AND на входе [0, 0]?'
    options:
      - >-
        Оба взвешенных вклада исчезают, поэтому предактивация определяется
        только смещением
      - Смещение умножается на сумму входов и поэтому исчезает
      - При нулевых входах вместо смещения используются сами веса
      - 'Смещение действует во время обучения, но не во время предсказания'
    correct: 0
    explanation: >-
      При входе [0, 0] оба произведения входов на веса равны нулю, но смещение
      −0.2 сохраняется. Поэтому предактивация равна −0.2 и модель выдаёт 0.
---

Веса и смещение — это не абстракция, а рецепт, который можно проверить руками за минуту, без запуска кода. После этого шага ты должен уметь взять числа [0.2, 0.1] и −0.2, подставить любую строку таблицы AND и сказать, что получится: умножаешь каждый вход на свой вес, складываешь, прибавляешь смещение, сравниваешь результат с нулём.

Спотыкаются тут обычно на двух местах. Первое — про смещение забывают. Оно ни на что не умножается и прибавляется всегда, даже когда на входе одни нули, и именно оно тянет сумму вниз, пока обеих единиц нет. Второе — вход, равный нулю, обнуляет свой вес целиком: у веса 0.1 при входе 0 вклад ноль, а не 0.1.
