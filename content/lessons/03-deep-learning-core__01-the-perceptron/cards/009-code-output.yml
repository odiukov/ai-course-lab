- concept: число входов должно совпадать с числом весов
  question: Заполни условие, которое не позволит вычислить результат перцептрона для несовместимых списков.
  explanation: Каждому входу должен соответствовать ровно один вес. Без явной проверки zip молча отбросит элементы более длинного списка, и функция вычислит результат для неполного набора данных.
  kind: cloze
  template: |-
    def perceptron_output(inputs, weights, bias):
        if ___:
            raise ValueError("Для каждого входа нужен вес")
        total = sum(x * w for x, w in zip(inputs, weights)) + bias
        return step(total)
  answer: len(inputs) != len(weights)
  accept:
    - len(weights) != len(inputs)
  id: 009-code-output-1
  fingerprint: 5c040a80
