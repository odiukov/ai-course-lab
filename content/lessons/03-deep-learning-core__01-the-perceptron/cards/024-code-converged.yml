- concept: сходимость на обучающем наборе выражается универсальной проверкой совпадения всех предсказаний с метками
  question: Заполните пропуск так, чтобы функция возвращала истину только при верной классификации всего обучающего набора.
  explanation: '`all` возвращает истину, только если каждое полученное условие истинно. Поэтому функция подтвердит сходимость лишь тогда, когда предсказание совпадает с меткой для каждого обучающего примера.'
  kind: cloze
  template: |-
    def perceptron_converged(model, training_data):
        return ___(model.predict(inputs) == target
                   for inputs, target in training_data)
  answer: all
  accept:
    - builtins.all
  id: 024-code-converged-1
  fingerprint: 23e52163
- concept: универсальную проверку можно завершить сразу после обнаружения контрпримера
  question: При проверке обученного перцептрона обнаружено несовпадение предсказания с правильной меткой. Как должна поступить функция, определяющая сходимость?
  explanation: Одного неверного предсказания достаточно, чтобы опровергнуть сходимость на всём обучающем наборе. Проверка остальных примеров уже не может изменить результат, поэтому допустим немедленный возврат `False`.
  kind: choice
  options:
    - Проверить остальные примеры, чтобы обязательно подсчитать все ошибки
    - Сразу вернуть True, поскольку часть набора уже проверена
    - Сразу вернуть False, поскольку условие верности на всём наборе уже нарушено
  correct: 2
  id: 024-code-converged-2
  fingerprint: e566eb57
