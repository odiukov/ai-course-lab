---
id: 045-itog
type: quiz
title: 'Итоговая проверка: правило отказа, остаток, α и дерево'
source_anchor: '## Key Terms'
check:
  - question: >-
      Черновик предложил токен, но точная проверка его отклонила. Из какого
      распределения нужно получить замену?
    options:
      - 'Из исходного распределения q, исключив только отклонённый токен'
      - Из нормированной положительной части разности p и q
      - Непосредственно из полного распределения p
      - Из равномерного распределения по остальным токенам
    correct: 1
    explanation: >-
      После отказа используется остаточное распределение (p − q)+, нормированное
      до единичной суммы. Оно возвращает недостающую вероятность target-модели и
      тем самым сохраняет точное распределение результата.
  - question: >-
      При неизменной длине черновика новая draft-модель немного повысила долю
      принятия, но каждый её проход стал существенно дороже. Что может произойти
      с ускорением?
    options:
      - >-
        Оно обязательно вырастет, потому что более высокая доля принятия
        гарантирует выигрыш
      - >-
        Оно может снизиться, если дополнительная стоимость черновика превысит
        прирост числа принятых токенов
      - 'Оно не изменится, поскольку стоимость draft-модели не входит в расчёт'
      - Оно изменится строго пропорционально росту доли принятия
    correct: 1
    explanation: >-
      Ускорение определяется не одной долей принятия. В его стоимости
      учитываются все проходы draft-модели, поэтому небольшой прирост принятия
      может не окупить более дорогой черновик.
  - question: >-
      Target-модель проверила дерево кандидатов, и допустимыми оказались узлы из
      нескольких ветвей. Что можно добавить к сгенерированному тексту?
    options:
      - 'Все допустимые узлы, расположив их по глубине дерева'
      - >-
        Одну непрерывную ветвь от корня, выбранную предусмотренным алгоритмом
        декодирования
      - >-
        Ветвь с максимальной вероятностью draft-модели независимо от результата
        проверки
      - По одному узлу из каждой допустимой ветви
    correct: 1
    explanation: >-
      Результат генерации должен оставаться одной последовательностью. Поэтому
      наружу выводится только согласованный путь, в котором каждый следующий
      узел продолжает предыдущий; узлы разных ветвей объединять нельзя.
  - question: >-
      Как повлияет слабая draft-модель на распределение результатов при
      корректно реализованных правиле принятия и остаточной коррекции?
    options:
      - Результат сместится к распределению draft-модели
      - >-
        Распределение останется распределением target-модели, но ускорение
        обычно уменьшится
      - Искажение будет расти вместе с длиной черновика
      - Точность сохранится только после достижения высокой доли принятия
    correct: 1
    explanation: >-
      Точное правило принятия и остаточная коррекция сохраняют распределение
      target-модели при любом качестве черновика. Слабый черновик чаще получает
      отказы и потому ухудшает производительность, а не корректность выборки.
---

Дальше в курсе спекуляция будет встречаться уже не как отдельная тема, а как несколько строчек в конфиге: длина черновика, тип спекулятора, доля принятия из логов. Из урока тебе понадобятся ровно три вещи. Правило отказа вместе с остатком — именно они держат точность, и без них весь трюк превращается в «быстро, но не то». Доля принятия — единственная величина, которая связывает качество черновика со скоростью. И дерево — способ поднять эту долю, не удлиняя цепочку.

Спотыкаются обычно в двух местах. Первое: при отказе тянут токен обратно из черновика — и распределение тихо уезжает. Второе: радуются выросшей доле принятия и забывают, что черновик стоит денег на каждом из K шагов.
