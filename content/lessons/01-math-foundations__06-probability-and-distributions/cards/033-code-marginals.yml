- concept: при вычислении маргиналов в DataFrame суммируется исчезающая ось, а ось нужной переменной сохраняется
  question: Строки DataFrame `joint` соответствуют значениям X, а столбцы — значениям Y. Какой код правильно вычисляет оба маргинальных распределения?
  explanation: '`axis="columns"` складывает элементы поперёк столбцов и оставляет по одному итогу для каждой строки X. `axis="index"` складывает строки и оставляет по одному итогу для каждого столбца Y.'
  kind: choice
  options:
    - '`p_x = joint.sum(axis="columns")`; `p_y = joint.sum(axis="index")`'
    - '`p_x = joint.sum(axis="index")`; `p_y = joint.sum(axis="columns")`'
    - '`p_x = joint.sum(axis="columns")`; `p_y = joint.sum(axis="columns")`'
    - '`p_x = joint.sum()`; `p_y = joint.sum()`'
  correct: 0
  id: 033-code-marginals-1
  fingerprint: ceabc265
