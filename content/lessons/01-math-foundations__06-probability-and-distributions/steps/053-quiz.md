---
id: 053-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      Аномалия встречается в 3% запросов. Детектор распознаёт 90% аномалий, но
      также помечает 5% нормальных запросов. Какова примерно вероятность
      реальной аномалии, если детектор сработал?
    options:
      - Около 3%
      - Около 36%
      - Около 90%
      - Около 95%
    correct: 1
    explanation: >-
      На аномалии приходится 0.03 × 0.90 = 0.027 срабатываний, а на нормальные
      запросы — 0.97 × 0.05 = 0.0485. Искомая вероятность равна 0.027 / (0.027 +
      0.0485) ≈ 36%.
  - question: >-
      Целевое распределение для трёх классов равно [0.75, 0.25, 0]. Как
      кросс-энтропия обработает такой пример?
    options:
      - >-
        Учтёт только первый класс, потому что у него наибольшая целевая
        вероятность
      - >-
        Учтёт первый и второй классы с весами 0.75 и 0.25, а вклад третьего
        будет нулевым
      - Возьмёт среднее трёх предсказанных вероятностей
      - Сначала заменит целевое распределение на argmax-метку
    correct: 1
    explanation: >-
      В выражении −Σ p_true log(p_predicted) каждый класс взвешивается своей
      целевой вероятностью. Поэтому оба класса с ненулевой целевой массой влияют
      на loss.
  - question: >-
      Классификатор выдал значения [-2, 1, 4] до применения softmax. Как их
      следует интерпретировать?
    options:
      - 'Это логиты: сырые оценки, которые ещё не являются вероятностями'
      - 'Это готовые вероятности, потому что последнее значение самое большое'
      - 'Это логарифмы вероятностей, которые обязаны суммироваться в ноль'
      - Это метки трёх предсказанных классов
    correct: 0
    explanation: >-
      Выходы до softmax называются логитами. Они могут быть отрицательными и не
      обязаны иметь единичную сумму; корректное распределение вероятностей
      получается после softmax.
  - question: >-
      Модель задаёт вероятности токенов A, B и C как 0.5, 0.3 и 0.2. Что
      ожидается при многократном независимом сэмплировании?
    options:
      - 'Всегда будет выбран A, поскольку у него максимальная вероятность'
      - Каждый токен будет встречаться одинаково часто
      - 'Частоты A, B и C будут приближаться к 50%, 30% и 20%'
      - После первого выбора вероятности остальных токенов станут нулевыми
    correct: 2
    explanation: >-
      При сэмплировании исход выбирается случайно согласно распределению.
      Отдельная выборка непредсказуема, но частоты большого числа независимых
      выборок приближаются к заданным вероятностям.
---

Дальше в курсе вероятность перестанет быть отдельной темой и растворится в коде: выход модели — распределение, функция потерь — расстояние между распределениями, обучение — сдвиг предсказанного распределения к истинному. Без этих четырёх идей — условная вероятность, ожидание и дисперсия, логарифмы вместо произведений, softmax с cross-entropy — код градиентного спуска будет выглядеть набором магических строк.

Где обычно спотыкаются: путают плотность с вероятностью (значение PDF спокойно бывает больше 1, вероятность — нет); считают независимыми события, которые просто не связаны на вид, вместо проверки через произведение; забывают, что дисперсия градиентов падает как единица на размер батча, и удивляются шумному обучению; берут `log(softmax(x))` отдельными шагами и теряют точность.

Четыре вопроса ниже — по главным линиям урока. Если где-то промахнёшься, вернись к соответствующему шагу: дальше эти вещи будут встречаться каждый день.
