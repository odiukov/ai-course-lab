- concept: функция базового решения — чистый поиск по паре (режим, класс риска), анализ самого действия делает отдельная функция классификации
  question: Функция получает название режима разрешений и уже вычисленный класс риска, а возвращает базовое решение политики. Что такая функция делать НЕ должна?
  explanation: Классификация действия и выбор базового решения — два разных слоя. Если функция режима начнёт сама смотреть на путь файла или на текст команды, логика риска раздвоится и разойдётся с основным классификатором. Её вход — уже готовый класс риска, выход — ячейка таблицы.
  kind: choice
  options:
    - Разбирать текст команды и заново определять, к какому классу риска она относится
    - Возвращать разное решение для одного режима при разных классах риска
    - Явно сигнализировать об ошибке, если полученное название режима ей неизвестно
    - Возвращать решение, которое затем может быть уточнено явными правилами
  correct: 0
  id: 019-code-mode-decision-1
  fingerprint: f854ae87
- concept: 'полнота таблицы решений: покрыть нужно каждую комбинацию режима и класса риска, а не только встреченные на практике'
  question: В системе 4 режима разрешений и 6 классов риска. Сколько комбинаций должна покрывать таблица базовых решений, чтобы ни одна пара не осталась без исхода?
  explanation: 'Базовое решение определяется парой координат, поэтому число ячеек — произведение: 4 × 6 = 24. Непокрытая пара опасна тем, что её исход достаётся ветке по умолчанию, которую никто сознательно не выбирал.'
  kind: numeric
  answer: 24
  tolerance: 1
  id: 019-code-mode-decision-2
  fingerprint: afcad6ae
- concept: неизвестное имя режима обрабатывается как ошибка поиска, а не подменяется разрешающим исходом по умолчанию
  question: В настройках режим записан с опечаткой, и таблица базовых решений не содержит такого ключа. Какое поведение функции корректно?
  explanation: 'Молчаливая подстановка превращает опечатку в разрешение, которого никто не давал. Цена ошибки несимметрична: остановка с ошибкой обходится минутой человека, случайное разрешение — изменённым состоянием системы.'
  kind: choice
  options:
    - Вернуть самое мягкое решение, чтобы работа не прерывалась
    - Вернуть решение ближайшего по написанию режима
    - Явно сообщить об ошибке и не выдавать решения политики
    - Вернуть решение режима, который используется чаще всего
  correct: 2
  id: 019-code-mode-decision-3
  fingerprint: 07e67387
