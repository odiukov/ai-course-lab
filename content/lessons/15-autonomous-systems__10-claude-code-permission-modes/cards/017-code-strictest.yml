- concept: выбор строгого исхода — операция, независимая от порядка и от повторов во входном наборе
  question: Функция сводит набор решений к одному итоговому. Какое свойство обязано выполняться для наборов `["allow", "ask", "allow"]` и `["allow", "allow", "ask"]`?
  explanation: Операция берёт максимум по шкале строгости. Максимум коммутативен и идемпотентен, поэтому перестановки и дубликаты не меняют итог. Подсчёт голосов дал бы прямо противоположное свойство и позволил бы «заглушить» запрет числом разрешений.
  kind: choice
  options:
    - 'Оба дают `ask`: результат не зависит ни от порядка, ни от количества повторов'
    - 'Первый даёт `ask`, второй — `allow`: побеждает решение большинства'
    - 'Оба дают `allow`: одиночное `ask` не перевешивает два `allow`'
    - Результат зависит от того, каким решением набор заканчивается
  correct: 0
  id: 017-code-strictest-1
  fingerprint: 190da4d1
- concept: сведение набора решений к одному выполняется как максимум по числовому рангу строгости
  question: 'Решения упорядочены по строгости словарём RANK = {"allow": 0, "ask": 1, "deny": 2}. Дополни выражение, возвращающее итоговое решение из списка decisions.'
  explanation: 'Ключ переводит строку в ранг строгости, и `max` берёт самый строгий. Без ключа `max` сравнивал бы строки лексикографически: там `deny` < `allow` — ложное совпадение с политикой было бы случайным и сломалось бы при переименовании решений.'
  kind: cloze
  template: 'return max(decisions, key=lambda d: ___[d])'
  answer: RANK
  accept:
    - RANK
    - rank
    - RANK.get
    - STRICTNESS
  id: 017-code-strictest-2
  fingerprint: 0cac2305
- concept: лексикографическое сравнение строк не отражает порядок строгости решений
  question: Реализация возвращает `max(decisions)` без ключа. На наборе `["allow", "deny"]` она вернёт `deny` и тест пройдёт. Почему такое решение всё равно неверно?
  explanation: 'Алфавитный порядок allow < ask < deny случайно совпал с нужным. Политика не должна опираться на написание слов: переименуй `deny` в `block`, и функция начнёт возвращать разрешение вместо запрета. Явный ранг делает намерение частью кода.'
  kind: choice
  options:
    - 'Совпадение случайно: сравниваются строки по алфавиту, и на наборе `["ask", "deny"]` вернётся `deny`, а на `["allow", "ask"]` — `ask`, но порядок строгости не закодирован и рассыплется при любом переименовании'
    - '`max` вообще не работает со списками строк'
    - Реализация верна, тесты просто избыточны
    - 'Нужно `min`, а не `max`: алфавит идёт в обратную сторону'
  correct: 0
  id: 017-code-strictest-3
  fingerprint: 429db37f
