---
id: 060-itog
type: quiz
title: 'Итоговая проверка: loss, данные, активации и градиенты'
source_anchor: '## Key Terms'
check:
  - question: >-
      На одном batch из 8 примеров loss почти не снижается даже после 200 шагов.
      Какой вывод стоит проверить первым?
    options:
      - Нужно немедленно увеличить датасет
      - 'В модели, loss или цикле обучения есть фундаментальная ошибка'
      - Модель уже переобучилась
      - Validation-выборка слишком мала
    correct: 1
    explanation: >-
      Если модель не может запомнить несколько примеров, проблема возникает ещё
      до обобщения: нужно проверять вычисления и цикл обучения.
  - question: >-
      Loss почти не меняется. У последних слоёв градиенты заметные, но к первым
      слоям уменьшаются почти до нуля. Где искать причину?
    options:
      - В затухающем потоке градиента
      - В слишком большом learning rate
      - В утечке данных
      - В дубликатах validation-примеров
    correct: 0
    explanation: >-
      Обучающий сигнал доходит до последних слоёв, но исчезает по пути к первым
      — это характерная картина затухающих градиентов.
  - question: >-
      После ReLU слой выдаёт ноль для каждого примера batch, а его градиенты
      тоже равны нулю. Какой диагноз лучше всего согласуется с наблюдениями?
    options:
      - Взрыв градиентов
      - Переобучение
      - Мёртвый ReLU
      - Data leakage
    correct: 2
    explanation: >-
      Постоянные нули на выходе ReLU блокируют обратный сигнал, поэтому такой
      слой перестаёт обучаться.
  - question: >-
      Train и validation показывают подозрительно высокое качество, а проверка
      находит одинаковые примеры в обеих выборках. Что делать?
    options:
      - Уменьшить learning rate
      - Добавить gradient clipping
      - Удалить пересечения и заново разделить данные
      - Увеличить число слоёв
    correct: 2
    explanation: >-
      Пересечение выборок создаёт утечку данных и завышает validation-метрики;
      сначала нужно восстановить независимое разбиение.
---

Дальше тебе пригодится не отдельный приём, а порядок расследования: маленький воспроизводимый случай и кривая loss, затем данные и цикл обучения, после — активации и градиенты. Не выбирай самый заметный сигнал: падающий train loss не отменяет leakage, а clipping не объясняет источник взрыва. Чаще всего спотыкаются именно здесь — начинают лечить симптом до проверки причины. Смотри на отладку как на репетицию оркестра.

> 🎒 **На пальцах.** Если концерт звучит плохо, ты не обвиняешь сразу скрипки. Сначала сверяешь ноты — данные, затем слушаешь общий результат — loss, проверяешь, звучит ли каждая группа — активации, и доходит ли до неё замечание дирижёра — градиенты. Один показатель задаёт направление, а совпадение нескольких подсказок локализует сбой.
