---
id: 048-pytorch-bagi
type: check
title: 'Проверка: восстанавливаем безопасный цикл PyTorch'
source_anchor: '### Common PyTorch Bugs'
check:
  - question: Какой порядок действий восстанавливает безопасный шаг обучения?
    options:
      - >-
        Обнулить градиенты, вычислить предсказание и loss, выполнить backward,
        затем обновить параметры
      - >-
        Вычислить предсказание, обновить параметры, обнулить градиенты, затем
        выполнить backward
      - >-
        Вычислить loss, отсоединить его от графа, выполнить backward, затем
        обновить параметры
    correct: 0
    explanation: >-
      Буфер градиентов очищают до нового backward. Оптимизатор обновляет
      параметры только после того, как backward вычислил актуальные градиенты.
  - question: >-
      Проверка на validation закончилась, и начинается следующая обучающая
      итерация. Что нужно сделать?
    options:
      - Оставить модель в режиме eval и только снова разрешить построение графа
      - >-
        Выполнить backward для validation loss, а после этого переключить модель
        в train
      - >-
        Переключить модель в режим train и вычислять градиенты уже в обучающем
        цикле
    correct: 2
    explanation: >-
      После validation модель необходимо вернуть в режим train. Validation не
      должен выполнять backward или обновлять параметры.
---

Здесь ты собираешь проверки из [шагов 44](#step-44)–[47](#step-47) в один безопасный ритм. В обучении сначала обнуляешь градиенты, затем считаешь предсказание и loss, запускаешь backward и только потом обновляешь параметры. Перед validation переключаешь модель в eval и отключаешь построение графа; новый обучающий цикл снова начинается с train. Сверяй device, формы и dtype на границах, а сохраняемые тензоры отсоединяй. Главная ловушка — выполнить правильное действие не в том месте.

> 🎒 **На пальцах.** Представь мастерскую с одним верстаком. Перед новой деталью ты убираешь стружку, затем измеряешь заготовку, выполняешь работу и лишь потом двигаешь резец. При контроле качества станок переводят в режим проверки, где он ничего не перенастраивает. Безопасность создаёт порядок знакомых действий.
