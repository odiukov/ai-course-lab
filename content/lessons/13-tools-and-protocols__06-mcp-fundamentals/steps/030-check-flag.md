---
id: 030-check-flag
type: check
title: 'Проверка: инструмент не вызывается — куда смотреть'
source_anchor: '### Client capabilities vs tool call details'
check:
  - question: >-
      Клиент объявил в capabilities поддержку tools, сервер отдал список из
      восьми инструментов, но модель раз за разом отвечает текстом и ни один из
      них не вызывает. Куда смотреть в первую очередь?
    options:
      - >-
        В объявленные capabilities: раз вызова нет, значит рукопожатие прошло с
        ошибкой
      - >-
        В описания инструментов и их параметров: модель решает по ним, подходит
        ли инструмент под задачу
      - 'В поле id последнего запроса: без совпадения id вызов не состоится'
    correct: 1
    explanation: >-
      Список инструментов уже пришёл — значит контракт на уровне протокола
      работает. Выбор конкретного инструмента модель делает по его названию,
      описанию и схеме параметров.
  - question: Что на самом деле обещает флаг capabilities.tools?
    options:
      - 'Что клиент будет вызывать инструменты, которые прислал сервер'
      - >-
        Что сторона поддерживает работу с инструментами и уведомления об
        изменении их списка
      - >-
        Что сервер гарантирует успешное выполнение любого вызова из своего
        списка
    correct: 1
    explanation: >-
      Это контракт уровня спецификации — что сторона умеет и о чём согласна
      получать уведомления. Вызов конкретного инструмента к этому обещанию
      отношения не имеет.
---

Ты собрал сервер, подключил, рукопожатие из [шага 20](#step-20) прошло, `tools/list` вернул список — а модель твой инструмент не трогает. Первый порыв почти у всех одинаковый: перечитывать `capabilities` и искать там опечатку.

После [шага 29](#step-29) ты должен уметь отличать эти два слоя: флаг возможности — договорённость двух программ о том, что вообще допустимо, а вызов инструмента — решение модели прямо во время работы. Спотыкаются здесь на том, что оба слова похожи на «инструменты» и кажется, будто один управляет другим. Не управляет. Если список доехал — протокол сделал свою часть, и дальше всё решает то, как инструмент описан.
