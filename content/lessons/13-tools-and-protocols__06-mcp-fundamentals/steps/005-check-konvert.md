---
id: 005-check-konvert
type: check
title: 'Проверка: запрос, ответ или уведомление'
source_anchor: '### Wire format: JSON-RPC 2.0'
check:
  - question: >-
      Тебе пришло сообщение: внутри есть jsonrpc со значением 2.0, есть method
      со значением notifications/progress и есть params с процентом выполнения.
      Поля id нет. Что это и чего ждать дальше?
    options:
      - 'Запрос: поле method всегда означает запрос, значит нужно отправить ответ'
      - >-
        Уведомление: без id никто не ждёт ответа, обработал прогресс и живёшь
        дальше
      - 'Ответ на предыдущий запрос, просто у него потеряли id'
      - 'Ошибка формата: сообщение без id считается недействительным'
    correct: 1
    explanation: >-
      Наличие method не решает ничего само по себе — решает id. Нет id, значит
      отправитель ответа не ждёт: это уведомление.
  - question: >-
      Клиент отправил сообщение с method tools/call и id 7. Какая форма
      ответного сообщения состыкуется с ним?
    options:
      - 'jsonrpc 2.0, method tools/call и params с результатом'
      - 'jsonrpc 2.0, id 7 и либо result, либо error'
      - 'jsonrpc 2.0, id 7, method tools/call и result одновременно'
      - 'jsonrpc 2.0 и result, id можно опустить — запрос был всего один'
    correct: 1
    explanation: >-
      В ответе нет method: он опознаётся по id, который совпадает с id запроса,
      и несёт либо result, либо error.
---

Три конверта из [шага 4](#step-4) выглядят похоже, и на глаз их легко перепутать: во всех есть `jsonrpc: "2.0"`, в двух из трёх — `method` и `params`. Здесь стоит проверить одну вещь: можешь ли ты взять незнакомое сообщение и за секунду сказать, ждёт оно ответа или нет.

Спотыкаются обычно на двух местах. Первое — решают по `method`: раз есть имя метода, значит запрос. Но `method` есть и у уведомления; развилку задаёт только `id`. Второе — ищут `method` в ответе, а его там нет: ответ узнают по `id` и по тому, что внутри `result` или `error` — ровно одно из двух, не оба сразу.
