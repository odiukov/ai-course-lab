- concept: LLM формирует запрос на инструмент, а выполняет его рантайм
  question: LLM вернула событие tool_call с именем функции и JSON-аргументами. Какой компонент должен фактически обратиться к внешнему сервису?
  explanation: Tool call — это структурированная заявка, а не результат выполнения функции. Рантайм разбирает имя и аргументы, вызывает разрешённый инструмент и возвращает результат модели.
  kind: choice
  options:
    - Рантайм, который проверяет запрос и вызывает соответствующую функцию
    - Сама LLM во время генерации следующего токена
    - TTS, получивший JSON вместе с текстом
  correct: 0
  id: 020-tool-calling-1
  fingerprint: 39b706cb
- concept: служебные события tool_call не должны передаваться в TTS
  question: Заполни пропуск так, чтобы синтезатор речи получал только предназначенный пользователю текст.
  explanation: После обработки tool_call оператор continue сразу переводит цикл к следующему событию. Поэтому служебный JSON не попадает в TTS, а озвучиваются только события типа text.
  kind: cloze
  template: |-
    async for chunk in llm.stream(user_text, tools=tools):
        if chunk.type == "tool_call":
            result = dispatch(chunk.name, chunk.args)
            continue_streaming(result)
            ___
        if chunk.type == "text":
            await tts.stream(chunk.text)
  answer: continue
  accept:
    - continue
  id: 020-tool-calling-2
  fingerprint: c28a5015
