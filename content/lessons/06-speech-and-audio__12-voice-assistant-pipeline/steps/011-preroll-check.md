---
id: 011-preroll-check
type: check
title: 'Проверка: pre-roll и рассинхрон chunk_ms'
source_anchor: '### Step 2: VAD-gated turn capture'
check:
  - question: >-
      Микрофон отдаёт чанки по 10 мс, а в capture_turn ты оставил chunk_ms=20 и
      pre_roll_ms=300. Сколько звука реально окажется в pre-roll?
    options:
      - '300 мс, как задумано: длина считается в миллисекундах'
      - '150 мс: буфер держит 15 чанков, но каждый теперь по 10 мс'
      - '600 мс: чанков влезет вдвое больше'
      - 'Ничего: буфер вообще не наберётся'
    correct: 1
    explanation: >-
      Длина очереди считается как 300 // 20 = 15 чанков, а каждый чанк теперь 10
      мс, а не 20. Итого 150 мс — половина обещанного pre-roll, и начало первого
      слова снова обрежется.
  - question: >-
      Что произойдёт, если вынести pre.append(chunk) из ветки else и делать его
      на каждом кадре?
    options:
      - 'Pre-roll станет свежее, и первое слово сохранится лучше'
      - >-
        Триггерный кадр попадёт в запись дважды — и через list(pre), и через
        buf.append
      - 'Ничего не изменится: очередь всё равно ограничена по длине'
      - Счётчик silent начнёт расти раньше времени
    correct: 1
    explanation: >-
      В момент срабатывания VAD кадр сначала лёг бы в pre, а pre целиком
      копируется в buf — и тот же кадр добавляется ещё раз через buf.append. В
      записи появился бы продублированный 20-мс фрагмент.
---

Останови себя на секунду перед тем, как отвечать. Дальше в уроке ты будешь ловить галлюцинации распознавания, и ошибки вроде «в реплике повторился слог» или «первое слово всё равно съедено» легко списать на модель — хотя виноват буфер.

Так что здесь нужно уметь одно: пересчитывать длину `pre` от реального размера чанка микрофона и видеть, откуда в `buf` попадает триггерный кадр. Спотыкаются обычно на первом: `pre_roll_ms` выглядит как «столько миллисекунд и будет», хотя `maxlen` живёт в чанках, и стоит микрофону отдавать не те 20 мс из [шага 5](#step-5) — pre-roll молча схлопывается. Второе место — ветка `else` из [шага 8](#step-8): она не украшение, а защита от двойного учёта.
