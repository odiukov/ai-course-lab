---
id: 019-stream-check
type: check
title: 'Проверка: что даёт ранний старт синтеза'
source_anchor: '### Step 3: streaming STT → LLM → TTS'
check:
  - question: >-
      LLM выдаёт 50 токенов в секунду, ответ целиком — 100 токенов. Сравни два
      варианта: ждать весь ответ и только потом запускать синтез, или запускать
      после 20 токенов. Через сколько человек услышит первый звук?
    options:
      - >-
        В обоих случаях примерно одинаково: синтез всё равно упирается в
        скорость LLM
      - '2 секунды в первом варианте и 0,4 секунды во втором'
      - '0,4 секунды в первом варианте и 2 секунды во втором'
      - '2 секунды в первом варианте и 2,4 секунды во втором'
    correct: 1
    explanation: >-
      Весь ответ копится 100 / 50 = 2 секунды, а первые 20 токенов — 20 / 50 =
      0,4 секунды. Ранний старт срезает ожидание первого звука в пять раз.
  - question: >-
      Ты поднял порог старта синтеза с 20 токенов до 50, скорость LLM та же — 50
      токенов в секунду. Что изменится для человека у микрофона?
    options:
      - 'Первый звук придёт через 1 секунду вместо 0,4 — ждать дольше'
      - 'Первый звук придёт быстрее, потому что TTS получит больше текста сразу'
      - 'Ничего не изменится: порог влияет только на качество голоса'
      - Весь ответ прозвучит в два с половиной раза дольше
    correct: 0
    explanation: >-
      50 / 50 = 1 секунда до старта вместо 0,4. Порог — это ровно то, сколько ты
      ждёшь перед первым звуком; ответ целиком от него не растягивается.
---

После [шага 17](#step-17) и [шага 18](#step-18) ты должен уметь одно конкретное действие: взять скорость генерации и порог старта, поделить одно на другое и сказать, через сколько человек услышит первый звук. И понять, какую часть бюджета из [шага 2](#step-2) это освобождает.

Спотыкаются обычно на двух вещах. Первая — путают время до первого звука с временем всего ответа: ранний старт не заставляет LLM думать быстрее, он только убирает паузу перед началом. Вторая — считают, что порог можно двигать бесплатно. Каждый лишний токен в пороге — это лишние миллисекунды тишины, и ты их платишь из того же бюджета.
