---
id: 010-dense-check
type: check
title: 'Проверка: нормализация, косинус и порядок dense-выдачи'
source_anchor: '### Step 1: baseline — dense embeddings with Sentence-BERT'
check:
  - question: >-
      Ты нормализовал векторы корпуса и запроса. Что теперь означает их
      скалярное произведение?
    options:
      - Оно равно косинусной близости
      - Оно равно длине вектора запроса
      - Оно показывает число слов в тексте
    correct: 0
    explanation: >-
      После нормализации нормы обоих векторов равны единице, поэтому знаменатель
      в формуле косинусной близости исчезает и остаётся скалярное произведение.
  - question: >-
      Два фрагмента получили одинаковую оценку. Какое ранжирование соответствует
      стабильной сортировке?
    options:
      - 'Сначала идёт фрагмент, который раньше стоял в корпусе'
      - Сначала идёт более длинный фрагмент
      - Порядок фрагментов выбирается случайно
    correct: 0
    explanation: >-
      Стабильная сортировка не меняет взаимный порядок элементов с одинаковыми
      оценками, поэтому раньше останется фрагмент, который раньше находился в
      корпусе.
---

Теперь проверь, состыковались ли три детали одного механизма. После нормализации скалярное произведение можно читать как косинусную близость — это связь из [шага 6](#step-6). Функция ранжирования из [шага 9](#step-9) получает по одной оценке на каждый фрагмент, ставит большую выше и при равенстве сохраняет исходный порядок. Частая ошибка — отсортировать оценки по возрастанию или нормализовать только одну сторону.

> 🎒 **На пальцах.** Представь забег, где результат записывают как близость к финишу: чем больше число, тем выше место. Перед стартом всем выдают одинаковую обувь — это нормализация условий. Если два бегуна показали один результат, судья оставляет впереди того, кто раньше был записан в стартовом списке.
