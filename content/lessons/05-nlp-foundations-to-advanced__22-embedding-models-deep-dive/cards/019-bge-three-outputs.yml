- concept: совместный инференс переиспользует токенизацию и вычисления общего энкодера
  question: Почему совместное получение dense-, sparse- и multi-vector-выходов обычно выгоднее отдельного запуска собственной модели для каждого типа?
  explanation: Многофункциональная модель переиспользует дорогую обработку текста, а затем формирует dense_vecs, lexical_weights и colbert_vecs отдельными выходными головами.
  kind: choice
  options:
    - Общие этапы токенизации и работы энкодера выполняются совместно для всех выходов
    - Все выходы хранятся в одинаковом формате и используют одну функцию оценки
    - Sparse-веса получаются простым удалением нулевых координат из dense-вектора
    - Multi-vector-выход автоматически заменяет dense- и sparse-представления при поиске
  correct: 0
  id: 019-bge-three-outputs-1
  fingerprint: c6b02c39
- concept: флаг return_colbert_vecs включает возврат токенных векторов для multi-vector-поиска
  question: Заполни значение флага, чтобы encode вернул токенные векторы для последующего MaxSim-сопоставления.
  explanation: При включённом return_colbert_vecs результат содержит colbert_vecs — набор токенных векторов для каждого текста.
  kind: cloze
  template: output = model.encode(corpus, return_colbert_vecs=___)
  answer: 'True'
  accept:
    - 'true'
  id: 019-bge-three-outputs-2
  fingerprint: 5dad7704
- concept: общий проход энкодера не делает dense-, sparse- и multi-vector-выходы взаимозаменяемыми
  question: Объясни своими словами, почему выходы многофункциональной embedding-модели нельзя оценивать и хранить одинаково, даже если они вычислены совместно.
  explanation: 'Общий энкодер экономит вычисления, но не объединяет представления: dense_vecs, lexical_weights и colbert_vecs сохраняют собственные форматы хранения и функции оценки.'
  kind: open
  reference: 'Совместно используется внутренняя обработка текста, но выходы имеют разные структуры и правила сопоставления: dense — фиксированный вектор, sparse — разреженные веса координат, multi-vector — набор токенных векторов для MaxSim.'
  id: 019-bge-three-outputs-3
  fingerprint: be8b8fca
