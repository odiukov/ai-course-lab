- concept: 'зависимость между циклом и моделью односторонняя: цикл обращается к модели, а компоненты цикла не упоминаются в реализации провайдера, поэтому изменения по обе стороны не распространяются'
  question: Агентный цикл держит ссылку на объект модели и зовёт у него метод генерации; сам объект модели ни к реестру инструментов, ни к буферу сообщений, ни к предикату остановки не обращается. Какое следствие этого устройства верно?
  explanation: 'Направление зависимости одностороннее: знает о модели только точка вызова. Реестр работает с именами инструментов, буфер — со строками, предикат остановки — с последней репликой и счётчиком ходов; ни одному из них не нужен объект модели. Поэтому набор инструментов и реализация провайдера меняются независимо друг от друга.'
  kind: choice
  options:
    - Расширение набора инструментов с трёх до девяти не требует правок в реализации провайдера, а смена провайдера не требует правок в реестре
    - Каждый новый инструмент нужно объявить и в реестре, и внутри класса провайдера, иначе модель его не увидит
    - Предикат остановки придётся переписывать под каждого нового провайдера, потому что он опрашивает объект модели
    - Буфер сообщений обязан хранить ссылку на модель, чтобы уметь дозапрашивать продолжение реплики
  correct: 0
  id: 029-prodovaya-forma-1
  fingerprint: 78ede736
- concept: подстановка живого провайдера выполняется заменой объекта в точке конструирования, а не правкой тела цикла
  question: Учебная заглушка модели должна уступить место живому провайдеру. Расположи шаги замены в правильном порядке.
  explanation: Тело цикла не трогается ни на одном шаге. Сначала появляется объект с тем же методом, потом внутри него — сеть, потом правится единственная строка конструирования, и только затем прогон подтверждает, что цикл не заметил подмены. Если бы для замены пришлось лезть в проверку остановки или в форматирование наблюдения, это значило бы, что цикл и модель срослись.
  kind: order
  items:
    - Объявить новый класс с тем же методом генерации, что вызывает цикл
    - Спрятать внутрь этого метода HTTP-запрос к провайдеру и разбор ответа в текст того же формата
    - Заменить объект, передаваемый циклу при создании, на экземпляр нового класса
    - Прогнать ту же задачу и сверить структуру трассы с прежней
  id: 029-prodovaya-forma-2
  fingerprint: 8887c2cb
