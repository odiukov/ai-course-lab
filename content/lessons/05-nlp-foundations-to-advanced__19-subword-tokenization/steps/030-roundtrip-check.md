---
id: 030-roundtrip-check
type: check
title: Как проверить равенство decode(encode(text)) исходному тексту?
source_anchor: '### Step 2: encode with the learned merges'
check:
  - question: >-
      Как надёжнее всего проверить, что токенизатор восстанавливает текст без
      потерь?
    options:
      - Точно сравнить исходную строку с результатом decode(encode(text))
      - 'Проверить, что encode вернул непустой список токенов'
      - 'Убедиться, что число токенов не превышает число символов'
    correct: 0
    explanation: >-
      Только точное равенство строк обнаружит исчезнувшие пробелы, знаки
      препинания и любые другие посимвольные изменения.
  - question: >-
      После encode и decode один из соседних пробелов исчез. Как
      интерпретировать результат?
    options:
      - 'Обратимость нарушена: восстановленная строка не равна исходной'
      - 'Обратимость сохранена, потому что набор слов не изменился'
      - Проверку нельзя выполнить без знания порядка BPE-слияний
    correct: 0
    explanation: >-
      Обратимость требует восстановления каждого символа на прежней позиции.
      Исчезновение даже одного пробела означает, что декодирована другая строка.
---

Возьми несколько неудобных строк: два пробела подряд, пунктуацию без пробела, пустую строку, кириллицу и эмодзи. Пропусти каждую через `encode` из [шага 26](#step-26), затем через `decode` из [шага 29](#step-29) и сравни результат с оригиналом именно посимвольно.

Ловушка — принять «выглядит похоже» за равенство. Строки `«да  нет»` и `«да нет»` читаются почти одинаково, но это разные данные. Если проверка падает, ищи первый различающийся символ: так станет понятно, потерялась ли граница слова, пробел или знак. Настоящая обратимость означает не сохранение смысла, а восстановление каждого символа на прежнем месте.
