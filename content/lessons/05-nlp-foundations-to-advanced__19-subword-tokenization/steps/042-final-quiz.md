---
id: 042-final-quiz
type: quiz
title: 'Итоговая проверка: BPE, Unigram, WordPiece, библиотеки и метрики токенизатора'
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты начинаешь с большого набора фрагментов и по очереди удаляешь те, без
      которых правдоподобие корпуса ухудшается меньше всего. Какой подход ты
      используешь?
    options:
      - BPE
      - Unigram
      - WordPiece
      - Byte-level BPE
    correct: 1
    explanation: >-
      Unigram начинает с избытка кандидатов и постепенно сокращает словарь,
      оценивая влияние удаления на правдоподобие корпуса.
  - question: >-
      Слово встречается в корпусе 40 раз. Сколько раз каждая его соседняя пара
      должна повлиять на подсчёт при обучении BPE?
    options:
      - 'Один раз, потому что слово хранится одной записью'
      - 'Сорок раз, потому что вклад умножается на частоту слова'
      - 'Столько раз, сколько символов в слове'
      - 'Ни разу, пока слово не станет отдельным токеном'
    correct: 1
    explanation: >-
      BPE обучается на частотах корпуса: одна запись слова представляет все 40
      появлений, поэтому каждая соседняя пара получает вклад 40.
  - question: >-
      Тебе нужно заранее посчитать токены запроса для уже выбранной модели
      OpenAI. Какой путь даст релевантный результат?
    options:
      - Обучить новый Unigram в SentencePiece на похожих запросах
      - Использовать родную кодировку модели через tiktoken
      - Обучить byte-level BPE со словарём такого же размера
      - Посчитать слова после разделения по пробелам
    correct: 1
    explanation: >-
      Число токенов определяется конкретным словарём и правилами кодировки
      модели, поэтому похожий или заново обученный токенизатор даст другие ID и
      длину.
  - question: >-
      У токенизатора нет OOV, но после декодирования иногда меняются пробелы.
      Можно ли считать его готовым?
    options:
      - 'Да, отсутствие OOV гарантирует сохранение текста'
      - 'Да, если средняя последовательность стала короче'
      - 'Нет, сначала round-trip должен без потерь восстанавливать исходный текст'
      - 'Нет, любой хороший токенизатор обязан использовать WordPiece'
    correct: 2
    explanation: >-
      Отсутствие OOV не гарантирует обратимость. Проверка round-trip выявляет
      потерю пробелов и другие изменения, которые средняя длина
      последовательности не показывает.
---

Дальше тебе пригодится не название алгоритма, а умение связать задачу с правильным инструментом и проверкой. Различай правила изменения словаря из [шага 10](#step-10), обучение и применение библиотеки из [шага 11](#step-11), а также скорость, OOV и round-trip из [шага 35](#step-35). Частая ловушка — выбрать токенизатор по одной красивой метрике: короткая последовательность бесполезна, если декодирование портит текст или на инференсе изменились правила.

> 🎒 **На пальцах.** Представь техосмотр велосипеда перед длинной поездкой. Лёгкие колёса похожи на короткую токенизацию, подходящие детали — на покрытие словаря, а возвращение руля точно в исходное положение — на round-trip. Один удачный показатель не отменяет остальные: в путь выпускают только велосипед, у которого вся система работает вместе.
