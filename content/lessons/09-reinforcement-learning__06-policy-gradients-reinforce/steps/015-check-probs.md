---
id: 015-check-probs
type: check
title: 'Проверяем форму выхода, сумму вероятностей и большие логиты'
source_anchor: '### Step 1: softmax policy network'
check:
  - question: >-
      У политики 3 действия, а у состояния 2 признака. Какой выход должна
      вернуть policy_probs?
    options:
      - '2 вероятности, потому что у состояния 2 признака'
      - '3 вероятности, сумма которых близка к единице'
      - '6 вероятностей, по одной на каждый вес'
    correct: 1
    explanation: >-
      На каждое действие приходится одна вероятность. Признаки участвуют в
      вычислении логитов, но не задают длину выхода; из-за округления сумма
      может незначительно отличаться от единицы.
  - question: 'Что вернёт устойчивый softmax при логитах 800, 798 и 795?'
    options:
      - 'Конечные вероятности, причём самая большая — у первого действия'
      - 'Все три вероятности станут одинаковыми, потому что логиты большие'
      - 'Первая вероятность станет бесконечностью, остальные — нулями'
    correct: 0
    explanation: >-
      После вычитания максимума показатели экспонент равны 0, −2 и −5,
      переполнения нет. Вычитание одной константы из всех логитов не меняет
      распределение, поэтому наибольшему логиту по-прежнему соответствует
      наибольшая вероятность.
---

Проверь функцию из [шага 14](#step-14) по трём признакам исправности: число вероятностей совпадает с числом действий, их сумма близка к единице, а большие логиты не создают бесконечности. Здесь легко перепутать форму весов с формой результата: сколько бы признаков ни описывало состояние, на выходе нужна одна вероятность на действие.

Ещё одна ловушка — требовать от суммы ровно `1.0`: из-за округления она может отличаться на крошечную величину. Сравнивай с небольшим допуском. Устойчивый softmax из [шага 12](#step-12) должен спокойно обработать даже логиты около 1000.
