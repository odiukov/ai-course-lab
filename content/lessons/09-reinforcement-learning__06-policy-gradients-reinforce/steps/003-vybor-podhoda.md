---
id: 003-vybor-podhoda
type: check
title: Проверяем выбор между Q-функцией и стохастической политикой
source_anchor: '## The Problem'
check:
  - question: >-
      Для управления роботом нужно выбирать действие из непрерывного
      пространства и сохранять случайность поведения. Какой способ подходит
      лучше?
    options:
      - 'Вычислять Q(s, a) и каждый раз брать действие с максимальным значением'
      - >-
        Получать параметры распределения действий из π_θ(a | s) и сэмплировать
        действие
      - Дискретизировать возврат G и брать действие с наибольшим возвратом
    correct: 1
    explanation: >-
      argmax по непрерывному вектору действий требует отдельной задачи
      оптимизации на каждом шаге и по построению детерминирован. Политика сразу
      выдаёт распределение: действие берётся одним сэмплом, случайность
      сохраняется.
  - question: >-
      Агент несколько раз попадает в одно и то же состояние и выбирает разные
      допустимые действия. Что это говорит о его способе принятия решений?
    options:
      - Решение принимается сэмплированием из распределения π_θ(a | s)
      - 'Значения Q(s, a) обязаны меняться при каждом обращении'
      - Состояние не используется при выборе действия
    correct: 0
    explanation: >-
      Стохастическая политика сопоставляет состоянию распределение, а не одно
      действие, поэтому повторные сэмплы различаются. Жадный выбор по
      фиксированной Q-функции в том же состоянии дал бы один и тот же ответ.
---

Здесь важно удержать одно различие. В [шаге 1](#step-1) выбор через максимум Q оказался неудобен для непрерывных действий и всегда давал один результат. В [шаге 2](#step-2) ты вместо оценок действий настроил их распределение. Теперь проверь себя на конкретных последствиях: где пришлось бы искать максимум среди бесконечного числа вариантов, а где действие можно получить сразу; где повторяется один выбор, а где сохраняется полезная случайность.

> 🎒 **На пальцах.** Представь распылитель воды с ручкой, которую можно повернуть на любой угол. Список оценок потребовал бы проверить бесконечно много положений ручки. Стохастическая политика похожа на настройку диапазонов: чаще поворачивать около удачного угла, но иногда пробовать соседние. Ты сразу получаешь действие и не запираешь себя в одном положении.
