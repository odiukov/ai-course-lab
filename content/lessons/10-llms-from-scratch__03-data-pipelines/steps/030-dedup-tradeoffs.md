---
id: 030-dedup-tradeoffs
type: check
title: 'Разбираем ложные кандидаты, пропущенные дубли и выбор порога 0.8'
source_anchor: '### Step 2: MinHash Deduplication'
check:
  - question: >-
      LSH пометил пару документов как кандидатов, но их точный Jaccard равен
      0.63. Что произойдёт при пороге 0.8?
    options:
      - 'Второй документ удалят, потому что совпадения полос уже достаточно'
      - >-
        Пару отбросят: кандидатство запускает проверку, но не доказывает
        дублирование
      - Порог автоматически снизится до 0.63
    correct: 1
    explanation: >-
      LSH лишь сокращает круг проверяемых пар. Документ удаляется только после
      точной проверки и результата не ниже 0.8.
  - question: >-
      Два документа имеют точный Jaccard 0.87, но не совпали ни в одной полосе
      LSH. Поможет ли снизить порог проверки до 0.7?
    options:
      - 'Да, пара сразу станет дублем'
      - 'Да, но только при увеличении числа хешей'
      - 'Нет, пара не стала кандидатом и до проверки порога не дошла'
    correct: 2
    explanation: >-
      Порог применяется лишь к кандидатам, найденным LSH. Пропущенную пару нужно
      возвращать изменением настройки поиска кандидатов, а не порога проверки.
---

Запусти дедупликацию из [шага 29](#step-29) и посмотри на две странные пары. Первая попала в кандидаты из-за совпавшей полосы, но точная проверка из [шага 17](#step-17) дала 0.63. Это ложный кандидат: при пороге 0.8 документы останутся. Вторая пара действительно набрала 0.87, однако LSH её не предложил. Это пропущенный дубль, и снижение порога не поможет — проверка вообще не запускалась.

Порог 0.8 отвечает только на вопрос: «достаточно ли похож уже найденный кандидат?» Выше — меньше удалённых разных документов, но больше сохранённых дублей; ниже — наоборот.

> 🎒 **На пальцах.** Представь металлоискатель и ручной досмотр в аэропорту. Сигнал рамки лишь отправляет чемодан на проверку: ложная тревога ничего не конфискует. Но если рамка промолчала, опасный предмет до досмотра не дойдёт. Строгость досмотра здесь не исправит промах рамки.
