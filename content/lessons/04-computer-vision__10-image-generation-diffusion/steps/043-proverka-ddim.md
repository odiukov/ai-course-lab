---
id: 043-proverka-ddim
type: check
title: 'Проверка: шаги, eta, seed и последний переход DDIM'
source_anchor: '### Step 6: DDIM sampler (deterministic, ~20x faster)'
check:
  - question: >-
      Ты дважды запускаешь DDIM с eta = 0, одинаковым seed и неизменными
      настройками. Что ожидаешь получить?
    options:
      - Одну и ту же картинку
      - Разные картинки из-за случайности каждого перехода
      - 'Одинаковую композицию, но случайные мелкие детали'
    correct: 0
    explanation: >-
      При eta = 0 переходы не добавляют свежий шум, а одинаковый seed создаёт
      одинаковый начальный шум. Поэтому весь маршрут повторяется.
  - question: >-
      Зачем после последнего выбранного индекса 0 в маршруте DDIM стоит
      фиктивный индекс -1?
    options:
      - Чтобы выполнить ещё один обычный шаг расписания
      - >-
        Чтобы обозначить переход к alpha bar = 1 и получить чистое предсказание
        картинки
      - Чтобы заново сгенерировать начальный шум
    correct: 1
    explanation: >-
      Индекс -1 служит меткой чистого состояния: для него подставляется alpha
      bar = 1. Случайная и шумовая части исчезают, поэтому результатом
      становится предсказанное x_0.
---

Проверь, различаешь ли ты три независимые ручки. Число шагов задаёт количество остановок DDIM и проходов модели; `eta` управляет свежей случайностью внутри переходов; `seed` фиксирует исходный шум. Поэтому одинаковый `seed` особенно заметен при `eta = 0`: при неизменных настройках маршрут повторяется целиком.

Главная ловушка — считать индекс `0` уже полностью чистым состоянием. В `alphas_cumprod[0]` ещё остаётся шум первого шага. Как разобрано в [шаге 41](#step-41), завершающая метка `-1` подставляет `alpha bar = 1`, обнуляет оставшиеся шумовые части и отдаёт именно предсказанное чистое изображение.
