- concept: модель получает отдельное временное условие для каждого элемента батча, даже если весь батч обрабатывается на одном шаге
  question: Заполни пропуск так, чтобы тензор временных шагов подходил для передачи в модель.
  explanation: '`x_t.shape[:1]` задаёт по одному значению времени на каждый элемент батча. Индексы временных шагов передаются как целые числа типа `torch.long`, а размещение на `x_t.device` предотвращает конфликт устройств.'
  kind: cloze
  template: t_batch = torch.full(x_t.shape[:1], t, dtype=___, device=x_t.device)
  answer: torch.long
  accept:
    - torch.int64
  id: 032-ddpm-step-1
  fingerprint: 3680eddc
- concept: '`model.eval()` и `torch.no_grad()` решают разные задачи при диффузионном сэмплировании'
  question: Зачем при сэмплировании одновременно вызывать `model.eval()` и отключать вычисление градиентов?
  explanation: '`model.eval()` управляет режимом модулей, но само по себе не отключает autograd. `torch.no_grad()` уменьшает расход памяти и вычислений, однако не переключает Dropout или BatchNorm в режим инференса.'
  kind: choice
  options:
    - '`model.eval()` очищает накопленные градиенты, а отключение градиентов переводит слои в режим инференса'
    - '`model.eval()` переносит параметры на нужное устройство, а отключение градиентов выбирает тип данных тензоров'
    - '`model.eval()` меняет поведение слоёв вроде Dropout и BatchNorm, а отключение градиентов не даёт autograd строить ненужный граф'
    - Обе операции полностью эквивалентны, поэтому достаточно любой из них
  correct: 2
  id: 032-ddpm-step-2
  fingerprint: 20e62323
