- concept: свежий гауссов шум выбирает конкретный результат вокруг предсказанного среднего обратного DDPM-перехода
  question: Для одного и того же текущего состояния и неизменного предсказания сети обратный DDPM-переход запускают несколько раз, каждый раз с новым независимым шумом z. Что произойдёт?
  explanation: При фиксированных текущем состоянии и выводе сети центр перехода не меняется. Независимая добавка σ_t z выбирает разные реализации рядом с этим центром, поскольку гауссов шум имеет нулевое среднее.
  kind: choice
  options:
    - Предсказанное среднее останется тем же, а полученные предыдущие состояния будут различаться вокруг него
    - Предсказанное среднее будет случайно меняться, но предыдущее состояние останется тем же
    - Все запуски дадут одинаковое предыдущее состояние, поскольку вход сети не меняется
    - Новый шум полностью заменит направление, заданное предсказанным средним
  correct: 0
  id: 031-formula-ddpm-shaga-1
  fingerprint: 7d7c0719
- concept: на завершающем переходе DDPM-сэмплера свежая случайная добавка отключается
  question: На каком переходе стандартный DDPM-сэмплер подставляет вместо свежего шума z нулевой тензор?
  explanation: На промежуточных переходах свежий шум поддерживает стохастичность траектории. Когда сэмплер получает итоговый чистый образец, дополнительный шум уже не добавляют и используют только вычисленное среднее.
  kind: choice
  options:
    - При переходе к итоговому чистому образцу
    - На каждом обратном переходе
    - Только на первом обратном переходе от начального шума
    - На случайно выбранном переходе каждого запуска
  correct: 0
  id: 031-formula-ddpm-shaga-2
  fingerprint: 17fa5f5f
