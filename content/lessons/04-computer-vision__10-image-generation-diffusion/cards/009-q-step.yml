- concept: torch.randn_like создаёт гауссов шум с формой, типом данных и устройством исходного тензора
  question: Заполни пропуск так, чтобы PyTorch создал стандартный гауссов шум, совместимый с тензором изображения по форме, типу данных и устройству.
  explanation: torch.randn_like(x_prev) создаёт новый случайный тензор стандартного нормального шума и наследует форму, dtype и device от x_prev. Это позволяет сразу выполнять поэлементные операции без ручного переноса или преобразования.
  kind: cloze
  template: eps = torch.___(x_prev)
  answer: randn_like
  accept:
    - randn_like
  id: 009-q-step-1
  fingerprint: 2b0d98c3
- concept: каждый прямой диффузионный переход должен сэмплировать независимую реализацию шума
  question: Разработчик один раз создаёт тензор гауссова шума вне q_step и использует его при каждом вызове функции. В чём главная ошибка?
  explanation: Переход q(x_t | x_{t-1}) задаёт распределение, поэтому каждый вызов должен брать свежую независимую реализацию гауссова шума. Повторное использование одного тензора делает результат детерминированным при фиксированных входах.
  kind: choice
  options:
    - При одинаковых x_prev и beta_t функция будет повторять одну и ту же реализацию перехода вместо нового случайного сэмпла
    - Дисперсия сохранённого шума будет автоматически увеличиваться после каждого вызова
    - PyTorch перестанет вычислять градиенты для x_prev
    - Форма результата станет меньше формы входного изображения
  correct: 0
  id: 009-q-step-2
  fingerprint: bc2e1f9a
