- concept: линейное расписание beta создаётся как одномерный тензор равномерных значений с включёнными границами
  question: Заполни тело функции, чтобы получить линейное расписание из T значений от beta_start до beta_end включительно.
  explanation: torch.linspace создаёт одномерный тензор из T равномерно расположенных значений и включает обе границы. В уроке значения по умолчанию дают расписание из 1000 элементов от 1e-4 до 2e-2.
  kind: cloze
  template: |-
    def linear_beta_schedule(T, beta_start, beta_end):
        return ___
  answer: torch.linspace(beta_start, beta_end, T)
  accept:
    - torch.linspace(start=beta_start, end=beta_end, steps=T)
    - torch.linspace(beta_start, beta_end, steps=T)
  id: 007-linear-beta-schedule-1
  fingerprint: 38f450de
