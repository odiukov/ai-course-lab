- concept: пара синуса и косинуса имеет постоянную норму при любом временном шаге
  question: Для одной частоты временной эмбеддинг хранит пару (cos(a), sin(a)). Чему равна сумма квадратов координат этой пары при любом a?
  explanation: По основному тригонометрическому тождеству cos²(a) + sin²(a) = 1. Поэтому изменение временного шага поворачивает каждую пару координат, но не меняет её норму.
  kind: numeric
  answer: 1
  tolerance: 0.000001
  id: 026-timestep-embedding-1
  fingerprint: 1ece4012
- concept: нулевой столбец дополняет синусоидальный эмбеддинг до нечётной требуемой ширины
  question: После объединения косинусной и синусной половин эмбеддинг для нечётной dim оказался на один столбец уже требуемой ширины. Заполни код дополнения.
  explanation: Синусоидальные координаты создаются парами, поэтому две половины дают чётную ширину. Для нечётной dim справа добавляют нулевой столбец с теми же устройством и типом данных, что у embedding.
  kind: cloze
  template: embedding = torch.cat([embedding, ___], dim=-1)
  answer: torch.zeros_like(embedding[:, :1])
  accept:
    - torch.zeros_like(embedding[:, :1])
    - embedding.new_zeros((embedding.shape[0], 1))
  id: 026-timestep-embedding-2
  fingerprint: 556d882a
