---
id: 013-check-ostatok
type: check
title: 'Проверка: чему равен остаток после первой ступени'
source_anchor: '### The core trick: Residual Vector Quantization (RVQ)'
check:
  - question: >-
      Выход энкодера для кадра — 0.83. Первая ступень выбрала ближайший код
      0.75. Что попадёт на вход второй ступени?
    options:
      - '0.83 — исходное значение, ступени работают с ним независимо'
      - '0.08 — то, что первая ступень не досказала'
      - '0.75 — код, выбранный первой ступенью'
      - 'Номер выбранного кода, то есть его позиция в таблице'
    correct: 1
    explanation: >-
      Вторая ступень квантует остаток: разницу между исходным значением и тем,
      что уже выразила первая ступень. 0.83 минус 0.75 = 0.08.
  - question: >-
      Первая ступень попала точно: выход энкодера 0.50, и в codebook есть код
      ровно 0.50. Что дальше делает вторая ступень?
    options:
      - >-
        Квантует остаток 0.0 и выбирает код, ближайший к нулю — то есть почти
        ничего не добавляет
      - 'Пропускается, и в кадре остаётся всего один номер вместо восьми'
      - 'Заново квантует 0.50 и выдаёт тот же номер, что и первая'
    correct: 0
    explanation: >-
      Ступени всегда отрабатывают все: остаток просто оказывается нулевым, и
      вторая ступень добавляет к сумме почти ноль. Число кодов в кадре не
      меняется.
---

К этому моменту ты должен уметь взять одно число, прогнать его через первую ступень и сказать, что именно уйдёт дальше по каскаду. Не «примерно остаток», а конкретную цифру: исходное значение минус выбранный код.

Спотыкаются обычно на двух вещах. Первая — путают код и его номер. В сумму декодера идёт значение из таблицы, а по каналу передаётся номер строки; остаток считается через значение, не через номер ([шаг 7](#step-7)).

Вторая — пытаются «сэкономить» ступень, когда остаток вышел нулевым или совсем крошечным. Каскад устроен жёстко: сколько ступеней задано, столько и отработает. Просто вклад такой ступени будет почти никакой — и это нормально, а не поломка.
