---
id: 027-check-kodek
type: check
title: 'Проверка: реставрация записи против потоковой речи'
source_anchor: '### The four codecs that matter in 2026'
check:
  - question: >-
      Тебе принесли архив старых концертных записей: надо восстановить их как
      можно ближе к оригиналу, результат слушают люди, никакой языковой модели в
      цепочке нет. Что берёшь?
    options:
      - >-
        Mimi — низкая частота кадров важнее точности реконструкции при любой
        задаче
      - DAC — он ориентирован на высокую верность восстановления и полную полосу
      - >-
        SNAC — многомасштабная структура гарантирует наиболее точное
        восстановление музыки
      - >-
        Любой кодек — если токены не генерирует языковая модель, различий между
        ними нет
    correct: 1
    explanation: >-
      Для реставрации важна верность декодированного сигнала, а длина токенной
      последовательности не ограничивает систему. DAC даёт лучшую точность
      восстановления среди перечисленных открытых кодеков и работает с полной
      полосой на 44.1 кГц.
  - question: >-
      Почему низкая частота кадров Mimi полезна языковой модели, генерирующей
      потоковую речь?
    options:
      - >-
        Модели нужно авторегрессионно предсказывать меньше временных кадров на
        каждую секунду речи
      - >-
        Каждый индекс автоматически описывает больше независимых вариантов
        тембра
      - >-
        Низкая частота кадров гарантирует более точное восстановление звуковой
        волны
      - Она устраняет необходимость генерировать акустические codebook'и
    correct: 0
    explanation: >-
      Mimi выдаёт 12.5 кадра в секунду против 86 у DAC — почти в семь раз меньше
      временных шагов. Это сокращает генерируемую последовательность, но само по
      себе не означает ни уменьшения числа codebook'ов, ни повышения точности
      реконструкции.
---

Тут проверяется не память на названия, а умение задать один вопрос перед выбором: кто потребитель кодов? Если коды слушает человек через декодер — важна верность, и ось из [шага 26](#step-26) толкает тебя к DAC. Если коды генерирует языковая модель — важна длина последовательности, и ты уходишь к Mimi или SNAC.

Спотыкаются обычно на двух вещах. Первая — «раз DAC точнее, он лучше вообще»: точность стоит 86 кадров в секунду, и в задаче реставрации это бесплатно, а в генерации — нет. Вторая — путают число codebook'ов (сколько кодов на один кадр) с частотой кадров (сколько кадров на секунду). Это разные оси, и режут они разное.
