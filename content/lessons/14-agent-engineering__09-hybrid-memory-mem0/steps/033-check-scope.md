---
id: 033-check-scope
type: check
title: 'Проверка: забыли user_id — это баг ранжирования?'
source_anchor: '### Scope taxonomy'
check:
  - question: >-
      В hybrid_search забыли передать user_id, и в выдаче Алисы оказался факт из
      памяти Боба. Как это классифицировать?
    options:
      - 'Баг ранжирования: веса подобраны неудачно, надо поднять w_importance'
      - >-
        Утечка данных: user_id — единственное, что разделяет память Алисы и
        Боба, и без фильтра чужая запись попала в выборку легально
      - >-
        Нормальное поведение: векторная ветка всё равно возвращает семантически
        близкие факты
      - 'Баг извлечения фактов: LLM записала факт Боба не в ту сущность'
    correct: 1
    explanation: >-
      Ранжирование только упорядочивает то, что уже отобрано. Без фильтра по
      user_id чужая запись входит в кандидаты как валидная, и никакой перебор
      весов её оттуда не выкинет: сломана изоляция, а не сортировка.
  - question: >-
      Пользователь в текущем чате сказал: «пока обсуждаем только черновик,
      финальную версию не трогай». Какая область видимости подходит и почему?
    options:
      - 'User memory: любое указание пользователя должно жить между сессиями'
      - >-
        Session memory: условие действует внутри этого треда и вместе с ним
        заканчивается
      - 'Agent memory: указание описывает поведение агента'
      - Все три области сразу — так надёжнее не потерять указание
    correct: 1
    explanation: >-
      Область выбирают по времени жизни факта. Это условие конкретного
      разговора, а не устойчивое свойство человека: в user-памяти оно всплывёт в
      другом чате через месяц и будет мешать, а запись во все области ещё и
      расширит доступ к нему.
---

Ты дописал `hybrid_search` в [шаге 28](#step-28) и покрутил веса в [шаге 29](#step-29) — тебе уже привычно объяснять странный top-3 через `w_recency` и `w_importance`. Здесь эта привычка ломается: когда в выдаче Алисы всплыл факт про проект Боба, крутить веса бесполезно. Запись попала в выборку до всякого ранжирования, потому что `user_id` в фильтре не было.

Проверь себя на двух вещах: умеешь ли ты отличать «плохо отсортировано» от «взято лишнее», и выбираешь ли область видимости по времени жизни факта — user между сессиями, session внутри треда, agent под конкретный экземпляр.

Обычно спотыкаются там, где хочется положить факт сразу во все области «на всякий случай». Это не запас надёжности, а способ вынести приватное за пределы того, кому оно принадлежит.
