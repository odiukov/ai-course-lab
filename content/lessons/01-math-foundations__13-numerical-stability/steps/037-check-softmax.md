---
id: 037-check-softmax
type: check
title: 'Проверка: изменится ли ответ softmax, если ко всем логитам прибавить 50'
source_anchor: '### Why Softmax Needs the Max-Subtraction Trick'
check:
  - question: Ты прибавил 50 к каждому логиту. Что произойдёт с результатом softmax?
    options:
      - Вероятности останутся теми же
      - Самый большой логит получит ещё большую вероятность
      - Все вероятности станут одинаковыми
      - Каждая вероятность увеличится на 50
    correct: 0
    explanation: >-
      Общий сдвиг не меняет разницы между логитами, а softmax зависит именно от
      этих разниц.
  - question: >-
      Почему после прибавления 50 наивный softmax может сломаться, хотя
      правильный ответ не изменился?
    options:
      - Сумма вероятностей станет больше единицы
      - Экспоненты могут переполниться
      - Логиты перестанут быть числами
      - Softmax начнёт вычитать среднее
    correct: 1
    explanation: >-
      Большие логиты дают слишком большие экспоненты. Устойчивый softmax сначала
      вычитает максимум и избегает переполнения.
---

Здесь важно разделить математический ответ и способ вычисления. Как показано на [шаге 34](#step-34), общий сдвиг всех логитов сохраняет расстояния между ними, поэтому вероятности должны остаться прежними.

Частая ловушка — решить, что большие числа обязательно дадут более уверенный прогноз. Но уверенность меняется только тогда, когда меняются разницы логитов. При этом наивное вычисление действительно может вернуть `inf` или `nan`: прибавленные 50 приблизили экспоненты к переполнению. Устойчивая функция из [шага 35](#step-35) вернёт прежние вероятности.
