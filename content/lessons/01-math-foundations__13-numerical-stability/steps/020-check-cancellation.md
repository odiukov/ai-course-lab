---
id: 020-check-cancellation
type: check
title: 'Проверка: почему у наивной дисперсии выходит отрицательное число'
source_anchor: '### Catastrophic Cancellation'
check:
  - question: >-
      Считаешь дисперсию наивной формулой для чисел около миллиона в float32 и
      получаешь минус 3.5. Что произошло?
    options:
      - 'В данных есть отрицательные значения, они и дали минус'
      - >-
        Оба слагаемых огромны и округлены, а их разница меньше самого округления
        — знак получился случайным
      - 'Формула для дисперсии требует делить на n минус 1, отсюда минус'
    correct: 1
    explanation: >-
      Дисперсия не может быть отрицательной по определению. Значит, весь
      результат состоит из шума округления двух больших почти равных чисел, и
      его знак ничем не защищён.
  - question: >-
      Программист переписал наивную формулу с float32 на float64 и минус исчез.
      Проблема решена?
    options:
      - 'Да, float64 точнее, значит вычитание больших чисел стало безопасным'
      - >-
        Нет, порог просто отодвинулся: при ещё больших значениях или большем
        разбросе средних тот же провал повторится
      - 'Нет, float64 вообще не хранит дробную часть, минус вернётся сразу'
    correct: 1
    explanation: >-
      Больше разрядов мантиссы — больше запас, но сама причина остаётся: разница
      двух почти равных гигантов всё равно съедает значащие цифры. Лечится
      переписыванием формулы, а не сменой типа.
---

После этого шага ты должна уметь смотреть на формулу и заранее говорить, где она развалится: если в ней вычитаются два больших почти равных числа, результат будет шумом, а не ответом. Для наивной дисперсии (шаг 17) это видно особенно ясно: математически она не бывает отрицательной, поэтому минус на выходе — не свойство данных, а чистая ошибка округления, которой хватило, чтобы перевернуть знак.

Спотыкаются тут обычно на двух вещах. Первая: искать баг в коде, хотя код верен, а виновата форма записи. Вторая: считать, что переход на float64 — это исправление; он лишь отодвигает порог, а провал возвращается на числах побольше. Настоящее лечение — Уэлфорд (шаг 18) или предварительное центрирование.
