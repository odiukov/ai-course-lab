- concept: отношение вероятностей двух классов после softmax определяется разностью их логитов
  question: Заполни пропуск в тождестве для классов p и q.
  explanation: Общий знаменатель softmax сокращается, поэтому отношение вероятностей равно экспоненте разности логитов. При одинаковом сдвиге всех логитов эта разность сохраняется, а значит, не меняются и вероятности.
  kind: cloze
  template: \frac{\operatorname{softmax}(x)_p}{\operatorname{softmax}(x)_q}=___
  answer: e^{x_p-x_q}
  accept:
    - \exp(x_p-x_q)
    - exp(x_p-x_q)
    - e^(x_p-x_q)
  id: 034-softmax-sdvig-1
  fingerprint: 6f23c250
