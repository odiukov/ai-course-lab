---
id: 026-check-prompt
type: check
title: 'Проверка: что попало в промпт, а что нет'
source_anchor: '## Use It'
check:
  - question: >-
      Агент решил взять навык release-notes-writer. Что из файла SKILL.md
      дословно окажется в системном промпте?
    options:
      - Только поля name и description из фронтматтера
      - Тело навыка после закрывающих трёх дефисов
      - >-
        Весь файл целиком, вместе с фронтматтером и всеми файлами из папки
        навыка
      - 'Пересказ тела, который загрузчик сокращает до 300 токенов'
    correct: 1
    explanation: >-
      Фронтматтер работает на этапе выбора, а в промпт уезжает тело — и без
      правок, слово в слово.
  - question: >-
      В теле навыка есть строка со ссылкой на templates/changelog.md. Когда
      содержимое этого файла попадёт в промпт?
    options:
      - 'Сразу вместе с телом навыка: загрузчик подставляет все упомянутые файлы'
      - 'Никогда: ссылки внутри тела просто текст, читать их нельзя'
      - Только когда агент явно попросит прочитать этот файл
    correct: 2
    explanation: >-
      Это и есть постепенное раскрытие: сначала едет тело, а подресурс
      подтягивается отдельным чтением по требованию.
---

После `code/main.py` ты должен уметь разделить три вещи, которые легко склеиваются в одну. Фронтматтер — топливо для выбора навыка: по нему агент решает, брать или нет. Тело — то, что уезжает в системный промпт слово в слово. Подресурсы — то, что лежит рядом и ждёт отдельной просьбы.

Спотыкаются обычно на середине: кажется, что загрузчик умный и что-то с телом делает — подрезает, переформулирует, дописывает контекст. Не делает. Он курьер, не редактор. Поэтому плохо написанное тело навыка так и приедет агенту плохо написанным — код это не спасёт.
