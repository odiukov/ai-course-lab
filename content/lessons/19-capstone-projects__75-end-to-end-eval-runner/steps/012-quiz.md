---
id: 012-quiz
type: quiz
title: Проверяем весь контракт сквозного eval-раннера
source_anchor: '## Going further'
check:
  - question: >-
      Один адаптер уже вернул ответ модели. Как раннер должен передать его
      дальше?
    options:
      - 'Повторно вызвать модель отдельно для метрик, калибровки и лидерборда'
      - 'Использовать тот же ответ для метрик, калибровки и итогового отчёта'
      - 'Отдать ответ только метрике, а лидерборд построить по числу токенов'
      - Сохранить ответ лишь при полном балле
    correct: 1
    explanation: >-
      Один вызов модели создаёт один результат, который питает всю последующую
      цепочку. Повторные вызовы сделали бы части отчёта несопоставимыми.
  - question: >-
      Бинарная метрика дала 0.8, а частичная — ровно 0.5. Какие значения correct
      получатся?
    options:
      - Оба результата будут верными
      - Оба результата будут неверными
      - 'Бинарный будет неверным, частичный — верным'
      - 'Бинарный будет верным, частичный — неверным'
    correct: 2
    explanation: >-
      Бинарной метрике нужен полный балл, поэтому 0.8 недостаточно. Для
      частичной метрики граница 0.5 включается и засчитывается.
  - question: >-
      Ты запускаешь два адаптера на трёх задачах параллельно. Какой результат
      соответствует контракту?
    options:
      - >-
        Шесть выполнений, а итог отсортирован так же, как в последовательном
        режиме
      - 'Три выполнения: каждая задача выбирает один свободный адаптер'
      - Шесть выполнений в порядке завершения запросов
      - 'Двенадцать выполнений: параллельный режим повторяет каждую пару'
    correct: 0
    explanation: >-
      Раннер исполняет каждую пару адаптер-задача ровно один раз. Конкурентность
      меняет время завершения, но не стабильный порядок итогов.
  - question: Каким должен быть ответ контрольного адаптера для задачи code_exec?
    options:
      - Любым текстом с названием ожидаемой функции
      - Исполнимой функцией в ожидаемом формате и с положительным token_count
      - Только числовым баллом без текста ответа
      - 'Оформленной буквой варианта, как для extract_letter'
    correct: 1
    explanation: >-
      Контрольный адаптер обязан выдавать ответ в формате конкретной задачи. Для
      code_exec это исполнимая функция, а статистика токенов должна оставаться
      правдоподобной.
---

Дальше ты сможешь заменять контрольный адаптер настоящим провайдером, не переделывая остальную цепочку. Для этого держи вместе главные гарантии из [шагов 1](#step-1)–[5](#step-5): один ответ проходит весь путь, каждая пара запускается один раз, а конкурентность не меняет итог. Затем проверяй преобразование и отчёт по [шагам 6](#step-6)–[9](#step-9). Чаще всего спотыкаются на маленьком расхождении форматов: числа есть, но весь контракт уже нарушен.

> 🎒 **На пальцах.** Представь предполётную проверку самолёта. Недостаточно отдельно увидеть топливо, закрытую дверь и работающий двигатель: важен весь маршрут проверки, включая запись результатов в журнал. Если один пункт пропущен или записан не туда, самолёт ещё может выглядеть готовым, но доверять такому контролю нельзя. Сквозной eval проверяется ровно так же — целиком.
