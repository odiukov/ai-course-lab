---
id: 002-contract-check
type: check
title: Проверяем границы интеграционного контракта
source_anchor: '## The adapter interface'
check:
  - question: >-
      Адаптер вернул текст ответа и confidence 0.8, но не передал token_nll и
      token_count. Нарушен ли контракт?
    options:
      - 'Да, оба поля обязательны для любой генерации'
      - 'Нет, оба поля необязательны'
      - 'Нет, но только для RuleBasedAdapter'
    correct: 1
    explanation: >-
      Контракт допускает отсутствие token_nll и token_count. Обязательны сам
      текст и confidence в допустимых границах.
  - question: >-
      Новый адаптер возвращает хороший текст, но confidence равен 1.2. Можно ли
      без изменений подключить его к раннеру?
    options:
      - 'Да, качество текста важнее формата остальных полей'
      - 'Да, раннер сам преобразует любое число в вероятность'
      - 'Нет, адаптер нарушает границу контракта confidence от 0 до 1'
    correct: 2
    explanation: >-
      Раннер рассчитывает получить вероятность в пределах от 0 до 1. Значение
      1.2 нельзя считать корректной Generation.
---

После сквозной сборки из [шага 1](#step-1) попробуй заменить `RuleBasedAdapter` новым адаптером. Хороший ответ модели ещё не означает, что замена безопасна: раннер ждёт `model_id`, метод `generate` с `prompt` и `task`, а в результате — текст и `confidence` от 0 до 1. На этом чаще всего и спотыкаются: принимают необязательные сведения о токенах за обязательные или пропускают недопустимую уверенность.

Ты проверяешь не внутреннее устройство модели, а то, состыкуется ли её выход с остальной системой.

> 🎒 **На пальцах.** Представь сортировочный пункт посылок. Сотрудникам неважно, на какой фабрике собрали вещь внутри. Но на коробке обязаны быть адрес и корректная маркировка; дополнительная опись может отсутствовать. Если адрес записан в неожиданном формате, даже отличная вещь не доедет дальше. Контракт адаптера — такая маркировка для результата модели.
