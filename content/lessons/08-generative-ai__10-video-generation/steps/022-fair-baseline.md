---
id: 022-fair-baseline
type: check
title: Проверьте честность независимой базовой линии
source_anchor: '### Step 4: temporal coherence test'
check:
  - question: >-
      Как должна быть устроена независимая покадровая базовая линия, чтобы
      сравнение выделяло именно временную связность?
    options:
      - Каждый кадр генерируется отдельной моделью с похожими параметрами
      - >-
        Для каждого момента времени тем же make_video генерируется новый полный
        клип, из которого берётся соответствующий кадр
      - >-
        Один совместно сгенерированный клип разбивается на кадры, которые затем
        перемешиваются
    correct: 1
    explanation: >-
      Каждый выбранный кадр остаётся образцом из того же распределения для
      своего момента времени, но кадры больше не принадлежат одной траектории.
      Поэтому из сравнения исключается именно временная зависимость.
  - question: >-
      В независимой базовой линии вместе со способом сэмплирования изменилась
      дисперсия кадров. Как теперь интерпретировать разницу в flicker_score?
    options:
      - Как чистый эффект разрушения временной зависимости
      - Как чистый эффект изменения дисперсии
      - >-
        Как совместный эффект временной зависимости и различия распределений
        кадров
    correct: 2
    explanation: >-
      Метрика реагирует и на потерю общей траектории, и на изменившийся разброс
      кадров. Поэтому разрыв уже нельзя приписать только временной связности.
---

Ты видишь `joint=0.61` и `independent=1.31` и можешь поспешно объявить совместную генерацию победителем. Но сначала проверь соперника: каждый его кадр должен приходить из того же `make_video` и соответствовать тому же моменту времени. Отличаться должна только принадлежность кадров общей траектории. Тогда показатель из [шага 21](#step-21) действительно проверяет временную связность. Если базовой линии поменять наклон, разброс или сам генератор, разрыв смешает несколько причин — красивое число перестанет отвечать на нужный вопрос.

> 🎒 **На пальцах.** Представь дегустацию двух партий йогурта, где ты проверяешь влияние холодильника. Если для второй партии заодно сменить рецепт и жирность, разница во вкусе ничего не докажет. Честное сравнение оставляет тот же йогурт и меняет только хранение; здесь так же меняется только общая траектория кадров.
