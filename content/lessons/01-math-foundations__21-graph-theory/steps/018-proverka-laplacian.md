---
id: 018-proverka-laplacian
type: check
title: 'Проверка: вычислить строку лапласиана и объяснить нулевую сумму'
source_anchor: '### The Graph Laplacian'
check:
  - question: >-
      Как определить элементы строки лапласиана, соответствующей выбранной
      вершине невзвешенного графа?
    options:
      - >-
        На диагонали записать степень вершины, в столбцах её соседей — -1, а в
        остальных столбцах — 0
      - >-
        На диагонали записать отрицательную степень вершины, в столбцах её
        соседей — 1, а в остальных столбцах — 0
      - 'На диагонали записать 0, а во всех остальных столбцах — степень вершины'
      - Во всех столбцах записать -1 независимо от наличия рёбер
    correct: 0
    explanation: >-
      Лапласиан вычисляется как L = D - A: матрица D даёт степень на диагонали,
      а вычитание A превращает единицы напротив соседей в -1. Поэтому для
      первого узла получается строка 3, -1, -1, -1.
  - question: Почему сумма элементов строки лапласиана невзвешенного графа равна нулю?
    options:
      - Потому что у любой квадратной матрицы сумма каждой строки равна нулю
      - >-
        Потому что диагональная степень вершины компенсируется значениями -1 по
        одному на каждого её соседа
      - >-
        Потому что в лапласиане все положительные элементы попарно равны
        отрицательным
      - Потому что число строк лапласиана совпадает с числом его столбцов
    correct: 1
    explanation: >-
      Степень равна числу соседей. В рассматриваемой строке значение 3 на
      диагонали компенсируется тремя значениями -1, поэтому сумма равна нулю.
---

Возьми первый узел и собери только его строку. Здесь легко споткнуться: положительное число относится к самому узлу, а минусы — к его соседям. Правило из [шага 15](#step-15) помогает проверить результат без пересчёта всей матрицы: сложи элементы строки. Степень узла должна точно погасить все значения $-1$ напротив соседей, поэтому получится ноль.

Если сумма не нулевая, ищи пропущенное ребро, лишний минус или неверную степень.

> 🎒 **На пальцах.** Представь гардеробщика: он получил три куртки и выдал по одной трём посетителям. Приход записан как $+3$, каждая выдача — как $-1$. Итоговая строка журнала даёт ноль: ничего не потерялось и не появилось из воздуха.
