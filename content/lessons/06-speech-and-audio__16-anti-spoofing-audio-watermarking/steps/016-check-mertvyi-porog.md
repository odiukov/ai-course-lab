---
id: 016-check-mertvyi-porog
type: check
title: 'Проверка: почему пара 0.85 и 0.92 молчит'
source_anchor: '### Step 1: a simple spectral-feature detector (toy)'
check:
  - question: >-
      Ты выставил percentile=0.90, а max_rolloff_ratio=0.90. Что будет делать
      детектор?
    options:
      - 'Начнёт помечать подряд всё, включая живую речь'
      - >-
        Не поднимет флажок ни на чём: даже у идеально плоского спектра отношение
        дотянет ровно до 0.90, а условие требует строго больше
      - Сработает только на записях с шумом на высоких частотах
      - 'Будет работать так же, как пара 0.95 и 0.92'
    correct: 1
    explanation: >-
      0.90 — это потолок отношения при percentile=0.90, а сравнение строгое,
      поэтому порог, равный percentile, недостижим так же, как и порог выше
      него.
  - question: >-
      Почему пара percentile=0.95 и max_rolloff_ratio=0.92 живая, а
      percentile=0.85 и max_rolloff_ratio=0.92 — мёртвая?
    options:
      - '0.95 — более точное значение, чем 0.85, поэтому детектор чувствительнее'
      - >-
        При 0.85 набирается меньше энергии, значит детектор просто реже
        срабатывает, но иногда всё же срабатывает
      - >-
        В первой паре порог ниже percentile, и до него можно дотянуться; во
        второй порог выше потолка 0.85, и дотянуться нельзя никогда
      - 0.92 подходит только к percentile=0.95 по историческим причинам
    correct: 2
    explanation: >-
      Отношение rolloff к длине спектра не может превысить percentile, поэтому
      порог обязан лежать ниже него — иначе условие не выполнится ни на одной
      записи.
---

Ты дописал `is_suspicious` в [шаге 15](#step-15) и теперь должен уметь отвечать на вопрос, который поначалу кажется придиркой: почему конкретная пара чисел ломает детектор, хотя каждое число по отдельности выглядит нормально.

Спотыкаются тут обычно на одном месте — читают `percentile` и `max_rolloff_ratio` как две независимые настройки строгости и рассуждают «0.85 — мягче, 0.92 — строже». На деле первое число задаёт потолок для второго: выше percentile отношение не поднимется никогда, а значит порог выше него превращает `if` в код, который не выполняется. Проверь, что ты видишь эту связь на числах, а не помнишь её как правило.
