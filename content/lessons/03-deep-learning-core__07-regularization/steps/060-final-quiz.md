---
id: 060-final-quiz
type: quiz
title: 'Итоговая проверка: gap, dropout, L2, нормализация и early stopping'
source_anchor: '## Key Terms'
check:
  - question: Какое описание точнее всего передаёт суть переобучения?
    options:
      - Модель не смогла обнаружить даже закономерности обучающих данных
      - >-
        Модель подогналась под шум и случайные особенности обучения, которые не
        переносятся на новые данные
      - Модель использует слишком мало вычислений на инференсе
      - Модель выдаёт одинаковые ответы для всех примеров
    correct: 1
    explanation: >-
      При переобучении модель усваивает не только устойчивый сигнал, но и
      случайные особенности обучающей выборки. Поэтому её результаты на новых
      данных ухудшаются.
  - question: Почему dropout можно рассматривать как приближённое обучение ансамбля?
    options:
      - Каждая случайная маска задаёт одну из множества подсетей с общими весами
      - Он создаёт независимую копию датасета для каждого нейрона
      - Он заменяет один выходной слой несколькими независимыми моделями
      - 'Он усредняет параметры моделей, обученных на разных задачах'
    correct: 0
    explanation: >-
      Разные dropout-маски образуют разные подсети, которые обучаются с общими
      параметрами. Их совокупный эффект напоминает ансамбль, хотя отдельные
      модели явно не сохраняются.
  - question: Какой метод регуляризации непосредственно преобразует обучающие входы?
    options:
      - Data augmentation
      - Weight decay
      - Early stopping
      - Layer normalization
    correct: 0
    explanation: >-
      Data augmentation изменяет входные примеры допустимыми преобразованиями,
      например отражением, обрезкой или добавлением шума. Остальные методы
      воздействуют на веса, активации или процесс обучения.
  - question: Какое утверждение о сочетании методов регуляризации верно?
    options:
      - После включения нормализации остальные регуляризаторы всегда бесполезны
      - 'Dropout, weight decay и early stopping нельзя использовать одновременно'
      - >-
        Методы с разными механизмами действия можно комбинировать, контролируя
        их силу по валидационным результатам
      - Любая комбинация регуляризаторов гарантированно повышает качество модели
    correct: 2
    explanation: >-
      Разные регуляризаторы воздействуют на обучение по-разному и могут
      дополнять друг друга. Однако слишком сильное совместное ограничение
      способно ухудшить подгонку, поэтому результат проверяют на валидационных
      данных.
---

Дальше тебе понадобится не список приёмов, а привычка ставить диагноз по числам: сопоставлять качество и gap из [шага 8](#step-8), выбирать силу регуляризации по [шагу 53](#step-53) и проверять результат контролируемым сравнением из [шага 54](#step-54). Частые ловушки — принять малый gap за успех, оставить модель в режиме train при тестировании или вернуть веса эпохи остановки вместо лучшей validation-эпохи.

> 🎒 **На пальцах.** Представь пульт с несколькими ручками: dropout, weight decay, нормализация и момент остановки. Если звук уже тихий и плоский, закручивать все ограничители сильнее бессмысленно. Сначала ты слушаешь и измеряешь искажение, затем меняешь одну настройку и сравниваешь. Регуляризация работает не как кнопка «улучшить», а как точно дозированная коррекция.
