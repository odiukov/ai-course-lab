---
id: 026-l2-check
type: check
title: 'Проверка: штраф, градиент и отличие AdamW'
source_anchor: '### Weight Decay (L2 Regularization)'
check:
  - question: >-
      Один вес равен 10, другой — 1. Как различаются их вклад в L2-штраф и
      добавка к градиенту?
    options:
      - 'Штраф больше в 10 раз, а добавка к градиенту — в 100 раз'
      - 'И штраф, и добавка к градиенту больше в 10 раз'
      - 'Штраф больше в 100 раз, а добавка к градиенту — в 10 раз'
      - 'И штраф, и добавка к градиенту больше в 100 раз'
    correct: 2
    explanation: >-
      Штраф зависит от квадрата веса, поэтому 10 даёт вклад в 100 раз больше,
      чем 1. Добавка к градиенту пропорциональна самому весу и отличается в 10
      раз.
  - question: >-
      Ты обучаешь модель с Adam и хочешь корректно применять weight decay. Что
      выбрать?
    options:
      - 'AdamW, где сжатие весов отделено от адаптивного градиента'
      - 'Обычный Adam: он всегда полностью эквивалентен AdamW'
      - 'Adam без weight decay, но с увеличенным learning rate'
    correct: 0
    explanation: >-
      AdamW применяет сжатие весов отдельно от адаптивного преобразования
      градиента. В обычном Adam добавление L2 к loss не даёт того же обновления.
---

Здесь легко перепутать две разные величины. Как видно из [шага 20](#step-20), сам штраф растёт квадратично, но его воздействие на обновление веса из [шага 22](#step-22) — лишь пропорционально весу. А при Adam появляется ещё одна ловушка: адаптивный оптимизатор преобразует градиент, поэтому L2 внутри loss и отдельное сжатие весов перестают быть равнозначными. Именно поэтому в [шаге 25](#step-25) появился AdamW.

> 🎒 **На пальцах.** Представь выступающие из стены полки. Плата за неудобство растёт по квадрату их длины, но мастер укорачивает каждую пропорционально тому, насколько она выступает. AdamW — это отдельный мастер: он укорачивает полки напрямую, не пропуская эту работу через умную систему, которая по-разному перенастраивает остальные ремонтные команды.
