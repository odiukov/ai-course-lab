---
id: 015-dropout-modes-check
type: check
title: 'Проверка: dropout в режимах обучения и инференса'
source_anchor: '### Dropout'
check:
  - question: >-
      Ты дважды подаёшь одни и те же активации в слой с inverted dropout. Что
      ожидается?
    options:
      - 'В обучении результаты могут различаться, а в инференсе должны совпасть'
      - 'В обучении результаты должны совпасть, а в инференсе могут различаться'
      - Результаты могут различаться в обоих режимах
    correct: 0
    explanation: >-
      При обучении каждый проход получает случайную маску. При инференсе dropout
      отключён, поэтому одинаковый вход даёт одинаковый результат.
  - question: >-
      Как слой должен обработать активации в режиме инференса после обучения с
      inverted dropout?
    options:
      - Случайно обнулить часть значений без масштабирования
      - Умножить все значения на вероятность сохранения нейрона
      - Вернуть активации без маски и дополнительного масштабирования
    correct: 2
    explanation: >-
      В inverted dropout масштаб уже скорректирован во время обучения, поэтому
      при инференсе активации проходят без изменений.
---

Здесь важно не перепутать два режима функции из [шага 14](#step-14). Во время обучения dropout случайно меняет результат и усиливает оставшиеся активации. Во время инференса случайности уже нет: используются все активации, а дополнительное масштабирование не требуется.

Частая ошибка — оставить маску включённой при проверке модели. Тогда один и тот же пример получает разные предсказания, хотя модель уже не обучается.

> 🎒 **На пальцах.** Повар тренируется, когда ему случайно убирают часть кухонных инструментов, а доступными разрешают работать интенсивнее. Но вечером ресторан открывается: повар получает весь набор и больше ничего не компенсирует. Если продолжать прятать инструменты при подаче блюда, результат будет случайно меняться от заказа к заказу.
