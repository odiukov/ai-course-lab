---
id: 040-layer-check
type: check
title: 'Проверка: почему LayerNorm одинаков при train и inference'
source_anchor: '### Layer Normalization'
check:
  - question: >-
      Один и тот же вектор признаков сначала проходит через LayerNorm при
      обучении в батче из 32 примеров, а затем отдельно при инференсе. Параметры
      слоя не изменились. Что произойдёт?
    options:
      - >-
        Результат LayerNorm будет тем же, потому что статистики считаются внутри
        этого вектора
      - 'Результат изменится, потому что при инференсе размер батча равен 1'
      - Слой возьмёт накопленные среднее и дисперсию обучающих батчей
      - При инференсе LayerNorm полностью отключится
    correct: 0
    explanation: >-
      LayerNorm каждый раз считает среднее и дисперсию по признакам самого
      примера. Соседние примеры и размер батча на этот расчёт не влияют.
  - question: >-
      Что нужно переключить внутри LayerNorm при переходе модели из режима train
      в режим inference?
    options:
      - Заменить текущие статистики на running statistics
      - Перестать вычитать среднее
      - 'Ничего: вычисление LayerNorm остаётся тем же'
      - Обнулить gamma и beta
    correct: 2
    explanation: >-
      У LayerNorm нет отдельных статистик для инференса: он одинаково
      нормализует текущий пример в обоих режимах. Обученные gamma и beta
      продолжают применяться.
---

Проверь, можешь ли ты связать два факта: из [шага 36](#step-36) — статистики считаются по признакам одного примера, а из [шага 38](#step-38) — соседи по батчу не нужны. Частая ошибка здесь — перенести на LayerNorm поведение BatchNorm из [шага 32](#step-32) и искать у него накопленные статистики. Их нет: если входной вектор и параметры слоя одинаковы, результат LayerNorm тоже одинаков при train и inference.

> 🎒 **На пальцах.** Ты настраиваешь яркость одной фотографии по её собственным светлым и тёмным участкам. Неважно, лежит рядом ещё 31 снимок или фотография открыта одна: расчёт для неё не меняется. Поэтому отдельный режим обработки для одиночного просмотра не нужен.
