- concept: каждая координата RMSNorm масштабируется соответствующим элементом gamma
  question: Заполни пропуск в реализации RMSNorm так, чтобы для каждой координаты использовался собственный обучаемый множитель.
  explanation: 'Общий RMS одинаков для всего вектора, но масштабирование выполняется покоординатно: элемент x[j] умножается именно на gamma[j].'
  kind: cloze
  template: output.append(___ * x[j] / rms)
  answer: self.gamma[j]
  accept:
    - gamma[j]
  id: 042-rms-norm-1
  fingerprint: 0b4e0e5b
- concept: реализация RMSNorm возвращает новый список, не изменяя входной вектор
  question: RMS уже вычислен, а x и gamma имеют одинаковую длину. Какой фрагмент вернёт нормализованный вектор, не изменяя x?
  explanation: Списковое включение создаёт новый список. Остальные варианты изменяют исходный x напрямую или через псевдоним output, указывающий на тот же объект.
  kind: choice
  options:
    - return [g * value / rms for value, g in zip(x, gamma)]
    - |-
      for j in range(len(x)): x[j] = gamma[j] * x[j] / rms
      return x
    - |-
      output = x
      for j in range(len(output)): output[j] = gamma[j] * output[j] / rms
      return output
  correct: 0
  id: 042-rms-norm-2
  fingerprint: 670521f6
