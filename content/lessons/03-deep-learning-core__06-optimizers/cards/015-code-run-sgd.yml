- concept: градиент на каждой итерации вычисляется для текущих значений параметров
  question: После шага оптимизатора параметры модели изменились. Какой градиент нужно использовать на следующей итерации обучения?
  explanation: Градиент описывает наклон функции потерь в конкретной точке пространства параметров. После обновления модель оказывается в другой точке, поэтому прежний градиент может задавать уже неверное направление.
  kind: choice
  options:
    - Заново вычислить градиент при обновлённых параметрах
    - Повторно использовать градиент с предыдущей итерации
    - Заменить предыдущий градиент нулевым
    - Вычислить градиент только после завершения всех обновлений
  correct: 0
  id: 015-code-run-sgd-1
  fingerprint: e8424262
- concept: изменённый плоский список параметров нужно записать обратно в структуру модели
  question: Заполни пропуск, чтобы обновление плоского списка параметров действительно изменило веса модели.
  explanation: '`get_params` собирает значения весов и смещений в отдельный плоский список. Оптимизатор изменяет этот список, а `set_params` переносит обновлённые значения обратно в поля модели.'
  kind: cloze
  template: |-
    params = model.get_params()
    optimizer.step(params, grads)
    ___
  answer: model.set_params(params)
  accept:
    - model.set_params(params)
  id: 015-code-run-sgd-2
  fingerprint: dae4e218
