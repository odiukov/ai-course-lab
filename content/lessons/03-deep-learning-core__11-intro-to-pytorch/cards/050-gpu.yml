- concept: model.to рекурсивно переносит зарегистрированные параметры и буферы вложенных модулей
  question: Модель состоит из нескольких вложенных слоёв и содержит зарегистрированные служебные буферы. Что переносит вызов model.to(device)?
  explanation: Метод model.to(device) рекурсивно обходит зарегистрированные модули и переносит их параметры и буферы. Данные из DataLoader не принадлежат модели и этим вызовом не затрагиваются.
  kind: choice
  options:
    - Только параметры верхнего уровня модели
    - Все зарегистрированные параметры и буферы модели, включая содержимое вложенных модулей
    - Модель и все будущие батчи, которые выдаст DataLoader
    - Только служебные буферы, но не обучаемые параметры
  correct: 1
  id: 050-gpu-1
  fingerprint: b3a8169b
- concept: проверка доступности CUDA позволяет автоматически выбрать CPU как резервное устройство
  question: Заполни пропуск так, чтобы код использовал CPU, когда CUDA недоступна.
  explanation: torch.cuda.is_available() сообщает, доступна ли CUDA. Если нет, условное выражение выбирает CPU, и тот же код обучения остаётся работоспособным.
  kind: cloze
  template: device = torch.device("cuda" if torch.cuda.is_available() else "___")
  answer: cpu
  accept:
    - cpu
  id: 050-gpu-2
  fingerprint: a7b27fef
