- concept: Adam служит стандартной отправной точкой при выборе оптимизатора
  question: Обучение новой модели только начинается, и специальных требований к оптимизатору пока нет. Какой вариант обычно выбирают в качестве отправной точки?
  explanation: Adam часто используют как начальный вариант по умолчанию. SGD обычно выбирают для отлаженных пайплайнов и свёрточных сетей, а AdamW — для трансформеров и дообучения.
  kind: choice
  options:
    - torch.optim.SGD
    - torch.optim.Adam
    - torch.optim.AdamW
    - torch.optim.LBFGS
  correct: 1
  id: 045-optimizers-1
  fingerprint: 4f1d15de
- concept: AdamW подходит для трансформеров и дообучения с weight decay
  question: Заполните имя оптимизатора для дообучения трансформера с параметром weight_decay.
  explanation: AdamW принимает weight_decay и часто применяется при обучении трансформеров и дообучении моделей.
  kind: cloze
  template: optimizer = torch.optim.___(model.parameters(), weight_decay=decay)
  answer: AdamW
  accept:
    - torch.optim.AdamW
  id: 045-optimizers-2
  fingerprint: 81eb9fe1
