- concept: MSE равен среднему квадратов отклонений прогнозов от целевых значений
  question: Прогнозы модели равны [6, 14], а целевые значения — [0, 8]. Чему равен MSE?
  explanation: Оба отклонения равны 6. После возведения в квадрат каждое даёт 36, поэтому их среднее также равно 36.
  kind: numeric
  answer: 36
  tolerance: 0.001
  id: 024-mse-v-torch-1
  fingerprint: e358e360
- concept: nn.MSELoss реализует среднеквадратичную ошибку для задач регрессии
  question: Какой готовый компонент PyTorch вычисляет среднеквадратичную ошибку для регрессии?
  explanation: nn.MSELoss() вычитает целевые значения из прогнозов, возводит отклонения в квадрат и по умолчанию усредняет результат.
  kind: choice
  options:
    - nn.MSELoss()
    - nn.L1Loss()
    - nn.CrossEntropyLoss()
  correct: 0
  id: 024-mse-v-torch-2
  fingerprint: fcae0c9f
