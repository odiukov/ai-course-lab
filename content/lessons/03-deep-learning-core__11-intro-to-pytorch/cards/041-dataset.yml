- concept: __len__ возвращает количество примеров в Dataset
  question: Что должен возвращать метод __len__ пользовательского класса Dataset в PyTorch?
  explanation: __len__ сообщает размер выборки — количество примеров, доступных по индексам. Размер батча и число батчей определяются отдельно, обычно объектом DataLoader.
  kind: choice
  options:
    - Размер одного изображения в байтах
    - Количество доступных примеров
    - Число признаков в одном примере
    - Количество батчей за эпоху
  correct: 1
  id: 041-dataset-1
  fingerprint: a9cc5017
- concept: __getitem__ извлекает согласованные входные данные и целевую метку по одному индексу
  question: Заполни тело метода так, чтобы Dataset возвращал изображение и соответствующую ему метку по запрошенному индексу.
  explanation: __getitem__ получает конкретный индекс и возвращает один согласованный пример. Одинаковый idx используется для изображения и его метки.
  kind: cloze
  template: |-
    def __getitem__(self, idx):
        return ___
  answer: self.images[idx], self.labels[idx]
  accept:
    - (self.images[idx], self.labels[idx])
  id: 041-dataset-2
  fingerprint: ec1dbf34
