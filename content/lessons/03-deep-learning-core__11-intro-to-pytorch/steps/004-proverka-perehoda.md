---
id: 004-proverka-perehoda
type: check
title: 'Проверка: что меняется при переходе на PyTorch, а что остаётся прежним'
source_anchor: '## The Problem'
check:
  - question: Ты переносишь знакомую модель в PyTorch. Что должно остаться прежним?
    options:
      - >-
        Логика: данные проходят через слои, ошибка вызывает backward, а
        оптимизатор обновляет параметры
      - Ручные циклы Python для обработки каждого числа
      - Самостоятельно написанный backward для каждого слоя
    correct: 0
    explanation: >-
      PyTorch сохраняет знакомую логику обучения, но берёт на себя
      низкоуровневые вычисления и автоматическое получение градиентов.
  - question: >-
      Модель в PyTorch решает ту же задачу гораздо быстрее. Какое объяснение
      точнее?
    options:
      - PyTorch незаметно заменяет математическую модель другой
      - 'Знакомые операции выполняют оптимизированные ядра, в том числе на GPU'
      - PyTorch пропускает часть примеров при обучении
    correct: 1
    explanation: >-
      Математика и последовательность действий сохраняются, а способ выполнения
      вычислений становится значительно эффективнее.
---

Ты переносишь модель и видишь знакомые `forward`, `backward` и `step`. Легко решить, что вообще ничего не изменилось. Но сравни два уровня: на уровне твоих действий сохраняется ментальная модель из [шага 2](#step-2), а внутри PyTorch ручные производные и медленные циклы уступают автоматическому дифференцированию и оптимизированным вычислениям из [шага 3](#step-3).

Проверка здесь — не на запоминание названий. Тебе нужно отделить знакомый процесс обучения от нового механизма, который выполняет его быстрее и снимает ручную работу.

> 🎒 **На пальцах.** Ты пересаживаешься с велосипеда на электропоезд: пункт назначения и маршрут остаются теми же, но крутить педали самому больше не нужно. PyTorch не меняет цель поездки — он меняет машину, которая везёт тебя по знакомому пути.
