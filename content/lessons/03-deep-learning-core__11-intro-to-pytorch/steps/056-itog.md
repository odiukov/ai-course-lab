---
id: 056-itog
type: quiz
title: 'Итоговая проверка: тензоры, autograd, nn.Module, цикл обучения и GPU'
source_anchor: '## Key Terms'
check:
  - question: >-
      Модель находится на CUDA и принимает батч формы 64 на 784, но входной
      тензор той же формы остался на CPU. Что произойдёт?
    options:
      - PyTorch автоматически перенесёт батч на CUDA
      - 'Возникнет ошибка устройств, поэтому батч тоже нужно перенести на CUDA'
      - Модель вернёт логиты на CPU
      - 'Вычисление выполнится правильно, но медленнее'
    correct: 1
    explanation: >-
      Подходящая форма не устраняет конфликт устройств. Модель и участвующие в
      вычислении тензоры должны находиться на одном устройстве.
  - question: >-
      Ты вызвал backward для двух батчей подряд, не обнулив градиенты между
      ними. Что окажется в полях grad параметров?
    options:
      - Только градиенты второго батча
      - Среднее градиентов двух батчей
      - Сумма градиентов двух батчей
      - Нулевые градиенты
    correct: 2
    explanation: >-
      PyTorch накапливает градиенты. Без zero_grad результат второго backward
      прибавится к уже сохранённым значениям.
  - question: >-
      В классе-наследнике nn.Module ты присвоил Linear полю self.layer внутри
      __init__. Почему оптимизатор видит веса этого слоя?
    options:
      - Linear копирует веса в каждый входной тензор
      - Оптимизатор ищет все тензоры среди локальных переменных
      - >-
        nn.Module автоматически регистрирует присвоенный дочерний модуль и его
        параметры
      - Параметры появляются только после первого forward
    correct: 2
    explanation: >-
      Присваивание дочернего модуля атрибуту регистрирует его внутри модели,
      поэтому веса попадают в model.parameters и state_dict.
  - question: Какой порядок действий описывает один обычный обучающий батч?
    options:
      - 'zero_grad, forward, вычисление loss, backward, optimizer.step'
      - 'forward, optimizer.step, backward, zero_grad, вычисление loss'
      - 'backward, zero_grad, forward, optimizer.step, вычисление loss'
      - 'optimizer.step, forward, вычисление loss, zero_grad, backward'
    correct: 0
    explanation: >-
      Сначала удаляют старые градиенты, затем получают предсказания и ошибку,
      вычисляют новые градиенты и только после этого обновляют параметры.
---

Здесь важно удержать не отдельные команды, а их сцепление: свойства тензора из [шага 7](#step-7) и [шага 11](#step-11), накопление градиентов из [шага 22](#step-22), регистрацию параметров из [шага 35](#step-35) и жизненный цикл батча из [шага 47](#step-47). Дальше ты будешь менять модели и данные, но эта связка останется каркасом обучения. Чаще всего программа ломается именно на стыках: забытое обнуление, незарегистрированный слой или разные устройства.

> 🎒 **На пальцах.** Представь съёмочную группу перед новым дублем: она стирает старые замечания, снимает сцену, оценивает результат, разбирает причины ошибок и вносит поправки. Если камера и плёнка находятся в разных павильонах, дубль не начнётся. В PyTorch важны не только участники, но и правильный порядок их совместной работы.
