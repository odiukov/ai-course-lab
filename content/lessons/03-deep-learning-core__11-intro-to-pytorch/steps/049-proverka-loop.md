---
id: 049-proverka-loop
type: check
title: 'Проверка: восстанавливаем порядок обучения и режим безопасной оценки'
source_anchor: '### Step 3: Training Loop'
check:
  - question: Какой порядок действий внутри обучающего батча верный?
    options:
      - 'forward, backward, обнуление градиентов, loss, обновление параметров'
      - 'обнуление градиентов, forward, loss, backward, обновление параметров'
      - 'обнуление градиентов, loss, forward, обновление параметров, backward'
    correct: 1
    explanation: >-
      Сначала ты убираешь накопленные градиенты, затем получаешь предсказания и
      ошибку, вычисляешь новые градиенты и только после этого обновляешь
      параметры.
  - question: Что нужно сделать при оценке модели на проверочной выборке?
    options:
      - Включить model.train и вызвать optimizer.step после каждого батча
      - 'Включить model.eval, но продолжать строить граф градиентов'
      - Включить model.eval и выполнять проходы внутри torch.no_grad
    correct: 2
    explanation: >-
      model.eval переключает режимозависимые слои, а torch.no_grad отключает
      ненужную запись вычислений для обратного прохода.
---

Здесь тебе нужно восстановить жизненный цикл батча из [шага 47](#step-47) и отделить обучение от оценки. Частая ошибка — переставить `zero_grad`, `backward` и `step`: тогда параметры обновляются по накопленным либо ещё не вычисленным градиентам.

При проверке параметры вообще не обновляются. Но одного `model.eval()` недостаточно: он меняет поведение режимозависимых слоёв, описанных в [шаге 48](#step-48), а граф отключает отдельный контекст `torch.no_grad()` из [шага 26](#step-26).

> 🎒 **На пальцах.** На тренировке ты решаешь задачу, разбираешь ошибки и исправляешь ответы. На проверке ты лишь сдаёшь готовую работу: включаешь режим проверки и убираешь черновики для исправлений.
