- concept: маркеры отрицания следует сопоставлять с целыми токенами, а не с подстроками
  question: Какой вариант корректно обнаруживает отрицание и не считает токен "notebook" отрицательным?
  explanation: Проверка token in NEGATIONS сравнивает токен с маркерами целиком. Поэтому "not" будет найден, а "notebook" не совпадёт с ним и не вызовет ложного срабатывания.
  kind: choice
  options:
    - return any(token in NEGATIONS for token in tokens)
    - return any(marker in token for token in tokens for marker in NEGATIONS)
    - return "not" in " ".join(tokens)
  correct: 0
  id: 013-has-negation-1
  fingerprint: a303eb35
- concept: any выражает проверку существования и завершает поиск после первого истинного результата
  question: Подставьте встроенную функцию Python, чтобы has_negation возвращала логический результат и не продолжала поиск после найденного маркера.
  explanation: any возвращает True, как только генератор выдаёт истинное значение, и False, если таких значений нет. Подсчитывать количество маркеров для этого не требуется.
  kind: cloze
  template: |-
    def has_negation(tokens):
        return ___(token in NEGATIONS for token in tokens)
  answer: any
  accept:
    - any
  id: 013-has-negation-2
  fingerprint: 106b6ff1
