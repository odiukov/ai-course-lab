- concept: множество хранит уникальные токены и поддерживает операцию пересечения
  question: Почему результат токенизации для лексического NLI-классификатора удобно хранить в Python-множестве?
  explanation: Для проверки лексического пересечения важны уникальные слова. Множество устраняет дубликаты и поддерживает оператор пересечения, поэтому одно многократно повторённое слово не считается несколькими разными совпадениями.
  kind: choice
  options:
    - Множество сохраняет исходный порядок слов
    - Множество удаляет повторы и позволяет напрямую вычислять пересечение токенов
    - Множество автоматически определяет отрицания
    - Множество восстанавливает удалённую пунктуацию
  correct: 1
  id: 011-tokenize-1
  fingerprint: b2de1391
- concept: регулярное выражение выделяет последовательности букв и цифр независимо от окружающей пунктуации
  question: Заполни тело функции так, чтобы она приводила текст к нижнему регистру, выделяла буквенно-цифровые токены без знаков пунктуации и возвращала множество.
  explanation: Класс `[^\W_]` соответствует буквам и цифрам, исключая подчёркивание, а `+` объединяет соседние символы в слова. `lower()` нормализует регистр, а `set()` удаляет повторяющиеся токены.
  kind: cloze
  template: |-
    def tokenize(text):
        return ___
  answer: set(re.findall(r"[^\W_]+", text.lower()))
  accept:
    - set(re.findall(r'[^\W_]+', text.lower()))
  id: 011-tokenize-2
  fingerprint: 5fe017a0
