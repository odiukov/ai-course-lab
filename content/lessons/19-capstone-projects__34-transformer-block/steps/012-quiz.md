---
id: 012-quiz
type: quiz
title: 'Сверяем causal-маску, residual-пути и градиентный эксперимент'
source_anchor: '## Exercises'
check:
  - question: >-
      Ты прогоняешь тензор 2 на 16 на 384 при 6 головах через pre-LN и post-LN
      блоки, инициализировав веса одинаково и выключив dropout. Что увидишь?
    options:
      - 'Формы выходов разойдутся: post-LN добавляет лишнее измерение'
      - 'Формы совпадут, но сами числа на выходе будут разными'
      - 'И формы, и числа совпадут: сложение residual коммутативно'
      - post-LN упадёт с ошибкой на длине 16
    correct: 1
    explanation: >-
      Оба варианта сохраняют форму 2 на 16 на 384. Однако LayerNorm расположен
      по-разному относительно residual-сложения, поэтому значения выходов
      расходятся и проверка allclose это обнаруживает.
  - question: >-
      Кто-то решил не заполнять верхний треугольник логитов минус
      бесконечностью, а занулить запрещённые веса уже после softmax. Что
      сломается?
    options:
      - 'Ничего: нули в верхнем треугольнике полностью решают задачу'
      - Форма весов перестанет быть B на T на T
      - Разрешённые веса уже будут нормированы с учётом будущих позиций
      - Градиенты станут нулевыми по всей матрице
    correct: 2
    explanation: >-
      Softmax вычисляет общий знаменатель по всей строке. Если будущие позиции
      участвовали в нормировке, разрешённые веса уже зависят от их логитов, а
      после зануления строка дополнительно перестаёт суммироваться в единицу.
  - question: Ты собрал стек из блоков и поставил depth равным нулю. Что вернёт модель?
    options:
      - 'Ошибку: стек без блоков не собирается'
      - Нулевой тензор нужной формы
      - 'Embedding, прошедший через финальную нормализацию'
      - Embedding без каких-либо изменений
    correct: 2
    explanation: >-
      Цикл по блокам не выполнится, но финальный LayerNorm расположен после него
      и сработает всегда. Поэтому модель вернёт нормализованные эмбеддинги той
      же формы.
  - question: >-
      В блоке на 768 измерений bias у линейных слоёв даёт 6912 параметров, а на
      12 блоках — 82 944 против 124 миллионов. Почему bias всё равно часто
      убирают?
    options:
      - Без bias модель всегда обучается точнее
      - Экономия составляет почти четверть модели
      - >-
        Пользы от bias мало, а дополнительный тензор усложняет ядра и
        шардирование
      - При наличии bias нельзя вычислить градиент эмбеддингов
    correct: 2
    explanation: >-
      Экономия параметров невелика, но удаление bias упрощает вычислительные
      ядра и распределение параметров между устройствами. При этом заметной
      пользы для качества bias обычно не даёт.
---

Дальше в курсе этот блок станет единицей повторения: всё, что ты соберёшь потом — полная GPT, генерация, обучение — опирается на то, что вход и выход блока имеют одну форму B×T×D и что стек можно нарастить, не меняя ни строчки внутри.

Спотыкаются обычно на двух вещах. Первая: совпадающие формы forward принимают за доказательство, что архитектуры одинаковые — а различие живёт в backward, в градиентах ([шаг 10](#step-10)). Вторая: маска. Её место — до softmax, иначе будущее успевает попасть в знаменатель нормировки и просочиться в веса прошлого.

Проверь себя на вопросах выше — они по всему уроку, не только по последнему экрану.
