---
id: 016-lockin-check
type: check
title: 'Проверяем, что придётся менять при отказе от LangChain'
source_anchor: '### The trap: instrumenting at the wrong layer'
check:
  - question: >-
      Какой мысленный эксперимент лучше всего выявляет привязку телеметрии к
      агентному фреймворку?
    options:
      - >-
        Посчитать, сколько точек инструментирования придётся переделать при
        переходе с LangChain на собственный цикл агента
      - Сравнить ответы двух моделей на одном промпте
      - Измерить среднее число токенов во всех вызовах
    correct: 0
    explanation: >-
      Если смена фреймворка требует восстанавливать датчики во множестве цепочек
      и узлов, телеметрия зависит от его внутренних конструкций.
  - question: >-
      Почему перехват вызовов OpenAI SDK обычно продолжает работать после замены
      LangChain собственным циклом агента?
    options:
      - >-
        Граница обращения к модели через SDK может остаться прежней при другой
        внутренней архитектуре агента
      - >-
        SDK автоматически воспроизводит все внутренние узлы удалённого
        фреймворка
      - OpenAI хранит полную структуру любого агентного приложения
    correct: 0
    explanation: >-
      Фреймворк управляет внутренней логикой агента, а SDK остаётся стабильной
      границей обращения к модели. Поэтому установленный на этой границе
      перехват не зависит от устройства цикла.
---

Проверь себя мысленным экспериментом: завтра ты удаляешь LangChain и пишешь собственный цикл агента. Составь список мест, где придётся восстанавливать телеметрию. Если в нём все цепочки и узлы, датчики стоят слишком высоко и зависят от фреймворка. Если вызов SDK остаётся прежним и наблюдение продолжает работать, ты сохранил переносимость, о которой говорилось в [шаге 15](#step-15).

Частая ошибка — считать глубокую детализацию автоматически лучшей. Она полезна, но цена проявляется именно при замене основы.

> 🎒 **На пальцах.** На фабрике ты считываешь штрихкод после упаковки товара. Упаковочный робот можно заменить: сканер у выхода продолжит считать коробки. Но если датчик встроен в руку конкретного робота, вместе с роботом придётся менять и учёт.
