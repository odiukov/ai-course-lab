- concept: граница eps-окрестности в DBSCAN включается благодаря нестрогому сравнению расстояния
  question: Расстояние от точки до кандидата в точности равно eps. Должен ли кандидат войти в eps-окрестность при условии distance <= eps?
  explanation: Условие distance <= eps включает точки, расположенные точно на границе радиуса. Строгое сравнение distance < eps дало бы другую окрестность.
  kind: choice
  options:
    - Нет, на границе кандидат всегда считается шумом
    - Да, нестрогое сравнение включает границу
    - Только если кандидат уже посещён
  correct: 1
  id: 031-dbscan-1
  fingerprint: 68de3ab3
- concept: visited отвечает за исследование окрестности точки, а labels — за её принадлежность кластеру
  question: Заполни условие, которое запрещает повторно исследовать окрестность уже обработанной точки.
  explanation: 'Проверять нужно visited: этот массив хранит факт исследования окрестности. Метка в labels решает другую задачу — фиксирует кластер или шум.'
  kind: cloze
  template: |-
    if ___[i]:
        continue
  answer: visited
  accept:
    - visited
  id: 031-dbscan-2
  fingerprint: 7c7c0976
- concept: простая реализация region_query выполняет полный перебор точек без пространственного индекса
  question: Почему функция region_query в простой реализации DBSCAN вычисляет расстояние до каждой точки набора?
  explanation: Полный перебор гарантирует, что ни один сосед не будет пропущен. Ускорить поиск на подходящих данных можно пространственными структурами, но в простой реализации их нет.
  kind: open
  reference: 'Потому что такая реализация не использует пространственный индекс: чтобы найти всех кандидатов внутри eps-окрестности, она исчерпывающе проверяет расстояние до каждой точки.'
  id: 031-dbscan-3
  fingerprint: eb7ad8f0
