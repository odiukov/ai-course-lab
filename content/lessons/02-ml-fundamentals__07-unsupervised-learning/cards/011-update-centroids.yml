- concept: пустой кластер в K-Means можно переинициализировать случайной точкой из исходных данных
  question: Чем заполнить пропуск, чтобы обновление центроидов корректно обработало пустой кластер?
  explanation: У пустого кластера нельзя вычислить среднее. Случайная точка из data становится новым центроидом и позволяет продолжить следующую итерацию K-Means.
  kind: cloze
  template: |-
    if not cluster:
        new_centroids.append(___)
        continue
  answer: random.choice(data)
  accept:
    - random.choice(data).copy()
  id: 011-update-centroids-1
  fingerprint: 83f6a2b6
