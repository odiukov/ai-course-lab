---
id: 049-final-quiz
type: quiz
title: 'Итоговая проверка: Unsupervised Learning'
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты назначил каждую точку ближайшему центроиду. Что должно произойти дальше
      в K-Means?
    options:
      - 'Остановиться: раз точки распределены, кластеры уже готовы'
      - >-
        Пересчитать каждый центроид как среднее точек его кластера и снова
        выполнить назначение
      - Переместить каждый центроид к самой далёкой точке
      - Увеличить число кластеров и начать заново
    correct: 1
    explanation: >-
      K-Means чередует назначение точек и пересчёт центроидов, пока разбиение не
      перестанет заметно меняться.
  - question: >-
      Для K равных 2, 3 и 4 silhouette равен 0,52, 0,68 и 0,41, а inertia с
      каждым K уменьшается. Какой выбор лучше всего поддерживают эти числа?
    options:
      - 'K равно 4, потому что inertia там обязательно меньше'
      - 'K равно 2, потому что меньшее число кластеров всегда надёжнее'
      - >-
        K равно 3, потому что silhouette указывает на наиболее цельное и
        разделённое разбиение
      - >-
        Выбрать нельзя, потому что inertia и silhouette нельзя использовать
        вместе
    correct: 2
    explanation: >-
      Уменьшение inertia при росте K ожидаемо само по себе, а максимальный
      silhouette среди проверенных вариантов поддерживает K, равное 3.
  - question: >-
      DBSCAN пометил точку как шум, но GMM не считает её маловероятной. Что
      разумнее заключить?
    options:
      - 'DBSCAN сломан, потому что детекторы обязаны совпадать'
      - 'Точку стоит исследовать: методы замечают разные виды отклонений'
      - 'GMM всегда прав, потому что использует вероятности'
      - Точку нужно немедленно удалить из данных
    correct: 1
    explanation: >-
      DBSCAN ищет недостаток локальной плотности, а GMM — низкую вероятность по
      своей модели распределения. Несогласие даёт повод для проверки, а не
      автоматический приговор.
  - question: Какое сопоставление задачи и метода наиболее осмысленно?
    options:
      - >-
        Изогнутые плотные группы — K-Means; вероятности принадлежности — DBSCAN;
        история слияний — GMM
      - >-
        Компактные группы — DBSCAN; шум — иерархическая кластеризация;
        вероятности — K-Means
      - >-
        История слияний — K-Means; компактные группы — GMM; изогнутые группы —
        Ward
      - >-
        Компактные группы — K-Means; плотные группы сложной формы и шум —
        DBSCAN; история слияний — иерархия; мягкие принадлежности — GMM
    correct: 3
    explanation: >-
      Каждый метод подходит под своё представление о структуре данных: центры,
      плотность, последовательность слияний или вероятностную смесь.
---

Перед тобой новый набор данных, и главное испытание — не запустить знакомый алгоритм, а заметить, какое устройство групп ты предполагаешь. Дальше в курсе это поможет тебе сравнивать модели без готовых ответов, выбирать метрику и относиться к аномалиям как к сигналам для проверки. Частая ловушка — принять красивое разбиение или минимальную inertia за доказательство правильности.

> 🎒 **На пальцах.** Представь фотографа с четырьмя объективами. Один хорошо снимает ровные портреты, другой охватывает извилистую улицу, третий показывает глубину сцены, четвёртый мягко размывает границы. Лучшего объектива вообще не существует: сначала ты смотришь на сцену и решаешь, какое её свойство важно сохранить. С алгоритмами кластеризации выбор устроен так же.
