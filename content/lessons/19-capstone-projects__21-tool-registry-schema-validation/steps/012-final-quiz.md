---
id: 012-final-quiz
type: quiz
title: Проверяем контракт целиком
source_anchor: '## Going further'
check:
  - question: Какое усложнение появляется при добавлении поддержки локальных $ref?
    options:
      - Строки приходится автоматически преобразовывать в числа
      - Нужно разрешать ссылки и учитывать возможные циклы в графе схем
      - Все свойства объектов становятся обязательными
      - Обработчики приходится запускать во время регистрации
    correct: 1
    explanation: >-
      $ref связывает разные части схемы и может образовывать циклы, поэтому
      простого обхода дерева уже недостаточно.
  - question: >-
      Какую роль выполняет транспорт JSON-RPC поверх stdio в следующем слое
      системы?
    options:
      - Показывает реестр клиенту модели и доставляет сообщения
      - Исправляет некорректные схемы при регистрации
      - Преобразует строки в числовые аргументы
      - Запрещает обработчикам выполнять опасные действия
    correct: 0
    explanation: >-
      Реестр описывает инструменты и форму аргументов, а транспорт предоставляет
      этот каталог клиенту модели и переносит вызовы.
  - question: >-
      В массиве из четырёх объектов неверное поле находится в третьем элементе.
      Что должен сделать валидатор?
    options:
      - Проверить только первый элемент как образец
      - Проверить все элементы и указать путь с индексом третьего
      - Сообщить только о неверном массиве без точного пути
      - Пропустить проверку объектов внутри массива
    correct: 1
    explanation: >-
      Схема items применяется к каждому элементу, а нулевой отсчёт делает
      индексом третьего элемента число 2.
  - question: >-
      Объект содержит все обязательные поля и ещё одно поле, которого нет в
      properties. Что произойдёт в рамках реализованного подмножества?
    options:
      - Лишнее поле обязательно вызовет ошибку
      - Весь объект будет пропущен без проверки
      - 'Известные поля проверятся, а лишнее само по себе не вызовет ошибку'
      - Лишнее поле автоматически добавится в схему
    correct: 2
    explanation: >-
      Запрет лишних полей требует additionalProperties: false, а поддержка этого
      ключевого слова оставлена за рамками урока.
---

Дальше транспорт будет показывать реестр клиенту модели, а диспетчер — передавать вызовы инструментам. Здесь особенно легко спутать два разных доверия: корректность самой схемы проверяется при регистрации, а соответствие аргументов этой схеме — при вызове. Для вложенных данных важны рекурсивный проход, проверка каждого элемента массива и точный путь ошибки из [шагов 8](#step-8)–[11](#step-11).
