---
id: 002-contract-check
type: check
title: Фиксируем границы реестра
source_anchor: '## What the validator is and is not'
check:
  - question: >-
      В журнале остались аргументы со строкой «42», хотя схема ожидает число.
      Что сделает повторный запуск валидатора?
    options:
      - Преобразует строку в число и примет аргументы
      - 'Вернёт ту же ошибку типа, что и раньше'
      - Удалит неподходящее поле и продолжит
    correct: 1
    explanation: >-
      Валидатор не приводит типы и ничего не обрезает. При одинаковых входных
      данных он снова выдаёт тот же результат.
  - question: >-
      Аргументы прошли схему инструмента из доверенного реестра. Что теперь
      можно утверждать?
    options:
      - >-
        Аргументы имеют ожидаемую форму, но обработчик всё ещё может быть
        опасным
      - Обработчик безопасен и может работать без ограничений
      - Все значения внутри аргументов автоматически стали доверенными
    correct: 0
    explanation: >-
      Реестр и валидатор подтверждают форму данных, а не безопасность поведения
      обработчика.
---

После этого фрагмента тебе важно удерживать границу обещаний реестра из [шага 1](#step-1). Валидатор предсказуемо проверяет дерево схемы, не запускает обработчик, не исправляет вход и не превращает строку «42» в число. Поэтому старый журнал можно проверить повторно и получить те же ошибки. Частая ловушка — принять успешную проверку формы за разрешение доверять всему вызову. Это разные гарантии: форма подтверждена, безопасность ещё нет.

> 🎒 **На пальцах.** Представь сортировочный стол для посылок с шаблоном размеров. Коробка либо проходит через нужное отверстие, либо нет: сотрудник не сминает её и не перекладывает содержимое. Но подходящий размер не доказывает, что внутри безопасный предмет. Шаблон отвечает только за форму посылки — ровно на этом заканчивается его обещание.
