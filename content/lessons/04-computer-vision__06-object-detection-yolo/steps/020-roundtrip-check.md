---
id: 020-roundtrip-check
type: check
title: 'Проверка: почему encode и decode должны давать roundtrip'
source_anchor: '### Step 3: Box encoding and decoding'
check:
  - question: >-
      Почему нельзя записать смещение центра внутри клетки напрямую в tx, если
      decode применяет к tx функцию sigmoid?
    options:
      - Смещение пройдёт через лишний sigmoid и восстановится другим значением
      - Ширина рамки станет равна ширине anchor box
      - Индекс клетки автоматически увеличится
    correct: 0
    explanation: >-
      Decode ожидает логит смещения. Если передать само смещение, sigmoid
      применится не как обратная операция и центр рамки сдвинется.
  - question: >-
      Что обеспечивает восстановление обычной рамки после последовательных
      encode и decode?
    options:
      - 'Логит отменяется sigmoid, а логарифм отношения размеров — экспонентой'
      - Sigmoid и exp обнуляют все поправки рамки
      - Anchor box полностью заменяет координаты исходной рамки
    correct: 0
    explanation: >-
      Для центра используются взаимно обратные logit и sigmoid, а для размеров —
      log и exp. Поэтому при согласованных параметрах клетки, stride и anchor
      box исходные координаты восстанавливаются с погрешностью вычислений.
---

Теперь проверь, состыкуются ли функции из [шага 19](#step-19) и [шага 17](#step-17): закодированная рамка после декодирования должна вернуться на прежнее место. Обычно здесь спотыкаются, записывая смещение центра напрямую: тогда `sigmoid` применяется лишний раз. Ещё одно исключение — центр ровно на границе клетки, где `clip` вносит крошечный сдвиг.

> 🎒 **На пальцах.** Представь аппарат для вакуумной упаковки куртки и режим обратного наполнения воздухом. Если первый режим сжал её именно так, как ожидает второй, ты снова получишь прежнюю куртку. Но если вместо упаковки просто засунуть куртку внутрь, обратный режим раздует её ещё раз. Roundtrip проверяет именно согласованность двух операций.
