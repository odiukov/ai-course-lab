- concept: Flash Attention должен быть встроен в реализацию attention, а не обёрнут вокруг готового Transformer-слоя
  question: Команда хочет заменить обычное вычисление attention на Flash Attention внутри готового стандартного Transformer-слоя. Почему внешней обёртки вокруг слоя недостаточно?
  explanation: 'Flash Attention меняет способ вычисления матрицы attention: промежуточные данные обрабатываются блоками без материализации всей таблицы оценок. Поэтому его подключают внутри attention; готовый слой без соответствующей точки расширения нельзя модернизировать одной внешней обёрткой.'
  kind: choice
  options:
    - Flash Attention изменяет размерность выхода всего Transformer-слоя
    - Flash Attention управляет внутренним вычислением attention и использованием памяти, поэтому нужна точка замены внутри attention
    - Flash Attention работает только после FFN
    - Flash Attention требует удалить residual-соединения
  correct: 1
  id: 040-svoi-blok-1
  fingerprint: f371e589
- concept: собственный Transformer-блок сохраняет точки замены для современных вариантов его компонентов
  question: Объясни своими словами, почему продакшен-модель может использовать собственный Transformer-блок, даже если библиотека предоставляет готовый стандартный слой.
  explanation: Причина не в отказе от стандартной структуры Transformer, а в необходимости менять её внутренние компоненты. Библиотечный слой может не поддерживать нужные механизмы либо не давать заменить их в подходящем месте.
  kind: open
  reference: Готовый слой фиксирует конкретные реализации attention, нормализации, FFN и позиционного кодирования. Собственный блок позволяет независимо подключать Flash Attention, GQA или MLA, RoPE, RMSNorm и SwiGLU, сохраняя общий интерфейс Transformer-блока.
  id: 040-svoi-blok-2
  fingerprint: 2d4838df
