---
id: 006-check-residual
type: check
title: 'Проверка: подслой выдал нули — что придёт в «+»'
source_anchor: '### Encoder block (used by BERT, T5 encoder)'
check:
  - question: >-
      Первый подслой блока (MHA) выдал на всех позициях одни нули. Что придёт в
      первый «+» и что будет на его выходе?
    options:
      - 'В «+» придут ноль и исходный x, на выходе получится x без изменений'
      - 'В «+» придут только нули, на выходе будет ноль, и сигнал дальше пропадёт'
      - 'В «+» придёт x, но он умножится на ноль, и на выходе будет ноль'
      - 'Блок выдаст ошибку: складывать нули с x бессмысленно'
    correct: 0
    explanation: >-
      В «+» сходятся две ветки: прямая (x) и та, что прошла через подслой
      (ноль). x + 0 = x, поэтому бесполезный подслой просто ничего не меняет и
      не ломает сигнал.
  - question: 'От чего отходит вторая развилка блока — та, что ведёт в FFN?'
    options:
      - 'От того же входного x, что и первая развилка'
      - 'От выхода первого «+», то есть от результата после attention'
      - От выхода LN перед первым подслоем
    correct: 1
    explanation: >-
      Подслои идут строго друг за другом: FFN работает уже с тем, что получилось
      после сложения attention с residual, а не с исходным входом блока.
---

После этого куска ты должен уметь провести пальцем по блоку и сказать, что окажется в каждой точке сложения. Проверь себя на крайнем случае: подслой выдал нули — в «+» всё равно приходит прямая ветка с исходным `x`, и на выходе `x`. Отсюда и берётся безопасность глубокой стопки из [шага 5](#step-5): блок умеет «ничего не делать».

Спотыкаются обычно на двух местах. Первое — думают, что нули из подслоя обнуляют весь поток; на самом деле они складываются, а не умножаются. Второе — считают, что обе развилки отходят от входного `x`. Нет: вторая начинается уже после первого «+».
