---
id: 033-check-512
type: check
title: 'Проверка: при d = 512 что тяжелее — attention или FFN'
source_anchor: '### Parameter count'
check:
  - question: >-
      Берём один блок с d = 512, обычный MHA и расширение FFN 2,6. Что весит
      больше?
    options:
      - 'Attention: примерно 1,05 млн против 2,05 млн у FFN'
      - 'FFN: примерно 2,05 млн против 1,05 млн у attention'
      - 'Поровну: обе части дают примерно по 1,5 млн'
      - 'Attention, потому что у него четыре матрицы, а у FFN всего две'
    correct: 1
    explanation: >-
      Attention даёт 4 на 512 в квадрате, это около 1,05 млн. FFN на SwiGLU даёт
      3 умножить на 2,6 и на 512 в квадрате, это около 2,05 млн — вдвое больше.
  - question: >-
      У Llama 3 8B разрыв не вдвое, а вчетверо: 176 млн на FFN против 42 млн на
      attention. Откуда лишняя разница?
    options:
      - 'У неё FFN расширяется сильнее, поэтому FFN и подрос'
      - 'У неё больше слоёв, и с каждым слоем attention дешевеет'
      - 'GQA урезала K и V, и подешевел именно attention, а не FFN'
      - 'Нормализации там весят столько же, сколько attention'
    correct: 2
    explanation: >-
      GQA хранит K и V только для 8 голов вместо 32, поэтому attention стоит 2,5
      на d в квадрате вместо 4 на d в квадрате. FFN этого урезания не касается,
      и разрыв растёт.
---

Ты только что написал `block_params` в [шаге 32](#step-32) — теперь подставь в него руками маленькие числа и посмотри, что выйдет. Возьми d = 512 и расширение 2,6.

После этого куска ты должен уметь на глаз сказать, какая половина блока тяжелее, и объяснить почему — не «потому что так в таблице», а из двух формул [шага 31](#step-31).

Спотыкаются обычно на двух местах. Первое: интуиция подсказывает, что раз attention — главная идея архитектуры, там и веса. Второе: забывают, что у SwiGLU три матрицы, и считают FFN как две — тогда числа сходятся неправильно, и вывод переворачивается.

Проверь себя.
