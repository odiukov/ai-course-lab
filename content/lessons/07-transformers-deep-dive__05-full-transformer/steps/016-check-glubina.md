---
id: 016-check-glubina
type: check
title: 'Проверка: какой вариант выдержит 32 слоя'
source_anchor: '### Pre-norm vs post-norm'
check:
  - question: >-
      Ты собираешь модель из 32 блоков подряд. В каком варианте путь от входа
      первого блока к выходу последнего идёт без единой нормализации?
    options:
      - >-
        В том, где нормализуется копия, уходящая в подслой, а к сумме
        прибавляется исходный x
      - >-
        В том, где сначала считается x плюс подслой от x, а потом результат
        нормализуется
      - >-
        Оба варианта одинаковы: нормализация всё равно стоит по одному разу на
        блок
      - 'Ни в каком: сложение и есть форма нормализации'
    correct: 0
    explanation: >-
      Когда нормализация стоит внутри ветки подслоя, слагаемое x переходит из
      блока в блок нетронутым — за 32 блока на магистрали не встретится ни одной
      нормализации.
  - question: Почему post-norm тяжело обучать в глубину без аккуратного warmup?
    options:
      - 'Он вообще не нормализует активации, и они разъезжаются по масштабу'
      - >-
        Он нормализует уже сложенную сумму, поэтому на каждом из 32 блоков
        сигнал магистрали пересчитывается заново
      - 'Он требует вычитать среднее, а pre-norm обходится без этого'
      - 'В нём нет остаточной связи, поэтому вход блока теряется'
    correct: 1
    explanation: >-
      В post-norm нормализация стоит поперёк магистрали: сумма x плюс подслой
      пересчитывается на каждом блоке, и за 32 блока прямой дороги от входа к
      выходу не остаётся.
---

Ты написал обе версии подслоя — [pre-norm](#step-13) и [post-norm](#step-15) — и по коду они отличаются одной перестановкой скобок. Теперь надо решить, какую из них ставить в стопку из 32 блоков.

Здесь обычно спотыкаются на трёх вещах. Первая: кажется, что pre-norm «не нормализует» — нормализует, но только копию, уходящую в подслой. Вторая: путают, где именно стоит нормализация относительно сложения, а решает всё именно это. Третья: post-norm считают неправильным — он рабочий, просто с ростом глубины требует аккуратного warmup, поэтому к 2019-му и уступил место. Llama, Qwen, GPT-3+, Mistral — все на pre-norm.

Ответь, глядя на то, что происходит со слагаемым `x`.
