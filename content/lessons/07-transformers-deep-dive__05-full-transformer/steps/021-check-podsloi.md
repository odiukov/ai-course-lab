---
id: 021-check-podsloi
type: check
title: 'Проверка: сколько подслоёв у энкодера, декодера и GPT'
source_anchor: '### Decoder block (used by GPT, T5 decoder)'
check:
  - question: >-
      Из полного декодера выбросили cross-attention — получился блок GPT.
      Сколько подслоёв осталось и совпадает ли этот блок с энкодерским?
    options:
      - 'Осталось два подслоя, и блок полностью совпадает с энкодерским'
      - >-
        Осталось два подслоя, но self-attention в нём с маской, а у энкодера —
        без
      - 'Остался один подслой: без cross-attention FFN тоже теряет смысл'
      - >-
        Осталось три подслоя: cross-attention нельзя убрать, он только перестаёт
        получать вход
    correct: 1
    explanation: >-
      Числом подслоёв GPT и энкодер совпадают — по два, — но энкодер смотрит во
      все стороны, а GPT только назад: его self-attention маскированный.
  - question: >-
      В связке энкодер-декодер закоротили cross-attention: он стал отдавать
      нули. Что перестанет работать?
    options:
      - Декодер перестанет видеть свои предыдущие позиции
      - Декодер потеряет нормализацию и residual в среднем подслое
      - >-
        Декодер перестанет получать хоть что-то из энкодера и будет писать
        вслепую
      - Энкодер перестанет читать вход
    correct: 2
    explanation: >-
      Cross-attention — единственный канал из энкодера в декодер; остальные два
      подслоя работают только с тем, что декодер написал сам.
---

Дальше по курсу тебе придётся с одного взгляда на схему говорить, какой это блок. Быстрее всего это делается счётом подслоёв: у энкодера их два ([шаг 18](#step-18)), у полного декодера — три ([шаг 19](#step-19)), у GPT — снова два.

И вот тут обычно спотыкаются дважды. Первое: раз у GPT и у энкодера по два — значит, блоки одинаковые? Нет: у GPT self-attention маскированный, у энкодера — нет, а это разные вещи. Второе: думают, что cross-attention — просто ещё одно внимание. Он единственный проводник из энкодера в декодер; уберёшь его — и подглядывать станет некуда.
