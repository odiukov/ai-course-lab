---
id: 011-check-3-4
type: check
title: 'Проверка: вектор [3, 4] через обе нормализации'
source_anchor: '### Step 1: the building blocks'
check:
  - question: >-
      Вектор из двух чисел, 3 и 4. Ты прогоняешь его через LayerNorm и через
      RMSNorm. Что получится?
    options:
      - >-
        В обоих случаях оба числа останутся положительными, разойдутся только
        величины
      - >-
        После LayerNorm одно число станет отрицательным, а после RMSNorm оба
        останутся положительными
      - >-
        После RMSNorm одно число станет отрицательным, а после LayerNorm оба
        останутся положительными
      - >-
        В обоих случаях получится один и тот же результат, потому что делят на
        похожие числа
    correct: 1
    explanation: >-
      LayerNorm сначала вычитает среднее 3,5, и 3 уходит в минус: получается
      примерно минус 0,5 и плюс 0,5. RMSNorm среднее не трогает, а только делит
      на 3,54, поэтому знаки исходных чисел сохраняются: примерно 0,85 и 1,13.
  - question: Почему RMSNorm считается дешевле LayerNorm?
    options:
      - Она обрабатывает меньше чисел в векторе
      - >-
        Она не выполняет один из проходов по вектору — тот, который считает
        среднее и вычитает его
      - Она работает с целыми числами вместо дробных
      - 'У неё нет обучаемых параметров, а у LayerNorm они есть'
    correct: 1
    explanation: >-
      Разница между двумя функциями — ровно одна выброшенная операция:
      центрирование. RMSNorm сразу делит на корень из среднего квадрата, экономя
      проход по вектору.
---

Здесь проверяется одна вещь: умеешь ли ты руками провести короткий вектор через обе формулы из [шага 8](#step-8) и [шага 9](#step-9) и предсказать, чем результаты будут отличаться.

Спотыкаются обычно на знаках. Кажется, что раз обе функции «приводят числа к одному масштабу», то и на выходе они дадут похожее. Но LayerNorm двигает вектор к нулю целиком — часть чисел неизбежно уезжает в минус, даже если на входе были одни плюсы. RMSNorm только сжимает, знаки остаются как были.

Второе место — сам делитель. У RMSNorm под корнем среднее квадратов исходных чисел, а не отклонений от среднего: для [3, 4] это (9+16)/2, а не что-то, посчитанное после вычитания 3,5.
