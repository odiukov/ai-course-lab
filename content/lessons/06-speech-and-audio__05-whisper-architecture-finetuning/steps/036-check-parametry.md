---
id: 036-check-parametry
type: check
title: 'Проверка: зачем temperature=0.0 и condition_on_previous_text=False'
source_anchor: '### Step 1: run Whisper out of the box'
check:
  - question: >-
      Ты дважды прогнал один и тот же файл через transcribe без указания
      temperature и получил два разных текста. Что это значит?
    options:
      - Модель подгружает разные веса при каждом запуске
      - >-
        Сработала цепочка отката: результат при 0.0 не прошёл эвристики, и
        модель пересэмплировала при 0.2 или 0.4
      - 'Аудио читается с разной длиной окна, поэтому мелы получаются разные'
      - Автоопределение языка каждый раз выбирает другой тег
    correct: 1
    explanation: >-
      По умолчанию температура задана списком 0.0, 0.2, 0.4 и так далее: если
      декодирование при 0.0 показалось плохим по эвристикам, модель пробует
      снова со сэмплированием. Жёсткое temperature=0.0 убирает эту лотерею.
  - question: >-
      Ты оставил condition_on_previous_text=True на часовой записи. Чем это
      опасно?
    options:
      - >-
        Куски будут обрабатываться медленнее, потому что промпт становится
        длиннее
      - 'Таймкоды съедут, потому что смещение куска перестанет учитываться'
      - Модель начнёт определять язык заново на каждом куске
      - >-
        Придуманный текст из одного куска попадёт в промпт следующего, и повторы
        будут нарастать по файлу
    correct: 3
    explanation: >-
      При включённом флаге предыдущий вывод подмешивается в промпт декодера,
      поэтому одна ошибка становится контекстом для следующего куска и повторы
      разгоняются. Значение False разрывает эту связь.
---

После кода из [шага 34](#step-34) ты должен уметь объяснить эти два аргумента не как «магические строчки из чужого сниппета», а как два разных способа сделать вывод предсказуемым.

`temperature=0.0` убирает случайность внутри одного куска: цепочка отката из [шага 35](#step-35) больше не подмешивает сэмплирование, и один и тот же файл даёт один и тот же текст. `condition_on_previous_text=False` убирает зависимость между кусками: то, что модель выдала на предыдущих 30 секундах, не становится частью промпта декодера для следующих.

Обычно спотыкаются здесь: считают, что нули и False улучшают качество расшифровки. Не улучшают. Они делают результат воспроизводимым — а без этого ты не сможешь понять, помогло ли твоё изменение в пайплайне или просто выпала другая карта.
