- concept: матричная форма embedding запроса сохраняется передачей запроса как батча
  question: Заполни аргумент вызова так, чтобы embedding запроса сохранил ось батча и подходил для последующего транспонирования.
  explanation: Список вокруг question передаёт запрос как батч. В результате encode возвращает матрицу, которую можно транспонировать перед умножением на матрицу embeddings корпуса.
  kind: cloze
  template: q_emb = encoder.encode(___, normalize_embeddings=True)
  answer: '[question]'
  accept:
    - '[ question ]'
  id: 016-retrieve-top-k-1
  fingerprint: '15407926'
- concept: embeddings запроса и корпуса должны находиться в общем векторном пространстве
  question: Embeddings корпуса заранее построены моделью SentenceTransformer. Как следует кодировать новый запрос для корректного сравнения по скалярному произведению?
  explanation: 'Одинаковой размерности векторов недостаточно: координаты разных моделей обычно имеют разный смысл. Запрос и корпус нужно кодировать совместимым энкодером и одинаково нормализовать.'
  kind: choice
  options:
    - Тем же энкодером и с той же схемой предобработки
    - Любым энкодером с совпадающей размерностью выхода
    - Отдельным энкодером, обученным только на запросах
    - Счётчиком слов без дополнительного преобразования
  correct: 0
  id: 016-retrieve-top-k-2
  fingerprint: ef5a5c2c
