- concept: бинарный результат exact_match получается преобразованием равенства нормализованных строк в целое число
  question: Заполни тело функции так, чтобы она возвращала целочисленный индикатор полного совпадения после нормализации.
  explanation: Каждый ответ нормализуется готовой функцией, после чего строки сравниваются целиком. Результат равенства — логическое значение, а int преобразует его в единицу или ноль.
  kind: cloze
  template: |-
    def exact_match(prediction, reference):
        return ___
  answer: int(normalize_answer(prediction) == normalize_answer(reference))
  accept:
    - int(normalize_answer(reference) == normalize_answer(prediction))
  id: 030-exact-match-code-1
  fingerprint: b5d1802d
