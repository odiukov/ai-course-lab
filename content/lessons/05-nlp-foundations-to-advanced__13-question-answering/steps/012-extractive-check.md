---
id: 012-extractive-check
type: check
title: 'Проверка: корректный span, невозможный вопрос и низкий score'
source_anchor: '### Step 1: extractive QA with a pretrained model'
check:
  - question: >-
      Какие две проверки нужны, чтобы оценить корректность результата extractive
      QA?
    options:
      - Проверить только высокий score
      - >-
        Убедиться, что passage[start:end] совпадает с answer, а сам фрагмент
        отвечает на вопрос
      - 'Убедиться только в том, что answer встречается где-нибудь в passage'
    correct: 1
    explanation: >-
      Совпадение среза с answer подтверждает техническую корректность границ.
      Отдельная смысловая проверка нужна, потому что правильно извлечённый
      фрагмент может не отвечать на вопрос.
  - question: >-
      На невозможный вопрос пайплайн без handle_impossible_answer вернул лучший
      span со score 0.03. Порог принятия равен 0.2. Как поступить?
    options:
      - 'Принять span, поскольку он лучший среди кандидатов'
      - 'Заменить score нулём, но всё равно показать span'
      - Отклонить результат как ответ с недостаточной уверенностью
    correct: 2
    explanation: >-
      Без handle_impossible_answer пайплайн может вернуть лучший span даже для
      вопроса без ответа. Поскольку 0.03 ниже порога 0.2, результат следует
      трактовать как отказ.
---

Ты уже умеешь получать границы из [шага 8](#step-8) и учитывать null score из [шага 11](#step-11). Теперь проверь результат в два приёма: совпадает ли `passage[start:end]` с `answer` и отвечает ли этот фрагмент на вопрос. Даже правильный срез может быть смысловой ошибкой.

Если вопрос невозможен, `handle_impossible_answer=True` позволяет вернуть пустой ответ, когда побеждает null. Без флага ориентируйся на заранее выбранный порог: низкий `score` — повод отказаться, а не довериться лучшему из слабых вариантов.

> 🎒 **На пальцах.** Ты проверяешь билет в гардеробе: номер действительно ведёт к конкретному пальто, но это ещё не значит, что пальто твоё. Если описание не совпало или уверенность гардеробщика слишком мала, безопаснее сказать «не найдено», чем выдать чужую вещь.
