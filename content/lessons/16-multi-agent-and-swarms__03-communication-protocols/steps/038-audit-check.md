---
id: 038-audit-check
type: check
title: Какие поля аудита должны сохраниться при исключении обработчика?
source_anchor: '### Step 4: ACP-Style Audit Trail'
check:
  - question: Обработчик выбросил исключение. Какая запись аудита должна остаться?
    options:
      - >-
        Только статус failed и текст ошибки — остальное относится к успешным
        запускам
      - >-
        Снимок входа и статус in-progress: результат не получен, значит попытка
        не закончена
      - >-
        Идентификаторы запуска и сессии, имя агента, снимок входа, пустой выход,
        ошибка в траектории, времена начала и завершения, статус failed
      - >-
        Ничего: незавершённый запуск удаляется из журнала, чтобы не искажать
        статистику
    correct: 2
    explanation: >-
      Сбой сам является исходом запуска, который потом расследуют. Поэтому
      сохраняются контекст (кто, в какой сессии), вход, ошибка в траектории, обе
      временные границы и терминальный статус failed.
  - question: Почему при исключении нельзя заменять сохранённый вход пустым массивом?
    options:
      - Потому что пустой массив нельзя клонировать
      - >-
        Потому что без исходного запроса нельзя установить, что именно привело к
        сбою и как его воспроизвести
      - Потому что статус failed требует непустого выхода
      - Потому что вход автоматически становится частью ответа агента
    correct: 1
    explanation: >-
      Снимок входа связывает ошибку с конкретными данными запуска. Без него
      запись сообщает только факт отказа и не даёт ни воспроизведения, ни ответа
      на вопрос, чья ошибка — запроса или обработчика.
---

В [шаге 36](#step-36) ты видел полный снимок запуска, а в [шаге 37](#step-37) собрал его в `audit_run`. Теперь обработчик падает до результата. Легко оставить лишь `failed`, но такая запись почти бесполезна: неизвестно, кто запускался, с каким входом и когда.

Сохрани `runId`, имя агента, снимок входа, `sessionId`, `startedAt`; выход останется пустым. В траекторию добавь ошибку с отметкой времени, поставь `failed` и заполни `completedAt`. Сбой завершает попытку, но не стирает её историю.
