- concept: MIME-тип задаёт синтаксис представления, но не структуру полей, поэтому совместимость требует отдельной версии схемы
  question: Агент по-прежнему отдаёт ответ с типом `application/json`, но поле результата из числа стало вложенным объектом. Почему совпадение MIME-типа не защитило клиента?
  explanation: Разбор синтаксиса и понимание структуры — разные уровни. Ответ остаётся корректным JSON, но контракт данных изменился, и это изменение фиксируется только версией схемы.
  kind: choice
  options:
    - MIME-тип говорит только о том, как разбирать байты, и ничего не обещает о наборе полей и их типах
    - MIME-тип обещает структуру, но клиент обязан был перечитать карточку перед каждым вызовом
    - Ошибка в том, что для объектов нужен другой MIME-тип, а `application/json` пригоден только для плоских значений
    - 'Проблемы нет: любой JSON-парсер приводит вложенный объект к числу'
  correct: 0
  id: 058-schema-drift-1
  fingerprint: 5545a91f
- concept: критерий смены версии схемы — способен ли прежний клиент по-прежнему корректно прочитать ответ
  question: Схема результата агента находится в версии 4. Какое из изменений требует объявить версию 5, а не остаться в прежней?
  explanation: Версия меняется, когда клиент, написанный под прежний контракт, перестаёт читать ответ корректно. Добавление необязательного поля таким не является, а переименование и смена типа обязательного поля — является.
  kind: choice
  options:
    - Прежнее обязательное поле переименовано, а его тип изменён
    - Добавлено новое необязательное поле, которое старые клиенты просто игнорируют
    - Расширено человекочитаемое описание навыка в карточке
    - Внутри агента заменена библиотека сериализации при том же выходе
  correct: 0
  id: 058-schema-drift-2
  fingerprint: 31bd6453
- concept: версия карточки агента и версия контракта отдельного навыка живут независимо, потому что навыки меняются в разное время
  question: Клиент видит, что версия карточки агента изменилась. Почему этого недостаточно, чтобы решить вопрос о совместимости нужного ему навыка?
  explanation: 'Один агент может выпустить новую карточку из-за правки одного навыка, не тронув остальные. Решение о вызове принимается по контракту конкретного навыка: набор полей, их типы и версия выходной схемы.'
  kind: choice
  options:
    - Версия карточки описывает выпуск агента целиком; отдельные навыки меняются независимо, поэтому клиенту нужен версионированный контракт именно того навыка, который он вызывает
    - Версия карточки вообще не имеет отношения к агенту и служит счётчиком публикаций реестра
    - 'Достаточно: смена версии карточки означает несовместимость всех навыков сразу'
    - 'Достаточно: версия карточки повторяет версию самого старого навыка'
  correct: 0
  id: 058-schema-drift-3
  fingerprint: 4f007da2
