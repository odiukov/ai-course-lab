---
id: 056-proekt-picker
type: check
title: >-
  Проектируем рекомендацию модели, квантизации и инференс-стека для заданного
  деплоя
source_anchor: '## Ship It'
check:
  - question: >-
      Тебе дают одну GPU на 24 ГБ и просят обслуживать чат. Кто-то предлагает
      MoE на 47B суммарных параметров, аргументируя это тем, что активны лишь
      13B. Что здесь не так?
    options:
      - 'Ничего: считать надо по активным параметрам, 13B в 24 ГБ помещаются'
      - >-
        В память надо загрузить все 47B весов — активные параметры экономят
        вычисления, а не VRAM
      - 'MoE вообще нельзя запускать на одной GPU, только на кластере'
    correct: 1
    explanation: >-
      Роутер выбирает экспертов на каждом токене заново, поэтому все веса должны
      быть под рукой; top-k влияет на FLOPs, а не на объём загруженного.
  - question: >-
      Плотная модель с 8-битными весами влезла в VRAM с большим запасом, но при
      контексте 32k сервер всё равно падает по памяти. Какое решение бьёт в
      причину?
    options:
      - Ужать веса до 4 бит — освободится ещё больше места
      - >-
        Взять модель с большим intermediate_size, у неё лучше пропускная
        способность
      - >-
        Уменьшить batch, длину контекста или квантизовать сам KV-кэш, либо взять
        модель с меньшим числом KV-голов
      - 'Отключить RoPE-масштабирование, чтобы кэш не рос'
    correct: 2
    explanation: >-
      KV-кэш — отдельная от весов статья памяти: он растёт от контекста, batch,
      числа слоёв и KV-голов, и битность весов на него не влияет.
---

Дальше тебе дадут не абстрактную «хорошую модель», а конкретную заявку: тип GPU, объём VRAM, длина контекста, потолок по latency и профиль задачи — чат, код, рассуждения или длинный контекст. Твоя работа — выдать тройку: модель, схему квантизации (урок 11) и стек инференса (урок 12), и объяснить выбор через шесть ручек из [шага 6](#step-6).

Спотыкаются обычно на двух вещах. Первая — путают три размера из [шага 53](#step-53): суммарные веса, активные FLOPs и KV-кэш живут в разных бюджетах, и MoE выигрывает только в одном из них. Вторая — забывают, что квантизация весов не трогает кэш.

Проверь себя на двух заявках, где всё «почти влезает».
