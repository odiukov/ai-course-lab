---
id: 010-proverka-rmsnorm
type: check
title: 'Сравниваем RMSNorm и LayerNorm на векторе [3, −4]'
source_anchor: '### Knob 1: RMSNorm'
check:
  - question: >-
      Ты подаёшь вектор из двух чисел, 3 и минус 4, в RMSNorm с гаммой равной
      единице. На какое число делятся оба элемента?
    options:
      - На минус 0.5 — это среднее по вектору
      - 'На 3.54 — это корень из среднего квадратов, то есть корень из 12.5'
      - На 3.5 — это стандартное отклонение после вычитания среднего
      - На 25 — это сумма квадратов элементов
    correct: 1
    explanation: >-
      RMSNorm берёт квадраты: 9 и 16, их среднее 12.5, корень примерно 3.54.
      Делим оба элемента и получаем примерно 0.85 и минус 1.13.
  - question: >-
      Тот же вектор 3 и минус 4 прогнали через LayerNorm. Что LayerNorm сделает
      такого, чего RMSNorm не делает вообще?
    options:
      - Возведёт элементы в квадрат перед усреднением
      - >-
        Вычтет среднее минус 0.5, то есть сдвинет вектор к 3.5 и минус 3.5, и
        добавит обучаемый сдвиг beta
      - 'Поделит на длину вектора, а не на корень из среднего квадратов'
      - Умножит результат на обучаемый масштаб gamma
    correct: 1
    explanation: >-
      Центрирование (вычитание среднего) и обучаемый сдвиг beta — ровно те две
      операции, которые RMSNorm выбрасывает; умножение на gamma есть у обеих.
---

Одни и те же два числа, две нормализации — и это самая быстрая проверка того, понял ли ты [шаг 7](#step-7) и [шаг 8](#step-8) на уровне арифметики, а не слов.

После этого куска ты должен уметь взять короткий вектор, посчитать для него RMS вручную и сказать, где именно расходятся пути LayerNorm и RMSNorm.

Спотыкаются обычно на двух местах. Первое: путают среднее самих чисел (для 3 и минус 4 это минус 0.5) со средним их квадратов (12.5). Второе: считают, что делитель 3.54 — это стандартное отклонение. Нет: у центрированного вектора отклонение равно 3.5, и совпадение цифр здесь случайное.

Проверь себя на вопросах ниже.
