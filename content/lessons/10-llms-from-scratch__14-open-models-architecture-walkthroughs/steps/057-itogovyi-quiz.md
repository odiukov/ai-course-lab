---
id: 057-itogovyi-quiz
type: quiz
title: >-
  Итоговая проверка: архитектурный диф, параметры, KV-кэш и выбор открытой
  модели
source_anchor: '## Exercises'
check:
  - question: >-
      Mixtral 8x7B выбирает top-2 из 8 экспертов, а DeepSeek V3 — top-8 из 256.
      Что отражает переход от 25% активных экспертов примерно к 3%?
    options:
      - >-
        Скорость автоматически возрастает пропорционально уменьшению доли
        активных экспертов
      - >-
        Более мелкая разреженная специализация повышает ёмкость на активный
        FLOP, хотя веса всех экспертов по-прежнему занимают память
      - >-
        Суммарное число параметров уменьшается вместе с долей выбранных
        экспертов
      - KV-кэш сокращается пропорционально разреженности MoE
    correct: 1
    explanation: >-
      Из большого набора мелких экспертов роутер может собирать более
      разнообразные комбинации для токенов. Вычисляются только выбранные
      эксперты, но память под веса определяется полным набором.
  - question: >-
      KV-кэш Llama 3 405B для контекста 128k переводят из BF16 в FP8 без
      изменения его формы. Что произойдёт с размером кэша?
    options:
      - Он уменьшится ровно вдвое
      - 'Он уменьшится вчетверо, поскольку хранятся и K, и V'
      - 'Он не изменится, поскольку битность относится только к весам'
      - Изменение нельзя определить без числа Q-голов
    correct: 0
    explanation: >-
      При неизменном числе элементов объём пропорционален их битности. FP8
      занимает 8 бит на элемент вместо 16 у BF16, поэтому весь KV-кэш становится
      вдвое меньше.
  - question: >-
      В конфиге новой открытой модели обнаружены RMSNorm, RoPE с YaRN, SwiGLU и
      GQA. Какой вывод об архитектуре обоснован этими данными?
    options:
      - >-
        Модель использует совершенно новый каркас, поэтому прежняя схема
        сравнения неприменима
      - >-
        Перечисленные решения укладываются в знакомые архитектурные ручки;
        остальные поля ещё нужно проверить отдельно
      - >-
        Модель гарантированно плотная, поскольку среди перечисленных решений нет
        MoE
      - 'Наличие YaRN означает, что модель не является декодерным трансформером'
    correct: 1
    explanation: >-
      Нормализация, позиционное кодирование, устройство MLP и организация
      attention уже описываются знакомыми ручками. По этому неполному набору
      нельзя определить наличие MoE или другие неуказанные решения.
  - question: >-
      У Gemma 2 половина слоёв хранит KV только для окна 4096 токенов, а другая
      половина — для полного контекста 8k. Как это меняет общий размер KV-кэша
      относительно полного кэша во всех слоях?
    options:
      - 'Не меняет его: все слои обязаны хранить полный контекст'
      - Уменьшает примерно на четверть
      - Уменьшает ровно вдвое
      - Уменьшает примерно в четыре раза
    correct: 1
    explanation: >-
      Половина слоёв сохраняет полный вклад, а у второй половины эффективная
      длина кэша сокращается примерно вдвое. Общий объём становится около 75%
      исходного, то есть экономия составляет примерно 25%.
---

Ты прошёл урок целиком — и стоит проверить, что осталось не список названий, а рабочая привычка читать конфиг.

Дальше в курсе тебе пригодятся ровно три вещи: умение по одной строке таблицы восстановить шесть ручек модели, умение перевести поля `config.json` в число параметров и в размер KV-кэша, и умение отделить память весов от активных FLOPs и от кэша ([шаг 53](#step-53)).

Спотыкаются обычно на двух местах. Первое — MoE: разреженность режет вычисления, но веса всех экспертов лежат в VRAM целиком. Второе — KV-кэш: он не зависит от числа Q-голов, только от KV-голов, слоёв, head_dim, длины контекста и битности.

Ответишь на четыре вопроса — и следующая незнакомая модель будет просто ещё одной строкой в таблице.
