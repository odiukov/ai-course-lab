- concept: MoE вычисляет выходы только выбранных роутером top-k экспертов и объединяет их с весами маршрутизации
  question: Заполни пропуск в формуле разреженного MoE-прохода.
  explanation: Роутер оценивает экспертов, TopK оставляет выбранных, а итоговый выход является взвешенной суммой только их результатов. Остальные эксперты для этого токена не вычисляются.
  kind: cloze
  template: output = sum_{i in ___} p_i * E_i(x)
  answer: TopK(router(x))
  accept:
    - top-k(router(x))
    - top_k(router(x))
    - выбранные top-k эксперты
  id: 029-moe-vmesto-dense-mlp-1
  fingerprint: 0bfaef94
