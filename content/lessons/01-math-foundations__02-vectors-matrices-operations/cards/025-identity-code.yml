- concept: главная диагональ матрицы определяется равенством индексов строки и столбца
  question: Какое условие нужно подставить, чтобы программно заполнить главную диагональ единицами, а остальные позиции — нулями?
  explanation: На главной диагонали индекс строки совпадает с индексом столбца. В Python int(True) превращается в единицу, а int(False) — в ноль.
  kind: cloze
  template: return Matrix([[int(___) for j in range(n)] for i in range(n)])
  answer: i == j
  accept:
    - j == i
  id: 025-identity-code-1
  fingerprint: 2e88fc73
- concept: фабричный метод создания единичной матрицы не зависит от состояния существующего экземпляра
  question: Объясни своими словами, почему метод Matrix.identity(n) можно объявить статическим и вызывать от класса.
  explanation: Статический метод подходит для операции-конструктора, которой не требуется состояние экземпляра. Он получает размер, формирует данные и возвращает новый объект Matrix.
  kind: open
  reference: Метод создаёт новую матрицу только по переданному размеру и не читает данные существующего объекта Matrix, поэтому параметр self ему не нужен.
  id: 025-identity-code-2
  fingerprint: 66d0d484
