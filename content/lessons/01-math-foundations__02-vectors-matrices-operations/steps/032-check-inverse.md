---
id: 032-check-inverse
type: check
title: 'Проверка: чему равно A @ A^-1 и что будет при det = 0'
source_anchor: '## Exercises'
check:
  - question: >-
      Ты перемножил обратимую матрицу A на её вычисленную обратную и получил
      значения, лишь приближённо равные единицам на диагонали и нулям вне её.
      Как интерпретировать результат?
    options:
      - Обратная матрица неверна при любом ненулевом отклонении
      - >-
        Это ожидаемая погрешность вычислений; произведение нужно сравнить с
        единичной матрицей с допуском
      - Произведение должно быть матрицей из одних единиц
      - 'Погрешность означает, что определитель A обязательно равен нулю'
    correct: 1
    explanation: >-
      Теоретически A @ A⁻¹ равно единичной матрице. В вычислениях с плавающей
      точкой возможны значения вроде 0.9999999 и 2.3e-17, поэтому используют
      приближённое сравнение.
  - question: >-
      У матрицы 2 на 2 в первой строке 1 и 2, во второй 2 и 4. Почему для неё не
      существует обратной матрицы?
    options:
      - >-
        Одна строка линейно зависит от другой, поэтому преобразование теряет
        направление и не позволяет однозначно восстановить вход
      - В матрице присутствуют чётные числа
      - Обратная матрица определена только для диагональных матриц
      - Её элементы слишком велики для обращения
    correct: 0
    explanation: >-
      Вторая строка вдвое больше первой, поэтому определитель равен нулю.
      Преобразование теряет информацию, а деление на нулевой определитель в
      формуле — следствие этой необратимости.
---

Это место, где проверяется, понял ли ты обратную матрицу не как формулу, а как действие. После этого куска ты должен уметь сказать заранее, что получится при умножении матрицы на свою обратную, — и распознать матрицу, у которой обратной нет вовсе, ещё до того, как код упадёт с ошибкой.

Спотыкаются обычно на двух вещах. Первое: ждут в результате аккуратные единицы и нули, а видят что-то вроде 0.9999999 и 2.3e-17. Это не ошибка — так дробные числа хранятся в компьютере, у них всегда есть крошечный хвост. Сравнивай не «равно точно», а «близко».

Второе: думают, что нулевой определитель — это просто «неудобный случай, деление на ноль». На самом деле деление на ноль тут только симптом. Причина глубже: такое преобразование теряет информацию, и никакая матрица не сможет её вернуть.

> 🎒 **На пальцах.** Определитель — это во сколько раз преобразование растягивает площадь. Представь, что ты сплющил апельсин в блин нулевой толщины. Объём стал нулём. Теперь попробуй восстановить апельсин обратно из блина — по блину невозможно понять, где что было внутри. Данные не спрятались, они исчезли. Вот почему при нулевом определителе обратной матрицы не существует в принципе, а не «её трудно посчитать».
