- concept: диапазоны A–Z и a–z не охватывают буквы за пределами ASCII
  question: Как регулярное выражение `[A-Za-z]+|[^\sA-Za-z0-9]` токенизирует строку `café`?
  explanation: '`[A-Za-z]+` распознаёт только английские буквы, поэтому на `é` слово заканчивается. Символ `é` затем совпадает с общей альтернативой для одиночного непробельного символа и становится отдельным токеном.'
  kind: choice
  options:
    - Вернёт `['café']`
    - Вернёт `['caf', 'é']`
    - Вернёт `['caf']`
    - Не найдёт совпадений
  correct: 1
  id: 014-code-tokenize-1
  fingerprint: d1714dc5
- concept: re.findall извлекает все неперекрывающиеся совпадения шаблона в порядке их появления
  question: Заполни пропуск так, чтобы функция вернула список фрагментов, совпавших с шаблоном токенов.
  explanation: '`re.findall` проходит по строке слева направо и возвращает список всех неперекрывающихся совпадений. Если шаблон описывает сами токены, каждое совпадение становится элементом результата.'
  kind: cloze
  template: |-
    def tokenize(text):
        return re.___(TOKEN_PATTERN, text)
  answer: findall
  accept:
    - findall
  id: 014-code-tokenize-2
  fingerprint: 0d320541
