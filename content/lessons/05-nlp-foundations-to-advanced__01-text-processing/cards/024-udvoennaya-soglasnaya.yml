- concept: после удаления суффикса одна из двух одинаковых конечных согласных удаляется, если согласная не входит в список исключений
  question: После удаления суффикса `-ed` из `planned` получилась промежуточная основа `plann`. Какой результат должно дать правило удвоенной согласной Портера?
  explanation: 'Основа `plann` заканчивается удвоенной `n`. Поскольку `n` не входит в список исключений `l`, `s`, `z`, одну согласную нужно удалить: получится `plan`.'
  kind: choice
  options:
    - plan
    - plann
    - pla
    - plane
  correct: 0
  id: 024-udvoennaya-soglasnaya-1
  fingerprint: bd7f091f
- concept: удвоенные согласные l, s и z сохраняются после удаления суффикса
  question: После удаления суффикса `-ed` из `filled` получается `fill`. Объясните, почему обработчик удвоенной согласной не должен превращать эту основу в `fil`.
  explanation: Правило Портера удаляет одну конечную согласную при удвоении, но не делает этого для `l`, `s` и `z`. Эти удвоения считаются частью основы, поэтому `fill` сохраняется.
  kind: open
  reference: Последняя удвоенная согласная — `l`, а `l` входит в список исключений вместе с `s` и `z`. Поэтому одну из одинаковых букв удалять нельзя и основа остаётся `fill`.
  id: 024-udvoennaya-soglasnaya-2
  fingerprint: f36434fe
