---
id: 011-check-stem-vs-lemma
type: check
title: 'Проверка: stem против lemma'
source_anchor: '## The Concept'
check:
  - question: >-
      Стеммер вернул для слова «relational» форму «relat», которой нет в
      словаре. Означает ли это, что алгоритм сломан?
    options:
      - 'Да: любая нормализованная форма обязана быть словом'
      - 'Нет: stem может быть условной меткой для группировки слов'
      - 'Да: отсутствие формы в словаре означает, что токенизация не сработала'
      - 'Нет: «relat» обязательно является грамматически правильной леммой'
    correct: 1
    explanation: >-
      Stem — не обязательно словарная форма. Если он используется как внутренний
      ключ для сопоставления документов, отсутствие такого слова в языке само по
      себе не является ошибкой.
  - question: >-
      Почему полноценный lemmatizer обычно требует больше ресурсов, чем простой
      stemmer?
    options:
      - >-
        Он сверяется с лексическими данными или выполняет морфологический
        анализ, а не только применяет правила удаления окончаний
      - Он всегда разбивает каждое слово на отдельные символы
      - Он обязан несколько раз прочитать весь корпус перед обработкой слова
      - Он хранит исходный текст в нескольких копиях
    correct: 0
    explanation: >-
      Для лемматизации нужны знания о словах и грамматических формах: словарь,
      таблицы или морфологическая модель. Stemmer обходится небольшим набором
      правил для окончаний, поэтому обычно работает легче и быстрее.
---

После этого куска ты должна уметь по конкретной паре слов сказать, кто её вытянет: stemmer или lemmatizer. Механика простая. Если вторая форма получается из первой отрезанием хвоста — справится и stemmer (`running` и `run`). Если внутри слова меняются буквы или это вообще другое слово — только словарь (`ran` и `run`, `better` и `good`).

Спотыкаются обычно на двух вещах. Первая: кажется, что stemmer «просто хуже», и его ошибки — брак. Нет, это осознанная сделка: шум в обмен на скорость, и для поискового индекса сделка часто выгодная. Вторая: результат stemmer'а путают со словом. `organ` из `organization` — не слово, а метка группы; она бессмысленна для человека, но годится, чтобы склеивать документы между собой.

> 🎒 **На пальцах.** Stemmer — сортировщик посылок, который читает только последние буквы адреса. Быстро, но иногда отправляет медицинский журнал в отдел менеджмента. Lemmatizer сверяется с полным справочником адресов: медленнее и справочник надо где-то хранить, зато посылка приезжает туда, куда нужно. Какой из двух нанимать — зависит от того, кто вскрывает посылку: машина или человек.
