- concept: после осмысления результата автомат может продолжить выполнение, перепланировать или завершить цель
  question: Бюджет не исчерпан, и цикл находится в `REFLECTING`. Какой набор содержит все содержательные варианты следующего состояния?
  explanation: Из `REFLECTING` цикл может перепланировать работу через `PLANNING`, продолжить следующий шаг через `EXECUTING` или завершить достигнутую цель через `DONE`.
  kind: choice
  options:
    - '`PLANNING`, `EXECUTING` или `DONE`'
    - '`AWAITING_TOOL`, `EXECUTING` или `DONE`'
    - '`PLANNING`, `AWAITING_TOOL` или `IDLE`'
    - '`IDLE`, `AWAITING_TOOL` или `DONE`'
  correct: 0
  id: 005-state-machine-1
  fingerprint: 015eab41
