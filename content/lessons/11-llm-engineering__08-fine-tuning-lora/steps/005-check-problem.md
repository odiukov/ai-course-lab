---
id: 005-check-problem
type: check
title: 'Проверка: что съедает больше всего памяти'
source_anchor: '## The Problem'
check:
  - question: >-
      Почему состояния оптимизатора Adam могут занимать больше памяти, чем сами
      веса модели?
    options:
      - >-
        Adam хранит для каждого обучаемого параметра две дополнительные
        статистики, поэтому его состояния требуют примерно вдвое больше памяти,
        чем fp16-веса
      - Adam дублирует только входные токены и не хранит данных о параметрах
      - >-
        Adam хранит одну статистику на весь слой, поэтому размер памяти почти не
        зависит от числа параметров
      - >-
        Adam заменяет веса модели копией в формате более высокой точности, но не
        хранит дополнительное состояние
    correct: 0
    explanation: >-
      Для каждого обучаемого параметра Adam хранит две статистики: первый момент
      и второй момент. Поэтому при полном дообучении его состояния дают около 32
      ГБ против 16 ГБ весов в fp16.
  - question: >-
      Что произойдёт с памятью, если обучаемыми оставить только 1% параметров, а
      базовую модель заморозить?
    options:
      - >-
        Градиенты и состояния Adam уменьшатся примерно пропорционально числу
        обучаемых параметров, но все веса базовой модели всё равно останутся
        загруженными
      - >-
        Из памяти исчезнет почти вся базовая модель, потому что замороженные
        веса не нужны для прямого прохода
      - >-
        Уменьшатся только активации, а градиенты и состояния Adam останутся
        прежними
      - >-
        Память не изменится, потому что число обучаемых параметров не влияет на
        состояние оптимизатора
    correct: 0
    explanation: >-
      Градиенты и состояния Adam нужны только для обучаемых параметров, поэтому
      они резко сокращаются. Однако замороженные веса всё равно участвуют в
      вычислении ответа модели и должны находиться в памяти.
---

После [шага 2](#step-2) у тебя должно быть не заученное число 56, а умение разложить его на части и сказать, какая часть от чего зависит. Веса — от размера модели. Градиенты и состояния Adam — от того, сколько параметров ты реально обучаешь.

Спотыкаются обычно на двух вещах. Первая: кажется, что модель — главный пожиратель памяти, хотя служебные данные обучения весят вдвое больше её самой. Вторая: кажется, что если обучать меньше параметров, то и весов в памяти станет меньше. Не станет — прогонять текст через сеть всё равно нужно целиком.
