- concept: нормировка счётчиков хромы на сумму превращает их в относительные веса
  question: Функция получила MIDI-ноты 53, 65, 77 и 58. Все элементы, кроме последнего, относятся к одному классу высоты; последний — к другому. Какой вес получит первый класс после деления счётчиков на их сумму?
  explanation: Первая группа содержит все ноты, кроме последней, поэтому после деления на общее число нот получает вес 0.75.
  kind: numeric
  answer: 0.75
  tolerance: 0.001
  id: 033-chroma-vector-1
  fingerprint: 15d4063c
- concept: аккумулятор хромы заранее создаётся с фиксированным числом ячеек
  question: Код использует NumPy и переменную n_pitch_classes с числом классов высоты. Какая инициализация гарантирует, что размер аккумулятора не зависит от количества входных нот?
  explanation: Ячейка нужна каждому классу высоты, включая отсутствующие во входе. Поэтому размер аккумулятора задаётся числом классов, а не длиной списка нот.
  kind: choice
  options:
    - counts = np.zeros(n_pitch_classes)
    - counts = np.zeros(len(notes))
    - counts = np.unique(notes)
  correct: 0
  id: 033-chroma-vector-2
  fingerprint: 00b78685
- concept: нормировка нулевого хрома-вектора требует защиты от деления на ноль
  question: Объясни, почему нормировку счётчиков на максимум нельзя безусловно применять к кадру без звучащих нот и что функция должна вернуть в этом случае.
  explanation: Проверка масштаба перед делением предотвращает ошибку или появление некорректных значений. Отсутствие нот корректно представляется нулевым хрома-вектором.
  kind: open
  reference: У кадра без нот все счётчики равны нулю, поэтому их максимум тоже нулевой и деление на него не определено. Функция должна пропустить деление и вернуть нулевой вектор фиксированной длины.
  id: 033-chroma-vector-3
  fingerprint: 74eff1d2
