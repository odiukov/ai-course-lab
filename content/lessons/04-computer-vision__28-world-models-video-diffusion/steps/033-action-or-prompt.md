---
id: 033-action-or-prompt
type: check
title: 'Проверка: начальное условие, явное действие и latent action'
source_anchor: '### Conditioning on actions: latent action models'
check:
  - question: >-
      Модель получает первый кадр с персонажем у двери и команду «шаг вправо».
      Как распределяются роли этих входов?
    options:
      - 'Первый кадр задаёт начальное условие, а команда является явным действием'
      - 'Команда задаёт начальное условие, а первый кадр является действием'
      - Оба входа являются latent actions
    correct: 0
    explanation: >-
      Первый кадр определяет исходное состояние генерации. Команда
      непосредственно указывает требуемое изменение, поэтому это явное действие.
  - question: >-
      В обучающих видео отсутствуют записи команд управления. Как модель может
      получить сигнал действия для обусловливания следующего кадра?
    options:
      - Принять любое изменение между кадрами за случайный шум
      - Вывести компактное latent action из пары соседних кадров
      - >-
        Считать начальный текстовый промпт точной записью всех последующих
        команд
    correct: 1
    explanation: >-
      Модель сопоставляет соседние кадры и кодирует произошедший между ними
      переход в latent action. Затем декодер использует этот скрытый управляющий
      сигнал при предсказании следующего кадра.
---

Проверь, что ты не смешиваешь три разные роли. Начальное условие показывает, **где** начинается продолжение. Явное действие прямо говорит, **что сделать**: например, повернуть руль. Latent action передаёт изменение без готовой подписи команды — модель сама извлекает его из соседних кадров, как в [шаге 31](#step-31), а затем использует для управления, как в [шаге 32](#step-32). Частая ошибка — считать latent action случайностью или текстовым промптом.

> 🎒 **На пальцах.** Ты стоишь перед закрытым турникетом — это начальное условие. Команда «приложи билет» была бы явным действием. Если подписи нет, но система видит состояние до прохода и после него, она может сжать скрытый переход в условный жетон. Этот жетон и играет роль latent action.
