---
id: 021-position-shapes
type: check
title: 'Проверка: сумма размерностей осей, чётность и форма позиционного кода'
source_anchor: '### Step 2: 3D position encoding'
check:
  - question: Ширина токена D равна 96. Какое разбиение по осям пройдёт обе проверки?
    options:
      - 't_dim 24, h_dim 32, w_dim 40'
      - 't_dim 24, h_dim 31, w_dim 41'
      - 't_dim 24, h_dim 32, w_dim 38'
      - 't_dim 25, h_dim 31, w_dim 40'
    correct: 0
    explanation: >-
      Все три размерности чётные, а их сумма равна 96. В остальных вариантах
      нарушена чётность или сумма.
  - question: >-
      Почему при корректных размерах сетки и осевых частей функция возвращает
      тензор той же формы, что и tokens?
    options:
      - >-
        Склеенный позиционный код имеет ту же длину последовательности и ширину
        D, а по оси батча распространяется broadcasting
      - >-
        Склейка осевых кодов утраивает ширину, после чего лишние каналы
        автоматически отбрасываются
      - >-
        Позиционный код добавляется только к одному токену, поэтому остальные
        размерности не участвуют
      - >-
        Операции sin и cos всегда преобразуют любой тензор к форме входных
        tokens
    correct: 0
    explanation: >-
      Проверка сетки обеспечивает нужное число позиционных строк, а сумма осевых
      ширин — D каналов. Код формы (T·H·W, D) распространяется по батчу и
      поэлементно складывается с tokens, не меняя их форму.
---

Здесь важно связать две проверки из [шага 20](#step-20): куски осей должны вместе заполнить всю ширину токена, а каждый кусок — делиться на пары sin/cos без остатка. Верная сумма при нечётной части ещё не спасает код: одна ось потеряет канал. Если обе проверки пройдены и число позиций совпадает с сеткой, сложение сохраняет исходную форму `tokens`.

> 🎒 **На пальцах.** Представь органайзер на 96 карандашей с тремя секциями. Все секции вместе должны занять ровно 96 мест, но карандаши кладут парами. Секция на 31 место формально помещается в органайзер, однако последнему карандашу не найдётся пары. Поэтому проверяешь и общую вместимость, и чётность каждой секции.
