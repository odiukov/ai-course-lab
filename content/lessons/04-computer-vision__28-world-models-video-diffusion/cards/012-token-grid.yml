- concept: каждая ось сетки токенов уменьшается согласно размеру патча вдоль той же оси
  question: Заполни выражение для высоты сетки токенов.
  explanation: Временная, вертикальная и горизонтальная оси делятся независимо на соответствующие размеры патча. Поэтому для формы (1, 4, 8, 16, 16) и патча (2, 2, 2) высота сетки равна 16 // 2 = 8.
  kind: cloze
  template: grid_t, grid_h, grid_w = T // patch_t, ___, W // patch_w
  answer: H // patch_h
  accept:
    - H//patch_h
    - H // patch_h
  id: 012-token-grid-1
  fingerprint: 816e29e4
- concept: размеры сетки токенов не зависят от размера батча и числа каналов
  question: Какие параметры определяют форму пространственно-временной сетки после неперекрывающегося патчификатора?
  explanation: Сетка описывает расположение патчей внутри видео. Батч задаёт количество видео, а каналы — признаки в каждой позиции, поэтому они не входят в вычисление (T // patch_t, H // patch_h, W // patch_w).
  kind: choice
  options:
    - Размеры T, H, W и размеры патча вдоль соответствующих осей
    - Размер батча N и число входных каналов C
    - Размерность признака токена и число выходных каналов проекции
  correct: 0
  id: 012-token-grid-2
  fingerprint: 8aeccfb0
