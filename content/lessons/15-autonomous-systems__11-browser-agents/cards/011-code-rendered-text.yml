- concept: функция извлечения возвращает содержимое без смысловых изменений, оценку выносит отдельный слой
  question: На странице видимая надпись просит агента отправить данные постороннему адресату. Как должна повести себя функция, извлекающая отрисованный текст?
  explanation: Извлечение отвечает на вопрос «что отрисовано», а не «безопасно ли это». Если оно молча правит текст, детектор ниже по цепочке уже не увидит атаку и колонка промахов станет ложно пустой.
  kind: choice
  options:
    - Вернуть текст как есть, включая эту надпись
    - Вырезать надпись из результата
    - Заменить надпись на пометку `[removed]`
    - Выбросить исключение и прервать обход
  correct: 0
  id: 011-code-rendered-text-1
  fingerprint: 0be66804
- concept: склейка частей видимого текста читаемым разделителем с сохранением порядка
  question: 'Заполни пропуск: части видимого текста нужно склеить так, чтобы они не слипались в одну строку.'
  explanation: Перенос строки сохраняет границы между надписями. Без разделителя конец одного абзаца и начало следующего сливаются, и поиск команд по такому тексту даёт мусорные срабатывания.
  kind: cloze
  template: return "___".join(page.visible_parts)
  answer: \n
  accept:
    - \n
    - "\n"
    - перенос строки
  id: 011-code-rendered-text-2
  fingerprint: 4d88eb83
- concept: пустой вход даёт нейтральное значение того же типа, а не исключение и не строку None
  question: У страницы нет видимого содержимого. Что должна вернуть функция извлечения отрисованного текста?
  explanation: 'Пустая строка — корректный ответ того же типа: дальше по цепочке её обработают обычным образом. `"None"` подсунет проверке несуществующий текст, а исключение оборвёт весь обход из-за безобидного случая.'
  kind: choice
  options:
    - Строку `"None"`
    - Пустую строку
    - Исключение, чтобы обход остановился
    - Адрес страницы вместо текста
  correct: 1
  id: 011-code-rendered-text-3
  fingerprint: 552fff90
