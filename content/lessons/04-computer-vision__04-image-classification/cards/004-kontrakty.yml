- concept: пакет изображений и меток должен сохранять попарное соответствие, а не только совместимые размеры
  question: DataLoader независимо перемешал изображения и метки, после чего собрал тензоры совместимых размеров. Обучение выполняется без ошибки. Какой контракт нарушен?
  explanation: 'Совпадения размеров недостаточно: пары «изображение — метка» должны сохраняться. При независимом перемешивании функция потерь сравнивает предсказания с чужими метками, поэтому модель получает неверный обучающий сигнал.'
  kind: choice
  options:
    - Метка должна относиться к изображению в той же позиции пакета
    - Каждое изображение должно иметь отдельный набор параметров модели
    - Планировщик должен перемешивать примеры перед каждым обновлением
  correct: 0
  id: 004-kontrakty-1
  fingerprint: edf10da4
- concept: выход классификатора содержит строку оценок для каждого элемента пакета и столбец для каждого класса
  question: Заполни проверку контракта между моделью и функцией потерь.
  explanation: Первая размерность logits должна совпадать с количеством меток, а вторая — с числом классов. Тогда каждой метке соответствует строка оценок по всем классам.
  kind: cloze
  template: assert logits.shape == (len(targets), ___)
  answer: num_classes
  accept:
    - C
    - число классов
  id: 004-kontrakty-2
  fingerprint: 52d7fee2
