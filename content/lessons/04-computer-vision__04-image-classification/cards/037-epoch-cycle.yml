- concept: число обновлений параметров за эпоху равно числу батчей с учётом последнего неполного батча
  question: Обучающая выборка содержит 750 изображений, размер батча — 128. После каждого батча оптимизатор обновляет параметры. Сколько обновлений произойдёт за эпоху?
  explanation: 'Число батчей равно округлённому вверх отношению размера выборки к размеру батча: ceil(750 / 128) = 6. Последний неполный батч также приводит к обновлению параметров.'
  kind: numeric
  answer: 6
  tolerance: 0.01
  id: 037-epoch-cycle-1
  fingerprint: 97e8e39c
- concept: scheduler с эпохальным расписанием вызывается после завершения цикла по батчам
  question: Заполни пропуск так, чтобы learning rate изменялся с частотой, предусмотренной эпохальным scheduler.
  explanation: Внутренний цикл выполняет обновление параметров на каждом батче. Вызов scheduler расположен после этого цикла, поэтому расписание продвигается только один раз по завершении эпохи.
  kind: cloze
  template: |-
    for epoch in epochs:
        for x, y in loader:
            train_batch(x, y)
        ___
  answer: scheduler.step()
  accept:
    - scheduler.step()
    - scheduler.step( )
  id: 037-epoch-cycle-2
  fingerprint: 0f23fc4f
