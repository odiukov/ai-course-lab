---
id: 022-check-transforms
type: check
title: 'Проверка: порядок преобразований и источник случайности'
source_anchor: '### Step 2: Normalisation and augmentation'
check:
  - question: Какой порядок преобразований лучше сохраняет смысл пайплайна обучения?
    options:
      - 'Сначала нормализация, затем случайная обрезка и отражение'
      - 'Сначала случайная обрезка и отражение, затем нормализация'
      - 'Нормализация только до обрезки, а отражение после неё'
    correct: 1
    explanation: >-
      Сначала создаётся допустимый вариант изображения, а затем его каналы
      приводятся к масштабу датасета.
  - question: >-
      Два запуска должны повторить одинаковые случайные преобразования. Что ты
      передашь в random_crop и random_hflip?
    options:
      - Один явно созданный генератор с тем же seed
      - Текущее время запуска
      - Глобальный np.random без отдельного генератора
    correct: 0
    explanation: >-
      Явный генератор хранит управляемую последовательность случайных чисел,
      поэтому запуск можно воспроизвести.
---

Здесь легко проверить функции по отдельности и всё же собрать неверный маршрут. Пространственные изменения сначала создают новый допустимый вид объекта, а нормализация из [шага 20](#step-20) уже приводит его каналы к нужному масштабу. Источник случайности тоже входит в контракт: обе случайные функции должны получать явный генератор из [шага 21](#step-21), а не незаметно обращаться к общему состоянию программы.

> 🎒 **На пальцах.** Представь фотобудку: ты сначала выбираешь кадр и при необходимости зеркально поворачиваешь снимок, а потом настраиваешь его цвета. Номер следующего случайного действия берёшь из своей пронумерованной колоды. С той же колодой и начальной картой вся последовательность повторится.
