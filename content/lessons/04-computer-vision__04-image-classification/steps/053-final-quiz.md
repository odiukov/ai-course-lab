---
id: 053-final-quiz
type: quiz
title: 'Итоговая проверка: собрать и диагностировать классификатор изображений'
source_anchor: '## Key Terms'
check:
  - question: >-
      Модель возвращает вероятности после softmax, а функция потерь ожидает
      логиты. Как правильно состыковать компоненты?
    options:
      - Передавать функции потерь логиты напрямую
      - Применить softmax к вероятностям ещё раз
      - Выбрать argmax и передать номер предсказанного класса
      - Округлить вероятности до нулей и единиц
    correct: 0
    explanation: >-
      Cross-entropy должна получать сырые логиты: внутри неё уже выполняется
      устойчивое преобразование, необходимое для расчёта потерь.
  - question: >-
      После добавления случайных поворотов validation accuracy стала заметно
      прыгать между одинаковыми запусками. Где сначала искать причину?
    options:
      - Увеличить число классов модели
      - 'Проверить, не попали ли случайные преобразования в validation transforms'
      - Применить mixup ещё и на валидации
      - Заменить логиты вероятностями перед argmax
    correct: 1
    explanation: >-
      Валидационный маршрут должен быть детерминированным. Случайные
      преобразования меняют проверочные изображения и делают сравнение эпох
      ненадёжным.
  - question: >-
      С mixup обычная train accuracy снизилась, но validation loss и validation
      accuracy улучшились. Как это разумнее трактовать?
    options:
      - 'Обучение сломано, потому что train accuracy обязана только расти'
      - Нужно считать loss только по первой метке смешанной пары
      - >-
        Train accuracy стала приблизительным сигналом из-за смешанных
        изображений и меток
      - Mixup следует включить и при оценке модели
    correct: 2
    explanation: >-
      При mixup один вход соответствует мягкой смеси целей, поэтому сравнение с
      одной жёсткой меткой неточно. Чистая валидация лучше показывает итоговое
      качество.
  - question: >-
      Общая accuracy выглядит приемлемо, но в строке истинного класса «кошка»
      confusion matrix большинство примеров оказалось в столбце «собака». Что
      это показывает?
    options:
      - 'У класса «кошка» низкий recall: модель часто его пропускает'
      - У класса «кошка» идеальная калибровка
      - Модель слишком часто предсказывает «кошку» для других классов
      - Top-k accuracy обязательно равна нулю
    correct: 0
    explanation: >-
      Строка показывает судьбу истинных примеров класса. Если кошки уходят в
      столбец собак, модель не находит значительную часть кошек, поэтому
      страдает recall.
---

Дальше тебе пригодится не отдельная формула, а привычка проверять классификатор слоями: сначала контракты пайплайна из [шага 4](#step-4), затем цикл обучения и оценки из [шагов 34](#step-34)–[38](#step-38), после — характер ошибок по метрикам из [шагов 43](#step-43)–[52](#step-52). Чаще всего спотыкаются, когда пытаются лечить архитектуру до проверки данных, режимов и преобразований.

> 🎒 **На пальцах.** Представь предполётный осмотр небольшого самолёта. Если он отклоняется от курса, ты не меняешь двигатель наугад: сначала проверяешь загрузку, приборы и управление, затем смотришь, в какую сторону возникает ошибка. С классификатором так же: хорошая диагностика превращает «модель плохая» в конкретное место, которое можно исправить.
