- concept: в векторной реализации дисконт умножается на массив оценок следующих состояний
  question: Заполни пропуск в векторной реализации TD-остатков для целого rollout.
  explanation: Дисконт относится только к ценности продолжения. Массив values (оценки состояний в момент t) вычитается как есть.
  kind: cloze
  template: deltas = rewards + gamma * ___ - values
  answer: next_values
  accept:
    - next_values
    - next_value
    - values_next
    - v_next
  id: 020-td-residuals-1
  fingerprint: de6cb865
