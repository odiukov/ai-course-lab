---
id: 024-check-parse
type: check
title: 'Проверка: когда звать json.loads'
source_anchor: '### Partial JSON and the parse-early trap'
check:
  - question: >-
      В накопленной строке пока лежит обрывок {"note": "смайлик } — но событие
      конца вызова от провайдера ещё не пришло. Что произойдёт, если вызвать
      json.loads, потому что простой счётчик скобок уже сошёлся?
    options:
      - >-
        Разбор пройдёт нормально: нулевой баланс скобок гарантирует готовность
        JSON
      - >-
        json.loads выбросит исключение: скобка внутри незакрытой строки не
        завершает объект
      - Парсер автоматически дождётся следующих фрагментов потока
    correct: 1
    explanation: >-
      Закрывающая скобка внутри строкового значения — обычный символ. Счётчик
      ошибочно принял её за конец объекта, хотя кавычка и структурная скобка ещё
      не закрыты.
  - question: 'На что правильно смотреть, чтобы понять, что пора звать json.loads?'
    options:
      - 'На длину накопленной строки: перестала расти — значит всё'
      - >-
        На сигнал конца от провайдера: finish_reason равный tool_calls у OpenAI,
        content_block_stop у Anthropic, конец стрима у Gemini
      - На первую закрывающую фигурную скобку в потоке
    correct: 1
    explanation: >-
      Только провайдер знает, что новых фрагментов уже не будет. Его явный
      сигнал завершения служит надёжным шлагбаумом перед одноразовым разбором
      накопленного JSON.
---

Здесь стоит остановиться и проверить одну вещь: умеешь ли ты отличать «строка выглядит целой» от «строка точно дописана».

После [шага 20](#step-20) и [шага 23](#step-23) у тебя должно быть готово решение: аргументы копятся кусками, а `json.loads` зовётся ровно один раз — после сигнала конца от провайдера. Не раньше.

Спотыкаются обычно на двух местах. Первое — соблазн разобрать пораньше, «чтобы показать пользователю, что модель уже придумала». Второе — вера в подсчёт скобок: он выглядит как честная проверка, но скобка внутри кавычек или экранированный текст ломают его молча, без ошибки. Именно молча — поэтому баг всплывёт не на твоём примере, а на чужом аргументе через месяц.
