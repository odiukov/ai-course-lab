---
id: 039-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      Три вызова инструментов идут параллельно и занимают 1200, 800 и 300 мс.
      Сколько примерно займёт весь веер?
    options:
      - 2300 мс — время всё равно складывается
      - 1200 мс — столько идёт самый медленный
      - 300 мс — столько идёт самый быстрый
      - 767 мс — среднее из трёх
    correct: 1
    explanation: >-
      При параллельном запуске ждёшь не сумму, а максимум: остальные два уже
      закончились, пока тянулся самый медленный.
  - question: >-
      Ты отправил три вызова разом, а результаты вернулись в другом порядке:
      сначала третий, потом первый, потом второй. Что делать?
    options:
      - 'Отсортировать результаты в исходном порядке, иначе модель их перепутает'
      - >-
        Отправить каждый результат с тем же tool call id, что был у его вызова —
        порядок не важен
      - Дождаться всех трёх и склеить в один результат
      - 'Переспросить модель, какой результат к какому вызову относится'
    correct: 1
    explanation: >-
      Сопоставление держится на id, а не на позиции. Порядок в ответе свободный,
      а вот id обязан совпадать — иначе модель уверенно ответит по чужим данным.
  - question: >-
      В стриме накопитель уже содержит строку, у которой все скобки закрыты.
      Пора звать json.loads?
    options:
      - 'Да, закрытые скобки — надёжный признак конца'
      - >-
        Нет, ждать сигнала конца от провайдера: закрытая скобка может оказаться
        внутри значения
      - 'Да, но только если аргументов ровно один'
      - 'Нет, разбирать нужно каждый кусок по мере прихода'
    correct: 1
    explanation: >-
      Подсчёт скобок врёт — фигурная скобка бывает внутри строкового значения.
      Разбирать можно только после сигнала завершения от провайдера.
  - question: >-
      Инструмент get_user_id возвращает идентификатор, который нужен второму
      инструменту get_orders. Что будет, если поставить их в один веер?
    options:
      - 'Выполнится быстрее, второй подождёт результат первого'
      - Второй вызов уйдёт без нужного идентификатора и сработает мимо цели
      - Провайдер сам определит зависимость и выстроит порядок
      - 'Ничего не изменится, кроме времени ответа'
    correct: 1
    explanation: >-
      Параллельные вызовы уходят одновременно, никто никого не ждёт. Связанные
      по данным инструменты приходится звать по очереди.
---

Дальше в курсе веер вызовов станет обычным делом: агент почти всегда дёргает несколько инструментов за ход, и именно тут ломается больше всего кода. Спотыкаются обычно на трёх вещах. Первая — ждут, что выигрыш будет пропорциональным, и удивляются, что три вызова по 300 мс поверх сети в 1200 мс почти ничего не ускорили ([шаг 13](#step-13)). Вторая — теряют `tool_call_id` и получают уверенный ответ по чужим данным ([шаг 35](#step-35)). Третья — разбирают `arguments` до сигнала конца ([шаг 23](#step-23)).

И держи в голове перевёрнутый флаг Anthropic: `disable_parallel_tool_use` выключает то, что у OpenAI включает `parallel_tool_calls`.
