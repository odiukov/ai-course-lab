---
id: 008-check-flagi
type: check
title: 'Проверка: перевёрнутый флаг Anthropic'
source_anchor: '### Enabling parallel'
check:
  - question: Ты выставил у Anthropic disable_parallel_tool_use в true. Что получишь?
    options:
      - 'Модель будет звать инструменты по одному, последовательно'
      - Модель сможет положить несколько вызовов в одно сообщение
      - 'Ничего не изменится: у Anthropic параллельность всегда включена'
    correct: 0
    explanation: >-
      Имя флага говорит про запрет: disable_parallel_tool_use = true означает
      «параллельность отключить», то есть вызовы пойдут один за другим.
  - question: >-
      У OpenAI стоит parallel_tool_calls: false. Как получить такое же поведение
      у Anthropic?
    options:
      - 'disable_parallel_tool_use: false'
      - 'disable_parallel_tool_use: true'
      - 'Оставить настройку по умолчанию, она уже последовательная'
    correct: 1
    explanation: >-
      Одно и то же поведение записывается противоположными значениями: у OpenAI
      false запрещает веер, у Anthropic запрещает его true.
---

После [шага 6](#step-6) ты должен уметь одну неприятную вещь: переводить настройку между провайдерами, не доверяя интуиции «true значит включено». У OpenAI флаг называется по разрешению, у Anthropic — по запрету, поэтому одинаковый режим пишется разными значениями. Спотыкаются тут почти все, причём молча: код не падает, ответы приходят, просто веера нет — или наоборот, он есть там, где ты его запрещал.

Второе, что стоит проверить на себе, — умеешь ли ты сказать, когда веер вообще не нужен: зависимость по порядку из [шага 7](#step-7) не лечится настройками, её лечит последовательность.
