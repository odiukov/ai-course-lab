- concept: speedup вычисляется как отношение последовательной длительности к параллельной
  question: Заполни формулу безразмерного коэффициента ускорения.
  explanation: В числителе стоит исходная последовательная длительность, а в знаменателе — длительность ускоренного параллельного прогона. Одинаковые единицы измерения сокращаются.
  kind: cloze
  template: speedup = ___
  answer: sequential_duration / parallel_duration
  accept:
    - T_seq / T_parallel
    - sequential_time / parallel_time
    - sequential / parallel
  id: 014-code-speedup-1
  fingerprint: dcc305aa
