---
id: 012-quiz
type: quiz
title: 'Проверяем токенизацию, граничные случаи, сглаживание, LCS и диспетчер'
source_anchor: '## Going further'
check:
  - question: 'После общей токенизации строка «Кот, КОТ! ёж-2» во что превратится?'
    options:
      - 'кот, кот, ёж-2'
      - 'кот, кот, ёж, 2'
      - 'Кот, КОТ, ёж, 2'
      - 'кот, ёж, 2'
    correct: 1
    explanation: >-
      Регистр станет нижним, пунктуация и дефис исчезнут как разделители, а
      повтор слова сохранится.
  - question: >-
      Какие баллы должен вернуть token F1 для двух пустых строк и для случая,
      когда пуста только одна строка?
    options:
      - В обоих случаях 0.0
      - В обоих случаях 1.0
      - 'Сначала 1.0, затем 0.0'
      - 'Сначала 0.0, затем 1.0'
    correct: 2
    explanation: >-
      Две пустые стороны полностью совпадают, а пустая строка с непустой не
      имеет общих токенов.
  - question: >-
      Короткий непустой кандидат не имеет совпавшей 4-граммы. Что произойдёт при
      описанном расчёте BLEU-4?
    options:
      - Итог обязательно станет 0.0
      - >-
        Сглаживание может сохранить ненулевой балл, а штраф за краткость его
        уменьшит
      - Штраф за краткость повысит балл
      - 'Кандидат получит тот же балл, что и пустая строка'
    correct: 1
    explanation: >-
      Add-one smoothing не даёт отдельной точности обнулить произведение, но
      короткий кандидат всё равно получает штраф. Пустой кандидат обрабатывается
      отдельно и получает 0.0.
  - question: Какой вариант верно объединяет поведение LCS и диспетчера метрик?
    options:
      - >-
        LCS требует соседних совпадений; диспетчер усредняет все цели и молча
        принимает неизвестное имя
      - >-
        LCS игнорирует порядок; диспетчер использует первую цель, а пустой
        список вызывает ошибку
      - >-
        LCS сохраняет порядок; диспетчер выбирает лучший балл среди целей, для
        пустого списка возвращает 0.0, а для неизвестного имени вызывает
        ValueError
      - >-
        LCS считает только одинаковые позиции; диспетчер всегда возвращает один
        и тот же балл
    correct: 2
    explanation: >-
      LCS допускает пропуски, но не перестановку порядка. Диспетчер выбирает
      максимум среди целей и явно обрабатывает пустой список и неизвестное имя.
---

Скоро ты будешь собирать более сложные оценки, и ошибки в классическом основании начнут маскировать их результат. Здесь важно состыковать контракты из [шага 3](#step-3), [шага 5](#step-5), [шага 7](#step-7), [шага 9](#step-9) и [шага 11](#step-11). Чаще всего спотыкаются не на обычной фразе, а на пустом вводе, повторах, коротком кандидате и неизвестном имени метрики.

> 🎒 **На пальцах.** Представь генеральную репетицию оркестра: каждый музыкант отдельно знает свою партию, но теперь ты проверяешь весь ансамбль. Один пропущенный такт похож на неверную токенизацию, слишком раннее вступление — на сломанный порядок LCS, а дирижёр, вызвавший несуществующий инструмент, — на ошибку диспетчера. Зачёт наступает, когда согласованы и партии, и исключения.
