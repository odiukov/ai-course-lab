- concept: лимит каждой поисковой ветки применяется до RRF, а общий лимит результата — после слияния
  question: Расположи операции retrieve_with_rewriter в правильном порядке.
  explanation: k_each управляет глубиной отдельного поиска, поэтому применяется при исполнении ветвей. k_out ограничивает уже общий ответ и применяется только после RRF; при отсутствии ветвей сливается пустой набор и результат остаётся пустым.
  kind: order
  items:
    - Получить RewriteResult от переписывателя
    - Выполнить каждую поисковую ветку с ограничением k_each
    - Объединить ранжированные списки через RRF
    - Обрезать объединённый рейтинг до k_out
  id: 011-retrieve-fuse-1
  fingerprint: fd086455
