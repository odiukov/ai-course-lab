---
id: 015-choose-metric
type: check
title: 'Проверка: L1, L2 или косинус'
source_anchor: '### Distance metrics'
check:
  - question: Ты выбираешь метрики для трёх задач. Какое соответствие лучше?
    options:
      - >-
        Числовые признаки сопоставимого масштаба — L2; числовые данные с
        выбросами — L1; текстовые эмбеддинги — косинусное расстояние
      - >-
        Числовые признаки сопоставимого масштаба — косинусное расстояние;
        числовые данные с выбросами — L2; текстовые эмбеддинги — L1
      - >-
        Числовые признаки сопоставимого масштаба — L1; числовые данные с
        выбросами — косинусное расстояние; текстовые эмбеддинги — L2
    correct: 0
    explanation: >-
      L2 подходит числовым признакам сопоставимого масштаба, L1 не раздувает
      большие отклонения возведением в квадрат, а косинусное расстояние
      сравнивает направления текстовых эмбеддингов.
  - question: >-
      Два ненулевых текстовых вектора направлены одинаково, но один получен
      умножением другого на положительный коэффициент. Чему равно косинусное
      расстояние между ними?
    options:
      - Оно положительно и растёт вместе с коэффициентом
      - 'Оно равно нулю, потому что направления совпадают'
      - Оно равно нулю только при использовании L1
    correct: 1
    explanation: >-
      Положительное масштабирование меняет длину вектора, но не его направление.
      Косинусное сходство сонаправленных ненулевых векторов равно единице,
      поэтому косинусное расстояние равно нулю.
---

Здесь важно уметь не вычислять расстояние, а выбирать его по задаче, как в [шаге 14](#step-14). Обычно спотыкаются на слове «расстояние» и автоматически берут L2. Но при выбросах полезнее свойство L1 из [шага 8](#step-8), а для текстовых эмбеддингов важнее направление, разобранное в [шаге 10](#step-10).

> 🎒 **На пальцах.** Ты сравниваешь три исполнения песни. Для точности нот подойдёт обычное числовое сравнение. Если один микрофон случайно дал резкий скачок громкости, лучше не усиливать эту ошибку. А если записи разной громкости, но мелодия одна, сравнивай их направление, а не силу звука.
