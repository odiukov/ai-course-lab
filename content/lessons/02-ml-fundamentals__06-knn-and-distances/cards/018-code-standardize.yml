- concept: стандартное отклонение столбца вычисляется как корень из среднего квадратов отклонений от среднего
  question: Заполни пропуск в вычислении стандартного отклонения столбца.
  explanation: Сначала вычисляется среднее квадратов отклонений значений столбца, затем из него извлекается квадратный корень.
  kind: cloze
  template: std = math.___(sum((X[i][j] - mean) * (X[i][j] - mean) for i in range(n)) / n)
  answer: sqrt
  accept:
    - sqrt
  id: 018-code-standardize-1
  fingerprint: 49d0fd41
- concept: постоянный признак после безопасной стандартизации превращается в столбец нулей
  question: В столбце все значения одинаковы. Реализация ограничивает стандартное отклонение снизу малым положительным epsilon. Каким станет этот столбец после стандартизации?
  explanation: Каждое значение совпадает со средним, поэтому числитель равен нулю. Деление на положительную нижнюю границу 1e-10 безопасно и по-прежнему даёт ноль.
  kind: choice
  options:
    - Все значения станут нулевыми
    - Все значения станут равны epsilon
    - Значения останутся исходными
    - Возникнет деление на ноль
  correct: 0
  id: 018-code-standardize-2
  fingerprint: 5b3839fc
