---
id: 025-check-grad
type: check
title: 'Проверка: производные x² и запрет скрытого состояния'
source_anchor: '### jax.grad: Functional Autodiff'
check:
  - question: >-
      Функция f(x) возвращает x в квадрате. Какое выражение описывает результат
      функции jax.grad(f) для произвольного x?
    options:
      - x
      - 2 * x
      - x ** 2
      - Результата нет без вызова backward
    correct: 1
    explanation: >-
      Производная квадрата равна 2x. Например, при x = 3.0 функция градиента
      вернёт 6.0; вызывать backward в JAX не требуется.
  - question: >-
      Функция изменяет внешний счётчик при каждом вызове. Как переписать её в
      соответствии с требованием JAX к чистым функциям?
    options:
      - Спрятать счётчик в поле объекта
      - Объявить счётчик глобальной переменной
      - Передавать счётчик аргументом и возвращать его новое значение
      - Вызвать backward перед изменением счётчика
    correct: 2
    explanation: >-
      Изменяемое состояние следует передавать через явные входы и выходы. Тогда
      результат определяется аргументами функции и не зависит от скрытых
      изменений.
---

Здесь важно соединить две мысли из [шага 21](#step-21) и [шага 24](#step-24): `grad` возвращает вызываемую функцию, но рассчитывает на чистый исходный расчёт. Обычно спотыкаются в двух местах: по привычке ищут `.backward()` или незаметно меняют внешний счётчик. Проверь, что ты можешь получить производную квадрата без состояния на тензоре и вынести любое изменяемое значение во входы и выходы.

> 🎒 **На пальцах.** Представь кассовый чек: по списку покупок ты должен каждый раз получать ту же сумму. Если кассир тайно прибавляет число из личного блокнота, проверить расчёт нельзя. Передай это число вместе со списком и напечатай новое на чеке — тогда весь расчёт виден.
