---
id: 009-check-immutable
type: check
title: 'Проверка: чем set через at отличается от присваивания'
source_anchor: '### jax.numpy: The Familiar Surface'
check:
  - question: 'После выполнения `b = a.at[0].set(9)` какое утверждение верно?'
    options:
      - 'Массив `a` изменяется на месте, а `b` получает ссылку на него'
      - 'Массив `a` остаётся прежним, а `b` получает обновлённый массив'
      - 'Операция завершается ошибкой, потому что массивы JAX неизменяемы'
    correct: 1
    explanation: >-
      Метод `set` не изменяет исходный массив `a`: выражение возвращает новый
      массив с заменённым элементом, который сохраняется в `b`.
  - question: >-
      Почему запись `a = a.at[0].set(9)` допустима, хотя массивы JAX
      неизменяемы?
    options:
      - Присваивание временно разрешает изменить исходный массив на месте
      - >-
        Правая часть возвращает новый массив, а присваивание связывает с ним имя
        `a`
      - >-
        Неизменяемость действует только для массивов, сохранённых в другой
        переменной
    correct: 1
    explanation: >-
      Сначала создаётся обновлённый массив, затем имя `a` переназначается на
      него. Прежний объект при этом не изменяется.
---

Здесь легко спутать изменение массива с изменением того, на что указывает имя. В [шаге 7](#step-7) ты видел, что запись `a[0] = 9` для массива JAX запрещена: она пыталась бы исправить существующий объект на месте.

Запись из [шага 8](#step-8) работает иначе. Выражение `a.at[0].set(9)` создаёт обновлённый результат. Если сохранить его в `b`, исходный `a` останется прежним. Если сохранить обратно в `a`, изменится лишь привязка имени: теперь оно указывает на новый массив. Поэтому одинаковые значения после операции ещё не означают мутацию старого массива.
