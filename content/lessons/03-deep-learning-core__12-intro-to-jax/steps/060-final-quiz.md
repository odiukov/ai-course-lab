---
id: 060-final-quiz
type: quiz
title: 'Итоговая проверка: чистые функции, ключи, pytrees, grad, jit и vmap'
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты вызываешь функцию dropout дважды и хочешь получить две независимые
      маски. Как поступить?
    options:
      - Передать один и тот же ключ в оба вызова
      - Разделить ключ и передать каждому вызову свой новый ключ
      - 'Не передавать ключ: JAX сам возьмёт случайное состояние'
      - Изменить исходный ключ на месте после первого вызова
    correct: 1
    explanation: >-
      Случайность в JAX является явным входом. Разделение ключа даёт независимые
      ключи, а повторное использование одного ключа воспроизводит тот же
      случайный результат.
  - question: >-
      Параметры модели хранятся во вложенном словаре массивов. Что лучше всего
      состыкуется с вычислением градиентов и обновлением параметров?
    options:
      - >-
        grad вернёт pytree той же структуры, а tree_map обновит соответствующие
        листья
      - grad объединит все параметры в один массив независимо от их форм
      - 'Градиенты появятся внутри исходных массивов, как скрытые поля'
      - Перед grad нужно вручную вычислить производную каждого слоя
    correct: 0
    explanation: >-
      JAX сохраняет структуру pytree: каждому дифференцируемому листу параметров
      соответствует лист градиента, поэтому их удобно попарно обновлять.
  - question: >-
      Ты обернул шаг обучения в jit. Почему самый первый вызов заметно медленнее
      следующих вызовов с такими же формами?
    options:
      - На первом вызове JAX случайно выбирает устройство
      - На первом вызове JAX трассирует и компилирует вычисление
      - Первый батч всегда обрабатывается без ускорителя
      - grad вычисляет производные только при первом вызове
    correct: 1
    explanation: >-
      Первый вызов включает трассировку и компиляцию. Затем JAX повторно
      использует готовую скомпилированную версию для совместимых входов.
  - question: >-
      Функция predict обрабатывает один пример, а параметры модели должны быть
      общими для всего батча. Какое применение vmap подходит?
    options:
      - Векторизовать параметры и оставить пример общим
      - 'Векторизовать и параметры, и пример по оси ноль'
      - 'Оставить параметры общими, а примеры векторизовать по оси ноль'
      - 'Сначала изменить параметры на месте, затем вызвать predict в цикле'
    correct: 2
    explanation: >-
      Параметры используются всеми примерами совместно, а ось ноль входных
      данных перечисляет элементы батча. Именно это задаётся через in_axes.
---

Дальше тебе пригодится не перечень возможностей JAX, а умение собирать их в один предсказуемый поток: ключ входит в функцию, параметры лежат в pytree, `grad` строит градиенты той же структуры, `vmap` добавляет батч, а `jit` компилирует всё вычисление. Обычно ошибка возникает в месте, где незаметно появляется скрытое состояние: повторно используется ключ, мутируется массив или параметры прячутся внутри объекта.

Сверяй каждое решение с одним вопросом: все ли данные, от которых зависит результат, видны среди входов? Если да, преобразования из [шага 36](#step-36) состыкуются, а шаг обучения останется проверяемым и переносимым.
