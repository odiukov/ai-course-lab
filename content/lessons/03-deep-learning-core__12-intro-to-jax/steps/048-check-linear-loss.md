---
id: 048-check-linear-loss
type: check
title: 'Проверка: формы предсказаний и скалярный результат MSE'
source_anchor: '### Step 3: Forward Pass'
check:
  - question: >-
      Для четырёх объектов predict вернула четыре предсказания, а y содержит
      четыре правильных ответа. Какой формы должен быть результат mse?
    options:
      - Одно число
      - Массив из четырёх ошибок
      - Таблица 4 строки на 4 столбца
    correct: 0
    explanation: >-
      MSE усредняет квадраты ошибок по всем объектам, поэтому итогом должно быть
      одно число.
  - question: >-
      Предсказания имеют форму 4, а y — форму 4 строки на 1 столбец. Почему
      скалярный результат mse ещё не доказывает, что всё верно?
    options:
      - JAX всегда удаляет лишний столбец перед вычислением
      - >-
        При вычитании broadcasting может создать таблицу 4 строки на 4 столбца,
        которую mean всё равно сведёт к одному числу
      - Скалярный loss возможен только для одного объекта
    correct: 1
    explanation: >-
      Несовпадающие формы могут расшириться до таблицы попарных ошибок, а
      итоговое усреднение скроет эту ошибку скалярным результатом.
---

После `predict` из [шага 45](#step-45) проверь не только числа, но и форму: одному объекту должно соответствовать ровно одно предсказание. Затем `mse` из [шага 47](#step-47) должна свести все ошибки к скаляру — одному числу, по которому позже можно будет вычислять градиент.

Главная ловушка — broadcasting. Если предсказания имеют форму `(4,)`, а цели `(4, 1)`, JAX может получить таблицу попарных разностей. Среднее всё равно вернёт скаляр, поэтому правильный тип результата ещё не гарантирует правильное вычисление: сначала должны совпасть формы предсказаний и целей.
