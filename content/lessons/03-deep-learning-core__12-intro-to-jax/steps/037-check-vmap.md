---
id: 037-check-vmap
type: check
title: 'Проверка: параметры общие, примеры идут по оси ноль'
source_anchor: '### vmap: Automatic Vectorization'
check:
  - question: >-
      Ты обрабатываешь батч примеров x одной моделью params. Какой in_axes
      подходит?
    options:
      - '(0, None)'
      - '(None, 0)'
      - '(0, 0)'
      - '(None, None)'
    correct: 1
    explanation: >-
      Параметры модели общие для всех примеров, поэтому им соответствует None.
      Примеры расположены вдоль первой оси x, поэтому для x указана ось 0.
  - question: 'Что изменится, если вместо None указать ось 0 и для params?'
    options:
      - JAX применит один набор параметров ко всему батчу
      - JAX будет ожидать отдельный набор параметров для каждого примера
      - JAX автоматически усреднит параметры
      - JAX перестанет батчить примеры
    correct: 1
    explanation: >-
      Ось 0 сообщает vmap, что аргумент меняется от примера к примеру. Значит,
      вдоль первой оси params должны лежать отдельные наборы параметров.
---

Здесь важно уверенно прочитать `in_axes`, а не просто узнать знакомую запись из [шага 35](#step-35). Для каждого аргумента спроси себя: он один на весь батч или меняется вместе с примерами? Чаще всего путаются именно с `None`: это не пустой аргумент, а указание не добавлять ему батчевую ось. Ноль же означает, что очередной пример берётся вдоль первой оси массива.

> 🎒 **На пальцах.** Представь типографию: печатная форма одна для всего тиража, а листы идут стопкой. Форме соответствует `None` — её не меняют между отпечатками. Листам соответствует `0` — типография последовательно различает их по положению в стопке. Если поставить `0` и форме, пришлось бы подготовить отдельную форму для каждого листа.
