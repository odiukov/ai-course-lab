---
id: 059-transfer
type: check
title: 'Проверка: dropout, градиенты по примерам и обрезка в функциональном стиле'
source_anchor: '## Exercises'
check:
  - question: >-
      Ты применяешь dropout в двух слоях MLP. Как поступить с ключом
      случайности?
    options:
      - Передать один и тот же ключ обоим слоям
      - Разделить ключ и передать каждому слою отдельный подключ
      - Спрятать ключ внутри модели и не передавать его в прямой проход
    correct: 1
    explanation: >-
      Каждому слою dropout нужна собственная случайная маска, поэтому исходный
      ключ разделяют на независимые подключи. Явная передача ключей сохраняет
      функциональный стиль.
  - question: >-
      Ты хочешь получить отдельный градиент параметров для каждой из 32
      картинок, не усредняя их потери. Какая композиция подходит?
    options:
      - Вычислить grad от среднего loss всего батча
      - >-
        Применить vmap к grad функции потерь одного примера, оставив параметры
        общими
      - 'Применить grad к функции, возвращающей вектор из 32 значений loss'
    correct: 1
    explanation: >-
      grad вычисляет градиент скалярного loss одного примера, а vmap батчирует
      это вычисление по картинкам и меткам. В результате pytree градиентов
      получает отдельную батчевую ось для 32 примеров.
---

Здесь важно связать три приёма: dropout получает отдельные подключи, градиенты примеров возникают из композиции `vmap` и `grad`, а обрезка встраивается в цепочку преобразований Optax. Частая ошибка — переиспользовать ключ, усреднить loss раньше времени или изменять градиенты вручную. Опирайся на явную случайность из [шага 12](#step-12) и общие параметры батча из [шага 35](#step-35).

> 🎒 **На пальцах.** Представь проверку 32 посылок на весах. Тебе нужны 32 отдельные квитанции, а не один средний вес. Если показание слишком велико, ограничитель не ломает весы и не переписывает квитанцию: он становится ещё одним явным этапом обработки перед отправкой результата дальше.
