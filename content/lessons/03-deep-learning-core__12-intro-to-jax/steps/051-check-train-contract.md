---
id: 051-check-train-contract
type: check
title: 'Проверка: где считаются градиенты и почему параметры возвращаются наружу'
source_anchor: '### Step 4: JIT-Compiled Training Step'
check:
  - question: Где в train_step вычисляются градиенты параметров?
    options:
      - В вызове jax.value_and_grad(loss_fn)
      - В optimizer.update при создании состояния оптимизатора
      - В optax.apply_updates при применении обновлений
    correct: 0
    explanation: >-
      jax.value_and_grad(loss_fn) вычисляет значение loss и его градиенты по
      параметрам. Оптимизатор получает уже готовые градиенты и преобразует их в
      обновления.
  - question: Почему train_step должен вернуть обновлённые params вызывающему коду?
    options:
      - Чтобы следующий вызов явно получил новую версию параметров вместо старой
      - Чтобы jax.jit смог найти функцию loss_fn после компиляции
      - Чтобы optimizer.update вычислял градиенты вне train_step
    correct: 0
    explanation: >-
      train_step не заменяет параметры в скрытом состоянии. Он создаёт и
      возвращает новую версию, которую вызывающий код должен передать следующему
      шагу обучения.
---

Здесь легко перепутать роли: градиенты появляются внутри `value_and_grad`, как в [шаге 31](#step-31), а оптимизатор лишь решает, какое обновление сделать по этим градиентам. Затем создаётся новая версия параметров.

Она возвращается наружу не ради `jit`, а ради явного потока состояния из [шага 40](#step-40): следующий вызов должен получить именно обновлённые параметры. Если их не вернуть, снаружи останется старая версия, и обучение не продолжится.

> 🎒 **На пальцах.** Представь эстафету с маршрутным листом. Судья внутри этапа отмечает отклонения и выдаёт исправленный лист, но не может тайком заменить документ у следующего участника. Поэтому новый лист нужно вынести наружу и передать в следующий забег.
