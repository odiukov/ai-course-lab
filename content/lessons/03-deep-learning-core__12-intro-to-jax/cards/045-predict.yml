- concept: предсказание линейной модели вычисляется как скалярное произведение входа и весов с последующим добавлением смещения
  question: Заполни тело функции линейной модели на JAX, если веса и смещение хранятся в словаре параметров.
  explanation: '`jnp.dot` объединяет входные признаки с соответствующими весами, а затем к результату прибавляется смещение `params[''b'']`.'
  kind: cloze
  template: |-
    def predict(params, x):
        return ___
  answer: jnp.dot(x, params['w']) + params['b']
  accept:
    - jnp.dot(x, params["w"]) + params["b"]
    - jnp.dot(x, params['w']) + params["b"]
    - jnp.dot(x, params["w"]) + params['b']
  id: 045-predict-1
  fingerprint: 3da97610
