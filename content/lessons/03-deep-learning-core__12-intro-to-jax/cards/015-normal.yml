- concept: вызов jax.random.normal для генерации массива заданной формы
  question: Заполни пропуск так, чтобы функция сгенерировала в JAX массив нормально распределённых значений, используя переданный PRNG-ключ.
  explanation: Функция jax.random.normal принимает явный PRNG-ключ и форму результата. Она сама генерирует массив нормально распределённых значений, поэтому реализовывать распределение вручную не нужно.
  kind: cloze
  template: |-
    def normal(key, shape):
        return jax.random.___(key, shape=shape)
  answer: normal
  accept:
    - normal
  id: 015-normal-1
  fingerprint: 03b9be14
- concept: значения нормального распределения сосредоточены около нуля, а большие отклонения встречаются реже
  question: Какое описание лучше всего соответствует значениям, которые по умолчанию генерирует jax.random.normal?
  explanation: 'jax.random.normal создаёт выборку из стандартного нормального распределения: плотность наибольшая около нуля и уменьшается по мере роста абсолютного отклонения.'
  kind: choice
  options:
    - Большинство значений находится около нуля, а большие по модулю значения встречаются реже
    - Все значения равномерно распределены внутри ограниченного интервала
    - Функция возвращает только неотрицательные значения
    - Знаки значений строго чередуются
  correct: 0
  id: 015-normal-2
  fingerprint: 4e276d21
