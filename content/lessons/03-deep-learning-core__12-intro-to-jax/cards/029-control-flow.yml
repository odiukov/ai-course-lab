- concept: jax.lax.cond выражает зависящее от данных ветвление внутри JIT-компилируемой функции
  question: Функция с декоратором @jax.jit должна выбрать increase или decrease в зависимости от условия score > limit, где score — трассируемый массив. Как следует выразить этот выбор?
  explanation: Python должен выбрать ветку уже во время трассировки, когда конкретное значение score ещё неизвестно. jax.lax.cond записывает ветвление как часть вычисления, которое затем может скомпилировать XLA.
  kind: choice
  options:
    - Обычным Python if
    - Через jax.vmap
    - Через jax.lax.cond
    - Через jax.grad
  correct: 2
  id: 029-control-flow-1
  fingerprint: 12d8a605
- concept: jax.lax.scan выражает повторяющееся вычисление с переносимым состоянием внутри программы JAX
  question: Заполни пропуск примитивом JAX для последовательного применения step_fn к элементам с переносом состояния.
  explanation: 'jax.lax.scan помещает цикл в трассируемую вычислительную программу: функция шага последовательно обрабатывает элементы и явно передаёт состояние дальше. Обычный Python-цикл остаётся частью управления Python.'
  kind: cloze
  template: final_carry, outputs = ___(step_fn, initial_carry, elements)
  answer: jax.lax.scan
  accept:
    - lax.scan
  id: 029-control-flow-2
  fingerprint: 284b0818
