---
id: 037-check-picker
type: check
title: Выбираем алгоритм по ограничениям задачи
source_anchor: '## Use It'
check:
  - question: >-
      У тебя шесть кооперативных агентов, общая награда и дискретные действия.
      Для успеха одному агенту иногда приходится ухудшать свой локальный
      результат ради команды. Что выбрать первым кандидатом?
    options:
      - 'QMIX, потому что действия дискретные'
      - 'MADDPG, потому что агентов меньше десяти'
      - >-
        MAPPO, потому что монотонное смешивание QMIX может не выразить такую
        жертву
      - 'Ни один из трёх, нужны mean-field методы'
    correct: 2
    explanation: >-
      Одного типа действий недостаточно: ограничение QMIX из шага 32 мешает
      представить некоторые командные выгоды от локальной жертвы. MAPPO здесь
      разумнее проверить первым.
  - question: >-
      В системе 150 агентов с общей наградой и дискретными действиями какой
      вывод наиболее обоснован?
    options:
      - Выбрать QMIX из-за дискретных действий
      - Выбрать MAPPO из-за общей награды
      - Выбрать MADDPG из-за большого числа агентов
      - >-
        Не выбирать автоматически ни один из трёх и рассмотреть mean-field
        методы
    correct: 3
    explanation: >-
      При таком масштабе вычислительные и координационные ограничения становятся
      решающими. Простое дерево выбора выводит задачу за пределы разумной
      области этих трёх алгоритмов.
---

Перед тобой три задачи: четыре дрона плавно меняют тягу, шесть роботов выбирают одну из кнопок, а сто пятьдесят курьеров одновременно распределяют заказы. Название алгоритма здесь ничего не решает — сначала отсекай варианты по ограничениям.

Проверь тип награды, пространство действий и масштаб команды. Затем учти выразительные ограничения: дискретность делает QMIX кандидатом, но не отменяет проблему локальной жертвы из [шага 32](#step-32). MADDPG естественнее для непрерывных действий, а MAPPO остаётся практичной отправной точкой, описанной в [шаге 36](#step-36). Главная ловушка — выбрать метод по одному признаку и проигнорировать остальные.
