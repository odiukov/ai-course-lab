- concept: первое использование общего префикса оплачивается по полной ставке, а повторные — по ставке кэшированного чтения
  question: Вычисли стоимость общего префикса, если repeat_count обозначает количество запросов после первого.
  explanation: Первый запрос передаёт префикс по полной входной ставке. Для каждого последующего запроса тот же префикс оплачивается как кэшированное чтение.
  kind: cloze
  template: prefix_cost = prefix_tokens * input_rate + ___
  answer: repeat_count * prefix_tokens * cached_input_rate
  accept:
    - prefix_tokens * repeat_count * cached_input_rate
    - cached_input_rate * prefix_tokens * repeat_count
  id: 007-cached-cost-1
  fingerprint: 59be519f
