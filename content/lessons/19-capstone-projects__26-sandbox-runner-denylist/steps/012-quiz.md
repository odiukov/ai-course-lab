---
id: 012-quiz
type: quiz
title: Разбираем границы денлиста и ненастоящей песочницы
source_anchor: '## Why this is not a real sandbox'
check:
  - question: >-
      Разрешённый интерпретатор запускают с флагом inline-кода. Как должен
      поступить runner?
    options:
      - 'Разрешить, потому что имя executable отсутствует в денлисте'
      - 'Отказать, потому что аргумент превращает строку в исполняемый код'
      - 'Разрешить, если строка не содержит shell-синтаксиса'
      - Заменить строку кода путём к временному файлу
    correct: 1
    explanation: >-
      Одного безопасного имени недостаточно: флаг inline-кода даёт
      интерпретатору выполнить произвольную строку, поэтому такой вызов
      блокируется.
  - question: >-
      Без shell программе передали один аргумент «отчёт; удалить данные». Что
      произойдёт с точкой с запятой?
    options:
      - Она автоматически запустит вторую команду
      - Она разделит аргумент на два
      - Она останется обычной частью одного аргумента
      - Она всегда вызовет отказ денлиста
    correct: 2
    explanation: >-
      При прямом запуске argv не разбирается как командная строка: специальный
      смысл точке с запятой может придать shell, а не subprocess сам по себе.
  - question: >-
      Аргумент выглядит как путь внутри project_root, но символическая ссылка
      ведёт наружу. Какое решение соответствует защите урока?
    options:
      - 'Разрешить, потому что написанный путь начинается с project_root'
      - Разрешить только для чтения
      - Отказать после сравнения фактически разрешённого пути с project_root
      - Проверить только расширение файла
    correct: 2
    explanation: >-
      Граница проверяется по фактической цели пути. Иначе символическая ссылка
      позволяет обойти проверку одним безопасно выглядящим написанием.
  - question: >-
      Runner отклоняет опасные executable, inline-код и выходящие наружу пути.
      Можно ли считать процесс изолированным от системы?
    options:
      - 'Да, денлист даёт такую же изоляцию, как контейнер'
      - 'Да, если все отказы возвращаются через SandboxResult'
      - >-
        Нет, для изоляции нужны независимые ограничения уровня операционной
        системы
      - 'Нет, но достаточно добавить больше имён в денлист'
    correct: 2
    explanation: >-
      Денлист уменьшает число опасных вызовов, но не ограничивает возможности
      уже запущенного процесса. Контейнеры, микровиртуалки и системные лимиты
      создают отдельные рубежи.
---

Дальше тебе пригодится привычка различать фильтр запуска и настоящую изоляцию. Проверки из [шагов 3](#step-3)–[9](#step-9) отсекают частые опасные формы, а единый результат из [шагов 10](#step-10)–[11](#step-11) делает отказ заметным. Но разрешённый процесс всё ещё обладает правами своего окружения. Поэтому в проде денлист становится первым дешёвым слоем, поверх которого добавляют контейнер, ограничения ресурсов, безопасное окружение и минимальные права.

> 🎒 **На пальцах.** Представь музейного смотрителя со списком запрещённых предметов. Он остановит посетителя с молотком, но пропущенный человек всё равно окажется рядом с экспонатами. Настоящую защиту дают ещё витрина, сигнализация и закрытые двери. Список полезен именно как один рубеж, а не как замена зданию.
