---
id: 049-check-vocabulary
type: check
title: 'Проверка: intent, slot, RAG, tool call и agent loop'
source_anchor: '## Key Terms'
check:
  - question: >-
      Бот нашёл в базе инструкцию по возврату, а затем запросил у платёжной
      системы оформление возврата. Как правильно классифицировать эти операции?
    options:
      - Поиск инструкции и оформление возврата — два вызова инструмента
      - >-
        Инструкция извлекается через RAG, а оформление выполняется через tool
        call
      - 'Инструкция является slot, а оформление — intent'
      - Обе операции выполняет RAG
    correct: 1
    explanation: >-
      RAG извлекает релевантные документы для обоснованного ответа. Tool call
      передаёт среде структурированный запрос на выполнение внешнего действия.
  - question: >-
      Пользователь просит заказать билет в Порту на пятницу. Модель вызывает
      сервис, получает ошибку из-за отсутствующего номера паспорта, запрашивает
      номер и повторяет вызов. Что в этом сценарии является agent loop?
    options:
      - Только распознанная цель «заказать билет»
      - Только извлечённые город и дата
      - Только первоначальный вызов сервиса
      - 'Вся последовательность решений, вызовов, наблюдений и следующих решений'
    correct: 3
    explanation: >-
      Agent loop — это управляемый повторяющийся процесс: модель выбирает
      действие, среда выполняет инструмент, результат возвращается как
      наблюдение, после чего модель определяет следующий шаг.
---

После этого блока тебе важно различать не названия, а роли частей системы. Intent и slots из [шага 16](#step-16) описывают цель и параметры запроса. RAG подбирает основания для ответа, tool call запрашивает действие у среды, а agent loop из [шага 28](#step-28) связывает решения, вызовы и результаты в повторяющийся процесс. Частая ошибка — считать найденный документ выполненным действием или один вызов целым агентом.

> 🎒 **На пальцах.** Представь поход в туристический центр. Сотрудник понимает, что тебе нужна экскурсия, записывает город и дату, сверяется с путеводителем, затем отправляет заявку организатору. Если мест нет, он получает ответ и предлагает другой день. Путеводитель дал сведения, заявка совершила действие, а вся цепочка с проверками стала циклом.
