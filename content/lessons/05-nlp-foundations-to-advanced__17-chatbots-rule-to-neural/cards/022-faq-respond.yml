- concept: лучший результат сходства и соответствующий ему FAQ-ответ необходимо обновлять вместе
  question: При переборе FAQ функция нашла новое максимальное сходство и обновила только переменную с оценкой, но не переменную с ответом. К чему это может привести?
  explanation: Максимальная оценка и связанный с ней ответ образуют одну пару. Если обновить только оценку, после проверки порога можно вернуть ответ от прежнего кандидата, а не от победителя.
  kind: choice
  options:
    - Функция может вернуть ответ, который не соответствует вопросу с максимальным сходством
    - Функция всегда вернёт None независимо от порога
    - Сходство Жаккара станет зависеть от порядка слов
    - Все оценки сходства автоматически станут одинаковыми
  correct: 0
  id: 022-faq-respond-1
  fingerprint: f934f0ce
- concept: совпадение с оценкой, равной порогу, считается допустимым
  question: Заполни условие отказа так, чтобы FAQ-ответ возвращался при сходстве, равном порогу.
  explanation: Отказ нужен только при результате строго ниже порога. Поэтому при best_score == threshold функция не возвращает None, а использует найденный FAQ-ответ.
  kind: cloze
  template: |-
    if best_score ___ threshold:
        return None
  answer: <
  accept:
    - <
  id: 022-faq-respond-2
  fingerprint: '8290e234'
