- concept: метод regex.match проверяет совпадение только с начала строки
  question: Что вернёт вызов regex.match("well, hello"), если regex создан как re.compile(r"hello", re.IGNORECASE)?
  explanation: Метод match пытается применить шаблон с начала строки. Для поиска hello в произвольной позиции понадобился бы метод search.
  kind: choice
  options:
    - Объект совпадения для слова hello
    - Совпадения не будет, потому что строка начинается с другого текста
    - Возникнет ошибка из-за запятой
  correct: 1
  id: 010-rule-based-respond-1
  fingerprint: 4a134543
- concept: удаление краевых пробелов нормализует ввод перед сопоставлением с шаблоном
  question: Заполни пропуск, чтобы пробелы по краям пользовательского ввода не мешали сопоставлению с регулярным выражением.
  explanation: Метод strip удаляет пробельные символы в начале и конце строки, после чего match проверяет начало уже нормализованного ввода.
  kind: cloze
  template: normalized_input = user_input.___()
  answer: strip
  accept:
    - strip
  id: 010-rule-based-respond-2
  fingerprint: 36f03b87
