- concept: имя в tool call выбирает функцию из реестра, а словарь arguments задаёт её именованные аргументы
  question: Контроллер получил объект call с полями name и arguments. Как корректно выполнить запрошенный инструмент после валидации этих полей?
  explanation: Поле name используется как ключ реестра tools, а оператор ** раскрывает словарь arguments в именованные аргументы выбранной функции.
  kind: choice
  options:
    - tools[call["name"]](**call["arguments"])
    - tools[call["arguments"]](call["name"])
    - llm(call["name"] + str(call["arguments"]))
    - eval(call["name"])(call["arguments"])
  correct: 0
  id: 029-tool-contract-1
  fingerprint: 190e6393
- concept: модель формирует заявку на инструмент, а среда проверяет её и выполняет зарегистрированную функцию
  question: Объясни своими словами, как распределяются роли модели и среды при обработке tool call.
  explanation: Tool call выражает намерение модели совершить действие. Реальный побочный эффект возникает только при выполнении зарегистрированной функции средой.
  kind: open
  reference: Модель выбирает имя доступного инструмента и формирует словарь аргументов, но сама функцию не исполняет. Среда проверяет структуру заявки, находит функцию по имени в реестре инструментов и вызывает её с переданными аргументами.
  id: 029-tool-contract-2
  fingerprint: 075e36d4
