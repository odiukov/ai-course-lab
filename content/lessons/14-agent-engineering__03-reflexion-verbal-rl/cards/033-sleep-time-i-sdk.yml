- concept: 'компоненты Reflexion опознаются по роли в цикле, а не по названию примитива фреймворка: отбраковщик по оценке играет роль Evaluator, объект памяти, переживающий прогон, — роль эпизодического буфера'
  question: Во фреймворке нет ни класса Reflexion, ни узла «self-reflection». Есть механизм, который прерывает прогон, если проверка результата не прошла, и объект памяти, который сохраняет переписку между запусками агента. Что из этого следует?
  explanation: 'Reflexion — это композиция ролей (актор, оценщик, автор рефлексии, память между попытками), а не библиотечный класс. Механизм, отбраковывающий траекторию по оценке, закрывает роль Evaluator; персистентная память между прогонами — эпизодический буфер. Недостающее — сам внешний цикл и вызов модели, пишущей рефлексию: проверка выдаёт только вердикт, текст причины она не порождает.'
  kind: choice
  options:
    - 'Паттерн собирается вручную: механизм проверки берёт на себя роль Evaluator, объект памяти — роль эпизодического буфера, внешний цикл дописывается поверх'
    - 'Паттерн нереализуем: без специального примитива рефлексии траектория не может быть оценена'
    - Нужно заменить фреймворк на тот, где рефлексия объявлена как отдельная сущность, иначе оценка будет некорректной
    - 'Достаточно включить механизм проверки: он сам начнёт формулировать причину провала текстом'
  correct: 0
  id: 033-sleep-time-i-sdk-1
  fingerprint: f11a91d5
- concept: 'вынос шага цикла в фоновый простой определяется тем, кто потребляет его выход: вердикт оценщика нужен текущему прогону для решения «стоп или ещё попытка», а текст рефлексии нужен только следующей попытке'
  question: Агент отвечает пользователю в реальном времени. Какой шаг цикла попыток можно перенести на фоновое время простоя, не сломав логику, и почему?
  explanation: 'Вердикт оценщика — вход решения «отдавать ответ или запускать ещё попытку», он на критическом пути. Текст рефлексии читается только при сборке промпта следующей попытки, поэтому его можно писать в простое (sleep-time compute): буфер к моменту следующего запроса уже пополнен, а латентность текущего ответа не выросла на лишний вызов модели.'
  kind: choice
  options:
    - 'Генерацию рефлексии: её текст потребляет только следующая попытка, поэтому на решение по текущему ответу он не влияет'
    - 'Оценку траектории: вердикт всё равно понадобится не сразу, а при разборе логов'
    - 'Вызов актора: пользователь может подождать ответ, пока фон досчитывает'
    - 'Любой шаг: порядок внутри цикла попыток не влияет на результат'
  correct: 0
  id: 033-sleep-time-i-sdk-2
  fingerprint: f8f22dc0
- concept: отложенная рефлексия требует сохранить саму траекторию провала, потому что фоновому воркеру нужен тот же вход, что и синхронному Self-Reflector'у
  question: Генерацию рефлексии решили выполнять фоновым процессом в простое, а не сразу после провала. Что обязательно нужно сохранить в момент провала, чтобы это работало?
  explanation: Автор рефлексии диагностирует причину по действиям и наблюдениям траектории плюс оценке. Отложив вызов, вы разрываете момент провала и момент диагноза, поэтому вход приходится персистить. Бинарный флаг сам по себе причину не несёт, а перезапуск задачи и дороже, и не воспроизводит ту же траекторию из-за недетерминизма.
  kind: choice
  options:
    - Траекторию попытки вместе с вердиктом оценщика — фоновому процессу нужен тот же вход, что и синхронному автору рефлексии
    - 'Только вердикт оценщика: по бинарному флагу причина провала восстанавливается'
    - 'Только финальный ответ актора: промежуточные действия для диагноза не нужны'
    - 'Ничего: фоновый процесс перезапустит задачу и получит траекторию заново'
  correct: 0
  id: 033-sleep-time-i-sdk-3
  fingerprint: b884bbb5
