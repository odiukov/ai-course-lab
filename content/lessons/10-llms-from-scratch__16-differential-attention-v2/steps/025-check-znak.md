---
id: 025-check-znak
type: check
title: 'Проверка: чему равен вес при a1=0.02 и lam·a2=0.05'
source_anchor: '### Step 3: two softmax branches + subtraction'
check:
  - question: >-
      В строке diff_weights для одного токена получилось a1 = 0.02, а lam
      умножить на a2 даёт 0.05. Чему равен итоговый вес этого токена и что с ним
      произойдёт дальше?
    options:
      - 'Вес равен 0.03, вклад токена складывается с остальными как обычно'
      - 'Вес равен минус 0.03, вектор значений этого токена вычитается из выхода'
      - 'Вес обрежется до нуля, токен просто выпадет из взвешенной суммы'
      - 'Вес равен минус 0.03, но перед умножением на V его надо взять по модулю'
    correct: 1
    explanation: >-
      Вычитание идёт напрямую: 0.02 минус 0.05 равно минус 0.03. Знак
      сохраняется, и вектор значений этого токена входит в сумму со знаком
      минус.
  - question: Что означает отрицательный вес после вычитания двух карт внимания?
    options:
      - 'Ошибку в коде: веса внимания по определению не бывают отрицательными'
      - >-
        Что вторая карта считает токен важнее, чем первая, и его вклад активно
        вычитается
      - >-
        Что сумма весов в строке перестала быть осмысленной и строку надо
        перенормировать
      - >-
        Что lambda подобрана неверно и её нужно уменьшить до значения меньше
        единицы
    correct: 1
    explanation: >-
      Отрицательный вес возникает там, где вторая карта дала токену больше
      внимания, чем первая. Это сигнал вычесть его вклад, а не сбой вычислений.
---

Здесь проверяется одна конкретная вещь: умеешь ли ты руками посчитать элемент строки `diff_weights` и правильно прочитать знак результата.

Спотыкаются обычно на двух местах. Первое — рефлекс из [шага 1](#step-1): «веса же всегда положительные», поэтому минус хочется списать на ошибку или заменить нулём. Второе — попытка «починить» отрицательное число: взять модуль, обрезать снизу, перенормировать строку. Любое из этих действий уничтожает ровно ту информацию, ради которой вычитание и делалось.

Считай честно, знак оставляй как есть — умножение на V разберётся с ним само.
