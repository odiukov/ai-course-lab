---
id: 049-quiz
type: quiz
title: 'Итоговая проверка: шумовой пол, вычитание, lambda и диф V1 → V2'
source_anchor: '## Key Terms'
check:
  - question: >-
      Контекст вырос с 4000 до 32000 токенов, а по-настоящему нужных токенов в
      нём по-прежнему два-три. Что произойдёт с шумовым полом?
    options:
      - >-
        Он исчезнет: каждый отдельный вес станет в восемь раз меньше, а значит,
        несвязанные токены перестанут влиять на ответ
      - >-
        Суммарный вес на несвязанных позициях не уменьшится: каждый вес упал, но
        самих позиций стало в восемь раз больше
      - >-
        Он вырастет только если модель обучена на коротких текстах, а на длинных
        softmax сам обнуляет хвост
    correct: 1
    explanation: >-
      Вес одного лишнего токена порядка одной N-ной, но таких токенов почти N
      штук, поэтому их сумма остаётся заметной долей внимания и на длинном
      контексте съедает сигнал.
  - question: 'Почему вычитание второй карты внимания гасит шум сильнее, чем сигнал?'
    options:
      - >-
        Потому что вторая карта считается по меньшей размерности и все её веса
        заведомо мельче
      - >-
        Потому что перед вычитанием вторая карта нормируется так, что на
        сигнальной позиции у неё всегда ноль
      - >-
        Потому что шум примерно одинаков в обеих картах и при вычитании взаимно
        сокращается, а сигнал есть только в первой карте и уцелевает
    correct: 2
    explanation: >-
      Обе половины Q и K видят одни и те же несвязанные токены и дают на них
      похожие мелкие веса; разность их почти обнуляет. Сигнальная позиция сильна
      только в первой карте, поэтому после вычитания она остаётся.
  - question: Ты подставил lambda = 0 в формулу вычитания. Что получится?
    options:
      - >-
        Обычное внимание по первой половине голов: вторая карта никак не
        участвует, шумовой пол не гасится
      - 'Полное обнуление внимания: без вычитания веса не нормируются'
      - 'Максимальное подавление шума, потому что вторая карта не мешает первой'
    correct: 0
    explanation: >-
      При lambda = 0 второе слагаемое пропадает целиком, и остаётся ровно
      softmax-карта первой половины — то есть базовый attention со всем его
      хвостом.
  - question: Чем V2 отличается от V1 по цене?
    options:
      - >-
        V1 платил параметрами, V2 платит скоростью декодинга: за FlashAttention
        приходится грузить KV-кеш дважды
      - >-
        V1 сохранял число параметров, урезая размерность головы, и платил
        скоростью декодинга; V2 сохраняет скорость и платит примерно восемью
        процентами лишних параметров
      - >-
        Обе версии стоят одинаково, разница только в том, что V2 добавил
        per-head RMSNorm для устойчивости
    correct: 1
    explanation: >-
      V1 держал бюджет параметров, ужимая голову вдвое, и из-за этого читал кеш
      значений дважды за шаг. V2 удваивает Q-головы при тех же KV-головах:
      декодинг идёт со скоростью базового блока, а счёт приходит в параметрах.
---

Этот урок дальше в курсе будет всплывать в двух ролях. Первая — диагностическая: когда модель на длинном контексте начинает «терять середину», ты уже умеешь померить это числом, а не ощущением — через отношение веса на нужной позиции к среднему весу на остальных. Вторая — архитектурная: DIFF V2 встаёт в стек как модуль рядом с GQA, MoE и FlashAttention, и решение о нём принимается один раз, до претрейна.

Чаще всего спотыкаются на трёх местах. Отрицательный вес после вычитания выглядит как баг, хотя это осмысленный сигнал ([шаг 23](#step-23)). Шумовой пол путают с ошибкой реализации, хотя это неизбежное свойство softmax ([шаг 1](#step-1)). И lambda принимают за настройку, которую можно подкрутить на инференсе, — а она обучается вместе с весами.
