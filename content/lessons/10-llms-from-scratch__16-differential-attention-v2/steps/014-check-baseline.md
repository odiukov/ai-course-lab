---
id: 014-check-baseline
type: check
title: 'Проверка: что вернут attention_weights и attend'
source_anchor: '### Step 1: standard softmax attention'
check:
  - question: 'У тебя 300 токенов, размерность d равна 64. Что вернёт attention_weights?'
    options:
      - Список из 300 чисел — по одному весу на каждый токен
      - >-
        Список из 300 строк по 300 чисел в каждой, и сумма чисел внутри любой
        строки равна 1
      - >-
        Список из 300 строк по 64 числа в каждой, потому что веса живут в той же
        размерности, что и векторы
      - Одно число — итоговый вес самого важного токена
    correct: 1
    explanation: >-
      Каждый из 300 запросов сравнивается с каждым из 300 ключей, значит
      получается таблица 300 на 300. Softmax применяется построчно, поэтому
      сумма внутри строки — ровно 1.
  - question: >-
      Те же 300 токенов и d равное 64. Что вернёт attend и чему равны суммы
      внутри результата?
    options:
      - '300 векторов по 64 числа, и сумма чисел внутри каждого вектора равна 1'
      - '300 векторов по 300 чисел, суммы произвольные'
      - >-
        300 векторов по 64 числа, суммы произвольные — единица там взяться
        неоткуда
      - Один вектор из 64 чисел — усреднение по всем токенам сразу
    correct: 2
    explanation: >-
      Каждая строка весов схлопывает 300 значений в один вектор той же длины,
      что и значения, то есть 64. Единице равна сумма весов, а не сумма
      координат результата — значения могут быть любыми, в том числе
      отрицательными.
---

Прежде чем идти дальше, стоит убедиться, что ты можешь предсказать форму результата, не запуская код. Возьми любые размеры — скажем, 300 токенов и d, равное 64 — и скажи вслух, сколько строк и сколько чисел в строке вернёт каждая из двух функций, написанных в [шаге 11](#step-11) и [шаге 13](#step-13).

Спотыкаются обычно на двух вещах. Первая: путают, где живёт число 300, а где 64 — веса сравнивают токены с токенами, а значения тянут за собой свою размерность. Вторая: переносят «сумма равна 1» с весов на выход. Единица — свойство строки весов после softmax, и дальше по конвейеру она не едет.
