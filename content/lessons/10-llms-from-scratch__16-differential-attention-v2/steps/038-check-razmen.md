---
id: 038-check-razmen
type: check
title: 'Проверка: кто чем платит — V1 или V2'
source_anchor: '### V1 vs V2: the diff'
check:
  - question: Какая цепочка правильно описывает инженерный размен V1 при декодинге?
    options:
      - >-
        Ради сохранения числа параметров размерность головы урезали, а две
        softmax-ветки потребовали двух чтений кеша значений за шаг
      - >-
        Сокращение числа параметров уменьшило только качество модели, не
        затронув скорость декодинга
      - 'Удвоение KV-голов увеличило кеш, зато позволило читать его реже'
    correct: 0
    explanation: >-
      V1 сохранял число параметров за счёт уменьшенной размерности головы, но
      кеш значений приходилось загружать отдельно для каждой softmax-ветки.
      Поэтому декодинг был медленнее базового.
  - question: >-
      Почему V2 может декодировать эффективнее V1, хотя содержит дополнительный
      блок параметров?
    options:
      - Дополнительные параметры полностью устраняют вычисления softmax
      - >-
        Декодинг ограничен прежде всего пропускной способностью памяти, поэтому
        одно чтение KV-кеша выгоднее двух
      - 'Число параметров не влияет ни на память, ни на вычисления модели'
    correct: 1
    explanation: >-
      На авторегрессионном декодинге главным ограничением часто становится
      перенос KV-кеша из HBM. V2 обслуживает обе query-ветви одним чтением кеша,
      поэтому дополнительные вычисления обходятся дешевле повторного трафика
      памяти.
---

Ты дочитал до места, где две версии одного и того же метода дают ровно противоположные ответы на вопрос «чем платим». Это удобный момент проверить себя, потому что оба варианта звучат разумно и легко перепутать, кто кому чего экономил.

После [шагов 33](#step-33)–[36](#step-36) ты должен уметь развернуть цепочку в обе стороны: от решения «держим параметры фиксированными» — к урезанной размерности головы, к половинному кешу значений, к двум чтениям за шаг декодинга. И обратно: от «удваиваем query-головы, KV-головы оставляем» — к одному прогону кеша, работающему без переделок FlashAttention и выросшей арифметической интенсивности.

Спотыкаются обычно на одном: считают, что «больше параметров» автоматически значит «медленнее». На шаге генерации токена узкое место не в арифметике, а в том, сколько байт подняли из памяти. Держи это в голове, отвечая.
