---
id: 044-check-sovmestimost
type: check
title: 'Проверка: почему MoE «да», а FlashAttention в V1 было «нет»'
source_anchor: '### How it stacks with other 2026 knobs'
check:
  - question: Почему в таблице совместимости MoE стоит «да» практически без оговорок?
    options:
      - >-
        MoE и DIFF используют одну и ту же операцию вычитания, поэтому
        складываются
      - >-
        MoE живёт в MLP-части блока, а DIFF меняет только внутренности attention
        — они не пересекаются
      - 'MoE уменьшает число KV-голов, и это компенсирует удвоение Q-голов в V2'
    correct: 1
    explanation: >-
      DIFF V2 — модуль внутри attention. MoE подменяет MLP-часть блока, эти две
      правки трогают разные куски и просто не встречаются.
  - question: 'Почему FlashAttention в V1 был «нет», а в V2 стал «да»?'
    options:
      - >-
        В V1 вычитание двух карт требовало самописного ядра, а V2 свёл всё к
        обычному attention на удвоенном числе Q-голов
      - 'В V1 не хватало параметров, и FlashAttention не мог обучиться'
      - >-
        FlashAttention не работает с обучаемой lambda, а в V2 lambda сделали
        фиксированной
    correct: 0
    explanation: >-
      Дело было не в математике, а в реализации: V1 не укладывался в готовое
      ядро, V2 переупаковал ту же идею так, что стандартное ядро подходит.
---

Посмотри на таблицу совместимости ещё раз и обрати внимание на странность: почти везде «да», и только одна клетка когда-то была «нет». Причём «нет» стояло не там, где ты бы ожидал — не у MoE, не у RoPE, а у FlashAttention.

Здесь и проходит граница, которую надо уметь проводить самому. Одни фичи не конфликтуют с DIFF потому, что живут в другой части блока: MoE меняет MLP, DIFF меняет attention, они друг о друге не знают. Другая была несовместима не по смыслу, а по реализации — из-за самописного ядра, и это чинилось перепаковкой ([шаг 36](#step-36)).

Спотыкаются обычно на том, что считают «нет» у FlashAttention математическим запретом. Оно было инженерным.
