---
id: 031-check-rule-of-two
type: check
title: 'Проверка: разложить три инструмента по колонкам'
source_anchor: '### The Rule of Two (Meta, 2026)'
check:
  - question: >-
      Агент использует описание инструмента с чужого MCP-сервера, держит в
      контексте пароль от прод-базы и собирается отправить результат через
      Slack. Что должен сделать host?
    options:
      - >-
        Пропустить вызов: описание пришло от сервера, поэтому считается
        доверенным
      - Отказать или запросить расширение прав до выполнения вызова
      - 'Пропустить вызов: отправка сообщения не является значимым действием'
      - 'Выполнить вызов, но записать его в журнал для последующего разбора'
    correct: 1
    explanation: >-
      В одном ходе сочетаются недоверенный ввод, чувствительные данные и
      значимое действие. По Rule of Two host должен остановить вызов или заранее
      получить явное расширение прав.
  - question: >-
      Агент читает issue с GitHub, использует данные из прода и должен отправить
      письмо. Как изменить ход, чтобы он удовлетворял Rule of Two без расширения
      прав?
    options:
      - Добавить в системный промпт просьбу игнорировать инструкции из issue
      - Проверить issue регулярным выражением перед обработкой
      - Убрать отправку письма и оставить только чтение данных
      - Включить подробное журналирование инструментальных вызовов
    correct: 2
    explanation: >-
      После удаления значимого действия в ходе остаются только недоверенный ввод
      и чувствительные данные. Prompt-инструкция, regex и журналирование не
      устраняют ни одну из категорий Rule of Two.
---

После [шагов 27](#step-27)–[30](#step-30) ты должен уметь брать конкретный ход агента и раскладывать его по трём колонкам: откуда пришёл текст, что лежит в контексте, что инструмент делает с миром. Дальше — просто счёт: два пункта проходят, три не проходят.

Спотыкаются обычно на двух вещах. Первая — описание инструмента считают доверенным, раз оно «от сервера, а не от человека»; оно попадает в промпт дословно, значит это тот же недоверенный ввод. Вторая — правило пытаются применить к инструменту вообще, а не к одному конкретному ходу: инструмент отправки письма сам по себе не запрещён, запрещена его встреча с двумя другими пунктами в одном вызове.
