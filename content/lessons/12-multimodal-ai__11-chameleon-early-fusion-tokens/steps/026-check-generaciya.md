---
id: 026-check-generaciya
type: check
title: 'Проверка: сколько шагов предсказания на кота с подписью'
source_anchor: '### Mixed-modality generation'
check:
  - question: >-
      На промпт «Нарисуй кота и опиши его» Chameleon выдал 1024 image-токена, а
      потом подпись из 9 текстовых токенов. Если учитывать также открывающий и
      закрывающий теги изображения, сколько всего шагов предсказания следующего
      токена произошло?
    options:
      - '1033: учитываются только image- и текстовые токены'
      - '1035: 1024 image-токена, 9 текстовых токенов и 2 тега'
      - '1034: один из тегов не предсказывается моделью'
      - >-
        11: картинка считается одним шагом, а токенами являются только теги и
        текст
    correct: 1
    explanation: >-
      Каждый токен предсказывается отдельным шагом. Поэтому к 1024 image-токенам
      и 9 текстовым добавляются оба служебных тега.
  - question: 'Модель начала ответ с картинки, а не с текста. Что за этим стоит?'
    options:
      - >-
        Перед первым image-токеном сработал внутренний переключатель в режим
        рисования
      - Тег <image> запускает вместо softmax отдельный декодер VQ-VAE
      - >-
        На каждом шаге выбирался самый вероятный ID из общего словаря, и первые
        1024 раза победители оказались из диапазона картинки
      - 'Порядок задаётся промптом: слово «нарисуй» стоит первым'
    correct: 2
    explanation: >-
      Распределение одно — по всем 40192 ID сразу, как в [шаге 20](#step-20).
      Модальность ответа — это просто то, из какой части словаря пришли
      победившие ID.
---

Ты видел в [шаге 24](#step-24) вывод на «Нарисуй кота и опиши его»: длинная строка чисел между тегами, потом фраза про рыжего кота. Соблазн прочитать это как два действия: сначала модель «нарисовала», потом «написала».

Проверь себя на счёте. Считать надо не картинки и не фразы, а шаги: сколько раз модель выбрала один ID из общего словаря. Здесь обычно спотыкаются дважды — думают, что картинка выходит одним шагом, и забывают, что теги тоже отдельные токены и тоже стоят по шагу.

После этого куска ты должен уметь по такому выводу сказать: сколько было шагов и почему ни один из них не был особенным.
