- concept: постоянные разделы памяти размещают вне зоны вытеснения буфера сообщений, поэтому их место в собранной строке задаётся не читаемостью, а политикой освобождения места
  question: Агент собирает промпт из постоянных фактов о пользователе и буфера диалога. Разработчик поставил постоянные факты в конец строки, после истории сообщений. Что произойдёт при первом же переполнении главного контекста?
  explanation: 'Вытеснение работает по позиции в собранной строке, а не по смыслу фрагмента. Постоянные разделы ставят выше буфера именно для того, чтобы их не задело освобождение места: они должны занимать фиксированный кусок окна всегда, а расходоваться должен только буфер сообщений.'
  kind: choice
  options:
    - 'Ничего: политика вытеснения по определению защищает постоянные разделы, где бы они ни лежали'
    - Постоянные факты попадут под ту же политику освобождения места, что и реплики, и могут быть удалены наравне с болтовнёй
    - Модель перестанет видеть последнее сообщение пользователя, потому что оно окажется не последним
    - 'Промпт просто не соберётся: сериализатор откажется класть словарь после списка'
  correct: 1
  id: 018-sborka-prompta-1
  fingerprint: 0bcc0155
- concept: 'детерминированный порядок сериализации главного контекста в одну строку: фиксированный префикс, затем история, затем свежий запрос последним'
  question: Расположи фрагменты в том порядке, в котором рантайм склеивает их в строку промпта перед вызовом модели.
  explanation: 'Порядок фиксирован и одинаков для каждого вызова: сначала неизменяемый верх (инструкция плюс постоянная память), затем буфер истории, последней строкой — текущий запрос. Новое сообщение всегда приклеивается в самый низ, поэтому верх промпта от хода к ходу не меняется, а растёт и обрезается только хвост.'
  kind: order
  items:
    - Системная инструкция и постоянные разделы памяти о пользователе
    - История предыдущих реплик диалога в хронологическом порядке
    - Новое сообщение пользователя текущего хода
  id: 018-sborka-prompta-2
  fingerprint: 2a77bed9
- concept: двухуровневая структура памяти существует только в рантайме; модель получает плоский текст, поэтому перед каждым вызовом требуется сериализация
  question: 'В коде агента память хранится как структура: словарь постоянных фактов и список сообщений. Объясни своими словами, что из этой структуры получает сама модель и какой шаг обязателен перед каждым вызовом.'
  explanation: Ключевое различие — между представлением состояния в коде и представлением на входе модели. Двухуровневость (постоянные разделы плюс буфер) — свойство рантайма; в промпт она приезжает как один текст с жёстким верхом и подвижным низом.
  kind: open
  reference: 'Модель не получает ни словаря, ни списка — API принимает текст. Структура живёт только на стороне рантайма как удобство для кода: адресное обновление разделов, подсчёт токенов, вытеснение. Перед каждым вызовом рантайм обязан сериализовать структуру в одну плоскую строку по детерминированному шаблону, и порядок склейки — часть контракта, потому что он определяет, что стоит в неизменяемом префиксе, а что попадает в вытесняемый хвост.'
  id: 018-sborka-prompta-3
  fingerprint: '36483294'
