- concept: softmax превращает логиты строки в нормированное распределение вероятностей
  question: 'Заполните формулу softmax для логита z_j:'
  explanation: Softmax экспоненцирует каждый логит и делит результат на сумму экспонент всех логитов строки. Поэтому вероятности неотрицательны и в сумме дают единицу.
  kind: cloze
  template: p_j = ___
  answer: e^{z_j} / sum_k e^{z_k}
  accept:
    - exp(z_j) / sum_k exp(z_k)
    - e^z_j / sum_k e^z_k
  id: 015-softmax-po-stroke-1
  fingerprint: f663d7b8
