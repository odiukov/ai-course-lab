---
id: 043-final-quiz
type: quiz
title: >-
  Итоговая проверка: составьте контракт, классифицируйте diff, выберите фичу и
  слейте ограничения без расширения прав
source_anchor: '## Exercises'
check:
  - question: >-
      Ты поручаешь исправить сброс токена только в app/auth/**. Изменять
      scripts/** нельзя, а развёртывание требует твоего решения. С каким
      контрактом можно начинать работу?
    options:
      - >-
        Есть task_id, goal, allowed_files, forbidden_files и rollback_plan, но
        приёмка сформулирована как «всё работает».
      - >-
        Есть task_id, проверяемый goal, allowed_files app/auth/**,
        forbidden_files scripts/**, команда и ожидаемый результат приёмки,
        rollback_plan и approval для развёртывания.
      - >-
        Есть task_id, проверяемый goal, allowed_files app/auth/**, команда
        приёмки и rollback_plan, но границы одобрения не указаны.
      - >-
        Указан только подробный goal, а разрешённые пути агент выведет из него
        сам.
    correct: 1
    explanation: >-
      Этот вариант задаёт проверяемое обещание, файловые границы, откат и
      решение, которое нельзя принимать автономно. Остальные варианты оставляют
      существенный пробел.
  - question: >-
      Контракт разрешает app/** и запрещает app/secrets/**. Diff изменил
      app/login.py, app/secrets/key.py и docs/runbook.md, а все тесты прошли.
      Какой отчёт верен?
    options:
      - >-
        Все три файла допустимы, потому что успешные тесты подтверждают
        результат.
      - >-
        app/login.py входит в объём, app/secrets/key.py нарушает явный запрет,
        docs/runbook.md находится вне прописанного объёма; прогон нельзя
        принять.
      - >-
        Оба пути внутри app/** допустимы, а замечание требуется только для
        docs/runbook.md.
      - >-
        Все три пути считаются вне объёма, потому что diff затронул больше
        одного семейства файлов.
    correct: 1
    explanation: >-
      Явный запрет сильнее разрешения, а непрописанный путь не получает
      разрешение автоматически. Успешные проверки не расширяют контракт.
  - question: >-
      В feature_list.json фича F-7 указана как active и имеет статус
      in_progress, а F-8 имеет статус todo. Что должна сделать новая сессия?
    options:
      - Продолжить F-7 и не начинать F-8.
      - Перевести F-7 в todo и выбрать F-8.
      - 'Оставить F-7 активной, но одновременно перевести F-8 в in_progress.'
      - Выбрать любую из двух фич по своему усмотрению.
    correct: 0
    explanation: >-
      Активная фича уже связывает сессию с начатой работой. Запуск второй
      нарушил бы инвариант единственного статуса in_progress.
  - question: >-
      Проект разрешает app/** и docs/**, запрещает scripts/**, даёт 40 минут,
      требует approval для deploy и допускает api.example.com и
      logs.example.com. Контракт задачи разрешает app/** и scripts/**, запрещает
      docs/private/**, даёт 25 минут, требует approval для migration и допускает
      только api.example.com. Что получится после слияния?
    options:
      - >-
        Разрешены app/**, docs/** и scripts/**; запреты сохраняются только
        общие; бюджет 40 минут; approval нужен только для deploy.
      - >-
        Разрешён app/**; запрещены scripts/** и docs/private/**; бюджет 25
        минут; approvals нужны для deploy и migration; сеть ограничена
        api.example.com.
      - >-
        Разрешён app/**; запрещён только scripts/**; бюджет 25 минут; approvals
        и сетевые списки выбираются из контракта задачи.
      - >-
        Разрешены app/** и scripts/**; запрещён docs/private/**; бюджет 40
        минут; сеть допускает оба хоста.
    correct: 1
    explanation: >-
      Наименьшие привилегии дают пересечение разрешений и сетевых списков,
      объединение запретов и approvals, а также минимальный временной бюджет.
---

Перед тобой один прогон, но проверить нужно сразу четыре решения: годится ли контракт до первого изменения, как разложить diff, какую фичу продолжить и что останется разрешённым после слияния ограничений. Опирайся на отрицательные границы из [шага 10](#step-10), стартовый инвариант из [шага 25](#step-25) и правила слияния из [шагов 31](#step-31)–[33](#step-33).

Главная ловушка одна: полезный результат, зелёные тесты или более широкий родительский контракт не выдают дополнительных прав. Если путь, хост, время или решение не проходят итоговую строгую границу, агент должен остановиться и оставить проверяемое объяснение. Именно такой гейт дальше отличает управляемую автономность от работы «по добрым намерениям».
