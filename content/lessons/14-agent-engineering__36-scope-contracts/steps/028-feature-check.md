---
id: 028-feature-check
type: check
title: >-
  Когда выбор фичи должен продолжиться, а когда сессия обязана отказаться от
  старта?
source_anchor: '### Two altitudes of scope: the feature list and the task contract'
check:
  - question: >-
      Указатель активной работы пуст, ни одна фича не имеет статус in_progress,
      но в списке две фичи со статусом todo. Что должна сделать сессия?
    options:
      - Выбрать ровно одну todo-фичу и начать только её
      - 'Начать обе, чтобы не терять время на второй запуск'
      - 'Отказаться от старта, потому что todo-фич больше одной'
    correct: 0
    explanation: >-
      Несколько todo — это очередь, а не нарушение: инвариант ограничивает число
      одновременно активных фич, а не число ожидающих. Сессия берёт из очереди
      ровно одну запись и фиксирует выбор в active.
  - question: >-
      Поле active указывает на фичу поиска, а единственный статус in_progress
      стоит у фичи импорта. Почему сессия не может просто выбрать одно из двух
      значений и продолжить?
    options:
      - >-
        Потому что доска даёт два разных ответа на вопрос «что сейчас в работе»,
        и данных о том, какой из них отражает решение человека, в ней нет
      - 'Потому что active всегда менее надёжен, чем статус фичи'
      - 'Потому что статус фичи всегда менее надёжен, чем active'
    correct: 0
    explanation: >-
      Ни одно из полей не объявлено приоритетным — они существуют именно для
      взаимной сверки. При расхождении неизвестно, что произошло: недописанная
      передача, ручная правка или сбой. Выбор «более вероятного» поля молча
      переписывает решение человека, поэтому сессия останавливается.
---

Посмотри на состояние доски перед выбором. Если `active` совпадает с единственной `in_progress`, сессия продолжает эту фичу и не ищет новую. Если оба указателя пусты, функция из [шага 27](#step-27) может выбрать ровно одну `todo`. Но нарушение инварианта из [шага 25](#step-25) — две `in_progress`, несовпадающие указатели или ссылка на отсутствующую фичу — требует отказа от старта. Это не повод «угадать намерение»: ошибка уже находится в общем состоянии проекта.

> 🎒 **На пальцах.** На однопутной железной дороге диспетчер пропускает уже заявленный поезд или, если путь свободен, назначает следующий. Но если табло показывает один поезд, а стрелка маршрута — другой, движение останавливают. Выбирать, какой сигнал считать правдой, во время движения слишком опасно.
