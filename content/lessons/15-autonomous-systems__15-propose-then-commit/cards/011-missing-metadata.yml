- concept: строка из одних пробелов считается незаполненной, поэтому проверка строкового значения идёт после удаления пробельных символов
  question: 'Заполни проверку: значение поля признаётся отсутствующим, если это None или строка, состоящая только из пробельных символов.'
  explanation: '`"   ".strip()` даёт пустую строку, то есть ложное значение, и поле попадает в список пропусков. Без `strip` пробельная заглушка выглядит заполненной, и ревьюер получает предложение с пустой графой.'
  kind: cloze
  template: |-
    if value is None or (isinstance(value, str) and not value.___()):
        missing.append(field)
  answer: strip
  accept:
    - strip
    - strip()
  id: 011-missing-metadata-1
  fingerprint: 65e8bdc9
- concept: 'ложное значение не равно отсутствующему: 0 и пустая коллекция — содержательные ответы, а не пробелы'
  question: В предложении поле радиуса поражения равно 0, а поле затронутых прав — пустой список. Остальные обязательные поля заполнены. Что должна вернуть функция, ищущая пропуски метаданных?
  explanation: Проверка через `if not value` отбрасывает 0, пустой список и `False` вместе с настоящими пропусками. Но нулевой радиус и пустой набор прав — это ответы ревьюеру, а не молчание. Отсутствие проверяется по None и пустой после `strip` строке, а не по общей ложности.
  kind: choice
  options:
    - 'Пустой список: оба значения содержательны — нулевой радиус и отсутствие дополнительных прав'
    - Оба поля как пропущенные, потому что их значения ложны при проверке на истинность
    - 'Только радиус поражения: число 0 всегда означает незаполненную графу'
    - 'Только права: пустая коллекция всегда означает, что поле не заполняли'
  correct: 0
  id: 011-missing-metadata-2
  fingerprint: c4acc72d
- concept: детектор пропусков возвращает перечень недостающих полей и не чинит предложение и не решает его судьбу
  question: Функция обнаружила, что в предложении не указан план отката. Каким должно быть её поведение?
  explanation: Задача функции — точно назвать пробелы. Придуманный «план отката по умолчанию» — это метаданные, которых никто не проверял; решение о допуске или отклонении принимает вызывающий уровень, опираясь на возвращённый список.
  kind: choice
  options:
    - Подставить план отката по умолчанию и вернуть пустой список
    - Вернуть название недостающего поля и ничего в предложении не менять
    - Пометить предложение отклонённым и завершить работу воркфлоу
    - Отправить предложение ревьюеру с пометкой о неполноте
  correct: 1
  id: 011-missing-metadata-3
  fingerprint: d5e51a17
