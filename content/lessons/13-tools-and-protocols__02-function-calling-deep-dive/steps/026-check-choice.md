---
id: 026-check-choice
type: check
title: 'Проверка: девять клеток tool_choice'
source_anchor: '### `tool_choice` behavior'
check:
  - question: >-
      Агент завис: на пятнадцатом ходу он снова и снова вызывает один и тот же
      инструмент, хотя все данные уже собраны. Что поставить на финальном ходу?
    options:
      - 'Auto — пусть модель сама решит, что пора отвечать текстом'
      - >-
        None — тогда модель обязана написать ответ пользователю, а не уйти в
        новый вызов
      - 'Required — принудить её сделать последний, самый нужный вызов'
      - 'Оставить как есть: цикл всё равно прервётся сам'
    correct: 1
    explanation: >-
      Auto оставляет модели право снова выбрать инструмент, а Required прямо
      этого требует. Только None закрывает дорогу к вызовам, и единственное, что
      модели остаётся, — текст.
  - question: >-
      Ты хочешь заставить модель вызвать именно инструмент get_weather по имени.
      У кого из трёх провайдеров такой режим есть?
    options:
      - Только у OpenAI
      - >-
        У OpenAI и Anthropic; у Gemini четвёртый режим другой — VALIDATED,
        прогон ответа через валидатор схемы
      - У всех трёх одинаково
      - 'Ни у кого: назвать конкретный инструмент нельзя, можно только Required'
    correct: 1
    explanation: >-
      OpenAI и Anthropic позволяют указать имя конкретного инструмента (у
      Anthropic вдобавок есть флаг disable_parallel_tool_use). Уникальный
      четвёртый режим Gemini — VALIDATED, он про проверку схемы, а не про выбор
      инструмента.
---

Три режима из [шага 22](#step-22) и по одному «своему» у каждого провайдера из [шага 23](#step-23) складываются в сетку, по которой ты теперь должен уметь ходить в обе стороны: от задачи — к режиму и к имени поля, и обратно.

Спотыкаются обычно на двух местах. Первое: None путают с «не передавать инструменты вообще». Инструменты остаются в запросе, модель их видит — ей просто запрещено их вызывать на этом ходу. Второе: думают, что Required и «вызови вот этот по имени» — одно и то же. Required говорит «хоть один», имя говорит «ровно этот». Разница видна как раз тогда, когда инструментов у тебя двадцать.
