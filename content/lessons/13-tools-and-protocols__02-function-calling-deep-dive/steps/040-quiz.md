---
id: 040-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты собрал ответ модели и сразу передал аргументы вызова в свою функцию как
      готовый словарь. У OpenAI код падает, у Anthropic и Gemini работает.
      Почему?
    options:
      - >-
        OpenAI кладёт аргументы строкой JSON, её надо сначала разобрать; двое
        других отдают уже готовый объект
      - 'OpenAI требует включить строгий режим, иначе аргументы не приходят вовсе'
      - >-
        У OpenAI аргументы лежат не в вызове, а в отдельном поле рядом с
        сообщением
      - >-
        OpenAI присылает аргументы в другом порядке, и словарь собирается
        неправильно
    correct: 0
    explanation: >-
      У OpenAI аргументы — строка с JSON внутри, её надо распарсить. У Anthropic
      и Gemini на этом месте уже структура, парсить нечего.
  - question: >-
      Агент третий круг подряд зовёт один и тот же инструмент и не может
      остановиться. Что из общих трёх режимов выбора инструмента тут поможет?
    options:
      - 'Auto: пусть модель сама решит, звать инструмент или ответить текстом'
      - 'Required: заставить её звать инструмент, но другой'
      - >-
        None: запретить вызовы на этом шаге, чтобы она была обязана ответить
        словами
      - Убрать инструменты из запроса — других способов нет
    correct: 2
    explanation: >-
      None запрещает вызовы на один шаг, и модель вынуждена сформулировать
      текстовый ответ вместо очередного круга.
  - question: >-
      Ты добавляешь в проект четвёртого провайдера. Какая архитектура из урока
      потребует меньше всего переделок?
    options:
      - >-
        Хранить объявления инструментов сразу в формате того провайдера, который
        используется чаще
      - >-
        Держать одно каноническое объявление и три (теперь четыре) переводчика в
        формат каждого провайдера
      - >-
        Писать объявления заново под каждого провайдера — форматы всё равно
        разные
      - Взять формат OpenAI за стандарт и подгонять под него ответы остальных
    correct: 1
    explanation: >-
      Одно каноническое объявление плюс переводчик на провайдера: добавление
      четвёртого — это один новый переводчик, а не переписывание всех
      инструментов.
  - question: >-
      Схема твоего инструмента вкладывает объект в объект шесть уровней вглубь.
      Что с ней будет?
    options:
      - 'У всех трёх провайдеров пройдёт: шесть уровней — небольшая глубина'
      - 'Не пройдёт нигде: предел везде ниже'
      - 'У OpenAI упрётся в предел глубины, у Anthropic запас ещё есть'
      - 'Глубина не проверяется, ограничено только число инструментов'
    correct: 2
    explanation: >-
      У OpenAI потолок глубины около 5 уровней, у Anthropic примерно вдвое
      больше — одна и та же схема пройдёт у одного и упадёт у другого.
---

Дальше в курсе ты будешь строить агентов, которые дёргают реальные инструменты — и почти всегда не на том провайдере, под который писался пример. Пригодится не список полей наизусть, а привычка: сначала спросить, где в этом ответе лежит вызов, объект там аргументы или строка ([шаг 6](#step-6)), и как этот провайдер ждёт результат обратно ([шаг 30](#step-30)).

Спотыкаются обычно на трёх местах. Забывают распарсить аргументы OpenAI. Возвращают результат не тем именем поля и получают ошибку без внятного текста. И тащат чужую схему целиком, не сверив глубину и лимиты ([шаг 21](#step-21)).

Проверь себя — вопросы по всему уроку.
