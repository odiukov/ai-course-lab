---
id: 005-check-upakovki
type: check
title: 'Проверка: чья это упаковка'
source_anchor: '## The Problem'
check:
  - question: >-
      В ответе модели ты видишь список content, а в нём блок с типом tool_use,
      где поле input — уже готовый объект с параметрами. Чья это упаковка?
    options:
      - OpenAI
      - Anthropic
      - Gemini
      - 'По этим признакам определить нельзя, форма одинаковая у всех трёх'
    correct: 1
    explanation: >-
      Блоки внутри content и пара tool_use / tool_result — это форма Anthropic,
      и input там приходит разобранным объектом, а не строкой.
  - question: >-
      Ты переносишь рабочего погодного агента с OpenAI на Anthropic и оставляешь
      на месте строку, которая разбирает аргументы вызова из JSON-строки. Что
      произойдёт?
    options:
      - 'Всё сработает: разбор JSON-строки безопасен для любого провайдера'
      - >-
        Код упадёт или выдаст мусор, потому что у Anthropic аргументы приходят
        объектом, и разбирать там нечего
      - 'Код упадёт, потому что Anthropic вообще не присылает аргументы в ответе'
      - Anthropic сам определит лишний разбор и вернёт аргументы строкой
    correct: 1
    explanation: >-
      У OpenAI arguments — строка, её нужно разобрать; у Anthropic input уже
      объект, и повторный разбор ломает код. Ровно на этом различии чаще всего
      падают портированные интеграции.
---

Представь, что тебе на стол положили распечатку ответа модели без единой подписи о том, откуда она приехала. Ни имени провайдера, ни адреса запроса — только структура полей. Сможешь ли ты по ней узнать упаковку?

Именно это и стоит проверить сейчас. После [шага 2](#step-2), [шага 3](#step-3) и [шага 4](#step-4) тебе нужно уметь две вещи: по характерным признакам — плоский список `tool_calls`, блоки внутри `content`, вложенность в `functionDeclarations` — опознать провайдера, и сразу сказать, приедут аргументы строкой или объектом.

Спотыкаются обычно на втором. Имена полей люди запоминают быстро, а вот привычку «на всякий случай разобрать JSON» тащат из прежнего кода — и она тихо ломает перенос.
