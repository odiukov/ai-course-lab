- concept: при пустой стороне association все элементы непустой стороны остаются непривязанными
  question: Заполни обработку случая, когда детекций нет.
  explanation: Без детекций совпадений быть не может, поэтому каждый индекс трека попадает в unmatched_tracks. Симметрично, при отсутствии треков все индексы детекций должны попасть в unmatched_detections.
  kind: cloze
  template: |-
    if not len(detection_boxes):
        return [], ___, []
  answer: list(range(len(track_boxes)))
  accept:
    - '[*range(len(track_boxes))]'
  id: 017-pishem-associate-1
  fingerprint: f9c74ba1
