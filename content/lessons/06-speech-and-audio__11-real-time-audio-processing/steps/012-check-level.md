---
id: 012-check-level
type: check
title: 'Проверка: level = 16000, что это значит'
source_anchor: '### Step 1: ring buffer'
check:
  - question: >-
      Сэмплирование 16 кГц. Ты печатаешь level() три раза подряд и видишь 3200,
      потом 6400, потом 9600. Что происходит?
    options:
      - 'Буфер заполняется до рабочего объёма, это нормальный старт'
      - >-
        Потребитель не успевает: отставание уже 600 мс и растёт примерно на 200
        мс за замер
      - 'Отсчёты начали вылетать из буфера, часть звука потеряна'
      - 'Микрофон отдаёт кадры реже, чем нужно, буфер скоро опустеет'
    correct: 1
    explanation: >-
      9600 отсчётов при 16 000 отсчётов в секунду — это 600 мс накопленного
      аудио. Уровень при каждом замере увеличивается на 3200 отсчётов, или 200
      мс, поэтому потребитель отстаёт всё сильнее.
  - question: >-
      Буфер ёмкостью 32 000 отсчётов при sample_rate = 16 000 показывает level()
      = 16 000. Почему для оценки задержки недостаточно сказать, что он заполнен
      наполовину?
    options:
      - >-
        Доля ёмкости показывает только запас до переполнения, а задержку
        вычисляют как level / sample_rate
      - >-
        Заполненность влияет только на расход памяти и никак не связана со
        временем
      - Задержка появляется лишь после достижения maxlen
      - Для вычисления задержки нужно разделить capacity на level
    correct: 0
    explanation: >-
      Половина ёмкости говорит о запасе до переполнения, но не о приемлемости
      latency. Здесь 16 000 / 16 000 = 1 секунда накопленного звука, хотя старые
      отсчёты ещё не начали вытесняться.
---

Ты запустил цикл, вывел `level()` и получил 16000. Число само по себе ни о чём не говорит — оно не в миллисекундах и не в процентах от ёмкости. Проверь, умеешь ли ты за секунду превращать его в задержку по формуле из [шага 10](#step-10) и читать динамику: одно значение — это фотография, а важен наклон.

Здесь обычно спотыкаются двумя способами. Первый: принять отсчёты за миллисекунды и решить, что отставание — 16 секунд. Второй: успокоиться, потому что до `maxlen` ещё далеко, — но полсекунды опоздания уже ломает ритм разговора из [шага 1](#step-1), задолго до того, как что-то начнёт вылетать из буфера.
