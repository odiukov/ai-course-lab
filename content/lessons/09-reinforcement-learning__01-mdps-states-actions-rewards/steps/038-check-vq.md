---
id: 038-check-vq
type: check
title: 'Связываем V, Q и выбор действия'
source_anchor: '## Key Terms'
check:
  - question: >-
      В одном состоянии Q-значения действий таковы: остаться — 3, перейти — 7.
      Какое действие выберет жадная политика?
    options:
      - 'Остаться, потому что любое положительное Q-значение достаточно'
      - 'Перейти, потому что его Q-значение максимально'
      - Усреднить Q-значения и выбрать действие случайно
    correct: 1
    explanation: >-
      Жадная политика сравнивает Q-значения всех доступных действий и выбирает
      действие с максимальным значением. Поэтому она выберет переход с Q = 7.
  - question: >-
      Известно только значение V текущего состояния. Каких данных не хватает для
      непосредственного выбора лучшего действия?
    options:
      - Номера текущего шага эпизода
      - >-
        Q-значений действий либо модели наград и переходов, позволяющей их
        вычислить
      - Средней награды за все прошлые эпизоды
    correct: 1
    explanation: >-
      V оценивает состояние при дальнейшем следовании политике, но не разделяет
      оценку по доступным действиям. Для выбора нужны Q-значения либо модель
      среды, по которой их можно вычислить.
---

Представь, что ты стоишь у развилки и видишь на табличке одну оценку места: 6. Это полезно, но табличка не говорит, куда повернуть. Именно здесь легко перепутать роли: `V` из [шага 27](#step-27) оценивает положение при дальнейшем следовании политике, а `Q` из [шага 28](#step-28) отдельно оценивает каждый доступный ход.

Поэтому выбор делается сравнением `Q`, как в [шаге 36](#step-36). Если известна только `V`, понадобятся награды и переходы, чтобы получить `Q`, — это ты делал в [шаге 34](#step-34). Запомни проверку: `V` отвечает «хорошо ли здесь?», а `Q` — «что выгоднее сделать отсюда?».
