- concept: вероятность действия умножает всю одноступенчатую целевую оценку Беллмана
  question: Заполни пропуск в накоплении математического ожидания по действиям политики.
  explanation: 'Вероятность действия взвешивает весь возможный исход: ближайшую награду вместе с дисконтированной ценностью продолжения. Если умножить на pi_a только награду, продолжение будет учтено с неверным весом.'
  kind: cloze
  template: v += pi_a * ___
  answer: (r + gamma * V[s_next])
  accept:
    - r + gamma * V[s_next]
    - (r + gamma * V[s_next])
  id: 032-code-policy-evaluation-1
  fingerprint: c9db5b46
- concept: результат резервного копирования Беллмана заменяет прежнюю оценку состояния, а не прибавляется к ней
  question: При оценке фиксированной политики переменная v уже содержит заново вычисленную оценку состояния s по всем действиям. Как правильно обновить таблицу ценностей?
  explanation: Переменная v содержит полную новую оценку состояния, а не поправку к старой. Поэтому прежнее значение V[s] заменяют; прибавление повторно учитывало бы уже накопленную оценку.
  kind: choice
  options:
    - v = V[s]
    - V[s] += v
    - V[s] = v
  correct: 2
  id: 032-code-policy-evaluation-2
  fingerprint: 62dab3a0
