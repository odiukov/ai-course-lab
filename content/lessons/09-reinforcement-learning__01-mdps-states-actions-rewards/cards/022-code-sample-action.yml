- concept: равномерный выбор ключа словаря игнорирует веса стохастической политики
  question: Стохастическая политика задана словарём «действие → вероятность», причём вероятности действий различаются. Почему вызов random.choice(list(policy)) не реализует эту политику?
  explanation: random.choice видит только список ключей и назначает каждому одинаковый шанс. Чтобы воспроизвести политику, алгоритм должен использовать вероятности из значений словаря как веса.
  kind: choice
  options:
    - Он всегда выбирает действие с наибольшей вероятностью
    - Он выбирает ключи равномерно и не использует записанные в значениях вероятности
    - Он изменяет вероятности в исходном словаре
    - Он может выбирать только действия с ненулевой вероятностью
  correct: 1
  id: 022-code-sample-action-1
  fingerprint: 51cde977
- concept: запасной возврат последнего действия защищает выборку от погрешности суммы вероятностей
  question: Чем заполнить последний return, чтобы корректная выборка не завершилась без действия из-за погрешности чисел с плавающей точкой?
  explanation: У нормированного распределения возврат обычно происходит внутри цикла. Однако сумма вероятностей в памяти может оказаться чуть меньше единицы, поэтому возврат последнего действия служит защитой от редкого численного зазора.
  kind: cloze
  template: |-
    def sample_action(probabilities):
        threshold = random.random()
        cumulative = float()
        last_action = None
        for action, probability in probabilities.items():
            last_action = action
            cumulative += probability
            if threshold < cumulative:
                return action
        return ___
  answer: last_action
  accept:
    - last_action
  id: 022-code-sample-action-2
  fingerprint: 48c3eb3b
