---
id: 008-check-pravila
type: check
title: 'Проверка: отладочный print и ответ на уведомление'
source_anchor: '### Dispatch loop'
check:
  - question: >-
      Ты добавил в цикл строку print("получил запрос") для отладки, и клиент
      сразу отвалился с ошибкой разбора. Почему?
    options:
      - Клиент не понимает русский текст в сообщениях
      - >-
        Строка попала в stdout, и клиент попытался разобрать её как отдельное
        сообщение JSON-RPC
      - >-
        Вызов print выполняется слишком медленно, поэтому клиент не дождался
        ответа
      - >-
        Вызов print не добавляет перевод строки, поэтому два сообщения
        обязательно склеиваются
    correct: 1
    explanation: >-
      stdout служит каналом протокола: каждую полученную из него строку клиент
      ожидает увидеть в формате JSON-RPC. Отладочные сообщения нужно направлять
      в stderr.
  - question: Цикл получил сообщение без поля id. Как он должен поступить?
    options:
      - 'Обработать сообщение и отправить ответ с id: null'
      - Обработать сообщение и подставить id предыдущего запроса
      - 'Обработать сообщение, не записывая ответ в stdout'
      - >-
        Полностью проигнорировать сообщение и не выполнять связанную с ним
        операцию
    correct: 2
    explanation: >-
      Сообщение без id является уведомлением. Сервер выполняет предусмотренную
      обработку, но не отправляет ответ, поскольку клиент его не ожидает.
---

Два места, где этот цикл ломается чаще всего, — и оба выглядят безобидно.

Первое: ты хочешь посмотреть, что происходит внутри, и ставишь `print()`. Проверь себя: понимаешь ли ты, куда именно уходит эта строка и почему клиент не может её проигнорировать. Второе: пришло сообщение без `id`, и рука сама тянется вернуть хоть что-нибудь — пустой результат, `id: null`, «ок». Различие «обработать» и «ответить» здесь придётся держать раздельно: уведомление обрабатывают молча.

Правила целиком — в [шаге 7](#step-7). Сейчас проверь, что ты применяешь их к конкретной строке, а не помнишь как список.
