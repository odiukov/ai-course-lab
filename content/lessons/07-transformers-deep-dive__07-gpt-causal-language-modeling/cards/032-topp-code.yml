- concept: накопительный порог top-p применяется к нормированным вероятностям, а не к логитам
  question: Почему перед вычислением накопленной суммы в top_p_filter необходимо применить softmax к логитам?
  explanation: 'Логиты — ненормированные оценки: они могут быть отрицательными, а их сумма не является вероятностной массой. После softmax вероятности нормированы, поэтому их накопленную сумму можно сравнивать с порогом p.'
  kind: choice
  options:
    - Softmax восстанавливает исходный порядок идентификаторов токенов после сортировки
    - Только нормированные вероятности образуют вероятностную массу, которую осмысленно сравнивать с p
    - Softmax гарантирует одинаковое число кандидатов для любого распределения
  correct: 1
  id: 032-topp-code-1
  fingerprint: 6e8e40dc
- concept: токен, на котором накопленная вероятность достигает порога top-p, сохраняется в наборе кандидатов
  question: Заполни условие для маски в отсортированном порядке так, чтобы пороговый токен остался, а удалялись только следующие за ним позиции.
  explanation: cutoff указывает на первую позицию, где накопленная вероятность достигла p. Поэтому маскировать нужно только позиции после cutoff; удаление самой пороговой позиции нарушило бы требование набрать вероятность не меньше p и могло бы оставить набор пустым.
  kind: cloze
  template: |-
    cutoff = np.searchsorted(cumulative_probs, p, side="left")
    remove_sorted = np.arange(logits.size) ___ cutoff
  answer: '>'
  accept:
    - '>'
  id: 032-topp-code-2
  fingerprint: 46469c01
