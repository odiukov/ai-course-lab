- concept: фильтрация логитов должна сохранять соответствие между индексом позиции и идентификатором токена
  question: Вектор логитов индексируется идентификаторами токенов. Как реализовать top-k-фильтр, не нарушив это соответствие?
  explanation: Индекс логита служит идентификатором токена. Сокращение или перестановка вектора разрушит это соответствие, поэтому фильтр сохраняет форму и исходный порядок позиций.
  kind: choice
  options:
    - Вернуть только выбранные логиты, отсортированные по убыванию
    - Сохранить исходную форму и порядок вектора, заменив логиты отфильтрованных токенов на минус бесконечность
    - Вернуть индексы выбранных токенов вместо логитов
    - Переместить выбранные логиты в начало вектора
  correct: 1
  id: 030-topk-code-1
  fingerprint: df73ded2
- concept: строгое сравнение с порогом сохраняет все токены с одинаковым пограничным логитом
  question: Заполни оператор сравнения так, чтобы все логиты, равные порогу top-k, оставались допустимыми.
  explanation: Маскируются только значения строго ниже порога. Если несколько логитов равны пограничному значению, все они сохраняются, поэтому конечных кандидатов иногда оказывается больше k.
  kind: cloze
  template: filtered = np.where(logits ___ threshold, -np.inf, logits)
  answer: <
  accept:
    - <
  id: 030-topk-code-2
  fingerprint: 19f1511e
