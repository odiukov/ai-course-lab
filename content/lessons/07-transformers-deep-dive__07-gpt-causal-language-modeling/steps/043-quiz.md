---
id: 043-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты подаёшь модели последовательность из 200 токенов на обучении, а потом
      просишь её сгенерировать 200 токенов. Сколько forward pass в каждом
      случае?
    options:
      - Один проход на обучении и 200 проходов на генерации
      - 200 проходов на обучении и один на генерации
      - 'По одному проходу в обоих случаях: маска делает генерацию параллельной'
      - По 200 проходов в обоих случаях
    correct: 0
    explanation: >-
      На обучении все правильные токены известны заранее, поэтому маска
      позволяет посчитать все 200 предсказаний за один проход. На генерации
      каждый следующий токен зависит от только что выданного, и проход
      приходится повторять 200 раз.
  - question: >-
      Что сломается, если убрать causal mask на обучении, но оставить всё
      остальное?
    options:
      - 'Модель перестанет обучаться: лосс не будет считаться'
      - 'Ничего: маска влияет только на скорость'
      - 'Лосс упадёт почти до нуля, но на генерации модель окажется беспомощной'
      - Модель начнёт генерировать по одному токену вместо всех сразу
    correct: 2
    explanation: >-
      Без маски позиция видит и сам ответ, который должна предсказать, поэтому
      на обучении лосс становится крошечным. На генерации будущих токенов нет, и
      модель, привыкшая подглядывать, не умеет предсказывать без них.
  - question: >-
      У тебя два распределения: в первом главный токен весит 0.9, во втором
      самый вероятный — 0.12. Ты применяешь min-p с порогом 0.1. Где останется
      больше кандидатов?
    options:
      - 'Во втором: порог получится 0.012, и под него пройдёт много токенов'
      - 'В первом: чем острее распределение, тем больше токенов проходит порог'
      - 'Поровну: min-p всегда оставляет фиксированное число токенов'
      - В обоих случаях останется ровно один токен
    correct: 0
    explanation: >-
      Порог у min-p считается от максимальной вероятности. При максимуме 0.9
      планка — 0.09, и почти всё отсекается; при максимуме 0.12 планка падает до
      0.012, и в размытом распределении выживает много вариантов.
  - question: >-
      Черновая модель предложила пять токенов, большая проверила их одним
      проходом и приняла первые три. Что будет с текстом на выходе?
    options:
      - 'Он совпадёт с тем, что выдала бы одна большая модель в greedy-режиме'
      - 'Он будет чуть хуже, зато считается быстрее — это плата за скорость'
      - 'Он совпадёт с текстом маленькой модели, но посчитается быстрее'
      - Он окажется смесью стилей двух моделей
    correct: 0
    explanation: >-
      Принимаются только те черновые токены, которые большая модель и сама бы
      выбрала; на первом несовпадении её собственный токен затирает черновик.
      Ускоряется вычисление, а не меняется результат.
---

Ты прошёл путь от одного вопроса «какой токен следующий» до треугольника из `-inf`, лосса со сдвигом и четырёх способов резать хвост распределения. Дальше в курсе это станет фундаментом: любой современный чат-ассистент внутри — тот же decoder-only цикл, а тюнинг, RLHF и длинный контекст надстраиваются поверх него.

Спотыкаются обычно на двух местах. Первое — путают обучение и генерацию: на обучении маска даёт все предсказания разом, на генерации проходов ровно столько, сколько токенов ([шаг 21](#step-21)). Второе — думают, что temperature, top-k, top-p и min-p что-то меняют в модели. Они не трогают ни один вес: это фильтры поверх готовых логитов.

Проверь себя на четырёх вопросах.
