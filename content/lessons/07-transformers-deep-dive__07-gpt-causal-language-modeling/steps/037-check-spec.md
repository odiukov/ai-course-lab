---
id: 037-check-spec
type: check
title: 'Проверка: почему выход обязан совпасть с greedy большой модели'
source_anchor: '## Exercises'
check:
  - question: >-
      Черновая модель предложила 4 токена. Большая проверила их одним
      параллельным проходом и согласилась с первыми тремя, а на четвёртом её
      greedy-выбор оказался другим. Что происходит дальше?
    options:
      - 'Все 4 токена принимаются, расхождение считается погрешностью черновика'
      - >-
        Три токена принимаются, четвёртый заменяется на выбор большой модели,
        дальше черновик предлагает снова
      - >-
        Вся четвёрка отбрасывается, и большая модель генерирует эти токены
        заново по одному
      - >-
        Принимаются три токена, а четвёртая позиция пропускается — её просто нет
        в выводе
    correct: 1
    explanation: >-
      Совпавший префикс берётся целиком, а на первом расхождении побеждает
      большая модель: её токен и идёт в вывод. Так вывод остаётся тем же, что
      дал бы greedy большой модели в одиночку.
  - question: >-
      Ты решил ускориться сильнее и принимать токен черновика даже там, где
      большая модель выбрала бы другой. Как оценить такой результат?
    options:
      - 'Это честное ускорение: просто чуть более разнообразная генерация'
      - >-
        Это ускорение с потерей качества — вывод теперь частично принадлежит
        маленькой модели, и сравнивать с greedy большой уже нельзя
      - 'Это не влияет на текст, потому что расхождения редки'
      - 'Это ошибка реализации, после которой генерация зацикливается'
    correct: 1
    explanation: >-
      Ценность спекулятивного декодирования именно в равенстве вывода. Как
      только принимаются несогласованные токены, ты получаешь не ускоренную
      большую модель, а смесь двух — и выигрыш по часам оплачен качеством.
---

Ты запустил спекулятивное декодирование, замерил время — стало вдвое быстрее. Радость держится ровно до момента, когда ты кладёшь два текста рядом: обычный greedy большой модели и новый. Они разошлись на седьмом токене.

Значит ли это, что всё сломалось? Да. Потому что честное ускорение здесь измеряется не только секундами: если текст другой, ты сравниваешь скорость двух разных моделей, а не двух способов запустить одну.

Проверь себя: понимаешь ли ты, откуда берётся выигрыш при частичном совпадении черновика — и почему принять «почти правильный» токен нельзя, даже если очень хочется.
