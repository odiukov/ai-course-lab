---
id: 016-check-minus100
type: check
title: 'Проверка: что если поставить -100 вместо -inf'
source_anchor: '### Where the triangle comes from'
check:
  - question: >-
      В строке скоров стоят обычные значения около нуля, а вместо будущих
      позиций ты подставил не минус бесконечность, а -100. Что получится после
      softmax?
    options:
      - Веса будущего станут ровно нулём — разницы с минус бесконечностью нет
      - >-
        Веса будущего будут порядка e в степени -100: не ноль, но неотличимо от
        нуля на практике
      - >-
        Softmax сломается и выдаст ошибку, потому что -100 слишком маленькое
        число
      - >-
        Веса будущего станут отрицательными, и строка перестанет суммироваться в
        единицу
    correct: 1
    explanation: >-
      Softmax сначала возводит скор в экспоненту: e в степени -100 — крошечное
      положительное число, а не ноль. Утечка есть, но настолько мелкая, что в
      вычислениях её не видно.
  - question: 'Почему в реализациях всё-таки пишут минус бесконечность, а не -100?'
    options:
      - Потому что -100 работает медленнее
      - >-
        Потому что exp от минус бесконечности даёт ровно ноль, и утечки из
        будущего нет ни на каплю
      - Потому что минус бесконечность делает веса прошлого больше
      - Потому что -100 нельзя записать в матрице скоров
    correct: 1
    explanation: >-
      Ноль первой стадии — точный ноль. Минус бесконечность переводит его в
      домен softmax без остатка, а -100 оставляет крошечный хвост.
---

Теперь проверь себя на подмене, которую очень хочется сделать: минус бесконечность выглядит пугающе, и рука тянется написать вместо неё «просто очень маленькое число», например `-100`.

После этого куска ты должен уметь ответить, что именно поменяется. Спотыкаются тут обычно на одном: путают «ноль на выходе softmax» и «ноль-подобное число». Softmax из [шага 7](#step-7) сначала берёт экспоненту, а экспонента от любого конечного числа строго больше нуля. Так что `-100` даёт вес порядка `e^-100` — на практике неотличимо от нуля, но формально будущее подмешано. Нули первой стадии из [шага 11](#step-11) были точными, и минус бесконечность просто переносит их в домен softmax без остатка.

> 🎒 **На пальцах.** Кран, который «почти закрыт», роняет каплю раз в час. Ведро под ним ты за смену не наполнишь — и всё же вода идёт. Перекрытый вентиль — это `-inf`: ноль капель, а не мало капель. Разница видна не в ведре, а в честности схемы: одно место в коде, где будущее вообще не может просочиться.
