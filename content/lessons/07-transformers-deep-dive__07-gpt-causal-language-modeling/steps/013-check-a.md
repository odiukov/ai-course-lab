---
id: 013-check-a
type: check
title: 'Проверка: строки матрицы A при n = 3'
source_anchor: '### Where the triangle comes from'
check:
  - question: >-
      Почему при построении строки i матрицы A ненулевые элементы делят на i+1,
      а не на полную длину последовательности n?
    options:
      - Потому что i+1 — число позиций в доступном префиксе
      - Потому что полная длина последовательности неизвестна до умножения
      - Потому что деление на n занулило бы элементы над диагональю
      - Потому что softmax требует знаменатель i+1
    correct: 0
    explanation: >-
      Строка i усредняет позиции от 0 до i, поэтому содержит i+1 ненулевых
      слагаемых. Деление на их количество даёт каждому вес 1/(i+1) и сохраняет
      сумму строки равной единице.
  - question: Откуда в матрице A берутся нули справа от диагонали?
    options:
      - Их добавляют отдельной маской после нормировки
      - Будущие позиции не входят в среднее по префиксу
      - Это результат округления очень маленьких весов
      - Их создаёт softmax при нормировке строки
    correct: 1
    explanation: >-
      На стадии равномерного усреднения маска и softmax не используются. Для
      строки i в сумму входят только позиции от 0 до i, поэтому коэффициенты
      будущих позиций остаются нулевыми.
---

Ты написал `prefix_average_matrix` в [шаге 12](#step-12), но пока это функция, а не картинка в голове. Проверим самое маленькое: `n = 3`. Три строки, каждая — рецепт для своей позиции: сколько взять от нулевого токена, сколько от первого, сколько от второго.

После этого куска ты должен уметь, не запуская код, назвать любую строку `A` вслух и объяснить, почему знаменатель именно такой.

Спотыкаются обычно на двух вещах. Первая — делят на `n` вместо `i+1`: тогда строка 0 получается `[1/3, 0, 0]`, и позиция теряет две трети себя. Вторая — путают, что означают нули справа: их никто не ставил, они остались от того, что в сумму просто не вошло.
