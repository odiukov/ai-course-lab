---
id: 026-check-loss
type: check
title: 'Проверка: какой лосс у необученной модели на 20 токенах'
source_anchor: '### Step 3: next-token prediction, end-to-end'
check:
  - question: >-
      Почему ожидаемый loss необученной модели увеличивается, когда словарь
      расширяют с 20 до 50 токенов?
    options:
      - >-
        Вероятность правильного токена при почти равномерном распределении
        уменьшается, поэтому её отрицательный логарифм растёт
      - >-
        В более крупном словаре последовательности автоматически становятся
        длиннее
      - Cross-entropy складывает номера всех токенов словаря
      - При расширении словаря causal mask закрывает больше позиций
    correct: 0
    explanation: >-
      Для почти равномерного распределения вероятность правильного токена равна
      1/|V|, а loss — ln(|V|). Поэтому для 50 токенов ориентир составляет около
      3.9 вместо примерно 3.0 для 20 токенов.
  - question: >-
      Первый forward pass необученной модели дал loss 0.08. Почему такой
      результат следует считать признаком ошибки?
    options:
      - >-
        Случайная модель не должна назначать правильным токенам настолько
        высокую вероятность; вероятна утечка цели из-за неверного сдвига или
        маски
      - Усреднение по позициям всегда должно давать loss не меньше единицы
      - 'Маленький loss означает, что словарь оказался слишком большим'
      - Cross-entropy нельзя вычислять до первого обновления весов
    correct: 0
    explanation: >-
      При словаре из 20 токенов необученная модель ожидаемо даёт loss около 3.0.
      Значение 0.08 означает почти точное знание ответов до обучения, что обычно
      указывает на неверный сдвиг целей или доступ к будущим токенам.
---

Ты собрал всё вместе: маску, softmax, сдвиг, усреднение. Модель ещё ни разу не обучалась, веса случайные. Запускаешь один forward pass на словаре из 20 токенов и получаешь число. Как понять, что код цел, если сравнивать не с чем?

Ориентир есть, и он считается заранее, до запуска. Случайные веса дают почти ровное распределение по всем 20 токенам — правильному достаётся примерно 0.05, и `-log(0.05)` из [шага 23](#step-23) даёт около 3.0. Это не результат, это термометр: увидел 3.0 — сборка жива.

Спотыкаются обычно на двух вещах. Первая: ориентир зависит от размера словаря, а не от длины последовательности. Вторая: подозрительно хороший лосс на необученной модели — не удача, а утечка ответа во вход.

> 🎒 **На пальцах.** Новые кухонные весы кладёшь на стол пустыми: должны показать ноль. Не потому, что ноль — цель, а потому что известное значение проверяет прибор. Здесь пустые весы — необученная модель, а ноль — это 3.0. Показали 40 — сбита калибровка. Показали 0.1 — под площадкой что-то лежит и подпирает.
