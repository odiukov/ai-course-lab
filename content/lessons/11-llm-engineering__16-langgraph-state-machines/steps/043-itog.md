---
id: 043-itog
type: quiz
title: Итоговая проверка
source_anchor: '## Key Terms'
check:
  - question: >-
      Что происходит с полем состояния, если узел возвращает для него новое
      значение, а редьюсер явно не задан?
    options:
      - Новое значение полностью заменяет прежнее
      - Новое значение автоматически добавляется к прежнему
      - Граф объединяет значения по ключам всех вложенных объектов
      - Выполнение графа завершается ошибкой
    correct: 0
    explanation: >-
      Поведение по умолчанию — перезапись поля. Объединение выполняется только
      редьюсером, например add_messages для списка сообщений.
  - question: >-
      Как возобновить выполнение графа после interrupt, передав решение
      человека?
    options:
      - Вызвать граф с Command(resume=...) в том же thread_id
      - Создать новый StateGraph и передать ему старое состояние
      - Передать checkpoint_id в качестве значения поля messages
      - Изменить interrupt_before на interrupt_after без нового запуска
    correct: 0
    explanation: >-
      После паузы выполнение продолжают командой Command(resume=...), используя
      тот же thread_id, чтобы обратиться к сохранённому состоянию сессии.
  - question: Что создаётся при возврате нескольких объектов Send из одного узла?
    options:
      - Несколько параллельных запусков указанного целевого узла
      - 'Несколько новых узлов, добавленных в структуру графа навсегда'
      - Один запуск целевого узла с массивом всех входных состояний
      - Несколько независимых графов без общего состояния
    correct: 0
    explanation: >-
      Каждый объект Send порождает отдельное выполнение целевого узла с
      переданным ему состоянием. Результаты затем объединяются механизмом
      редьюсера.
  - question: Какую роль выполняет StateGraph до вызова compile?
    options:
      - >-
        Хранит описание узлов и рёбер, из которого затем строится исполняемый
        граф
      - Немедленно запускает узлы при каждом добавлении ребра
      - Сохраняет чекпоинты всех разговоров в базе данных
      - Заменяет checkpointer и управляет thread_id
    correct: 0
    explanation: >-
      StateGraph — объект-конструктор: в него добавляют узлы и рёбра.
      Исполняемую версию получают после компиляции.
---

Дальше в курсе граф перестанет быть учебным примером: на нём будут висеть настоящие инструменты, живой чекпоинтер и человек, который что-то подтверждает. Поэтому пригодятся не определения, а привычки: сначала решить, какие поля состояния копятся, а какие перезаписываются, и только потом писать узлы; ставить шлагбаум до побочного эффекта, а не после.

Спотыкаются обычно на двух местах. Первое — молчаливая перезапись поля без редьюсера: ошибки нет, просто половина разговора исчезает. Второе — thread_id и checkpoint_id: продолжить разговор и вернуться в его середину — разные операции с разными ключами.

Проверь себя на четырёх вопросах.
