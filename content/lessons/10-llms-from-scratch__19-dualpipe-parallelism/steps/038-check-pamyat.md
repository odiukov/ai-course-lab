---
id: 038-check-pamyat
type: check
title: 'Проверка: где пик активаций выше — GPipe или 1F1B'
source_anchor: '## The Problem'
check:
  - question: >-
      Конвейер из 8 стадий обрабатывает 16 микробатчей. У какого расписания пик
      живых активаций на первой стадии выше?
    options:
      - 'У GPipe: до первого backward накапливаются активации всех 16 микробатчей'
      - >-
        У 1F1B: чередование forward и backward заставляет хранить больше
        активаций
      - 'Пик одинаков: оба расписания выполняют одинаковое количество операций'
      - 'У GPipe, но его пик ограничен числом стадий конвейера'
    correct: 0
    explanation: >-
      GPipe сначала выполняет forward для всех 16 микробатчей, поэтому их
      активации одновременно остаются живыми. В 1F1B после разогрева backward
      регулярно освобождает активации, ограничивая их накопление.
  - question: >-
      Почему в 1F1B число одновременно живых активаций ограничивается глубиной
      конвейера, а не общим числом микробатчей?
    options:
      - Каждая стадия хранит активации только одного слоя
      - >-
        После разогрева новый forward чередуется с backward, который освобождает
        активации ранее обработанного микробатча
      - 1F1B всегда пересчитывает активации и никогда их не хранит
      - В 1F1B микробатчи автоматически становятся меньше
    correct: 1
    explanation: >-
      Во время разогрева активации накапливаются, но в установившемся режиме
      каждый новый forward сопровождается backward. Поэтому очередь живых
      активаций не продолжает расти вместе с числом микробатчей.
---

Здесь проверяется одна вещь: умеешь ли ты по порядку задач из [шага 8](#step-8) и [шага 10](#step-10) сказать, сколько микробатчей одновременно держат свои активации в памяти — и почему это число у GPipe растёт вместе с числом микробатчей, а у 1F1B упирается в глубину конвейера.

Спотыкаются обычно на двух местах. Первое: кажется, что раз суммарная работа одинакова, то и память одинакова — но память определяется не суммой, а тем, сколько активаций живо в один момент. Второе: «плотнее» из [шага 36](#step-36) читают как «памяти меньше», хотя плотность расписания как раз тянет пик вверх — именно этот пик и считает функция из [шага 37](#step-37).
