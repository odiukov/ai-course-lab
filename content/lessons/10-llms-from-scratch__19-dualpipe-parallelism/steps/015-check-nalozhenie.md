---
id: 015-check-nalozhenie
type: check
title: 'Проверка: что с чем перекрывается'
source_anchor: '### Idea 1: chunk decomposition'
check:
  - question: >-
      Пока GPU считает attention для чанка номер 2, что в это же время может
      идти по сети?
    options:
      - All-to-all dispatch чанка номер 1 — рассылка его токенов экспертам
      - 'All-to-all dispatch самого чанка номер 2, ведь его токены уже готовы'
      - 'Ничего: сеть простаивает, пока GPU занят вычислением'
    correct: 0
    explanation: >-
      Dispatch чанка 1 уже начался и занимает только сеть, а attention чанка 2
      занимает только счёт, поэтому они идут одновременно.
  - question: 'Зачем forward-чанк вообще режут на четыре компонента, а не гоняют целиком?'
    options:
      - Чтобы уменьшить объём пересылаемых по сети данных
      - Чтобы сеть и счёт чередовались и могли накладываться друг на друга
      - Чтобы каждый компонент можно было посчитать на отдельном GPU
    correct: 1
    explanation: >-
      Два компонента из четырёх — чистая сеть, два — чистый счёт. Неделимый чанк
      пришлось бы честно ждать до конца рассылки; разрезанный даёт зазоры, куда
      встаёт работа соседнего чанка.
---

После [шага 12](#step-12) и [шага 13](#step-13) ты должен уметь про любой из четырёх компонентов сказать одно: это счёт или это сеть. А дальше — назвать ему пару, которая честно идёт в то же время, и от какого чанка эта пара.

Спотыкаются обычно на двух вещах. Первая: пытаются перекрыть dispatch и attention **одного и того же** чанка — так нельзя, attention должен закончиться раньше, иначе рассылать нечего. Пара всегда из разных чанков. Вторая: путают, кто с кем. Dispatch держится за attention следующего чанка, combine — за MLP того, что идёт ещё дальше.

Проверь себя.
