---
id: 015-check-rbac
type: check
title: 'Проверка: кому чтение, кому запись'
source_anchor: '## Exercises'
check:
  - question: >-
      Ты раздаёшь права команде из 50 разработчиков на пять бэкендов: notes,
      github, postgres, jira, slack. Джуниору нужно читать задачи в Jira и вести
      свои заметки. Какая запись в словаре разрешений ближе к делу?
    options:
      - >-
        Разрешить ему серверы jira и notes целиком — раз доступ к серверу есть,
        пусть пользуется всеми инструментами
      - >-
        Разрешить конкретные объединённые имена: jira_search_issues,
        jira_get_issue, notes_read, notes_write — и больше ничего
      - 'Разрешить всё, кроме postgres: остальные бэкенды не опасные'
      - Не давать ничего и пропускать каждый вызов через ручное согласование
    correct: 1
    explanation: >-
      Шлюз проверяет объединённое имя вида server_tool, а не сервер целиком.
      Доступ к jira не должен автоматически означать право закрывать чужие
      задачи, поэтому разрешения выписываются поинструментно.
  - question: >-
      Коллега говорит: «чтение можно раздать всем, ломать-то нечего». Что с этим
      не так на примере postgres?
    options:
      - 'Ничего не так: read-инструменты не меняют данные, значит риска нет'
      - >-
        Чтение postgres выгружает боевые данные клиентов — утечка не требует
        записи
      - 'Чтение опасно только потому, что грузит базу и упирается в рейт-лимит'
      - 'Проблема лишь в том, что read-инструменты труднее логировать в аудите'
    correct: 1
    explanation: >-
      Право на чтение ничего не портит, но выносит наружу содержимое: один
      SELECT по таблице клиентов — уже утечка, хотя ни одна строка не
      изменилась.
---

Пока ты писал `rbac_allows` в [шаге 14](#step-14), словарь был игрушечный: пара пользователей, пара имён. Теперь проверь, удержишь ли ты ту же логику на живой задаче — 50 человек и пять бэкендов, где надо честно решить, кому чтение, а кому запись.

Спотыкаются тут обычно на двух местах. Первое: права выдают серверу целиком, хотя [шаг 13](#step-13) построен вокруг объединённых имён — доступ к `jira` и право `jira_close_issue` это разные вещи. Второе: чтение считают безопасным по умолчанию. `postgres_query` ничего не ломает — и при этом выносит боевые данные клиентов наружу.

Ответь на вопросы, держа в голове конкретные пять серверов, а не абстрактную «политику».
