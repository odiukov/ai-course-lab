---
id: 033-check-reestry
type: check
title: 'Проверка: откуда шлюз тянет сервер по умолчанию'
source_anchor: '### Registries'
check:
  - question: >-
      Админ корпоративного шлюза нашёл нужный сервер только в MCP.so,
      официальный реестр его не знает. Что произойдёт по умолчанию?
    options:
      - >-
        Шлюз автоматически установит сервер, раз в официальном реестре нет
        одноимённой записи
      - >-
        Сервер не появится, пока админ явно не разрешит этот источник и не
        закрепит версию хешем
      - 'Шлюз установит сервер, но разрешит только инструменты без записи данных'
      - Шлюз добавит сервер после проверки синтаксиса его имени
    correct: 1
    explanation: >-
      По умолчанию корпоративный шлюз получает серверы из официального реестра.
      Добавление из метареестра требует решения администратора, а конкретная
      версия должна быть закреплена хешем.
  - question: >-
      Почему официальный MCP Registry подходит на роль источника серверов по
      умолчанию лучше перечисленных метареестров?
    options:
      - 'Он гарантирует, что каждый опубликованный сервер бесплатен'
      - >-
        Он проверяет принадлежность пространства имён издателю и выполняет
        базовую фильтрацию
      - Он запускает все серверы на собственной инфраструктуре
      - Он предоставляет только серверы с инструментами для чтения
    correct: 1
    explanation: >-
      Официальный реестр проверяет пространства имён, построенные по обратному
      DNS, и выполняет базовую фильтрацию заявок. Наличие сервера в метареестре
      само по себе такого уровня доверия не даёт.
---

Представь: тебе прислали ссылку на MCP-сервер для внутренней вики. Он лежит в MCPMarket, красиво описан, вендор солидный. Ты добавляешь его в конфиг шлюза — и он не поднимается.

Ничего не сломалось. Шлюз просто не считает MCPMarket источником, из которого можно брать что угодно.

Здесь легко перепутать две разные вещи: «сервер существует в каталоге» и «шлюз готов его тянуть». Из шести каталогов, разобранных в [шагах 29](#step-29) и [30](#step-30), проверенное имя даёт только официальный реестр — тот самый обратный DNS из [шага 32](#step-32). Остальные пять принимают заявки открыто, и попасть на шлюз они могут лишь поштучно, решением админа.

Второе место, где спотыкаются: даже разрешённый источник не отменяет прибитый хеш из [шага 17](#step-17). Одобрение источника и одобрение конкретной версии — два независимых замка, и открыть надо оба.
