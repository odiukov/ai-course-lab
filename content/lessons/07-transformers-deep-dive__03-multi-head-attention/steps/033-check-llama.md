---
id: 033-check-llama
type: check
title: 'Проверка: 64 query-головы и 8 KV — во сколько раз меньше кэш'
source_anchor: '### Step 3: Grouped-Query Attention variant'
check:
  - question: >-
      В Llama 3 70B стоит 64 query-головы и 8 KV-голов. Во сколько раз меньше
      ячеек занимает KV-кэш по сравнению с обычным MHA на 64 головах?
    options:
      - В 8 раз
      - В 64 раза
      - В 56 раз
      - 'Размер не меняется, меняется только скорость'
    correct: 0
    explanation: >-
      В кэше лежат только K и V, а их теперь 8 наборов вместо 64. Экономия ровно
      64 делить на 8, то есть в 8 раз — это и есть коэффициент repeat.
  - question: >-
      Сколько матриц внимания посчитается за один вызов такого GQA-слоя при N
      токенах?
    options:
      - 8 — по числу KV-голов
      - 64 — по числу query-голов
      - 72 — сумма query- и KV-голов
      - 1 — головы схлопываются в одну
    correct: 1
    explanation: >-
      Каждая из 64 query-голов считает свою softmax-матрицу, просто восемь из
      них смотрят на один и тот же набор ключей. Экономится память под K и V, а
      не количество счёта.
---

После [шага 30](#step-30) и [шага 31](#step-31) у тебя есть всё, чтобы прочитать конфиг настоящей модели и сказать, сколько она сэкономит. Проверь себя на живых числах Llama 3 70B: 64 головы query, 8 голов KV.

Здесь обычно спотыкаются в двух местах. Первое: путают, что именно ужалось. Query-голов по-прежнему 64, и матриц внимания считается 64 — сжались только K и V, а они и есть содержимое кэша из [шага 26](#step-26). Второе: пытаются сложить или вычесть числа вместо деления. Экономия — это отношение, тот самый repeat.

Посчитай, потом проверяй.
