---
id: 009-check-wo
type: check
title: 'Проверка: что будет, если убрать W_o'
source_anchor: '## The Concept'
check:
  - question: >-
      Ты убрал W_o: восемь голов дают по 64 числа, ты их просто склеиваешь в
      вектор на 512 и отдаёшь дальше. Что сломается?
    options:
      - >-
        Форма выхода перестанет совпадать с d_model, и следующий слой не примет
        вход
      - >-
        Формы сойдутся, но координаты с 1 по 64 навсегда останутся
        собственностью первой головы, с 65 по 128 — второй, и так далее
      - Attention внутри каждой головы начнёт считаться неправильно
      - >-
        Число обучаемых параметров вырастет, потому что головам придётся
        компенсировать потерю
    correct: 1
    explanation: >-
      8 на 64 как раз дают 512, так что размер выхода в порядке — именно поэтому
      ошибку легко не заметить. Но без W_o каждая координата выхода приходит
      ровно из одной головы, и восемь голов остаются восемью независимыми
      моделями, лежащими рядом.
  - question: >-
      Первая голова заметила, что «она» относится к «кошке», восьмая отследила
      предыдущий токен. В каком месте эти два наблюдения впервые оказываются
      внутри одного числа?
    options:
      - Внутри softmax каждой головы
      - 'В момент concat, когда куски выкладывают рядом'
      - При умножении на W_o
      - 'Нигде: головы остаются независимыми до самого конца сети'
    correct: 2
    explanation: >-
      Concat только выкладывает куски встык — ни одно число при этом не
      меняется. Первое число, в которое входят слагаемые сразу от всех голов,
      появляется при умножении на W_o.
---

После [шага 8](#step-8) ты должен уметь отвечать на неудобный вопрос: а что, собственно, сломается без выходной матрицы? Формы-то сходятся — восемь кусков по 64 честно дают 512, следующий слой примет такой вход и не пожалуется.

Здесь и спотыкаются чаще всего. Кажется, что раз размерность совпала, склейки достаточно, и W_o — просто ещё один слой «для мощности». На деле проверять надо не форму, а происхождение каждого числа: после concat координата 17 знает только про первую голову, координата 300 — только про пятую, и ни одно число в выходе не содержит вклада двух голов сразу. Смешивание — отдельная операция, и её надо было куда-то положить.
