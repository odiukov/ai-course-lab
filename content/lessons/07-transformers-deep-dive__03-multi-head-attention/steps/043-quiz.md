---
id: 043-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      У тебя d_model = 512. Ты переделываешь слой с одной головы на восемь. Что
      произойдёт с числом обучаемых параметров в W_q, W_k, W_v?
    options:
      - 'Вырастет примерно в восемь раз: восемь голов — восемь наборов проекций'
      - >-
        Останется тем же: те же матрицы 512 на 512, просто их столбцы поделены
        на восемь полос по 64
      - >-
        Уменьшится в восемь раз, потому что каждая голова работает с 64 числами
        вместо 512
      - 'Вырастет в два раза: добавляются split и combine'
    correct: 1
    explanation: >-
      Проекции остаются размером 512 на 512, а деление на головы — это нарезка
      уже посчитанных столбцов на полосы по d_head = 64. Ни одного нового веса
      тут не появляется.
  - question: >-
      Восемь голов посчитали свои выходы по 64 числа, ты склеил их обратно в
      полосу из 512 и выбросил W_o. Что сломается?
    options:
      - 'Ничего: склейка уже объединила головы в один вектор'
      - 'Размерность не сойдётся, выход будет не 512 чисел'
      - >-
        Каждый кусок выхода останется продуктом только своей головы — головы так
        и не смешаются между собой
      - Softmax перестанет давать сумму единица по строке
    correct: 2
    explanation: >-
      Concat кладёт головы рядом, но не перемешивает: первые 64 числа пришли
      только от головы 0, следующие 64 — только от головы 1. Смешивает их именно
      W_o.
  - question: >-
      Модель с 64 query-головами переводят на GQA с 8 KV-головами. Что
      происходит с KV-кэшем и почему?
    options:
      - >-
        Он в 8 раз меньше: хранится 8 наборов K и V, а не 64, а до 64 они
        разворачиваются ссылками в момент счёта
      - 'Он в 64 раза меньше: остаётся один общий набор K и V'
      - 'Он не меняется: query-голов по-прежнему 64, а значит и K с V нужно 64'
      - >-
        Он в 8 раз больше: приходится хранить и группы, и копии для каждой
        query-головы
    correct: 0
    explanation: >-
      Хранится ровно n_kv_heads наборов — 8, отсюда восьмикратная экономия.
      Коэффициент repeat = 64 / 8 = 8 не занимает памяти: это ссылки на те же
      строки, а не новые копии.
  - question: >-
      Пока восемь голов считают свои матрицы attention, что каждая из них знает
      о других?
    options:
      - Каждая видит распределения весов остальных и подстраивает своё
      - >-
        Ничего: голова видит только свои Q, K, V, и её результат зависит только
        от них
      - 'Головы обмениваются значениями после softmax, но до умножения на V'
      - 'Голова 0 задаёт масштаб, остальные делят свои веса на её максимум'
    correct: 1
    explanation: >-
      Внутри счёта головы полностью изолированы — поэтому цикл по головам на GPU
      и превращается в один батчевый matmul. Единственная точка обмена — W_o
      после склейки.
---

Урок закончился, но почти всё из него дальше будет всплывать в чужом коде. Форма `(N, d_model) ↔ (n_heads, N, d_head)` — то, во что ты будешь упираться каждый раз, когда tensor не состыкуется. Строчка `d_head = 64` в конфиге — то, из чего ты за секунду достанешь число голов. А `n_kv_heads` в карточке любой свежей модели читается уже не как загадочный флаг, а как «во столько раз меньше кэш».

Спотыкаются обычно на двух вещах. Первая: кажется, что восемь голов — это восемь моделей, а значит восемь раз по параметрам; на деле там та же матрица, нарезанная полосами. Вторая: concat принимают за смешивание — а он только кладёт головы рядом.
