---
id: 012-proverka-form
type: check
title: 'Проверка: на каких двух участках меняется последняя размерность?'
source_anchor: '### The GPT Architecture'
check:
  - question: На каких двух участках конвейера меняется последняя размерность тензора?
    options:
      - >-
        При сложении token и position эмбеддингов и после каждого блока
        трансформера
      - >-
        При превращении ID токенов в эмбеддинги и в линейной проекции перед
        softmax
      - В первом и в последнем блоке трансформера
      - В финальной layer normalization и в softmax
    correct: 1
    explanation: >-
      Эмбеддинги дают каждому ID вектор из 768 чисел, линейная голова заменяет
      эти 768 чисел на 50 257 логитов. Между этими двумя точками последняя
      размерность равна 768: сложение эмбеддингов, блоки, layer norm и softmax
      меняют значения, но не форму.
  - question: >-
      Почему все 12 блоков трансформера обязаны сохранять последнюю размерность
      тензора?
    options:
      - Потому что attention не умеет умножать на матрицы другой формы
      - >-
        Потому что остаточная связь складывает выход блока с его входом, а
        сложение требует совпадающих форм
      - Потому что размерность задана размером словаря и её нельзя менять
      - Потому что layer normalization работает только с размерностью 768
    correct: 1
    explanation: >-
      Блок прибавляет свой результат к собственному входу (residual), поэтому
      вход и выход должны иметь одну форму. Отсюда и возможность ставить блоки
      стопкой любой глубины: каждый читает и пишет один и тот же поток ширины
      768. Внутри блока размерности временно другие (например, в feedforward),
      но на выходе ширина восстанавливается.
---

Проследи только за последним числом в формах из [шага 10](#step-10). Обычно здесь спотыкаются, принимая любое вычисление за изменение формы: 12 блоков и нормализация сильно перерабатывают значения, но оставляют размерность 768. Два настоящих перехода стоят по краям: ID токена получает вектор из 768 чисел, а затем этот вектор превращается в 50 257 логитов.

> 🎒 **На пальцах.** Представь листы, которые проходят через двенадцать редакторов. Каждый исправляет текст, но сохраняет 768 полей в бланке. Только при приёме пустой номер превращают в заполненный бланк, а в конце его раскладывают по 50 257 возможным ответам. Меняется содержание часто, формат — дважды.
