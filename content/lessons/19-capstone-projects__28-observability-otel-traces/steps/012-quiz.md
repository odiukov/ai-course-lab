---
id: 012-quiz
type: quiz
title: Проверяем весь контракт трассировки и метрик
source_anchor: '## Running it'
check:
  - question: >-
      Ты записал задержки 0,1 и 0,3 секунды. Что покажут корзины с границами
      0,1, 0,25 и +Inf?
    options:
      - '1, 0 и 1'
      - '1, 1 и 2'
      - '1, 0 и 2'
      - '2, 2 и 2'
    correct: 1
    explanation: >-
      Корзины гистограммы кумулятивны: каждая считает все значения не больше
      своей границы, а +Inf включает оба замера.
  - question: Какой спан переживёт проверку json.loads(json.dumps(span)) == span?
    options:
      - 'Спан, где время хранится объектом datetime'
      - 'Спан, где значение атрибута хранится множеством'
      - >-
        Спан только с числами, строками, списками и словарями со строковыми
        ключами
      - 'Спан, где ключом словаря служит кортеж'
    correct: 2
    explanation: >-
      Числа, строки, списки и словари со строковыми ключами сохраняют форму при
      круге JSON-сериализации; остальные варианты либо не сериализуются, либо
      меняют представление.
  - question: >-
      Счётчик вызвали без меток, а затем с двумя допустимыми наборами меток. Что
      должно появиться в выдаче?
    options:
      - Только общий ряд с суммой всех вызовов
      - Только два ряда с метками
      - Общий ряд без меток и отдельные ряды для каждого набора меток
      - Один ряд для последнего набора меток
    correct: 2
    explanation: >-
      Вызов без меток относится к собственному общему ряду, а каждый набор
      значений меток накапливается отдельно при неизменном контракте их порядка.
  - question: >-
      Что происходит, если внутри контекстного менеджера спана возникает
      исключение?
    options:
      - 'Исключение подавляется, а спан получает статус OK'
      - Спан экспортируется без сведений об ошибке
      - >-
        Ошибка записывается в спан, спан экспортируется, а исключение передаётся
        дальше
      - 'Экспорт отменяется, чтобы не сохранить незавершённый спан'
    correct: 2
    explanation: >-
      Обработка ошибки добавляет сведения об исключении, finally гарантирует
      экспорт, а исходное исключение не скрывается от вызывающего кода.
---

Когда телеметрия подключится к настоящему сервису, тебе придётся проверять не отдельную строку, а весь договор сразу: сериализацию спанов из [шагов 8](#step-8)–[11](#step-11), ряды меток и кумулятивные корзины из [шагов 2](#step-2)–[7](#step-7). Чаще всего ломается связь между частями: спан выглядит правильно в памяти, но не переживает JSON, либо отдельные замеры верны, но Prometheus-текст искажает их смысл.

> 🎒 **На пальцах.** Представь приёмку нового автомобиля. Недостаточно отдельно проверить фары, тормоза и ремни: машина должна завестись, остановиться и сохранить безопасность при сбое. Здесь так же: JSONL подтверждает, что историю можно прочитать позже, метрики — что числа складываются в правильные ряды, а обработка исключения — что авария не исчезнет из отчёта.
