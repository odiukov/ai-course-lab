- concept: ограничение аргумента перед вычислением экспоненты предотвращает переполнение sigmoid
  question: Зачем в реализации sigmoid ограничивать локальное значение входа перед вызовом `math.exp`?
  explanation: В выражении `math.exp(-x)` большое отрицательное значение `x` превращается в огромный положительный показатель экспоненты. Ограничение входа диапазоном от -500 до 500 предотвращает переполнение, а результат на краях всё равно практически равен 0 или 1.
  kind: choice
  options:
    - Чтобы избежать переполнения экспоненты, практически не изменяя результат на насыщенных участках
    - Чтобы функция возвращала точные граничные значения для любого конечного входа
    - Чтобы автоматически вычислить производную sigmoid
    - Чтобы сделать sigmoid симметричной относительно начала координат
  correct: 0
  id: 006-pishem-sigmoid-1
  fingerprint: 451a9775
- concept: минус перед аргументом экспоненты делает sigmoid возрастающей функцией
  question: Какой знак нужно подставить, чтобы стандартная sigmoid возрастала при увеличении входа?
  explanation: 'Стандартная формула использует `math.exp(-x)`. Поэтому положительный вход даёт большее значение sigmoid, чем соответствующий отрицательный: например, `sigmoid(2) > sigmoid(-2)`.'
  kind: cloze
  template: math.exp(___x)
  answer: '-'
  accept:
    - −
  id: 006-pishem-sigmoid-2
  fingerprint: c905f252
