- concept: вычитание максимального логита перед экспонентой защищает softmax от переполнения
  question: Заполни выражение для численно устойчивого вычисления экспонент в softmax.
  explanation: После вычитания максимума наибольший аргумент экспоненты становится нулевым, а остальные — неположительными. Это предотвращает переполнение при больших положительных логитах.
  kind: cloze
  template: |-
    max_x = max(xs)
    exps = [math.exp(___) for x in xs]
  answer: x - max_x
  accept:
    - (x - max_x)
  id: 040-pishem-softmax-1
  fingerprint: '59561341'
- concept: softmax сохраняет порядок логитов по величине
  question: Как найти класс с наибольшей вероятностью softmax, не вычисляя сами вероятности?
  explanation: Экспонента строго возрастает, а все её значения делятся на один положительный знаменатель. Поэтому логиту большей величины соответствует большая вероятность.
  kind: choice
  options:
    - Взять индекс наибольшего логита
    - Взять индекс логита с наибольшим модулем
    - Взять индекс логита, ближайшего к среднему
    - Сначала отсортировать логиты по возрастанию и выбрать первый
  correct: 0
  id: 040-pishem-softmax-2
  fingerprint: a0faab67
