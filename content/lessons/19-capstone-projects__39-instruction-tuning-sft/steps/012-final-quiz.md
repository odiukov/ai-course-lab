---
id: 012-final-quiz
type: quiz
title: 'Диагностируем SFT по маске, сдвигу, генерации и exact-match'
source_anchor: '## Exact-Match Evaluation'
check:
  - question: >-
      Инструкция остаётся в контексте, но её позиции помечены как игнорируемые.
      Что это меняет?
    options:
      - 'Модель не видит инструкцию вовсе, поэтому учится отвечать наугад'
      - >-
        Модель читает инструкцию через attention, но градиент идёт только с
        позиций ответа
      - 'Инструкция обрезается из последовательности, чтобы батч был короче'
    correct: 1
    explanation: >-
      Маска влияет на loss, а не на видимость: инструкция целиком доступна
      attention, но в сумму ошибки попадают только токены ответа.
  - question: Ты подготовил маску ответа и забыл сдвиг на один токен. Что сломается?
    options:
      - 'Ничего, сдвиг нужен только при генерации'
      - >-
        Позиции перестанут совпадать: предсказание с позиции i будут сверять не
        с токеном i+1, а с самим i
      - Батч перестанет паддиться до одинаковой длины
    correct: 1
    explanation: >-
      Модель на позиции i предсказывает следующий токен, поэтому цель для неё —
      токен i+1. Без сдвига цель уезжает, и модель учится копировать вход.
  - question: >-
      Эталон — «The capital of France is Paris.». Модель выдала «Paris.». Какой
      exact-match получится?
    options:
      - '1, ответ по смыслу верный'
      - '0.5, совпала часть строки'
      - '0, нормализация убирает только регистр и лишние пробелы'
    correct: 2
    explanation: >-
      Нормализация лечит косметику, но не разницу в словах: строки не равны,
      значит на этом примере метрика равна нулю.
  - question: >-
      Обучение идёт, masked loss падает, а exact-match держится на 0. О чём это
      скорее говорит?
    options:
      - 'Loss посчитан неправильно, раз метрика не растёт'
      - >-
        Модель приближается к эталону, но пока не совпадает с ним символ в
        символ
      - Генерация не запускалась вообще
    correct: 1
    explanation: >-
      Loss меняется плавно и реагирует на каждый токен, а exact-match
      переключается только при полном совпадении строки — он отстаёт и прыгает
      скачками.
---

Ты собрал весь SFT-шов: пара с границей ответа, маска, сдвиг, loss по ответу, генерация после `RESP` и exact-match рядом с ней. Дальше в курсе на этот шов будут навешивать другие метрики и другие функции потерь — и он останется тем же самым.

Спотыкаются обычно на двух местах. Первое: маска и сдвиг кажутся одной операцией, хотя маска отвечает на «какие позиции считать», а сдвиг — на «с чем их сверять» ([шаг 6](#step-6)). Второе: exact-match читают как оценку качества, а он всего лишь проверяет равенство строк — правильный по смыслу, но короткий ответ даёт ноль.
