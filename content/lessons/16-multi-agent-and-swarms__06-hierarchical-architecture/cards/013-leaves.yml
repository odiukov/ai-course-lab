- concept: базовая ветвь рекурсивного сбора возвращает список из одного узла, чтобы обе ветви функции имели одинаковый тип результата и склеивались
  question: Функция собирает всех исполнителей поддерева. Заполни пропуск в базовом случае.
  explanation: Ветвь с детьми возвращает список, значит и базовая ветвь обязана вернуть список. Возврат самого `node` даёт объект вместо списка, и `result += leaves(child)` либо упадёт, либо разложит словарь по ключам.
  kind: cloze
  template: |-
    def leaves(node):
        if not node["children"]:
            return ___
        result = []
        for child in node["children"]:
            result += leaves(child)
        return result
  answer: '[node]'
  accept:
    - '[node]'
    - '[ node ]'
    - list([node])
    - '[node["name"]]'
  id: 013-leaves-1
  fingerprint: 7f068a8e
- concept: 'порядок исполнителей в результате обхода задаётся порядком дочерних узлов: ветвь проходится целиком, затем следующая'
  question: 'Сбор исполнителей идёт рекурсивно: узел без подчинённых даёт сам себя, узел с подчинёнными — склейку результатов по своим детям в порядке их перечисления. Что определяет порядок имён в итоговом списке?'
  explanation: 'Обход детерминирован структурой: для каждого ребёнка вызывается та же функция, и её результат целиком дописывается к накопителю. Поэтому исполнители одной ветви идут подряд, а не группируются по уровням.'
  kind: choice
  options:
    - 'Порядок дочерних узлов в структуре: каждая ветвь выдаётся целиком, до самого низа, потом начинается следующая'
    - 'Глубина: сначала все исполнители, лежащие ближе к корню, затем более глубокие'
    - Порядок появления узлов в памяти, поэтому результат недетерминирован
  correct: 0
  id: 013-leaves-2
  fingerprint: 120873b3
- concept: узел без подчинённых остаётся исполнителем, даже если он одновременно корень организации — управляющего уровня над ним не требуется
  question: Организация описана одним узлом, у которого список подчинённых пуст. Что вернёт функция сбора исполнителей, вызванная от этого узла?
  explanation: 'Роль узла определяется его подчинёнными, а не позицией. Пустой список детей — признак исполнителя, и корень тут не исключение: он сам и есть адресат задания.'
  kind: choice
  options:
    - 'Пустой список: над узлом нет менеджера, значит поручать некому'
    - Список из этого единственного узла
    - 'Ошибку: у корня обязаны быть подчинённые'
  correct: 1
  id: 013-leaves-3
  fingerprint: 90e0d615
