- concept: 'предел глубины включающий: сигнал выдаётся при строгом превышении, дерево ровно на пределе допустимо'
  question: Функция сигналит о превышении предела глубины. Дерево глубиной ровно в предел разрешено. Заполни пропуск.
  explanation: С `>=` дерево, укладывающееся в предел, ложно помечалось бы слишком глубоким. Граничное значение — разрешённое, поэтому сравнение строгое.
  kind: cloze
  template: |-
    def too_deep(node):
        return depth(node) ___ MAX_DEPTH
  answer: '>'
  accept:
    - '>'
    - '> '
  id: 023-too-deep-1
  fingerprint: 71c1570c
- concept: проверка предела переиспользует существующую функцию измерения глубины, а не пересчитывает уровни собственным обходом
  question: Почему предикат проверки предела вызывает готовую функцию `depth`, а не считает уровни собственным обходом дерева?
  explanation: 'Глубина уже определена: считать ли корень, считать ли лист. Дублирующий обход — второй источник истины, и расхождение проявится ровно на границе, где решение и принимается.'
  kind: choice
  options:
    - Собственный обход работал бы медленнее на глубоких деревьях
    - 'Определение глубины остаётся в одном месте: два независимых счёта разойдутся, например в трактовке узла-листа, и предикат начнёт судить по другой метрике'
    - Рекурсию нельзя писать дважды в одном модуле
    - '`depth` заодно проверяет структуру дерева на циклы'
  correct: 1
  id: 023-too-deep-2
  fingerprint: 2c75c3b5
