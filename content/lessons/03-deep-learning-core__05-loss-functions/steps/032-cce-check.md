---
id: 032-cce-check
type: check
title: 'Проверка: логиты, one-hot цель и сумма градиентов'
source_anchor: '### Step 3: Categorical Cross-Entropy with Softmax'
check:
  - question: >-
      Для логитов [2, 1, 0] верен второй класс. Почему one-hot цель имеет вид
      [0, 1, 0], а не совпадает с логитами или вероятностями?
    options:
      - >-
        Цель кодирует правильный класс независимо от текущего предсказания
        модели
      - Цель должна повторять самые большие оценки модели
      - Цель получается нормализацией логитов через softmax
    correct: 0
    explanation: >-
      One-hot цель описывает известный правильный ответ: единица стоит на
      позиции верного класса. Логиты [2, 1, 0] и вероятности [0.665, 0.245,
      0.090] относятся к предсказанию модели.
  - question: >-
      Почему при градиенте softmax с categorical cross-entropy компоненты по
      всем логитам взаимно компенсируются?
    options:
      - Все компоненты градиента имеют одинаковый знак
      - >-
        Вероятности и one-hot цель имеют одинаковую сумму, поэтому сумма их
        разности исчезает
      - Softmax делает все вероятности одинаковыми
      - Градиент вычисляется только для истинного класса
    correct: 1
    explanation: >-
      Градиент равен probabilities минус target. Вероятности суммируются до 1, и
      one-hot цель также суммируется до 1, поэтому сумма компонентов градиента
      равна 1 − 1 = 0.
---

Здесь легко перепутать три списка: логиты — сырые оценки модели, вероятности после softmax и one-hot цель с единственной единицей. Используй правило из [шага 29](#step-29) и проверь каждый градиент. У истинного класса он отрицательный, поэтому при шаге оптимизатора его логит вырастет; у остальных — положительный, поэтому их логиты уменьшатся. Ещё одна быстрая проверка: сумма всех градиентов должна дать ноль.

> 🎒 **На пальцах.** Представь три сообщающихся сосуда с одним литром воды. Ты переливаешь воду к сосуду с правильной наклейкой, но общий объём не меняется. Поэтому прибавка в одном месте уравновешивается убавками в других: суммарная команда на изменение равна нулю.
