- concept: 'идемпотентность внешней операции: повтор с тем же ключом возвращает сохранённый результат, не создавая второго эффекта'
  question: Платёжный API называют идемпотентным по ключу операции. Что это гарантирует при повторном запросе с тем же ключом?
  explanation: 'Идемпотентность разрешает повторять команду: система запоминает ключ вместе с результатом и при повторе отдаёт тот же результат. Внешний эффект — списание — остаётся единственным.'
  kind: choice
  options:
    - Списание произойдёт один раз, а повтор вернёт сохранённый результат первой операции
    - Повторный запрос будет отклонён с ошибкой, и клиенту придётся начать заново
    - Второе списание произойдёт, но потом автоматически отменится возвратом
    - Запрос встанет в очередь и выполнится, когда завершится первый
  correct: 0
  id: 017-idempotency-1
  fingerprint: '04786487'
- concept: повторная доставка неизбежна, потому что между внешним эффектом и подтверждением задачи нет атомарности — переставить их местами проблему не решает
  question: Воркер списывает деньги и подтверждает задачу очереди. Почему выбором порядка этих двух действий нельзя избавиться от риска двойного списания или потери платежа?
  explanation: Внешний вызов и подтверждение — две разные системы, общей транзакции у них нет. Щель между ними закрывается не порядком операций, а идемпотентным ключом на стороне внешней системы.
  kind: choice
  options:
    - 'Между ними всегда есть момент, когда воркер может упасть: подтверждение раньше даёт потерю платежа, позже — повторную доставку'
    - Очередь всегда доставляет задачу ровно один раз, поэтому порядок вообще ни на что не влияет
    - Подтверждение очереди технически невозможно выполнить до внешнего вызова
    - Платёжные системы запрещают подтверждать задачу после списания
  correct: 0
  id: 017-idempotency-2
  fingerprint: '80710922'
- concept: ключ идемпотентности выводится из устойчивого идентификатора намерения, а не генерируется заново при каждой попытке
  question: Дополни формирование ключа идемпотентности так, чтобы все повторные доставки одной задачи использовали один и тот же ключ.
  explanation: Ключ должен обозначать намерение пользователя и переживать перезапуск воркера. Вызов вроде `uuid4()` при каждой попытке даёт новый ключ, внешняя система считает операцию новой и списывает деньги повторно.
  kind: cloze
  template: charge(amount=1450, idempotency_key=f"payment-order-{___}")
  answer: order_id
  accept:
    - order_id
    - order.id
    - job['order_id']
    - идентификатор заказа
  id: 017-idempotency-3
  fingerprint: fba85995
