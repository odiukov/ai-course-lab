---
id: 023-check-streaming
type: check
title: 'Проверка: кто из трёх стримит без доработки'
source_anchor: '## The Concept'
check:
  - question: >-
      Нужно выводить субтитры во время выступления без изменения архитектуры
      распознавания. Какие из рассматриваемых схем подходят для этого?
    options:
      - >-
        Attention encoder-decoder, потому что его декодер порождает текст
        последовательно
      - >-
        CTC и RNN-T, потому что для текущего решения им не нужны будущие
        аудиокадры
      - >-
        Только CTC, потому что predictor в RNN-T должен сначала получить полную
        транскрипцию
      - >-
        Только RNN-T, потому что независимые покадровые решения CTC нельзя
        выдавать сразу
    correct: 1
    explanation: >-
      CTC обрабатывает доступные кадры без обращения к будущему, а RNN-T
      использует прошлые кадры и уже выданный префикс. Неограниченному
      attention-декодеру доступно всё аудио, поэтому для потоковой работы его
      приходится отдельно ограничивать.
  - question: >-
      Почему последовательная генерация токенов в attention encoder-decoder сама
      по себе не делает распознавание потоковым?
    options:
      - >-
        Потому что потоковая модель обязана выдавать ровно один токен для
        каждого аудиокадра
      - >-
        Потому что авторегрессивность описывает порядок выходных токенов, а
        cross-attention всё ещё может обращаться к будущим аудиокадрам
      - >-
        Потому что attention-модели не поддерживают скрытые состояния между
        соседними токенами
      - >-
        Потому что для потокового распознавания обязательно требуется
        CTC-схлопывание
    correct: 1
    explanation: >-
      Авторегрессивность ограничивает декодер предыдущими выходными токенами, но
      ничего не говорит о доступе к входу. При неограниченном cross-attention
      декодер может использовать любую часть записи, включая ещё не поступившую
      в потоковой системе.
---

Представь, что тебя позвали делать живые субтитры на конференции: докладчик говорит, текст должен появляться на экране через полсекунды. У тебя на руках три схемы из [шага 3](#step-3), и надо выбрать не «лучшую по качеству», а ту, что вообще запустится.

Соблазн — взять ту, у которой WER ниже. Но качество тут не главный фильтр.

Главный вопрос один: **чтобы выдать токен сейчас, нужно ли модели что-то из будущего?** CTC голосует на каждом кадре независимо — будущее не нужно, ответ готов сразу ([шаг 4](#step-4)). RNN-T обусловлен прошлыми кадрами и уже напечатанными токенами ([шаг 19](#step-19)) — тоже смотрит только назад. А декодер attention-модели через cross-attention волен обратиться к любому кадру энкодера ([шаг 22](#step-22)), и «любому» означает, что весь энкодер уже должен быть посчитан — то есть аудио записано целиком.

Отсюда сортировка: CTC и RNN-T стримят из коробки, attention — нет, пока ему не порежут обзор на куски.

Где обычно спотыкаются: путают авторегрессивность с потоковостью. Attention выдаёт токены по одному, и кажется, что это уже стрим. Нет — по одному он выдаёт *выход*, а *вход* ему нужен весь сразу. И вторая ловушка: думать, что память RNN-T мешает стримить. Память смотрит назад, а назад всегда доступно.
