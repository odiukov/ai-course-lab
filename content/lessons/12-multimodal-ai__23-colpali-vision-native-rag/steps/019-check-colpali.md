---
id: 019-check-colpali
type: check
title: 'Проверка: что кешируется, а что считается на запросе'
source_anchor: '### ColPali'
check:
  - question: >-
      В индекс с уже посчитанными страницами добавляют новую пачку документов.
      Что требуется вычислить?
    options:
      - Только эмбеддинги добавленных страниц
      - Эмбеддинги всех старых и новых страниц
      - 'MaxSim для всех запросов, когда-либо отправленных в систему'
      - 'Ничего: новые страницы будут кодироваться при каждом поиске'
    correct: 0
    explanation: >-
      Каждая страница кодируется независимо, поэтому сохранённые эмбеддинги
      старых страниц остаются пригодными для поиска.
  - question: >-
      Почему MaxSim нельзя вычислить при загрузке страницы и сохранить как
      постоянное свойство этой страницы?
    options:
      - 'Полученное число заняло бы больше места, чем патчевые эмбеддинги'
      - Операцию максимума нельзя применять к сохранённым векторам
      - >-
        Оценка зависит и от патчей страницы, и от токенов ещё неизвестного
        запроса
      - 'Текст запроса нельзя представить в том же пространстве, что и патчи'
    correct: 2
    explanation: >-
      Патчевые эмбеддинги можно подготовить заранее, но конкретный балл
      появляется только после кодирования пользовательского запроса.
---

После [шага 17](#step-17) и [шага 18](#step-18) у тебя должно быть чёткое разделение: страница кодируется один раз, при загрузке, и её 729 векторов лежат на диске; запрос кодируется в момент запроса; MaxSim из [шага 6](#step-6) считается только тогда, когда есть обе стороны.

Спотыкаются обычно в двух местах. Первое — думают, что можно закешировать сам балл: нельзя, он зависит от запроса, которого при загрузке не существует. Второе — считают, что новый документ ломает старые векторы. Не ломает: кодирование страницы не смотрит ни на другие страницы, ни на запросы. Отсюда и цена — 373 кБ на страницу вместо 3 кБ.
