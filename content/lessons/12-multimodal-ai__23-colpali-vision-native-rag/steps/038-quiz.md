---
id: 038-quiz
type: quiz
title: Итоговая проверка
source_anchor: '## Key Terms'
check:
  - question: >-
      В маленьком примере ты заменил максимум по патчам на среднее по всем
      патчам страницы. Одна страница содержит нужную цифру в одном-единственном
      месте, остальные патчи — про другое. Что произойдёт с её баллом?
    options:
      - 'Балл почти не изменится: сильное совпадение перевесит остальные патчи'
      - >-
        Балл упадёт: одно сильное совпадение растворится среди множества слабых,
        и страница может уйти вниз выдачи
      - 'Балл вырастет: среднее всегда учитывает детали точнее максимума'
      - >-
        Балл обязательно станет отрицательным, потому что непохожие патчи имеют
        отрицательный косинус
    correct: 1
    explanation: >-
      Максимум сохраняет сильное локальное совпадение, а среднее распределяет
      его вклад по всем патчам страницы. Поэтому редкая точная деталь может
      почти исчезнуть из итогового балла.
  - question: >-
      У тебя миллион проиндексированных страниц, пользователь вводит новый
      запрос. Что вычисляется именно во время этого запроса?
    options:
      - Эмбеддинги всех страниц пересчитываются заново
      - >-
        Кодируется запрос, после чего он сравнивается с сохранёнными
        эмбеддингами страниц
      - Для каждой страницы заново запускается распознавание текста
      - 'Ничего: все возможные запросы были закодированы при построении индекса'
    correct: 1
    explanation: >-
      Страницы кодируются заранее, а их эмбеддинги сохраняются в индексе. Во
      время поиска система кодирует новый запрос и сопоставляет его с уже
      подготовленными представлениями страниц.
  - question: >-
      Как PQ-квантование уменьшает размер многовекторного индекса и какой
      компромисс при этом возникает?
    options:
      - >-
        Заменяет точные компоненты векторов компактными кодами, поэтому сходство
        оценивается приближённо и возможна небольшая потеря качества
      - 'Удаляет все патчевые векторы, кроме среднего, не меняя правило сравнения'
      - >-
        Сохраняет исходные числа без изменений, но переставляет страницы в
        другом порядке
      - >-
        Переносит кодирование страниц на время запроса, полностью устраняя
        индекс
    correct: 0
    explanation: >-
      PQ хранит компактные коды вместо полноточных компонентов эмбеддингов. Это
      заметно сокращает объём индекса, но делает вычисление близости
      приближённым и может немного ухудшить ранжирование.
  - question: >-
      К тебе пришли две задачи. Первая: искать по архиву отсканированных отчётов
      с таблицами и диаграммами. Вторая: искать по чистой текстовой документации
      в Markdown, где вёрстка почти не несёт смысла. Как разумно выбрать
      архитектуры?
    options:
      - >-
        Обе задачи решать через ColPali, потому что визуальный ретривер всегда
        точнее
      - >-
        Обе задачи решать через текстовый RAG, потому что он всегда даёт такое
        же качество
      - 'Для отчётов использовать ColPali, а для Markdown — текстовый RAG'
      - 'Для отчётов использовать текстовый RAG, а для Markdown — ColPali'
    correct: 2
    explanation: >-
      Визуальный поиск полезен, когда смысл заключён в таблицах, диаграммах и
      расположении элементов. Для чистого текста без значимой вёрстки текстовый
      RAG обычно проще и экономнее.
---

Этим уроком ты закрываешь целый слой, который дальше в курсе будет всплывать постоянно: позднее взаимодействие как приём — сравнивать много мелких кусочков вместо двух свёрнутых векторов — встречается далеко за пределами картинок.

Спотыкаются обычно на трёх вещах. Первая: думают, будто ColPali где-то внутри всё-таки распознаёт текст — нет, страница остаётся картинкой от начала до конца. Вторая: путают, что дорого при загрузке, а что на запросе, и пугаются цифр не в том месте. Третья: считают пулинг из [шага 21](#step-21) бесплатным улучшением, хотя это размен recall на диск.

Держи в голове арифметику из [шага 27](#step-27) — она отвечает на вопрос «влезет ли это вообще» быстрее любых бенчмарков.
