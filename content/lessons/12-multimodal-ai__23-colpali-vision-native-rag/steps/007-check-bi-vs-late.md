---
id: 007-check-bi-vs-late
type: check
title: 'Проверка: почему это называют «поздним» взаимодействием'
source_anchor: '### ColBERT (2020)'
check:
  - question: >-
      Ты сравниваешь bi-encoder и ColBERT. В какой момент запрос и документ
      впервые по-настоящему взаимодействуют в ColBERT?
    options:
      - >-
        При индексации: представление документа заранее подстраивается под
        будущие запросы
      - >-
        Во время поиска: независимо вычисленные эмбеддинги токенов
        сопоставляются с текущим запросом
      - 'После генерации ответа: ретривер только проверяет готовый результат'
      - >-
        Они не взаимодействуют: сравниваются лишь заранее вычисленные итоговые
        баллы
    correct: 1
    explanation: >-
      Документные эмбеддинги вычисляются и кешируются заранее, но их попарное
      сопоставление с токенами конкретного запроса происходит только во время
      поиска.
  - question: >-
      Для каких элементов MaxSim отдельно выбирает лучшее совпадение в
      документе?
    options:
      - Для каждого токена запроса
      - Для каждого токена документа
      - Только для одной самой похожей пары во всём запросе
      - Для каждой координаты эмбеддинга
    correct: 0
    explanation: >-
      Каждый токен запроса ищет наиболее похожий токен документа. Затем
      полученные максимальные сходства складываются в балл документа.
---

Слово «поздний» здесь про момент, а не про скорость. У bi-encoder из [шага 5](#step-5) документ сжат в один вектор ещё до того, как ты ввёл запрос: решение о том, что важно, принято заранее и без тебя. В ColBERT из [шага 6](#step-6) векторы токенов тоже посчитаны заранее, но они лежат порознь, нетронутые, и сравнение начинается только в момент запроса.

Тут обычно спотыкаются на двух вещах. Первая: думают, что «поздно» значит «документ обрабатывается во время запроса» — нет, эмбеддинги закешированы, поздно происходит именно сопоставление. Вторая: путают направление MaxSim — максимум берётся по токенам документа для каждого токена запроса, а не наоборот.
