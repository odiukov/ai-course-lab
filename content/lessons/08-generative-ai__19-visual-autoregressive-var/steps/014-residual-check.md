---
id: 014-residual-check
type: check
title: >-
  Что должен кодировать второй масштаб после вычитания вклада первого и зачем
  всем масштабам один codebook?
source_anchor: '### VQ-VAE Multi-Scale Tokenizer'
check:
  - question: >-
      Первый масштаб уже передал общую форму объекта. Что должен кодировать
      второй масштаб?
    options:
      - 'Ту же общую форму заново, но большим числом токенов'
      - 'Только различия, которые остались после вклада первого масштаба'
      - Сразу все мельчайшие детали исходной картинки
    correct: 1
    explanation: >-
      Второй масштаб работает с остатком: он дополняет первый вклад, а не
      повторяет полное представление.
  - question: Почему разным масштабам полезно выбирать токены из одного codebook?
    options:
      - Чтобы каждый масштаб мог придавать одному индексу собственное значение
      - Чтобы второй масштаб всегда копировал токены первого
      - >-
        Чтобы один индекс обозначал один и тот же латентный вектор на любом
        масштабе
    correct: 2
    explanation: >-
      Общий codebook задаёт единый словарь векторов: значение индекса не
      меняется при переходе между масштабами.
---

После [шага 12](#step-12) важно не перепутать «более подробный масштаб» с «повторным кодированием всей картинки». Второй масштаб получает остаток после вклада первого и ищет токены именно для него. А общий codebook из [шага 5](#step-5) сохраняет единый смысл индексов: номер токена указывает на тот же латентный вектор независимо от размера сетки.

> 🎒 **На пальцах.** Представь бригаду, выравнивающую стену. Первый рабочий наносит толстый слой штукатурки и убирает крупные ямы. Второй не начинает стену заново: он проходит правилом и заполняет оставшиеся впадины. Оба берут смеси с одной пронумерованной полки, поэтому номер состава означает одно и то же для всей бригады.
