---
id: 003-nerf-ili-splat
type: check
title: 'Проверка: запросы к MLP или явные гауссианы'
source_anchor: '## The Problem'
check:
  - question: >-
      Почему явное гауссово представление быстрее формирует новый вид сцены, чем
      NeRF?
    options:
      - >-
        Оно растеризует сохранённые примитивы, тогда как NeRF вычисляет значения
        через сеть вдоль лучей
      - Оно хранит готовое изображение для любого возможного положения камеры
      - Оно переобучает нейросеть после каждого поворота камеры
    correct: 0
    explanation: >-
      NeRF получает данные для пикселей посредством повторных вычислений MLP.
      Gaussian Splatting хранит явные примитивы сцены и выводит их средствами
      GPU-rasterization.
  - question: >-
      Как способ представления сцены влияет на возможность переместить
      восстановленный объект?
    options:
      - Веса NeRF образуют отдельный редактируемый объект для каждого предмета
      - >-
        Связанные с предметом гауссианы можно переместить напрямую, а NeRF
        обычно требуется переобучить
      - >-
        Любое представление позволяет переместить предмет изменением одного
        пикселя
    correct: 1
    explanation: >-
      Гауссианы являются явными элементами со своими пространственными
      параметрами. Веса MLP кодируют сцену неявно и не выделяют предметы в
      непосредственно перемещаемые группы.
---

Здесь важно удержать различие из [шага 1](#step-1) и [шага 2](#step-2): NeRF отвечает на запросы о сцене вычислением, а 3DGS хранит элементы, которые можно сразу использовать. Частая ошибка — решить, что это лишь два формата одного и того же файла. Нет: способ хранения меняет и путь к кадру, и возможность редактирования.

> 🎒 **На пальцах.** Представь склад. В первом случае кладовщик по каждому запросу сверяется с длинной инструкцией и собирает нужную посылку. Во втором коробки уже стоят на подписанных полках: их можно сразу отправить или переставить. MLP похожа на инструкцию, а явные гауссианы — на готовые коробки.
