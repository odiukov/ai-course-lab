---
id: 017-density-check
type: check
title: 'Проверка: плотность в центре, на одной сигме и далеко от splat'
source_anchor: '### Step 1: A 2D Gaussian'
check:
  - question: >-
      У гауссианы центр в точке 0, 0, а масштабы по осям равны 2 и 3. Почему
      плотности в точках 2, 0 и 0, 3 совпадают?
    options:
      - Обе точки находятся на одинаковом нормированном расстоянии от центра
      - Обе точки находятся на одинаковом обычном расстоянии от центра
      - Плотность гауссианы не зависит от координат точки
    correct: 0
    explanation: >-
      Каждая точка удалена от центра на одну сигму вдоль соответствующей оси.
      Поэтому квадратичная форма одинакова, и в обеих точках плотность
      составляет примерно 0,61.
  - question: >-
      Почему gaussian_density достигает максимума в центре splat независимо от
      его масштаба?
    options:
      - 'Вектор смещения равен нулю, поэтому квадратичная форма равна нулю'
      - Обратная ковариация в центре всегда является единичной матрицей
      - Масштаб автоматически обнуляется при вычислении экспоненты
      - Координаты центра заменяются на значения масштаба
    correct: 0
    explanation: >-
      В центре diff равен нулю, поэтому diffᵀΣ⁻¹diff тоже равна нулю. В
      результате exp(−0,5 · 0) = 1 при любом невырожденном масштабе.
---

Проверь функцию из [шага 16](#step-16) на трёх характерных расстояниях. В центре ожидай `1`. На одной сигме — примерно `0.607`, причём по широкой оси до этой точки физически дальше, чем по узкой. Далеко от splat значение должно быстро приближаться к нулю: на трёх сигмах это около `0.011`.

Частая ошибка — измерять обычное расстояние в пикселях. Здесь важна нормированная удалённость из [шага 15](#step-15): форма эллипса решает, что считать близким.

> 🎒 **На пальцах.** Представь холм с пологим восточным склоном и крутым северным. Чтобы спуститься до одной и той же высоты, на восток придётся пройти дальше. Точки окажутся на разном расстоянии от вершины, но высота в них совпадёт. Так и плотность гауссианы сравнивает не метры, а положение относительно формы склона.
