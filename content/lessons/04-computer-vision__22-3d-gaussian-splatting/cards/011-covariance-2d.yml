- concept: сборка матрицы тензорными операциями сохраняет граф автоматического дифференцирования
  question: Ковариация вычисляется из обучаемых тензоров угла и масштабов. Почему её строки следует собирать через `torch.stack`, а не передавать вычисленные элементы в `torch.tensor([...])`?
  explanation: 'Для обучения мало получить правильные значения ковариации: результат должен оставаться связанным с исходными параметрами графом автодифференцирования. `torch.stack` сохраняет эту связь, а также устройство и тип данных входов.'
  kind: open
  reference: '`torch.stack` объединяет исходные тензоры операцией, которая остаётся в графе вычислений, поэтому градиенты могут дойти до угла и масштабов. Создание нового тензора через `torch.tensor([...])` копирует значения и может разорвать эту связь; кроме того, можно непреднамеренно сменить устройство или тип данных.'
  id: 011-covariance-2d-1
  fingerprint: 81b5c451
- concept: обратная ковариация задаёт нормированное эллиптическое расстояние до центра гауссианы
  question: Почему при вычислении плотности гауссианы смещение пикселя входит в квадратичную форму вместе с обратной ковариацией?
  explanation: Обратная ковариация преобразует обычное смещение в расстояние, согласованное с ориентацией и шириной гауссианы. Поэтому точки на одинаковом эллиптическом уровне получают одинаковую плотность.
  kind: choice
  options:
    - 'Она нормирует смещение с учётом формы эллипса: отклонение вдоль узкого направления влияет сильнее, чем такое же отклонение вдоль широкого'
    - Она сортирует гауссианы по глубине перед смешиванием цветов
    - Она превращает координаты пикселя в целочисленные индексы изображения
    - Она гарантирует, что сумма плотностей всех гауссиан равна единице
  correct: 0
  id: 011-covariance-2d-2
  fingerprint: 2a9d7c68
