- concept: минимальная конфигурация Swarm-агента передаётся через параметры name, instructions и functions
  question: Заполни пропуск, чтобы создать Swarm-агента из имени, системной инструкции и списка инструментов.
  explanation: '`name` идентифицирует агента, `instructions` задаёт его системный промпт, а `functions` перечисляет инструменты, доступные модели.'
  kind: cloze
  template: triage_agent = Agent(___)
  answer: name="triage", instructions=route_prompt, functions=handoffs
  accept:
    - instructions=route_prompt, name="triage", functions=handoffs
    - functions=handoffs, name="triage", instructions=route_prompt
    - name='triage', instructions=route_prompt, functions=handoffs
  id: 003-agent-config-1
  fingerprint: 5612cafd
