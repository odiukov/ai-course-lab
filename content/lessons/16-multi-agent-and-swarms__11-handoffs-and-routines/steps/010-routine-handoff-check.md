---
id: 010-routine-handoff-check
type: check
title: Отличаем инструкцию рутины от операции хендоффа
source_anchor: '### Two primitives'
check:
  - question: Как правильно разделены обязанности рутины и хендоффа?
    options:
      - >-
        Рутина подсказывает, когда выбрать специалиста, а функция хендоффа
        возвращает Agent, по которому рантайм переключает роль
      - >-
        Рутина непосредственно переключает активного агента, а хендофф только
        меняет текст инструкции
      - 'Рутина создаёт нового агента, а хендофф добавляет ему системный промпт'
    correct: 0
    explanation: >-
      Рутина направляет решение модели. Операция передачи начинается с вызова
      хендоффа, который возвращает Agent, а непосредственное переключение
      выполняет рантайм.
  - question: >-
      Сортировщик вызвал функцию, которая вернула объект агента поддержки. Какой
      компонент непосредственно меняет активного агента?
    options:
      - Системная инструкция сортировщика
      - Сама функция до возврата результата
      - 'Рантайм, распознавший объект Agent'
    correct: 2
    explanation: >-
      Функция только возвращает целевой объект. Активного агента меняет рантайм
      после проверки типа результата.
---

Проверь, не смешиваешь ли ты намерение и действие. Рутина из [шага 2](#step-2) подсказывает агенту, когда выбрать специалиста, а список инструментов из [шага 5](#step-5) определяет, может ли он это сделать. Сам хендофф происходит только при вызове функции и возврате объекта `Agent`, как в [шаге 9](#step-9). Поэтому фраза «передай в поддержку» без соответствующего инструмента остаётся инструкцией, а не переключением.
