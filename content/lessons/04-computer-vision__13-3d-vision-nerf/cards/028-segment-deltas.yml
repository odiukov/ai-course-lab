- concept: torch.full_like сохраняет форму, тип данных и устройство тензора-образца
  question: Почему завершающий элемент массива длин сегментов удобно создавать через torch.full_like на срезе t_vals?
  explanation: torch.full_like создаёт заполненный тензор с той же формой, типом данных и устройством, что и образец. Это предотвращает конфликты, например между CPU и GPU или разными типами чисел.
  kind: choice
  options:
    - Полученный тензор наследует форму, dtype и device среза
    - Функция автоматически вычисляет соседние разности
    - Функция сортирует отметки вдоль луча
  correct: 0
  id: 028-segment-deltas-1
  fingerprint: dfbd0da9
- concept: torch.cat объединяет части вдоль существующей оси без добавления новой
  question: Вставь операцию PyTorch, которая объединит массив соседних разностей и завершающий элемент в один плоский массив.
  explanation: torch.cat соединяет тензоры вдоль существующей оси. В отличие от torch.stack, эта операция не создаёт дополнительную ось и допускает разные размеры частей вдоль оси объединения.
  kind: cloze
  template: delta = torch.___([diffs, tail])
  answer: cat
  accept:
    - concat
  id: 028-segment-deltas-2
  fingerprint: a885adfd
