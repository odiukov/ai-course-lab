- concept: поточечная Conv1d меняет число каналов, сохраняя число точек
  question: 'Тензор облака точек имеет форму (5, 7, 300): batch, channels, points. Поточечная Conv1d преобразует 7 входных каналов в 48 выходных. Какую форму получит результат?'
  explanation: Conv1d принимает данные в формате (batch, channels, points). Свёртка с ядром 1 меняет описание каждой точки с 7 до 48 признаков, но не изменяет количество точек.
  kind: choice
  options:
    - (5, 300, 48)
    - (5, 48, 300)
    - (5, 7, 48)
    - (300, 48, 5)
  correct: 1
  id: 009-shared-mlp-1
  fingerprint: fc90c73b
- concept: BatchNorm1d в блоке общего MLP настраивается на число выходных каналов предыдущей свёртки
  question: Заполни аргумент нормализации, которая стоит сразу после поточечной свёртки.
  explanation: После Conv1d тензор содержит out_channels каналов, поэтому BatchNorm1d должна хранить параметры нормализации именно для этого числа каналов. В цепочке 3 → 64 нормализация получает 64.
  kind: cloze
  template: |-
    layers.append(nn.Conv1d(in_channels, out_channels, kernel_size=1))
    layers.append(nn.BatchNorm1d(___))
  answer: out_channels
  accept:
    - out_channels
    - c_out
  id: 009-shared-mlp-2
  fingerprint: 9ff2c439
