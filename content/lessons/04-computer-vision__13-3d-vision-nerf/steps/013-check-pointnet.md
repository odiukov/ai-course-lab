---
id: 013-check-pointnet
type: check
title: 'Проверка: перестановка точек и переменное число N'
source_anchor: '### The PointNet architecture'
check:
  - question: >-
      Ты переставил точки облака в случайном порядке и снова вычислил глобальный
      признак. Что должно произойти?
    options:
      - Признак должен остаться тем же
      - Координаты признака переставятся в том же порядке
      - Длина признака станет равна числу точек
    correct: 0
    explanation: >-
      Общий MLP одинаково и независимо обрабатывает каждую точку, а симметричный
      max pool не зависит от порядка точек.
  - question: >-
      В одном облаке 200 точек, а в другом 1500. Что верно для выхода
      pointnet_global_feature?
    options:
      - Длина выхода будет 200 и 1500 соответственно
      - >-
        Оба выхода будут одной фиксированной длины, но их значения могут
        различаться
      - Облако из 200 точек нельзя обработать
    correct: 1
    explanation: >-
      Max pool сжимает признаки любого числа точек в глобальный вектор
      фиксированной длины. Однако другой состав точек может дать другие
      поканальные максимумы.
---

После [шагов 10](#step-10)–[12](#step-12) ты должен уметь проверить два свойства PointNet: перестановка точек не меняет глобальный признак, а изменение числа точек не меняет его длину. Частая ошибка — решить, что и значения обязаны совпасть при разном числе точек. Нет: новые или удалённые точки могут изменить максимумы, хотя форма выхода останется прежней.

> 🎒 **На пальцах.** Представь прослушивание в оркестр: каждого музыканта оценивают по одинаковым критериям, а затем записывают лучший балл по каждому из них. Если кандидаты поменяются местами в очереди, итог не изменится. Если кандидатов станет больше или меньше, бланк сохранит прежнее число граф, но лучшие баллы могут стать другими.
