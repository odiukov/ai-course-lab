---
id: 018-check-cnn
type: check
title: 'Проверка: что общего у AdaptiveAvgPool и summarize'
source_anchor: '### Step 4: upgrade to CNN on log-mels'
check:
  - question: >-
      На вход CNN подали два клипа: один длиной 2 секунды, другой — 8 секунд.
      Оба прошли body с AdaptiveAvgPool2d(1). Что придёт в Linear(128, 50)?
    options:
      - В обоих случаях 128 чисел
      - >-
        У короткого клипа меньше чисел, у длинного больше — пропорционально
        длине
      - >-
        128 чисел у короткого и 512 у длинного, потому что MaxPool ужимает
        вчетверо
    correct: 0
    explanation: >-
      AdaptiveAvgPool2d(1) усредняет всё, что осталось от осей времени и
      частоты, в одно число на канал. Каналов 128 — значит, ровно 128 чисел
      независимо от длины клипа.
  - question: >-
      Чем AdaptiveAvgPool в CNN отличается от summarize, которую ты писал
      руками?
    options:
      - 'Ничем: обе усредняют вдоль времени и дают вектор фиксированной длины'
      - >-
        Задачей: summarize сжимала время, а AdaptiveAvgPool сжимает число
        классов
      - >-
        Тем, что усредняются выученные признаки свёрток, а не заданные формулой
        MFCC-коэффициенты
    correct: 2
    explanation: >-
      Идея одна — усреднить вдоль времени и получить вектор фиксированной длины.
      Разница в том, что именно усредняется: MFCC заданы формулой, а 128 каналов
      свёртка выучила сама под задачу.
---

Посмотри на `body` из предыдущего шага ещё раз и попробуй ответить: чем эта конструкция по сути отличается от того, что ты уже написал руками в [шаге 10](#step-10)?

Ответ неприятный для тех, кто ждал магии: почти ничем. В [шаге 8](#step-8) ты упёрся в то, что 998 кадров надо превратить в вектор фиксированной длины, и решил это средним по времени. Здесь ровно та же проблема и ровно то же решение — только усредняются не 40 MFCC-коэффициентов, заданных формулой, а 128 каналов, которые свёртка подобрала сама.

Здесь обычно спотыкаются в двух местах. Первое: думают, что `AdaptiveAvgPool2d(1)` как-то зависит от размера входа, и на длинном клипе чисел будет больше. Не будет: 128 каналов на входе — 128 чисел на выходе, всегда. Второе: путают, что именно даёт выигрыш в точности. Не пулинг — он про форму. Выигрыш даёт то, что признаки выучены под конкретную задачу, а не зафиксированы заранее.
