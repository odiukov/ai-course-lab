---
id: 018-check-cnn
type: check
title: 'Проверка: что общего у AdaptiveAvgPool и summarize'
source_anchor: '### Step 4: upgrade to CNN on log-mels'
check:
  - question: Что общего у summarize для MFCC и AdaptiveAvgPool в аудио-CNN?
    options:
      - >-
        Обе операции превращают последовательность или карту признаков
        переменного размера в вектор фиксированной длины
      - 'Обе операции обучают фильтры, выделяющие спектро-временные шаблоны'
      - Обе операции вычисляют вероятности классов
    correct: 0
    explanation: >-
      Обе операции агрегируют признаки по изменяющимся осям, чтобы классификатор
      получил вход фиксированного размера. Обучение признаков и вычисление
      классов выполняют другие слои.
  - question: >-
      В чём главное различие между summarize для MFCC и AdaptiveAvgPool после
      свёрточных слоёв?
    options:
      - >-
        summarize усредняет признаки, заданные формулой, а AdaptiveAvgPool
        агрегирует признаки, которые свёртки выучили под задачу
      - >-
        summarize создаёт классы, а AdaptiveAvgPool выбирает наиболее вероятный
        класс
      - >-
        summarize работает только с клипами фиксированной длины, а
        AdaptiveAvgPool — только с клипами переменной длины
    correct: 0
    explanation: >-
      Идея агрегации одинакова, но MFCC определены заранее, тогда как каналы CNN
      формируются обучаемыми свёрточными фильтрами.
---

Посмотри на `body` из предыдущего шага ещё раз и попробуй ответить: чем эта конструкция по сути отличается от того, что ты уже написал руками в [шаге 10](#step-10)?

Ответ неприятный для тех, кто ждал магии: почти ничем. В [шаге 8](#step-8) ты упёрся в то, что 998 кадров надо превратить в вектор фиксированной длины, и решил это средним по времени. Здесь ровно та же проблема и ровно то же решение — только усредняются не 40 MFCC-коэффициентов, заданных формулой, а 128 каналов, которые свёртка подобрала сама.

Здесь обычно спотыкаются в двух местах. Первое: думают, что `AdaptiveAvgPool2d(1)` как-то зависит от размера входа, и на длинном клипе чисел будет больше. Не будет: 128 каналов на входе — 128 чисел на выходе, всегда. Второе: путают, что именно даёт выигрыш в точности. Не пулинг — он про форму. Выигрыш даёт то, что признаки выучены под конкретную задачу, а не зафиксированы заранее.
