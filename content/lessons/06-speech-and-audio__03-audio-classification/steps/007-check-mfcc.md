---
id: 007-check-mfcc
type: check
title: 'Проверка: почему n_mels=40, а не 80'
source_anchor: '### Step 1: featurize'
check:
  - question: >-
      Почему увеличение n_mels при неизменном n_mfcc не расширяет вектор MFCC
      каждого кадра?
    options:
      - >-
        Потому что ширину итогового вектора задаёт n_mfcc, а n_mels определяет
        размер промежуточного входа DCT
      - Потому что mel-фильтры применяются уже после вычисления MFCC
      - Потому что n_mels управляет только числом временных кадров
      - Потому что реализация всегда игнорирует n_mels
    correct: 0
    explanation: >-
      При n_mfcc=13 DCT оставляет 13 коэффициентов независимо от того, построено
      перед ней 40 или 80 mel-полос. Дополнительные полосы меняют промежуточное
      представление и стоимость вычислений, но не ширину выхода.
  - question: >-
      Почему конфигурация, в которой n_mfcc больше n_mels, некорректна по
      смыслу?
    options:
      - Она обязательно уменьшает число временных кадров
      - >-
        DCT не может получить больше осмысленных коэффициентов, чем значений
        было на её входе
      - 'Mel-фильтры требуют, чтобы n_mfcc всегда совпадал с frame_len'
      - Логарифм можно применять только при равных n_mfcc и n_mels
    correct: 1
    explanation: >-
      В каждом кадре DCT преобразует вектор из n_mels mel-значений. Поэтому
      запросить, например, 40 коэффициентов из входа длины 13 нельзя: число
      выходных компонент ограничено размерностью входа.
---

Здесь нужно уметь ответить на один вопрос: кто в `featurize_mfcc` из [шага 6](#step-6) задаёт длину вектора, а кто — только подробность промежутка. `n_mfcc` — это сколько чисел останется в кадре. `n_mels` — сколько полос было до DCT.

Спотыкаются обычно на привычке «больше — точнее»: раз 80 mel-бинов подробнее сорока, значит и признаки будут лучше. Но DCT всё равно оставит первые 13 коэффициентов, и лишняя подробность в них почти не доедет — зато время счёта вырастет.

Второе место, где легко ошибиться: попросить у DCT больше коэффициентов, чем ей дали значений на входе.
