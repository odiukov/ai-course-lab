---
id: 024-proverka-align
type: check
title: 'Проверка: RoIPool против RoI Align'
source_anchor: '### Step 2: Compare to torchvision''s RoIAlign'
check:
  - question: >-
      Рамку немного сдвинули, но после округления её границы попали в те же
      целые клетки карты признаков. Как могут измениться результаты?
    options:
      - 'RoIPool может вернуть прежний результат, а RoI Align учтёт сдвиг'
      - 'RoI Align обязательно вернёт прежний результат, а RoIPool учтёт сдвиг'
      - Оба метода обязательно вернут прежний результат
    correct: 0
    explanation: >-
      RoIPool квантует координаты, поэтому небольшой сдвиг может исчезнуть при
      округлении. RoI Align сохраняет дробные координаты, и его точки выборки
      сдвигаются вместе с рамкой.
  - question: >-
      Максимальное абсолютное отличие собственной реализации RoI Align от
      torchvision составляет 0,00001. Как правильнее интерпретировать результат?
    options:
      - Собственная реализация фактически выполняет RoIPool
      - Координаты рамки необходимо округлить
      - Реализации совпадают в пределах обычной погрешности float32
    correct: 2
    explanation: >-
      Расхождение порядка 1e-5 нормально для вычислений во float32. Ошибка в
      преобразовании координат, например пропущенное смещение на полпикселя,
      обычно дала бы разницу на несколько порядков больше.
---

Проверь, связываешь ли ты округление из [шага 15](#step-15) с потерей небольших сдвигов, а дробные точки из [шага 20](#step-20) — с сохранением положения объекта. Частая ловушка: считать любое ненулевое отличие от эталона ошибкой. После сверки из [шага 23](#step-23) важно различать крошечную float-погрешность и заметное расхождение из-за неверных координат.

> 🎒 **На пальцах.** Представь два термометра: один показывает только целые градусы, второй — сотые доли. При потеплении на 0,2 градуса первый может не измениться, а второй заметит сдвиг. Но если показания двух точных термометров расходятся на 0,00001 градуса, это шум измерения, а не разная погода.
