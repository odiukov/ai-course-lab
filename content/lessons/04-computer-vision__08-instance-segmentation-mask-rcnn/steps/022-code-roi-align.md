---
id: 022-code-roi-align
type: code
title: Пишем roi_align
source_anchor: '### Step 1: RoIAlign from scratch'
exercise_fn: roi_align
---

Собери `roi_align_single`: функция получает карту признаков формы `(C, H, W)` и одну рамку `(x1, y1, x2, y2)` в пикселях исходного изображения. На выходе нужен фрагмент `(C, output_size, output_size)`, где каждое значение снято из центра своей ячейки билинейной интерполяцией из [шага 19](#step-19).

```python
import torch
import torch.nn.functional as F

def roi_align_single(
    feature,
    box,
    output_size=7,
    spatial_scale=1 / 16.0,
):
    """
    feature: карта признаков (C, H, W)
    box: рамка (x1, y1, x2, y2) в пикселях изображения
    """
    # 1. Переведи координаты рамки на карту признаков.
    # 2. Найди ширину и высоту одной ячейки результата.
    # 3. Построй координаты центров всех ячеек.
    # 4. Собери сетку формы (1, output_size, output_size, 2).
    # 5. Вызови F.grid_sample и убери временную batch-ось.
    pass
```

Сначала примени `spatial_scale`, как в [шаге 21](#step-21). При соглашении `align_corners=False` координаты карты также сдвигаются на `-0.5`: центр первой клетки тогда расположен в точке `0`, а не на её границе.

После размещения точек по правилу из [шага 20](#step-20) подготовь их для `F.grid_sample`. Эта функция принимает координаты не в клетках карты, а в диапазоне от `-1` до `1`. В сетке последняя ось хранит сначала `x`, затем `y`; `torch.meshgrid(..., indexing="ij")` обычно возвращает их в обратном порядке, поэтому здесь легко незаметно транспонировать результат.

Добавь временную batch-ось и карте, и сетке. Проверь три вещи: форма результата равна `(C, output_size, output_size)`, рамка остаётся дробной на всём пути, а в реализации нет `round`, `floor` или преобразования в целые числа.

> 🎒 **На пальцах.** Представь дегустационный лоток, размеченный на 49 равных секций. В каждой секции ты берёшь пробу строго из центра, даже если центр оказался между делениями мерной шкалы. Прибор смешивает показания ближайших датчиков. Если сначала округлить положение пробника, соседние секции начнут получать пробы не из тех мест — именно этого RoI Align не допускает.
