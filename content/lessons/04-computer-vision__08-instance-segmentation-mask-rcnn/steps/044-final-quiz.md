---
id: 044-final-quiz
type: quiz
title: 'Итоговая проверка: собрать и диагностировать Mask R-CNN'
source_anchor: '## Exercises'
check:
  - question: >-
      Разрешение выхода mask head увеличили с 28×28 до 56×56, не меняя число
      каналов и RoI. Как изменится число предсказываемых значений для одной
      маски?
    options:
      - 'Удвоится, потому что удвоилась сторона'
      - >-
        Увеличится в четыре раза, потому что выросли обе пространственные
        размерности
      - 'Не изменится, потому что число классов осталось прежним'
      - Уменьшится вдвое из-за более точной интерполяции
    correct: 1
    explanation: >-
      Площадь выхода выросла с 28² = 784 до 56² = 3136 элементов. Поэтому память
      и объём вычислений в пространственной части mask head возрастают примерно
      в четыре раза.
  - question: >-
      Backbone Mask R-CNN заморожен перед дообучением на маленьком датасете.
      Какой результат одного шага оптимизатора подтверждает, что настройка
      выполнена правильно?
    options:
      - 'Параметры backbone изменились, а параметры голов остались прежними'
      - Не изменился ни один параметр модели
      - 'Параметры backbone не изменились, а параметры RPN и RoI-голов обновились'
      - Обновились только статистики итоговой метрики AP
    correct: 2
    explanation: >-
      Заморозка должна исключить параметры backbone из обучения, но оставить
      обучаемыми RPN, box head и mask head. Если головы тоже не обновляются,
      модель не сможет адаптироваться к новому датасету.
  - question: >-
      Какой результат корректнее использовать для отчёта о дообучении Mask R-CNN
      на собственном наборе данных?
    options:
      - Mask AP на отложенной выборке с настоящими масками экземпляров
      - Минимальный training loss без проверки предсказаний
      - 'Box AP на тех же изображениях, на которых модель обучалась'
      - Среднюю уверенность всех предложений RPN
    correct: 0
    explanation: >-
      В упражнении требуется mask AP@0.5, поэтому оценивать нужно именно маски
      на данных, не использованных для обновления весов. Training loss и Box AP
      измеряют другие свойства модели.
  - question: >-
      Нужно честно сравнить mask head с выходом 28×28 и вариант с выходом 56×56.
      Как организовать эксперимент?
    options:
      - >-
        Обучить варианты на разных датасетах, чтобы каждый получил подходящие
        примеры
      - >-
        Для нового варианта одновременно заменить backbone и увеличить число
        эпох
      - 'Сравнить только объём памяти, не измеряя качество масок'
      - >-
        Оставить одинаковыми данные, обучение и оценивание, изменив только
        разрешение mask head
    correct: 3
    explanation: >-
      Такое сравнение является контролируемой абляцией: различие в mAP@IoU=0.75
      можно связать с разрешением mask head, только если остальные существенные
      условия одинаковы.
---

Когда ты дальше будешь собирать детектор под свой датасет, полезнее помнить не список блоков, а место возможной поломки. Кандидаты и дубли ведут к [шагам 6](#step-6)–[12](#step-12), сдвинутые маски — к [шагам 17](#step-17)–[22](#step-22), а разрыв между Box AP и Mask AP — к [шагу 43](#step-43). Частая ошибка — менять всю модель, не выяснив, на каком участке испортился результат.

> 🎒 **На пальцах.** Представь техосмотр автомобиля. Если машина не заводится, бессмысленно сразу менять шины; если едет ровно, но плохо тормозит, двигатель уже не главный подозреваемый. Так же и здесь: рамки, классы и маски дают отдельные признаки неисправности. Сначала находишь слабый узел по результатам, затем меняешь именно его.
