---
id: 031-output-traps
type: check
title: 'Проверка: фон, индексы классов и порог маски'
source_anchor: '### Output format'
check:
  - question: >-
      У тебя четыре собственных класса. Как правильно назначить им
      идентификаторы для Mask R-CNN?
    options:
      - 'От 0 до 3, потому что отсчёт обычно начинается с нуля'
      - 'От 1 до 4, потому что идентификатор 0 зарезервирован для фона'
      - 'От 0 до 4, причём последний идентификатор означает фон'
    correct: 1
    explanation: 'Нулевой идентификатор занят фоном, поэтому реальные классы начинаются с 1.'
  - question: >-
      В трёх пикселях маски записаны вероятности 0,49, 0,50 и 0,51. Ты
      применяешь условие больше 0,5. Какие пиксели станут передним планом?
    options:
      - Все три
      - 'Пиксели со значениями 0,50 и 0,51'
      - 'Только пиксель со значением 0,51'
      - 'Только пиксель со значением 0,49'
    correct: 2
    explanation: >-
      Строгое условие больше 0,5 не включает само значение 0,5, поэтому проходит
      только 0,51.
---

Проверь, что ты читаешь контракт из [шага 30](#step-30) буквально: `label = 0` означает фон, а не первый объектный класс. Поэтому четыре твоих класса занимают индексы 1–4. Значения маски — ещё не ответы «объект» или «фон», а уверенности от 0 до 1. Лишь сравнение с порогом превращает их в бинарную маску; при условии `> 0.5` само значение 0,5 не проходит.

> 🎒 **На пальцах.** Представь гардероб: жетон 0 висит у служебной двери и посетителям не выдаётся, поэтому их номера начинаются с 1. Рядом контролёр пропускает только билеты с рейтингом строго выше 0,5. Билет с ровно 0,5 остаётся снаружи — слово «выше» здесь решает всё.
