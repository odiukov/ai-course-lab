- concept: предсказания для батча организованы как отдельный словарь для каждого изображения
  question: Как организован результат инференса torchvision Mask R-CNN для батча изображений?
  explanation: Число обнаруженных объектов может различаться между изображениями, поэтому модель возвращает отдельный словарь предсказаний для каждого элемента батча.
  kind: choice
  options:
    - Один словарь только для изображения с наибольшим числом обнаружений
    - Единый тензор, в котором обнаружения всех изображений смешаны без указания их принадлежности
    - Кортеж из общего набора рамок и одной общей маски на весь батч
    - Список словарей, где каждый словарь содержит предсказания для соответствующего изображения
  correct: 3
  id: 030-output-contract-1
  fingerprint: 442fdc76
- concept: одинаковый индекс связывает рамку, класс, оценку и маску одного экземпляра
  question: После фильтрации обнаружений по уверенности инженер оставил часть элементов scores, но не применил тот же фильтр к boxes, labels и masks. Почему результат стал некорректным?
  explanation: Поля словаря согласованы по индексу. Любое удаление, перестановку или отбор обнаружений необходимо одинаково выполнять для boxes, labels, scores и masks.
  kind: open
  reference: 'Первые измерения всех полей перечисляют одни и те же экземпляры в одинаковом порядке. Фильтрация только одного поля нарушает соответствие: рамка, класс, оценка и маска с одинаковым индексом перестают описывать один объект. Один и тот же индексный или булев фильтр нужно применить ко всем полям.'
  id: 030-output-contract-2
  fingerprint: 39ee50ef
- concept: вероятностную маску превращают в бинарную сравнением с выбранным порогом
  question: Заполни оператор, который превращает вероятностные маски Mask R-CNN в булевы при заданном пороге.
  explanation: Поле masks содержит вещественные карты уверенности, а не готовые булевы силуэты. Сравнение каждого значения с порогом создаёт бинарную маску.
  kind: cloze
  template: binary_masks = prediction["masks"] ___ threshold
  answer: '>'
  accept:
    - '>'
  id: 030-output-contract-3
  fingerprint: 11f544e6
