---
id: 003-contract-check
type: check
title: Проверяем границы модели
source_anchor: '### Weight tying'
check:
  - question: >-
      Таблица эмбеддингов содержит 50257 строк по 768 чисел. Почему её можно
      использовать и в языковой голове?
    options:
      - >-
        Скрытый вектор из 768 чисел можно сопоставить с каждой из 50257 строк и
        получить 50257 оценки
      - Любую матрицу можно повторно использовать независимо от её размеров
      - Языковая голова всегда уменьшает число координат до одного
    correct: 0
    explanation: >-
      Размер скрытого вектора совпадает с длиной строки эмбеддинга, поэтому для
      каждого токена можно получить отдельную оценку.
  - question: >-
      Ты скопировал значения эмбеддингов в отдельную матрицу языковой головы.
      Получилось ли связывание весов?
    options:
      - 'Да, одинаковых начальных значений достаточно'
      - 'Нет, после обновлений две независимые матрицы могут разойтись'
      - 'Да, если обе матрицы имеют одинаковую форму'
    correct: 1
    explanation: >-
      Связывание означает один общий тензор, а не две одинаково заполненные
      копии. Общий тензор получает совместные обновления.
---

Теперь проверь границы собранной модели: вход и выход должны соответствовать контракту из [шага 2](#step-2), а таблица эмбеддингов — подходить языковой голове по форме. Чаще всего здесь путают одинаковые значения с общей памятью. Две матрицы могут выглядеть одинаково в момент создания, но это ещё не связанные веса: обучение начнёт менять их независимо.

> 🎒 **На пальцах.** Представь библиотечный каталог: одна карточка помогает и найти книгу по названию, и определить название найденной книги. Ксерокопия карточки сначала выглядит так же, но исправление на оригинале в копию не попадёт. Связывание весов — это одна карточка для обеих операций.
