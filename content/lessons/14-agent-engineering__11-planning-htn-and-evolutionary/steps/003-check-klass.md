---
id: 003-check-klass
type: check
title: 'Проверка: какой класс задачи'
source_anchor: '## The Problem'
check:
  - question: >-
      Тебе дают задачу: подобрать порядок проходов оптимизатора компилятора,
      чтобы собранный код работал быстрее. К какому классу она относится?
    options:
      - 'К задачам с доказуемой корректностью: порядок проходов нельзя нарушать'
      - >-
        К оптимизациям с машинно-проверяемой метрикой: любой рабочий порядок
        годится, вопрос в скорости
      - 'Ни к тому, ни к другому: тут не нужен ни поиск, ни валидация плана'
    correct: 1
    explanation: >-
      Нет запрета, нарушение которого делает результат браком: любой порядок,
      дающий рабочий бинарник, допустим, а машина детерминированно замеряет
      время выполнения и сравнивает кандидатов. Это второй класс — тот, под
      который построен AlphaEvolve.
  - question: >-
      План маршрутов самолётов от LLM оказывается корректным в девяти случаях из
      десяти. Почему нельзя считать, что задача решена «почти хорошо»?
    options:
      - >-
        Потому что доля корректных планов низковата, надо дотянуть её до
        девяноста девяти процентов
      - >-
        Потому что в задаче с жёсткими ограничениями некорректный маршрут не
        ухудшает оценку, а делает план неисполнимым — усреднять по попыткам
        нечего
      - 'Потому что LLM работает слишком медленно, чтобы перебрать все маршруты'
    correct: 1
    explanation: >-
      «Редко ошибается» и «не ошибается» — разные вещи только в первом классе
      задач, и разница там качественная. Комплаенс-процесс, корректный в девяти
      случаях из десяти, — это не почти корректный процесс, а сломанный. Поэтому
      корректность должен гарантировать символьный слой, а не частота удачных
      генераций.
---

Дальше в уроке пойдут два разных механизма, и выбрать между ними можно только после того, как ты назвал класс задачи. Поэтому потренируйся именно на этом: посмотреть на постановку и ответить, что происходит при одном нарушении. Если ответ «результат не годится вовсе» — это первый класс, из [шага 2](#step-2). Если «результат чуть хуже, и машина сама скажет насколько» — второй.

Спотыкаются обычно на двух вещах. Первая: наличие числовой метрики принимают за признак оптимизации, хотя в расписании смен тоже можно посчитать «сколько запретов нарушено». Вторая: путают «редко ошибается» с «не ошибается» — 90 процентов корректных планов в комплаенсе не складываются в почти корректный процесс.
