- concept: проверка ключа идемпотентности стоит после предусловия и до вызова, а попадание возвращает прежний результат без нового вызова
  question: Где в шаге стоит проверка ключа идемпотентности и что происходит при попадании в уже завершённый ключ?
  explanation: 'Смысл ключа — не допустить второго внешнего эффекта, поэтому проверка обязана предшествовать вызову. Предусловие она не заменяет: одно спрашивает «допустимо ли действие сейчас», другое — «не совершено ли оно уже».'
  kind: choice
  options:
    - 'После вызова действия: результат сравнивается с прежним и при совпадении шаг считается успешным'
    - 'После проверки предусловия и до вызова: при попадании шаг сразу возвращает прежний успешный результат, не вызывая действие'
    - 'До проверки предусловия: попадание в ключ делает проверку предусловия ненужной'
    - 'Внутри постпроверки: ключ используется как критерий совпадения целевого состояния'
  correct: 1
  id: 029-run-step-1
  fingerprint: e55f8ebb
- concept: чекпоинт успешного перехода создаётся только после прохождения постпроверки
  question: Почему новый чекпоинт успешного состояния записывается после постпроверки, а не сразу по успешному ответу вызова?
  explanation: Записанный чекпоинт становится точкой, от которой продолжат другие воркеры. Фиксировать переход, эффект которого не подтверждён независимым чтением, — значит закрепить расхождение с внешним миром как проверенный факт.
  kind: choice
  options:
    - Потому что запись чекпоинта требует времени, а ответ вызова приходит раньше
    - Потому что чекпоинт, записанный по одному лишь ответу, зафиксирует как достигнутый переход, эффект которого мог не наступить, и следующее восстановление продолжит от ложной границы
    - Потому что до постпроверки ключ идемпотентности ещё не сформирован
    - Потому что чекпоинты разрешено записывать только в конце запуска
  correct: 1
  id: 029-run-step-2
  fingerprint: 6a4c3b1a
- concept: провал постпроверки ведёт к откату на чекпоинт начала шага, а не к повтору действия
  question: Дополни ветку неуспешной постпроверки внутри шага.
  explanation: 'Действие уже вызвано, поэтому повторять его нельзя — это дало бы второй эффект. Единственный корректный ход: снять последствия и вернуть внутреннее состояние к чекпоинту, сохранённому в начале шага, вернув неуспех наружу.'
  kind: cloze
  template: |-
    if not verify_target_state(action):
        ___(before_checkpoint)
        return StepResult(ok=False)
  answer: rollback_to
  accept:
    - rollback_to
    - rollback_to(
    - откат к чекпоинту
  id: 029-run-step-3
  fingerprint: a55994be
