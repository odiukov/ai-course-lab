- concept: статус намерения записывается до внешнего вызова, чтобы любое падение оставляло состояние с известной процедурой доследования
  question: Заполни пропуск в безопасной последовательности коммита побочного эффекта.
  explanation: Запись намерения до вызова не предотвращает падение — она задаёт, что делать после него. Состояние «начинал, результат не подтверждён» лечится одной проверкой целевого состояния; состояние «непонятно, вызывал ли вообще» не лечится ничем.
  kind: cloze
  template: |-
    save_status(run_id, "___")
    result = call_tool(action, idempotency_key=k)
    verify_target_state(action)
    save_status(run_id, "committed")
  answer: in_flight
  accept:
    - in-flight
    - in flight
    - inflight
    - in_flight
  id: 032-pravilnyy-poryadok-1
  fingerprint: 71cd7320
