---
id: 003-contract-check
type: check
title: Проверяем границы данных и обучения
source_anchor: '### Frozen encoder is the trick'
check:
  - question: >-
      Какой результат после обратного прохода подтверждает, что граница
      заморозки соблюдена?
    options:
      - >-
        Градиенты есть у проектора, а у визуального энкодера и текстовой таблицы
        их нет
      - >-
        Градиенты есть у всех компонентов, но оптимизатор обновляет только
        энкодер
      - Градиентов нет ни у одного компонента модели
    correct: 0
    explanation: >-
      Для обучения моста проектор должен получать градиенты. Замороженные
      визуальная и текстовая части участвуют в прямом проходе, но градиенты для
      их параметров не вычисляются.
  - question: >-
      За счёт чего заморозка тяжёлых компонентов непосредственно удешевляет шаг
      обучения?
    options:
      - Она уменьшает размер каждого входного изображения
      - Она исключает вычисление и хранение градиентов для их параметров
      - Она автоматически увеличивает размер синтетического корпуса
    correct: 1
    explanation: >-
      Обратный проход обслуживает только параметры проекции, а не десятки
      миллионов параметров тяжёлых компонентов. Поэтому требуется меньше
      вычислений и памяти.
---

Ты запускаешь несколько шагов обучения и видишь падающую потерю. Этого ещё недостаточно: проверь, за счёт каких параметров она падает и на каких парах учится модель. Контракт пары задан в [шаге 2](#step-2), а обучаемый мост — в [шаге 1](#step-1). Частая ошибка — случайно обновлять тяжёлые энкодеры: результат выглядит успешным, но игрушечные данные начинают менять знания, которые должны были остаться опорой. Вторая ошибка — считать синтетический корпус полноценным материалом для обучения всей системы.
