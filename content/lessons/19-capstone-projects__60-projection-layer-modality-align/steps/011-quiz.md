---
id: 011-quiz
type: quiz
title: 'Проверяем полный шов данных, проекции, заморозки и выравнивания'
source_anchor: '## Tests'
check:
  - question: >-
      Ты дважды создаёшь синтетический пример с одним seed. Какой результат
      подтверждает контракт данных?
    options:
      - >-
        Совпали изображение и подпись, длина подписи не превышает max_len, а id
        0 используется как padding
      - >-
        Совпало только изображение, потому что подпись должна меняться при
        каждом запуске
      - 'Совпала только подпись, а изображение осталось случайным'
      - 'Подпись всегда имеет ровно max_len, а id 0 обозначает обычный токен'
    correct: 0
    explanation: >-
      Один seed должен воспроизводить оба связанных объекта. Ограничение max_len
      и отдельный id для padding сохраняют контракт подписи.
  - question: >-
      Проектор получает признаки формы 1 на D. Какая форма выхода показывает,
      что он соблюдает контракт?
    options:
      - Один вектор длины out_dim без размерности batch
      - Форма 1 на out_dim
      - Форма 1 на D независимо от настройки
      - Форма out_dim на 1
    correct: 1
    explanation: >-
      Проектор меняет последнюю ось на out_dim, но сохраняет ведущую размерность
      batch, даже когда в нём один пример.
  - question: >-
      Что ты ожидаешь увидеть после обратного прохода при правильно настроенной
      заморозке?
    options:
      - 'Градиенты появились и у таблицы эмбеддингов, и у проектора'
      - 'Градиентов нет ни у таблицы эмбеддингов, ни у проектора'
      - >-
        У замороженной таблицы нет обучаемых параметров, а у проектора есть
        ненулевые градиенты
      - 'У таблицы есть градиенты, но оптимизатор должен просто игнорировать их'
    correct: 2
    explanation: >-
      Замороженная часть не участвует в обучении, зато градиент обязан дойти до
      проектора — иначе мост не сможет выравнивать модальности.
  - question: >-
      Какой набор результатов подтверждает и смысл косинусной потери, и работу
      обучения?
    options:
      - >-
        Для одинаковых направлений потеря 2, для противоположных 0, после
        обучения она выросла
      - 'Для любых ненулевых векторов потеря 0, после обучения она не изменилась'
      - >-
        Для одинаковых направлений потеря 0, для противоположных 2, к шагу 200
        она стала меньше, чем на шаге 0
      - Потеря зависит только от длины векторов и обязана стать отрицательной
    correct: 2
    explanation: >-
      Граничные значения проверяют направление сравнения, а снижение потери
      показывает, что градиент действительно изменяет проектор в полезную
      сторону.
---

Дальше тебе понадобится проверять не отдельные детали, а весь путь сразу: контракт пары из [шага 4](#step-4), маскирование padding из [шага 6](#step-6), форму проекции из [шага 8](#step-8) и поведение потери из [шага 10](#step-10). Чаще всего всё ломается тихо: форма выглядит правдоподобно, но batch исчез, замороженные параметры обучаются или градиент не доходит до проектора.

> 🎒 **На пальцах.** Представь проверку поезда перед отправлением. Недостаточно осмотреть колёса каждого вагона: сцепки должны подходить, запертые вагоны — оставаться закрытыми, а тяга локомотива — доходить до последнего вагона. Итоговая проверка делает то же с моделью: подтверждает и исправность частей, и то, что вместе они действительно движутся в нужную сторону.
