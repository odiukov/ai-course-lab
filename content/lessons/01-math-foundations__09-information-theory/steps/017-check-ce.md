---
id: 017-check-ce
type: check
title: 'Проверка: потери при 90% и при 10% уверенности'
source_anchor: '### Cross-Entropy (The Loss Function You Use Every Day)'
check:
  - question: >-
      На одной картинке модель дала верному классу 0.9, на другой — 0.1. Во
      сколько примерно раз отличаются потери?
    options:
      - Ровно в 9 раз — во столько же отличаются сами вероятности
      - 'Примерно в 20 раз: 0.105 против 2.3'
      - 'Ровно в 2 раза, логарифм почти сглаживает разницу'
      - 'Потери одинаковые, ведь верный класс один и тот же'
    correct: 1
    explanation: >-
      Минус логарифм от 0.9 даёт около 0.105, от 0.1 — около 2.3. Логарифм
      растягивает низкие вероятности, поэтому разрыв гораздо больше, чем разрыв
      самих вероятностей.
  - question: >-
      Модель дала верному классу 0.1, а оставшиеся 0.9 в первом случае отдала
      одному классу, а во втором размазала поровну по девяти. Что с потерями?
    options:
      - 'Одинаковые: в счёт идёт только вероятность верного класса'
      - 'Больше там, где 0.9 достались одному классу — ошибка увереннее'
      - 'Больше там, где вероятность размазана — больше беспорядка'
    correct: 0
    explanation: >-
      При one-hot метке в сумме выживает единственное слагаемое — про верный
      класс. Как распределён остаток, на число потерь не влияет.
---

Дальше ты будешь смотреть на число потерь и по нему решать, учится сеть или стоит на месте, — значит, нужно чувство масштаба этого числа. Считается оно коротко (шаг 16): берёшь вероятность, которую модель дала верному классу, и от неё минус логарифм. Дала 0.9 — потери около 0.105, почти ноль. Дала 0.1 — около 2.3.

Спотыкаются тут о два места. Первое: кажется, что потери меняются пропорционально вероятности. Вероятности отличаются в девять раз, а потери — больше чем в двадцать: логарифм сильно растягивает низ шкалы, и уверенная ошибка наказывается непропорционально жёстко. Второе: начинают считать, что модель ответила про остальные классы. При one-hot метке остальные слагаемые умножаются на ноль и в счёт не идут.
