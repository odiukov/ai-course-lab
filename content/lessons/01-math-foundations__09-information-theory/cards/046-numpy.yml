- concept: np.asarray с вещественным типом подготавливает входные распределения к векторным вычислениям
  question: Зачем в NumPy-реализации кросс-энтропии преобразовывать оба входа через np.asarray с dtype=float?
  explanation: np.asarray превращает входные последовательности в массивы, а dtype=float задаёт подходящий тип для вероятностей и логарифмов. Это преобразование само по себе не сортирует и не нормирует данные.
  kind: choice
  options:
    - Чтобы автоматически отсортировать вероятности по возрастанию
    - Чтобы автоматически нормировать каждое распределение
    - Чтобы единообразно принимать списки и массивы и выполнять вычисления в вещественном типе
  correct: 2
  id: 046-numpy-1
  fingerprint: 860b3a5b
- concept: одна и та же булева маска должна сохранять соответствие исходов в истинном и предсказанном распределениях
  question: Заполни пропуск в векторизованной реализации кросс-энтропии.
  explanation: Маска выбирает исходы по истинному распределению p. Применение той же маски к q сохраняет попарное соответствие вероятностей одного и того же исхода перед умножением.
  kind: cloze
  template: |-
    mask = p > 0
    return -(p[mask] * np.log(___)).sum()
  answer: q[mask]
  accept:
    - q[mask]
  id: 046-numpy-2
  fingerprint: 1ca2b59b
