- concept: тестовый двойник самокритика должен воспроизводить ограничения моделируемого компонента, а не превосходить их
  question: Учебная реализация цикла Self-Refine собрана на заглушках. Разработчик пишет функцию самокритики так, что внутри она вызывает точный детерминированный валидатор (он измеряет длину каждого пункта и знает истину), а его вердикт лишь переводит в текст. Что при этом ломается?
  explanation: 'Роль feedback в Self-Refine — это мнение модели о собственном выводе, а не измерение. Заглушка, делегирующая точному валидатору, отвечает всегда верно, и учебный цикл начинает показывать поведение, которого у настоящей самокритики нет. Именно слепое пятно самопроверки — та слабость, ради лечения которой CRITIC подставляет внешние инструменты (поиск, интерпретатор, тесты). Правдоподобный двойник обязан ошибаться там же, где ошибается оригинал: ловить длину и пропускать неверный факт.'
  kind: choice
  options:
    - Заглушка становится непогрешимой и перестаёт воспроизводить ненадёжность LLM в самопроверке — исчезает сама причина, по которой нужен внешний верификатор
    - Роль refine теряет доступ к истории предыдущих черновиков и критик
    - Критика перестаёт быть строкой и превращается в число, из-за чего refine не может её прочитать
    - Цикл начинает крутиться до потолка итераций, потому что критика всегда одинакова
  correct: 0
  id: 017-self-feedback-1
  fingerprint: ba9c3941
- concept: остановка по вердикту критика — сравнение с одной литеральной константой, а не с вольным написанием строки
  question: В цикле Self-Refine критик возвращает свободный текст, а сигналом «дефектов нет» служит одна условленная строка. Заполни пропуск так, чтобы условие остановки не зависело от того, как строку набрали в разных местах кода.
  explanation: 'Сигнал остановки здесь — литерал, а не семантика: сравнение строгое. Если критик вернёт «No issues.» или «no issues!», а сравнение идёт с «no issues», условие не сработает ни разу, и цикл дойдёт до потолка итераций, оплатив лишние вызовы модели на пустом месте. Единая константа, используемая и при возврате, и при сравнении, убирает расхождение написаний. Для настоящей модели этого мало — там сигнал нормализуют или просят структурированный флаг.'
  kind: cloze
  template: |-
    NO_ISSUES = "no issues"

    critique = self_feedback(task, draft)
    if critique == ___:
        break
  answer: NO_ISSUES
  accept:
    - NO_ISSUES
    - 'NO_ISSUES  # константа'
    - константа NO_ISSUES
  id: 017-self-feedback-2
  fingerprint: 679d022b
