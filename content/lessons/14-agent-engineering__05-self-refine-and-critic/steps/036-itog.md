---
id: 036-itog
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты собрал цикл, где feedback и refine идут одним и тем же промптом, а на
      вход refine подаётся только последний вывод. Что вероятнее всего увидишь?
    options:
      - >-
        Критика начнёт возвращать «замечаний нет» уже на первом витке, а правки
        будут ходить по кругу и ломать то, что было исправлено раньше
      - >-
        Качество вырастет быстрее, потому что модель не отвлекается на лишний
        контекст
      - 'Цикл упадёт с ошибкой, потому что refine не может работать без истории'
      - >-
        Ничего не изменится: история влияет только на скорость, но не на
        результат
    correct: 0
    explanation: >-
      Одинаковые промпты дают самосогласие — критика штампует «всё хорошо». А
      без прошлых выводов и критик refine не помнит, что уже пробовал, и
      возвращает отброшенные варианты.
  - question: >-
      Задача: посчитать сумму по накладной. Что делает проверку в этом цикле
      заземлённой, а не самооценкой?
    options:
      - >-
        Попросить модель перечитать свой ответ и оценить уверенность по шкале от
        1 до 10
      - >-
        Прогнать числа калькулятором или кодом и сравнить результат с ответом
        модели
      - 'Задать критику вопрос вежливее, чтобы она была строже'
      - Увеличить потолок итераций с 3 до 10
    correct: 1
    explanation: >-
      Заземление — это внешний сигнал, который не зависит от мнения модели.
      Калькулятор или тест возвращают факт; шкала уверенности возвращает то же
      самое мнение другими словами.
  - question: >-
      Почему условие остановки «критика сказала: замечаний нет» нельзя оставлять
      единственным?
    options:
      - Потому что оно слишком строгое и цикл почти никогда не остановится
      - >-
        Потому что оно требует запускать верификатор на каждом витке, а это
        дорого
      - >-
        Потому что модель может выдать «замечаний нет» на плохом выводе, а без
        потолка итераций цикл в другом случае будет крутиться и жечь запросы
      - >-
        Потому что остановку вообще нельзя доверять тексту — только внешнему
        верификатору
    correct: 2
    explanation: >-
      Одно условие ломается в обе стороны: ложное «всё хорошо» останавливает
      рано, а вечные замечания не останавливают никогда. Поэтому — верификатор
      прошёл ИЛИ нет замечаний, И жёсткий потолок.
---

Ты собрал этот цикл руками: критерий, генератор по скрипту, две формы критики, refine с историей, условие остановки, `while` вокруг них. Дальше в курсе это перестанет быть отдельной темой и станет деталью внутри агентов — почти каждый агент где-то внутри крутит виток «сделал → проверил → переделал», и ты будешь узнавать его по этим четырём функциям.

Спотыкаются обычно на трёх местах. Забывают историю в промпте refine и удивляются, почему модель ходит кругами. Дают критике тот же промпт, что и генерации, и получают «замечаний нет» на кривом выводе. И оставляют одно условие остановки вместо двух — либо цикл встаёт рано, либо не встаёт вообще.

И главное, что стоит унести: прирост даёт не «модель умнее», а доступ к внешнему сигналу.
