---
id: 006-proverka-nulya
type: check
title: 'Объясните, почему необученный ControlNet не меняет ответ базы'
source_anchor: '### ControlNet (Zhang et al., 2023)'
check:
  - question: >-
      Ты запускаешь базовую модель и ту же модель с только что подключённым
      необученным ControlNet, сохраняя одинаковые промпт, seed и настройки. Что
      ожидаешь увидеть?
    options:
      - 'Одинаковый результат: управляющая ветвь пока добавляет нулевую поправку'
      - Случайно изменённую композицию из-за необученных весов управляющей ветви
      - Точное повторение управляющего изображения
    correct: 0
    explanation: >-
      Нулевая свёртка на старте обнуляет выход ControlNet, поэтому декодер
      получает те же признаки, что и без него.
  - question: >-
      Внутри необученной управляющей ветви уже возникают ненулевые признаки.
      Почему они не влияют на ответ базы?
    options:
      - Базовая модель автоматически исправляет их
      - Они проходят через нулевую свёртку и превращаются в нулевую добавку
      - Управляющее изображение временно заменяется промптом
    correct: 1
    explanation: >-
      Важно не то, что вычисляет ветвь внутри, а что доходит до базы: нулевая
      свёртка пока ничего не пропускает.
---

Ты сравниваешь два запуска с одинаковыми промптом, seed и настройками: чистую базовую модель и её же с только что подключённым ControlNet. Результаты совпадают. Это не значит, что новая ветвь ничего не вычисляет: внутри неё уже могут появляться сложные признаки. Но нулевая свёртка из [шага 5](#step-5) превращает их в нулевую поправку, поэтому до декодера не доходит никакого изменения.

Здесь легко спутать «ветвь работает» и «ветвь влияет». До обучения верно первое, но не второе.

> 🎒 **На пальцах.** Представь нового советника, который уже пишет записки режиссёру, но передаёт их через закрытый почтовый ящик. Записки существуют, однако режиссёр их не получает и продолжает ставить сцену по прежнему плану. Обучение постепенно открывает щель ящика — только тогда советы начинают менять постановку.
