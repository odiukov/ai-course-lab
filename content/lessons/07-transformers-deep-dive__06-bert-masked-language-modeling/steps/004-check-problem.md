---
id: 004-check-problem
type: check
title: 'Проверка: что именно переиспользуют'
source_anchor: '## The Problem'
check:
  - question: >-
      У тебя три задачи: определить тональность отзыва, распознать названия
      компаний и найти семантически похожие документы. Что в типичном
      BERT-подходе переиспользуется между задачами?
    options:
      - >-
        Небольшая задача-специфичная голова, а encoder для каждой задачи
        обучается с нуля
      - >-
        Предобученный encoder, к которому добавляют отдельную небольшую голову
        или выходной слой для каждой задачи
      - >-
        Размеченные данные одной задачи, на которых без изменений обучают
        остальные задачи
      - >-
        Только токенизатор, тогда как все параметры модели для каждой задачи
        обучают заново
    correct: 1
    explanation: >-
      Общее языковое представление находится в предобученном encoder и
      переиспользуется. Под конкретную задачу добавляют и дообучают небольшую
      надстройку; при необходимости вместе с ней могут дообучать и веса encoder.
  - question: >-
      После добавления классификационной головы к предобученному BERT где
      находится основная часть параметров всей модели?
    options:
      - В классификационной голове
      - В предобученном encoder
      - В токенизаторе
      - Параметры распределены примерно поровну между encoder и головой
    correct: 1
    explanation: >-
      Encoder содержит сотни миллионов параметров, тогда как простая
      классификационная голова — обычно лишь тысячи. Поэтому почти вся ёмкость
      модели приходит из предобученного encoder, даже если при адаптации
      обновляются веса обоих блоков.
---

После этих трёх экранов ты должен уметь показать пальцем, где в схеме проходит граница: что приходит готовым, а что ты обучаешь сам. Слева от границы — encoder, сотни миллионов параметров, одна и та же тяжёлая часть для тональности, NER и поиска. Справа — голова под конкретную задачу, тысячи параметров и твои размеченные данные.

Спотыкаются обычно на двух вещах. Первая: кажется, что «дообучить» — это как-то мягко переучить всю модель заново, и экономия только во времени. Вторая: кажется, что раз голова решает задачу, то она и есть главное. Проверь себя на числах из [шага 2](#step-2) — они разводят эти два блока на три порядка.
