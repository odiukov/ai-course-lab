---
id: 033-check-head
type: check
title: 'Проверка: сколько чисел на выходе каждой головы'
source_anchor: '### Step 4: fine-tune head'
check:
  - question: >-
      Почему после перехода на токенизатор с более крупным словарём нельзя
      оставить MLM-голову с прежней длиной выхода?
    options:
      - >-
        Каждая координата выхода соответствует токену-кандидату, поэтому
        выходной слой должен соответствовать новому словарю
      - >-
        Размер скрытого вектора encoder автоматически становится равен размеру
        нового словаря
      - >-
        Длина выхода MLM-головы должна совпадать только с количеством
        замаскированных позиций
      - Длину выхода MLM-головы задаёт количество классов задачи тональности
    correct: 0
    explanation: >-
      MLM-голова оценивает каждый токен словаря как возможный ответ. Поэтому при
      переходе от словаря на 20 слов к словарю на 30000 токенов её выходной слой
      тоже приходится расширить.
  - question: >-
      Новая голова тональности получает скрытый вектор encoder и предсказывает
      один из нескольких классов. По какому правилу выбирают форму её матрицы
      весов?
    options:
      - hidden_size × num_classes
      - num_classes × sequence_length
      - vocab_size × hidden_size
      - num_classes × num_classes
    correct: 0
    explanation: >-
      Первая размерность должна принимать все координаты скрытого вектора, а
      вторая — создавать по одному логиту на класс. Поэтому для hidden_size 64 и
      двух классов нужна матрица 64 × 2.
---

После [шагов 30](#step-30)–[32](#step-32) ты должен уметь по одной фразе про задачу назвать длину выхода головы: у MLM это размер словаря, у тональности — число классов, и ничего кроме этих двух чисел тут не участвует.

Спотыкаются обычно на двух вещах. Первая: путают длину вектора с выхода encoder (те самые 64 числа на токен) с длиной выхода головы — это разные размеры, они стоят по разные стороны матрицы из [шага 19](#step-19). Вторая: забывают, что MLM-голова отвечает на каждую замаскированную позицию отдельно, а голова тональности — один раз на весь текст, потому что перед ней стоит пулинг.
