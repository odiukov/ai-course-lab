---
id: 031-zero-shot-check
type: check
title: 'Проверка: провести батч изображений через весь zero-shot конвейер'
source_anchor: '### Step 3: Zero-shot classifier'
check:
  - question: >-
      Ты подаёшь 100 изображений и 5 усреднённых текстовых представлений
      классов. Какой результат должен получиться после сравнения и выбора
      лучшего класса?
    options:
      - Матрица похожести из 5 строк и 100 столбцов и 5 предсказаний
      - Матрица похожести из 100 строк и 5 столбцов и 100 предсказаний
      - Матрица похожести из 100 строк и 100 столбцов и 5 предсказаний
    correct: 1
    explanation: >-
      Каждая строка относится к одному изображению, а каждый столбец — к одному
      классу. Выбор лучшего столбца в каждой строке даёт 100 предсказаний.
  - question: >-
      Что произойдёт, если в матрице из 100 строк и 5 столбцов выбрать максимум
      по строкам вместо выбора по классам для каждого изображения?
    options:
      - Получится по одному предсказанию для каждого изображения
      - Получится по одному изображению-победителю для каждого класса
      - Вероятности станут равны единице
      - Число классов автоматически увеличится до 100
    correct: 1
    explanation: >-
      Так ты сравнишь изображения внутри каждого класса и получишь 5
      победителей, а не класс для каждой из 100 картинок.
---

Теперь собери маршрут из [шага 21](#step-21) целиком: подготовь описания классов, усредни их представления, обработай батч изображений, сравни все пары и сопоставь лучший индекс имени класса, как в [шаге 30](#step-30). Главная ловушка здесь — перепутать ось выбора: одна строка матрицы должна превратиться ровно в одно предсказание для одной картинки.

> 🎒 **На пальцах.** Представь кинопрокат с сотней фильмов и пятью полками жанров. Для каждого фильма ты заполняешь строку из пяти оценок, а затем выбираешь лучшую полку именно в этой строке. Если искать лучший фильм в каждом столбце, получишь пять чемпионов жанров — но остальные фильмы останутся без места.
