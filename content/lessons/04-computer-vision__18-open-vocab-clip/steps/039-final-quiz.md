---
id: 039-final-quiz
type: quiz
title: 'Итоговая проверка: две башни, две функции потерь и zero-shot инференс'
source_anchor: '## Key Terms'
check:
  - question: >-
      Башня изображений выдала 32 embedding размерности 512, а текстовая башня —
      7 embedding той же размерности. Какую форму получит матрица похожести?
    options:
      - 32 строки на 7 столбцов
      - 32 строки на 512 столбцов
      - 7 строк на 512 столбцов
      - >-
        Матрицу построить нельзя, потому что количество изображений и текстов
        различается
    correct: 0
    explanation: >-
      Совпадать должна размерность embedding, а не число объектов. Каждая из 32
      картинок сравнивается с каждым из 7 текстов.
  - question: >-
      Ты добавил в батч ещё несколько текстов. Как это по-разному влияет на CLIP
      loss и SigLIP loss?
    options:
      - >-
        В CLIP новые тексты меняют конкурс внутри строки, а в SigLIP каждая
        новая пара оценивается независимо
      - 'В SigLIP новые тексты меняют softmax, а CLIP оценивает пары независимо'
      - Обе функции сравнивают только диагональные пары и игнорируют остальные
      - Обе функции требуют фиксированного размера батча
    correct: 0
    explanation: >-
      CLIP использует softmax-конкуренцию внутри батча. SigLIP задаёт каждой
      паре отдельный вопрос о соответствии, хотя затем усредняет полученные
      ошибки.
  - question: >-
      Модель не обучалась на классе «рысь». Что нужно сделать для zero-shot
      классификации с этим новым классом?
    options:
      - >-
        Добавить текстовый prompt про рысь, получить его embedding и сравнить с
        embedding изображения
      - Добавить новый выход в визуальную башню и переобучить её
      - Собрать размеченный батч рысей и пересчитать CLIP loss
      - Изменить размерность общего пространства embedding
    correct: 0
    explanation: >-
      В zero-shot набор кандидатов задаётся текстами на инференсе. Новый класс
      появляется как новый текстовый embedding без переобучения модели.
  - question: >-
      Ты увеличил положительный logit_scale перед softmax, не меняя embedding.
      Что произойдёт с zero-shot результатом?
    options:
      - >-
        Вероятности станут резче, но класс с максимальным score обычно не
        изменится
      - 'Вероятности станут равномернее, а лучший класс обязательно изменится'
      - Поменяется размерность embedding обеих башен
      - Текстовые prompts перестанут влиять на ответ
    correct: 0
    explanation: >-
      Положительный масштаб сохраняет порядок scores, но усиливает различия
      между ними. Поэтому softmax становится увереннее, а argmax остаётся тем
      же.
---

Дальше тебе пригодится не список терминов, а умение восстановить весь маршрут: согласовать выходы двух башен, выбрать подходящую функцию потерь и на инференсе превратить названия новых классов в сопоставимые текстовые embedding. Чаще всего путают две вещи: одинаковая размерность не означает одинаковое число картинок и текстов, а zero-shot не означает «модель ничего не учила» — она лишь не переобучается под твой новый список классов.

> 🎒 **На пальцах.** Представь редакцию журнала: фототека и отдел подписей работают отдельно, но пользуются одной системой тегов. CLIP устраивает конкурс подписей для каждого снимка, SigLIP проверяет каждую пару отдельно. Когда появляется рубрика «рыси», редакции не нужно переучивать сотрудников — достаточно подготовить новую подпись и найти ближайшие к ней фотографии.
