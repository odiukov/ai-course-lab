---
id: 023-check-forma
type: check
title: 'Проверка: сколько экспертов реально посчитали на один токен'
source_anchor: '### The FFN swap'
check:
  - question: >-
      В слое 256 экспертов, k = 8. Один токен проходит через MoE-слой. Сколько
      экспертных FFN будет выполнено и сколько оценок вычислит роутер?
    options:
      - 'Будет выполнено 8 FFN, а роутер вычислит 8 оценок'
      - 'Будет выполнено 8 FFN, а роутер вычислит 256 оценок'
      - 'Будут выполнены 256 FFN, но в итоговую сумму войдут только 8'
      - Будет выполнен только FFN эксперта с максимальной оценкой
    correct: 1
    explanation: >-
      Роутер вычисляет оценку для каждого из 256 экспертов, чтобы выбрать top-8.
      После выбора запускаются только 8 соответствующих FFN; остальные 248
      экспертных сетей этот токен не обрабатывают.
  - question: >-
      В батче 100 токенов используется MoE-слой с k = 2. Обязаны ли все токены
      пройти через одну и ту же пару экспертов?
    options:
      - 'Да, потому что top-k выбирается один раз для всего батча'
      - 'Нет, роутер выбирает пару отдельно для каждого токена'
      - 'Да, поскольку у всех токенов один и тот же набор экспертов'
      - >-
        Нет, причём пары экспертов у любых двух токенов обязательно должны
        различаться
    correct: 1
    explanation: >-
      Строка оценок и top-k вычисляются отдельно для каждого токена. Поэтому
      токены одного батча могут выбрать как одинаковые, так и разные пары
      экспертов.
---

К этому месту у тебя собран весь слой целиком — от `router_scores` до `moe_forward`. Проверим ровно одну вещь: что происходит с одним токеном за один проход.

Тут обычно спотыкаются на двух местах. Первое: роутер и эксперты считаются по-разному. Оценки нужны по всем E, иначе не из чего брать top-k, — но FFN запускаются только у k выбранных. Смешивать эти два числа нельзя, именно на их разнице и держится вся экономия из [шага 2](#step-2).

Второе: выбор идёт на уровне токена, а не батча. Соседние токены одного предложения могут уйти к совершенно разным экспертам.
