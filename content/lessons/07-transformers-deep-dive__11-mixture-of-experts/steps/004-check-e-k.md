---
id: 004-check-e-k
type: check
title: 'Проверка: что растёт с E, а что с k'
source_anchor: '## The Problem'
check:
  - question: >-
      Ты берёшь конфигурацию из 256 экспертов, где на токен работают 8, и
      увеличиваешь число экспертов до 512, оставив 8 активными. Что изменится?
    options:
      - >-
        Памяти под веса экспертов нужно вдвое больше, а вычислений экспертных
        FFN на токен — столько же
      - 'И память, и вычисления экспертных FFN на токен вырастут вдвое'
      - >-
        Память останется прежней, а вычисления экспертных FFN на токен вырастут
        вдвое
      - >-
        Ни память, ни вычисления не изменятся — важна только сумма числа
        доступных и активных экспертов
    correct: 0
    explanation: >-
      Хранить нужно веса всех 512 экспертов, поэтому память удваивается. Для
      каждого токена по-прежнему вычисляются только 8 экспертов, поэтому объём
      вычислений экспертных FFN не меняется.
  - question: >-
      Две MoE-модели имеют одинаковый размер одного эксперта: у первой 64
      эксперта и 8 активных на токен, у второй — 64 эксперта и 2 активных. Что
      верно?
    options:
      - Вторая требует вчетверо меньше памяти для весов экспертов
      - >-
        Обе требуют одинаковую память для весов экспертов, но у второй
        вычислений экспертных FFN на токен вчетверо меньше
      - 'У обеих одинаковы вычисления на токен, но вторая требует меньше памяти'
      - 'Первая дешевле и по памяти, и по вычислениям экспертных FFN'
    correct: 1
    explanation: >-
      Обе модели хранят по 64 одинаковых эксперта, поэтому требования к памяти
      совпадают. Отношение активных экспертов равно 8 к 2, значит у второй
      вычислительная работа экспертных FFN на токен вчетверо меньше.
---

После [шага 2](#step-2) у тебя есть два независимых рычага, и главное умение здесь — не путать, за что отвечает каждый. Крути число экспертов — растёт вес модели: место на диске, объём памяти, стоимость железа, на котором она вообще поместится. Крути число активных — растёт счёт за каждый обработанный токен: скорость ответа и цена генерации.

Спотыкаются обычно на двух вещах. Первая: кажется, что «671B параметров» автоматически означает «671B стоимость расчёта» — нет, платишь за 37B активных. Вторая: наоборот, кажется, что раз считает только 8 экспертов, то и хранить хватит восьми — а лежать в памяти должны все 256, роутер выбирает их заново для каждого токена.
