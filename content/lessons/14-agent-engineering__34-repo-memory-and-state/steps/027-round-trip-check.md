---
id: 027-round-trip-check
type: check
title: Почему успешной записи недостаточно без повторного чтения?
source_anchor: '## Build It'
check:
  - question: >-
      commit завершился без ошибки, а состояние в памяти выглядит верно. Что
      действительно докажет успешное сохранение?
    options:
      - Повторный вызов commit с теми же данными
      - Проверка размера файла на диске
      - 'Новая загрузка файла, валидация и сравнение с ожидаемым состоянием'
    correct: 2
    explanation: >-
      Только повторная загрузка проверяет именно сохранённый файл, а не объект,
      который всё ещё находится в памяти.
  - question: >-
      После записи файл загрузился и прошёл схему, но полученное состояние
      отличается от ожидаемого. Как оценить результат?
    options:
      - 'Круг проверки провален: сохранён не тот снимок'
      - 'Всё успешно, потому что схема приняла файл'
      - Достаточно записать файл ещё раз без сравнения
    correct: 0
    explanation: >-
      Валидация подтверждает допустимую форму, но сравнение проверяет,
      сохранился ли нужный снимок.
---

Ты изменил состояние, вызвал `commit` из [шага 24](#step-24) и не получил ошибки. Но пока перед тобой всё ещё объект из памяти — тот самый, который был правильным ещё до записи. Успех операции не доказывает, что следующий запуск прочитает именно эти данные.

Поэтому круг из [шага 26](#step-26) заканчивается новой загрузкой: файл разбирается, проверяется по схеме и сравнивается с ожидаемым снимком. Так обнаруживаются неверный путь, потерянное поле или запись не того состояния. Схема отвечает «допустимо ли?», сравнение — «то ли это?».

> 🎒 **На пальцах.** Фоторедактор показал «сохранено», но ты проверяешь результат, закрыв снимок и открыв файл заново. Только тогда видно, что сохранилась нужная версия, в нужном месте и в читаемом виде. Надпись подтверждает действие редактора; повторное открытие подтверждает результат, который переживёт закрытие программы.
