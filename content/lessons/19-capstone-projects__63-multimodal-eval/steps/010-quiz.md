---
id: 010-quiz
type: quiz
title: Проверяем весь контракт мультимодальной оценки
source_anchor: '## Tests'
check:
  - question: >-
      Матрица похожести единичная, а k равно 1. Какой результат должен дать
      двунаправленный Recall@K?
    options:
      - Ровно 0.0
      - Ровно 0.5
      - Ровно 1.0
      - Любое значение от 0.0 до 1.0
    correct: 2
    explanation: >-
      Правильная пара стоит первой в каждой строке и каждом столбце, поэтому оба
      направления дают полный recall.
  - question: >-
      Предсказания VQA содержат четыре идентификатора, а эталоны — три. Что
      должна сделать метрика?
    options:
      - Сравнить первые три пары
      - Считать отсутствующий эталон несовпадением
      - Отклонить вход из-за разных длин
      - Вернуть 0.0 без объяснения
    correct: 2
    explanation: >-
      Разные длины нарушают контракт попарного сравнения: нельзя молча отбросить
      или придумать пару.
  - question: >-
      Кандидат подписи не имеет ни одной общей n-граммы с эталонами. Какой
      BLEU-4 ожидается?
    options:
      - Ровно 0.0
      - Ровно 0.25
      - Ровно 0.5
      - Ровно 1.0
    correct: 0
    explanation: >-
      Без пересечений одна или несколько точностей n-грамм равны нулю, поэтому
      итоговая оценка тоже равна нулю.
  - question: >-
      Ты дважды строишь eval suite с одинаковыми n_samples и seed. Какое
      поведение подтверждает контракт?
    options:
      - 'Размеры списков могут отличаться, если подписи разные'
      - Получаются воспроизводимые данные и по n_samples записей каждого типа
      - Совпадает только список retrieval
      - Каждая подпись обязательно получает ровно три эталона
    correct: 1
    explanation: >-
      Одинаковый seed должен воспроизводить весь набор, а каждый из трёх списков
      обязан иметь заданный размер; эталонов подписи может быть до трёх.
---

Дальше ты будешь менять модели, но смысл их чисел останется надёжным только при неизменном контракте оценки. Здесь легко проверить счастливый пример и забыть границы: недопустимый `k`, разные длины VQA, пустую подпись или невоспроизводимый набор. Проверки из [шагов 3](#step-3), [5](#step-5), [7](#step-7) и [9](#step-9) вместе превращают три метрики и генератор данных в единый защитный контур. Если ломается хотя бы один крайний случай, сравнивать модели уже нельзя.

> 🎒 **На пальцах.** Представь пульт запуска спутника: связь, питание, камера и маршрут проверяются отдельно, но разрешение на старт появляется лишь после общей проверки. Один зелёный индикатор не компенсирует красный. Так и здесь: правильный BLEU-4 не спасает оценку, если Recall@K принимает невозможный `k` или набор данных меняется при том же seed.
