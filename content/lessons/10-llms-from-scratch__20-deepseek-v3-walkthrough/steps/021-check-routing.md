---
id: 021-check-routing
type: check
title: 'Проверка: 256 экспертов, top-8 — какая ровная загрузка'
source_anchor: '### The routing: auxiliary-loss-free load balancing'
check:
  - question: >-
      Почему при 256 экспертах и top-8 целевую загрузку нельзя считать как
      1/256?
    options:
      - >-
        Каждый токен выбирает восемь экспертов, поэтому ориентир задаётся
        отношением top-k к числу экспертов
      - Bias каждого эксперта удваивает число назначений
      - Доли загрузки всех экспертов должны суммироваться до числа экспертов
      - Целевая загрузка определяется числом параметров внутри эксперта
    correct: 0
    explanation: >-
      Каждый токен попадает сразу к восьми экспертам, поэтому сумма долей
      загрузки равна восьми. Равномерный ориентир составляет 8/256 ≈ 3.1%.
  - question: >-
      Загрузка эксперта 47 составила 6% при равномерном ориентире около 3%. Как
      отреагирует схема балансировки DeepSeek-V3?
    options:
      - Добавит в основной loss штраф за перекос и подберёт его вес
      - 'Уменьшит bias эксперта, чтобы он реже попадал в top-8'
      - Полностью запретит выбирать этого эксперта на следующем шаге
      - 'Увеличит bias эксперта, чтобы ускорить его разгрузку'
    correct: 1
    explanation: >-
      Перегруженному эксперту понижают bias. Его скорректированный логит
      становится меньше, поэтому он реже проходит в top-8 без добавления штрафа
      в основной loss.
---

Здесь нужно уметь две вещи. Первая — за десять секунд назвать ориентир ровной загрузки для конкретной конфигурации: 256 экспертов, восемь мест на токен, значит около 3% на эксперта. Вторая — по замеренной загрузке сказать, в какую сторону поедет bias, и не приплести туда штраф в loss из [шага 18](#step-18).

Спотыкаются обычно на первом. Рука сама пишет 1/256 — как будто токен выбирает одного эксперта. Но восьмёрка из [шага 14](#step-14) означает, что каждый токен раздаёт восемь голосов, и доли по всем экспертам в сумме дают не единицу, а восемь.

Проверь, что число 3% ты получаешь делением, а не памятью.
