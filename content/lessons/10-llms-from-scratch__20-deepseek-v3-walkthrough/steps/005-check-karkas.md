---
id: 005-check-karkas
type: check
title: 'Проверка: что здесь новое, а что старое'
source_anchor: '### The invariant core, again'
check:
  - question: >-
      Тебе дали список деталей DeepSeek-V3: SwiGLU в MLP, pre-norm, RoPE, MoE
      вместо обычного MLP. Какая из них не относится к знакомой основе?
    options:
      - SwiGLU в MLP
      - pre-norm
      - MoE вместо обычного MLP
      - RoPE
    correct: 2
    explanation: >-
      SwiGLU, pre-norm и RoPE входят в знакомый декодерный каркас, используемый
      в семействах Llama и Mistral. MoE меняет устройство MLP-части: вместо
      одной плотной сети токен направляется к выбранным экспертам.
  - question: >-
      Из 671 миллиарда параметров на каждый токен работают около 37 миллиардов.
      Что это меняет в схеме одного блока декодера?
    options:
      - >-
        Ничего: в блоке по-прежнему остаются attention, MLP-часть и две RMSNorm,
        но внутри MLP-части активируется только выбранная доля экспертов
      - >-
        Блок перестаёт быть авторегрессионным и обрабатывает всю
        последовательность одновременно
      - >-
        Из блока исчезает нормализация, поскольку неактивные параметры уже
        стабилизируют значения
      - >-
        Attention заменяется экспертами, поэтому отдельного механизма attention
        больше нет
    correct: 0
    explanation: >-
      Соотношение общего и активного числа параметров описывает разреженное
      включение экспертов, а не новый порядок подчастей блока. Базовый
      декодерный каркас сохраняется.
---

После этих четырёх экранов ты должен уметь одну конкретную вещь: взять любую деталь DeepSeek-V3 и быстро сказать, читал ли ты про неё ещё в главах про GPT-2 и Llama, или это то новое, что урок разберёт дальше. Шесть знакомых ручек из [шага 3](#step-3) и карта четырёх новых деталей из [шага 4](#step-4) — это ровно тот фильтр.

Спотыкаются обычно на двух местах. Первое: цифры 671B и 37B из [шага 2](#step-2) кажутся чем-то, что перестраивает всю модель, — а они про то, сколько параметров включается на токен. Второе: SwiGLU, pre-norm и RoPE иногда записывают в новинки просто потому, что названия звучат сложно.
