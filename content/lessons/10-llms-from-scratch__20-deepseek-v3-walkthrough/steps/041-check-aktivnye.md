---
id: 041-check-aktivnye
type: check
title: 'Проверка: откуда берутся 37B активных'
source_anchor: '### Parameter accounting'
check:
  - question: >-
      В сумме активных параметров attention даёт 8.8B — это все 144M на слой,
      умноженные на 61. Почему из attention ничего не вычитается, хотя из
      MLP-блока в работу идёт лишь малая часть?
    options:
      - >-
        Attention у DeepSeek-V3 меньше, чем MLP, поэтому его не имеет смысла
        экономить
      - >-
        Роутер выбирает эксперты только внутри MLP, а attention в каждом слое
        считается для токена целиком
      - 'MLA уже сжала attention один раз, и второй раз сжимать нечего'
      - 'Attention лежит в первых трёх плотных слоях, а там роутера нет'
    correct: 1
    explanation: >-
      Выбор из 256 экспертов происходит только в MLP-части блока. Attention
      никакого выбора не делает: все его 144M на слой участвуют в каждом прямом
      проходе, поэтому 61 × 144M входят в активные без вычетов.
  - question: >-
      Представь, что роутер стали бы выбирать не 8 экспертов, а 16, а всё
      остальное оставили бы как есть. Что изменится в двух числах — 671B лежит и
      37B работает?
    options:
      - 'Вырастут оба: и лежит, и работает'
      - 'Лежит останется тем же, а работает вырастет примерно на 15B за счёт MLP'
      - 'Оба останутся теми же, изменится только скорость'
      - 'Лежит вырастет вдвое, а работает не изменится'
    correct: 1
    explanation: >-
      Экспертов в модели по-прежнему 256, значит хранимый объём тот же. Но
      активный MLP на слой считается по числу включённых экспертов: было 9 (8
      routed + 1 shared) на ~260M, стало 17 — примерно вдвое больше на каждом из
      58 MoE-слоёв.
---

К этому моменту у тебя должно складываться в голове само сложение: 8.8B на attention по всем 61 слою, около 15.9B на активный MLP, ещё 1.2B на embedding и нормализации — это те самые ~26B ядра, к которым добавляется MTP-модуль и получается ≈37B.

Спотыкаются тут обычно на двух вещах. Первая — пытаются «сэкономить» и на attention: из [шага 39](#step-39) видно, что экономии там нет вовсе. Вторая — путают, какое из двух чисел двигает ручка top-k: она не меняет то, что лежит на диске, она меняет только то, что включается на каждый токен.
