---
id: 049-quiz
type: quiz
title: 'Итоговая проверка: MLA, роутинг без aux-loss, параметры и разреженность'
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты сравниваешь содержимое KV-кеша у MLA и GQA для одного токена одного
      слоя. Что хранит каждый механизм?
    options:
      - 'MLA хранит готовые K и V для каждой головы, а GQA — один общий латент'
      - 'MLA хранит общий KV-латент, а GQA — готовые K и V для групп голов'
      - Оба механизма хранят только исходное скрытое состояние токена
      - 'MLA не использует кеш, а заново обрабатывает весь контекст'
    correct: 1
    explanation: >-
      В MLA в кеш записывается общий сжатый латент размерности kv_lora_rank =
      512, из которого K и V восстанавливаются по требованию. GQA хранит готовые
      K и V для восьми групп: всего 2048 чисел против 512 у MLA.
  - question: >-
      Один routed-эксперт систематически получает больше токенов, чем остальные.
      Как auxiliary-loss-free routing корректирует этот перекос?
    options:
      - Добавляет к основной функции потерь штраф за неравномерную загрузку
      - >-
        Уменьшает bias перегруженного эксперта, снижая вероятность его попадания
        в top-k
      - Исключает перегруженного эксперта из модели до конца обучения
      - Обнуляет его веса и заново инициализирует роутер
    correct: 1
    explanation: >-
      Bias перегруженного эксперта уменьшают, а bias недогруженных повышают.
      Коррекция влияет на выбор экспертов, но не добавляет балансировочное
      слагаемое к loss языковой модели.
  - question: >-
      В MoE-модели увеличили количество routed-экспертов, но сохранили прежние
      top-k и число shared-экспертов. Как изменятся параметры экспертных MLP?
    options:
      - >-
        Общее число параметров вырастет, а основной объём активных expert-MLP
        параметров на токен останется прежним
      - 'Общее число параметров останется прежним, а активное число вырастет'
      - >-
        Общее и активное числа параметров обязательно вырастут в одинаковой
        пропорции
      - 'Ни общее, ни активное число параметров не изменится'
    correct: 0
    explanation: >-
      Веса всех routed-экспертов нужно хранить, поэтому общий размер растёт.
      Однако каждый токен по-прежнему проходит только через top-8
      routed-экспертов и один shared-эксперт, поэтому основной объём активных
      expert-MLP весов не меняется.
  - question: >-
      Какая часть DeepSeek-V3 создаёт основную разницу между общими 671B и
      активными 37B параметров?
    options:
      - Разреженный выбор восьми routed-экспертов из 256 в MoE-блоках
      - Пропуск большей части трансформерных слоёв для каждого токена
      - Хранение всех весов в FP8 вместо BF16
      - Случайное отключение части весов внутри каждого активного эксперта
    correct: 0
    explanation: >-
      Разреженность сосредоточена в экспертных MLP: все routed-эксперты входят в
      общий размер модели, но для токена выбираются только восемь из 256.
      Attention и shared-эксперт при этом выполняются для каждого токена.
---

Ты прошёл всю модель насквозь: посчитал KV cache для MLA и для GQA, собрал загрузку экспертов и подкрутку bias, сложил параметры по блокам и вывел активные 37B. Дальше в курсе это будет рабочим инструментом: почти любая новая MoE-модель описывается тем же набором полей конфига, и читать её ты будешь так же покомпонентно, как в [шаге 48](#step-48).

Где обычно спотыкаются: путают, что именно лежит в кеше при MLA (латент, а не готовые K и V); считают, что аux-loss-free балансировка всё-таки добавляет слагаемое в loss; и смешивают «лежит» с «работает» — увеличив число экспертов, ждут подорожания токена.

Проверь эти четыре места на себе.
