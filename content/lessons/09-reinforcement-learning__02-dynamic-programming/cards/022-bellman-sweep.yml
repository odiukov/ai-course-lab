- concept: синхронный проход Беллмана вычисляет все новые ценности из одного неизменного снимка V
  question: Какой фрагмент корректно выполняет синхронный проход Беллмана, не изменяя исходный словарь V?
  explanation: Независимая копия хранит результаты, а q_value получает исходный V. Поэтому все состояния обновляются по одному снимку ценностей. Простое присваивание создаёт псевдоним, а чтение из V_new смешивает старые и уже обновлённые значения.
  kind: choice
  options:
    - |-
      V_new = V
      for s in states():
          if s != TERMINAL:
              V_new[s] = max(q_value(V, s, a, gamma) for a in ACTIONS)
    - |-
      V_new = V.copy()
      for s in states():
          if s != TERMINAL:
              V_new[s] = max(q_value(V, s, a, gamma) for a in ACTIONS)
    - |-
      V_new = V.copy()
      for s in states():
          if s != TERMINAL:
              V_new[s] = max(q_value(V_new, s, a, gamma) for a in ACTIONS)
  correct: 1
  id: 022-bellman-sweep-1
  fingerprint: f9881bff
