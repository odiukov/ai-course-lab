---
id: 032-dr-check
type: check
title: 'Проверяем DR-план для AWQ-модели, vLLM и пяти LoRA-адаптеров'
source_anchor: '## Exercises'
check:
  - question: >-
      Какой DR-манифест позволяет воспроизвести AWQ-модель в vLLM вместе со
      всеми пятью LoRA-адаптерами?
    options:
      - 'Только шарды AWQ-весов, их индекс и контрольные суммы'
      - >-
        Шарды и индекс AWQ, конфигурация модели, токенизатор и конфигурация
        квантизации; зафиксированные образ, версию и параметры запуска vLLM;
        веса и adapter_config.json каждого LoRA; манифест развёртывания и
        лок-файл зависимостей
      - >-
        Файлы базовой модели, конфигурация одного LoRA и адрес действующего
        GPU-кластера
      - 'Контейнер vLLM, экспорт KV-кэша и журналы последних запросов'
    correct: 1
    explanation: >-
      Восстановлению подлежит весь воспроизводимый сервис: базовая AWQ-модель со
      служебными файлами, среда и параметры vLLM, каждый адаптер и конфигурация
      развёртывания.
  - question: >-
      После восстановления базовая AWQ-модель отвечает, но пятый LoRA не
      загружается: его файл весов есть, а adapter_config.json отсутствует. Как
      оценить результат?
    options:
      - 'Восстановление успешно, потому что базовая модель доступна'
      - 'Восстановление успешно, если остальные четыре LoRA работают'
      - 'Восстановление провалено: заявленный сервис восстановлен не полностью'
      - 'Результат зависит только от того, соблюдён ли SLO по TTFT'
    correct: 2
    explanation: >-
      Конфигурация определяет архитектуру и совместимость адаптера. Если хотя бы
      один заявленный LoRA нельзя загрузить, проверка полного сервиса не
      пройдена.
---

Здесь ты проверяешь не наличие папки с весами, а полноту цепочки восстановления. Опирайся на категории из [шага 28](#step-28) и собранный манифест из [шага 30](#step-30): AWQ-модель должна открываться правильным загрузчиком, vLLM — стартовать с зафиксированными параметрами, а каждый из пяти LoRA — иметь собственные веса и совместимую конфигурацию.

Частая ловушка — объявить учение успешным после первого ответа базовой модели. Но DR-план проходит проверку только тогда, когда развёрнут весь заявленный сервис и результат подтверждён запуском, как в [шаге 31](#step-31).
