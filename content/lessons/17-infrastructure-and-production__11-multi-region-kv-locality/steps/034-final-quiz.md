---
id: 034-final-quiz
type: quiz
title: >-
  Итоговая проверка: проектируем мультирегиональный LLM-сервинг с локальностью
  KV
source_anchor: '### Numbers you should remember'
check:
  - question: >-
      Локальная реплика даст промах с TTFT 800 мс, а удалённая — попадание с
      TTFT 80 мс и сетевой ценой 75 мс. Куда направить запрос, если
      резидентность разрешает оба региона?
    options:
      - 'В локальную реплику: любой межрегиональный маршрут медленнее'
      - >-
        В удалённую реплику: её полная ожидаемая цена около 155 мс, что ниже 800
        мс
      - 'В локальную реплику: стоимость сети нельзя складывать с TTFT'
      - 'Выбрать реплику случайно, чтобы равномерно распределить запросы'
    correct: 1
    explanation: >-
      Сравнивать нужно полную цену маршрута. В этом случае сетевые затраты не
      перекрывают экономию от попадания в KV-кэш.
  - question: >-
      В индексе найден горячий префикс в регионе, куда данные пользователя
      передавать нельзя. Что должен сделать роутер?
    options:
      - 'Использовать этот регион, если выигрыш по TTFT достаточно велик'
      - >-
        Исключить запрещённый регион и выбирать по кэшу и нагрузке только среди
        разрешённых реплик
      - Удалить найденный префикс из KV-кэша удалённой реплики
      - Игнорировать регион и выбрать GPU с самой короткой очередью
    correct: 1
    explanation: >-
      Резидентность задаёт жёсткую границу допустимых маршрутов.
      Производительность оптимизируется только внутри этой границы.
  - question: >-
      У двух стратегий маршрутизации близкий средний TTFT. Какие дополнительные
      метрики лучше всего раскроют различия между ними?
    options:
      - Размер модели и число реплик
      - 'Доля попаданий в кэш, перцентили TTFT и доля межрегионального трафика'
      - Только средняя загрузка GPU
      - Только количество событий добавления KV-блоков
    correct: 1
    explanation: >-
      Среднее значение может скрывать тяжёлый хвост и сетевую цену стратегии.
      Перцентили показывают распределение задержек, а hit rate и межрегиональный
      трафик объясняют его причины.
  - question: В резервном регионе сохранены только веса AWQ-модели. Какой вывод верен?
    options:
      - 'Этого достаточно: токенизатор и конфигурации выводятся из весов'
      - Этого достаточно при автоматическом переключении DNS
      - >-
        План неполон: нужны служебные файлы модели, конфигурация квантизации,
        среда сервинга, адаптеры, описание развёртывания и проверенный плейбук
      - Для полноты не хватает только сохранённого индекса KV-кэша
    correct: 2
    explanation: >-
      Веса не описывают токенизацию, квантизацию, параметры движка и
      инфраструктуру. Даже полный набор артефактов должен быть проверен
      практическим восстановлением.
---

Дальше тебе пригодится не отдельная функция, а порядок решений: сначала ограничения резидентности, затем полная цена маршрута, после — загрузка и устойчивость. Здесь легко спутать высокий hit rate с низким TTFT или принять автоматическое переключение региона за готовый DR. Сверяй решение с метриками из [шага 24](#step-24) и составом восстановления из [шага 30](#step-30).
