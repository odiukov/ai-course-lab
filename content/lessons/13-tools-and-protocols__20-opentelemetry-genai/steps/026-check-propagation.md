---
id: 026-check-propagation
type: check
title: 'Проверка: два трейса вместо одного'
source_anchor: '### Propagation across MCP'
check:
  - question: >-
      MCP-клиент вызывает сервер по stdio и не прокидывает traceparent. Что ты
      увидишь в бэкенде?
    options:
      - 'Один трейс, в котором спаны сервера просто придут с задержкой'
      - >-
        Два независимых трейса с разными trace id — клиентский и серверный,
        связать их можно только по времени, на глазок
      - 'Один трейс, потому что trace id восстановится по имени сервера'
      - 'Спаны сервера вообще не появятся, пока нет traceparent'
    correct: 1
    explanation: >-
      Без переданного контекста сервер начинает свой корневой спан и генерирует
      новый trace id. Получаются два трейса, и сопоставлять их приходится
      вручную по времени.
  - question: Транспорт — stdio. Куда класть traceparent сегодня?
    options:
      - 'В HTTP-заголовок запроса, как при streamable HTTP'
      - 'Никуда: по stdio контекст пробросить нельзя в принципе'
      - Руками в поле _meta каждого JSON-RPC запроса
      - 'В имя метода JSON-RPC, через дефис после названия'
    correct: 2
    explanation: >-
      Stdio заголовков HTTP не несёт вовсе, поэтому traceparent кладут в _meta
      запроса руками — поле _meta.traceparent в спецификации только обсуждается
      в роадмапе на 2026 год.
---

После этого куска ты должен уметь глядя на пару «клиент — MCP-сервер» сказать, где именно потеряется trace id и куда его положить на этом транспорте: заголовок при streamable HTTP, `_meta` руками при stdio.

Спотыкаются обычно на двух вещах. Первая — кажется, что если оба сервиса шлют спаны в один бэкенд, они сами склеятся в один трейс. Не склеятся: без переданного контекста сервер заводит новый trace id, и у тебя два дерева вместо одного. Вторая — `_meta.traceparent` воспринимают как готовый стандарт. Пока это обсуждение в роадмапе, а класть поле нужно самому, в каждый запрос.
