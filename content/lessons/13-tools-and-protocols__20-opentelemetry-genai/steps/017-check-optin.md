---
id: 017-check-optin
type: check
title: 'Проверка: где можно включать захват'
source_anchor: '### Opt-in content capture'
check:
  - question: >-
      Ты собираешься включить захват промптов и ответов модели. В каком случае
      это решение выглядит нормально?
    options:
      - 'В проде, потому что именно там встречаются реальные баги'
      - 'На стейджинге, на синтетических данных, которые ты придумал сам'
      - 'В проде, но только для одного пользователя из десяти'
      - 'Везде сразу: спаны и так уходят в закрытый контур команды'
    correct: 1
    explanation: >-
      Стейджинг с синтетикой даёт тебе тексты промптов без риска: утекать
      нечему. В проде содержимое включают только после явного разрешения
      юристов.
  - question: >-
      Захват контента выключен. Что ты всё равно увидишь в спане обращения к
      модели?
    options:
      - 'Ничего: пустой спан без атрибутов'
      - Только имя спана и больше ничего
      - 'Длительность, статус, имя операции, провайдера, счётчики токенов'
      - 'Промпт целиком, но с вырезанными персональными данными'
    correct: 2
    explanation: >-
      Дефолт убирает только содержимое — сам промпт и ответ. Тайминги, статус,
      gen_ai.operation.name, gen_ai.provider.name и токены остаются на месте.
---

После [шага 16](#step-16) ты должен уметь отвечать на два разных вопроса, не путая их. Первый: что вообще исчезает из спана, когда захват контента выключен, — и что остаётся (а остаётся почти всё, из-за чего мы этот спан и заводили). Второй: где включать захват можно, а где сначала нужен чей-то «да».

Обычно спотыкаются на первом. «Контент выключен» читают как «данных нет» и решают, что без промптов трейс бесполезен. А ещё путают опасность: страшен не сам факт логирования, а то, что текст пользователя оседает в системе, где он лежит недели и виден всей команде.
