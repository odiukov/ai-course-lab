---
id: 011-check-funnel
type: check
title: 'Проверка: 196 патчей и 900 патчей на выходе'
source_anchor: '### Perceiver resampler'
check:
  - question: >-
      Если изображение становится более детализированным и ViT выдаёт больше
      patch-tokens, что происходит с числом визуальных токенов на выходе
      resampler?
    options:
      - Оно остаётся фиксированным
      - Оно увеличивается ровно на число новых патчей
      - Оно становится равным числу патчей после softmax
    correct: 0
    explanation: >-
      Размер выхода задаётся числом обучаемых латентов, выступающих в роли
      запросов. Дополнительные патчи меняют содержимое агрегированных
      представлений, но не количество выходных строк.
  - question: >-
      Почему resampler может принять изображение с другим числом patch-tokens и
      сохранить совместимую форму выхода?
    options:
      - Потому что cross-attention строит по одному выходу на каждый patch-token
      - >-
        Потому что patch-tokens используются как K и V, а число выходов задают
        латенты в Q
      - Потому что resampler отбрасывает все патчи после фиксированного порога
    correct: 1
    explanation: >-
      В cross-attention длина результата определяется количеством запросов Q.
      Патчи участвуют в вычислении весов и содержимого, но не определяют число
      результирующих латентов.
---

После этого куска ты должен уметь ответить на один вопрос, не заглядывая в код: сколько токенов выйдет из resampler, если размер картинки поменять. И объяснить, откуда это число берётся — из латентов, а не из патчей.

Спотыкаются обычно в двух местах. Первое: кажется, что «сжатие» означает потерю патчей — будто лишние выбрасываются. Нет, все 900 прочитаны, просто результат чтения уложен в 64 строки. Второе: путают, какая сторона attention задаёт длину выхода. Проверь себя по [шагу 7](#step-7) и по функции из [шага 8](#step-8): форма результата всегда наследуется от Q.
