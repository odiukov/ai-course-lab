---
id: 027-check-who-sees-what
type: check
title: 'Проверка: что видит «подпись B»'
source_anchor: '### Masked cross-attention for interleaved inputs'
check:
  - question: >-
      Промпт: картинка A, подпись A, картинка B, подпись B, картинка C, вопрос.
      На чьи визуальные tokens смотрят tokens «подписи B» в gated
      cross-attention?
    options:
      - Только на визуальные tokens картинки B
      - На визуальные tokens картинок A и B — все предшествующие
      - 'На визуальные tokens всех трёх картинок, включая C'
      - 'Ни на какие: подпись B стоит после картинки, значит маска её закрывает'
    correct: 0
    explanation: >-
      Flamingo выбрал вариант «только ближайшая предшествующая картинка»: для
      позиций подписи B ближайшая слева — B, поэтому A и C замаскированы.
  - question: >-
      Какую возможность теряет модель из-за правила «каждая текстовая позиция
      видит только ближайшую предшествующую картинку»?
    options:
      - Она не может обработать больше одной картинки в промпте
      - Ни один текстовый token не видит визуальную информацию вообще
      - Она не может напрямую сопоставить картинки A и C в одном cross-attention
      - Порядок картинок в промпте перестаёт что-либо значить
    correct: 2
    explanation: >-
      Каждая текстовая позиция получает только один визуальный источник, поэтому
      cross-attention не может одновременно обратиться к A и C.
---

Разберись на этом конкретном промпте: картинка A, подпись A, картинка B, подпись B, картинка C, вопрос. Ты должен уметь для любой текстовой позиции сказать, чьи 64 латента она видит, — и заметить, что «подпись B» получает только B, хотя A стоит раньше в последовательности.

Спотыкаются обычно на двух местах. Первое: путают правило из [шага 22](#step-22) с «видит все предшествующие» — оба варианта работали, Flamingo взял первый. Второе: решают, что картинка A для модели пропала совсем. Не пропала — маска ограничивает только cross-attention; сам текст подписи A остаётся в потоке LLM и доступен дальше через обычное self-attention.
