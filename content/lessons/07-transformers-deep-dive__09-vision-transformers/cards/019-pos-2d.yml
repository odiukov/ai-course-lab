- concept: частное и остаток от деления линейного индекса на ширину сетки дают строку и столбец
  question: Заполни пропуск, чтобы получить координаты элемента последовательности в растровом порядке.
  explanation: '`divmod(index, width)` возвращает частное и остаток. Частное задаёт номер строки, а остаток — номер столбца.'
  kind: cloze
  template: row, col = ___(index, width)
  answer: divmod
  accept:
    - divmod
    - builtins.divmod
  id: 019-pos-2d-1
  fingerprint: 312bd97f
- concept: координаты токенов должны образовывать биекцию со всеми ячейками сетки
  question: Функция перечисляет координаты всех ячеек квадратной сетки ширины `width`. Как проверить, что она не пропустила ячейки и не создала дубликаты?
  explanation: Совпадение множеств подтверждает наличие каждой допустимой пары координат, а ожидаемая длина списка исключает повторения.
  kind: open
  reference: Сравнить множество результата с декартовым произведением `range(width) × range(width)` и проверить, что длина списка равна `width * width`.
  id: 019-pos-2d-2
  fingerprint: 20fe1a14
