---
id: 038-itog
type: quiz
title: Итоговая проверка урока
source_anchor: '## Exercises'
check:
  - question: >-
      Цветное изображение размером 35 на 25 пикселей разбивают без перекрытий на
      патчи 5 на 5. Сколько получится патчей и какова длина плоского вектора
      каждого патча?
    options:
      - 35 патчей по 75 чисел
      - 25 патчей по 105 чисел
      - 175 патчей по 25 чисел
      - 7 патчей по 15 чисел
    correct: 0
    explanation: >-
      Сетка имеет размер 35/5 на 25/5, поэтому получается 7 · 5 = 35 патчей. В
      цветном патче 5 · 5 · 3 = 75 значений.
  - question: >-
      При неизменном изображении сторону квадратного патча уменьшили втрое. Как
      изменятся число патч-токенов и число рассматриваемых пар токенов в полном
      self-attention?
    options:
      - 'Токенов станет втрое больше, пар — в девять раз больше'
      - 'Токенов станет в девять раз больше, пар — в восемьдесят один раз больше'
      - 'Токенов станет в девять раз больше, пар — в девять раз больше'
      - 'Токенов станет в восемьдесят один раз больше, пар — в девять раз больше'
    correct: 1
    explanation: >-
      Число патчей обратно пропорционально площади патча: уменьшение обеих
      сторон втрое даёт девятикратный рост числа токенов. Полный attention
      рассматривает квадратичное число пар, поэтому рост составляет 9² = 81.
  - question: >-
      После обучения классификатора на небольшой выборке ты добавил
      предварительное восстановление представлений замаскированных патчей. Как
      проверить, помогло ли это предобучение?
    options:
      - >-
        Сравнить тестовую точность одинаковых классификаторов с предобучением и
        без него при прочих равных условиях
      - Сравнить только финальные значения функции потерь на этапе предобучения
      - 'Проверить, стала ли модель содержать больше обучаемых параметров'
      - Сравнить время одного прямого прохода до и после предобучения
    correct: 0
    explanation: >-
      В упражнении проверяется влияние предобучения на итоговую классификацию.
      Поэтому архитектуру, данные и протокол оценки фиксируют, а затем
      сравнивают тестовую точность двух вариантов.
  - question: >-
      Два патча находятся в одной строке сетки, но в разных столбцах. Что верно
      для их двумерных синусоидальных позиционных эмбеддингов, построенных
      склейкой независимых кодов?
    options:
      - >-
        Части, кодирующие строку, совпадут, а части, кодирующие столбец, будут
        различаться
      - >-
        Части, кодирующие столбец, совпадут, а части, кодирующие строку, будут
        различаться
      - Оба позиционных эмбеддинга обязательно совпадут
      - 'Оба эмбеддинга будут нулевыми, потому что патчи лежат в одной строке'
    correct: 0
    explanation: >-
      Одинаковая координата строки даёт одинаковый row-код. Разные координаты
      столбца дают разные col-коды, поэтому после склейки эмбеддинги различаются
      только столбцовой частью.
---

Урок держится на одной цепочке чисел, и её стоит уметь прогонять в голове: размер картинки делится на размер патча, даёт сетку, сетка даёт число токенов, а размер патча в квадрате на число каналов — длину одного вектора. Дальше всё уже знакомо из [шага 24](#step-24): обычный энкодер, никаких правок.

Спотыкаются обычно в двух местах. Первое — каналы: у цветной картинки их три, у MNIST один, и 16 вместо 48 чисел ломает не модель, а арифметику в голове. Второе — рост стоимости: половинный патч даёт вчетверо больше токенов, а пар в attention — в шестнадцать раз ([шаг 8](#step-8)).

Дальше в курсе ты будешь брать готовый ViT-бэкбон и пристраивать к нему свою голову. Там эти формы придётся стыковать руками.
