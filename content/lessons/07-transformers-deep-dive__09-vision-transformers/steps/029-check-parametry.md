---
id: 029-check-parametry
type: check
title: 'Проверка: во сколько раз ViT-Base тяжелее ResNet-50'
source_anchor: '### Step 4: count parameters for a realistic ViT'
check:
  - question: >-
      Как правильно вычислить, во сколько раз ViT-Base тяжелее ResNet-50 по
      числу параметров?
    options:
      - Разделить число параметров ViT-Base на число параметров ResNet-50
      - Вычесть число параметров ResNet-50 из числа параметров ViT-Base
      - Сложить числа параметров обеих моделей
      - Разделить число параметров ResNet-50 на число параметров ViT-Base
    correct: 0
    explanation: >-
      Кратность находят отношением большего значения к меньшему: 86 / 25 ≈ 3,44.
      Поэтому ViT-Base примерно втрое тяжелее ResNet-50.
  - question: >-
      Почему при переходе от ViT-Base к ViT-Large нельзя ожидать, что точность
      вырастет пропорционально числу параметров?
    options:
      - >-
        Число параметров и точность — разные величины, а увеличение ёмкости
        модели обычно даёт убывающую отдачу
      - Дополнительные параметры автоматически отключаются во время инференса
      - Рост числа параметров обязательно уменьшает число классов
      - Точность любой нейросети не зависит от её размера
    correct: 0
    explanation: >-
      При переходе от 86M к 307M число параметров растёт кратно, но прирост
      точности обычно измеряется лишь единицами процентов. Большая ёмкость не
      превращается в пропорциональный рост качества.
---

После [шага 28](#step-28) у тебя на руках четыре числа: 25, 86, 307, 632 миллиона. Проверь, что ты умеешь их читать, а не просто помнишь порядок.

Первое: поделить 86 на 25 и назвать множитель вслух. Второе: не перепутать, за что именно ты платишь — параметры ViT растут не от количества патчей, а от глубины и ширины энкодера, который считает их из [шага 27](#step-27).

Обычно спотыкаются на третьем: кажется, что раз модель втрое тяжелее, то и качество должно прыгнуть втрое. Посмотри на ряд 86 → 307 → 632 ещё раз и спроси себя, что там на самом деле растёт кратно, а что — на единицы процентов.
