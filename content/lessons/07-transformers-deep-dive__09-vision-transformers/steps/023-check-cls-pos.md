---
id: 023-check-cls-pos
type: check
title: 'Проверка: форма после приписывания `[CLS]`'
source_anchor: '### Step 3 — prepend `[CLS]` token, add positional embeddings'
check:
  - question: >-
      У тебя 196 патчей, каждый спроецирован в 768 чисел. Ты приписал в начало
      CLS-токен. Какая форма получилась?
    options:
      - 197 строк по 768 чисел
      - 196 строк по 769 чисел
      - 197 строк по 769 чисел
      - '196 строк по 768 чисел, а CLS лежит отдельно'
    correct: 0
    explanation: >-
      CLS — это ещё один токен той же ширины, поэтому растёт число строк (196
      плюс 1), а длина каждой строки остаётся 768.
  - question: >-
      Таблицу позиций собрали на 196 строк, а последовательность уже 197. Что
      произойдёт при сложении?
    options:
      - 'Сложение упадёт по формам: строк не поровну'
      - 'Всё сложится, CLS просто получит нулевую позицию'
      - 'Всё сложится, последний патч останется без позиции'
      - 'Сложатся первые 196 строк, а CLS добавится позже сам'
    correct: 0
    explanation: >-
      Позиции прибавляются построчно, строка к строке. 197 и 196 не состыкуются,
      и код остановится прямо здесь.
---

Здесь проверяем одно: умеешь ли ты держать в голове две цифры формы одновременно — сколько токенов и какой они ширины — и понимать, какая из них меняется, когда ты что-то приписываешь.

Спотыкаются обычно в двух местах. Первое: путают, куда именно добавился `[CLS]`. Он не удлиняет вектор до 769 — он становится отдельной строкой, поэтому меняется только счётчик токенов, как в [шаге 20](#step-20). Второе: собирают таблицу позиций по числу патчей, забыв, что секретарь из [шага 17](#step-17) тоже сидит в последовательности и тоже нуждается в своей строке позиции.

Обе ошибки видно сразу — код падает на сложении, а не через десять слоёв.
