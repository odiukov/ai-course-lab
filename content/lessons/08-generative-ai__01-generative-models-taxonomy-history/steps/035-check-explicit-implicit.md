---
id: 035-check-explicit-implicit
type: check
title: Объясните расхождение гистограммы и KDE и пустой ответ генератора
source_anchor: '## Build It'
check:
  - question: >-
      Почему KDE оценивает вероятность ложбины между модами немного выше, чем
      гистограмма?
    options:
      - >-
        Гауссовы ядра сглаживают распределение и переносят в ложбину часть массы
        соседних мод
      - KDE создаёт дополнительные наблюдения и учитывает их как реальные данные
      - Гистограмма по определению занижает вероятность каждого интервала
    correct: 0
    explanation: >-
      В примере KDE даёт 0.033 против 0.025 у гистограммы: ядра шириной 0.3
      имеют хвосты, которые вносят в ложбину часть массы из областей около мод.
      Это нормальное расхождение двух способов оценки, а не поломка.
  - question: >-
      Генератор вывел образцы, но не вернул вероятность заданного интервала. Как
      правильно интерпретировать отсутствие этого числа?
    options:
      - 'Генерация завершилась ошибкой, поэтому результат нельзя использовать'
      - Вероятность попасть в интервал обязательно равна нулю
      - >-
        Генератор задаёт способ получать образцы, но не предоставляет вычислимую
        функцию плотности
    correct: 2
    explanation: >-
      Неявный генератор умеет создавать новые образцы, однако не даёт функции
      плотности, которую можно проинтегрировать по интервалу. Поэтому пустой
      ответ означает недоступность такой оценки, а не нулевую вероятность или
      сбой.
---

Сопоставь результаты из [шага 34](#step-34): расхождение 0.025 и 0.033 не означает, что один метод сломан. Гистограмма зависит от границ корзин, а KDE из [шага 30](#step-30) сглаживает данные и затягивает немного массы в ложбину. Тебе важно отличить такую разницу оценок от третьего случая: пустое место у генератора — не нулевая вероятность и не сбой. Он выдаёт образцы, но у него просто нет функции плотности, которую можно проинтегрировать.

> 🎒 **На пальцах.** Представь два фонаря по краям тёмного двора. Гистограмма размечает двор квадратами и считает, сколько людей стоит в каждом. KDE заменяет каждого человека маленьким световым пятном, поэтому центр слегка подсвечивается. Генератор же приводит новых людей, но не умеет ответить, насколько освещён выбранный квадрат.
