---
id: 025-check-raskladka
type: check
title: 'Проверка: угадай форму после transpose'
source_anchor: '### Two layout conventions: HWC and CHW'
check:
  - question: >-
      Массив с формой (64, 96, 3) прогнали через transpose(2, 0, 1). Какая форма
      получится?
    options:
      - '(3, 64, 96)'
      - '(96, 64, 3)'
      - '(64, 3, 96)'
      - '(3, 96, 64)'
    correct: 0
    explanation: >-
      Аргументы (2, 0, 1) значат: на первое место встаёт бывшая ось номер 2 (это
      3), потом бывшая ось 0 (это 64), потом бывшая ось 1 (это 96).
  - question: >-
      Тензор уже в CHW и имеет форму (3, 200, 200). Его по ошибке ещё раз
      прогнали через transpose(2, 0, 1). Что произойдёт?
    options:
      - 'Ошибка: такую перестановку нельзя применить дважды'
      - 'Ничего не изменится, форма останется (3, 200, 200)'
      - >-
        Получится (200, 3, 200) — форма выглядит правдоподобно, но каналы уехали
        не туда
      - 'Тензор вернётся в HWC, то есть (200, 200, 3)'
    correct: 2
    explanation: >-
      Перестановка отработает молча и даст (200, 3, 200): числа те же, но ось
      каналов оказалась посередине, и дальше всё поедет вкривь.
---

После этого куска ты должен уметь смотреть на форму и на аргументы `transpose` — и называть результат, не запуская код. Это базовая проверка, которую делаешь перед тем, как отдать картинку модели: если форма не та, ошибка вылезет глубоко внутри сети, где её тяжело разбирать.

Спотыкаются обычно на двух вещах. Первая: аргументы `transpose` читают как «куда переехала ось», хотя это «откуда её взять». Число `2` на первом месте значит «на первое место поставь то, что раньше было осью номер 2». Вторая: квадратные картинки. Если высота и ширина совпали, любая путаница между ними невидима, форма выглядит правильной — и ошибка всплывёт позже, на прямоугольном изображении.
