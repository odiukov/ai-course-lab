---
id: 015-check-kanaly
type: check
title: 'Проверка: каналы и форма'
source_anchor: '### Why three channels'
check:
  - question: >-
      Почему при оценке объёма памяти RGB-изображения недостаточно перемножить
      только высоту и ширину?
    options:
      - >-
        Потому что для каждой пространственной точки отдельно хранятся значения
        всех цветовых каналов
      - >-
        Потому что высоту и ширину перед умножением всегда нужно возвести в
        квадрат
      - Потому что каждый пиксель обязательно занимает один килобайт
      - Потому что к числу пикселей нужно прибавить разрешение экрана
    correct: 0
    explanation: >-
      Произведение высоты и ширины даёт число пространственных точек. В RGB
      каждой точке соответствуют три значения, поэтому полный размер массива
      равен H × W × 3.
  - question: >-
      Почему массив формы 512 × 512 × 1 не обязательно означает, что изображение
      повреждено?
    options:
      - >-
        Один канал может хранить единственное измерение интенсивности в каждой
        точке, как в grayscale или рентгеновском снимке
      - 'Последняя единица означает, что ширина изображения неизвестна'
      - >-
        Любая библиотека автоматически восстанавливает два отсутствующих
        цветовых канала
      - 'Число каналов не связано с тем, какие данные хранит изображение'
    correct: 0
    explanation: >-
      Последняя ось обозначает число каналов. Одноканальные изображения
      нормальны: в каждой точке они хранят одно значение интенсивности вместо
      RGB-триплета.
---

Форма массива — первое, что ты будешь проверять каждый раз, когда картинка попадает в код. Если модель ждёт три канала, а ты дал один, она не «догадается» — она упадёт с ошибкой. Поэтому уметь читать тройку чисел `(H, W, 3)` важнее, чем помнить, как называются каналы.

Тебе нужно уметь две вещи: по форме сказать, сколько чисел внутри (перемножить все три), и по последнему числу сказать, сколько «мнений о свете» хранится в каждой точке.

Спотыкаются обычно на двух местах. Первое: забывают умножить на число каналов и считают только пиксели — получается втрое меньше. Второе: думают, что каналов всегда три. Один канал (рентген, чёрно-белое фото) — такая же нормальная картинка, просто в каждой точке одно число вместо трёх.

> 🎒 **На пальцах.** Представь склад стеллажей: H полок, на каждой W ячеек, в каждой ячейке лежит по 3 бумажки. Форма `(H, W, 3)` — это адресная система склада, а не количество бумаг. Чтобы узнать, сколько бумажек всего, перемножь всё: полки на ячейки на бумажки в ячейке. Бывает склад, где в ячейке одна бумажка, а бывает — где сто.
