---
id: 034-check-standardize
type: check
title: 'Проверка: roundtrip и нулевое среднее'
source_anchor: '### Step 4: Normalize, standardize, and reverse it'
check:
  - question: >-
      После preprocess_imagenet все значения остались в диапазоне [0, 1] и
      совпали с результатом rgb_uint8.astype(np.float32) / 255.0. Какой этап
      предобработки, вероятнее всего, пропущен?
    options:
      - Перестановка осей из HWC в CHW
      - Стандартизация (x - mean) / std
      - Преобразование uint8 в float32
      - Ограничение значений с помощью clip
    correct: 1
    explanation: >-
      Деление на 255 переводит интенсивности в диапазон [0, 1]. Если после
      preprocess значения не изменились дальше, значит, скорее всего, не
      выполнены вычитание mean и деление на std.
  - question: >-
      В ошибочной deprocess_imagenet после перехода из CHW в HWC снова
      выполняется (x - mean) / std. Чем нужно заменить эту операцию, чтобы
      обратить стандартизацию?
    options:
      - x / std - mean
      - x * std + mean
      - x + std * mean
      - (x + mean) / std
    correct: 1
    explanation: >-
      Прямое преобразование y = (x - mean) / std обращается формулой x = y * std
      + mean. После этого можно вернуть масштаб пикселей умножением на 255.
---

Эта проверка нужна, чтобы ты умел ловить ошибку предобработки до того, как она превратится в загадочно плохие предсказания модели. Модель не ругается на неправильно подготовленный вход — она просто молча выдаёт мусор, и найти причину потом тяжело.

Поэтому у тебя есть две дешёвые проверки. Первая: после preprocess среднее по каждому каналу близко к нулю, разброс — близок к единице. Вторая: preprocess и deprocess подряд возвращают исходную картинку с разницей 0 или 1 в пикселе.

Спотыкаются обычно на порядке шагов: обратная функция должна повторять прямую задом наперёд, а не в том же порядке. И на том, что «близко к нулю» — это не ровно ноль: конкретная картинка почти никогда не совпадает со средним по всему ImageNet.
