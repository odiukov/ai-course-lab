- concept: NumPy сопоставляет оси при broadcasting справа налево, не учитывая их смысл
  question: Тензор изображения имеет форму (4, 96, 128), а вектор средних по каналам — форму (4,). Что произойдёт при вычислении `x - mean`?
  explanation: Broadcasting сравнивает формы с последних осей. В CHW последней осью является ширина, поэтому вектор каналов с ней не совмещается. Назначение осей NumPy не учитывает.
  kind: choice
  options:
    - 'NumPy выдаст ошибку: крайние справа размеры 128 и 4 несовместимы'
    - Каждое среднее автоматически вычтется из соответствующего канала
    - Средние вычтутся только из первых четырёх столбцов
    - Результат получит форму (4, 96, 4)
  correct: 0
  id: 029-poryadok-preprocess-1
  fingerprint: 865e2a4c
- concept: Добавление singleton-осей позволяет применить вектор статистик по каналам к тензору CHW
  question: Тензор `x` хранится в формате CHW, а `channel_mean` имеет форму `(C,)`. Заполни пропуск так, чтобы среднее каждого канала вычиталось из всех его пространственных позиций.
  explanation: Индексирование `channel_mean[:, None, None]` добавляет после канальной оси две singleton-оси. Они растягиваются по высоте и ширине, поэтому отдельное среднее применяется ко всей плоскости соответствующего канала.
  kind: cloze
  template: centered = x - channel_mean[___]
  answer: :, None, None
  accept:
    - :, np.newaxis, np.newaxis
  id: 029-poryadok-preprocess-2
  fingerprint: 0d7d128c
