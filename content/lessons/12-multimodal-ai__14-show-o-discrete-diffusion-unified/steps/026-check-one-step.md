---
id: 026-check-one-step
type: check
title: 'Проверка: почему не размаскировать всё за один шаг'
source_anchor: '## Exercises'
check:
  - question: >-
      Ты запускаешь masked diffusion sampler с одним шагом. Почему итоговая
      картинка может оказаться несогласованной?
    options:
      - Модель не успевает вычислить распределения для всех позиций
      - >-
        Все позиции выбираются без возможности использовать выборы соседей как
        контекст
      - Косинусное расписание обязательно выдаёт отрицательное число токенов
      - Дискретный словарь перестаёт использоваться
    correct: 1
    explanation: >-
      За один проход все замаскированные позиции предсказываются на одном и том
      же неполном контексте. Поэтому их выборы не могут последовательно
      согласовать детали изображения.
  - question: >-
      Зачем часть предсказаний фиксируют, а остальные оставляют замаскированными
      до следующего прохода?
    options:
      - Чтобы уменьшить размер словаря
      - Чтобы зафиксированные токены стали контекстом для предсказания остальных
      - Чтобы модель дообучилась во время генерации
      - Чтобы логиты не нужно было нормализовать
    correct: 1
    explanation: >-
      После фиксации часть изображения становится известной модели. Следующий
      проход использует эти токены при предсказании оставшихся позиций, что
      повышает согласованность результата.
---

После [шага 22](#step-22) и [шага 24](#step-24) у тебя есть весь механизм, но главный вопрос про него ещё не задан: а зачем вообще шестнадцать проходов, если модель и на первом выдаёт распределение для всех 1024 позиций? Здесь надо уметь ответить не «так принято», а через независимость: один проход даёт 1024 отдельных мнения, каждое построено на одном и том же наборе масок, и ни одно не знает про остальные.

Спотыкаются обычно на двух вещах. Первая — путают «модель предсказала все токены» с «модель предсказала согласованную картинку»: предсказывает она действительно всё сразу, а согласованность приходит только от того, что часть выборов становится контекстом для следующих. Вторая — думают, что многошаговость нужна для точности каждого отдельного токена. Точность тут вторична; шаги покупают именно взаимное согласие соседей.
