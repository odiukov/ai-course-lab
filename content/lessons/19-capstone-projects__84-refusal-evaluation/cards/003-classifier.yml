- concept: классификатор отказа ищет маркер во всём ответе и преобразует результат поиска в bool
  question: Заполни тело классификатора, если `_REFUSAL_RE` уже содержит шаблоны фраз отказа.
  explanation: '`search` ищет совпадение в любой части ответа и возвращает объект совпадения либо `None`. Вызов `bool` приводит этот результат к обещанному типу `True` или `False`.'
  kind: cloze
  template: |-
    def classify_refusal(response: str) -> bool:
        return ___
  answer: bool(_REFUSAL_RE.search(response))
  accept:
    - bool(_REFUSAL_RE.search(response))
  id: 003-classifier-1
  fingerprint: 85b4bebf
