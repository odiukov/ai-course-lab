---
id: 010-kontekst-kv
type: check
title: 'Почему удвоение контекста удваивает KV-кэш, но не веса?'
source_anchor: '### Why FP8 is still the floor for KV cache'
check:
  - question: >-
      Какая часть памяти модели зависит от числа уже обработанных токенов, а
      какая остаётся фиксированной?
    options:
      - 'KV-кэш растёт с числом токенов, а веса остаются фиксированными'
      - 'Веса растут с числом токенов, а KV-кэш остаётся фиксированным'
      - 'И веса, и KV-кэш являются фиксированными'
      - 'И веса, и KV-кэш создаются заново для каждого токена'
    correct: 0
    explanation: >-
      Параметры модели не меняются при обработке запроса. KV-кэш сохраняет
      данные для поступающих токенов, поэтому увеличение контекста увеличивает
      именно его объём.
  - question: >-
      Что сохраняется в KV-кэше для повторного использования механизмом
      attention?
    options:
      - Только итоговые вероятности следующего токена
      - Ключи и значения для уже обработанных токенов
      - Копия всех весов модели для каждого запроса
      - Только накопители softmax
    correct: 1
    explanation: >-
      KV-кэш хранит key и value, вычисленные для предыдущих токенов. Это
      позволяет не пересчитывать их на каждом следующем шаге генерации.
---

Ты запускаешь ту же модель, но разрешаешь ей удерживать не 64 тысячи, а 128 тысяч токенов. Здесь легко решить, что вся память должна вырасти вдвое. Проверь различие: веса — фиксированное содержимое модели, поэтому их объём из [шага 5](#step-5) не меняется. KV-кэш — записи, создаваемые для каждого токена и каждого запроса в батче. В расчёте из [шага 8](#step-8) длина контекста входит как множитель: токенов стало вдвое больше — KV-кэш тоже стал вдвое больше.

Главная ловушка — перепутать размер самой модели с объёмом её текущей рабочей памяти.
