---
id: 007-vesa-120b
type: check
title: 'Сколько занимают веса 120B в BF16, FP8 и NVFP4?'
source_anchor: '## Exercises'
check:
  - question: >-
      Почему оценки объёма весов недостаточно, чтобы решить, поместится ли
      модель в HBM при запуске?
    options:
      - >-
        Помимо весов, память нужна KV-кэшу, рабочим буферам и служебным данным
        движка
      - Перед запуском все веса автоматически преобразуются обратно в BF16
      - 'HBM хранит только активации, а веса всегда остаются в оперативной памяти'
      - Объём весов невозможно определить до появления первого запроса
    correct: 0
    explanation: >-
      Расчёты 240, 120 и 60 ГБ описывают только веса 120B-модели. Полный бюджет
      HBM также включает KV-кэш, активации, рабочие буферы и служебный резерв.
  - question: >-
      В MoE-модели на каждом токене работает лишь часть экспертов. Что это
      означает для хранения весов без выгрузки экспертов из HBM?
    options:
      - Хранить достаточно только веса активных на текущем токене экспертов
      - >-
        В HBM по-прежнему должны находиться веса всех экспертов, хотя вычисления
        читают лишь активную часть
      - Объём весов начинает зависеть от длины контекста
      - Неактивные эксперты можно заменить KV-кэшем
    correct: 1
    explanation: >-
      Доля активных параметров определяет объём вычислений и чтений при
      обработке токена, но не общий объём резидентных весов. У модели 120B с 30%
      активных параметров без офлоада всё равно хранятся все 120B параметров.
---

Здесь важно уверенно применить пересчёт из [шага 5](#step-5): для 120B получаются 240 ГБ в BF16, 120 ГБ в FP8 и 60 ГБ в NVFP4. Частая ошибка — принять биты за байты или решить, что переход с 16 до 4 бит уменьшает объём всего вдвое. На самом деле это четырёхкратное сокращение.

Сейчас ты считаешь только веса модели — не весь расход памяти при запуске.
