- concept: выбор метрики примеси определяется параметром criterion
  question: Заполни условное выражение, которое выбирает Gini для соответствующего значения criterion, а во всех остальных случаях — энтропию.
  explanation: При точном совпадении criterion со строкой "gini" используется gini_impurity. Иначе выражение выбирает entropy.
  kind: cloze
  template: measure = gini_impurity if criterion == ___ else entropy
  answer: '"gini"'
  accept:
    - '''gini'''
  id: 012-code-information-gain-1
  fingerprint: 158168d5
- concept: разбиение с пустым дочерним узлом получает нулевой information gain
  question: Кандидатное разбиение отправило все объекты в правый дочерний узел, а левый узел остался пуст. Что должна вернуть information_gain?
  explanation: Пустой дочерний узел означает, что объекты фактически не разделились. Поэтому функция сразу возвращает 0.0 и не рассматривает такой кандидат как полезное разбиение.
  kind: choice
  options:
    - Нулевой прирост информации
    - Примесь родительского узла
    - Примесь непустого дочернего узла
  correct: 0
  id: 012-code-information-gain-2
  fingerprint: 4175bcef
