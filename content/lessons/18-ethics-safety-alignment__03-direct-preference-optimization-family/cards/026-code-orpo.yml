- concept: численно устойчивое вычисление логарифма шансов непосредственно из log-probability
  question: Заполните выражение для логарифма шансов, не переводя log-probability сначала в обычную вероятность.
  explanation: Логарифм шансов равен log p минус log(1 − p). Операция log1p сохраняет точность, а ограничение сверху защищает случай, когда log p слишком близок к нулю.
  kind: cloze
  template: |-
    safe_logp = torch.clamp(logp, max=-eps)
    log_odds = safe_logp - ___
  answer: torch.log1p(-torch.exp(safe_logp))
  accept:
    - torch.log1p(-safe_logp.exp())
  id: 026-code-orpo-1
  fingerprint: 3f355361
