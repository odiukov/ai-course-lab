- concept: суммарный объём состояний SSM вычисляется без дополнительного множителя за пару K и V
  question: Функция должна вернуть в байтах суммарный объём рекуррентных состояний всех SSM-слоёв. Заполните выражение.
  explanation: У каждого SSM-слоя хранится одно состояние размером hidden_dim × state_size. Поэтому для 28 слоёв Jamba в BF16 получается 28 × 4096 × 16 × 2 ≈ 3,7 МБ; множитель за отдельные K и V здесь не нужен.
  kind: cloze
  template: return ___
  answer: n_layers * hidden_dim * state_size * bytes_per_elem
  accept:
    - n_layers*hidden_dim*state_size*bytes_per_elem
    - bytes_per_elem * n_layers * hidden_dim * state_size
  id: 027-code-ssm-state-bytes-1
  fingerprint: 3f447852
