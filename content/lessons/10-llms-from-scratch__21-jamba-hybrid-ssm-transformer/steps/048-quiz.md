---
id: 048-quiz
type: quiz
title: 'Итоговая проверка: рекуррентность, пропорция 1:7, бюджет памяти и Mamba-3'
source_anchor: '## Key Terms'
check:
  - question: >-
      Слой Mamba обрабатывает токен номер 200000. Что ему нужно, чтобы выдать
      выход на этом токене?
    options:
      - Все 199999 предыдущих токенов — по ним считаются веса
      - 'Состояние, накопленное к предыдущему токену, и сам текущий токен'
      - Последние 4096 токенов — окно фиксированной длины
    correct: 1
    explanation: >-
      Вся прошлая история уже упакована в состояние предыдущего шага, поэтому
      стоимость одного токена не зависит от того, двухсотый он или
      двухсоттысячный.
  - question: >-
      Ты меняешь пропорцию в блоке Jamba с одного слоя attention на семь Mamba
      до одного на пятнадцать. Что произойдёт?
    options:
      - >-
        Память под KV cache вырастет, зато точность на длинном контексте
        улучшится
      - >-
        KV cache станет меньше, но модель начнёт хуже вытаскивать точные детали
        из контекста
      - 'Ничего заметного: слои attention влияют только на скорость'
      - >-
        Модель перестанет понимать порядок токенов: позиция приходит только из
        attention
    correct: 1
    explanation: >-
      Слоёв attention становится вдвое меньше — экономия на кэше есть, но именно
      они обеспечивают прямой доступ к отдельным позициям, поэтому точное
      извлечение деталей может ухудшиться.
  - question: >-
      Почему увеличение длины контекста расходует дополнительную память в слоях
      attention, но не увеличивает состояние слоёв Mamba?
    options:
      - >-
        Attention сохраняет ключи и значения для прошлых токенов, а Mamba
        сворачивает историю в состояние фиксированного размера
      - >-
        Attention хранит копию весов для каждого токена, а Mamba использует
        общие веса
      - 'Состояние Mamba размещается на CPU, поэтому его размер не учитывается'
      - >-
        Mamba удаляет предыдущие токены и повторно вычисляет их при каждом
        следующем шаге
    correct: 0
    explanation: >-
      KV cache содержит данные для каждой сохранённой позиции и поэтому растёт
      вместе с контекстом. Рекуррентное состояние Mamba имеет фиксированный
      размер на слой независимо от числа уже обработанных токенов.
  - question: >-
      Mamba-3 — чистый SSM, и она сильнее прежних чистых SSM. Почему это не
      отменяет гибриды вроде Jamba?
    options:
      - 'Mamba-3 медленнее на инференсе, чем слой attention'
      - >-
        Сжатая история по-прежнему теряет точные детали, поэтому несколько слоёв
        attention остаются нужны
      - Mamba-3 не работает с контекстом длиннее 64k
    correct: 1
    explanation: >-
      Улучшенная рекуррентность делает сжатие лучше, но не превращает его в
      точное хранение — задачи на дословное припоминание всё ещё выигрывают от
      attention.
---

Дальше в курсе ты будешь выбирать архитектуру под конкретную нагрузку, и оттуда пригодятся три вещи.

Первая: у рекуррентности шаг стоит одинаково на любой позиции, поэтому память слоя Mamba фиксирована ([шаг 8](#step-8)). Вторая: считать байты, а не «примерно больше» — формула KV cache из [шага 24](#step-24) и разрыв 16 ГБ против 3.7 МБ из [шага 28](#step-28). Третья: 1:7 — не круглое число из головы, а результат абляций ([шаг 19](#step-19)).

Спотыкаются обычно на трёх местах: путают «attention не нужен» с «attention нужен реже»; забывают, что с длиной растёт только KV cache, а веса и состояние — нет; и читают силу Mamba-3 как конец гибридов.
