---
id: 013-check-ssm
type: check
title: 'Проверка: что нужно для y_t и что растёт с длиной'
source_anchor: '### An SSM in one page'
check:
  - question: 'Каких данных достаточно, чтобы вычислить выход SSM на текущем такте?'
    options:
      - Всей последовательности входов от начала контекста
      - Предыдущего состояния и текущего входа
      - KV-кэша всех предыдущих токенов
    correct: 1
    explanation: >-
      Из предыдущего состояния и текущего входа сначала вычисляется новое
      состояние, после чего его проекция даёт текущий выход. Повторно обращаться
      ко всей истории не требуется.
  - question: >-
      Что произойдёт при увеличении длины последовательности, если размер
      состояния SSM не меняется?
    options:
      - Состояние будет расширяться после каждого токена
      - >-
        Возрастёт число рекуррентных шагов, но память и стоимость отдельного
        шага останутся постоянными
      - >-
        Каждый следующий шаг будет требовать попарного сравнения со всеми
        предыдущими токенами
    correct: 1
    explanation: >-
      SSM выполняет по одному вычислению постоянной стоимости на токен. Поэтому
      суммарное время растёт с числом токенов, а размер состояния и рабочая
      память отдельного шага не растут.
---

Перед тем как идти к блоку Jamba, стоит проверить одну развилку, на которой чаще всего заносит.

После [шагов 6](#step-6)–[9](#step-9) у тебя в руках рекуррентная формула и код к ней. Значит, ты должен уметь ответить на два вопроса, не подглядывая: какие ровно две вещи нужны, чтобы получить $y_t$, и что именно меняется, когда контекст растёт с 1000 токенов до 256 000.

Спотыкаются обычно так: помнят, что «память постоянна», и переносят это на общее время работы — а оно всё равно линейно, просто шагов больше. Или наоборот: думают, что состояние тихо распухает вместе с историей, потому что история-то никуда не исчезла. Она не исчезла, но лежит внутри состояния фиксированного размера, а не рядом с ним.
