---
id: 019-vybor-instrumenta
type: check
title: 'Выбираем инструмент для бенчмарка, CI-гейта и проверки шлюза'
source_anchor: '### 2026 tool mapping'
check:
  - question: >-
      Тебе нужны три проверки: подробный прогон производительности LLM, гейт SLA
      в CI/CD и насыщение HTTP-шлюза постоянной частотой. Какой набор лучше
      подходит?
    options:
      - 'LLMPerf для прогона, k6 для CI-гейта, Vegeta для шлюза'
      - >-
        Vegeta для прогона, LLMPerf для CI-гейта, Locust без расширений для
        шлюза
      - 'k6 для прогона, Vegeta для CI-гейта, GenAI-Perf для шлюза'
    correct: 0
    explanation: >-
      Инструмент выбирают по задаче: LLMPerf измеряет производительность LLM, k6
      удобен для автоматического SLA-гейта, а Vegeta проверяет HTTP-шлюз и
      рейт-лимиты.
  - question: >-
      LLMPerf показал TPOT 11 мс, а GenAI-Perf на том же сервере — 6 мс. Как
      поступить?
    options:
      - 'Объявить GenAI-Perf более точным, потому что число меньше'
      - 'Усреднить результаты и записать TPOT 8,5 мс'
      - >-
        Не сравнивать числа напрямую: инструменты по-разному учитывают TTFT в
        интервале между токенами
    correct: 2
    explanation: >-
      Разные определения метрики дают разные числа даже на одном сервере. Их
      нельзя смешивать в одном сравнительном отчёте.
---

Здесь проверяется не знание рейтинга инструментов, а умение подобрать ключ к замку. Для подробного LLM-бенчмарка бери LLMPerf, для автоматической проверки SLA — k6, для шлюза и рейт-лимитов — Vegeta. Основания выбора разобраны в [шаге 17](#step-17) и [шаге 18](#step-18).

Главная ловушка — сравнить одинаково подписанные метрики, рассчитанные по-разному. У GenAI-Perf интервал между токенами не включает TTFT, а у LLMPerf включает, поэтому их TPOT нельзя складывать в одну таблицу как равнозначные результаты.

> 🎒 **На пальцах.** На аэродроме двигатель проверяют на стенде, готовность рейса — по предполётному чек-листу, а пропускную способность входа — у турникета. Один прибор не обязан хорошо решать все три задачи. И если два секундомера запускают в разные моменты, меньшее число ещё не означает более быстрый самолёт.
