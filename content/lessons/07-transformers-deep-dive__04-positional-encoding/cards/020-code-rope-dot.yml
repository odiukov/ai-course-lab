- concept: rope_dot вычисляет скалярное произведение двух независимо повёрнутых векторов
  question: Заполни тело функции, которая применяет RoPE к каждому вектору на его позиции и возвращает одно скалярное произведение.
  explanation: Сначала каждый вектор поворачивается на собственной позиции, затем между полученными векторами вычисляется скалярное произведение.
  kind: cloze
  template: |-
    def rope_dot(a, b, pos_a, pos_b):
        ra = apply_rope(a, pos_a)
        rb = apply_rope(b, pos_b)
        return ___
  answer: np.dot(ra, rb)
  accept:
    - ra @ rb
    - torch.dot(ra, rb)
  id: 020-code-rope-dot-1
  fingerprint: e700963a
- concept: инварианты с вычислениями в плавающей точке проверяют с численным допуском
  question: Два вычисления теоретически должны дать одинаковый скалярный результат, но выполняются в арифметике с плавающей точкой. Какая проверка корректнее?
  explanation: Порядок операций и округление могут оставить небольшой численный хвост даже при верной реализации. Поэтому сравнивают модуль разности с допуском, а не требуют побитового равенства.
  kind: choice
  options:
    - Проверить точное равенство результатов оператором ==
    - Проверить, что модуль разности результатов не превышает выбранный допуск
    - Округлить каждый результат до целого и сравнить
    - Считать результаты равными, если у них совпадают знаки
  correct: 1
  id: 020-code-rope-dot-2
  fingerprint: 2f0fee04
