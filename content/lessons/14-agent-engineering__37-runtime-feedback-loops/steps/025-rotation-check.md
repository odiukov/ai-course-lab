---
id: 025-rotation-check
type: check
title: 'Почему агент читает текущий файл, а CI хранит весь ротированный набор?'
source_anchor: '## Production patterns in the wild'
check:
  - question: >-
      Последний запуск успешен, но причина двух предыдущих падений находится в
      ротированном файле .2. Кто и что должен читать?
    options:
      - >-
        Агент читает только текущий файл, а CI сохраняет весь ротированный набор
        для последующего разбора
      - 'Агент читает весь набор, а CI сохраняет только текущий файл'
      - 'И агент, и CI используют только текущий файл'
    correct: 0
    explanation: >-
      Агенту нужна ограниченная по размеру обратная связь для следующего
      действия, а полный набор нужен CI для аудита истории.
  - question: 'Что нарушится, если при каждом шаге загружать агенту все пять файлов?'
    options:
      - Ротированные записи перестанут быть JSONL
      - >-
        Вырастет стоимость чтения и контекста без необходимости для ближайшего
        решения
      - CI больше не сможет сохранить файлы как артефакты
    correct: 1
    explanation: >-
      Текущий файл ограничивает рабочий контекст одним мегабайтом; старые записи
      остаются доступными в артефактах CI.
---

После ротации из [шага 23](#step-23) у тебя есть текущая запись и несколько старых файлов. Легко решить, что раз история полезна, агенту надо читать её целиком. Но его задача уже: выбрать следующий шаг по свежему результату. Старые попытки увеличивают время загрузки и занимают контекст.

CI решает другую задачу: сохранить свидетельства для позднего разбора, проверки цепочки повторов и аудита. Поэтому он забирает весь набор, не заставляя агента перечитывать архив при каждом действии.

> 🎒 **На пальцах.** Механик держит на верстаке лист с сегодняшними работами, а закрытые листы складывает в папку мастерской. Для следующего ремонта ему нужен свежий лист; если клиент позже оспорит историю обслуживания, поднимают всю папку. Удалять архив не надо — не надо лишь каждый раз раскладывать его на верстаке.
