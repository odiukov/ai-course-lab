- concept: проверка области собирает только находки и не выставляет общий вердикт — свёртка статуса происходит в одном месте позже
  question: 'Функция проверки области нашла запись в запрещённый каталог. Почему она возвращает находку, а не выставляет passed: false прямо на месте?'
  explanation: 'Отсюда и структура кода: функции-сборщики фактов возвращают списки находок, а вычисление passed остаётся отдельным шагом над объединённым списком.'
  kind: open
  reference: 'Каждая частная проверка отвечает только за свой предмет и выдаёт факты в общем формате находок: код, сообщение, серьёзность, ссылку на свидетельство. Общий вердикт считается один раз, когда все находки из всех проверок собраны вместе — тогда правило «есть хотя бы одна находка block ⇒ passed: false» применяется единообразно, а список предупреждений не теряется. Если бы каждая функция сама выставляла статус, статус пришлось бы согласовывать между функциями и порядок их вызова начал бы влиять на результат.'
  id: 010-gate-scope-1
  fingerprint: 7a5e4974
- concept: находка фиксирует только отклонение — пустой список нарушений не порождает записи об успехе
  question: В отчёте области изменений поле запрещённых записей — пустой список, а в изменениях вне разрешённой области указано 2 пути. Что вернёт функция проверки области при мягкой политике?
  explanation: 'Пустой список нарушений — это отсутствие проблемы, «успешные находки» не создаются. Непустой список изменений вне области даёт находку, а её серьёзность берётся из переданной политики строгости: при мягкой — warn.'
  kind: choice
  options:
    - 'Две находки: одну об успешной проверке запретов и одну о выходе за область'
    - Одну находку с серьёзностью warn о двух путях вне разрешённой области
    - Одну находку block, потому что любые два лишних пути считаются нарушением запрета
    - Пустой список, поскольку блокирующих нарушений нет
  correct: 1
  id: 010-gate-scope-2
  fingerprint: 335ed551
