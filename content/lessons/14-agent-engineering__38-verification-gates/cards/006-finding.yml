- concept: ненулевой код завершения приёмочной команды — блокирующая находка
  question: 'Приёмочная команда завершилась ненулевым кодом. Какое значение severity ставит проверка, чтобы находка запретила выставить `passed: true`?'
  explanation: 'Ненулевой код выхода приёмочной команды — блокирующее свидетельство: с ним положительный вердикт невозможен. Значение `warn` оставило бы задачу проходимой, лишь добавив пометку.'
  kind: cloze
  template: finding(check="acceptance_command_exit", severity="___", message="pytest exited 1", evidence="feedback_record.jsonl:12")
  answer: block
  accept:
    - block
    - '"block"'
    - '''block'''
  id: 006-finding-1
  fingerprint: 8ef9dc11
- concept: функция сборки finding только упаковывает переданные сведения и не вычисляет вердикт
  question: Какая обязанность НЕ входит в функцию, собирающую finding?
  explanation: Функция упаковывает переданные сведения в предсказуемую форму с неизменными именами полей. Она не читает отчёты, не запускает команды и не считает вердикт — вердикт вычисляется позже, при обходе списка findings по их severity.
  kind: choice
  options:
    - Записать имя сработавшей проверки
    - Решить, будет ли итоговый вердикт задачи положительным
    - Записать severity находки
    - Записать ссылку на свидетельство в исходном артефакте
  correct: 1
  id: 006-finding-2
  fingerprint: 74d0bf2f
