---
id: 007-check-tri-slabosti
type: check
title: 'Проверка: три слабости по описанию'
source_anchor: '## The Problem'
check:
  - question: >-
      RNN-энкодер прочитал 500 токенов и передал декодеру только один
      контекстный вектор фиксированной ширины. Обучение проходит устойчиво, но
      при увеличении входа перевод теряет детали из его начала. Какая слабость
      здесь указана наиболее непосредственно?
    options:
      - 'Последовательность вычислений: позиции нельзя обработать одновременно'
      - >-
        Затухающие градиенты: сигнал проходит через длинную цепочку
        преобразований
      - >-
        Фиксированный контекстный вектор: весь вход сжимается в представление
        постоянной ёмкости
      - 'Нехватка памяти GPU: длинный вход не помещается на устройстве'
    correct: 2
    explanation: >-
      Решающая деталь — декодер получает весь вход через единственный вектор
      неизменной ширины. Для 500 токенов он имеет ту же ёмкость, что и для
      короткого текста, поэтому становится информационным бутылочным горлышком.
  - question: >-
      Ту же RNN без изменения архитектуры и гиперпараметров перенесли на GPU с
      вдвое большей пиковой производительностью. Какое из трёх архитектурных
      ограничений обязательно исчезнет?
    options:
      - Ни одно
      - Зависимость очередного рекуррентного шага от предыдущего
      - Ослабление градиента на длинном пути
      - Фиксированная ширина контекстного вектора
    correct: 0
    explanation: >-
      Более быстрый GPU может уменьшить постоянную составляющую времени
      выполнения, но не меняет граф вычислений модели. Рекуррентная зависимость
      шагов, длина пути градиента и ширина контекстного вектора остаются
      прежними.
---

Три слабости легко перепутать, потому что все они звучат как «модель плохо работает на длинном входе». Но лечатся они разным, и различать их придётся весь оставшийся курс.

Быстрый способ развести: спроси, где именно теряется. Не успеваем по времени, GPU простаивает — это последовательность шагов из [шага 2](#step-2). Модель обучилась, но забыла начало предложения — это затухающие градиенты из [шага 5](#step-5). Вход не помещается независимо от обучения, потому что ёмкость одна и та же на 5 и на 500 токенов — это фиксированный вектор из [шага 6](#step-6).

Типичная ошибка — списывать всё на «памяти не хватает». Проверь на двух вопросах ниже.
