---
id: 004-check-serial
type: check
title: 'Проверка: почему RNN не ускорить видеокартой'
source_anchor: '## The Problem'
check:
  - question: >-
      Ты обучаешь RNN на последовательности из 1024 токенов и меняешь видеокарту
      на вдвое более мощную — с 20 000 ядер вместо 10 000. Что произойдёт со
      временем прохода по этой последовательности?
    options:
      - 'Оно сократится примерно вдвое: ядер больше, значит, шаги пойдут быстрее'
      - 'Оно почти не изменится: шагов всё равно 1024, и каждый ждёт предыдущий'
      - 'Оно вырастет: чем больше ядер, тем дороже их синхронизировать'
    correct: 1
    explanation: >-
      Узкое место не в количестве ядер, а в том, что шаг t+1 не может стартовать
      без скрытого состояния шага t. Лишние ядра просто простаивают, как в цехе
      из шага 3.
  - question: >-
      Ты обучаешь RNN и подаёшь в неё сразу 64 предложения одной пачкой. Что
      здесь реально считается параллельно?
    options:
      - 'Ничего: рекуррентность запрещает любой параллелизм'
      - >-
        Все 64 предложения и все токены внутри каждого — пачка снимает
        ограничение
      - >-
        64 предложения между собой, но внутри каждого токены всё равно идут по
        очереди
    correct: 2
    explanation: >-
      Предложения независимы друг от друга, поэтому по ним параллелиться можно.
      А вот ось времени внутри предложения остаётся последовательной — именно её
      рекуррентность и запирает.
---

К этому моменту ты должен уметь объяснить не просто «RNN медленная», а почему её медленность нельзя вылечить деньгами. Формула из [шага 1](#step-1) задаёт цепочку зависимостей, [шаг 2](#step-2) показал, что цепочку не разорвать, а [шаг 3](#step-3) — что железо от этого простаивает.

Спотыкаются обычно в двух местах. Первое: путают «много вычислений» с «долгая цепочка». Второе: решают, что параллелизма в RNN нет вообще — хотя разные примеры в пачке считаются одновременно совершенно спокойно. Заперта именно ось времени, и только она.
