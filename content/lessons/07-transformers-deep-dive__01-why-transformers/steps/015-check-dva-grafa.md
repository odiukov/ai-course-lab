---
id: 015-check-dva-grafa
type: check
title: 'Проверка: какую из двух строк можно считать в 8 потоков'
source_anchor: '### Step 1: measure serial depth'
check:
  - question: >-
      Вот две строки. Первая: h = 0.9 * h + x внутри цикла по элементам. Вторая:
      sum(xs) / len(xs). Какую из них можно честно посчитать в 8 потоков?
    options:
      - 'Первую: умножение и сложение прекрасно распараллеливаются'
      - >-
        Вторую: слагаемые не зависят друг от друга, их можно разложить по
        потокам и сложить частичные суммы
      - 'Обе: математика в них одна и та же, значит и считаются они одинаково'
      - 'Ни одну: в Python потоки всё равно не дают ускорения'
    correct: 1
    explanation: >-
      Во второй строке каждое слагаемое готово сразу и ни от чего не ждёт,
      поэтому 8 потоков могут взять по своему куску. В первой новое h собирается
      из старого h — второй поток просто нечего дать, пока первый не закончил.
  - question: >-
      Ты перемешал порядок элементов в списке xs и запустил обе функции заново.
      Что произойдёт с результатами?
    options:
      - 'Изменится результат в стиле RNN, а среднее останется прежним'
      - 'Изменятся оба результата: порядок влияет на любые вычисления'
      - 'Изменится среднее, а результат в стиле RNN останется прежним'
      - 'Не изменится ничего: числа-то те же самые'
    correct: 0
    explanation: >-
      В цепочке h = 0.9 * h + x ранние элементы успевают умножиться на 0.9 много
      раз, а поздние почти нет — порядок входит в ответ. В среднем слагаемые
      равноправны, поэтому их можно складывать в любом порядке и любыми
      группами, что и есть условие для 8 потоков.
---

После двух написанных функций — [шага 12](#step-12) и [шага 14](#step-14) — у тебя должен появиться очень практичный навык: взглянуть на строчку кода и за секунду сказать, ждёт ли она чего-то от предыдущего результата. Если да — потоки бесполезны, сколько бы ядер ты ни арендовал.

Спотыкаются обычно на двух вещах. Первая: кажется, что дело в объёме арифметики — мол, где операций меньше, там и быстрее. Дело не в этом, а в том, кто кого ждёт. Вторая: `sum()` в Python и правда написан на C, и легко решить, что вся разница только в этом. Но ускорение от C — разовая надбавка, а свобода порядка — то, ради чего строят GPU.
