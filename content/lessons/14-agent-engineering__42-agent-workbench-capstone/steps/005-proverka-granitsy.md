---
id: 005-proverka-granitsy
type: check
title: 'Куда отнести схему состояния, текущую задачу проекта и вызов вендорского SDK?'
source_anchor: '### What stays in, what stays out'
check:
  - question: >-
      Ты собираешь переносимый пак. Куда отнести схему состояния
      agent_state.json, текущую задачу проекта и вызов вендорского SDK?
    options:
      - Все три оставить внутри пака
      - 'Схему оставить внутри, а задачу и вызов SDK — снаружи'
      - 'Схему и задачу оставить внутри, а вызов SDK — снаружи'
      - Все три оставить снаружи
    correct: 1
    explanation: >-
      Схема задаёт универсальный контракт пака. Текущая задача относится к
      конкретному проекту, а вызов SDK привязывает пак к определённому вендору.
  - question: >-
      Вызов SDK работает одинаково в нескольких твоих проектах. Почему его всё
      равно не стоит включать в универсальный пак?
    options:
      - Потому что любые вызовы SDK всегда небезопасны
      - Потому что повторяемый код нельзя переносить между проектами
      - >-
        Потому что он создаёт зависимость от конкретного вендора, которой может
        не быть в другом репозитории
    correct: 2
    explanation: >-
      Совпадение нескольких проектов ещё не делает зависимость универсальной.
      Переносимый пак должен сохранять контракт без обязательной привязки к
      одному SDK.
---

Здесь важно применить границу из [шага 3](#step-3), а к результату `classify_pack_candidates` из [шага 4](#step-4) отнестись как к осмысленному решению, а не сортировке по расширению файла. Обычно спотыкаются на SDK: его вызов может выглядеть полезным и многократно использоваться, но всё равно привязывает пак к одному вендору. Проверка простая: схема состояния описывает общий контракт, поэтому остаётся внутри; сегодняшняя задача и конкретный способ обратиться к сервису меняются вместе с проектом, поэтому остаются снаружи.

> 🎒 **На пальцах.** Представь универсальный переходник для розеток. Форма разъёмов и правила напряжения принадлежат самому переходнику. Записка «зарядить телефон в номере 312» относится к текущей поездке, а фирменное приложение одной энергокомпании — к конкретному поставщику. Полезность обоих не делает их частью переходника.
