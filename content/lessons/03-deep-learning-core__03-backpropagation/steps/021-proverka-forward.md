---
id: 021-proverka-forward
type: check
title: 'Проверка: какие значения из forward понадобятся при движении назад'
source_anchor: '### Forward vs Backward'
check:
  - question: >-
      Какие значения из прямого прохода нужны, чтобы пройти назад через сигмоиду
      и линейную операцию второго слоя?
    options:
      - Только итоговый выход сети
      - Вход второго слоя и его выход после сигмоиды
      - Только веса второго слоя
      - Только исходный вход сети
    correct: 1
    explanation: >-
      Вход второго слоя нужен для градиентов его весов, а сохранённый выход
      сигмоиды — для вычисления её локальной производной.
  - question: >-
      Что потребуется сделать, если перед обратным проходом удалить активацию
      первого слоя?
    options:
      - 'Отказаться от обратного прохода: восстановить значение уже нельзя'
      - >-
        Продолжить без изменений: эта активация не участвует в вычислении
        градиентов
      - Повторно вычислить активацию перед нахождением градиентов второго слоя
      - Подставить вместо неё итоговый выход сети
    correct: 2
    explanation: >-
      Активация первого слоя служит входом второго. Если её не сохранить, для
      вычисления градиентов весов второго слоя придётся повторить
      соответствующую часть прямого прохода.
---

Проверь, можешь ли ты мысленно собрать «набор для дороги назад». Для каждого слоя нужен его вход: именно с ним было связано влияние весов. Для сигмоиды пригодится уже найденный выход — это следует из [шага 14](#step-14). В сети из [шага 18](#step-18) такими значениями становятся вход сети и активации слоёв.

Частая ошибка — сохранять только последний ответ: кажется, что остальные числа уже отработали. Но тогда обратному проходу нечем вычислять локальные градиенты, и часть прямого прохода придётся повторить. Поэтому сохранённые активации из [шага 20](#step-20) — не архив, а рабочие данные для движения назад.
