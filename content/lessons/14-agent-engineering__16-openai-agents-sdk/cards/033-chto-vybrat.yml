- concept: явное внешнее состояние и возобновление прогона с места падения — критерий выбора LangGraph, а не SDK вокруг одного провайдера
  question: Пайплайн работает около 90 минут и иногда падает в середине. После перезапуска процесс должен продолжиться с того шага, на котором упал, а не начинаться заново. Какой вариант рантайма подходит под это требование?
  explanation: LangGraph держит состояние прогона явно и снаружи, поэтому его можно поднять после падения и продолжить с того же шага. SDK вокруг конкретного провайдера решают задачу маршрутизации и guardrails, но не дают надёжного продолжения; своя реализация означала бы писать механику состояния самому.
  kind: choice
  options:
    - OpenAI Agents SDK
    - Claude Agent SDK
    - LangGraph
    - Собственная реализация цикла вызовов модели
  correct: 2
  id: 033-chto-vybrat-1
  fingerprint: 63f918e1
- concept: своя реализация рантайма оправдана конкретным требованием (голос, несколько провайдеров, федеративное развёртывание, точный контроль), а не общей желательностью гибкости
  question: Команда предлагает написать свой агентный рантайм вместо готового SDK, аргумент — «так будет гибче». Объясни своими словами, какой аргумент считается достаточным для своей реализации, а какой нет.
  explanation: 'Проверка простая: можешь назвать конкретное слово-требование (голос, несколько провайдеров, федеративность, точный контроль) — своё оправдано. Не можешь — платишь за ручную реализацию того, что готовый рантайм уже даёт.'
  kind: open
  reference: '«Гибкость» — не аргумент, потому что она не называет требование, которое готовый рантайм не закрывает. Своя реализация окупается, когда задачу описывает конкретное условие: голосовой поток, где обработку надо резать по миллисекундам и готовый цикл не влезает; работа сразу с несколькими провайдерами моделей, под которые один провайдерский SDK тесен; федеративные развёртывания; необходимость точного контроля над циклом. Если ни одно такое условие назвать нельзя — берётся готовый рантайм.'
  id: 033-chto-vybrat-2
  fingerprint: e691862f
- concept: цена отказа от готового рантайма — повторная реализация его встроенных механизмов, а не только основного цикла вызовов модели
  question: Команда решает писать свой агентный рантайм, аргументируя это тем, что «там же только цикл и вызовы модели». Что в этой оценке трудозатрат упущено сильнее всего?
  explanation: 'Цикл «вызвали модель — обработали tool call» действительно короткий. Основная работа — в обвязке вокруг него: счётчик переходов против петель маршрутизации, места подключения guardrails, дерево спанов и очистка их содержимого. Всё это в готовом рантайме уже есть, и своя реализация означает написать это заново.'
  kind: choice
  options:
    - Стоимость токенов вырастет, потому что свой цикл шлёт больше запросов к модели
    - 'Придётся заново писать обвязку, которую готовый рантайм даёт из коробки: ограничение переходов между агентами, точки установки проверок, трассировку и вынос чувствительных данных из неё'
    - Модель откажется вызывать инструменты, если запрос отправлен не через SDK
    - Без SDK нельзя вести диалог из нескольких ходов
  correct: 1
  id: 033-chto-vybrat-3
  fingerprint: ab4ba4fb
