---
id: 020-check-rezhim
type: check
title: 'Проверка: какой режим под эту нагрузку'
source_anchor: '### Guardrails'
check:
  - question: >-
      Внутренний бот для сотрудников: подавляющее большинство запросов
      нормальные, основной вызов заметно дороже guardrail. Люди жалуются, что
      ответ приходит медленно, а бюджет на токены не поджимает. Какой режим
      ставишь?
    options:
      - >-
        Parallel: guardrail крутится одновременно с основной LLM, поэтому к
        нормальным ответам не приклеивается ожидание проверки
      - 'Blocking: он дешевле, а задержку никто не заметит'
      - >-
        Blocking: только он умеет остановить запрос до основного вызова, в
        parallel проверка уже ни на что не влияет
      - 'Разницы нет: оба режима тратят одинаково и отвечают одинаково'
    correct: 0
    explanation: >-
      Жалоба на скорость, а не на деньги. В parallel guardrail-LLM работает
      параллельно основной, её время прячется под временем основного вызова —
      хвостовая задержка ниже. Платишь за это лишними токенами guardrail на
      каждом запросе, но при редких срабатываниях это дёшево.
  - question: >-
      Публичная форма, куда льётся спам: guardrail отсекает примерно половину
      запросов. Счёт за токены растёт. Что даст переход на blocking?
    options:
      - 'Он поймает больше плохих запросов, чем parallel'
      - >-
        На отсечённых запросах основной вызов не запустится вовсе, но каждый
        прошедший ответ придёт с задержкой guardrail сверху
      - >-
        Он уберёт задержку, потому что проверка идёт первой и не ждёт основную
        LLM
      - 'Он снизит цену и на прошедших, и на отсечённых запросах'
    correct: 1
    explanation: >-
      Ловят оба режима одинаково — отличается только порядок. В blocking
      guardrail отрабатывает первым: на отсечённых запросах токены основного
      вызова не тратятся совсем, а на пропущенных время guardrail добавляется к
      общему ожиданию. При такой доле срабатываний экономия перевешивает
      задержку.
---

После [шагов 17](#step-17) и [18](#step-18) ты должен уметь не пересказать разницу между parallel и blocking, а выбрать режим под конкретную нагрузку: посмотреть на долю плохих запросов, на цену основного вызова и на то, что болит сильнее — счёт или секунды.

Спотыкаются обычно на двух вещах. Первая: кажется, что blocking «надёжнее ловит». Нет — ловят оба одинаково, tripwire срабатывает в любом случае, меняется только порядок. Вторая: кажется, что parallel быстрее всегда. Он быстрее по хвостовой задержке, но токены за выброшенный основной ответ ты уже заплатил.
