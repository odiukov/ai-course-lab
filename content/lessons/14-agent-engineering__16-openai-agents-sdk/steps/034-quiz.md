---
id: 034-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      Triage-агент вызвал инструмент transfer_to_billing. Что происходит с
      диалогом дальше?
    options:
      - 'Запускается второй независимый прогон, история начинается с нуля'
      - >-
        Диалог тот же, но дальше отвечает биллинг-агент со своими инструкциями и
        своим набором инструментов
      - 'Triage продолжает отвечать, а биллинг только подсказывает ему текст'
      - >-
        Runner ждёт, пока пользователь подтвердит перевод, и только потом меняет
        агента
    correct: 1
    explanation: >-
      Хендофф — обычный вызов инструмента, но его результат — смена активного
      агента. Диалог и история те же, меняется тот, кто на них смотрит.
  - question: >-
      Цепочка: triage передаёт агенту-исследователю, тот передаёт
      агенту-редактору. Input guardrail стоит на triage, output guardrail — на
      редакторе. Что останется непроверенным?
    options:
      - 'Ничего: первый и последний закрывают всю цепочку'
      - Только финальный ответ редактора
      - >-
        То, что исследователь получил на вход и выдал на выход в середине
        цепочки
      - Только исходный запрос пользователя
    correct: 2
    explanation: >-
      Input проверяется у первого агента, output — у последнего. Всё, что
      породил и съел агент в середине, проходит мимо обеих проверок.
  - question: >-
      Guardrail срабатывает примерно на одном запросе из двадцати, а сама
      проверка стоит 200 токенов. Основной ответ — 5 000. Какой режим выгоднее
      по токенам?
    options:
      - 'Parallel: проверка идёт рядом, и токены не тратятся впустую'
      - >-
        Blocking: платишь 200 на каждом запросе, но не сжигаешь 5 000 на тех,
        что отклонены
      - 'Разницы нет, режимы отличаются только кодом'
      - 'Parallel, потому что 200 токенов дешевле 5 000'
    correct: 1
    explanation: >-
      В parallel отклонённый запрос всё равно успел сгенерировать ответ — эти
      токены сгорают. Blocking платит небольшую задержку, зато не оплачивает
      выброшенные ответы.
  - question: >-
      Ты добавил свой процессор через add_trace_processor, чтобы вырезать номера
      карт из спанов. Почему чувствительные данные всё равно могут уйти наружу?
    options:
      - 'Процессор не видит спаны инструментов, только спаны генераций'
      - >-
        add_trace_processor добавляет твой процессор к стандартному, а не
        заменяет его — исходные спаны продолжают уходить туда же
      - 'Редактирование спанов запрещено, SDK откатывает изменения'
      - 'Спаны отправляются до того, как вообще создаётся процессор'
    correct: 1
    explanation: >-
      Это добавление, а не перенаправление. Стандартный экспортёр остаётся в
      списке и получает нетронутые спаны — свой процессор придётся ставить
      вместо него.
---

Урок закончился, и дальше в курсе тебе понадобится не список типов из SDK, а несколько связок, которые ты вывел сам.

Первая: хендофф — не отдельный механизм, а инструмент, и потому он живёт по правилам инструментов ([шаг 4](#step-4)). Вторая: guardrails прикрепляются к точкам, а не к системе целиком, поэтому середина цепочки остаётся дырой ([шаг 14](#step-14)). Третья: выбор parallel против blocking — это арифметика на твоей частоте срабатывания, а не вопрос вкуса ([шаги 17](#step-17)–[18](#step-18)).

Спотыкаются обычно на трёх вещах. Думают, что хендофф сбрасывает историю. Считают, что input и output guardrails закрывают всё, что между ними. И читают add_trace_processor как «перенаправить», хотя это «добавить» ([шаг 27](#step-27)).

Проверь эти места на вопросах ниже.
