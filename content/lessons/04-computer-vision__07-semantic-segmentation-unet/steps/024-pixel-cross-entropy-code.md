---
id: 024-pixel-cross-entropy-code
type: code
title: Пишем pixel_cross_entropy
source_anchor: '### Cross-entropy on a pixel grid'
exercise_fn: pixel_cross_entropy
---

Ты запускаешь обучение и видишь странность: функция из [шага 20](#step-20) уже умеет получать вероятности, поэтому хочется сначала применить `softmax`, а затем передать результат в loss. Здесь это лишний шаг. `F.cross_entropy` ожидает именно сырые логиты и сама выполняет нужное преобразование устойчивым к вычислительным ошибкам способом.

Тебе нужно дописать функцию:

```python
import torch
import torch.nn.functional as F


def pixel_cross_entropy(
    logits: torch.Tensor,
    target: torch.Tensor,
) -> torch.Tensor:
    # Твоё решение
    pass
```

Контракт берём из [шага 18](#step-18): `logits` имеют форму `(N, C, H, W)`, а `target` — `(N, H, W)`. Каждый элемент `target` хранит целочисленный номер правильного класса, поэтому маска должна иметь тип `torch.long`, а не содержать вероятности или отдельный канал для каждого класса.

Внутри достаточно одного вызова подходящей функции из `torch.nn.functional`. Передай ей логиты и целевую маску в правильном порядке. Не переставляй оси, не превращай изображения в длинный список пикселей и не добавляй `softmax`: PyTorch распознаёт пространственные измерения и усреднит потери по всей сетке сам.

Проверь результат: функция должна вернуть одно число — среднюю кросс-энтропию для всего батча. Если на выходе осталась карта формы `(N, H, W)`, значит усреднение отключено. Смысл этого числа уже разобран в [шаге 23](#step-23): теперь твоя задача — аккуратно состыковать его с тензорами PyTorch.

> 🎒 **На пальцах.** Представь кассу с пачкой билетов: на каждом билете напечатаны оценки всех возможных маршрутов и номер нужного маршрута. Кассир сам сравнит оценки, найдёт нужную и посчитает общий штраф. Если ты заранее превратишь оценки в проценты или разрежешь пачку на отдельные стопки, то лишь повторишь часть его работы и добавишь место для ошибки.
