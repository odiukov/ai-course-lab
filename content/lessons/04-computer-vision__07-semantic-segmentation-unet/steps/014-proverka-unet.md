---
id: 014-proverka-unet
type: check
title: 'Проверка: что хранит каждый путь U-Net'
source_anchor: '### The U-Net shape'
check:
  - question: >-
      Что именно хранится в skip-пути U-Net до его объединения с признаками
      decoder?
    options:
      - Промежуточная карта признаков encoder соответствующего масштаба
      - Готовая маска с окончательным классом каждого пикселя
      - 'Только один класс, выбранный для всего изображения'
    correct: 0
    explanation: >-
      Skip-путь сохраняет признаки encoder с пространственными деталями. Это не
      готовая разметка: decoder ещё должен объединить их с контекстом и
      построить маску.
  - question: Как корректно распределены функции между частями U-Net?
    options:
      - >-
        Bottleneck хранит наиболее сжатое представление с контекстом, skip-пути
        передают детали разных масштабов, decoder объединяет эту информацию
      - >-
        Bottleneck хранит готовую маску, skip-пути выбирают классы, а decoder
        только копирует результат
      - >-
        Bottleneck сохраняет исходные пиксели без изменений, skip-пути сжимают
        признаки, а decoder определяет единственный класс изображения
    correct: 0
    explanation: >-
      Bottleneck содержит богатое по смыслу, но пространственно сжатое
      представление. Skip-пути возвращают детали соответствующих масштабов, а
      decoder использует контекст и детали для точной попиксельной маски.
---

Собери роли из [шагов 9](#step-9)–[12](#step-12) в одну картину. Encoder отвечает прежде всего за контекст, bottleneck хранит его в самом сжатом виде, skip-пути передают детали соответствующего разрешения, а decoder соединяет смысл и точную локализацию. Частая ошибка — считать skip готовой разметкой. Там не ответ, а признаки, по которым decoder восстанавливает границы.

> 🎒 **На пальцах.** Представь монтаж фильма. Краткий синопсис хранит смысл всей истории, но по нему не найдёшь точный кадр, где герой поднял руку. Для этого монтажёр оставляет метки на исходной плёнке. Собирая финальную сцену, он соединяет общий замысел с метками: синопсис даёт контекст, метки — точность.
