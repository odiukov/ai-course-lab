- concept: softmax сохраняет форму тензора логитов
  question: Заполни проверку формы результата softmax.
  explanation: Softmax нормализует значения вдоль выбранной оси, но не удаляет, не добавляет и не объединяет измерения тензора.
  kind: cloze
  template: assert softmax(logits).shape ___ logits.shape
  answer: ==
  accept:
    - ==
  id: 020-softmax-1
  fingerprint: 3d7ef261
- concept: softmax преобразует конечные логиты в допустимые вероятности
  question: Почему softmax конечных логитов не может вернуть отрицательную вероятность или вероятность, превышающую единицу? Объясни через вычисление softmax.
  explanation: Экспонента любого конечного логита положительна. Деление этой экспоненты на сумму экспонент всех конкурирующих классов даёт неотрицательную величину, не превышающую единицу.
  kind: open
  reference: Каждый логит экспоненцируется, поэтому числитель положителен. Знаменатель является суммой положительных экспонент и не меньше числителя, поэтому их отношение лежит в допустимом для вероятности диапазоне.
  id: 020-softmax-2
  fingerprint: e289ff72
