- concept: mIoU усредняет только определённые значения IoU классов
  question: Вектор IoU содержит `nan` для классов, отсутствующих и в предсказании, и в эталоне. Как корректно вычислить mIoU по батчу?
  explanation: Отсутствующий класс модель не распознала ни правильно, ни неправильно, поэтому он не должен влиять на среднее. В PyTorch такое усреднение можно выполнить через `torch.nanmean`.
  kind: choice
  options:
    - Заменить все `nan` нулями и вычислить обычное среднее
    - Исключить `nan` и усреднить только определённые значения IoU
    - Заменить все `nan` единицами и вычислить обычное среднее
    - Если встретился хотя бы один `nan`, считать весь mIoU неопределённым
  correct: 1
  id: 034-iou-per-class-code-1
  fingerprint: fda1ea7e
- concept: вычисление метрики без построения графа автоматического дифференцирования
  question: Заполни декоратор функции метрики, чтобы PyTorch не строил для её операций вычислительный граф.
  explanation: '`@torch.no_grad()` отключает запись операций в граф автодифференцирования. IoU используется для оценки, а не для обратного распространения, поэтому градиенты здесь не нужны.'
  kind: cloze
  template: |-
    @torch.___()
    def iou_per_class(logits, targets, num_classes):
        ...
  answer: no_grad
  accept:
    - no_grad
  id: 034-iou-per-class-code-2
  fingerprint: 9be0fa8b
