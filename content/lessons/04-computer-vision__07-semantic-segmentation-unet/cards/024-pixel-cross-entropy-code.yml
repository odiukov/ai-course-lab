- concept: F.cross_entropy принимает сырые логиты и сама вычисляет кросс-энтропию численно устойчивым способом
  question: Модель семантической сегментации вернула сырые логиты, а целевая маска содержит индексы классов. Как правильно вычислить loss?
  explanation: Нужно вызвать F.cross_entropy(logits, target). Эта функция ожидает ненормированные логиты и внутри объединяет необходимые операции так, чтобы избежать лишних вычислений и потери численной устойчивости.
  kind: choice
  options:
    - Сначала применить softmax к логитам, затем вызвать F.cross_entropy
    - Передать logits и target напрямую в F.cross_entropy
    - Преобразовать target в вероятности и сравнить её с логитами через F.mse_loss
  correct: 1
  id: 024-pixel-cross-entropy-code-1
  fingerprint: d0054581
- concept: целевая маска для многоклассовой кросс-энтропии хранит индексы классов в целочисленном типе torch.long
  question: Заполни пропуск, чтобы маску с номерами классов можно было передать в F.cross_entropy.
  explanation: Для многоклассовой сегментации target хранит индекс правильного класса каждого пикселя. F.cross_entropy ожидает такие индексы в формате torch.long, который в PyTorch соответствует torch.int64.
  kind: cloze
  template: target = class_ids.to(dtype=___)
  answer: torch.long
  accept:
    - torch.int64
  id: 024-pixel-cross-entropy-code-2
  fingerprint: 92acd3e0
