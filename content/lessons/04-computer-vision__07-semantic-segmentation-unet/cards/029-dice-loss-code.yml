- concept: для многоклассового Dice целевую маску преобразуют из индексов классов в поканальные бинарные маски
  question: Вероятности после softmax имеют форму [B, C, H, W], а целевая маска с индексами классов — [B, H, W]. Как подготовить цель для вычисления Dice по классам?
  explanation: Каждому каналу вероятностей должна соответствовать бинарная эталонная маска того же класса. Поэтому индексы классов переводят в one-hot-представление, не превращая мягкие предсказания в жёсткие.
  kind: choice
  options:
    - Преобразовать индексы в one-hot-маски формы [B, C, H, W] и привести их к вещественному типу
    - Продублировать исходную маску по оси классов без изменения значений
    - Применить softmax к индексам целевой маски
    - Свести вероятности к индексам классов через argmax
  correct: 0
  id: 029-dice-loss-code-1
  fingerprint: adfe80e8
- concept: функция потерь должна возвращать скалярный тензор, связанный с предсказанием вычислительным графом
  question: Функция вычислила скалярный тензор loss из вероятностей модели. Что она должна вернуть, чтобы последующий вызов backward() обновил параметры сети?
  explanation: Исходный тензор loss сохраняет вычислительный граф до предсказаний модели. item(), detach() и преобразование в число разрывают эту связь, поэтому использовать их можно для журналирования, но не как возвращаемую обучающую потерю.
  kind: choice
  options:
    - Сам тензор loss
    - Результат loss.item()
    - Результат loss.detach()
    - Обычное число, полученное через float(loss)
  correct: 0
  id: 029-dice-loss-code-2
  fingerprint: 87968a8d
