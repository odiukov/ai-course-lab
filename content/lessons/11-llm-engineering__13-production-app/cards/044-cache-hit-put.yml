- concept: попадание в кеш завершает обработку до вызова модели
  question: 'Заполните строку, которая завершает обработку при найденном кешированном результате:'
  explanation: При попадании в кеш сервис возвращает сохранённый ответ сразу, поэтому выбор шаблона, вызов модели и последующие операции не выполняются.
  kind: cloze
  template: |-
    cached = cache.get(effective_query)
    if cached:
        ___
  answer: 'return {"response": cached["response"], "cache_hit": True}'
  accept:
    - немедленно вернуть кешированный результат
    - return cached response
  id: 044-cache-hit-put-1
  fingerprint: cdc99190
