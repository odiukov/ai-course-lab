---
id: 061-itog
type: quiz
title: Итоговая проверка
source_anchor: '## Exercises'
check:
  - question: >-
      Пользователь трижды задал похожий вопрос, и на второй раз ответ пришёл из
      семантического кеша. Что произошло с остальными частями пайплайна?
    options:
      - 'Пайплайн отработал целиком, просто быстрее — кеш ускоряет вызов модели'
      - >-
        Пять шагов, включая вызов модели, не выполнились вовсе, а запись в лог
        всё равно появилась
      - 'Кеш подменил только выходной guardrail, всё остальное считалось заново'
    correct: 1
    explanation: >-
      Попадание в кеш срезает середину пайплайна вместе с вызовом модели —
      поэтому запрос стоит почти ноль, но в лог он попадает, иначе доля
      попаданий кеша будет неизвестна.
  - question: >-
      Модель вернула ошибку 500, а следом другой запрос получил 400 — «неверный
      формат параметра». Что делать с каждым?
    options:
      - Повторить оба с экспоненциальным backoff — сеть капризна
      - >-
        Повторить 500 с backoff и джиттером, а 400 не повторять: сама по себе
        она не пройдёт
      - 'Не повторять ни один: повтор удваивает счёт за токены'
    correct: 1
    explanation: >-
      Повторяют временные отказы — 500, таймаут, лимит частоты. Ошибка в самом
      запросе от повтора не исчезнет, только съест время и деньги.
  - question: >-
      Ты раскатываешь новую версию промпта на 10% пользователей. Почему бакет
      считается хешем от идентификатора пользователя, а не случайным числом на
      каждый запрос?
    options:
      - 'Хеш быстрее, чем генератор случайных чисел, и это экономит задержку'
      - >-
        Иначе один человек в течение диалога прыгал бы между вариантами, и
        сравнивать метрики стало бы бессмысленно
      - 'Случайное число не даёт ровно 10%, а хеш даёт точное деление'
    correct: 1
    explanation: >-
      Детерминированный хеш закрепляет пользователя за одним вариантом: опыт
      остаётся связным, а метрики версий сравнимыми.
  - question: >-
      Средняя задержка сервиса 900 мс, и она держится ровно неделю. Жалобы всё
      равно идут. Куда смотреть?
    options:
      - >-
        В p95 и p99: хвост показывает тех, кто ждёт 6 секунд, а среднее их
        прячет
      - 'В общее число запросов: жалоб больше просто потому, что трафика больше'
      - 'В цену запроса: медленные запросы всегда дороже'
    correct: 0
    explanation: >-
      Среднее размазывает редкие долгие запросы по массе быстрых. Боль живёт в
      хвосте, поэтому на дашборде стоят перцентили, а не одно среднее.
---

Урок начался с прототипа, который работает на одном госте, и закончился сервисом, который держит 400 запросов и не разоряет тебя за неделю. Дальше в курсе это станет фоном: любой агент, любой RAG-пайплайн, любая цепочка инструментов живёт внутри этих же семи компонентов — guardrails, кеш, роутер шаблонов, повторы, фолбэк, логи, метрики.

Спотыкаются обычно на трёх местах. Путают «повторить» и «провалиться»: повтор лечит только временный отказ. Забывают, что попадание кеша срезает середину пайплайна, но не лог. И читают среднее вместо хвоста — а жалобы приходят именно из хвоста.

Проверь себя на четырёх вопросах: они про решения, а не про определения.
