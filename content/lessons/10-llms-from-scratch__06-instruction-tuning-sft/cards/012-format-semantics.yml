- concept: однооборотная схема с полями instruction/input/output не вмещает многоходовой диалог, для него нужен формат со списком реплик
  question: 'Есть диалог из 6 реплик: пользователь и ассистент чередуются, причём поздние вопросы опираются на ранние ответы. Нужно сохранить его в датасете для SFT. Что произойдёт при записи в схему с полями instruction / input / output?'
  explanation: 'Alpaca-схема (instruction / input / output, где input необязателен) физически описывает одну пару «задание — ответ». Многоходовой диалог требует формата с упорядоченным списком реплик и полем роли — ShareGPT (conversations с from/value) или ChatML. Склейка 6 реплик в одно поле output теряет разметку ролей: модель будет учиться генерировать и вопросы пользователя тоже.'
  kind: choice
  options:
    - 'Запись пройдёт без потерь: все 6 реплик уложатся в поле output через переносы строк, роли сохранятся'
    - Схема хранит один обмен, поэтому диалог придётся либо разрезать на отдельные примеры, либо взять формат со списком реплик (conversations)
    - Схема сама развернёт список ролей, если добавить в input номер хода
    - Такой диалог вообще нельзя использовать в SFT — многоходовые данные для дообучения непригодны
  correct: 1
  id: 012-format-semantics-1
  fingerprint: '71078203'
- concept: разделители ChatML — это специальные токены, добавляемые в словарь токенизатора, а не обычный текст
  question: Формат ChatML размечает роли метками вида `<|im_start|>` и `<|im_end|>`. Что нужно сделать с этими метками на стороне токенизатора при дообучении?
  explanation: Метки ChatML вводят в словарь токенизатора при дообучении. Иначе `<|im_start|>` распадётся на случайный набор подслов, граница роли перестанет быть надёжным сигналом и будет конкурировать с обычным текстом, который может выглядеть похоже.
  kind: choice
  options:
    - 'Ничего: любая строка и так разбивается токенизатором, разметка работает как обычный текст'
    - Добавить их в словарь как отдельные специальные токены, чтобы каждая граница роли была одним неделимым токеном
    - Запретить их появление, заменив на пробелы перед подачей в модель
    - Хранить их отдельным файлом метаданных и не подавать в последовательность вовсе
  correct: 1
  id: 012-format-semantics-2
  fingerprint: 9d2e9f6a
- concept: при конвертации между форматами инвариантны роли, порядок и содержание реплик, а меняется точная строка и последовательность токенов
  question: Один и тот же разговор записали в двух разных форматах данных для SFT. Объясни своими словами, что при такой конвертации остаётся неизменным, а что меняется, и почему различать эти два слоя важно на практике.
  explanation: Форматы (поля instruction/input/output, список conversations с from/value, метки `<|im_start|>` … `<|im_end|>`) кодируют одну и ту же информацию — кто что сказал. Разница только в разделителях. Смысловой слой переносится между ними без потерь, если формат вообще способен вместить нужное число реплик; слой сериализации задаёт конкретные токены разметки.
  kind: open
  reference: 'Неизменен смысловой слой: набор реплик, их роли (system / user / assistant), порядок реплик и текст каждой из них. Меняется слой записи: имена полей, разделители, служебные метки, переносы строк — то есть точная строка, а после токенизации точная последовательность токенов, которую видит модель. Различать слои нужно потому, что обучать модель можно только на плоской последовательности токенов, а не на абстрактной структуре: конвертация форматов не меняет содержания урока для модели, но меняет то, по каким сигналам она опознаёт границы ролей и место, где начинается её собственный ответ.'
  id: 012-format-semantics-3
  fingerprint: 21e8215f
