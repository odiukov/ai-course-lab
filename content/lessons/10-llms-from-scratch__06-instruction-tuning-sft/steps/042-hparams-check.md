---
id: 042-hparams-check
type: check
title: 'Выбираем безопасный режим между lr 1e-4, 2e-5 и 1e-6'
source_anchor: '## Exercises'
check:
  - question: >-
      Ты трижды обучил одну модель на одинаковых данных и с одинаковым числом
      эпох, меняя только learning rate. По какому признаку выбрать режим?
    options:
      - 'по прогону, чья кривая loss упала первой на старте обучения'
      - 'по прогону, чей loss снижался устойчиво и оказался самым низким к концу'
      - >-
        по прогону, чей loss почти не менялся: значит предобученные веса не
        пострадали
    correct: 1
    explanation: >-
      Эффектный старт при 1e-4 ничего не решает: если затем loss оказывается
      выше, шаг был слишком резким и обучение проскочило нужное положение.
      Неподвижная кривая при 1e-6 означает не бережность, а отсутствие обучения.
      Остаётся 2e-5 — заметное, но контролируемое обновление весов.
---

Ты сравниваешь три прогона с одинаковыми данными, числом эпох и остальными настройками. Здесь легко выбрать `1e-4`: его кривая эффектно падает первой. Но важен не старт, а итог. Если затем loss становится выше, крупный шаг оказался слишком резким. При `1e-6` веса почти не сдвигаются. Поэтому среди этих трёх режимов безопасная отправная точка — `2e-5`: достаточно быстро для обучения и достаточно бережно для предобученных весов, как обсуждалось в [шаге 39](#step-39).

> 🎒 **На пальцах.** Ты настраиваешь резкость фото кольцом объектива. Поворот на `1e-4` быстро проскакивает нужное положение, а `1e-6` почти ничего не меняет. `2e-5` подводит к резкости заметными, но контролируемыми движениями. Выбирай не самое размашистое движение, а самый ясный итоговый кадр.
