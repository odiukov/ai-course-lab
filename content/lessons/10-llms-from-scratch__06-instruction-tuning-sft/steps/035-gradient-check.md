---
id: 035-gradient-check
type: check
title: Проверяем нулевые градиенты prompt-позиций и нулевую сумму по vocabulary
source_anchor: '### Step 4: SFT Training Loop'
check:
  - question: >-
      Что должно получиться в градиенте logits на позиции инструкции, где маска
      равна нулю?
    options:
      - Все значения по словарю равны нулю
      - Только значение целевого токена равно нулю
      - 'Значения ненулевые, но их сумма равна нулю'
    correct: 0
    explanation: >-
      Маска умножает весь вектор градиента этой позиции на ноль, поэтому ни один
      токен словаря не передаёт обучающий сигнал.
  - question: >-
      На позиции ответа сумма градиентов по всему словарю равна нулю. Что это
      означает?
    options:
      - Модель не обучается на этой позиции
      - >-
        Отрицательный вклад целевого токена уравновешивается положительными
        вкладами остальных
      - Все вероятности токенов стали одинаковыми
    correct: 1
    explanation: >-
      На позиции ответа отдельные градиенты обычно ненулевые, но их
      положительные и отрицательные значения взаимно компенсируются.
---

Здесь ты проверяешь два разных нуля. После функции из [шага 34](#step-34) на prompt-позиции должен быть нулевым весь вектор по словарю: маска полностью отключила обучение. На позиции ответа вектор обычно ненулевой, но сумма его элементов должна быть нулевой — с небольшой погрешностью вычислений. Частая ошибка — принять нулевую сумму за отсутствие градиента.

> 🎒 **На пальцах.** Представь ряд банок с монетами. На prompt-позиции кассир вообще не открывает ящик: во всех банках изменение нулевое. На позиции ответа он перекладывает монеты между банками: в одной становится меньше, в других больше. Общая сумма изменений остаётся нулевой, хотя движение произошло.
