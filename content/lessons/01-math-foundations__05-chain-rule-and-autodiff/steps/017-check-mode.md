---
id: 017-check-mode
type: check
title: 'Проверка: какой режим выбрать для функции с 1 входом и 1000 выходов'
source_anchor: '### Forward Mode vs Reverse Mode'
check:
  - question: >-
      У функции один вход и 1000 выходов. Какой режим посчитает все производные
      быстрее?
    options:
      - 'Прямой: один проход от входа даст сразу все 1000 производных'
      - 'Обратный: он всегда быстрее, поэтому его и используют в нейросетях'
      - 'Оба одинаково: количество проходов от числа входов и выходов не зависит'
      - 'Прямой, но проходов всё равно понадобится 1000 — по одному на выход'
    correct: 0
    explanation: >-
      Прямой режим запускается один раз на каждый вход. Вход один, значит проход
      один, и по пути он несёт производные всех 1000 выходов сразу.
  - question: >-
      Сколько проходов понадобилось бы обратному режиму на той же функции с 1
      входом и 1000 выходов?
    options:
      - 'Один: обратный проход всегда раздаёт градиенты всем сразу'
      - '1000: обратный режим запускается отдельно для каждого выхода'
      - 'Два: один вперёд и один назад'
    correct: 1
    explanation: >-
      Обратный режим стартует с одного выхода, ставя его производную по себе
      равной единице. Выходов 1000, значит и стартов 1000.
---

На шаге про миллион входов и одну лампочку обратный режим победил. Но это не свойство обратного режима вообще — это свойство формы задачи. Здесь ты проверяешь, умеешь ли ты выбирать режим по числу входов и выходов, а не по привычке.

Правило простое: прямому режиму нужен один проход на каждый **вход**, обратному — один проход на каждый **выход**. Считаешь, чего меньше, — тот режим и берёшь.

Спотыкаются обычно на том, что «обратный режим = правильный режим». В нейросетях он правильный потому, что там весов миллионы, а потеря одна. Переверни пропорцию — и выгода перевернётся вместе с ней.
