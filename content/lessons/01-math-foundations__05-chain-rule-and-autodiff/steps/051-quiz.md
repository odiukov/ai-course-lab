---
id: 051-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: Что означает define by run в системе с динамическим вычислительным графом?
    options:
      - Граф создаётся один раз перед запуском и больше не меняется
      - >-
        Граф строится из операций, фактически выполненных в текущем прямом
        проходе
      - Граф появляется только после вызова backward
      - Граф содержит только обучаемые параметры
    correct: 1
    explanation: >-
      Динамический граф перестраивается при каждом прямом проходе и фиксирует
      выполненные операции. Это позволяет использовать обычные условия и циклы
      языка программирования.
  - question: Что представляют части a и b в дуальном числе a + b·ε?
    options:
      - 'a — значение, b — производная'
      - 'a — производная, b — значение'
      - a и b — два независимых входа
      - 'a — градиент выхода, b — номер операции'
    correct: 0
    explanation: >-
      Дуальное число переносит пару: обычная часть a содержит значение, а
      коэффициент b при ε содержит производную.
  - question: Как autograd получает градиент выражения?
    options:
      - Случайно возмущает параметры до совпадения результатов
      - >-
        Записывает выполненные операции и применяет их локальные производные по
        цепному правилу
      - Заранее хранит производные всех возможных программ
      - Заменяет каждую операцию её численной конечной разностью
    correct: 1
    explanation: >-
      Autograd строит граф выполненных операций, а затем соединяет их локальные
      производные по цепному правилу. Конечные разности для самого вычисления
      градиента не нужны.
  - question: Какое описание соответствует многослойному перцептрону?
    options:
      - >-
        Сеть со скрытыми слоями, где нейроны применяют активацию к взвешенной
        сумме со смещением
      - Только одна линейная операция без скрытых нейронов
      - Алгоритм сортировки узлов вычислительного графа
      - Способ численно сравнивать две реализации производной
    correct: 0
    explanation: >-
      MLP состоит из одного или нескольких скрытых слоёв. Каждый его нейрон
      вычисляет взвешенную сумму входов со смещением и применяет функцию
      активации.
---

Ты прошёл путь от «производная сложной функции — это произведение локальных производных» до работающего движка, который считает это сам: граф из узлов `Value`, топологический порядок, накопление градиентов и `backward()`.

Дальше в курсе это станет фундаментом. Обучение любой сети — это тот же цикл из шага 48: обнулить, прямой проход, `backward`, шаг по весам. Всё, что добавится потом — новые слои, оптимизаторы, регуляризация — меняет только детали внутри этого цикла, а не сам механизм.

Обычно спотыкаются на трёх местах. Первое: путают, кто чей градиент — правило из шага 36 (твой градиент это значение соседа) забывается быстрее всех. Второе: забывают обнулять градиенты и потом долго ищут, почему обучение расходится. Третье: выбирают режим по привычке, а не по числу входов и выходов.
