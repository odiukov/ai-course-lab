---
id: 002-write-contract
type: check
title: Проверяем контракт записи и хвоста
source_anchor: '### Resizable HDF5 done right'
check:
  - question: >-
      chunk_size равен 8192, а в шарде записано 20 000 токенов. Читатель
      открывает датасет и читает всё до конца выделенной длины, не глядя на
      token_count. Что он получит?
    options:
      - 'Ровно 20 000 токенов: HDF5 сам обрезает лишнее место'
      - >-
        20 000 токенов и ещё 4576 нулей в конце — до границы последнего
        расширения
      - '16 384 токена: незаполненный хвост вообще не читается'
      - 'Ошибку чтения, потому что последний диапазон записан не полностью'
    correct: 1
    explanation: >-
      Датасет расширялся шагами по 8192, поэтому места выделено 24 576. Реальных
      токенов 20 000, остальные 4576 позиций остались нулями — и без token_count
      читатель заберёт их как настоящие токены.
  - question: Какая из записей в шард НЕ будет выровнена по границе чанка?
    options:
      - 'Первая: датасет ещё пустой, и выравнивать нечего'
      - 'Любая, где буфер заполнился ровно до chunk_size'
      - 'Только последняя — та, куда уходит остаток буфера в конце шарда'
      - 'Каждая вторая, потому что расширение и запись идут по разным смещениям'
    correct: 2
    explanation: >-
      Пока буфер заполняется целиком, датасет растёт ровно на chunk_size и
      запись ложится в новый чанк целиком. Выбивается только финальный остаток —
      он короче чанка, и именно из-за него нужен token_count.
---

Прежде чем писать код, стоит проверить одну вещь: держишь ли ты в голове разницу между «сколько места выделено» и «сколько токенов реально записано». Это два разных числа, и именно на их расхождении спотыкаются чаще всего.

Здесь обычно два промаха. Первый — считать, что длина датасета и есть количество токенов; она кратна `chunk_size` и почти всегда больше. Второй — забыть, что финальный остаток буфера — единственная запись, которая не заполняет чанк до конца, и что ровно для неё в атрибутах живёт `token_count`.

Ответь на два вопроса — и пойдём кодировать текст в id.
