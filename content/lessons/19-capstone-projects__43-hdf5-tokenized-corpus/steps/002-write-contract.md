---
id: 002-write-contract
type: check
title: Проверяем контракт записи и хвоста
source_anchor: '### Resizable HDF5 done right'
check:
  - question: >-
      Как следует настроить одномерный HDF5-датасет, если его итоговая длина
      заранее неизвестна, но размер физических чанков должен оставаться
      фиксированным?
    options:
      - 'Задать maxshape=(None,) и chunks=(chunk_size,)'
      - Задать maxshape равным начальной длине и отключить чанки
      - 'Не задавать maxshape, а после каждой записи создавать датасет заново'
      - 'Задать chunks=(None,), а maxshape сделать равным chunk_size'
    correct: 0
    explanation: >-
      maxshape=(None,) разрешает изменять логическую длину датасета, а
      chunks=(chunk_size,) фиксирует размер блоков его физического хранения.
  - question: >-
      Размер чанка равен 4096, а после полных сбросов в буфере осталось 1700
      токенов. Как должен поступить финальный сброс?
    options:
      - >-
        Увеличить логическую длину датасета на 1700 и записать весь остаток в
        следующий непрерывный диапазон
      - >-
        Увеличить логическую длину на 4096 и обязательно заполнить свободные
        позиции нулями
      - 'Отбросить остаток, поскольку записывать разрешено только полные чанки'
      - Перенести остаток в начало предыдущего чанка
    correct: 0
    explanation: >-
      Размер чанка не обязывает логическую длину датасета быть ему кратной. При
      закрытии шарда хвост записывается как последний неполный диапазон.
---

Прежде чем писать код, стоит проверить одну вещь: держишь ли ты в голове разницу между «сколько места выделено» и «сколько токенов реально записано». Это два разных числа, и именно на их расхождении спотыкаются чаще всего.

Здесь обычно два промаха. Первый — считать, что длина датасета и есть количество токенов; она кратна `chunk_size` и почти всегда больше. Второй — забыть, что финальный остаток буфера — единственная запись, которая не заполняет чанк до конца, и что ровно для неё в атрибутах живёт `token_count`.

Ответь на два вопроса — и пойдём кодировать текст в id.
