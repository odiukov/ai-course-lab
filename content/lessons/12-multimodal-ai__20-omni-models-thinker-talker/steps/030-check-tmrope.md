---
id: 030-check-tmrope
type: check
title: 'Проверка: что видит модель при жесте на t=1.2 с'
source_anchor: '### TMRoPE — time-aligned multimodal positions'
check:
  - question: >-
      Человек махнул рукой и сказал «привет». Видеокадр с жестом получил
      таймстемп 1.2 с, аудиотокены со словом — 1.21-1.28 с. Что позволяет модели
      понять, что это один момент?
    options:
      - >-
        Токены идут подряд в последовательности, поэтому модель считает их
        связанными
      - >-
        У всех токенов есть абсолютный таймстемп на общей шкале, и модель просто
        сравнивает числа
      - Видео и аудио заранее склеивают в один поток на 4 кадрах в секунду
      - 'Thinker переспрашивает у Talker, что было в этот момент'
    correct: 1
    explanation: >-
      Связывает не порядок в последовательности, а таймстемп: 1.2 и 1.21
      отличаются на 10 мс, и для модели это один миг — независимо от того, из
      какой модальности пришёл токен.
  - question: >-
      Видео идёт на 4 кадрах в секунду, аудио — на 50. Сколько аудиокадров
      приходится на один видеокадр и почему это важно?
    options:
      - >-
        Примерно 12 — и без общей шкалы непонятно, какие именно 12 относятся к
        этой картинке
      - Примерно 50 — аудио дублируется на каждый кадр видео
      - Ровно 4 — частоты выравнивают до меньшей
      - По одному — модель берёт только ближайший аудиокадр
    correct: 0
    explanation: >-
      50 делить на 4 — это 12,5 аудиокадров на картинку. Общая временная шкала и
      нужна, чтобы модель знала, какой звук лежит внутри интервала конкретного
      кадра.
---

После этого куска ты должен уметь взять момент времени — скажем, 1.2 с — и сказать, какие токены из трёх потоков в него попадают и почему модель считает их одновременными. Это про механику из [шага 25](#step-25) и [шага 27](#step-27), не про новое.

Спотыкаются обычно на двух вещах. Первая: кажется, что близость токенов в последовательности и есть выравнивание. Нет — порядок ничего не гарантирует, работает только сравнение таймстемпов. Вторая: разные частоты воспринимают как проблему, которую надо чинить приведением к одной. Их не приводят — просто на картинку приходится примерно дюжина аудиокадров, и это нормально, пока у всех есть время.
