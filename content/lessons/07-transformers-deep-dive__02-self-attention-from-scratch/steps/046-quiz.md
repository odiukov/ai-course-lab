---
id: 046-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      У тебя 6 токенов, эмбеддинг длиной 8, а матрицы проекций переводят в
      размерность 4. Какого размера получится матрица оценок до softmax?
    options:
      - 6 строк на 6 столбцов
      - 6 строк на 4 столбца
      - 8 строк на 8 столбцов
      - 4 строки на 4 столбца
    correct: 0
    explanation: >-
      Q и K оба имеют 6 строк и 4 столбца, а перемножается Q с транспонированным
      K: 4 столбца стыкуются с 4 строками, снаружи остаются 6 и 6. Число строк и
      столбцов задают токены, а не размерность.
  - question: >-
      Ты убрал деление на корень из dk и оставил всё остальное как было, при dk
      = 64. Что произойдёт с весами attention?
    options:
      - 'Веса станут почти одинаковыми, все примерно по 1 делить на число токенов'
      - >-
        Оценки разъедутся, softmax отдаст почти всю единицу одному токену, а
        градиент по остальным почти исчезнет
      - 'Ничего не изменится: softmax всё равно нормирует сумму к единице'
      - Сумма весов в строке перестанет быть равной единице
    correct: 1
    explanation: >-
      Скалярное произведение складывает 64 слагаемых, поэтому оценки растут по
      модулю. Softmax от больших разниц насыщается: один вес около единицы,
      остальные почти нули, и учиться становится нечем.
  - question: 'Softmax применили по столбцам, а не по строкам. Почему это ломает смысл?'
    options:
      - Сумма перестанет быть равной единице
      - Числа станут отрицательными
      - >-
        Единицу начнут делить между запросами за одну и ту же позицию, а не
        между тем, куда смотрит один токен
      - Матрица перестанет быть квадратной
    correct: 2
    explanation: >-
      Строка — это один токен и его распределение внимания по всем позициям.
      Нормировать надо именно её, иначе доли считаются поперёк и складываются
      веса разных запросов.
  - question: >-
      Причинная маска ставит минус бесконечность выше диагонали. Что это даёт
      после softmax?
    options:
      - >-
        Токен на позиции 3 получает нулевой вес на позициях 4, 5, 6 и опирается
        только на себя и предыдущих
      - Токен на позиции 3 видит только позицию 3
      - Все веса в строке становятся одинаковыми
      - Из матрицы физически удаляются лишние столбцы
    correct: 0
    explanation: >-
      Экспонента от минус бесконечности равна нулю, поэтому будущие позиции
      получают вес 0 и не попадают во взвешенную сумму values. Форма матрицы при
      этом не меняется.
---

Ты прошёл весь путь от «пятьдесят человек в цепочке» из [шага 1](#step-1) до готовой функции в [шаге 34](#step-34) — и дальше в курсе именно этот кусок будет фундаментом: трансформер целиком собирается из повторов того, что ты уже написал руками.

Что понадобится держать в голове: формы матриц стыкуются по внутренней размерности, оценки всегда N×N по числу токенов, softmax нормирует строку, а маска гасит будущее нулями.

Спотыкаются обычно на трёх местах: путают, кто задаёт вопрос (Q), а кто отвечает меткой (K); считают, что N×N зависит от размерности эмбеддинга; забывают, что softmax по столбцам даёт правильную сумму, но неправильный смысл.
