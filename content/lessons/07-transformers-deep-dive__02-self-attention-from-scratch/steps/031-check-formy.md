---
id: 031-check-formy
type: check
title: 'Проверка: какие формы на входе и выходе'
source_anchor: '### Step 2: Scaled dot-product attention'
check:
  - question: >-
      У тебя 12 токенов, dk = 64. Какой формы будет матрица, которая выходит из
      softmax?
    options:
      - 12 строк на 64 столбца
      - 12 строк на 12 столбцов
      - 64 строки на 64 столбца
      - 64 строки на 12 столбцов
    correct: 1
    explanation: >-
      Q размера 12 на 64 умножается на K с транспозицией размера 64 на 12,
      внутренние 64 схлопываются, остаётся 12 на 12. Softmax считается по
      строкам и форму не меняет.
  - question: >-
      Те же 12 токенов, но V сделали шире: dk = 64, а dv = 32. Какой формы будет
      выход attention?
    options:
      - 12 строк на 64 столбца
      - 12 строк на 32 столбца
      - 12 строк на 12 столбцов
      - 32 строки на 12 столбцов
    correct: 1
    explanation: >-
      Веса 12 на 12 умножаются на V размера 12 на 32: строк столько же, сколько
      токенов, а столбцов столько, сколько их в V. Ширину выхода задаёт V, а не
      dk.
---

Останови руку перед тем, как запускать функцию из [шага 30](#step-30), и скажи вслух, какой формы будет каждая из четырёх промежуточных матриц. Если получается — весь конвейер из [шага 29](#step-29) у тебя в голове держится не на памяти, а на правиле стыковки форм из [шага 9](#step-9).

Спотыкаются обычно в двух местах. Первое: после умножения на K с транспозицией ждут матрицу с dk столбцами — а dk как раз схлопывается, и остаётся квадрат «токены на токены». Второе: думают, что выход обязан быть шириной dk. Нет — ширину выхода диктует V, потому что выход складывается именно из строк V.

Держи в голове одно: строк всегда столько, сколько токенов. Меняется только ширина.
