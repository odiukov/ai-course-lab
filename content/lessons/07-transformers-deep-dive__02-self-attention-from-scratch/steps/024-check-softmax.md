---
id: 024-check-softmax
type: check
title: 'Проверка: softmax от [2.0, 1.0, 0.1] вручную'
source_anchor: '### Step 1: Softmax from scratch'
check:
  - question: >-
      После вычитания максимума из логитов [2.0, 1.0, 0.1] получился вектор [0,
      -1, -1.9]. Какая запись правильно продолжает вычисление softmax?
    options:
      - >-
        [exp(0), exp(-1), exp(-1.9)] нужно разделить на сумму этих трёх
        экспонент
      - '[0, -1, -1.9] нужно разделить на сумму исходных логитов'
      - '[exp(0), exp(-1), exp(-1.9)] нужно разделить на количество логитов'
      - 'Достаточно вычислить экспоненты: дополнительная нормализация не нужна'
    correct: 0
    explanation: >-
      После сдвига экспоненты равны примерно 1, 0.368 и 0.150. Softmax делит
      каждую из них на их общую сумму 1.518, поэтому итоговые доли складываются
      в единицу.
  - question: >-
      Ученик получил экспоненты 1, 0.368 и 0.150, а затем разделил каждую на
      количество элементов. В чём ошибка?
    options:
      - 'Знаменателем должна быть сумма экспонент, а не количество элементов'
      - Перед делением нужно вернуть к каждому логиту вычтенный максимум
      - Делить нужно только на наибольшую экспоненту
      - Экспоненты следовало вычислять только для положительных логитов
    correct: 0
    explanation: >-
      В softmax используется один общий знаменатель — сумма всех экспонент.
      Здесь это 1 + 0.368 + 0.150 = 1.518; деление на него даёт примерно 0.659,
      0.242 и 0.099.
---

Здесь проверяется одно умение: пройти softmax руками, без numpy, и получить те же три числа, что и код из [шага 22](#step-22). Порядок жёсткий — вычесть максимум, взять экспоненты, поделить на их сумму.

Спотыкаются обычно в двух местах. Первое: вычитают максимум и думают, что тем самым «портят» ответ — нет, доли остаются прежними, это разобрано в [шаге 21](#step-21). Второе: делят не на ту величину — на количество элементов, на максимум, на что угодно, кроме суммы экспонент. Знаменатель всегда один и тот же для всей строки, и именно поэтому доли складываются в единицу.
