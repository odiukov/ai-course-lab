- concept: линейная проекция decoder заменяет последнюю ось hidden_dim на размер целевого словаря, сохраняя оси батча и последовательности
  question: Тензор `out` имеет форму `(B, T, H)`, а выходной слой задан как `nn.Linear(H, V)`. Какую форму получит `logits` после вызова `self.fc(out)`?
  explanation: '`nn.Linear` применяется к последней оси тензора: размер `H` заменяется на `V`, а оси батча `B` и длины последовательности `T` сохраняются.'
  kind: choice
  options:
    - '`(B, V)`'
    - '`(T, V)`'
    - '`(B, H, V)`'
    - '`(B, T, V)`'
  correct: 3
  id: 017-decode-step-1
  fingerprint: 1032c883
- concept: число весов выходной проекции decoder равно произведению hidden_dim и размера целевого словаря
  question: Выходной слой decoder задан как `nn.Linear(384, 8192)`. Сколько скалярных весов содержит его матрица без учёта смещения?
  explanation: 'Матрица проекции содержит по одному весу для каждой пары «скрытый признак — токен словаря»: 384 × 8192 = 3 145 728 весов.'
  kind: numeric
  answer: 3145728
  tolerance: 0.5
  id: 017-decode-step-2
  fingerprint: '33000870'
