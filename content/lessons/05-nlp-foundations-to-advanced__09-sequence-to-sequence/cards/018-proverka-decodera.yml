- concept: размер выхода Embedding должен совпадать с input_size слоя GRU
  question: Заполни пропуск в объявлении GRU, получающей векторы непосредственно от слоя Embedding.
  explanation: Embedding заменяет каждый индекс токена вектором длины embed_dim, поэтому именно embed_dim должен быть входным размером GRU. Размер hidden_dim при этом может отличаться.
  kind: cloze
  template: |-
    self.embed = nn.Embedding(tgt_vocab_size, embed_dim)
    self.gru = nn.GRU(___, hidden_dim, batch_first=True)
  answer: embed_dim
  accept:
    - embed_dim
  id: 018-proverka-decodera-1
  fingerprint: 5823d630
- concept: batch_first не меняет порядок осей hidden state в PyTorch GRU
  question: Однослойный однонаправленный GRU-decoder обрабатывает один токен для каждого элемента батча. Как расположены оси token и hidden при batch_first=True?
  explanation: batch_first=True переставляет оси только у входов и выходов GRU. Формат hidden остаётся [число слоёв, размер батча, hidden_dim].
  kind: choice
  options:
    - token имеет форму [batch, sequence], а hidden — [layers, batch, hidden_dim]
    - token имеет форму [sequence, batch], а hidden — [batch, layers, hidden_dim]
    - token и hidden всегда имеют форму [batch, sequence, hidden_dim]
    - token имеет форму [batch], а hidden — [batch, hidden_dim, layers]
  correct: 0
  id: 018-proverka-decodera-2
  fingerprint: 229ee179
