---
id: 005-proverka-arkhitektury
type: check
title: 'Проверка: роли encoder, decoder и context vector'
source_anchor: '## The Concept'
check:
  - question: Как context vector участвует в работе классического decoder без attention?
    options:
      - Сам последовательно выбирает слова перевода.
      - Используется для инициализации скрытого состояния decoder.
      - Заменяет целевой словарь во время генерации.
    correct: 1
    explanation: >-
      Context vector — это финальное скрытое состояние encoder и сжатое
      представление входа. Он инициализирует decoder, а дальнейшие вычисления и
      выбор токенов выполняет decoder.
  - question: >-
      Модель должна получить вероятности возможных следующих слов перевода.
      Какая часть модели их вычисляет?
    options:
      - Decoder по целевому словарю.
      - Encoder по исходному словарю.
      - Context vector без участия нейросети.
    correct: 0
    explanation: >-
      На каждом шаге decoder выдаёт распределение по целевому словарю. Из него
      выбирают следующий токен, который затем подают decoder на следующем шаге.
---

Здесь легко перепутать участника, который понимает вход, с участником, который создаёт выход. Держи в голове направление движения информации из [шага 3](#step-3): encoder читает исходную последовательность, context vector переносит её сжатую сводку, а decoder разворачивает эту сводку в новую последовательность слово за словом.

Особенно внимательно следи за формулировкой «context vector генерирует перевод». Сам вектор ничего не генерирует: это данные, которыми encoder инициализирует decoder. А ограничение из [шага 4](#step-4) возникает именно потому, что размер этой сводки не растёт вместе с предложением.
