---
id: 043-itogovaya-proverka
type: quiz
title: >-
  Итоговая проверка: encoder-decoder, teacher forcing, лосс и стратегии
  генерации
source_anchor: '## Key Terms'
check:
  - question: >-
      На длинных входах классическая seq2seq-модель начинает терять детали.
      Какое изменение точнее всего бьёт в причину проблемы?
    options:
      - Разрешить decoder обращаться ко всем hidden states encoder
      - Увеличить долю padding в обучающих батчах
      - Всегда выбирать токен с максимальной вероятностью
      - Начинать генерацию с EOS вместо BOS
    correct: 0
    explanation: >-
      Последнее состояние становится информационным бутылочным горлышком. Доступ
      ко всем состояниям позволяет decoder брать сведения из нужных позиций.
  - question: >-
      На обучении decoder почти всегда получает правильный предыдущий токен, а
      при генерации — собственный. Что может произойти?
    options:
      - >-
        Ошибки начнут накапливаться, потому что модель не тренировалась
        исправлять собственные промахи
      - Encoder перестанет создавать hidden states
      - Padding начнёт влиять на softmax
      - Beam search превратится в greedy decoding
    correct: 0
    explanation: >-
      Это разрыв между обучением и инференсом: одна ранняя ошибка меняет
      следующие входы decoder и может потянуть за собой новые ошибки.
  - question: >-
      В батче одна целевая строка закончилась раньше остальных и дополнена
      padding. Как правильно считать sequence cross-entropy?
    options:
      - Игнорировать позиции padding с помощью маски
      - Считать padding обычным целевым токеном
      - Удалить из батча все короткие строки
      - Считать лосс только для последнего токена каждой строки
    correct: 0
    explanation: >-
      Маска оставляет в лоссе только настоящие целевые токены. Иначе модель
      наказывается или вознаграждается за техническое выравнивание длины.
  - question: >-
      Greedy decoding выбрал самый вероятный первый токен, но итоговая строка
      оказалась хуже другой возможной строки. Почему beam search может помочь?
    options:
      - >-
        Он временно сохраняет несколько частичных последовательностей и
        сравнивает их продолжения
      - Он заменяет decoder новым encoder
      - Он обучает модель заново без teacher forcing
      - Он гарантирует найти идеальную строку при любой ширине луча
    correct: 0
    explanation: >-
      Локально лучший токен не гарантирует лучшую последовательность целиком.
      Луч сохраняет несколько перспективных путей, хотя не даёт абсолютной
      гарантии.
---

Дальше тебе понадобится видеть encoder-decoder не как две отдельные коробки, а как единый путь: источник превращается в состояния, decoder порождает токены, маскированный лосс обучает весь путь, а стратегия декодирования определяет итоговую строку. Главные ловушки здесь связаны не с вычислениями, а с несовпадениями: teacher forcing и реальная генерация дают разные входы, padding искажает лосс без маски, а лучший токен на одном шаге не обязательно ведёт к лучшей последовательности. Если ты замечаешь такое несовпадение, то уже понимаешь, где искать причину поведения модели.
