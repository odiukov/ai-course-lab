---
id: 012-forward-check
type: check
title: >-
  Объясните, почему для получения x_t не нужно выполнять t последовательных
  шагов
source_anchor: '### Step 2: sample `x_t` in one shot'
check:
  - question: >-
      Почему forward_sample может сразу получить x_t, не моделируя все
      предшествующие переходы?
    options:
      - >-
        Накопленный коэффициент α̅_t уже объединяет влияние предшествующих
        переходов, поэтому достаточно смешать исходное значение с одним
        гауссовым шумом
      - >-
        Для состояния x_t имеет значение только шум, добавляемый на последнем
        переходе
      - >-
        Предшествующие переходы можно пропустить, потому что прямой процесс не
        изменяет распределение данных
    correct: 0
    explanation: >-
      В α̅_t свёрнут суммарный эффект всех переходов до момента t. Замкнутая
      формула поэтому сразу даёт выборку из нужного распределения x_t без
      вычисления промежуточных состояний.
---

Представь, что тебе нужен только кадр на шаге 700, а не вся история его зашумления. Накопленная величина из [шага 6](#step-6) уже сжала влияние первых 700 переходов в одно число. Поэтому формула из [шага 10](#step-10) позволяет сделать одно вычисление и получить случайный \(x_t\) с тем же распределением, что и после длинной цепочки. Это не тот же конкретный маршрут, но статистически правильный результат.

> 🎒 **На пальцах.** Навигатор может показать, где ты окажешься через 700 метров, не рисуя на экране каждое движение колеса. Итоговая точка учитывает весь пройденный отрезок. Так и \(\bar{\alpha}_t\): она хранит суммарный эффект предыдущих шагов, поэтому ради одного \(x_t\) прокручивать их по очереди не требуется.
