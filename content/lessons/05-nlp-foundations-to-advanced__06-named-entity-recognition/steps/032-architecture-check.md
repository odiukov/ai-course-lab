---
id: 032-architecture-check
type: check
title: 'Проверка: что добавляют CRF, BiLSTM и трансформер'
source_anchor: '### Step 5: what a BiLSTM-CRF adds'
check:
  - question: >-
      Что прежде всего заменяет BiLSTM при переходе от CRF с ручными признаками
      к BiLSTM-CRF?
    options:
      - Ручное конструирование контекстных признаков токенов
      - Набор допустимых BIO-меток
      - Матрицу переходов между соседними метками
    correct: 0
    explanation: >-
      BiLSTM строит контекстные представления токенов из эмбеддингов и тем самым
      заменяет ручные признаки. CRF при этом можно сохранить для моделирования
      последовательности меток.
  - question: 'Что поступает в линейный слой, вычисляющий эмиссии в BiLSTM-голове?'
    options:
      - Только последнее состояние прямого прохода для всего предложения
      - Объединённые состояния прямого и обратного проходов для каждого токена
      - 'Только оценки переходов, уже вычисленные CRF'
    correct: 1
    explanation: >-
      Для каждого токена BiLSTM объединяет скрытые состояния двух направлений.
      Линейный слой преобразует это двустороннее контекстное представление в
      оценки меток.
---

Проверь, можешь ли ты для каждой ошибки назвать компонент, который её исправляет. CRF из [шага 25](#step-25) согласует метки, но сам не учится понимать слова. BiLSTM из [шага 30](#step-30) заменяет ручные признаки выученным контекстом. Трансформер из [шага 31](#step-31) тоже учитывает обе стороны, но работает с subword-частями.

Частая ловушка — приписывать CRF понимание смысла или считать BiLSTM и CRF взаимозаменяемыми. Они решают разные части задачи и могут работать вместе.

> 🎒 **На пальцах.** Представь съёмочную площадку. BiLSTM и трансформер — операторы: они собирают информацию о каждом фрагменте сцены. CRF — монтажёр: он не переснимает материал, зато не допускает, чтобы соседние кадры сложились в нелогичную последовательность.
