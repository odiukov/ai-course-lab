- concept: для расчёта стоимости вход объединяет prompt, tool и memory, а response считается выходом
  question: Заполни выражение для передачи токенов в функцию расчёта стоимости.
  explanation: Prompt, результаты инструментов и память входят в контекст, передаваемый модели, поэтому их складывают как input_tokens. Сгенерированный response относится к output_tokens.
  kind: cloze
  template: input_tokens = ___; output_tokens = response_tokens
  answer: prompt_tokens + tool_tokens + memory_tokens
  accept:
    - tool_tokens + prompt_tokens + memory_tokens
    - memory_tokens + prompt_tokens + tool_tokens
    - prompt_tokens + memory_tokens + tool_tokens
  id: 012-attribute-1
  fingerprint: b9805eb1
