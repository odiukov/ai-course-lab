---
id: 010-check-vid-spana
type: check
title: 'Проверка: какой вид спана здесь'
source_anchor: '### Agent span naming'
check:
  - question: >-
      Ты запускаешь агента с именем Researcher внутри CrewAI — он работает в том
      же процессе, что и твой код. Какое имя и вид спана правильные?
    options:
      - 'Имя invoke_agent Researcher, вид CLIENT'
      - 'Имя invoke_agent Researcher, вид INTERNAL'
      - 'Имя Researcher, вид INTERNAL'
      - 'Имя invoke_agent, вид INTERNAL'
    correct: 1
    explanation: >-
      Имя собирается как invoke_agent плюс имя агента, а раз агент живёт в твоём
      процессе и сетевого вызова нет — вид INTERNAL.
  - question: >-
      Твой код обращается к OpenAI Assistants API: агент крутится на чужих
      серверах. Почему тут важно поставить именно CLIENT, а не INTERNAL?
    options:
      - 'Потому что у удалённых агентов не бывает имени, и CLIENT это показывает'
      - Потому что CLIENT-спаны всегда медленнее и их надо помечать отдельно
      - >-
        Потому что по CLIENT-спанам считают сетевые тайм-ауты и чужие сбои, а
        INTERNAL означает, что тормозит твой код
      - 'Потому что вид спана задаёт, в какое хранилище попадут данные'
    correct: 2
    explanation: >-
      Вид спана отвечает на вопрос «чья это задержка». CLIENT — ушли за границу
      процесса, виноват может быть провайдер; INTERNAL — всё внутри, значит
      разбираться со своим кодом.
---

Здесь проверяется одна связка: по описанию агента собрать имя спана и выбрать вид. Имя — из [шага 8](#step-8), вид — по границе процесса из [шага 9](#step-9).

Спотыкаются обычно на двух вещах. Первая: забывают префикс `invoke_agent` и пишут в имя просто «Researcher» — тогда спан не состыкуется с остальными по договору из [шага 2](#step-2). Вторая: путают «агент удалённый» с «агент сложный». Вид спана не про сложность и не про длительность, а ровно про то, ушёл ли вызов из твоего процесса по сети. Локальный ReAct-цикл может молотить полминуты и всё равно остаётся INTERNAL.
