- concept: TimeSeriesSplit генерирует индексы обучающей и проверочной частей каждого временного фолда
  question: Что возвращает метод tscv.split(X) на каждой итерации?
  explanation: TimeSeriesSplit отвечает за схему разбиения и выдаёт train_index и test_index. По этим индексам отдельно выбирают соответствующие строки из X и y.
  kind: choice
  options:
    - Пару массивов с индексами обучающей и проверочной частей
    - Обученную модель и её прогнозы
    - Среднее значение и стандартное отклонение оценок
  correct: 0
  id: 048-sklearn-split-1
  fingerprint: 925e9564
- concept: объект TimeSeriesSplit можно передать параметру cv функции cross_val_score
  question: Вставьте объект временного разбиения, чтобы cross_val_score использовал walk-forward-валидацию.
  explanation: Параметр cv принимает объект, задающий фолды. Получив TimeSeriesSplit, cross_val_score последовательно обучает и оценивает модель на временных разбиениях, а затем возвращает оценки всех проверок.
  kind: cloze
  template: scores = cross_val_score(model, X, y, cv=___)
  answer: tscv
  accept:
    - TimeSeriesSplit(n_splits=7)
  id: 048-sklearn-split-2
  fingerprint: 88477a5d
