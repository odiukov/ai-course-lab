---
id: 046-round-check
type: check
title: 'Интерпретируем mean_reward, best_reward и std_reward одного раунда'
source_anchor: '### Step 6: Self-Improvement Round'
check:
  - question: >-
      За один промпт получены mean_reward 0,5, best_reward 1,1 и заметный
      std_reward. Что разумнее всего заключить?
    options:
      - Модель всегда отвечает хорошо
      - 'Модель иногда находит сильный ответ, но делает это нестабильно'
      - Группа не даёт обучающего сигнала
    correct: 1
    explanation: >-
      Высокий best_reward показывает доступный модели результат, а более низкий
      mean_reward и разброс — что она достигает его не во всех попытках.
  - question: >-
      mean_reward сравнялся с best_reward, а std_reward стал равен нулю. Что это
      означает для текущей группы?
    options:
      - >-
        Все ответы получили одинаковую награду, поэтому их групповые
        преимущества обнулятся
      - Все ответы обязательно совпадают слово в слово
      - Лучший ответ исчез из группы
    correct: 0
    explanation: >-
      Нулевой разброс означает одинаковые награды, а не обязательно одинаковые
      тексты. Без различий в наградах GRPO некого предпочесть внутри группы.
---

Ты запускаешь раунд из [шага 44](#step-44) и видишь высокий `best_reward`. Победа? Пока нет: если `mean_reward` заметно ниже, модель лишь иногда находит хороший ответ. `std_reward` показывает разброс наград: высокий оставляет различия для обучения, нулевой обнуляет групповые преимущества из [шага 31](#step-31). Важно: одинаковые награды не доказывают, что тексты ответов одинаковы. Эти три числа описывают один раунд, а устойчивое улучшение видно только при сравнении раундов.

> 🎒 **На пальцах.** Ты выпускаешь восемь стрел. Лучшая попала в десятку, но средняя — в шестёрку: меткий выстрел уже возможен, однако нестабилен. Если все стрелы получили одинаковые очки, разброс равен нулю — по этой серии невозможно понять, какое движение стоит закрепить. При этом стрелы могли лежать в разных местах одного кольца.
