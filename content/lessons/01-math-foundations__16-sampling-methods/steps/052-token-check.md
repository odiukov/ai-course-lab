---
id: 052-token-check
type: check
title: 'Проверка: сравнить жадный выбор, T = 0.7, top-k = 3 и top-p = 0.9'
source_anchor: '## Exercises'
check:
  - question: >-
      В серии запусков метод A дал одну и ту же последовательность, а метод B —
      несколько разных. Какой вывод обоснован результатами эксперимента?
    options:
      - >-
        В этой серии метод B показал большее разнообразие, но для общего вывода
        нужны проверки на других входах
      - Метод B всегда создаёт более осмысленный текст
      - Метод A обязательно использовал top-p
      - 'Методы эквивалентны, поскольку длина последовательностей одинакова'
    correct: 0
    explanation: >-
      Серия прогонов позволяет сравнить наблюдаемое разнообразие, но не
      доказывает универсальное превосходство метода или качество его текста.
      Результат зависит от модели, входа и параметров декодирования.
  - question: >-
      Инженер проверяет жадный выбор, температуру 0.7, top-k = 3 и top-p = 0.9,
      но для каждого метода использует разные начальные тексты. Как сделать
      сравнение корректнее?
    options:
      - Для каждого метода увеличить длину последовательности по-разному
      - >-
        Использовать один и тот же начальный текст, модель и длину генерации,
        меняя только способ декодирования
      - Оценить каждый метод только по его самой удачной последовательности
      - Заменить все случайные методы жадным выбором
    correct: 1
    explanation: >-
      В контролируемом сравнении условия генерации должны совпадать. Тогда
      различия в повторяемости и разнообразии можно связать именно со способом
      декодирования.
---

Запусти каждый способ несколько раз с одинаковым началом и сравни не красоту одной строки, а повторяемость и разнообразие. Жадный выбор должен служить контрольной точкой: при неизменных логитах он повторяет результат. Температура 0.7 из [шага 39](#step-39) делает распределение острее, но не запрещает редкие токены полностью. Top-k из [шага 43](#step-43) всегда оставляет трёх кандидатов, а top-p из [шага 46](#step-46) меняет размер ядра вслед за уверенностью модели.

Частая ошибка — считать top-k и top-p самостоятельным выбором токена. Это лишь фильтры: после них вероятности перенормируются, а окончательный выбор остаётся случайным. Поэтому проверяй методы по пяти прогонам, а не по одному удачному примеру.
