---
id: 009-sample-exponential
type: code
title: Пишем sample_exponential
source_anchor: '### Step 1: Uniform and inverse CDF sampling'
exercise_fn: sample_exponential
---

Ты запускаешь таймер для следующего события — например, прихода нового посетителя. Иногда он срабатывает почти сразу, иногда приходится ждать заметно дольше. Простое умножение равномерного числа на константу здесь не подойдёт: оно ограничит время сверху и сделает все участки диапазона одинаково вероятными.

Напиши функцию `sample_exponential(lam)`, которая возвращает одно случайное время из экспоненциального распределения. Внутри понадобятся `random.random()` и `math.log()`. Возьми равномерное число `u`, а затем примени к нему преобразование из [шага 8](#step-8). Параметр `lam` должен быть положительным: при нуле возникнет деление на ноль, а отрицательное значение не задаёт нужное распределение.

```python
import math
import random

def sample_exponential(lam):
    # Проверь, что lam положителен
    # Получи u
    # Преобразуй u и верни результат
    pass
```

Обрати внимание на границу: `random.random()` иногда может вернуть ровно `0`, поэтому вариант с `math.log(u)` способен упасть. Выражение через `1 - u`, полученное в [шаге 8](#step-8), избегает этой проблемы.

Для быстрой самопроверки вызови функцию несколько раз. Результаты должны быть неотрицательными и разными; обычно будет много небольших значений и лишь изредка — крупные. Пока не пытайся доказывать это по среднему: этому посвящён следующий экран.

> 🎒 **На пальцах.** Представь кнопку случайного таймера в кофейне. Ручка `lam` регулирует темп посетителей: чем больше значение, тем чаще звенит таймер и тем короче ожидание. Твоя функция нажимает кнопку один раз и возвращает продолжительность одного ожидания — не расписание всех посетителей сразу.
