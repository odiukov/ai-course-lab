---
id: 049-k-p-check
type: check
title: 'Проверка: какой фильтр лучше ведёт себя в уверенном и неоднозначном контексте'
source_anchor: '### Top-p (Nucleus) Sampling'
check:
  - question: >-
      В уверенном контексте один токен получил вероятность 0,92. Что произойдёт
      при top-k = 40 и top-p = 0,9?
    options:
      - Оба фильтра оставят только этот токен
      - 'Top-k оставит 40 токенов, а top-p может оставить только лидера'
      - 'Top-p оставит больше токенов, чем top-k'
      - Оба фильтра обязательно оставят все токены
    correct: 1
    explanation: >-
      Top-k сохраняет фиксированное число кандидатов независимо от уверенности.
      Для top-p одного лидера уже достаточно, чтобы покрыть порог 0,9.
  - question: >-
      В неоднозначном контексте вероятность распределена между множеством
      правдоподобных токенов. Как поведёт себя top-p?
    options:
      - >-
        Расширит набор кандидатов, пока они вместе не покроют заданную
        вероятность
      - Всегда оставит один самый вероятный токен
      - Всегда оставит ровно p умножить на 100 токенов
      - Заменит вероятности равномерным распределением
    correct: 0
    explanation: >-
      Когда явного лидера нет, для достижения порога top-p приходится включить
      больше токенов. Поэтому размер ядра меняется вместе с уверенностью модели.
---

Здесь важно различать фиксированное правило из [шага 43](#step-43) и адаптивное ядро из [шага 46](#step-46). Частая ошибка — считать, что меньше кандидатов всегда лучше. В уверенном контексте лишние варианты действительно добавляют риск, но в неоднозначном слишком узкий список обедняет продолжение. Смотри не только на число оставшихся токенов, но и на форму распределения.

> 🎒 **На пальцах.** Представь прожектор на сцене. Если один актёр явно произносит главную реплику, top-p освещает только его, а top-k по привычке захватывает ещё статистов. Если реплику могут продолжить многие, луч top-p расширяется и включает их. Top-k же сохраняет одну и ту же ширину независимо от сцены.
