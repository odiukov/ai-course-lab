- concept: Gumbel-Softmax заменяет недифференцируемый argmax функцией softmax
  question: Какая функция превращает зашумлённые логарифмы вероятностей в дифференцируемое приближение категориального выбора?
  explanation: Жёсткий argmax возвращает победителя и почти везде не даёт полезного градиента. Softmax плавно меняет выход при изменении логитов, поэтому через него можно выполнить обратное распространение.
  kind: cloze
  template: y = ___((log p + g) / τ)
  answer: softmax
  accept:
    - softmax
    - функция softmax
  id: 057-gumbel-softmax-1
  fingerprint: b7bca0e8
- concept: выход Gumbel-Softmax является мягким one-hot вектором на вероятностном симплексе
  question: Что возвращает Gumbel-Softmax вместо индекса выбранной категории?
  explanation: 'Результат Gumbel-Softmax — мягкий one-hot: категории получают непрерывные доли с единичной суммой. Такой результат приближает дискретный выбор, но остаётся дифференцируемым.'
  kind: choice
  options:
    - Непрерывный вектор положительных весов, сумма которых равна единице
    - Единственный дискретный индекс без информации об остальных категориях
    - Независимый гауссов шум для каждой категории
  correct: 0
  id: 057-gumbel-softmax-2
  fingerprint: c7932207
