---
id: 011-proverka-naivnosti
type: check
title: 'Проверка: что в Naive Bayes наивно, а что остаётся полезным'
source_anchor: '### The Naive Independence Assumption'
check:
  - question: >-
      В тексте встречаются слова, которые явно связаны друг с другом, но Naive
      Bayes всё равно выбирает верный класс. Как это объяснить?
    options:
      - 'Правильный класс доказывает, что слова условно независимы'
      - 'Допущение может искажать оценки вероятностей, не меняя порядок классов'
      - Naive Bayes автоматически исключает связанные слова из расчёта
    correct: 1
    explanation: >-
      Условная независимость может быть нарушена, а вероятности — искажены. Для
      правильной классификации достаточно, чтобы итоговая оценка верного класса
      всё ещё была максимальной.
  - question: Что полезного даёт наивное допущение при большом словаре?
    options:
      - >-
        Позволяет заменить недоступную оценку совместной вероятности подсчётами
        по отдельным словам
      - Гарантирует точные posterior-вероятности классов
      - >-
        Позволяет восстановить вероятности всех сочетаний слов без
        дополнительных данных
    correct: 0
    explanation: >-
      Вместо распределения над 2^10 000 комбинаций модель оценивает отдельные
      вероятности признаков внутри класса. Для каждой такой оценки достаточно
      простых счётчиков.
---

Ты видишь странный результат: модель считает связанные слова независимыми, хотя в обычном тексте это неверно, но всё же выбирает подходящий класс. После [шага 9](#step-9) тебе важно разделять две вещи: наивно именно допущение об условной независимости признаков, а полезно — резкое упрощение подсчёта.

Главная ловушка здесь — требовать от Naive Bayes точных вероятностей. Как показал [шаг 10](#step-10), классификатору часто достаточно правильного порядка классов. Поэтому неверное устройство вероятностной модели ещё не означает неверный ответ: приблизительные оценки могут уверенно поставить нужный класс выше остальных.
