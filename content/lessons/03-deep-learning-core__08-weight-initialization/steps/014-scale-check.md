---
id: 014-scale-check
type: check
title: 'Проверка: чем N(0,1) отличается от N(0,0.01)'
source_anchor: '### Step 4: The Experiment'
check:
  - question: 'Ты меняешь scale с 1 на 0,01. Что изменится в случайных весах?'
    options:
      - 'Среднее сместится с 0 к 0,01'
      - Веса станут одинаковыми
      - Веса будут гораздо плотнее собраны около нуля
      - Форма матрицы уменьшится
    correct: 2
    explanation: >-
      Центр остаётся в нуле, а scale управляет размером разброса. При scale 0,01
      типичные веса намного меньше.
  - question: 'Что верно для обеих инициализаций: N(0,1) и N(0,0.01)?'
    options:
      - Обе создают только нулевые веса
      - Обе могут разделить роли нейронов случайными весами
      - Обе гарантируют безопасный масштаб в глубокой сети
      - Обе создают веса одинакового размера
    correct: 1
    explanation: >-
      Случайность может нарушить симметрию в обоих случаях, но подходящий
      масштаб она сама по себе не гарантирует.
---

Сравни две матрицы из функции со [шага 13](#step-13). В обеих значения случайны и располагаются вокруг нуля, поэтому нейроны могут получить разные наборы весов. Но `scale=1` допускает заметно более крупные значения, а `scale=0.01` тесно прижимает их к нулю — именно так параметр работает в [шаге 12](#step-12).

Главная ловушка: «случайные» не значит «одинакового масштаба». Обе инициализации нарушают симметрию, но запускают сеть из совсем разных состояний.

> 🎒 **На пальцах.** Представь две банки с конфетти. В первой кусочки разлетаются по всей комнате, во второй падают почти рядом с банкой. Направления случайны в обоих случаях, но размах движения совершенно разный. Так и N(0,1) с N(0,0.01): общий центр один, а типичный размер отклонений — нет.
