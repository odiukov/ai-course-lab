---
id: 048-final-quiz
type: quiz
title: 'Итоговая проверка: симметрия, дисперсия, Xavier, Kaiming и residual scaling'
source_anchor: '## Key Terms'
check:
  - question: >-
      У двух нейронов одинаковые строки весов. После первого обновления они
      получают одинаковые изменения. Что это означает?
    options:
      - 'Симметрия сохраняется: нейроны продолжают учить один и тот же признак'
      - Дисперсия обязательно взрывается
      - Сеть автоматически переходит к Xavier-инициализации
      - Один из нейронов станет residual-ветвью
    correct: 0
    explanation: >-
      Одинаковые веса и одинаковые обновления оставляют нейроны неразличимыми.
      Для разделения их ролей строки весов должны отличаться.
  - question: >-
      Ты увеличил число входов слоя, но оставил прежний разброс весов. Что
      вероятнее всего произойдёт с разбросом взвешенной суммы?
    options:
      - 'Он вырастет, потому что слой складывает больше случайных вкладов'
      - Он обязательно станет нулевым
      - 'Он не изменится: число входов не связано с масштабом сигнала'
      - Он уменьшится ровно вдвое
    correct: 0
    explanation: >-
      При большем fan_in накапливается больше вкладов. Поэтому масштаб весов
      связывают с размером слоя, чтобы сигнал не разрастался.
  - question: >-
      Ты собираешь две глубокие сети: одну с tanh, другую с ReLU. Какая пара
      стартовых стратегий лучше согласуется с активациями?
    options:
      - 'Xavier для tanh, Kaiming для ReLU'
      - 'Kaiming для tanh, Xavier для ReLU'
      - 'Нулевые веса для tanh, одинаковые случайные веса для ReLU'
      - Kaiming для обеих сетей независимо от активации
    correct: 0
    explanation: >-
      Xavier балансирует масштаб для tanh и sigmoid, а Kaiming компенсирует
      потерю части сигнала после ReLU.
  - question: >-
      Ты увеличил число residual-слоёв трансформера, не меняя масштаб
      residual-ветвей. Какой риск усиливается?
    options:
      - 'Вклады ветвей накапливаются, и дисперсия residual-потока растёт'
      - Все нейроны получают одинаковые строки весов
      - ReLU превращается в sigmoid
      - Fan_in каждого слоя автоматически становится равным единице
    correct: 0
    explanation: >-
      Residual-ветви многократно прибавляются к общему потоку. Масштабирование,
      зависящее от числа слоёв, сдерживает накопление дисперсии.
---

Дальше тебе понадобится смотреть на неудачное обучение как на диагноз, а не сразу менять оптимизатор. Сначала проверь, различаются ли нейроны, затем проследи масштаб сигнала и только после этого сопоставь инициализацию с активацией и архитектурой. Здесь чаще всего путают две независимые проблемы: случайность нарушает симметрию, но безопасный разброс она сама по себе не гарантирует. Xavier и Kaiming решают вопрос масштаба ([шаг 40](#step-40)), а глубокий residual-поток требует дополнительной поправки ([шаг 43](#step-43)). Верный старт — это согласованная система, а не название одной формулы.
