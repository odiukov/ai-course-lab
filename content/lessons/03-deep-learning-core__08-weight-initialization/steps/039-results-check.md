---
id: 039-results-check
type: check
title: 'Проверка: диагностируем шесть комбинаций инициализации и активации'
source_anchor: '### Step 4: The Experiment'
check:
  - question: >-
      Какая диагностика шести запусков лучше всего согласуется с их траекториями
      по 50 слоям?
    options:
      - >-
        Нули с sigmoid дают здоровую стабильность; обе случайные
        ReLU-инициализации устойчивы; Xavier и Kaiming вызывают взрыв
      - >-
        Нули с sigmoid дают мёртвую константу; N(0,1) с ReLU вызывает взрыв;
        N(0,0.01) с ReLU — затухание; Xavier и Kaiming удерживают управляемый
        масштаб
      - 'Все запуски здоровы, поскольку на первом слое их значения конечны'
      - >-
        Любая случайная инициализация одинаково пригодна для ReLU, если строки
        весов различаются
    correct: 1
    explanation: >-
      Случайность нарушает симметрию, но не гарантирует подходящий масштаб.
      Большая дисперсия разгоняет ReLU-активации, малая стирает сигнал, а
      согласованные с активацией Xavier и Kaiming поддерживают рабочий масштаб.
      Нулевые веса с sigmoid лишь создают постоянное значение 0,5.
  - question: >-
      У Zero init + Sigmoid и Xavier + Sigmoid средний модуль на многих слоях
      близок к 0,5. Почему этого недостаточно, чтобы признать оба запуска
      здоровыми?
    options:
      - >-
        Нужно проверить разнообразие активаций: при нулевых весах все нейроны
        выдают одну и ту же константу
      - 'Любое среднее значение около 0,5 означает взрыв сигнала'
      - Sigmoid можно диагностировать только по первому слою
      - Средний модуль применим только к активациям ReLU
    correct: 0
    explanation: >-
      Одинаковое среднее может скрывать принципиально разную внутреннюю картину.
      При нулевых весах предактивация каждого нейрона равна нулю, поэтому
      sigmoid везде выдаёт 0,5; при Xavier отдельные нейроны могут сохранять
      различные активации вокруг похожего среднего.
---

Перед тобой шесть строк измерений, и главная ловушка — выбрать победителя только по последнему числу. Используй признаки из [шага 38](#step-38): стабильный средний модуль ещё не доказывает, что внутри слоя сохранилось разнообразие сигналов.

Сначала отдели мёртвую константу Zero init + Sigmoid от настоящей стабильности. Затем найди две противоположные аварии: большой случайный масштаб с ReLU разгоняет значения, маленький — постепенно стирает их. Наконец, сравни Xavier и Kaiming не по одному слою, а по всей траектории: здоровый запуск удерживает управляемый масштаб без превращения всех нейронов в одинаковые копии.
