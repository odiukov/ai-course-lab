- concept: параметр nonlinearity определяет коэффициент масштаба в готовом инициализаторе Kaiming
  question: Что сообщает PyTorch аргумент nonlinearity='relu' при вызове kaiming_normal_?
  explanation: Аргумент nonlinearity нужен для расчёта коэффициента gain и масштаба распределения весов. Инициализатор не добавляет ReLU в модель и не применяет её к весам.
  kind: choice
  options:
    - Какую нелинейность нужно автоматически добавить в модель
    - Для какой нелинейности нужно рассчитать масштаб начальных весов
    - Какую функцию применить к уже сгенерированным весам
    - Как инициализировать смещение линейного слоя
  correct: 1
  id: 045-pytorch-1
  fingerprint: 8d043560
- concept: суффикс подчёркивания в имени инициализатора PyTorch означает изменение тензора на месте
  question: Что означает подчёркивание в конце имени функции nn.init.kaiming_normal_?
  explanation: 'Суффикс _ следует соглашению PyTorch об операциях in-place: значения переданного тензора заменяются, а отдельный тензор весов создавать и присваивать слою не требуется.'
  kind: choice
  options:
    - Функция изменяет переданный тензор весов на месте
    - Функция возвращает новый слой вместе с активацией
    - Функция инициализирует только скрытые параметры
    - Функция сохраняет прежние веса как резервную копию
  correct: 0
  id: 045-pytorch-2
  fingerprint: 728a41c5
- concept: веса и смещение слоя можно инициализировать отдельными вызовами
  question: Заполни вызов, который после инициализации весов отдельно обнуляет смещение линейного слоя.
  explanation: Инициализатор, применённый к layer.weight, не меняет layer.bias. Для явного обнуления смещения используется отдельный in-place вызов nn.init.zeros_(layer.bias).
  kind: cloze
  template: nn.init.___(layer.bias)
  answer: zeros_
  accept:
    - zeros_
  id: 045-pytorch-3
  fingerprint: 83d71550
