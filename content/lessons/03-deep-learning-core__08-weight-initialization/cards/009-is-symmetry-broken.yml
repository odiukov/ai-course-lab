- concept: без пары весовых строк функция не может обнаружить нарушение симметрии
  question: Функция `is_symmetry_broken(weights)` получила набор весов, из которого невозможно образовать пару строк разных нейронов. Что она должна вернуть?
  explanation: Нарушение симметрии означает наличие различающихся весовых строк. Если пары строк нет, свидетельства различия тоже нет, поэтому функция возвращает `False`.
  kind: choice
  options:
    - '`True`, если имеющаяся строка ненулевая'
    - '`False`, поскольку сравнивать поведение разных нейронов невозможно'
    - Возбудить исключение независимо от содержимого весов
  correct: 1
  id: 009-is-symmetry-broken-1
  fingerprint: d9a9ce9e
- concept: экзистенциальная проверка допускает немедленный возврат после первого найденного различия
  question: Заполни пропуск так, чтобы функция сразу завершалась при обнаружении строки, отличной от образца.
  explanation: Одной отличающейся строки достаточно, чтобы подтвердить нарушение симметрии. Проверять оставшиеся строки после этого не требуется.
  kind: cloze
  template: |-
    for row in rows:
        if row != reference:
            ___
    return False
  answer: return True
  accept:
    - return True
  id: 009-is-symmetry-broken-2
  fingerprint: 51bcd68d
