---
id: 028-check-lu
type: check
title: 'Проверка: где платится O(n³), а где остаётся O(n²)'
source_anchor: '### LU decomposition'
check:
  - question: >-
      Для одной матрицы A нужно решить системы с 500 разными правыми частями.
      Какова суммарная асимптотическая стоимость с использованием сохранённого
      LU-разложения?
    options:
      - 'O(n³ + 500n²): одно разложение и 500 пар треугольных подстановок'
      - 'O(500n³): отдельное разложение для каждой правой части'
      - 'O(n² + 500n³): одна подстановка и 500 разложений'
    correct: 0
    explanation: >-
      Матрица A неизменна, поэтому факторизация за O(n³) выполняется один раз.
      Каждая из 500 правых частей добавляет решение двух треугольных систем за
      O(n²).
  - question: >-
      После решения системы Ax = b матрицу коэффициентов заменили на другую
      матрицу A_new, сохранив ту же правую часть. Что обычно потребуется для
      следующего решения?
    options:
      - Только подставить прежний b в сохранённые L и U
      - >-
        Выполнить новое разложение A_new за O(n³), а затем треугольные
        подстановки
      - 'Заменить диагональ прежней U, не пересчитывая остальные элементы'
    correct: 1
    explanation: >-
      Матрицы L и U построены из элементов конкретной A. Для другой матрицы
      коэффициентов старые множители исключения и верхнетреугольная матрица в
      общем случае неприменимы.
---

Здесь важно не спутать две разные работы. В [шаге 24](#step-24) цена $O(n^3)$ относилась к построению разложения, а в [шаге 27](#step-27) цена $O(n^2)$ — к решению с уже готовыми $L$ и $U$.

Обычно спотыкаются на фразе «каждое решение стоит $O(n^2)$»: она верна только при неизменной матрице $A$. Новый вектор $b$ дешёвый — меняются лишь подстановки. Новая $A$ дорогая — разложение приходится строить заново. Проверяй первым делом не число систем, а что именно между ними меняется.
