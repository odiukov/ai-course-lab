- concept: линтер отдельного инструмента композиционно возвращает единый список находок всех проверок
  question: Заполни возврат функции, которая только агрегирует результаты проверок и не управляет выпуском реестра.
  explanation: '`lint_tool` объединяет находки проверок имени, описания и схемы в плоский список. Чистая спецификация даёт пустой список; печать, исключения и решение о блокировке остаются вызывающему коду.'
  kind: cloze
  template: |-
    def lint_tool(tool):
        findings = []
        findings.extend(lint_name(tool.get("name", "")))
        findings.extend(lint_description(tool.get("description", "")))
        findings.extend(lint_schema(tool.get("parameters", {})))
        return ___
  answer: findings
  accept:
    - findings
  id: 034-lint-tool-1
  fingerprint: 50429efd
