---
id: 032-check-versii-otravlenie
type: check
title: 'Проверка: новая версия или безопасная правка'
source_anchor: '### Versioning'
check:
  - question: >-
      У тебя работает get_weather с обязательным аргументом city (строка).
      Продукт просит разрешить ещё и числовой код города, то есть принимать
      строку или число. Что это за правка?
    options:
      - 'Безопасная правка: тип только ослабили, старые вызовы всё ещё проходят'
      - 'Нужен get_weather_v2, а старый оставить на релиз с deprecated: true'
      - 'Безопасная правка, если добавить в описание строку про два формата'
      - Нужно переименовать инструмент в get_weather_by_city_or_code
    correct: 1
    explanation: >-
      Любое изменение типа аргумента, даже ослабление, требует новой версии:
      модель уже видела старую схему, и валидация на строгом режиме перестаёт
      совпадать с ней. Переименование старого имени ломает вызовы отдельно.
  - question: >-
      К тому же get_weather хочешь добавить units с enum из celsius и
      fahrenheit, поле необязательное, по умолчанию celsius. Как выкатывать?
    options:
      - 'Только через get_weather_v2: enum меняет схему'
      - 'Прямо в текущий инструмент: необязательный параметр ничего не ломает'
      - 'Через v2, потому что в схеме появилось новое поле'
      - 'Никак: добавлять поля в стабильный инструмент нельзя'
    correct: 1
    explanation: >-
      Необязательные параметры добавляются свободно: кто их не передаёт,
      получает прежнее поведение, старые вызовы остаются валидными.
---

После [шага 30](#step-30) от тебя требуется одно практическое умение: взять предложенную правку инструмента и сказать, поедет она в текущую версию или требует `v2` со окном устаревания.

Разделительная линия простая. Меняешь имя или тип аргумента — новая версия. Добавляешь необязательное поле — правь на месте.

Спотыкаются обычно на двух вещах. Первая: кажется, что ослабление типа безопасно — «раньше только строка, теперь строка или число, никто же не пострадал». Пострадает: схема, на которую модель уже насмотрелась, больше не совпадает с новой.

Вторая: удаление инструмента путают с мгновенной операцией. Сначала `deprecated: true`, удаление — следующим релизом.
