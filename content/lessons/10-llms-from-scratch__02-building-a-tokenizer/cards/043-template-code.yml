- concept: 'порядок сборки плоской строки по chat-шаблону Llama 3: маркер начала последовательности, заголовок роли, разделитель, содержимое, маркер конца хода'
  question: Ты собираешь промпт формата Llama 3 из списка сообщений и обрабатываешь первое сообщение диалога. Расставь операции в порядке, в котором соответствующие фрагменты попадают в итоговую строку.
  explanation: 'Шаблон — не набор независимых вставок, а строгая последовательность. <|begin_of_text|> относится ко всей последовательности, поэтому идёт один раз до первого заголовка; далее на каждое сообщение — открытие заголовка, роль, закрытие заголовка, разделитель, содержимое, <|eot_id|>. Модель училась именно на этом порядке: любая перестановка или пропущенный разделитель выводит вход за пределы обучающего распределения.'
  kind: order
  items:
    - дописать <|begin_of_text|> — ровно один раз, в самом начале всей последовательности
    - открыть заголовок маркером <|start_header_id|>
    - вписать имя роли (system, user или assistant)
    - закрыть заголовок маркером <|end_header_id|>
    - добавить разделитель из двух переводов строки
    - вставить content сообщения без обрезки пробелов
    - закрыть ход маркером <|eot_id|>
  id: 043-template-code-1
  fingerprint: dc996ff7
- concept: неизвестная роль не вызывает сбоя в кодировании, а тихо даёт заголовок, которого модель не видела при обучении — поэтому роли валидируются на входе
  question: В список сообщений попала запись с ролью "developer", хотя модель обучалась на ролях system, user и assistant. Функция сборки шаблона не проверяет роли и просто подставляет строку в заголовок. Что произойдёт?
  explanation: 'Имя роли — обычный текст между маркерами заголовка, поэтому оно спокойно разбивается на обычные токены: ни исключения, ни предупреждения не будет. Сломается только распределение — модель не видела такого заголовка. Отказ на этапе валидации ввода превращает бесшумную деградацию в понятную ошибку, поэтому роль и типы полей role/content проверяются до сборки строки.'
  kind: choice
  options:
    - 'Кодирование упадёт с ошибкой: токенизатор не найдёт токен для неизвестной роли'
    - Строка соберётся и закодируется без ошибок, но комбинация токенов заголовка не встречалась при обучении, и качество ответа непредсказуемо падает
    - Модель автоматически интерпретирует незнакомую роль как system, потому что это роль по умолчанию
    - Роль будет проигнорирована, и сообщение попадёт в последовательность как продолжение предыдущего хода
  correct: 1
  id: 043-template-code-2
  fingerprint: 564ef0cc
- concept: в формате Llama 3 заголовок отделён от содержимого разделителем ровно из двух переводов строки
  question: Заполни разделитель между закрытием заголовка и содержимым сообщения в шаблоне Llama 3.
  explanation: После <|end_header_id|> в формате Llama 3 идёт пустая строка, то есть два символа перевода строки, и только потом содержимое. В формате с <|im_start|>/<|im_end|> перевод строки после роли один — из-за этого шаблоны нельзя переносить между моделями. Один лишний или потерянный \n даёт строку, визуально похожую на шаблон, но посимвольно другую, и ответ рассыпается.
  kind: cloze
  template: chunk = f"<|start_header_id|>{role}<|end_header_id|>___{content}<|eot_id|>"
  answer: \n\n
  accept:
    - "\n\n"
    - \n\n
    - '"\n\n"'
    - два перевода строки
  id: 043-template-code-3
  fingerprint: 5c1f4ccd
