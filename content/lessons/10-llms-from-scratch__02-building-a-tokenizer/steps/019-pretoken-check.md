---
id: 019-pretoken-check
type: check
title: >-
  Как разрежутся Don't и слово с ведущим пробелом, и где BPE больше не сможет
  сделать merge?
source_anchor: '### Step 2: Pre-Tokenizer with Regex'
check:
  - question: Как pre_tokenize разрежет строку `Don't stop`?
    options:
      - '`Don''t`, ` `, `stop` — апостроф внутри слова, пробел отдельно'
      - '`Don`, `''t`, `stop` и пробел выброшен как разделитель'
      - '`Don`, `''t`, ` stop` — пробел входит в последний чанк'
    correct: 2
    explanation: >-
      Сокращение отрезается веткой `'(?:[sdmt]|ll|ve|re)` в отдельный чанк `'t`,
      а одиночный пробел перед словом захватывается веткой ` ?\p{L}+` и остаётся
      внутри чанка ` stop`. Ничего не выбрасывается: конкатенация чанков даёт
      исходную строку.
  - question: >-
      В обученной таблице merges есть правило для пары «конец `Don` + начало
      `'t`». Сработает ли оно при кодировании `Don't`?
    options:
      - Да — эти части стоят рядом в исходной строке
      - >-
        Нет — `Don` и `'t` попали в разные чанки, и BPE кодирует каждый чанк
        независимо
      - 'Да, но только если сначала убрать апостроф из чанка `''t`'
    correct: 1
    explanation: >-
      Соседство в сыром тексте не даёт права на merge. После претокенизации BPE
      видит чанки по отдельности, пара через границу для него не существует.
      Такое правило и не могло бы обучиться: частоты пар считаются внутри
      чанков.
---

Проверь, различаешь ли ты пробел как отдельный кусок и пробел внутри следующего чанка. В результате из [шага 18](#step-18) строка `Don't stop` превращается в `Don`, `'t` и ` stop`: третий чанк начинается с пробела. Главная ловушка — считать соседство в исходной строке разрешением для merge. После предварительного разбиения BPE видит чанки по отдельности, поэтому граница между `Don` и `'t` для него непроходима.

> 🎒 **На пальцах.** Представь три запечатанных конверта: в первом лежит `Don`, во втором — `'t`, в третьем — ` stop` вместе с начальным пробелом. Внутри каждого конверта карточки ещё можно склеивать. Но переклеить карточку из одного конверта к карточке из другого нельзя, даже если конверты лежат рядом.
