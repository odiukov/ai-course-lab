---
id: 033-update-check
type: check
title: 'Что разрешено менять между эпохами, а что должно оставаться замороженным?'
source_anchor: '## Build It'
check:
  - question: >-
      Между первой и второй эпохой ты заново вычислил текущие вероятности
      действий по обновлённым параметрам актёра. Что нарушилось?
    options:
      - 'Ничего: текущие вероятности должны отражать новую политику'
      - Исчезла неподвижная точка сравнения
      - Размер минибатча стал некорректным
    correct: 0
    explanation: >-
      Текущие вероятности пересчитываются на каждом минибатче — это нормально и
      необходимо для вычисления ratio. Неизменными остаются log π_old и
      преимущества, которые были вычислены один раз до начала всех эпох.
  - question: >-
      После третьего минибатча второй эпохи ты пересчитал advantage для всех
      переходов ролаута по обновлённому критику. Почему это ошибка?
    options:
      - Критик обучается отдельно от актёра
      - Advantage должен оставаться неизменным снимком для всех эпох
      - Размер ролаута изменился
    correct: 1
    explanation: >-
      Advantage вычисляется один раз для всего ролаута перед началом эпох и
      затем замораживается. Если его пересчитывать, следующие минибатчи будут
      обучаться на другой цели — точка сравнения сдвинется, и контроль за
      изменением политики потеряется.
---

На новом проходе легко решить: «Раз сеть обновилась, пересчитаю всё». Но тогда исчезает неподвижная точка сравнения. Между эпохами меняются параметры актёра и критика, поэтому ты заново получаешь текущие вероятности, значения, loss и градиенты. Можно также перемешать порядок переходов и собрать другие минибатчи.

Сам ролаут остаётся снимком прошлого: состояния, действия, награды, флаги завершения, сохранённые `log π_old`, цели критика и один раз нормированные преимущества не меняются. Именно этот неизменный набор позволяет сравнивать каждую новую версию политики с одной и той же старой версией, как было организовано в [шаге 31](#step-31). Двигай ученика, а не экзаменационный лист.
