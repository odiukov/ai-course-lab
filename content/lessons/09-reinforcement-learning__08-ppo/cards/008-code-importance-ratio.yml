- concept: importance ratio вычисляется через экспоненту разности логарифмов вероятностей
  question: 'Дополни формулу вычисления importance ratio через логарифмы:'
  explanation: Ratio = exp(log π_new - log π_old). Разность логарифмов превращает деление малых вероятностей в устойчивое вычитание, а экспонента возвращает результат в обычный масштаб.
  kind: cloze
  template: ratio = torch.exp(___ - log_prob_old)
  answer: log_prob_new
  accept:
    - log_prob
    - log_probs_new
    - new_log_prob
  id: 008-code-importance-ratio-1
  fingerprint: d9d63c69
