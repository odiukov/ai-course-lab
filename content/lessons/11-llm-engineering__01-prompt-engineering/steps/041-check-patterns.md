---
id: 041-check-patterns
type: check
title: 'Проверка: три группы паттернов'
source_anchor: '### Prompt Patterns'
check:
  - question: >-
      Тебе нужно, чтобы модель разобрала длинный отзыв: сначала выделила
      отдельные претензии, потом оценила каждую, потом собрала общий вывод. К
      какой группе паттернов ты потянешься?
    options:
      - 'К паттернам личности и рамок: persona и boundary'
      - 'К паттернам формы ответа: template и few-shot'
      - 'К паттернам хода мысли: chain-of-thought и decomposition'
      - 'К мета-промпту: пусть модель сама напишет себе промпт'
    correct: 2
    explanation: >-
      Здесь ты задаёшь не роль и не вид ответа, а порядок рассуждения — сначала
      разбить, потом решить по частям, потом собрать. Это ровно то, чем
      занимаются chain-of-thought и decomposition.
  - question: >-
      Ты положил в промпт два примера «вход — выход» и нигде не объяснил, что
      значит «mixed». Модель всё равно ставит «mixed» там, где надо. Что это за
      приём и в какой он группе?
    options:
      - >-
        Модель просто знает слово «mixed» из обучения, а приём относится к
        паттернам рамок
      - >-
        Это few-shot: правило считывается прямо из пар вход-выход, группа —
        форма ответа
      - 'Это chain-of-thought: примеры показывают модели ход мысли'
      - 'Это мета-промпт: примеры дописывают промпт за тебя'
    correct: 1
    explanation: >-
      Few-shot задаёт форму: из двух пар видно и структуру JSON, и границу между
      «mixed» и «negative», хотя словами это нигде не сказано.
---

После этого куска ты должен уметь по описанию задачи назвать группу, а не перебирать в памяти список из десяти названий. Вопрос каждый раз один и тот же: что ты сейчас задаёшь — кто говорит, как выглядит ответ или в каком порядке модель думает.

Спотыкаются обычно на двух местах. Первое: few-shot кажется способом «показать модели рассуждение», хотя примеры задают именно форму — правило вычитывается из пар вход-выход, а не из объяснения ([шаг 36](#step-36)). Второе: guardrail и boundary путают с форматом, потому что оба звучат как жёсткие правила, — но они очерчивают рамки поведения, а не вид ответа. Мета-промпт же не входит ни в одну группу: его результат — другой промпт, а не ответ на задачу ([шаг 40](#step-40)).
