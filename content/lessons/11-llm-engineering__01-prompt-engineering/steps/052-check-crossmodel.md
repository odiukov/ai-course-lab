---
id: 052-check-crossmodel
type: check
title: 'Проверка: почему тестируют на temperature=0'
source_anchor: '### Cross-Model Prompt Design'
check:
  - question: >-
      Ты сравниваешь два варианта промпта и запускаешь каждый по разу на
      temperature=0.8. Первый дал ответ лучше. Что с этим выводом не так?
    options:
      - >-
        На высокой температуре модель всегда отвечает хуже, поэтому сравнение
        вообще бессмысленно
      - >-
        Разница могла возникнуть из-за случайности сэмплирования, а не из-за
        самого промпта
      - >-
        Температура влияет только на длину ответа, поэтому сравнивать надо было
        по количеству слов
    correct: 1
    explanation: >-
      При высокой температуре один и тот же промпт даёт разные ответы сам по
      себе. Один запуск не отделяет вклад промпта от броска кубика.
  - question: >-
      Ты отладил промпт на temperature=0, он стабильно даёт то, что нужно. Что
      делать дальше, если задача — генерировать варианты слоганов?
    options:
      - >-
        Оставить temperature=0 навсегда: раз промпт проверен, менять параметр
        нельзя
      - >-
        Поднять температуру под задачу — ноль был режимом отладки, а не рабочей
        настройкой
      - 'Поднять и температуру, и top-p, чтобы разброс точно стал шире'
    correct: 1
    explanation: >-
      Ноль нужен, чтобы отделить качество промпта от случайности. Для генерации
      вариантов температуру поднимают — и крутят что-то одно, как в правиле про
      top-p.
---

Здесь проверяется одна вещь: понимаешь ли ты, зачем в списке из [шага 50](#step-50) стоит пункт про temperature=0.

Типичное место, где спотыкаются, — путают отладку и работу. Ноль не делает промпт лучше и не является «правильной» настройкой; он просто убирает шум, чтобы разница между двумя ответами означала разницу промптов, а не разный бросок дротика из [шага 29](#step-29). Отладил — возвращай температуру, которая нужна задаче.

Второе место — сравнение по одному запуску на высокой температуре. Такой замер ничего не доказывает: перезапусти — и «победитель» может поменяться.
