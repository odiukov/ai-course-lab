- concept: название класса выбирается по идентификатору, предсказанному классификатором
  question: Функция получает предсказание `(cls_id, cls_score)` и индекс исходной детекции `detection_index`. Как правильно определить `class_name` для результата классификации?
  explanation: '`detection_index` связывает классификацию с объектом на изображении, а `cls_id` обозначает предсказанный класс. Поэтому название нужно извлекать как `class_names[int(cls_id)]`.'
  kind: choice
  options:
    - '`class_names[detection_index]`'
    - '`class_names[int(cls_id)]`'
    - '`class_names[len(classifications)]`'
    - '`class_names[int(cls_score)]`'
  correct: 1
  id: 021-attach-classifications-1
  fingerprint: 779e7e1d
- concept: уверенность классификатора приводится к обычному числу Python перед записью в типизированный результат
  question: Заполни пропуск так, чтобы уверенность классификатора соответствовала числовому полю выходного контракта.
  explanation: Явное преобразование через `float` отделяет выходной контракт от конкретного числового типа, который вернула модель, и делает результат пригодным для проверки схемой и сериализации.
  kind: cloze
  template: Classification(class_id=int(cls_id), class_name=class_names[int(cls_id)], score=___)
  answer: float(cls_score)
  accept:
    - float(cls_score)
  id: 021-attach-classifications-2
  fingerprint: d116c13b
