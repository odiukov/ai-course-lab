---
id: 018-proverka-cropov
type: check
title: 'Проверка: рамка за кадром, нулевая площадь и пропущенный crop'
source_anchor: '### Step 2: A minimal Pipeline class'
check:
  - question: >-
      Почему для рамки, целиком оказавшейся за границей кадра, безопасным
      результатом зажима считается нулевая ширина или высота?
    options:
      - >-
        Такую рамку можно распознать проверкой размера и пропустить до вырезания
        тензора
      - >-
        Нулевая сторона заставляет классификатор автоматически восстановить
        рамку
      - 'Нулевая сторона означает, что рамка должна быть растянута на весь кадр'
    correct: 0
    explanation: >-
      Обе границы прижимаются к одному краю изображения, поэтому сторона
      становится нулевой. Проверка min_crop отбрасывает такой crop до среза и не
      допускает появления перевёрнутой рамки.
  - question: >-
      После зажима рамка пересекает кадр лишь узкой полосой и не проходит
      min_crop. Почему pipeline должен пропустить crop, а не расширить рамку до
      минимального размера?
    options:
      - >-
        Расширение изменило бы предсказанную область и добавило бы в crop
        произвольные пиксели
      - Интерполяция умеет работать только с рамками исходного размера
      - Расширенная рамка обязательно вышла бы за обе границы изображения
    correct: 0
    explanation: >-
      min_crop проверяет пригодность уже зажатой области для классификации.
      Искусственное расширение подменило бы результат детектора, поэтому
      детекцию сохраняют, а создание и классификацию crop пропускают.
---

Здесь легко спутать два разных решения: исправить координаты и выбрать, стоит ли вырезать объект. Сначала рамка зажимается по правилам из [шага 12](#step-12). Если она оказалась целиком за кадром, её ширина или высота станет нулевой. Если виден лишь узкий край, площадь может быть ненулевой, но одна сторона всё равно окажется меньше `min_crop`. В обоих случаях проверка из [шага 15](#step-15) пропускает crop, а детекция остаётся в списке. Именно поэтому в [шаге 17](#step-17) количество детекций и вырезок может различаться.

> 🎒 **На пальцах.** Представь садовника, размечающего грядки внутри участка. Колышки за забором он переносит на его границу. Если оба колышка оказались в одной точке, грядки нет. Если осталась полоска шириной 20 сантиметров, она существует, но для растения слишком узка: отметку сохраняют, а посадку пропускают.
