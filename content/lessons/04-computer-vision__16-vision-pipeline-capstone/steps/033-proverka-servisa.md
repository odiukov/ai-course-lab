---
id: 033-proverka-servisa
type: check
title: 'Проверка: что выполняется один раз, а что на каждый запрос'
source_anchor: '### Step 4: FastAPI service'
check:
  - question: >-
      Как правильно распределить создание моделей, вызов pipe.run и завершающую
      очистку между lifespan и эндпоинтом?
    options:
      - >-
        Создавать модели до yield, вызывать pipe.run в эндпоинте, выполнять
        очистку после yield
      - >-
        Создавать модели и выполнять очистку в эндпоинте, а pipe.run вызывать
        после yield
      - 'Создавать модели после yield, а pipe.run и очистку выполнять до yield'
      - 'Создавать модели, вызывать pipe.run и выполнять очистку внутри эндпоинта'
    correct: 0
    explanation: >-
      Модели создаются один раз до yield и затем переиспользуются. Эндпоинт
      вызывает pipe.run для каждого принятого изображения, а код очистки после
      yield выполняется при остановке сервиса.
  - question: >-
      Что произойдёт, если создание detector и classifier перенести внутрь
      detect_endpoint?
    options:
      - Модели будут заново создаваться при обработке каждого запроса
      - Модели будут созданы только при остановке сервиса
      - Модели останутся общими для всех запросов благодаря объекту app
      - Изменится только формат успешного JSON-ответа
    correct: 0
    explanation: >-
      Тело эндпоинта выполняется отдельно для каждого запроса. Поэтому
      инициализация моделей внутри него будет повторяться, увеличивая задержку и
      расход ресурсов.
---

Представь: сервис получил подряд три файла. Проверка типа, чтение, декодирование и `pipe.run` должны сработать трижды — это путь каждого запроса из [шага 31](#step-31). Но создание детектора, классификатора и пайплайна должно произойти только при запуске: до `yield` в `lifespan`. Код после `yield` выполнится один раз при остановке.

Главная ловушка — принять эндпоинт за «начало программы». На деле это повторяемая дверь: каждый новый файл снова проходит через неё.

> 🎒 **На пальцах.** Перед открытием обсерватории ты один раз устанавливаешь и настраиваешь телескоп. Затем для каждого посетителя отдельно наводишь его на выбранную звезду и делаешь снимок. В конце смены один раз закрываешь купол. Собирать телескоп заново ради каждого снимка — та же ошибка, что загружать модели внутри эндпоинта.
