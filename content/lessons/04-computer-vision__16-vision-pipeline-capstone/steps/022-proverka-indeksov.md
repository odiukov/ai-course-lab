---
id: 022-proverka-indeksov
type: check
title: 'Проверка: почему номер crop не равен номеру детекции'
source_anchor: '### Step 2: A minimal Pipeline class'
check:
  - question: >-
      Какое значение нужно записывать в Classification.detection_index после
      классификации отфильтрованных crops?
    options:
      - Позицию результата в пакете классификатора
      - Сохранённый индекс соответствующей детекции в исходном списке
      - 'Идентификатор класса, предсказанный классификатором'
    correct: 1
    explanation: >-
      После пропуска маленьких crops позиции в пакете классификатора могут не
      совпадать с позициями детекций. valid_indices сохраняет исходные индексы и
      позволяет правильно связать результаты.
  - question: >-
      Почему позицию crop в пакете нельзя считать надёжным индексом исходной
      детекции?
    options:
      - Классификатор обрабатывает crops в случайном порядке
      - >-
        Фильтрация может удалить отдельные crops и сдвинуть позиции последующих
        элементов
      - Детектор присваивает каждой рамке идентификатор класса
    correct: 1
    explanation: >-
      Если crop некоторой детекции не прошёл проверку min_crop, он не попадает в
      пакет. Следующие crops сдвигаются, но индексы соответствующих им исходных
      детекций не меняются.
---

Здесь важно не спутать два номера. Номер crop — его место среди изображений, дошедших до классификатора. Номер детекции — место объекта в исходном списке. Если детекция пропущена по правилам из [шага 16](#step-16), номера расходятся: crops `0` и `1` могут принадлежать детекциям `0` и `2`. Поэтому в [шаге 21](#step-21) ответ возвращается через сохранённый `detection_index`, а не через позицию crop.

> 🎒 **На пальцах.** Представь пекарню с заказами `0`, `1`, `2`. Заказ `1` отменили, поэтому в печь отправились только два противня. Второй противень не становится заказом `1`: на его бирке всё ещё написано `2`. `valid_indices` и есть такие бирки.
