---
id: 043-itogovyi-quiz
type: quiz
title: 'Итоговая проверка: собрать, защитить и измерить vision-пайплайн'
source_anchor: '## Exercises'
check:
  - question: >-
      Пайплайн прогнали на 10 изображениях из открытого набора данных. Какой
      отчёт лучше всего соответствует заданию на измерение его работы?
    options:
      - Только суммарное время обработки всех изображений
      - >-
        Среднее время по каждому этапу и распределение числа детекций на
        изображение
      - Только средняя уверенность классификатора
      - 'Только объём памяти, занятый весами моделей'
    correct: 1
    explanation: >-
      Поэтапные времена характеризуют стоимость частей пайплайна, а
      распределение детекций показывает, насколько различается объём работы
      между изображениями.
  - question: >-
      Нужно добавить бинарные маски объектов в JSON-ответ, не раздувая его
      списками пикселей. Какое представление следует выбрать согласно заданию?
    options:
      - Двумерный JSON-массив из нулей и единиц
      - Отдельную строку для каждого пикселя
      - RLE с длинами последовательных серий нулей и единиц
      - Копию исходного изображения для каждой детекции
    correct: 2
    explanation: >-
      RLE заменяет длинные последовательности одинаковых значений их длинами.
      Для обычных объектов с небольшим числом границ это намного компактнее
      полного списка пикселей.
  - question: >-
      Как надёжнее всего проверить требование, что JSON с масками для
      изображения с 10 объектами остаётся меньше 1 МБ?
    options:
      - Посчитать только число пикселей в одной маске
      - Оценить размер по количеству полей модели Detection
      - Проверить лишь объём масок в оперативной памяти
      - >-
        Сериализовать полный репрезентативный ответ и измерить размер полученных
        байтов
    correct: 3
    explanation: >-
      Лимит относится к готовому JSON, поэтому учитывать нужно реальное
      кодирование масок, остальные поля и накладные расходы сериализации.
  - question: >-
      Как корректно измерить эффект микробатчера перед классификатором при 5
      одновременных запросах в секунду?
    options:
      - >-
        Сравнить варианты с микробатчером и без него на одинаковой нагрузке,
        измерив пропускную способность и добавленную задержку
      - После включения микробатчера измерить только загрузку GPU
      - >-
        До изменения измерить задержку, а после изменения — только пропускную
        способность
      - >-
        Сравнить последовательный запуск без микробатчера с произвольной
        конкурентной нагрузкой после его включения
    correct: 0
    explanation: >-
      Для сопоставимого эксперимента поток запросов и условия должны оставаться
      одинаковыми. Отчёт должен одновременно показывать выигрыш в пропускной
      способности и цену ожидания батча в задержке.
---

Дальше тебе пригодится умение защищать не отдельную модель, а весь маршрут данных. Проверяй контракты из [шагов 6](#step-6)–[9](#step-9), сохраняй соответствие фрагментов исходным детекциям из [шага 20](#step-20), разделяй ошибки запроса и сервиса по [шагу 28](#step-28), а производительность подтверждай замерами из [шага 35](#step-35). Чаще всего спотыкаются именно там, где корректные части соединены неверно или одно среднее скрывает медленные запросы.

> 🎒 **На пальцах.** Перед походом ты проверяешь не только палатку: карта должна совпадать с маршрутом, вещи — лежать в подписанных рюкзаках, аптечка — быть доступной, а время переходов — измеренным. Исправная палатка не спасёт, если перепутаны рюкзаки или путь занимает вдвое дольше плана. Так же защищают и vision-пайплайн: по стыкам, отказам и фактическим числам.
