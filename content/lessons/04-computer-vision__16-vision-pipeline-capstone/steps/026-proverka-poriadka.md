---
id: 026-proverka-poriadka
type: check
title: 'Проверка: восстановите порядок полного инференса'
source_anchor: '### Step 2: A minimal Pipeline class'
check:
  - question: В каком порядке должен выполняться полный инференс vision-пайплайна?
    options:
      - >-
        Детекция → классификация всего изображения → извлечение crop → сборка
        результата
      - >-
        Предобработка → детекция → обработка рамок и отбор crop → классификация
        → привязка ответов по сохранённым индексам → сборка результата
      - >-
        Предобработка → извлечение crop → детекция → классификация → сборка
        результата
      - Детекция → сборка результата → отбор crop → классификация
    correct: 1
    explanation: >-
      Каждый этап зависит от результата предыдущего: детектор получает
      подготовленный тензор, crop извлекаются по найденным рамкам, а ответы
      классификатора связываются с исходными детекциями до сборки результата.
  - question: >-
      Из трёх детекций классификацию прошли только первая и третья. Как
      правильно связать два ответа классификатора с детекциями?
    options:
      - >-
        Присвоить их первой и второй детекциям, поскольку ответы классификатора
        идут подряд
      - Присвоить каждый ответ всем детекциям
      - >-
        Присвоить их детекциям с исходными индексами 0 и 2, сохранёнными при
        отборе crop
      - Присвоить оба ответа только третьей детекции
    correct: 2
    explanation: >-
      После пропуска промежуточного crop позиция ответа в пакете классификатора
      уже не совпадает с индексом детекции. Сохранённые индексы 0 и 2
      восстанавливают правильное соответствие.
---

Тебе нужно восстановить маршрут из [шага 25](#step-25) по зависимостям между действиями. Детектор должен получить подготовленный тензор, классификатор — только отобранные crop, а его ответы нельзя присоединять без сохранённых индексов. Частая ошибка — считать, что номер ответа классификатора всегда совпадает с номером детекции. Если один crop пропущен, это соответствие сдвигается.

> 🎒 **На пальцах.** Представь монтаж концерта: сначала записывают сцену, затем отмечают удачные фрагменты и сохраняют номера песен. Звукорежиссёр обрабатывает только выбранные записи, а готовый звук возвращают по сохранённым номерам. Если приклеивать его просто «по порядку», после первого пропуска чужая дорожка попадёт не к той песне.
