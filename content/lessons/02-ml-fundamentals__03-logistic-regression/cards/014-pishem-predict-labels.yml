- concept: вероятность, равная порогу, относится к положительному классу
  question: Заполни оператор сравнения так, чтобы совпавшая с порогом вероятность получила положительную метку.
  explanation: 'Оператор >= включает саму границу: вероятность ниже порога даёт отрицательную метку, а равная порогу или превышающая его — положительную.'
  kind: cloze
  template: labels = (probabilities ___ threshold).astype(int)
  answer: '>='
  accept:
    - ≥
  id: 014-pishem-predict-labels-1
  fingerprint: '045053e0'
- concept: булев результат поэлементного сравнения преобразуется в целочисленные метки приведением типа
  question: В NumPy выражение probabilities >= threshold вернуло булев массив. Как превратить его элементы в целочисленные метки, сохранив форму и порядок?
  explanation: astype(int) поэлементно преобразует False и True в целочисленные метки. Остальные операции либо не меняют тип элементов, либо меняют представление или порядок данных.
  kind: choice
  options:
    - (probabilities >= threshold).astype(int)
    - (probabilities >= threshold).copy()
    - (probabilities >= threshold).tolist()
    - (probabilities >= threshold).sort()
  correct: 0
  id: 014-pishem-predict-labels-2
  fingerprint: 5314e670
