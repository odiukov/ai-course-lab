---
id: 014-proverka-schetchikov
type: check
title: 'Проверка: паддинг, контекст и нулевая вероятность'
source_anchor: '### Step 1: trigram counts'
check:
  - question: >-
      Предложение содержит L обычных токенов. Для n-граммной модели в его начало
      добавляют n − 1 токенов <s>, а в конец — один </s>. Сколько n-грамм
      получится?
    options:
      - L + 1
      - L
      - L + n − 1
      - L + n
    correct: 0
    explanation: >-
      После паддинга длина последовательности равна L + n. Число окон длиной n
      составляет L + n − n + 1 = L + 1. Поэтому предложение из пяти слов в
      примере даёт шесть триграмм.
  - question: >-
      В каких двух разных случаях raw_probability возвращает нулевую
      вероятность?
    options:
      - >-
        Когда контекст не встречался и когда знакомый контекст не встречался с
        выбранным продолжением
      - >-
        Когда контекст встретился один раз и когда слово стоит в начале
        предложения
      - Когда контекст содержит <s> и когда продолжением служит </s>
      - Когда предложение короче порядка модели и когда продолжение повторяется
    correct: 0
    explanation: >-
      Для неизвестного контекста функция сразу возвращает 0.0. Если контекст
      известен, но нужная n-грамма не встречалась, её нулевой счётчик также даёт
      0.0 после деления на частоту контекста.
---

Здесь важно не смешать три разные вещи. Паддинг из [шага 8](#step-8) даёт контекст словам у границ предложения. Для триграммы контекст состоит ровно из двух предыдущих токенов, как задаёт порядок модели из [шага 7](#step-7). А ноль появляется не только при неизвестном контексте: знакомый контекст с невиданным продолжением тоже получает нулевую сырую вероятность — и возникает проблема из [шага 13](#step-13).

> 🎒 **На пальцах.** Представь журнал пропусков: две пустые клетки перед первой фамилией сохраняют одинаковый размер каждой строки. Но если в журнале после пары «Анна, Борис» ни разу не записана «Вера», такая тройка получает ноль. Ровная разметка помогает посчитать запись, но не делает невиданную запись встречавшейся.
