- concept: extend сохраняет плоский список индексов при объединении частей классов
  question: Заполни пропуск так, чтобы индексы очередного класса добавились в общий плоский список validation-индексов.
  explanation: Метод extend добавляет элементы val_part по отдельности. Метод append добавил бы весь val_part одним элементом и создал бы вложенный список.
  kind: cloze
  template: folds[i]["val"].___(val_part)
  answer: extend
  accept:
    - extend
  id: 041-code-stratified-1
  fingerprint: 144cbf51
- concept: последний срез должен забирать остаток при делении индексов на фолды
  question: Список индексов одного класса не делится на число фолдов без остатка. Как задать правую границу последнего validation-среза и почему?
  explanation: Обычные срезы могут иметь длину fold_size, но последний должен доходить до len(indices). Тогда остаток попадает в последний validation-фолд.
  kind: open
  reference: Правой границей последнего среза нужно сделать len(indices), чтобы он забрал весь оставшийся хвост и ни один индекс класса не потерялся.
  id: 041-code-stratified-2
  fingerprint: 29b8203f
