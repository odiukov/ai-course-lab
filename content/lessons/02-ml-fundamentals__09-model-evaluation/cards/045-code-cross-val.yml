- concept: пользовательская метрика сравнивает истинные ответы и предсказания на validation-части текущего фолда
  question: Какой фрагмент корректно вычисляет пользовательскую метрику после обучения модели на `X_train` и `y_train`?
  explanation: Предсказания и правильные ответы должны относиться к одним и тем же объектам validation-части. Поэтому модель применяют к `X_val`, а метрике передают `y_val` и полученные `predictions`.
  kind: choice
  options:
    - |-
      `predictions = [model.predict(x) for x in X_val]`
      `score = metric_fn(y_val, predictions)`
    - |-
      `predictions = [model.predict(x) for x in X_train]`
      `score = metric_fn(y_val, predictions)`
    - |-
      `predictions = [model.predict(x) for x in X_val]`
      `score = metric_fn(y_train, predictions)`
  correct: 0
  id: 045-code-cross-val-1
  fingerprint: c8e2b708
- concept: оценка каждого фолда добавляется в список результатов внутри цикла
  question: Заполни пропуск так, чтобы сохранить отдельную оценку текущего фолда.
  explanation: Добавление выполняется внутри цикла, поэтому ни одна оценка не теряется. После завершения всех итераций функция возвращает список отдельных результатов, а не заранее усреднённое значение.
  kind: cloze
  template: |-
    scores = []

    for train_idx, val_idx in folds:
        # обучение, предсказания и вычисление score
        ___

    return scores
  answer: scores.append(score)
  accept:
    - scores.append(score)
    - scores.extend([score])
  id: 045-code-cross-val-2
  fingerprint: 0ffc0630
