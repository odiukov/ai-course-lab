---
id: 033-check-33-i-5
type: check
title: 'Проверка: считаем 128/96 и 100.8/96 для 32 слоёв'
source_anchor: '### Step 4: Cost Model'
check:
  - question: >-
      Ты запускаешь checkpoint_cost для 32 слоёв и меняешь размер сегмента с 4
      на 8. Что произойдёт с итоговым числом 128?
    options:
      - 'Оно не изменится: пересчёт всё равно проходит каждый слой ровно один раз'
      - 'Оно вырастет вдвое: сегменты длиннее, значит пересчитывать надо больше'
      - 'Оно упадёт до 112: чекпойнтов меньше, пересчёта тоже меньше'
      - 'Оно упадёт до 96: при больших сегментах пересчёт исчезает'
    correct: 0
    explanation: >-
      В этой модели пересчёт равен ровно одному полному forward: каждый слой
      внутри своего сегмента прогоняется заново по одному разу, сколько бы
      сегментов ни было. Размер сегмента двигает память, а не эти FLOPs.
  - question: >-
      Почему накладные расходы считаются как 128 делить на 96, а не 128 делить
      на 128?
    options:
      - '96 — это стоимость шага без чекпойнтинга, и с ней мы сравниваем'
      - '96 — это стоимость только backward, forward в сравнение не входит'
      - '96 — это округление 100.8 вниз, чтобы числа были ровнее'
      - '96 — это стоимость forward плюс пересчёт, backward считают отдельно'
    correct: 0
    explanation: >-
      Без чекпойнтинга шаг стоит 32 (forward) плюс 64 (backward) = 96. Это база,
      относительно неё и меряют, на сколько дороже стал шаг с пересчётом.
---

Здесь ты должен уметь взять две функции из [шага 32](#step-32), подставить в них 32 слоя и получить числа руками — не «примерно треть», а 32, 32, 64 и 128 против 96.

Спотыкаются обычно на двух местах. Первое: делят на 128 вместо 96 и получают вместо 33 % какие-то 25 % — база для сравнения всегда шаг без чекпойнтинга. Второе: думают, что `segment_size` влияет на итог. Не влияет: пересчёт — это ровно один лишний forward целиком, разбиение меняет только пиковую память.

И проверь селективный случай: 32 × 0,15 = 4,8, итого 100,8, отсюда те самые 5 % из [шага 31](#step-31).
