---
id: 008-check-relu
type: check
title: 'Проверка: что хранить для производной ReLU'
source_anchor: '### What Backward Actually Needs'
check:
  - question: >-
      Что минимально нужно знать о входе ReLU, чтобы на backward определить
      судьбу каждого элемента градиента?
    options:
      - 'Значение градиента, пришедшего из предыдущего слоя'
      - Был ли соответствующий элемент входа положительным
      - Среднее значение всех активаций слоя
      - Матрицу весов перед ReLU
    correct: 1
    explanation: >-
      ReLU либо пропускает элемент градиента, либо обнуляет его. Решение
      определяется маской h_pre > 0, то есть знаком каждого элемента входа.
  - question: >-
      Внутри checkpoint-сегмента находятся несколько ReLU, но сохранён только
      вход сегмента. Обязательно ли отдельно хранить маски знаков для этих ReLU?
    options:
      - 'Да, знаки невозможно восстановить после завершения forward'
      - >-
        Нет, если повторный детерминированный forward восстановит те же входы
        ReLU
      - Нужно хранить маску только последней ReLU
      - 'Нет, потому что производная ReLU не зависит от входа'
    correct: 1
    explanation: >-
      При повторном детерминированном forward получаются те же промежуточные
      значения и маски знаков. Они нужны во время backward, но не обязаны всё
      время храниться в памяти.
---

После [шага 6](#step-6) и [шага 7](#step-7) у тебя есть два разных ответа на вопрос «что backward держит в памяти»: для линейного слоя — вход, для активации — какое-то промежуточное значение. Здесь проверяем именно второй пункт, самый скользкий.

Ты должен уметь сказать, что ReLU требует от прошлого не сам вход целиком «на всякий случай», а всего один факт на каждое число: был он положительным или нет. И понимать, что этот факт не обязательно хранить — его можно получить обратно, если пересчитать forward от сохранённой точки.

Обычно спотыкаются на том, что путают «нужно значение» с «нужно хранить значение». Нужно — да; хранить — не всегда.
