---
id: 017-check-cena-hraneniya
type: check
title: 'Проверка: сколько тензоров живёт в памяти при 32 слоях'
source_anchor: '### Step 2: Naive Backward Needing All Activations'
check:
  - question: >-
      Наивный forward прошёл по модели из 32 слоёв и сложил активации в список.
      Сколько тензоров-входов лежит в памяти к моменту, когда начинается
      обратный проход?
    options:
      - 'Один — вход самого первого слоя, остальное посчитается по цепочке'
      - Тридцать два — по одному сохранённому входу на каждый слой
      - Два — вход модели и её итоговый выход
      - 'Ни одного, список освобождается сразу после forward'
    correct: 1
    explanation: >-
      Каждому слою для его градиентов нужен именно его собственный вход, поэтому
      список растёт вместе с числом слоёв: 32 слоя — 32 живых тензора, и все они
      ждут до самого конца обратного прохода.
  - question: >-
      В наивном обратном проходе вход слоя x_in берётся из списка, а h_pre
      (результат первого linear) считается заново. Почему его не сохранили
      вместе с остальным?
    options:
      - Без пересчёта производная ReLU получилась бы неверной
      - 'h_pre физически невозможно сохранить: он исчезает сразу после forward'
      - >-
        Хранить его — это второй такой же список на все 32 слоя, а восстановить
        его из x_in стоит один linear
      - >-
        h_pre нужен только последнему слою, для остальных он не участвует в
        градиентах
    correct: 2
    explanation: >-
      Память дороже арифметики: держать ещё по тензору на слой — почти удвоенный
      список, а пересчёт из уже сохранённого входа обходится одним умножением на
      матрицу.
---

После [шагов 13](#step-13)–[16](#step-16) ты должен уметь ответить на простой вопрос: если у модели 32 слоя, сколько тензоров держит наивный подход одновременно — и почему именно столько.

Спотыкаются тут обычно на двух местах. Первое: кажется, что достаточно последней активации, ведь идём с конца. Но каждому слою нужен его собственный вход, и слой номер 0 дождётся своей очереди в самом конце — значит, список живёт целиком, от начала forward до последнего шага backward.

Второе: строчка, где `h_pre` считается заново прямо внутри обратного прохода. Это не забывчивость, а осознанный обмен: одно умножение матриц вместо ещё одного тензора на каждый слой.
