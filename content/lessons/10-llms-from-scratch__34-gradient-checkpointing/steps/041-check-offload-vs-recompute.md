---
id: 041-check-offload-vs-recompute
type: check
title: 'Проверка: когда полосы PCIe уже нет'
source_anchor: '### Offload'
check:
  - question: >-
      Ты обучаешь модель с FSDP: по шине CPU–GPU уже постоянно передаются
      шардированные веса и градиенты. Что разумно сделать с активациями слоя?
    options:
      - >-
        Выгружать активации в память CPU: оценка в 12 мс остаётся верной при
        любой нагрузке
      - >-
        Пересчитывать активации, если из-за занятой полосы их фактическая
        передача стала медленнее лишнего forward
      - Обязательно делить каждую активацию пополам между выгрузкой и пересчётом
      - 'Увеличить размер checkpoint-сегмента: это гарантированно освободит PCIe'
    correct: 1
    explanation: >-
      Оценка в 12 мс предполагает свободный канал. Обмен FSDP создаёт
      конкуренцию за полосу, поэтому сравнивать нужно фактическое время offload
      со временем пересчёта.
  - question: >-
      От чего зависит, что выгоднее для конкретного слоя — выгрузить его
      активации или пересчитать их?
    options:
      - Только от общей глубины модели
      - >-
        От сравнения полного времени передачи по реально доступной полосе со
        временем лишнего forward слоя
      - Только от числа параметров слоя
      - >-
        От точности вычислений, потому что offload обязательно изменяет значения
        активаций
    correct: 1
    explanation: >-
      Нужно сопоставить две длительности: отправку активаций в память CPU и их
      возврат на GPU против повторного forward этого слоя. Поэтому разные слои
      одной модели могут использовать разные стратегии.
---

После [шага 40](#step-40) у тебя есть два способа не хранить активации: пересчитать их или отправить погостить в память CPU. Здесь проверяется, умеешь ли ты выбрать между ними — и понимаешь ли, что выбор делается не раз на всю модель, а по слоям: часть чекпойнтим, часть выгружаем.

Спотыкаются обычно на числе 12 мс. Его читают как свойство железа, хотя это оценка для полосы, которой никто больше не пользуется. Как только по той же шине идёт обмен FSDP, «12 мс» превращаются в ожидание в очереди, и дешёвая выгрузка становится дороже честного пересчёта.
