- concept: размер checkpoint-сегмента получают округлением квадратного корня и приводят к целому типу
  question: Заполни тело функции, возвращающей практический размер checkpoint-сегмента.
  explanation: Квадратный корень задаёт непрерывный оптимум, `round` выбирает ближайший допустимый размер сегмента, а `int` превращает результат в целое значение для индексов и диапазонов.
  kind: cloze
  template: |-
    def optimal_segment(n_layers):
        return ___
  answer: int(round(np.sqrt(n_layers)))
  accept:
    - int(np.round(np.sqrt(n_layers)))
    - int(np.rint(np.sqrt(n_layers)))
  id: 036-code-optimal-segment-1
  fingerprint: e12da594
