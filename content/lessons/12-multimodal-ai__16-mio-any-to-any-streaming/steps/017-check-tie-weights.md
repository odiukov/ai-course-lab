---
id: 017-check-tie-weights
type: check
title: 'Проверка: 400M из 8B и зачем связывают веса'
source_anchor: '## Exercises'
check:
  - question: >-
      Почему отдельная выходная проекция может иметь столько же параметров,
      сколько входная таблица эмбеддингов?
    options:
      - >-
        Потому что она содержит строку для каждого токена и работает со скрытыми
        векторами той же размерности
      - Потому что каждый токен кодируется одновременно всеми модальностями
      - Потому что выходная проекция хранит копию всей истории контекста
      - Потому что размер матрицы определяется только числом слоёв трансформера
    correct: 0
    explanation: >-
      Входная таблица имеет по вектору на токен, а выходная проекция
      сопоставляет скрытый вектор с оценкой для каждого токена. Поэтому при
      одинаковом словаре и скрытой размерности их формы совпадают.
  - question: Что именно происходит с параметрами при tie weights?
    options:
      - Входная и выходная операции начинают использовать одну общую матрицу
      - 'Вторая матрица остаётся, но её значения заменяются нулями'
      - 'Размер словаря уменьшается, чтобы матрицы стали меньше'
      - Модель перестаёт использовать выходную проекцию
    correct: 0
    explanation: >-
      Связывание весов означает совместное использование одной матрицы входом и
      выходом. Словарь и выходное предсказание сохраняются, но отдельная копия
      весов больше не нужна.
---

Ты посчитал входную таблицу в [шаге 16](#step-16) и получил около 198 миллионов. Но это только половина пути: модель ещё должна на выходе сказать, какой из 48394 токенов идёт следующим, а для этого нужна матрица ровно такой же формы. Держишь её отдельно — платишь дважды, почти 400M из 8B уходит на одну бухгалтерию словаря.

После этого куска ты должен уметь: назвать обе матрицы, объяснить, откуда берётся удвоение, и сказать, что именно исчезает при tie weights.

Обычно спотыкаются на двух местах. Первое — думают, что 400M получается из-за четырёх модальностей; нет, из-за двух матриц. Второе — считают, что связывание весов урезает словарь. Словарь остаётся прежним, пропадает вторая копия таблицы.
