- concept: 'радиус поражения считается по уникальным ресурсам: повтор ресурса в перечне доступа не расширяет границу'
  question: Функция получает перечень ресурсов, которых одно действие агента может коснуться, и должна вернуть размер границы доступа. Один и тот же путь может попасть в перечень несколько раз. Заполни пропуск.
  explanation: 'Длина исходного списка завышает границу: если инструменту дважды передали один путь, доступных ресурсов от этого не стало больше. set схлопывает дубликаты, len по нему даёт число различимых ресурсов.'
  kind: cloze
  template: |-
    def blast_radius(targets):
        return len(___(targets))
  answer: set
  accept:
    - set
    - set()
    - frozenset
  id: 031-blast-radius-1
  fingerprint: 97017c89
- concept: подсчёт границы доступа по перечню с повторами
  question: 'Действию агента разрешено обращаться к ресурсам: src/db.py, src/api.py, src/db.py, tests/test_api.py, src/api.py, config.yaml, src/db.py. Чему равен радиус поражения этого действия?'
  explanation: 'Различимых ресурсов четыре: src/db.py, src/api.py, tests/test_api.py, config.yaml. Число упоминаний в перечне — свойство записи разрешений, а не размера доступа.'
  kind: numeric
  answer: 4
  tolerance: 0.4
  id: 031-blast-radius-2
  fingerprint: 6bc695ba
- concept: единица учёта ресурсов должна совпадать с тем, на что распространяется разрешение, иначе каталог маскирует всё своё содержимое
  question: Агенту выдано разрешение на каталог logs/ (внутри 40 файлов) и на три отдельных файла. Перечень доступа записан как ["logs/", и три пути к файлам], подсчёт даёт радиус 4. Что не так с такой оценкой?
  explanation: Радиус — верхняя граница того, что действие способно затронуть при выданных правах. Запись «logs/» покрывает 40 файлов, поэтому честный подсчёт даёт 43, а не 4. Считать надо в тех единицах, которыми оперирует разрешение, иначе крупная выдача прав выглядит такой же скромной, как узкий доступ к одному файлу.
  kind: choice
  options:
    - 'Ничего: разрешений действительно выдано четыре, число верное'
    - 'Оценка занижена: разрешение на каталог распространяется на все 40 файлов внутри, значит и в радиус входят они, а не одна запись'
    - 'Оценка завышена: каталог считать нельзя, пока агент не открыл ни один файл внутри'
    - 'Оценка неприменима: радиус поражения определён только для отдельных файлов'
  correct: 1
  id: 031-blast-radius-3
  fingerprint: 4af81754
