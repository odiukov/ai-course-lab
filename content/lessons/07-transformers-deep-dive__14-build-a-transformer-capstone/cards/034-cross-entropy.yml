- concept: одинаковое сплющивание осей батча и последовательности сохраняет соответствие логитов целевым токенам
  question: Логиты имеют форму (7, 13, 29), а targets — (7, 13). Какие формы подходят для вычисления кросс-энтропии по всем позициям?
  explanation: Каждая пара «элемент батча, позиция» превращается в отдельную строку, а ось классов остаётся последней. Если логиты и targets сплющены в одинаковом порядке, каждая цель сохраняет соответствие своей строке логитов.
  kind: choice
  options:
    - Логиты (91, 29), targets (91,)
    - Логиты (29, 91), targets (91,)
    - Логиты (91, 29), targets (29,)
    - Логиты (7, 377), targets (91,)
  correct: 0
  id: 034-cross-entropy-1
  fingerprint: 3c42fab4
- concept: логит правильного класса выбирается в каждой строке по индексу из targets
  question: Заполни пропуск так, чтобы из каждой строки flat_logits был выбран логит целевого токена.
  explanation: gather использует значение target как индекс по оси словаря. Дополнительная одноэлементная ось нужна, чтобы форма индексов соответствовала требованиям операции; squeeze затем удаляет её.
  kind: cloze
  template: correct_logits = flat_logits.gather(dim=-1, index=___).squeeze(-1)
  answer: flat_targets.unsqueeze(-1)
  accept:
    - flat_targets[..., None]
  id: 034-cross-entropy-2
  fingerprint: 273bd55d
