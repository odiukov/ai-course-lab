---
id: 009-check-ship
type: check
title: 'Проверка: что отличает GPT от BERT в коде'
source_anchor: '### What we ship'
check:
  - question: >-
      Что наиболее вероятно произойдёт, если обучать авторегрессионную языковую
      модель без каузальной маски?
    options:
      - >-
        Потери могут быстро стать очень низкими, но авторегрессионная генерация
        будет плохой
      - >-
        Обучение замедлится, потому что attention сможет использовать меньше
        контекста
      - Модель не сможет вычислить градиенты для будущих позиций
      - >-
        Ничего не изменится, поскольку маска влияет только на выбор следующего
        токена
    correct: 0
    explanation: >-
      Без маски модель видит будущие символы обучающей последовательности и
      может подсматривать правильный ответ. При генерации будущих символов ещё
      нет, поэтому выученная стратегия не работает.
  - question: >-
      Какое изменение в self-attention превращает двунаправленный
      Transformer-блок в каузальный блок для GPT-подобной модели?
    options:
      - Замена обычного FFN на SwiGLU
      - Перенос нормализации перед подслоем
      - 'Добавление маски, запрещающей внимание к будущим позициям'
      - Добавление остаточных связей вокруг подслоёв
    correct: 2
    explanation: >-
      Определяющее изменение — запрет смотреть вправо в attention. SwiGLU,
      расположение нормализации и остаточные связи сами по себе не задают
      каузальность.
---

Ты дописал модель, запустил обучение, и потери валятся вниз подозрительно быстро — почти до нуля за пару сотен шагов. Радость длится до первой генерации: на экране бессвязный набор букв.

Такое почти всегда значит одно — модель подсматривала. В обучающем куске текста все символы лежат рядом, и если при предсказании пятого символа attention видит пятый символ, задача становится тривиальной: скопировать. На генерации копировать нечего, будущего ещё нет.

Здесь ты должен уметь объяснить, что каузальность — не отдельный слой и не хитрый алгоритм, а один запрет: смотреть только назад. Технически — минус бесконечность на позициях справа, после softmax они превращаются в нулевой вес. И понимать, что именно этот запрет отделяет GPT от BERT: SwiGLU, pre-norm и остаточные связи из [шага 7](#step-7) одинаковы у обоих.

Спотыкаются обычно на том, что путают маску с сэмплированием и ищут разницу в генерации, а не в attention.
