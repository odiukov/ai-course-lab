---
id: 046-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      Ты запустил обучение, и на первом же шаге loss равен примерно 4.17 при
      словаре из 65 символов. Что это значит?
    options:
      - >-
        Модель уже что-то выучила: 4.17 меньше, чем максимально возможное
        значение
      - >-
        Модель пока раскладывает вероятность поровну между всеми 65 символами —
        это ожидаемый старт
      - 'В коде ошибка: стартовый loss всегда должен быть около нуля'
      - 'Маска не работает, и модель подглядывает в будущее'
    correct: 1
    explanation: >-
      Натуральный логарифм 65 — примерно 4.17. Такой loss получается ровно
      тогда, когда модель считает все символы равновероятными, то есть ничего
      ещё не знает. Это первая проверка, что прямой проход и функция потерь
      собраны верно.
  - question: >-
      Позиция 10 в последовательности длиной 64 обрабатывается внутри attention.
      Какие позиции она может учитывать?
    options:
      - Только позицию 10 — свою собственную
      - 'Все 64 позиции: батч обрабатывается целиком'
      - Позиции с 0 по 10 включительно
      - 'Позиции с 11 по 63: модель предсказывает будущее'
    correct: 2
    explanation: >-
      Каузальная маска обнуляет вес всех позиций правее текущей, но себя позиция
      видит. Поэтому доступны 0-10, и предсказание следующего символа честное —
      подсказки из будущего не просачиваются.
  - question: >-
      Ты обучаешь модель на 155 тысяч параметров на файле в 900 символов. Train
      loss падает почти до нуля, val loss сначала снижается, а потом растёт. Что
      происходит?
    options:
      - >-
        Модель запомнила крошечный корпус наизусть вместо того, чтобы выучить
        закономерности
      - 'Learning rate слишком мал, нужно увеличить'
      - 'Сломалась обрезка градиентов, норма улетела'
      - 'Val loss всегда растёт после warmup, это нормально'
    correct: 0
    explanation: >-
      Параметров сильно больше, чем текста. Модель просто заучивает символ за
      символом, поэтому train loss стремится к нулю, а на невиданных данных
      качество начинает падать. Расхождение двух кривых — главный детектор
      переобучения.
  - question: >-
      Ты хочешь превратить свою модель на 800 тысяч параметров в модель уровня
      125 миллионов. Что придётся сделать?
    options:
      - 'Переписать attention и FFN: на больших размерах нужны другие формулы'
      - Добавить недостающие слои — в маленькой модели их нет
      - >-
        Поменять числа в конфиге: d_model, число голов, число блоков, длину
        контекста
      - 'Отказаться от tied embeddings, иначе модель не сойдётся'
    correct: 2
    explanation: >-
      Архитектура остаётся той же самой: те же блоки, та же маска, тот же
      softmax. Меняются только размерности и количество блоков — плюс, конечно,
      объём данных и вычислений.
---

Ты дошёл до конца файла, который сам же и написал: от `stoi` из [шага 4](#step-4) до top-k сэмплирования из [шага 38](#step-38). Дальше в курсе этот файл станет системой отсчёта — когда пойдёт речь о RoPE, KV-кэше или MoE, ты будешь понимать, какую именно строчку они заменяют.

Три места, где обычно спотыкаются. Первое — формы: путают, что `(B, N, d_model)` превращается в `(B, N, V)` только в самом конце, на LM-голове. Второе — маска: кажется, что она про генерацию, а на самом деле она нужна при обучении, иначе 64 предсказания за проход были бы жульничеством. Третье — loss: число само по себе ничего не говорит, пока не сравнишь его с $\ln V$ и не посмотришь на val.

Проверь, держатся ли эти три вещи.
