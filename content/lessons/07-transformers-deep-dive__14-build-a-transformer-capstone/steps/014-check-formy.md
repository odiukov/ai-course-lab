---
id: 014-check-formy
type: check
title: 'Проверка: формы на входе и выходе linear'
source_anchor: '### Step 2: model'
check:
  - question: >-
      На вход linear ты подаёшь x размером 4 строки на 8 столбцов, веса W — 8
      строк на 3 столбца, смещение b — 3 числа. Какой формы получится результат?
    options:
      - 4 строки на 3 столбца
      - 8 строк на 3 столбца
      - 4 строки на 8 столбцов
      - 3 строки на 4 столбца
    correct: 0
    explanation: >-
      Строки — это независимые примеры, их число linear не меняет: как было 4,
      так и осталось. А столбцы задаёт W: их было 8 на входе, стало 3 на выходе,
      и ровно столько же чисел в b.
  - question: >-
      Ты подаёшь x размером 2 строки на 5 столбцов, а веса W у тебя — 3 строки
      на 5 столбцов. Что будет?
    options:
      - 'Всё сойдётся: результат выйдет 2 строки на 5 столбцов'
      - 'Умножение не состыкуется: у W должно быть 5 строк, по числу столбцов x'
      - 'Всё сойдётся, но b придётся сделать длиной 5'
      - 'Умножение пройдёт, только строки и столбцы результата поменяются местами'
    correct: 1
    explanation: >-
      Каждая строка x — это 5 чисел, и на каждое из них нужен свой ряд весов.
      Значит, у W обязано быть ровно 5 строк; сколько у неё столбцов — дело
      твоё, это и есть ширина выхода.
---

Функцию из [шага 13](#step-13) ты уже написал — теперь проверим не код, а то, что важнее при сборке: сходятся ли размеры. Дальше в этом файле linear будет вызываться десятки раз подряд, и почти каждая ошибка сборки — это несостыковка формы, а не неправильная арифметика.

Уметь надо две вещи. Первое: по формам входа и весов назвать форму выхода, не запуская код. Второе: увидеть несостыковку заранее.

Спотыкаются обычно на двух местах. Путают, какая сторона W «входная», а какая «выходная». И забывают, что b — это одна строка на весь батч: его длина равна числу столбцов выхода, а не числу строк.
