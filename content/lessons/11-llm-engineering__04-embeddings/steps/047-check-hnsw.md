---
id: 047-check-hnsw
type: check
title: 'Проверка: чем платим за скорость HNSW'
source_anchor: '### Vector Databases and HNSW'
check:
  - question: >-
      База на 10 миллионов векторов, HNSW возвращает top-5 за 3 миллисекунды. Ты
      проверяешь результат полным перебором — и одного из настоящих пяти
      ближайших в ответе нет. Что происходит?
    options:
      - Индекс обязательно повреждён и требует перестроения
      - >-
        Это штатное свойство ANN: HNSW исследует только часть графа и может
        пропустить ближайший вектор
      - 'Любой такой промах доказывает, что векторы не нормализованы'
      - HNSW намеренно исключает самый близкий результат из выдачи
    correct: 1
    explanation: >-
      HNSW не сравнивает запрос со всеми векторами. Поэтому его высокая скорость
      сопровождается recall ниже 100%: отдельный ближайший сосед может остаться
      в неисследованной части графа.
  - question: >-
      Прототип ищет по 5 000 заметок. Полный перебор отвечает за 20 миллисекунд,
      допустимая задержка — 50 миллисекунд, а пропуск ближайшей заметки
      критичен. Что выбрать?
    options:
      - 'Оставить полный перебор: он уже выполняет требования и даёт точный top-k'
      - Обязательно перейти на HNSW независимо от требований
      - 'Перейти на HNSW, потому что он повышает точность поиска'
      - 'Использовать любой метод: их выдача гарантированно совпадает'
    correct: 0
    explanation: >-
      Полный перебор уже укладывается в лимит и проверяет каждый вектор. В этой
      ситуации HNSW добавит риск пропуска результата без необходимого выигрыша
      для продукта.
---

Здесь проверяем одно: ты понимаешь, что скорость HNSW не бесплатная. Полный перебор из [шага 40](#step-40) всегда даёт настоящий top-k — он честно сравнил всё. HNSW из [шага 43](#step-43) сравнивает только те векторы, которые попались по дороге вниз, и за это отдаёт часть ответов.

Спотыкаются обычно на двух вещах. Первая — принимают промах за баг и лезут перестраивать индекс, хотя пропущенный сосед в приближённом поиске штатное поведение. Вторая — ставят HNSW на десять тысяч записей, где перебор укладывается в миллисекунды: платят потерей точности за ускорение, которого не заметно.
