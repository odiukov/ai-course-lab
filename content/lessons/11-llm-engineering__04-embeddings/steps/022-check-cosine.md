---
id: 022-check-cosine
type: check
title: 'Проверка: что вернёт косинус для нулевого вектора'
source_anchor: '### Similarity Metrics'
check:
  - question: >-
      В эталонной реализации перед делением проверяются norm_a и norm_b. Что
      произойдёт, если одна из норм нулевая?
    options:
      - 'Сработает защитная ветка, и функция завершится до деления'
      - Функция обязательно выполнит деление на ноль
      - Косинус автоматически станет равен единице
      - Размерность второго вектора изменится
    correct: 0
    explanation: >-
      Проверка выполняется до вычисления дроби и возвращает 0.0, поэтому деления
      на ноль не происходит.
  - question: >-
      Почему значение, возвращаемое реализацией для нулевого вектора, является
      техническим соглашением, а не настоящим косинусом?
    options:
      - Для нулевого вектора не хватает точности вычислений
      - Косинус определён только для положительных координат
      - 'У нулевого вектора нет направления, поэтому угол с ним не определён'
      - Нулевой вектор всегда имеет неправильную размерность
    correct: 2
    explanation: >-
      Косинус отвечает на вопрос о взаимном направлении векторов. Нулевой вектор
      никуда не направлен, поэтому математического угла с ним не существует.
---

К этому моменту у тебя есть три работающие функции: `dot` из [шага 17](#step-17), `norm` из [шага 19](#step-19) и `cosine_similarity` из [шага 21](#step-21). Проверь, что ты понимаешь не только как они считают, но и где ломаются.

Главная ловушка — нулевой вектор. Люди обычно рассуждают так: «ноль ни на что не похож, значит вернётся 0.0». Но формула из [шага 20](#step-20) ставит длины в знаменатель, а длина нулевого вектора равна нулю. Ответа не будет вообще — будет падение.

Вторая ловушка тоньше: кажется, что это чисто техническая мелочь, которую можно залатать проверкой. На самом деле проблема смысловая — мерить направление у того, что никуда не направлено, нечего.
