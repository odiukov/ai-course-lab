---
id: 034-check-chunking
type: check
title: 'Проверка: что случится без перекрытия'
source_anchor: '### Chunking Strategies'
check:
  - question: >-
      Ты режешь инструкцию на фрагменты по 512 токенов без перекрытия. Ровно на
      границе 511/512 стоит фраза «возврат средств возможен, если заявка подана
      в течение 14 дней». Что произойдёт с поиском по запросу про срок возврата?
    options:
      - >-
        Ничего не изменится: обе половины фразы всё равно лежат в базе, поиск
        склеит их обратно
      - >-
        Ни один фрагмент не содержит мысль целиком, поэтому вектор каждой
        половины похож на запрос слабее, чем был бы у целой фразы
      - Поиск сломается полностью и не вернёт вообще ничего
      - 'Фрагменты станут длиннее, потому что модель дописывает обрезанный хвост'
    correct: 1
    explanation: >-
      Эмбеддер кодирует каждый фрагмент отдельно и обратно их не склеивает.
      Обрубок «возврат средств возможен, если заявка подана» и обрубок «в
      течение 14 дней» по отдельности несут неполный смысл, и близость к запросу
      падает.
  - question: >-
      Коллега решил: раз перекрытие спасает от разрезанных фраз, поставим
      фрагмент 512 и перекрытие 500. Что он получит?
    options:
      - Идеальный поиск без единого недостатка
      - Фрагменты станут короче и дешевле в хранении
      - >-
        Огромное число почти одинаковых фрагментов: больше векторов, больше
        денег и дублей в выдаче при том же тексте
      - Модель откажется кодировать такие фрагменты
    correct: 2
    explanation: >-
      Каждый следующий фрагмент сдвигается всего на 12 токенов, поэтому одного и
      того же текста получается в десятки раз больше векторов — растёт стоимость
      и выдача забивается почти идентичными кусками. Отсюда и практический
      ориентир около 50 токенов перекрытия.
---

Здесь стоит остановиться и проверить одну вещь: понимаешь ли ты, чем именно платит система, у которой перекрытия нет.

После [шага 32](#step-32) ты должен уметь взять конкретную фразу, посмотреть, куда попадает граница фрагмента, и сказать, что случится с её вектором. И отдельно — объяснить, почему перекрытие нельзя крутить вверх бесконечно.

Спотыкаются обычно на двух местах. Первое: кажется, что раз обе половины фразы лежат в базе, поиск как-нибудь их соберёт — не соберёт, каждый фрагмент кодируется сам по себе. Второе: перекрытие путают с размером фрагмента и увеличивают «на всякий случай», не замечая, что счёт за векторы растёт вместе с ним.
