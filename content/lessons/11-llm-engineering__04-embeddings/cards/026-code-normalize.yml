- concept: L2-нормализация делит все координаты ненулевого вектора на одну общую норму и безопасно обрабатывает нулевой вектор
  question: Заполни пропуск в реализации L2-нормализации.
  explanation: Все координаты нужно делить на норму исходного вектора. Проверка перед делением оставляет нулевой вектор без изменений и предотвращает деление на ноль.
  kind: cloze
  template: |-
    def normalize(vec):
        length = norm(vec)
        if not length:
            return vec
        return [value / ___ for value in vec]
  answer: length
  accept:
    - norm(vec)
  id: 026-code-normalize-1
  fingerprint: dcd5fe8b
