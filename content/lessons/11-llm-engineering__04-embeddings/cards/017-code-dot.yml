- concept: скалярное произведение в NumPy можно вычислить через np.dot и преобразовать результат в обычный Python float
  question: Заполни тело функции так, чтобы она вернула скалярное произведение как обычный Python `float`.
  explanation: '`np.dot(a, b)` вычисляет сумму покоординатных произведений, а `float(...)` преобразует NumPy-скаляр в стандартный тип Python.'
  kind: cloze
  template: |-
    def dot_product(a, b):
        return ___
  answer: float(np.dot(a, b))
  accept:
    - float(a @ b)
  id: 017-code-dot-1
  fingerprint: 08db822d
