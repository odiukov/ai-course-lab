---
id: 026-batching-check
type: check
title: >-
  Что произойдёт на следующей итерации после EOS и почему padding больше не
  нужен?
source_anchor: '### Continuous batching at the iteration level'
check:
  - question: >-
      Запрос выдал EOS во время итерации 137. Что произойдёт перед вычислением
      итерации 138?
    options:
      - Запрос останется в RUNNING до завершения самого длинного соседа
      - >-
        Запрос удалят, его KV-блоки освободят, а свободное место смогут отдать
        запросу из очереди
      - 'Запрос заменят padding-токеном, чтобы сохранить прежний размер батча'
    correct: 1
    explanation: >-
      На следующей итерации планировщик сначала удаляет завершившийся запрос и
      освобождает его память, затем может принять новый запрос и только после
      этого запускает вычисление.
  - question: Почему после удаления завершившегося запроса не нужно добавлять padding?
    options:
      - Потому что EOS автоматически превращается в обычный токен
      - Потому что размер RUNNING может меняться перед каждой итерацией
      - Потому что все запросы теперь обязаны завершаться одновременно
    correct: 1
    explanation: >-
      Forward запускается только для текущих активных последовательностей. Батч
      пересобирается на каждой итерации, поэтому пустой слот сохранять незачем.
---

Представь: запрос выдаёт EOS на итерации 137. Это ещё результат выполненного forward, поэтому освободить его заранее нельзя. Но перед итерацией 138 планировщик удалит запрос из `RUNNING`, вернёт его KV-блоки в общий пул и сможет принять ожидающий запрос — именно в порядке из [шага 21](#step-21).

Частая ошибка — мысленно сохранять «дыру» и заполнять её padding. Здесь фиксированной формы батча нет: на следующем forward участвует только новый состав `RUNNING`. Завершившаяся последовательность исчезла, значит вычислять для неё пустые токены больше не требуется.
