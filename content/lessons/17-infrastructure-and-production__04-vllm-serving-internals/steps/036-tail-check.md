---
id: 036-tail-check
type: check
title: >-
  Почему 63 куска по 512 токенов лучше одной паузы на 800 мс и какую метрику это
  защищает?
source_anchor: '### Chunked prefill protects TTFT tail'
check:
  - question: >-
      Что именно сокращает chunked prefill, если суммарный объём вычислений
      длинного промпта остаётся примерно прежним?
    options:
      - Максимальную длительность одной непрерывной блокировки decode
      - 'Общее число токенов, которые должна обработать модель'
      - Размер весов модели в памяти
      - Необходимое число слоёв attention
    correct: 0
    explanation: >-
      Весь prefill по-прежнему нужно вычислить, но после каждого куска возникает
      новая точка планирования. Поэтому одна непрерывная пауза decode
      ограничивается временем куска, а не всего prefill.
  - question: Какой компромисс возникает при разбиении длинного prefill на куски?
    options:
      - >-
        Длинный prefill может немного растянуться, зато межтокенные задержки
        активных ответов становятся стабильнее
      - >-
        Длинный prefill завершается быстрее, но активные ответы получают более
        длинные паузы
      - TTFT всех запросов уменьшается без дополнительных накладных расходов
      - 'Снижается объём KV-кэша, но задержки decode не меняются'
    correct: 0
    explanation: >-
      Границы кусков могут добавить небольшие накладные расходы к prefill, но
      позволяют чаще обслуживать decode и срезают редкие большие выбросы ITL.
---

Здесь легко приписать нарезке лишнее: она не превращает 800 мс вычислений в 13 мс. Общий prefill остаётся примерно тем же и из-за переключений может даже немного растянуться. Меняется длительность одной непрерывной блокировки.

Как в [шаге 29](#step-29), 32 000 токенов дают 63 куска: 62 полных и один остаток. После каждого куска планировщик получает возможность продвинуть decode. Поэтому защищается не TTFT длинного запроса, а P99 ITL остальных пользователей — редкие межтокенные паузы перестают приближаться к полной длительности prefill. Именно эту причинную связь ты должен отличать от простого «нарезка ускоряет работу».
