- concept: построение ответа об ошибке JSON-RPC
  question: Расположи действия при реализации `make_error` в правильном порядке.
  explanation: Сначала сохраняют связь с исходным запросом, затем формируют содержимое ошибки и внешний конверт. Ответ об ошибке не должен одновременно содержать успешный `result`.
  kind: order
  items:
    - Взять идентификатор исходного запроса
    - Сформировать объект `error` с кодом и сообщением
    - Собрать верхнеуровневый ответ с `jsonrpc`, `id` и `error`
    - Убедиться, что поле `result` отсутствует
  id: 014-make-error-1
  fingerprint: 7e083f12
