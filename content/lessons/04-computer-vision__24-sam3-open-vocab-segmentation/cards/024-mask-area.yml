- concept: площадь бинарной маски равна сумме всех её элементов
  question: Объясни, почему суммирование всех элементов бинарной маски даёт площадь выделенного объекта и почему форма массива не влияет на результат.
  explanation: В бинарной маске значения внутри объекта учитываются суммой, а значения фона её не увеличивают. Операция суммирования проходит по всему массиву, поэтому площадь не зависит от его формы.
  kind: open
  reference: При суммировании логических или бинарных значений каждый пиксель объекта даёт вклад, а фон — нет. Поэтому итог равен количеству пикселей объекта. Агрегация охватывает все элементы массива независимо от расположения по строкам и столбцам.
  id: 024-mask-area-1
  fingerprint: 1a2dc320
- concept: результат NumPy приводится к встроенному типу int для соблюдения контракта функции
  question: Заполни пропуск так, чтобы функция возвращала обычное целое число Python, а не целочисленный скаляр NumPy.
  explanation: Агрегация NumPy часто возвращает собственный скалярный тип. Вызов int(area) явно преобразует его во встроенный Python int; для целочисленного скаляра NumPy эквивалентен вызов area.item().
  kind: cloze
  template: |-
    area = binary_mask.sum()
    return ___
  answer: int(area)
  accept:
    - area.item()
  id: 024-mask-area-2
  fingerprint: a59b69f9
