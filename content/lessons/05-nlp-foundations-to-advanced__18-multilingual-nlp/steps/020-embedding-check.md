---
id: 020-embedding-check
type: check
title: 'Проверка: перевод, тематически чужой текст и top-k выдача'
source_anchor: '### Step 2: multilingual embedding space'
check:
  - question: >-
      Запрос — «The cat is sleeping». В коллекции есть французский и испанский
      переводы этой фразы, а также английское предложение про лающую собаку. Что
      вернёт top-2 поиск?
    options:
      - Французский и испанский переводы
      - Английское предложение и французский перевод
      - Только английское предложение
    correct: 0
    explanation: >-
      Модель сравнивает смысл, поэтому оба перевода окажутся ближе тематически
      чужого текста, даже если он написан на языке запроса.
  - question: Ты увеличиваешь top-k с 2 до 3. Что изменится?
    options:
      - Сходство всех документов станет выше
      - Поиск добавит следующий документ из уже рассчитанного рейтинга
      - Модель заново переведёт запрос на три языка
    correct: 1
    explanation: >-
      Параметр top-k задаёт число возвращаемых результатов, но не меняет их
      эмбеддинги или оценки сходства.
---

Здесь ты проверяешь связку из [шагов 18](#step-18) и [19](#step-19): перевод должен обойти тематически чужой текст, даже если чужой текст написан на языке запроса. Главная ловушка — принять совпадение языка за совпадение смысла.

Сначала модель строит единый рейтинг по сходству, затем `top-k` просто отрезает первые `k` позиций. Поэтому увеличение `k` не улучшает понимание запроса и не пересчитывает близость: в выдачу лишь попадает следующий, менее похожий документ. Язык влияет меньше, чем содержание.
