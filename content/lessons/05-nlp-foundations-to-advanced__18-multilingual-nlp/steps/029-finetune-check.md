---
id: 029-finetune-check
type: check
title: 'Проверка: эпохи, learning rate, удаление сырых колонок и padding'
source_anchor: '### Step 3: few-shot fine-tuning strategy'
check:
  - question: >-
      Почему при few-shot адаптации несколько проходов по небольшому датасету
      сочетают с маленьким learning rate?
    options:
      - >-
        Повторные проходы помогают усвоить немногочисленные примеры, а маленький
        шаг ограничивает перезапись многоязычных представлений
      - >-
        Маленький learning rate автоматически уменьшает число примеров в каждой
        эпохе
      - >-
        Повторные проходы выравнивают длины последовательностей, поэтому padding
        становится не нужен
    correct: 0
    explanation: >-
      Пять эпох дают модели несколько встреч с каждым примером, а learning rate
      2e-5 удерживает обновления небольшими. Это позволяет адаптироваться к
      целевому языку без резкого разрушения многоязычного пространства.
  - question: >-
      Что может произойти, если после токенизации оставить сырые text и label,
      включить truncation, но не использовать padding-коллатор?
    options:
      - >-
        Trainer сам удалит строки, а truncation дополнит все последовательности
        до одинаковой длины
      - >-
        Коллатор может столкнуться со строками и последовательностями разной
        длины, из-за чего сборка батча завершится ошибкой
      - >-
        Модель обучится нормально, но всегда будет обрабатывать
        последовательности полной максимальной длины
    correct: 1
    explanation: >-
      Truncation лишь обрезает примеры длиннее max_length=128 и не выравнивает
      короткие. remove_columns удаляет сырые text и label, а
      DataCollatorWithPadding дополняет числовые последовательности до
      максимальной длины текущего батча.
---

Перед проверкой свяжи четыре решения в одну цепочку. Для 100–500 примеров пять эпох дают модели несколько проходов по данным, а маленький learning rate из [шага 27](#step-27) не позволяет этим проходам резко перезаписать многоязычные знания. После токенизации исходные `text` и `label` нужно удалить, сохранив созданное поле `labels`: иначе сборщик встретит строки вместо числовых тензоров.

Главная ловушка — перепутать обрезание и выравнивание. `truncation` ограничивает слишком длинные примеры, но не делает короткие одинаковыми. Эту работу выполняет динамический padding из [шага 28](#step-28), причём отдельно для каждого батча.
