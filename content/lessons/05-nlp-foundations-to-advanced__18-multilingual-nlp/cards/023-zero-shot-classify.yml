- concept: отключение autograd при инференсе уменьшает лишние вычисления и расход памяти
  question: Зачем оборачивать вызов NLI-модели при zero-shot-классификации в torch.no_grad()?
  explanation: При классификации параметры модели не обновляются, поэтому граф для обратного распространения не нужен. torch.no_grad() отключает его построение и сокращает накладные расходы инференса.
  kind: choice
  options:
    - Чтобы autograd не строил граф вычислений и не сохранял данные для обратного прохода
    - Чтобы автоматически перевести логиты в вероятности
    - Чтобы модель сама выбрала индекс класса entailment
  correct: 0
  id: 023-zero-shot-classify-1
  fingerprint: d44c8d19
- concept: softmax превращает равные NLI-логиты в равномерное распределение вероятностей
  question: Логиты NLI-модели для contradiction, neutral и entailment равны [7, 7, 7]. Какова вероятность entailment после softmax?
  explanation: При равных логитах softmax распределяет всю вероятность поровну между классами. Поэтому entailment получает одну треть общей вероятности.
  kind: numeric
  answer: 0.333
  tolerance: 0.001
  id: 023-zero-shot-classify-2
  fingerprint: 1b622658
- concept: индекс класса entailment следует получать из конфигурации модели, а не задавать вручную
  question: Заполни пропуск так, чтобы получить индекс entailment из отображения меток, сохранённого в конфигурации модели.
  explanation: Порядок выходных классов зависит от конфигурации конкретного чекпоинта. model.config.label2id связывает имя entailment с правильной позицией в векторе логитов.
  kind: cloze
  template: entailment_id = model.config.label2id[___]
  answer: '"entailment"'
  accept:
    - '''entailment'''
  id: 023-zero-shot-classify-3
  fingerprint: cb40f1ea
