---
id: 029-check-decomposition
type: check
title: 'Проверка: что усреднять по моделям, а что по точкам'
source_anchor: '### Step 3: Computing Bias^2, Variance Decomposition'
check:
  - question: >-
      В массиве 200 строк с моделями и 50 столбцов с тестовыми точками. Что
      получится после усреднения по строкам?
    options:
      - Одно среднее число для всех предсказаний
      - 50 средних предсказаний — по одному для каждой точки
      - 200 средних предсказаний — по одному для каждой модели
    correct: 1
    explanation: >-
      Для каждой из 50 точек усредняются предсказания всех 200 моделей, поэтому
      остаётся средняя кривая из 50 значений.
  - question: Как правильно получить общую оценку variance?
    options:
      - >-
        Сначала измерить разброс моделей в каждой точке, затем усреднить
        результаты по точкам
      - >-
        Сначала усреднить точки внутри каждой модели, затем измерить разброс
        этих средних
      - Сравнить среднее предсказание с истинными значениями во всех точках
    correct: 0
    explanation: >-
      Variance показывает, насколько разные модели расходятся в одной и той же
      точке; затем эти локальные разбросы объединяются усреднением по точкам.
---

После [шага 28](#step-28) у тебя есть таблица предсказаний: строки соответствуют моделям, столбцы — тестовым точкам. Здесь легко перепутать направления и получить вполне правдоподобное, но бессмысленное число. Проверь, что для bias² и variance ты сначала смотришь на множество моделей в каждой фиксированной точке. Лишь затем результаты по точкам складываются в общую оценку.

> 🎒 **На пальцах.** Представь 200 фотографов, снимающих 50 одинаковых предметов. Для каждого предмета ты сначала сравниваешь снимки всех фотографов: находишь их средний результат или разброс. Если сперва смешать 50 предметов внутри работы каждого фотографа, ты уже измеряешь различия между предметами, а не между фотографами.
