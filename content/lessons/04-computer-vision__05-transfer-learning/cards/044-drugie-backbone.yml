- concept: timm выбирают при нехватке моделей в torchvision или необходимости единообразно сравнивать разные backbone
  question: Команда хочет сравнить несколько предобученных зрительных backbone, часть которых отсутствует в torchvision, и не писать отдельный код создания для каждого семейства. Какой инструмент лучше подходит?
  explanation: timm содержит большой каталог предобученных зрительных моделей и предоставляет единый интерфейс их создания. Это упрощает замену и сравнение backbone, которых нет в torchvision.
  kind: choice
  options:
    - timm
    - Только torchvision с ручной реализацией недостающих архитектур
    - AutoModelForCausalLM
  correct: 0
  id: 044-drugie-backbone-1
  fingerprint: ae00639a
- concept: num_labels задаёт размер классификационного выхода в AutoModelForImageClassification
  question: Заполни имя аргумента, который адаптирует классификационный выход предобученной модели Transformers под число классов целевой задачи.
  explanation: Метод from_pretrained загружает предобученную архитектуру и checkpoint, а аргумент num_labels задаёт размер выхода классификатора для целевой задачи.
  kind: cloze
  template: model = AutoModelForImageClassification.from_pretrained(name, ___=n_classes)
  answer: num_labels
  accept:
    - num_labels
  id: 044-drugie-backbone-2
  fingerprint: '81654635'
