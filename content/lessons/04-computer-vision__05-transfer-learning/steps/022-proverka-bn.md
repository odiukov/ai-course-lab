---
id: 022-proverka-bn
type: check
title: 'Проверка: веса и статистики BatchNorm — не одно и то же'
source_anchor: '### The BatchNorm problem'
check:
  - question: >-
      Ты запретил градиенты для всех параметров backbone, а затем вызвал
      model.train(). Что произойдёт с BatchNorm?
    options:
      - 'Ни параметры, ни running_mean и running_var не изменятся'
      - 'Параметры не обучаются, но running_mean и running_var могут обновляться'
      - 'Параметры обучаются, а running_mean и running_var остаются прежними'
    correct: 1
    explanation: >-
      requires_grad управляет градиентами обучаемых параметров, но не буферами
      BatchNorm. После model.train() бегущие статистики могут продолжить
      обновляться.
  - question: >-
      Ты перевёл BatchNorm в режим eval, но оставил его обучаемые параметры
      доступными оптимизатору. Что верно?
    options:
      - Режим eval автоматически запрещает градиенты для параметров BatchNorm
      - BatchNorm перестаёт выполнять нормализацию в прямом проходе
      - >-
        Бегущие статистики не обновляются, но параметры масштаба и сдвига всё
        ещё могут обучаться
    correct: 2
    explanation: >-
      eval фиксирует поведение бегущих статистик, но не меняет requires_grad.
      Поэтому обучаемые параметры масштаба и сдвига могут обновляться
      оптимизатором.
---

Проверь, не смешиваешь ли ты два независимых переключателя. `requires_grad` управляет весами, а `train()` и `eval()` — поведением бегущих статистик BatchNorm. Поэтому замороженный backbone после `model.train()` ещё не полностью неподвижен: его `running_mean` и `running_var` могут меняться. Именно поэтому в [шаге 20](#step-20) важен порядок вызовов, а функция из [шага 21](#step-21) отдельно переводит BatchNorm в `eval`.

> 🎒 **На пальцах.** Представь кассира с настройками сканера и журналом средней суммы покупок. Запретить менять настройки — не значит запретить дописывать журнал. Веса BatchNorm похожи на настройки, а бегущие статистики — на журнал: для полной заморозки нужно отдельно остановить каждое изменение.
