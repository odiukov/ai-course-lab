---
id: 037-proverka-cikla
type: check
title: 'Проверка: режимы модели, scheduler и порядок обновлений'
source_anchor: '### Step 5: A minimal end-to-end fine-tuning loop'
check:
  - question: >-
      Ты хочешь заморозить бегущие статистики BatchNorm на время обучающей
      эпохи. В каком порядке нужно переключать режимы?
    options:
      - 'Сначала перевести слои BatchNorm в eval, затем вызвать model.train()'
      - 'Сначала вызвать model.train(), затем перевести слои BatchNorm в eval'
      - Вызвать model.eval() и провести в этом режиме всю обучающую эпоху
    correct: 1
    explanation: >-
      model.train() рекурсивно возвращает BatchNorm в обучающий режим. Поэтому
      после него нужно снова перевести именно BatchNorm-слои в eval, оставив
      остальную модель в train.
  - question: >-
      Какой порядок обновлений соответствует расписанию learning rate,
      рассчитанному по эпохам?
    options:
      - >-
        В начале эпохи вызвать scheduler.step(), а optimizer.step() — один раз
        после всех батчей
      - >-
        На каждом батче очистить градиенты, выполнить backward и
        optimizer.step(), а после всех обучающих батчей вызвать scheduler.step()
      - 'На каждом батче вызвать scheduler.step(), затем model.eval() и backward'
    correct: 1
    explanation: >-
      Градиенты и параметры обновляются для каждого обучающего батча. Если шаг
      расписания соответствует эпохе, scheduler.step() вызывают один раз после
      завершения всех её обучающих батчей.
---

Здесь важно удержать не отдельные команды, а их порядок. После `model.train()` снова заморозь статистики BatchNorm, если выбрал этот режим в [шаге 19](#step-19). Внутри батча следуй безопасной последовательности из [шага 34](#step-34), а scheduler переключай только после всех обучающих батчей. Проверку запускай через `model.eval()` без вычисления градиентов. Частая ошибка — поставить правильную команду в неправильное место и незаметно изменить обучение.

> 🎒 **На пальцах.** Представь теплицу: утром ты включаешь общий дневной режим, затем отдельно закрываешь клапаны, которые не должны менять настройки. Растения поливаешь по очереди, а расписание полива сдвигаешь лишь в конце дня. Если сначала закрыть клапаны, а потом включить общий режим, автоматика откроет их снова.
