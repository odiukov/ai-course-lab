---
id: 047-itogovyj-quiz
type: quiz
title: 'Итоговая проверка: выбрать, настроить и диагностировать перенос'
source_anchor: '## Key Terms'
check:
  - question: Зачем перед настройкой полного fine-tuning измерять качество linear probe?
    options:
      - >-
        Чтобы оценить полезность замороженных признаков и получить базовую точку
        для диагностики fine-tuning
      - Чтобы обновить статистики BatchNorm исходного домена
      - Чтобы подобрать число слоёв в классификационной голове
      - Чтобы проверить качество модели после обучения с нуля
    correct: 0
    explanation: >-
      Linear probe показывает, насколько задача уже решается готовым
      представлением. Если последующий fine-tuning ухудшает этот результат,
      сначала ищут ошибку в настройке переноса.
  - question: >-
      Ты обучаешь только новую голову и вызываешь model.train() в начале эпохи.
      Какая настройка сохраняет замороженное представление backbone?
    options:
      - 'Разморозить backbone, оставив BatchNorm в train-режиме'
      - >-
        Заморозить backbone и после model.train() вернуть его BatchNorm в
        eval-режим
      - 'Заморозить голову, но разрешить backbone обновлять веса'
      - Заморозить все параметры и продолжать обновлять статистики BatchNorm
    correct: 1
    explanation: >-
      Заморозка параметров не останавливает обновление running_mean и
      running_var. Поэтому BatchNorm замороженного backbone отдельно возвращают
      в eval-режим после общего model.train().
  - question: >-
      После нескольких эпох ты разморозил позднюю стадию backbone. Что нужно
      сделать до следующего обновления параметров?
    options:
      - >-
        Продолжить со старым оптимизатором: он автоматически обнаружит
        размороженные параметры
      - >-
        Назначить ранним стадиям learning rate выше, чем классификационной
        голове
      - >-
        Пересобрать оптимизатор с размороженными параметрами и подходящими для
        них learning rate
      - Заново случайно инициализировать классификационную голову
    correct: 2
    explanation: >-
      Изменение requires_grad не добавляет параметры в уже созданные группы
      оптимизатора. После разморозки оптимизатор пересобирают с актуальным
      набором параметров.
  - question: >-
      Linear probe показывает слабый результат. Fine-tuning проходит стабильно,
      предсказания не вырождаются в один класс, но обучение только головы и
      последней стадии быстро выходит на плато. Какую гипотезу разумнее
      проверить следующей?
    options:
      - Предобученные признаки разрушаются из-за катастрофического коллапса
      - Для новой задачи адаптируется недостаточная часть backbone
      - Linear probe случайно обновляет все параметры backbone
      - Нужно полностью запретить градиенты классификационной голове
    correct: 1
    explanation: >-
      Слабый linear probe говорит, что готового представления недостаточно, а
      стабильное обучение без коллапса не похоже на разрушительно высокий
      learning rate. Следующий разумный тест — разморозить больше поздних
      стадий.
---

Дальше тебе понадобится не отдельный приём, а связка решений: выбрать режим по условиям задачи ([шаги 7](#step-7)–[8](#step-8)), проверить обучаемые параметры и BatchNorm ([шаги 15](#step-15), [20](#step-20)), назначить скорости стадиям и пересобрать оптимизатор после разморозки ([шаги 23](#step-23), [33](#step-33)). Главная ловушка — лечить падение качества наугад. Сначала сравни linear probe и fine-tuning, затем проверь три тихие поломки из [шага 45](#step-45): learning rate, статистики BatchNorm и недостаточную адаптацию.

> 🎒 **На пальцах.** Ты ведёшь экспедицию с опытной командой. Сначала решаешь, кого отправить на маршрут, затем проверяешь снаряжение и распределяешь нагрузку. Если группа сбилась, ты не меняешь сразу весь план: сначала выясняешь, подвела ли скорость, приборы или нехватка разведки. В переносе порядок проверки так же важен, как выбранный рецепт.
