---
id: 041-check-rerank
type: check
title: 'Проверка: что реранкер может, а чего не может'
source_anchor: '## Use It'
check:
  - question: >-
      Гибрид вернул 50 кандидатов, нужного чанка среди них нет. Что даст
      добавление cross-encoder-реранкера?
    options:
      - Реранкер дотянется до остального корпуса и найдёт пропущенный чанк
      - 'Ничего: он только переставит те же 50, нужного среди них не появится'
      - Реранкер поднимет качество за счёт более точных векторов на этапе поиска
    correct: 1
    explanation: >-
      Реранкер работает строго с переданным ему списком кандидатов. Он меняет
      порядок, но не добавляет новые документы — промах поиска остаётся
      промахом.
  - question: >-
      Ты решил убрать первый этап и прогнать cross-encoder сразу по всем двум
      миллионам чанков. Что произойдёт?
    options:
      - >-
        Качество вырастет, но запрос станет считаться часами: cross-encoder в
        100–1000 раз медленнее
      - 'Ничего не изменится: cross-encoder кэширует эмбеддинги, как bi-encoder'
      - 'Ответ будет тот же и так же быстро, просто без лишнего кода'
    correct: 0
    explanation: >-
      Cross-encoder гоняет модель на каждой паре «запрос + документ» и ничего не
      кэширует заранее. Именно поэтому нужна воронка: дешёвый поиск отбирает 50,
      дорогой реранкер сортирует только их.
---

После блока про реранжирование ты должен уметь отвечать на два практических вопроса: что реранкер исправит и что не исправит.

Исправит — порядок. Из [шага 33](#step-33) понятно почему: cross-encoder читает запрос и документ вместе и видит, отвечает ли текст на вопрос, а не просто похож ли он на него.

Не исправит — состав списка. Реранкер получает ровно тех кандидатов, что дал гибрид, и не имеет доступа к остальному корпусу.

Спотыкаются обычно на двух вещах. Первая: ждут от реранкера, что он «найдёт» пропущенное — а он только сортирует. Вторая: недооценивают цену и пробуют реранжировать весь корпус, забыв, откуда взялась воронка 50 → 5 из [шага 34](#step-34).
