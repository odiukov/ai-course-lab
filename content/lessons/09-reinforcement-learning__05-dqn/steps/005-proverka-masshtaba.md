---
id: 005-proverka-masshtaba
type: check
title: 'Проверяем: зачем DQN нужна аппроксимация и что делает её нестабильной'
source_anchor: '## The Problem'
check:
  - question: >-
      Почему табличный Q-learning непрактичен для агента, который получает на
      вход изображения?
    options:
      - >-
        Для каждой возможной пары состояния и действия пришлось бы хранить
        отдельное Q-значение
      - Табличный Q-learning может работать только с текстовыми состояниями
      - Изображения не позволяют агенту получать числовую награду
    correct: 0
    explanation: >-
      Число возможных изображений огромно, а Q-таблица требует отдельной записи
      для каждой пары состояния и действия. Нейросетевая аппроксимация позволяет
      не перечислять все состояния.
  - question: >-
      Что непосредственно создаёт риск расхождения при наивном обучении
      нейросетевой Q-функции?
    options:
      - Большое число признаков во входном изображении
      - >-
        Зависимость целей обновления от Q-оценок, которые эта же сеть постоянно
        изменяет
      - Отсутствие отдельного выхода сети для каждого возможного состояния
    correct: 1
    explanation: >-
      Большая размерность входа создаёт проблему масштаба, но не объясняет
      расхождение. Нестабильность возникает, когда бутстрэппинговая цель зависит
      от изменяемых оценок сети: цель движется, а ошибки могут усиливаться.
---

Здесь важно не смешать две разные проблемы. Из [шага 2](#step-2) тебе нужно вынести причину аппроксимации: перечислить все сложные состояния невозможно. Из [шага 3](#step-3) — её пользу: сеть переносит найденные закономерности на незнакомые состояния.

Но способность обобщать ещё не гарантирует устойчивого обучения. Как показано в [шаге 4](#step-4), сеть учится по целям, которые зависят от её же меняющихся оценок. Частая ошибка — считать, что нестабильность возникает просто из-за большого числа пикселей. Пиксели создают проблему масштаба; движущаяся цель создаёт риск расхождения.
