---
id: 015-proverka-form
type: check
title: Проверяем формы 676 параметров и смысл четырёх выходов сети
source_anchor: '### Step 2: a tiny Q-network (manual MLP)'
check:
  - question: >-
      Почему подсчёт только элементов матриц W1 и W2 не даёт полного числа
      обучаемых параметров QNet?
    options:
      - Потому что входной вектор тоже становится параметром сети
      - >-
        Потому что списки b1 и b2 содержат смещения нейронов, участвующие в
        вычислениях и обучении
      - >-
        Потому что функция ReLU имеет отдельный обучаемый коэффициент для
        каждого входа
      - Потому что скрытые активации h сохраняются как постоянные параметры
    correct: 1
    explanation: >-
      Кроме 512 и 128 весов сеть содержит 32 и 4 обучаемых смещения. Поэтому
      полный подсчёт даёт 676 параметров.
  - question: >-
      Почему четыре значения, возвращаемые QNet, нельзя интерпретировать как
      вероятности действий?
    options:
      - >-
        Потому что каждое значение является независимой Q-оценкой ожидаемой
        выгоды действия, а нормализация в сети отсутствует
      - Потому что вероятности можно вычислять только для двух действий
      - Потому что первый линейный слой всегда удаляет информацию о вероятностях
      - Потому что список h уже содержит вероятности всех действий
    correct: 0
    explanation: >-
      Выходной слой возвращает четыре Q-оценки — по одной на действие. Softmax
      или другой нормализации здесь нет, поэтому значения могут быть
      отрицательными и не обязаны складываться в единицу.
---

Посмотри на сеть из [шага 9](#step-9): легко сосчитать только веса и получить 640. Но смещения тоже обучаются, поэтому полный счёт — 512 + 32 + 128 + 4 = 676. Формы должны стыковаться с проходом из [шага 13](#step-13): 16 входов превращаются в 32 скрытых значения, затем — в четыре Q-оценки. Эти четыре числа не обязаны быть положительными или складываться в единицу: каждое относится к своему действию.

> 🎒 **На пальцах.** Представь табло лифта с четырьмя направлениями движения робота. На нём не проценты уверенности, а четыре независимые оценки выгоды. Робот смотрит, у какого направления число выше. А 676 регуляторов внутри табло — все веса и смещения, совместно породившие эти четыре показания.
