---
id: 033-proverka-cikla
type: check
title: Проверяем порядок действий агента и обработку терминального перехода
source_anchor: '### Step 4: the outer loop'
check:
  - question: >-
      Агент получил награду, а done равен true. Что нужно сделать с полученным
      переходом?
    options:
      - 'Не записывать его, поскольку эпизод уже завершён'
      - >-
        Записать его с done равным true и при обучении не добавлять
        бутстрэппинговую оценку будущего
      - 'Записать его с done равным false, чтобы сеть оценила продолжение эпизода'
    correct: 1
    explanation: >-
      Терминальный переход сохраняют вместе с наградой. При done равном true
      TD-цель не включает оценку следующего состояния, поскольку продолжения
      эпизода нет.
  - question: В каком порядке агент обрабатывает один шаг взаимодействия со средой?
    options:
      - 'Обучает сеть, выбирает действие, выполняет его и записывает переход'
      - >-
        Выбирает действие, обучает сеть, выполняет действие и записывает
        результат
      - >-
        Выбирает действие, выполняет его, записывает полный переход и затем при
        достаточном размере буфера обучается
    correct: 2
    explanation: >-
      Сначала агент должен получить полный переход из среды и сохранить его.
      Только после этого накопленный опыт можно использовать для выборки
      минибатча и обновления сети.
---

Проследи один переход из [цикла на шаге 31](#step-31): ты выбираешь действие, получаешь новое состояние, награду и `done`, а затем целиком записываешь этот опыт. Только после записи можно брать минибатч и обучаться. Частая ошибка — выбросить последний переход эпизода. Но именно он нередко содержит решающую награду: его сохраняют с `done = true`, чтобы цель из [шага 17](#step-17) не учитывала несуществующее будущее.

> 🎒 **На пальцах.** Представь каскадёра, который ведёт журнал каждого дубля. Сначала он выполняет трюк, затем записывает результат и лишь потом разбирает прошлые записи. Если дубль завершил съёмку, страницу не вырывают: на ней ставят печать «конец». Благодаря печати при разборе никто не придумывает продолжение сцены, которого уже не было.
