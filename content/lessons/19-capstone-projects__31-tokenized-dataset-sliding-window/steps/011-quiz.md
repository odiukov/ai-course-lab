---
id: 011-quiz
type: quiz
title: 'Связываем T, stride, число окон и воспроизводимый порядок батчей'
source_anchor: '## Why a sliding window'
check:
  - question: >-
      Почему перекрывающиеся окна могут создавать более разнообразные задачи
      предсказания, хотя исходный поток токенов остаётся тем же?
    options:
      - Токены оказываются на разных позициях относительно границ контекста
      - Токенизатор назначает словам новые id для каждого окна
      - Для каждого окна вручную создаётся новая разметка
      - Размер словаря увеличивается при каждом сдвиге окна
    correct: 0
    explanation: >-
      Меньший stride сдвигает границы контекста. Поэтому одни и те же участки
      потока встречаются в разных контекстных позициях, создавая новые задачи
      предсказания.
  - question: >-
      Что изменится, если оставить поток токенов, T и stride прежними, но
      выбрать seed другой эпохи?
    options:
      - 'Изменится порядок уже доступных окон, но не их состав и количество'
      - Изменится длина каждого окна
      - Появятся дополнительные окна в конце потока
      - Target перестанет быть сдвинут относительно input
    correct: 0
    explanation: >-
      T и stride определяют сами окна. Seed управляет только перестановкой их
      индексов перед формированием батчей.
  - question: >-
      Почему при предобучении на огромном корпусе часто выбирают stride, равный
      T, вместо сильного перекрытия окон?
    options:
      - >-
        Перекрытие увеличивает вычисления, а выгода от дополнительных границ
        мала на и без того огромном корпусе
      - При меньшем stride невозможно построить target
      - Перекрывающиеся окна нельзя объединять в батчи
      - Stride меньше T меняет словарь токенизатора
    correct: 0
    explanation: >-
      Маленький stride создаёт больше перекрывающихся примеров и удлиняет эпоху.
      Если модель всё равно не успевает пройти весь корпус, дополнительные
      вычисления обычно не окупаются.
  - question: >-
      Два DataLoader используют один датасет, одинаковые base_seed и epoch,
      shuffle=True и drop_last=False, но разные batch_size. Что должно совпасть?
    options:
      - 'Порядок индексов после разворачивания всех батчей, но не границы батчей'
      - 'И порядок индексов, и границы батчей'
      - Только количество батчей
      - 'Ничего: batch_size изменяет случайную перестановку'
    correct: 0
    explanation: >-
      Одинаковый seed эпохи задаёт одну перестановку индексов. Batch size не
      меняет её, но разрезает последовательность на группы другого размера.
---

Эта связка понадобится дальше, когда ты будешь сравнивать запуски честно: `T` задаёт задачу модели, `stride` — число и перекрытие примеров, а seed эпохи — их порядок. Чаще всего путают изменение содержимого датасета с перестановкой: первые два параметра определяют доступные окна ([шаги 4](#step-4)–[7](#step-7)), seed лишь раскладывает их по батчам ([шаги 9](#step-9)–[10](#step-10)).
