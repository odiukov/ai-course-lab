- concept: агент — это система, в которой последовательность шагов и вызовов инструментов определяет сама модель, а не код
  question: 'LLM получает задачу «почини падающий тест»: сама решает, какие файлы прочитать, какие команды запустить, сколько итераций сделать и когда остановиться. Ни одна из этих последовательностей не прописана в коде. Как классифицируется такая система?'
  explanation: 'Разделение проходит по тому, кто владеет графом выполнения. Ограниченный список инструментов и наличие повторяющегося цикла ничего не меняют: пока следующий шаг выбирает модель, это агент.'
  kind: choice
  options:
    - 'Агент: маршрут выполнения выбирает модель на лету, а не заранее написанный код'
    - 'Воркфлоу: набор доступных инструментов всё равно ограничен разработчиком'
    - 'Воркфлоу: раз есть цикл «прочитал — запустил — проверил», порядок шагов фиксирован'
  correct: 0
  id: 003-checkgraf-1
  fingerprint: 61f82f64
- concept: и воркфлоу, и агент состоят из LLM плюс инструменты — различает их источник управления, а не наличие инструментов или решений модели
  question: Система вызывает LLM четыре раза и обращается к трём внешним инструментам; модель принимает содержательные решения на каждом вызове. Достаточно ли этого, чтобы назвать её агентом?
  explanation: Оба типа систем — это LLM плюс инструменты. Если порядок вызовов и развилки заранее написаны в коде, это воркфлоу, сколько бы решений модель ни принимала внутри отдельных шагов. Агент — когда путь выбирает модель.
  kind: choice
  options:
    - 'Да: несколько вызовов модели с инструментами и есть определение агента'
    - 'Нет: и воркфлоу, и агент — это LLM с инструментами; смотреть надо, кто задаёт последовательность шагов'
    - 'Нет: агентом систему делает только память между запусками'
  correct: 1
  id: 003-checkgraf-2
  fingerprint: b17b9eef
- concept: 'практическое следствие владения графом: у воркфлоу отладка сводится к чтению собственного кода, у агента — к разбору фактической траектории'
  question: Почему воркфлоу дешевле, быстрее и проще в отладке, чем агент, и что тогда вообще оправдывает выбор агента?
  explanation: Место есть для обоих. Воркфлоу выигрывает на предсказуемых задачах с ограничением по стоимости или требованиями аудита; агент берётся там, где граф шагов нельзя выписать заранее.
  kind: open
  reference: 'У воркфлоу путь фиксирован в коде: число вызовов модели и шагов известно заранее, значит стоимость и латентность ограничены сверху, а поведение воспроизводимо и его можно аудировать. Агент решает задачи без заранее заданных границ — когда нужный набор шагов нельзя перечислить при написании кода. Оба подхода легитимны, выбор делается по природе задачи, а не по «продвинутости» решения.'
  id: 003-checkgraf-3
  fingerprint: 9d79a019
