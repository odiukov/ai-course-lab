---
id: 037-check-otkazy
type: check
title: 'Проверка: повторить или переинициализироваться'
source_anchor: '### Transport failure modes'
check:
  - question: >-
      Клиент отправил POST на /mcp и получил в ответ 502 от прокси. Что он
      должен сделать?
    options:
      - Подождать короткую паузу и повторить тот же запрос один раз
      - Сразу пройти рукопожатие заново и получить новый идентификатор сессии
      - 'Повторять запрос в цикле, пока сервер не ответит успехом'
      - Считать сессию мёртвой и закрыть SSE-канал
    correct: 0
    explanation: >-
      502 и 504 означают, что споткнулся посредник или вышестоящий сервис, а
      сессия на сервере жива. Достаточно одного повтора после паузы; бесконечный
      цикл повторов и новое рукопожатие тут лишние.
  - question: >-
      Сервер вернул ресурс со сроком годности, а часы клиента спешат на две
      минуты. Как клиенту считать, когда ресурс протух?
    options:
      - По своим локальным часам — они ближе к пользователю
      - Взять среднее между своим временем и временем сервера
      - По меткам времени сервера — они единственно верные
    correct: 2
    explanation: >-
      Расхождение часов у клиента и сервера неизбежно, и любая арифметика по
      локальным часам разъезжается с серверной. Клиент опирается на серверные
      метки времени.
---

Два предыдущих экрана разложили пять отказов на две реакции, и теперь важно, чтобы ты различал их не по названию, а по тому, что видишь в ответе. Сервер жив, но споткнулся посредник — повторяешь. Сервер тебя не помнит или процесс умер — идёшь на `initialize` заново, как в [шаге 14](#step-14).

Спотыкаются обычно на двух вещах. Первая: повторяют запрос на 404 — получается цикл, который никогда не сойдётся, потому что сессии больше нет и повторение её не воскресит. Вторая: обрыв SSE принимают за смерть сессии и делают лишнее рукопожатие вместо переподключения с прежним `Mcp-Session-Id` и `last-event-id`.

Проверь себя на двух вопросах.
