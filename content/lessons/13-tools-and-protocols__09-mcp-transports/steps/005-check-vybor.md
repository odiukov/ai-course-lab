---
id: 005-check-vybor
type: check
title: 'Проверка: какой транспорт брать'
source_anchor: '## The Problem'
check:
  - question: >-
      Ты ставишь сервер, который читает файлы в твоей же папке, и запускает его
      Claude Desktop на этом ноутбуке. Что выбираешь?
    options:
      - 'stdio: клиент сам поднимает процесс на этой машине, сеть не нужна'
      - 'Streamable HTTP: так надёжнее, даже если всё лежит рядом'
      - 'HTTP+SSE: локальным серверам исторически положен SSE-канал'
      - stdio для запросов и HTTP для ответов — по одному каналу на направление
    correct: 0
    explanation: >-
      Всё происходит на одной машине, поэтому хватает потоков ввода-вывода
      запущенного процесса. Тянуть сюда сеть, порты и заголовки сессии просто
      нечем оправдать.
  - question: >-
      Ты поднимаешь новый сервер, к которому будут подключаться коллеги из
      других городов, и это середина 2026 года. Как правильно?
    options:
      - 'Сделать две точки входа: POST для запросов и отдельный SSE-канал'
      - >-
        Одна точка входа Streamable HTTP: POST и GET на один адрес, сессия по
        Mcp-Session-Id
      - 'stdio, а наружу пробросить порт — транспорт всё равно один и тот же'
      - 'Сразу оба удалённых режима, чтобы старые клиенты тоже подключились'
    correct: 1
    explanation: >-
      Streamable HTTP — действующий удалённый транспорт с 2025-03-26: один
      адрес, POST и GET на нём, общий заголовок сессии. Старый режим с двумя
      точками входа серверы как раз выключают.
---

Здесь проверяем ровно одно умение: по описанию ситуации назвать транспорт, не заглядывая в шпаргалку. Услышал «клиент сам запускает сервер на моей машине» — это stdio. Услышал «подключаются извне, через сеть» — Streamable HTTP, одна точка входа.

Спотыкаются обычно на двух местах. Первое: пытаются выбрать «который лучше» — а выбора по качеству тут нет, есть выбор по расположению, как в [шаге 4](#step-4). Второе: путают старый HTTP+SSE с двумя точками входа и нынешний Streamable HTTP с одной, хотя разница ровно в этом ([шаг 2](#step-2)) — и от даты она не зависит, зависит только от того, что сервер согласен принять ([шаг 3](#step-3)).
