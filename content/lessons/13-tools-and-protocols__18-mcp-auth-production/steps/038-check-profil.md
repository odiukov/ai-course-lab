---
id: 038-check-profil
type: check
title: 'Проверка: где в челлендже ошибка'
source_anchor: '### MCP Spec 2025-11-25 Auth Profile'
check:
  - question: >-
      Сервер отвечает 401 и отдаёт WWW-Authenticate: Bearer
      error="invalid_token", resource="https://mcp.example.com/api". Что здесь
      не так?
    options:
      - 'Всё верно: клиент увидит адрес ресурса и сам определит адрес метаданных'
      - >-
        Параметр должен называться resource_metadata и указывать на документ
        метаданных, а не на сам ресурс
      - >-
        На 401 нельзя возвращать WWW-Authenticate — этот заголовок допустим
        только на 403
      - В челлендже обязательно должен присутствовать параметр scope
    correct: 1
    explanation: >-
      В Bearer-челлендже MCP используется параметр resource_metadata. Его
      значением служит URL PRM-документа, с которого клиент начинает
      обнаружение, а не URL защищённого API.
  - question: >-
      Разработчик проверяет aud, iss, exp и скоупы только при открытии
      клиентской сессии. Все последующие вызовы в этой сессии проходят без
      проверки. Какую проблему это создаёт?
    options:
      - 'Проблемы нет: результаты проверки действуют до закрытия сессии'
      - Сервер теряет возможность публиковать метаданные защищённого ресурса
      - >-
        Истёкший токен или запрос без актуально требуемого скоупа может
        продолжить проходить до конца сессии
      - Клиент больше не сможет использовать PKCE при следующей авторизации
    correct: 2
    explanation: >-
      Сессия может пережить срок действия токена, а требования к доступу могут
      измениться. Поэтому exp и остальные условия авторизации проверяются заново
      на каждом запросе.
---

Ты уже собрал весь профиль: [шаг 33](#step-33) — откуда клиент узнаёт адрес PRM, [шаг 34](#step-34) — только заголовок `Authorization: Bearer`, [шаг 35](#step-35) — проверка на каждом запросе, [шаг 36](#step-36) — что именно лежит в челлендже на 401 и 403.

Теперь надо уметь другое: взять чужой ответ сервера и увидеть в нём ошибку. Не «назвать RFC», а посмотреть на строку заголовка и сказать, что клиент по ней не доедет до обнаружения.

Спотыкаются обычно на двух вещах. Первая — путают, куда ведёт `resource_metadata`: подставляют URL самого ресурса вместо адреса документа с метаданными. Вторая — считают проверку токена событием подключения, а не свойством каждого запроса.
