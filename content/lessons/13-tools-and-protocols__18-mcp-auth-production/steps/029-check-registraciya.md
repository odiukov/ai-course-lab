---
id: 029-check-registraciya
type: check
title: 'Проверка: CIMD, DCR или предварительная регистрация'
source_anchor: >-
  ### RFC 7591 — Dynamic Client Registration (fallback / backwards
  compatibility)
check:
  - question: >-
      IdP умеет только POST /register, CIMD не поддерживает. Клиент — Claude
      Desktop на ноутбуке пользователя. Какой запрос на регистрацию корректный?
    options:
      - >-
        token_endpoint_auth_method: none, response_types: код, redirect_uri на
        127.0.0.1 — секрета у клиента нет, владение доказывает PKCE
      - >-
        token_endpoint_auth_method: client_secret_post — сервер выдаст секрет, и
        клиент будет им подписывать обмен кода
      - >-
        token_endpoint_auth_method: none, но response_types: token — раз секрета
        нет, токен проще забрать сразу
    correct: 0
    explanation: >-
      Клиент живёт на чужом устройстве: любой client_secret оттуда вытащат,
      поэтому auth_method — none, а доказательством владения служит PKCE.
      Response_types остаётся только код.
  - question: На /register нет ограничения частоты по исходному IP. Что именно ломается?
    options:
      - >-
        Ничего страшного: лишние client_id просто займут место в базе, их можно
        чистить по расписанию
      - >-
        Скрипт с одной машины нагенерит миллионы записей и исчерпает
        пространство имён client_id — поэтому проверку частоты ставят до
        регистратора, а не после
      - Злоумышленник сможет переписать redirect URI у чужих клиентов
    correct: 1
    explanation: >-
      Тысяча регистраций в секунду с одной машины за сутки даёт под сто
      миллионов мусорных клиентов. Проверка частоты обязана срабатывать раньше,
      чем регистратор возьмётся за запрос. Переписывание чужого redirect URI —
      это про украденный registration_access_token, другая грабля.
---

Тут проверяем, умеешь ли ты выбрать путь регистрации под конкретный IdP и не испортить его деталями. После [шагов 17](#step-17)–[19](#step-19) и [23](#step-23)–[28](#step-28) у тебя три варианта: CIMD (сервер сам забирает client.json по URL), DCR через POST /register (теперь MAY, живёт ради обратной совместимости) и предварительная регистрация руками — та самая переписка с администратором из [шага 2](#step-2).

Спотыкаются обычно на двух местах. Первое: тянутся к client_secret, потому что «клиент же должен себя доказать» — а клиент стоит на ноутбуке пользователя, и секрет оттуда достанут. Второе: ставят рейт-лимит после регистратора, где он уже ничего не спасает.
