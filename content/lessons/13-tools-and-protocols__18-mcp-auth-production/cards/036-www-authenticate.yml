- concept: при недостаточном скоупе Bearer-челлендж сообщает клиенту требуемый scope
  question: Заполните имя параметра, который сообщает клиенту недостающее разрешение.
  explanation: При insufficient_scope токен распознан, но его прав недостаточно. Параметр scope называет требуемое разрешение, а resource_metadata указывает на документ PRM; параметр resource для этого не используется.
  kind: cloze
  template: 'WWW-Authenticate: Bearer error="insufficient_scope", resource_metadata="https://tools.example.org/.well-known/oauth-protected-resource", ___="files.write"'
  answer: scope
  accept:
    - scope
  id: 036-www-authenticate-1
  fingerprint: 49defd1b
