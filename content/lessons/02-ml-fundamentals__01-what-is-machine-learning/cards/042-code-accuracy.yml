- concept: истинная метка и предсказание должны сопоставляться для одного и того же объекта
  question: Заполни пропуск так, чтобы функция считала совпадения попарно, сохраняя порядок объектов.
  explanation: 'zip объединяет элементы с одинаковыми позициями: истинная метка каждого объекта сравнивается с предсказанием именно для этого объекта.'
  kind: cloze
  template: matches = sum(true == pred for true, pred in ___)
  answer: zip(y_true, y_pred)
  accept:
    - zip(y_true,y_pred)
  id: 042-code-accuracy-1
  fingerprint: db851f5b
- concept: несовпадающие длины истинных меток и предсказаний должны считаться ошибкой входных данных
  question: Почему перед вычислением accuracy нужно проверить равенство длин y_true и y_pred?
  explanation: zip обрабатывает пары только до конца более короткой последовательности. Без явной проверки функция может вернуть правдоподобное число, скрыв ошибку в данных.
  kind: choice
  options:
    - zip остановится на более коротком списке и может незаметно оставить часть объектов без оценки
    - zip автоматически отсортирует списки и изменит порядок объектов
    - Деление списков разной длины всегда округляет результат
    - Сравнение элементов разных списков в Python запрещено
  correct: 0
  id: 042-code-accuracy-2
  fingerprint: 79a64879
