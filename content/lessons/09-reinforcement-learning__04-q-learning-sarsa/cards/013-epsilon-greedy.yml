- concept: ε-жадная функция возвращает индекс выбранного действия, а не его Q-значение
  question: Функция выбирает жадное действие из строки `Q[state]`. Объясни, почему `return np.max(Q[state])` неверно и что следует вернуть.
  explanation: Q-значение показывает полезность действия, но не обозначает само действие. `argmax` возвращает индекс, который можно передать среде.
  kind: open
  reference: '`np.max` возвращает оценку действия, а среде нужен идентификатор действия. Следует вернуть индекс максимального элемента: `int(np.argmax(Q[state]))`.'
  id: 013-epsilon-greedy-1
  fingerprint: ffe91339
- concept: случайная ветвь ε-жадной политики выбирает индекс из множества действий текущего состояния
  question: Заполни пропуск так, чтобы верхняя граница случайного индекса определялась количеством доступных действий.
  explanation: Длина строки состояния равна количеству доступных действий. Длина всей таблицы Q обычно соответствует количеству состояний и потому не задаёт правильный диапазон индексов действий.
  kind: cloze
  template: |-
    row = Q[state]
    random_action = rng.integers(len(___))
  answer: row
  accept:
    - Q[state]
  id: 013-epsilon-greedy-2
  fingerprint: 31a3f3a6
