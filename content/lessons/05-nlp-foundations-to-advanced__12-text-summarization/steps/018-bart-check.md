---
id: 018-bart-check
type: check
title: 'Что меняют min_length, max_length и do_sample=False?'
source_anchor: '### Step 2: abstractive with BART'
check:
  - question: >-
      Ты дважды запускаешь суммаризацию одного текста с min_length=60,
      max_length=120 и do_sample=False. Какой результат ожидается?
    options:
      - Две разные выжимки длиной ровно 120 слов
      - Одинаковая выжимка длиной от 60 до 120 токенов
      - Одинаковая выжимка длиной от 60 до 120 слов
    correct: 1
    explanation: >-
      Границы задаются в токенах, а do_sample=False отключает случайный выбор,
      поэтому результат повторяется.
  - question: 'Что вероятнее произойдёт, если убрать min_length, оставив max_length=120?'
    options:
      - Модель обязательно использует все 120 токенов
      - Выжимка может оказаться слишком короткой
      - Выжимка станет случайной
    correct: 1
    explanation: >-
      max_length только ограничивает длину сверху. Без нижней границы модель
      может закончить генерацию гораздо раньше.
---

Здесь легко перепутать ограничение с приказом: `max_length=120` не заставляет модель написать 120 токенов, а лишь запрещает выйти за этот предел. После [шага 17](#step-17) тебе важно различать токены и слова, понимать роль нижней границы и видеть, что `do_sample=False` управляет не длиной, а повторяемостью результата.

> 🎒 **На пальцах.** Представь выступление с регламентом от двух до четырёх минут. Четыре минуты — потолок, но без нижней границы докладчик может закончить через двадцать секунд. А запрет импровизации означает, что при повторном выступлении он произнесёт тот же подготовленный текст.
