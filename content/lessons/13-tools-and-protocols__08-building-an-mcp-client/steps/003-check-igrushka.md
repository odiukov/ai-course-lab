---
id: 003-check-igrushka
type: check
title: 'Проверка: где игрушка зависнет'
source_anchor: '## The Problem'
check:
  - question: >-
      Клиент отправил запрос серверу A и ждёт ответ. В этот момент сервер B
      присылает уведомление, что список его инструментов изменился. Что отличает
      рабочий клиент от игрушки?
    options:
      - >-
        Рабочий клиент кладёт сообщение от B в очередь и продолжает ждать A, а
        игрушка застревает, потому что читает только одну линию
      - >-
        Рабочий клиент разрывает соединение с A, обрабатывает B и потом
        переподключается к A
      - >-
        Рабочий клиент игнорирует уведомления, пока не закроет все запросы, —
        уведомления не обязательны
    correct: 0
    explanation: >-
      Игрушка сидит в ожидании ровно одного ответа и не слышит остальных.
      Рабочий клиент принимает входящее от любого сервера в очередь, не блокируя
      текущий запрос.
  - question: >-
      К хосту подключены три сервера: у первого 3 инструмента, у второго 2, у
      третьего 2. Что видит модель и что должен помнить клиент?
    options:
      - Модель видит три отдельных списка и сама выбирает сервер по имени
      - >-
        Модель видит один список из 7 имён, а клиент держит соответствие
        имя-сервер, чтобы отправить вызов туда, куда надо
      - >-
        Модель видит один список из 7 имён, и клиенту достаточно разослать вызов
        всем трём серверам
    correct: 1
    explanation: >-
      Списки сплющиваются в одно пространство имён, поэтому знание о том, чей
      это инструмент, остаётся только у клиента — и потерять его нельзя.
---

Останови себя на секунду, прежде чем идти дальше к коду. К этому месту у тебя должно быть готово два умения.

Первое: по описанию ситуации сказать, какая из шести обязанностей клиента из [шага 2](#step-2) сейчас работает — запуск, хендшейк, сбор списков, маршрутизация вызова, приём уведомлений, переподключение.

Второе: отличать «ждать» от «блокировать». Тут спотыкаются чаще всего: кажется, что раз клиент ждёт ответа от одного сервера, то он вообще занят. Нет — он ждёт одну конкретную линию и при этом продолжает слышать остальные.

И вторая частая осечка: три сервера сплющиваются в один список имён, но соответствие «имя — чей сервер» никуда не исчезает. Оно просто переезжает целиком на плечи клиента.
