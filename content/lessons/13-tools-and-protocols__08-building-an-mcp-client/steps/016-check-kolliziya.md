---
id: 016-check-kolliziya
type: check
title: 'Проверка: два search, чей вызов уйдёт на диск'
source_anchor: '### Merged namespace'
check:
  - question: >-
      Ты подключил два сервера: сначала notes (его search читает заметки), потом
      files (его search лезет в диск). Хост выбрал silent first-come — побеждает
      тот, кто пришёл позже. Модель просит инструмент search. Куда уйдёт вызов?
    options:
      - 'В notes: он зарегистрировался первым, а первая запись закрепляется'
      - 'В files: его search перекрыл прежний, и вызов пойдёт на диск'
      - 'В оба сразу, а хост сравнит два ответа'
      - 'Никуда: хост увидит два одинаковых имени и вернёт ошибку'
    correct: 1
    explanation: >-
      При first-come с перезаписью последний загруженный сервер занимает имя.
      Модель просила search, думая про заметки, а вызов ушёл читать диск — и
      никто об этом не предупредил.
  - question: >-
      Почему Cursor с отказом по коллизии считается безопаснее хоста с
      молчаливой перезаписью?
    options:
      - 'Отказ работает быстрее: не надо строить сводный список'
      - >-
        Отказ ломает конфликт сразу и на старте, а перезапись прячет его до
        момента вызова
      - 'Отказ гарантирует, что имена инструментов будут короче'
      - При отказе модель сама выбирает нужный сервер во время вызова
    correct: 1
    explanation: >-
      Проблема перезаписи не в том, что она не работает, а в том, что она
      молчит: ошибка всплывёт когда-нибудь позже, на живом вызове. Отказ
      переносит её на старт, где её видно и можно исправить.
---

Теперь проверь себя на самом неприятном случае — том, где ничего не падает и никто не жалуется.

После [шага 14](#step-14) и [шага 15](#step-15) ты должен уметь пройти по конкретной цепочке: два сервера отдали инструмент с одинаковым именем, хост выбрал одну из трёх стратегий — и предсказать, что увидит модель и куда физически уйдёт вызов. Не «как называется стратегия», а именно куда.

Обычно спотыкаются на слове «first-come». Оно звучит как «первый занял имя и держит его», но в разборе речь была о другом: имя достаётся тому, кто загрузился позже, а прежний тихо исчезает из сводного списка. Второе место, где легко ошибиться, — считать отказ по коллизии придиркой. Он не про красоту имён, а про то, чтобы конфликт стал заметен на запуске, а не на вызове.
