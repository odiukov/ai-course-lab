- concept: строгое сравнение с порогом отклоняет предложение при точном равенстве u и вероятности принятия
  question: Verifier назначил черновиковому токену вероятность 0.42, draft-модель — 0.84, а u равно 0.5. Что вернёт выражение `u < min(1.0, q_prob / p_prob)`?
  explanation: 'Порог равен 0.42 / 0.84 = 0.5. Используется строгое сравнение: при равенстве `u` порогу условие ложно, поэтому результат — `False`.'
  kind: choice
  options:
    - '`True`'
    - '`False`'
    - Возникнет деление на ноль
  correct: 1
  id: 015-accept-draft-1
  fingerprint: 475108d9
- concept: при уже заданных q_prob и p_prob решение не зависит от идентификатора черновикового токена
  question: Для двух разных `draft_token` в `accept_or_reject` передали одинаковые `q_prob`, `p_prob` и `u`. Объясни, почему функция вернёт для них одинаковый результат.
  explanation: Решение вычисляется только из `q_prob`, `p_prob` и `u`. Параметр `draft_token` может быть полезен вызывающему коду или логированию, но сам по себе на булев результат не влияет.
  kind: open
  reference: Идентификатор токена не используется в выражении принятия. Вся необходимая информация об оценках этого токена уже содержится в `q_prob` и `p_prob`, а случайный исход задаётся `u`, поэтому при одинаковых значениях этих аргументов результат совпадёт.
  id: 015-accept-draft-2
  fingerprint: 04d5802e
