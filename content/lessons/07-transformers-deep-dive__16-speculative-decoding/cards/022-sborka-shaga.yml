- concept: распределение verifier для черновика должно быть обусловлено контекстом до этого черновика
  question: В speculative decoding для черновика `drafts[i]` нужно получить его вероятность по verifier. Какое выражение использует правильный условный контекст?
  explanation: '`q_dists[i]` обусловлено исходным префиксом и всеми черновиками перед `drafts[i]`. Сам проверяемый токен в контекст не входит: verifier должен оценить вероятность его появления следующим.'
  kind: choice
  options:
    - '`q_model(prefix + drafts[:i])[drafts[i]]`'
    - '`q_model(prefix + drafts[:i + 1])[drafts[i]]`'
    - '`q_model(prefix)[drafts[i]]`'
  correct: 0
  id: 022-sborka-shaga-1
  fingerprint: 421eeff5
- concept: вероятность выбранного черновика сохраняется в момент сэмплирования под его исходным условным контекстом
  question: Draft-модель вычислила распределение `p_dist` и выбрала из него токен `d`. Заполните строку, сохраняющую данные для последующей проверки токена без повторного вызова draft-модели.
  explanation: Проверке понадобится вероятность, которую draft-модель присвоила выбранному токену именно в момент его генерации. Сохранение `p_dist[d]` позволяет затем вычислить отношение вероятностей verifier и draft без повторного прохода draft-модели.
  kind: cloze
  template: p_probs.append(___)
  answer: p_dist[d]
  accept:
    - p_dist[d]
    - p_dist.get(d)
  id: 022-sborka-shaga-2
  fingerprint: d0f19c3b
