---
id: 036-check-chi2
type: check
title: 'Проверка: почему 50 000 токенов, а не 1000'
source_anchor: '### Step 5: verify distribution equivalence'
check:
  - question: >-
      Ты сравнил 1000 токенов спекулятивного цикла с прямым сэмплированием из
      verifier, и критерий хи-квадрат не отверг совпадение распределений. Какой
      вывод обоснован?
    options:
      - Корректность реализации полностью доказана
      - >-
        На этой выборке статистически значимого расхождения не обнаружено, но
        существующий перекос мог остаться незамеченным
      - Спекулятивный цикл обязательно выдаёт токены быстрее verifier
      - Draft-модель и verifier имеют одинаковые распределения
    correct: 1
    explanation: >-
      Тест сообщает, что данных недостаточно для отклонения гипотезы о
      совпадении, а не доказывает её. На 1000 токенов редкие исходы наблюдаются
      слишком мало раз, поэтому в задании используется выборка из 50 000
      токенов.
  - question: >-
      Какую ошибку реализации должен выявлять тест, сравнивающий гистограмму
      спекулятивного цикла с прямым сэмплированием из verifier?
    options:
      - Слишком высокую стоимость одного прохода verifier
      - Слишком низкую долю принятых черновых токенов
      - >-
        Искажение итогового распределения из-за неверного правила принятия или
        остаточной коррекции
      - Неоптимальную длину чернового блока
    correct: 2
    explanation: >-
      Этот тест проверяет статистическую честность алгоритма: после всех
      принятий и коррекций выход должен иметь то же распределение, что и прямое
      сэмплирование из verifier. Скорость и доля принятия проверяются отдельно.
---

Ты дописал цикл, запустил, гистограммы легли почти одна в одну — и хочется поставить галочку. Не спеши: важно, на скольких токенах ты это увидел.

После [шага 35](#step-35) ты должен уметь отвечать на два вопроса: что проверяет этот тест и почему выборка такая большая. Проверяет он не скорость, а то, что выход остался распределением verifier — то есть что правило принятия из [шага 12](#step-12) и остаток из [шага 17](#step-17) собраны верно.

Спотыкаются обычно здесь: «тест не нашёл расхождения» читают как «расхождения нет». На 1000 токенов редкий токен выпадет пару раз, и перекос в нём не отличить от случайности.
