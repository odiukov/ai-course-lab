---
id: 003-contract-check
type: check
title: Проверяем формы и направление softmax
source_anchor: '### Causal mask or not?'
check:
  - question: >-
      Почему softmax в self-attention применяют по оси ключей, отдельно для
      каждого токена-запроса?
    options:
      - Чтобы запрос распределял свой вес внимания между доступными ключами
      - Чтобы определить относительную важность разных голов
      - Чтобы нормализовать каждую координату векторов значений
    correct: 0
    explanation: >-
      Строка оценок соответствует одному запросу, а её элементы — разным ключам.
      Softmax превращает эту строку в распределение весов, с которыми запрос
      собирает информацию от ключей.
  - question: >-
      Как интерпретировать результат умножения весов внимания на векторы
      значений?
    options:
      - Как один скаляр сходства для каждой головы
      - Как взвешенную сумму векторов значений для каждого запроса
      - Как новую матрицу попарных оценок между запросами
    correct: 1
    explanation: >-
      Для каждого запроса веса внимания определяют вклад каждого вектора
      значения. Их взвешенная сумма образует выходной вектор запроса в данной
      голове.
---

На [шаге 2](#step-2) ты зафиксировал размеры внимания. Теперь проверь направление: строка матрицы принадлежит одному токену-запросу, а её 197 столбцов — токенам, на которые он смотрит. Значит, softmax идёт по столбцам каждой строки. Частая ошибка — нормировать столбец или головы: формы при этом могут выглядеть допустимо, но запрос уже не распределяет единицу внимания между ключами.
