- concept: 'session affinity как альтернатива внешнему хранилищу: она сохраняет память в процессе ценой потери взаимозаменяемости реплик'
  question: Чат-сервис держит собранного агента пользователя в словаре внутри процесса. Чтобы второе сообщение попадало туда же, где лежит первое, на балансировщик включают привязку сессии к реплике (по cookie). Какова главная плата за такое решение?
  explanation: 'Привязка не убирает состояние из памяти процесса — она лишь маршрутизирует вокруг него. Пользователь оказывается прибит к конкретной копии: рестарт при деплое, OOM или скейл-ин теряют его сессию, а новые реплики получают только новых пользователей, поэтому нагрузка размазывается неровно. Продакшен-путь Agno обратный: состояние сессии в базе, агент собирается заново на каждый запрос (около 2μs), и любая копия может обслужить любой запрос.'
  kind: choice
  options:
    - 'Реплики перестают быть взаимозаменяемыми: падение или перезапуск одной уносит все её сессии, а добавление новых реплик не перераспределяет уже привязанный трафик'
    - Каждый запрос начинает требовать нового соединения с базой, из-за чего растёт задержка ответа
    - Модель теряет доступ к истории диалога, потому что привязка стирает контекст между сообщениями
    - Сборка агента становится дороже, так как привязанная реплика вынуждена пересобирать его на каждый запрос
  correct: 0
  id: 011-besstatusnyy-backend-1
  fingerprint: 8c5947bd
- concept: 'бесстатусность ограничивает состояние пользователя, а не любые объекты в памяти процесса: разделяемые безличные ресурсы кэшировать можно'
  question: 'Бэкенд объявлен бесстатусным: состояние сессий хранится в базе. Что из перечисленного допустимо держать в памяти процесса между запросами, не нарушая это свойство?'
  explanation: 'Бесстатусность — про состояние, потеря которого что-то значит для пользователя. Пулы, клиенты и конфиг безличны, идентичны на всех копиях и заново поднимаются при рестарте, поэтому кэшировать их нормально и нужно. А дубль истории или список «своих» пользователей делают реплики неодинаковыми: запрос, попавший на другую копию, увидит другую картину.'
  kind: choice
  options:
    - Пул соединений к базе, HTTP-клиент провайдера модели и разобранный конфиг — они одинаковы для всех пользователей и восстанавливаются при старте
    - Историю сообщений последних активных пользователей — она всё равно продублирована в базе
    - Идентификаторы пользователей, закреплённых за этой репликой, чтобы не ходить в базу лишний раз
    - 'Ничего: бесстатусность требует, чтобы процесс между запросами не удерживал в памяти никаких объектов'
  correct: 0
  id: 011-besstatusnyy-backend-2
  fingerprint: d4048849
- concept: 'порядок обработки запроса в бесстатусном бэкенде: состояние читается из хранилища до сборки агента и записывается обратно до отдачи ответа'
  question: Расставьте по порядку шаги обработки одного запроса в бесстатусном FastAPI-бэкенде на Agno.
  explanation: 'Ключевые точки — вторая и пятая: состояние приходит из внешнего хранилища до сборки агента и уходит туда обратно до конца запроса. Всё, что между ними, живёт только на время запроса, поэтому объект агента можно выбросить. При стоимости сборки около 2μs против секундного вызова модели это ничего не стоит, зато любая реплика способна выполнить эту же последовательность для того же пользователя.'
  kind: order
  items:
    - Принять HTTP-запрос с идентификатором сессии и сообщением пользователя
    - Прочитать состояние сессии из базы по этому идентификатору
    - Собрать свежий экземпляр агента, передав ему прочитанное состояние
    - 'Выполнить цикл агента: вызовы модели и инструментов'
    - Записать обновлённое состояние сессии обратно в базу
    - Вернуть ответ и уничтожить объект агента вместе с процессной памятью запроса
  id: 011-besstatusnyy-backend-3
  fingerprint: 1bbde770
