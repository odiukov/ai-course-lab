- concept: 'агрегат «X из Y» непригоден для анализа горизонта: нужен исход каждой задачи вместе с её экспертным временем'
  question: 'Итог прогона записан одной строкой: «модель решила 48 задач из 90». Что теряется в такой записи для последующей оценки временного горизонта?'
  explanation: Подгонка кривой берёт пары «log-время эксперта — успех/провал». Агрегированное число сохраняет долю успехов, но стирает, какие именно задачи по длительности провалены, и восстановить пары из него невозможно.
  kind: choice
  options:
    - 'Ничего: доли успехов достаточно, чтобы подогнать кривую вероятности'
    - Связь каждого исхода с длительностью его задачи — без неё нельзя построить зависимость вероятности успеха от времени эксперта
    - Только удобство чтения отчёта, расчёт остаётся полностью возможным
  correct: 1
  id: 013-run-manifest-1
  fingerprint: '64852273'
- concept: множество результатов в манифесте обязано совпадать с зафиксированной выборкой — без пропусков и без посторонних задач
  question: В манифесте нашлась запись результата для задачи, которой нет в зафиксированной выборке прогона. Как это трактовать?
  explanation: 'Манифест — протокол конкретного испытания. Если множество результатов шире выборки, отчёт говорит об одном наборе задач, а числа получены на другом. Симметрично проверяется и обратное: результат обязан существовать для каждой выбранной задачи.'
  kind: choice
  options:
    - 'Это допустимо: лишний результат только увеличивает объём данных'
    - Манифест перестаёт описывать заявленную выборку — состав измерения изменился незаметно, запись нужно отвергнуть
    - Это ошибка только если задача повторяет уже имеющийся идентификатор
  correct: 1
  id: 013-run-manifest-2
  fingerprint: 03791cbe
- concept: 'порядок сборки манифеста: общие условия один раз, затем запись исхода на каждую задачу, затем сверка результатов с выборкой'
  question: Расставь шаги сборки манифеста прогона в правильном порядке.
  explanation: Условия общие для всего прогона, поэтому хранятся один раз — иначе их копии начнут расходиться. Уникальность идентификаторов проверяется до записи исходов, чтобы результат нельзя было приписать двум задачам сразу. Сверка с выборкой идёт последней, когда есть что сверять.
  kind: order
  items:
    - 'Зафиксировать общие условия прогона один раз: модель и её версия, конфигурация доступа, разрешённые инструменты, лимиты запуска'
    - Проверить, что идентификаторы задач в выборке уникальны
    - 'Записать отдельный результат по каждой задаче: идентификатор, набор, категорию, время эксперта и однозначный исход'
    - 'Сверить множество результатов с выборкой: нет пропущенных и нет посторонних задач'
  id: 013-run-manifest-3
  fingerprint: 291a7107
