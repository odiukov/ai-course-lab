- concept: подгоняемая величина — вероятность успеха модели на задаче заданной экспертной длительности, а не время работы самой модели
  question: Логистическая регрессия обучена на парах «log-время выполнения задачи экспертом — исход модели». На какой вопрос она отвечает?
  explanation: Признак — длительность задачи для эксперта, целевая переменная — бинарный исход модели. Выход подгонки — вероятность класса «успех», то есть доля успехов среди задач сопоставимой длительности, а не прогноз времени работы модели.
  kind: choice
  options:
    - Сколько минут потратит модель на конкретную задачу
    - Какова вероятность успеха модели на задаче такой экспертной длительности
    - Какой набор задач стоит выдать модели следующим
  correct: 1
  id: 015-logistic-curve-1
  fingerprint: 0602f4a7
- concept: признаком регрессии служит логарифм экспертного времени, а не сами минуты
  question: 'Заполни пропуск: какое преобразование применяют к экспертному времени перед подгонкой кривой успеха?'
  explanation: 'В регрессию подаётся логарифм длительности: тогда умножение времени в одинаковое число раз даёт одинаковый сдвиг признака. Основание логарифма меняет лишь масштаб коэффициента, но не форму подгонки; целевая переменная остаётся бинарной — успех или провал.'
  kind: cloze
  template: |-
    features = ___(expert_minutes).reshape(len(expert_minutes), 1)
    model = LogisticRegression().fit(features, outcomes)
  answer: np.log
  accept:
    - numpy.log
    - np.log10
    - math.log
    - np.log2
  id: 015-logistic-curve-2
  fingerprint: 17f6cc01
