- concept: метод DataFrame.corr вычисляет матрицу попарных корреляций числовых столбцов
  question: Дополните функцию, возвращающую корреляционную матрицу признаков из pandas DataFrame.
  explanation: Вызов X.corr() вычисляет коэффициент корреляции для каждой пары числовых столбцов и сохраняет названия признаков в строках и столбцах результата.
  kind: cloze
  template: |-
    def correlation(X):
        return X.___()
  answer: corr
  accept:
    - corr
  id: 014-correlation-1
  fingerprint: 3df206fa
- concept: матрица попарных корреляций симметрична
  question: Почему в корреляционной матрице значение C[A, B] совпадает с C[B, A] и как это можно использовать при самостоятельной реализации функции?
  explanation: Перестановка двух переменных не меняет их ковариацию и произведение стандартных отклонений. Следовательно, коэффициент корреляции и вся матрица симметричны.
  kind: open
  reference: Корреляция не зависит от порядка признаков в паре. Поэтому достаточно вычислить одну треугольную часть матрицы, а затем отразить найденные значения в другую.
  id: 014-correlation-2
  fingerprint: 8e7c1b04
- concept: корреляция постоянного признака неопределена из-за отсутствия изменчивости
  question: Что должна вернуть корректная реализация корреляционной матрицы для пары, в которую входит постоянный признак?
  explanation: У постоянного признака нулевое стандартное отклонение. В формуле корреляции на него пришлось бы делить, поэтому коэффициент не определён и обычно представлен значением NaN.
  kind: choice
  options:
    - Максимальную положительную корреляцию
    - Нейтральную корреляцию
    - Неопределённое значение, например NaN
  correct: 2
  id: 014-correlation-3
  fingerprint: 060b0a55
