- concept: обратный проход создаёт градиенты параметров, а оптимизатор только применяет их
  question: Почему вызов optimizer.step() не может заменить loss.backward()?
  explanation: 'У backward и оптимизатора разные обязанности: первый вычисляет производные, второй использует их для изменения параметров.'
  kind: open
  reference: backward проходит по вычислениям сети и заполняет градиенты параметров. optimizer.step() не вычисляет эти градиенты, а читает уже заполненные буферы и обновляет параметры.
  id: 049-proverka-loop-1
  fingerprint: 2df66b15
- concept: обнуление после шага оптимизатора эквивалентно обнулению перед следующим backward, если между ними градиенты не накапливаются
  question: После optimizer.step() программа выполняет логирование, а затем обрабатывает следующий батч. При каком условии zero_grad можно перенести с момента сразу после step на момент перед следующим backward без изменения обучения?
  explanation: Оба варианта очищают те же градиентные буферы до начала нового накопления. Логирование между ними ничего не меняет, если оно не запускает backward.
  kind: choice
  options:
    - Если между этими точками не вызывается backward
    - Только если learning rate равен нулю
    - Только если модель переведена в режим eval
  correct: 0
  id: 049-proverka-loop-2
  fingerprint: 05acafed
