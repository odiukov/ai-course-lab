---
id: 040-proverka-xor-data
type: check
title: 'Проверка: входы, цели и формы данных XOR'
source_anchor: '### Step 10: Train a 4-Layer Network on Circle Classification'
check:
  - question: >-
      Входные значения XOR различаются. Как одновременно правильно выбрать метку
      и согласовать её с одноэлементным выходом модели?
    options:
      - Выбрать положительную метку и поместить её в одноэлементный список
      - Выбрать отрицательную метку и поместить её в одноэлементный список
      - 'Выбрать положительную метку, но оставить её скаляром'
    correct: 0
    explanation: >-
      Для различающихся входов XOR даёт 1.0. Цель записывается как `[1.0]`,
      поскольку модель возвращает одно предсказание в списке.
  - question: >-
      Модель выдаёт результат `[p]`, а цель одного примера записана как `[u,
      v]`. В чём проблема?
    options:
      - 'Цель содержит больше компонентов, чем выход модели'
      - Вход модели должен быть скаляром
      - Функция потерь требует две независимые модели
    correct: 0
    explanation: >-
      Одноэлементный выход нужно сравнивать с одноэлементной целью. Цель из двух
      элементов не соответствует форме результата модели.
---

Проверь результат функции из [шага 39](#step-39) не только по значениям, но и по форме каждой пары. У XOR четыре примера: каждый вход содержит два числа, а цель — одно число внутри списка. Частая ошибка — записать цель просто числом или случайно дать ей два элемента. Тогда данные перестают совпадать с одним выходом модели.

> 🎒 **На пальцах.** Представь карточки для сортировщика посылок: на лицевой стороне стоят два переключателя, а на обороте — одна отметка «да» или «нет». Если оборот пустой либо на нём две отметки, сортировщик уже не понимает, с каким единственным ответом сравнивать результат.
