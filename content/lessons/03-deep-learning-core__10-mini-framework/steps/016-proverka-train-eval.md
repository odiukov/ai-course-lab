---
id: 016-proverka-train-eval
type: check
title: 'Проверка: что изменяется после model.eval()'
source_anchor: '### Training vs Evaluation Mode'
check:
  - question: >-
      Ты вызвал model.eval() перед проверкой модели. Как теперь работают Dropout
      и BatchNorm?
    options:
      - >-
        Dropout случайно обнуляет значения, а BatchNorm использует статистику
        текущего батча
      - >-
        Dropout пропускает все значения, а BatchNorm использует накопленную
        бегущую статистику
      - 'Dropout обнуляет все значения, а BatchNorm перестаёт выдавать результат'
    correct: 1
    explanation: >-
      В режиме оценки случайность Dropout отключается, а BatchNorm опирается на
      статистику, накопленную во время обучения.
  - question: Что непосредственно меняет вызов model.eval()?
    options:
      - Сбрасывает обученные параметры модели
      - Запрещает любые последующие вычисления градиентов
      - >-
        Переключает флаг training у модели и вложенных модулей, не меняя их
        параметры
    correct: 2
    explanation: >-
      eval() переключает режим поведения модулей. Обученные веса и смещения при
      этом остаются прежними.
---

Здесь важно не перепутать режим с содержимым модели. После `model.eval()` флаг `training` переключается у всей модели, как в [шаге 13](#step-13). Поэтому Dropout меняет поведение из [шага 14](#step-14), а BatchNorm выбирает другой источник статистики из [шага 15](#step-15). Сами обученные параметры от этого вызова не переписываются.

> 🎒 **На пальцах.** Представь автопилот корабля. На испытаниях он иногда намеренно игнорирует отдельные датчики и уточняет, какие волны считать обычными. Перед настоящим рейсом капитан переводит систему в рабочий режим: все датчики снова участвуют, а автопилот использует уже накопленное представление о норме. Корабль тот же — изменились правила его поведения.
