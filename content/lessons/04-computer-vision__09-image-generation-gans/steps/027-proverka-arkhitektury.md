---
id: 027-proverka-arkhitektury
type: check
title: 'Проверка: проведите формы через G и D и расставьте активации'
source_anchor: '### DCGAN architecture rules'
check:
  - question: >-
      Какой маршрут тензора через generator и discriminator согласован по
      формам?
    options:
      - 'G: B × 64 × 1 × 1 → B × 3 × 64 × 64; D: B × 3 × 64 × 64 → B × 1'
      - 'G: B × 3 × 64 × 64 → B × 64 × 1 × 1; D: B × 64 × 1 × 1 → B × 3 × 64 × 64'
      - 'G: B × 64 × 1 × 1 → B × 1; D: B × 1 → B × 3 × 64 × 64'
    correct: 0
    explanation: >-
      Generator превращает латентный тензор каждого элемента батча в
      изображение, а discriminator принимает изображение и возвращает по одному
      логиту на пример. Размер батча B сохраняется.
  - question: Какая расстановка активаций соответствует правилам DCGAN?
    options:
      - >-
        В G скрытые слои используют LeakyReLU, а выход — sigmoid; в D все слои
        используют tanh
      - >-
        В G скрытые слои используют ReLU, а выход — tanh; в D скрытые слои
        используют LeakyReLU, а последний слой выдаёт логит без sigmoid
      - 'В обеих сетях скрытые слои используют tanh, а выходные — ReLU'
    correct: 1
    explanation: >-
      ReLU ставят в скрытых слоях G, tanh — только на выходе G, а LeakyReLU — в
      скрытых слоях D. Последний слой D оставляют линейным, поскольку функция
      потерь принимает логиты.
---

Проведи тензор от шума до изображения, а затем через discriminator. Размер батча \(B\) нигде не исчезает: меняются каналы и пространственный размер. Generator увеличивает высоту и ширину, discriminator делает обратное и оставляет один логит на пример.

Чаще всего здесь путают две вещи: tanh относится только к изображению на выходе G, а LeakyReLU — к скрытым слоям D. После последнего слоя D sigmoid не нужен: как разобрано в [шаге 10](#step-10), потеря работает прямо с логитом. Если формы состыковались, но активация стоит не там, маршрут всё ещё собран неверно.
