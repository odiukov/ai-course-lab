---
id: 003-proverka-zadachi
type: check
title: 'Проверка: почему попиксельная ошибка создаёт размытый результат'
source_anchor: '## The Problem'
check:
  - question: >-
      В обучающих изображениях белый мяч с равной вероятностью находится слева
      или справа. Какой результат выгоден модели, минимизирующей среднюю
      попиксельную ошибку?
    options:
      - Чёткий мяч только слева
      - Чёткий мяч в случайно выбранной стороне
      - 'Два бледных мяча: слева и справа'
      - Чёткий мяч строго по центру
    correct: 2
    explanation: >-
      Два бледных мяча усредняют оба возможных изображения и уменьшают ожидаемую
      ошибку каждого пикселя, хотя такой кадр не встречался в данных.
  - question: >-
      Модель выдаёт размытые изображения при минимизации попиксельной ошибки.
      Как точнее объяснить причину?
    options:
      - У модели обязательно слишком мало пикселей на выходе
      - Функция ошибки поощряет усреднение нескольких правдоподобных вариантов
      - 'Размытие означает, что обучение ещё не начиналось'
      - Любая генеративная модель неизбежно размывает изображения
    correct: 1
    explanation: >-
      Когда допустимо несколько разных результатов, попиксельная ошибка делает
      их среднее выгоднее выбора одного чёткого варианта.
---

Проверь, удерживаешь ли ты связь из [шага 2](#step-2): если возможны несколько правильных изображений, попиксельная ошибка оценивает не правдоподобность целого кадра, а отклонение каждого пикселя. Поэтому безопасное для неё усреднение превращает резкие детали в полупрозрачные пятна. Частая ловушка — принять размытие за нехватку разрешения или слабость модели. Здесь проблема в самой цели обучения: модель успешно делает именно то, за что её награждают.

> 🎒 **На пальцах.** Представь два снимка одной сцены: на одном фонарь горит, на другом выключен. Если наложить снимки с прозрачностью 50%, получится тусклый фонарь. Такого состояния могло не быть ни разу, но по яркости отдельных пикселей оно меньше ошибается сразу относительно обоих снимков. Усреднение выигрывает в числах и проигрывает в реалистичности.
