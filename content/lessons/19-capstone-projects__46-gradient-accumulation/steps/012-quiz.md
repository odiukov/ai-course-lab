---
id: 012-quiz
type: quiz
title: Защищаем контракт накопления градиентов
source_anchor: '## Exercises'
check:
  - question: >-
      Микробатч содержит 16 примеров, а накопление равно 4. Какой обычный батч
      должен дать сопоставимые градиенты?
    options:
      - Батч из 4 примеров
      - Батч из 16 примеров
      - Батч из 64 примеров
      - Батч из 80 примеров
    correct: 2
    explanation: Четыре микробатча по 16 примеров образуют эффективный батч из 64 примеров.
  - question: >-
      При накоплении в 4 шага ты перестал делить лосс микробатча. Что произойдёт
      на первом шаге SGD?
    options:
      - Обновление параметров станет примерно в 4 раза больше эталонного
      - Обновление параметров станет примерно в 4 раза меньше
      - Параметры не изменятся
      - Изменится только скорость вычислений
    correct: 0
    explanation: >-
      Без масштабирования складываются четыре полных градиента, поэтому величина
      обновления возрастает примерно в accum_steps раз.
  - question: Где должны вызываться optimizer.step() и обнуляться градиенты?
    options:
      - После каждого микробатча
      - Один раз на границе эффективного батча
      - Только после завершения всей эпохи
      - Когда лосс перестанет уменьшаться
    correct: 1
    explanation: >-
      Все микробатчи одного эффективного батча должны накопить общий градиент,
      после чего происходит ровно одно обновление и одно обнуление.
  - question: >-
      Разбиения 2 по 8 и 4 по 4 дают небольшую разницу в градиентах. Как это
      разумнее всего проверить?
    options:
      - Потребовать полного побитового совпадения
      - Сравнить с допуском и учесть разный порядок сложения чисел
      - Сравнивать только время выполнения
      - Увеличивать learning rate до совпадения
    correct: 1
    explanation: >-
      Математический результат совпадает, но числа с плавающей точкой
      складываются в разном порядке, поэтому нужен небольшой допуск.
---

Дальше тебе пригодится весь контракт сразу: масштабирование из [шага 3](#step-3), сравнение до обновления из [шага 4](#step-4) и граница эффективного шага из [шага 6](#step-6). Чаще всего ошибка выглядит безобидно: итоговый лосс снижается, хотя параметры обновляются слишком сильно или оптимизатор продвигается после каждого микробатча. Поэтому контракт защищают числами, а результаты измерений сохраняют, как в [шаге 11](#step-11).

> 🎒 **На пальцах.** Представь секретаря, который собирает четыре листа голосования в один протокол. Каждый лист даёт только четверть итогового веса, а подпись ставится один раз — после четвёртого листа. Если считать каждый лист целым протоколом или подписывать его отдельно, бумаги останутся похожими, но решение уже будет другим.
