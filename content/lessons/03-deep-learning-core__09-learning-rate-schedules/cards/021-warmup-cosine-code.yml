- concept: точка окончания warmup относится к cosine-фазе составного расписания
  question: Заполни условие выбора линейной фазы в реализации warmup с последующим cosine decay.
  explanation: 'Линейная ветвь действует только до границы warmup. При t = warmup_steps управление переходит в cosine-ветвь: её progress в этой точке равен нулю, поэтому learning rate начинается с lr_max.'
  kind: cloze
  template: |-
    if t ___ warmup_steps:
        return lr_max * (t / warmup_steps)
  answer: <
  accept:
    - <
  id: 021-warmup-cosine-code-1
  fingerprint: 08b75771
- concept: непустая cosine-фаза требует, чтобы общее число шагов превышало длительность warmup
  question: Какое соотношение параметров необходимо, если после warmup должна существовать cosine-фаза, а её progress вычисляется без отдельной обработки граничных случаев?
  explanation: 'Знаменатель cosine-фазы равен total_steps - warmup_steps. Он должен быть положительным: при равенстве возникнет деление на ноль, а при меньшем total_steps фаза затухания не имеет корректной положительной длительности.'
  kind: choice
  options:
    - total_steps > warmup_steps
    - total_steps = warmup_steps
    - total_steps < warmup_steps
  correct: 0
  id: 021-warmup-cosine-code-2
  fingerprint: bcd88493
