---
id: 038-quiz
type: quiz
title: Итоговая проверка урока
source_anchor: '## Key Terms'
check:
  - question: >-
      Клиенту нужно сначала узнать, завершилась ли асинхронная задача, а затем
      получить её полезную нагрузку. Какие методы предназначены для этих
      действий?
    options:
      - 'Сначала tasks/result, затем tasks/status'
      - 'Сначала tasks/status, затем tasks/result'
      - 'Сначала tasks/list, затем tasks/cancel'
      - Оба действия выполняются повторным вызовом исходного инструмента
    correct: 1
    explanation: >-
      tasks/status сообщает текущее состояние и может вернуть подсказку о
      прогрессе. Готовая полезная нагрузка запрашивается отдельно через
      tasks/result.
  - question: >-
      Из-за сетевого сбоя клиент не получил ответ на tasks/cancel и повторил тот
      же запрос. Какое свойство метода делает такой повтор безопасным?
    options:
      - Идемпотентность
      - Долговечность
      - Синхронность
      - Обязательный опрос
    correct: 0
    explanation: >-
      Идемпотентная отмена допускает повтор одного и того же запроса: он не
      создаёт дополнительного эффекта и не повреждает состояние задачи.
  - question: >-
      Какое значение taskSupport следует указать для инструмента, который
      разрешено вызывать только в режиме асинхронной задачи?
    options:
      - forbidden
      - optional
      - required
      - durable
    correct: 2
    explanation: >-
      required означает, что инструмент должен вызываться как задача. optional
      допускает оба режима, а forbidden запрещает задачный режим.
  - question: Какое хранилище соответствует понятию durable store для MCP-задач?
    options:
      - 'Объект в памяти процесса, исчезающий после перезапуска'
      - Локальная переменная обработчика запроса
      - 'SQLite, Redis или файловое хранилище, сохраняющее записи между запусками'
      - Кэш ответа внутри одного сетевого соединения
    correct: 2
    explanation: >-
      Durable store сохраняет состояние независимо от времени жизни серверного
      процесса. Поэтому для него подходят постоянные файловые или внешние
      хранилища, а не оперативная память обработчика.
---

Этот урок дал тебе модель, которая дальше в курсе всплывёт не один раз: долгая работа отвязывается от одного соединения и живёт как объект с идентификатором, состоянием и сроком хранения. Ты уже писал руками и `new_task`, и `advance`, и `recover_after_crash` — это тот же скелет, на который потом сядут очереди, воркеры и фоновые пайплайны.

Спотыкаются обычно в трёх местах. Путают `tasks/status` и `tasks/result`: первый отвечает всегда, второй — только когда готово. Забывают, что машина состояний ходит только вперёд, и пробуют вернуть завершённую задачу в работу. И держат состояние в памяти процесса, пока первый же перезапуск не съедает все задачи целиком.
