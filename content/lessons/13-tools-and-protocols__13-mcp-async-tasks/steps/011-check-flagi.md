---
id: 011-check-flagi
type: check
title: 'Проверка: какой флаг у этого инструмента'
source_anchor: '### Per-tool opt-in'
check:
  - question: >-
      У инструмента convert_video время ответа гуляет: маленький файл — 300 мс,
      часовая запись — четыре минуты. Какой флаг ему честнее всего поставить?
    options:
      - forbidden — ведь иногда он отвечает за 300 мс
      - 'optional — клиент сам решит, просить номерок или подождать'
      - 'required — раз бывают четыре минуты, синхронно нельзя никогда'
      - 'флаг вообще не нужен, клиент разберётся по времени ответа'
    correct: 1
    explanation: >-
      optional подходит инструменту с заметным разбросом времени работы: клиент
      может выполнить короткий вызов синхронно или запросить задачу для долгой
      операции.
  - question: >-
      На инструменте стоит taskSupport: "required". Клиент всё равно зовёт его
      обычным запросом, без _meta.task. Что это означает?
    options:
      - >-
        Клиент нарушил контракт: при required он обязан расширять запрос до
        задачи
      - 'Всё нормально: required — это лишь пожелание сервера'
      - Сервер обязан молча превратить обычный вызов в задачу
      - 'Инструмент вернёт результат, но запретит узнавать его состояние'
    correct: 0
    explanation: >-
      required — обязательное условие вызова, а не рекомендация. Клиент должен
      запросить выполнение как задачу; обычный синхронный вызов недопустим.
---

После [шага 9](#step-9) и функции из [шага 10](#step-10) ты должен уметь одно конкретное действие: взять незнакомый инструмент, посмотреть на его время работы и назвать флаг — `forbidden`, `optional` или `required`.

Спотыкаются обычно на двух вещах. Первая: слово `required` читают как «сервер обязан сделать задачу», хотя это требование к **клиенту** — тот не имеет права звать инструмент по-быстрому. Вторая: инструменту с разбросом времени ставят `forbidden` или `required`, ориентируясь на один крайний случай. Разброс — это ровно повод для `optional`.

И держи в голове: `forbidden` не значит «плохой инструмент». Это значит «отвечу быстрее, чем ты успеешь спрятать номерок в карман».
