---
id: 026-check-cikl-oprosa
type: check
title: 'Проверка: вокруг какого метода строится цикл клиента'
source_anchor: '### Methods'
check:
  - question: >-
      Клиент отправил долгую задачу и получил taskId. Как ему правильно
      дождаться исхода?
    options:
      - 'Вызывать tasks/result по кругу, пока вместо отказа не придёт payload'
      - >-
        Опросить tasks/status до терминального состояния, вызвать tasks/result
        только при completed, а failed и cancelled обработать без ожидания
        результата
      - >-
        Опросить tasks/list и считать исчезновение taskId признаком успешного
        завершения
      - >-
        Ничего не вызывать: сервер обязан самостоятельно прислать готовый
        результат
    correct: 1
    explanation: >-
      tasks/status сообщает состояние на всём протяжении работы. После completed
      клиент забирает результат, а failed и cancelled обрабатывает как
      терминальные исходы без готового payload.
  - question: Почему цикл ожидания не следует строить вокруг tasks/result?
    options:
      - tasks/result не принимает taskId
      - tasks/result удаляет задачу при каждом вызове
      - >-
        До завершения задачи tasks/result не даёт содержательного статуса, а
        блокируется либо отвечает отказом
      - tasks/result разрешён только для синхронных инструментов
    correct: 2
    explanation: >-
      Для промежуточного ожидания предназначен tasks/status: он сообщает текущее
      состояние и прогресс. tasks/result нужен для получения уже готового
      результата.
---

Здесь стоит остановиться и проверить одну вещь: ты должен уметь сказать, какой из четырёх методов из [шага 19](#step-19) клиент дёргает многократно, а какой — ровно один раз, и объяснить, почему их нельзя поменять местами. Спотыкаются обычно на том, что `result` кажется главным — ведь нужен-то именно он. Но `status` из [шага 20](#step-20) отвечает всегда, а `result` до готовности отдаёт 404 ([шаг 24](#step-24)). Цикл, построенный на ошибках, теряет способность отличить «работаю» от «сломалось».

> 🎒 **На пальцах.** Пирог в духовке. Через стекло ты смотришь сколько угодно: бледный, подрумянился, готов. Дверцу открываешь один раз — когда уже готов. Если проверять готовность только открыванием дверцы, каждый раз получаешь «нет» и выпускаешь жар. Стекло — это `status`, дверца — `result`.
