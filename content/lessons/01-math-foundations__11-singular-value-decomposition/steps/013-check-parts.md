---
id: 013-check-parts
type: check
title: 'Проверка: где V, где U, если A имеет форму 5x3'
source_anchor: '### The full decomposition'
check:
  - question: >-
      Почему в полном SVD размер V определяется числом столбцов A, а размер U —
      числом её строк?
    options:
      - 'V действует в выходном пространстве, а U — во входном'
      - 'V задаёт базис входного пространства, а U — базис выходного пространства'
      - Размеры U и V всегда определяются только рангом A
      - 'Их размеры выбираются произвольно, если Sigma прямоугольная'
    correct: 1
    explanation: >-
      Матрица A принимает векторы с числом координат, равным числу её столбцов,
      и выдаёт векторы с числом координат, равным числу строк. Поэтому для A
      формы 5 на 3 матрица V имеет форму 3 на 3, а U — 5 на 5.
  - question: >-
      Как правильно определить число правых сингулярных векторов и верхнюю
      границу числа ненулевых сингулярных чисел у прямоугольной матрицы?
    options:
      - Обе величины всегда равны числу строк
      - >-
        Число правых сингулярных векторов равно числу столбцов, а ненулевых
        сингулярных чисел не больше меньшей стороны матрицы
      - >-
        Число правых сингулярных векторов равно числу строк, а ограничений на
        ненулевые сингулярные числа нет
      - Обе величины определяются большей стороной матрицы
    correct: 1
    explanation: >-
      Правые сингулярные векторы являются столбцами V, размер которого задаётся
      числом столбцов A. Число ненулевых сингулярных чисел равно рангу и не
      превосходит меньшую размерность, поэтому для матрицы 5 на 3 правых
      векторов три и ненулевых сингулярных чисел также не больше трёх.
---

Формулу с формами множителей ты уже видел на шаге 6. Здесь важно другое: уметь мгновенно, без подсказок, расставить размеры для конкретной матрицы — иначе на первом же вычислении множители не состыкуются, и непонятно будет, где ошибка.

Держись за одну опору: V стоит со стороны входа, поэтому его размер берётся от числа столбцов A, а U — со стороны выхода, поэтому от числа строк. Sigma — переходник между ними и повторяет форму самой A.

Чаще всего спотыкаются на двух вещах. Первая — путают U и V местами: буква U идёт раньше в алфавите, но в работе матрицы V срабатывает первым. Вторая — думают, что Sigma обязана быть квадратной. Нет: у неквадратной A она вытянутая, и ненулевых сингулярных чисел столько, сколько меньшая из двух сторон, а часто и меньше.

> 🎒 **На пальцах.** Переходник для розетки: с одной стороны вилка под европейскую розетку, с другой — гнездо под британскую. Стороны разные, и перепутать их нельзя — воткнётся только правильным концом. Так же и здесь: одна сторона разложения подогнана под вход матрицы, другая — под выход, а посередине сама переходная деталь, у которой края разной формы.
