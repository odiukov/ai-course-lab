---
id: 010-release-lure
type: check
title: 'Куда попадут app.py, test_app.py и scripts/release.sh?'
source_anchor: '### The sample app'
check:
  - question: >-
      Diff затронул модуль приложения, его тест и релизный скрипт. Контракт
      разрешает область приложения и область тестов. Каким будет результат
      классификации?
    options:
      - 'Все три пути разрешены: они лежат в одном репозитории'
      - >-
        Модуль приложения и тест — в разрешённых, релизный скрипт — за пределами
        области задачи
      - 'Разрешён только модуль приложения: тесты и скрипты трогать нельзя'
      - >-
        Все три пути — за пределами области: в diff есть нарушение, значит
        скомпрометирован весь набор
    correct: 1
    explanation: >-
      Категорию получает каждый путь отдельно. Общий репозиторий не даёт прав на
      релизный скрипт, а одно нарушение не окрашивает законные правки в
      запрещённые — иначе отчёт перестал бы показывать, где именно проходит
      граница.
---

Проверь, что ты связываешь функцию из [шага 9](#step-9) с адаптированным контрактом из [шага 6](#step-6): `app.py` и `test_app.py` относятся к разрешённой области, а `scripts/release.sh` — к запретной. Частая ошибка — считать безопасным любой файл внутри `sample_app/` или, наоборот, объявлять весь diff запрещённым из-за одного нарушения. Здесь каждый путь получает собственную категорию.
