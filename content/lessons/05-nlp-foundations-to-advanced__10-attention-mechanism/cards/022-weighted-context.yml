- concept: веса attention определяются query и keys, а context собирается из values
  question: В механизме attention query и keys оставили прежними, а values заменили другими векторами той же формы. Что обязательно верно?
  explanation: Scores вычисляются из query и keys, поэтому при их сохранении softmax даст прежние веса. Context — взвешенная сумма values, поэтому замена values может изменить результат.
  kind: choice
  options:
    - Веса останутся прежними, а context может измениться
    - Веса обязательно изменятся, а context останется прежним
    - Scores будут пересчитаны из новых values
    - И веса, и context обязательно останутся прежними
  correct: 0
  id: 022-weighted-context-1
  fingerprint: f22854fe
- concept: при фиксированных весах изменение одного value сдвигает context на это изменение, умноженное на вес позиции
  question: Scores и веса attention зафиксированы. Value на позиции j заменили с v_j на v_j + Δ. Дополните формулу нового context.
  explanation: 'В сумме меняется только слагаемое позиции j: α_j v_j превращается в α_j(v_j + Δ). Поэтому ко всему context добавляется α_j Δ.'
  kind: cloze
  template: c' = ___
  answer: c + α_j Δ
  accept:
    - c+α_jΔ
    - c + α_j * Δ
    - c + alpha_j * Δ
  id: 022-weighted-context-2
  fingerprint: 4a43ac71
