---
id: 040-final-quiz
type: quiz
title: 'Итоговая проверка: scores, masking, alignment и multi-head attention'
source_anchor: '## Key Terms'
check:
  - question: >-
      Одна padding-позиция получила самый высокий score. Что произойдёт при
      правильной обработке?
    options:
      - После softmax она получит самый большой вес
      - >-
        Её исключат до softmax, поэтому её вес станет нулём, а веса настоящих
        позиций дадут сумму 1
      - Её вес вычтут из context после взвешенной суммы
      - Все scores заменят нулями
    correct: 1
    explanation: >-
      Маска применяется до нормализации. Тогда padding не получает долю
      внимания, а softmax распределяет весь вес только между настоящими
      позициями.
  - question: >-
      Декодер сделал 4 шага, а вход содержит 6 позиций. Как устроена alignment
      matrix?
    options:
      - '6 строк на 4 столбца, сумма каждого столбца равна 0'
      - '4 строки на 6 столбцов, сумма каждой строки равна 1'
      - '4 строки на 4 столбца, сумма всей матрицы равна 1'
      - '6 строк на 6 столбцов, каждая строка повторяет scores'
    correct: 1
    explanation: >-
      Каждая строка хранит распределение весов для одного шага декодера по всем
      позициям входа. Поэтому строк 4, столбцов 6, а каждая строка нормализована
      отдельно.
  - question: >-
      Query имеет 128 чисел, а каждый key — 64. Какой способ позволит посчитать
      совместимость?
    options:
      - Обычный dot score без изменений
      - Softmax над query
      - General score с обучаемой проекцией в совместимую размерность
      - Masking лишних 64 чисел query
    correct: 2
    explanation: >-
      Для прямого скалярного произведения размерности должны совпадать. General
      score обучаемо проецирует представление, после чего сравнение становится
      возможным.
  - question: >-
      Модель размерности 128 использует 8 голов attention. Что происходит с
      размерностями?
    options:
      - >-
        Каждая голова обрабатывает 16 измерений, а результаты голов объединяются
        обратно
      - Каждая голова получает 128 новых позиций входа
      - Все головы обязаны вычислить одинаковые веса
      - Выход автоматически уменьшается до 8 измерений
    correct: 0
    explanation: >-
      При равном разбиении каждой голове достаётся 16 измерений. Затем
      результаты всех голов объединяются, сохраняя общую размерность
      представления.
---

Перед тобой четыре места, где особенно легко перепутать роли: score принять за готовый вес, применить маску после softmax, развернуть alignment matrix не той стороной или решить, что головы увеличивают размерность. Здесь пригодятся связи из [шага 7](#step-7), [шага 10](#step-10), [шага 26](#step-26) и [шага 34](#step-34). Дальше в курсе ты будешь встречать те же операции внутри более крупных моделей, поэтому важнее видеть весь путь данных, чем помнить отдельные названия.

> 🎒 **На пальцах.** Представь редакцию: кандидаты на полосу сначала получают оценки, служебные заглушки исключаются, а оставшиеся делят место. Таблица выпусков показывает отдельный выбор для каждого дня, а несколько редакторов параллельно ищут разные сюжеты. Если перепутать хотя бы один этап, итоговая полоса уже расскажет другую историю.
