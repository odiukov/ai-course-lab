---
id: 017-luong-check
type: check
title: 'Проверка: когда dot невозможен, а general работает'
source_anchor: '## Shapes (the thing that bites everyone)'
check:
  - question: >-
      Query декодера содержит 256 чисел, а состояние энкодера — 512. Какой
      способ позволяет вычислить оценку внимания без ручного изменения
      размерностей состояний?
    options:
      - 'Dot, потому что он автоматически отбросит лишние координаты'
      - 'General, потому что обучаемая матрица состыкует разные размерности'
      - 'Оба способа, потому что размерности не влияют на скалярное произведение'
      - >-
        Ни один способ: любая оценка внимания требует равных исходных
        размерностей
    correct: 1
    explanation: >-
      Dot требует одинаковой длины векторов и при 256 против 512 вообще не
      определён. General добавляет обучаемое линейное преобразование, которое
      приводит представления к совместимым размерностям.
  - question: >-
      Query и состояние энкодера оба имеют длину 256. Какие варианты Luong
      attention теперь допустимы по форме?
    options:
      - Только dot
      - Только general
      - 'И dot, и general'
      - 'Ни dot, ни general'
    correct: 2
    explanation: >-
      При равных размерностях обычный dot уже корректен. General тоже допустим:
      его обучаемая матрица может преобразовывать признаки перед вычислением
      скалярной оценки, даже когда стыковать разные размеры не требуется.
---

Проверь, различаешь ли ты несовместимость форм и выбор модели. Если длины query и состояния энкодера различаются, `dot` не выдаст плохую оценку — он вообще не сможет выполнить операцию. `General` из [шага 15](#step-15) устраняет именно это препятствие, а функция из [шага 16](#step-16) должна сохранить по одной оценке на каждую позицию входа.

> 🎒 **На пальцах.** Ты пытаешься сравнить две ведомости: в одной 256 граф, в другой 512. Простое построчное сравнение останавливается — пары есть не для всех граф. `General` сначала пропускает одну ведомость через обучаемый шаблон и получает совместимый набор граф. Теперь сравнение возможно; шаблон при этом учится сохранять полезные признаки.
