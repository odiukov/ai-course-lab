---
id: 021-additive-shapes-check
type: check
title: 'Проверка: формы проекций в additive attention'
source_anchor: '### Step 1: additive (Bahdanau) attention'
check:
  - question: >-
      encoder_states имеет 12 строк по 256 чисел, а пространство attention — 128
      чисел. Какая форма U_a даст projected_enc форму 12 строк по 128 чисел?
    options:
      - 256 строк на 128 столбцов
      - 128 строк на 256 столбцов
      - 12 строк на 128 столбцов
    correct: 1
    explanation: >-
      В коде encoder_states умножается на транспонированную U_a. Поэтому U_a
      имеет 128 строк на 256 столбцов, а её транспонированная версия — 256 строк
      на 128 столбцов.
  - question: >-
      projected_enc имеет 12 строк по 128 чисел, а projected_dec — вектор из 128
      чисел. Что получится после их сложения и умножения на v_a из 128 чисел?
    options:
      - Один вектор из 128 чисел
      - 12 отдельных оценок
      - Таблица из 12 строк по 256 чисел
    correct: 1
    explanation: >-
      Broadcasting добавит projected_dec к каждой из 12 строк, сохранив форму 12
      на 128. Затем v_a свернёт каждую строку в одну оценку.
---

Здесь легко перепутать форму матрицы с формой её транспонированной версии. Проверяй не название параметра, а внутренние размеры умножения: соседние размерности должны совпасть. После двух проекций query и все состояния оказываются в одном пространстве attention. Затем работает broadcasting из [шага 20](#step-20), а не создание новых обучаемых векторов. В итоге должно появиться по одному score на позицию, как в функции из [шага 19](#step-19).

> 🎒 **На пальцах.** На сортировке стоят 12 посылок, у каждой анкета из 256 полей. Аппарат переводит каждую анкету в карточку из 128 меток. Запрос инспектора тоже переводится в 128 меток и прикладывается ко всем карточкам. Последняя проверка оставляет по одному числу на посылку — значит, оценок ровно 12.
