---
id: 012-mask-check
type: check
title: 'Проверка: нулевой вес padding и сумма весов'
source_anchor: '## Exercises'
check:
  - question: >-
      После softmax веса позиций равны 0,5, 0,3 и 0,2. Последняя позиция —
      padding, и её вес заменили на ноль без повторной нормализации. Что
      произойдёт?
    options:
      - >-
        Сумма оставшихся весов станет 0,8, поэтому может измениться масштаб
        контекстного вектора
      - 'Сумма весов останется равной 1, потому что softmax уже был вычислен'
      - >-
        Softmax автоматически перераспределит удалённый вес между настоящими
        позициями
    correct: 0
    explanation: >-
      После softmax перерасчёта уже не происходит: удаление веса 0,2 оставляет
      сумму 0,8. Из-за этого меняется масштаб взвешенной суммы скрытых
      состояний.
  - question: >-
      Какой результат проверки masked_softmax на батче с последовательностями
      разной длины подтверждает корректное маскирование?
    options:
      - >-
        В каждой строке padding получает нулевой вес, а веса настоящих позиций в
        сумме дают 1
      - Во всём батче сумма всех весов равна 1 независимо от числа строк
      - Все настоящие позиции каждой последовательности получают одинаковые веса
    correct: 0
    explanation: >-
      Нормализация выполняется отдельно для каждой строки attention: padding
      полностью исключается, а вся единичная масса распределяется между
      настоящими позициями этой строки.
---

Здесь ты проверяешь два условия одновременно: у padding должен быть нулевой вес, а сумма весов настоящих позиций должна оставаться равной единице. Главная ловушка — обнулить padding уже после softmax. Как разобрано в [шаге 10](#step-10), тогда его прежняя доля просто исчезнет и масштаб контекста изменится. Проверяй `masked_softmax` из [шага 11](#step-11) на последовательностях разной длины: короткая строка батча особенно хорошо обнаруживает ошибку.

> 🎒 **На пальцах.** Представь кассира, распределяющего скидку в 100 рублей между покупками. Одна позиция в чеке отменена. Если сначала выдать ей 20 рублей скидки, а затем зачеркнуть, покупкам достанется лишь 80. Правильно сначала убрать отменённую позицию, а потом разделить все 100 рублей между оставшимися.
