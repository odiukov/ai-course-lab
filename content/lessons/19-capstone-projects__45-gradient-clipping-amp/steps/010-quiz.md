---
id: 010-quiz
type: quiz
title: 'Проверяем контракт клиппинга, AMP и пропуска шага'
source_anchor: '## Production Patterns'
check:
  - question: >-
      Перед клиппингом scaler ещё не снял масштабирование с градиентов. Что
      произойдёт?
    options:
      - >-
        Клиппинг сравнит с порогом искусственно увеличенные градиенты и исказит
        результат
      - Клиппинг автоматически снимет масштабирование
      - Норма станет точнее благодаря большему масштабу
      - 'Ничего не изменится, порядок этих операций не важен'
    correct: 0
    explanation: >-
      Сначала нужен unscale_, и только затем проверка конечности и клиппинг.
      Иначе порог применяется не к настоящим градиентам.
  - question: >-
      У двух параметров нормы градиентов равны 3 и 4. Какую глобальную L2-норму
      нужно использовать?
    options:
      - '7'
      - '4'
      - '5'
      - '3,5'
    correct: 2
    explanation: >-
      Все градиенты рассматриваются как один общий вектор: квадраты норм
      суммируются, поэтому получается 5.
  - question: >-
      После unscale_ в одном градиенте обнаружен Inf. Как должен завершиться
      безопасный шаг?
    options:
      - Обнулить только этот градиент и обновить веса по остальным
      - 'Пропустить изменение весов, записать причину и вызвать scaler.update()'
      - 'Сначала клиппировать Inf, затем выполнить обычный шаг'
      - 'Пропустить и optimizer.step(), и scaler.update()'
    correct: 1
    explanation: >-
      Неконечный градиент делает весь шаг небезопасным. Веса не меняются, но
      scaler.update() вызывается, чтобы масштаб мог уменьшиться.
  - question: >-
      В окне из 1000 шагов пропущено 60. Как интерпретировать это при пороге
      тревоги 5 процентов?
    options:
      - 'Это обычный фон: тревога начинается со 100 пропусков'
      - 'Тревога не нужна, если loss остаётся конечным'
      - 'Нужно увеличить max_norm, потому что пропуски всегда вызваны клиппингом'
      - 'Доля равна 6 процентам, поэтому это повод для алерта'
    correct: 3
    explanation: >-
      60 из 1000 — это 6 процентов, что выше порога 5 процентов. Частые пропуски
      означают, что обучение заметно буксует.
---

Дальше тебе понадобится видеть AMP-шаг как единый контракт: порядок операций из [шага 8](#step-8), общая норма, клиппинг и атомарный пропуск из [шага 9](#step-9). Чаще всего ошибаются не в вычислениях, а в деталях: клиппируют до `unscale_`, меняют часть весов при `Inf` или забывают вызвать `scaler.update()` после пропуска. Журнал и счётчик пропусков превращают такие тихие сбои в заметный сигнал.

> 🎒 **На пальцах.** Представь диспетчера железной дороги: он переводит стрелки только после проверки всего маршрута. Если один участок закрыт, поезд не отправляют наполовину, но происшествие записывают и обновляют расписание. Здесь градиенты — маршрут, шаг оптимизатора — отправление, а `scaler.update()` — обязательная корректировка следующего рейса.
