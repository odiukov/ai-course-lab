---
id: 018-tag-accuracy
type: code
title: Пишем tag_accuracy
source_anchor: '### Step 1: most-frequent-tag baseline'
exercise_fn: tag_accuracy
---

Ты запускаешь теггер на пяти токенах и получаешь предсказания:

```text
gold:      [DET, NOUN, VERB, ADJ, NOUN]
predicted: [DET, NOUN, NOUN, ADJ, NOUN]
```

Четыре позиции совпали, значит функция должна вернуть `0.8`. Именно такое позиционное сравнение мы разобрали в [шаге 17](#step-17). Теперь собери его в `tag_accuracy`.

Функция принимает две последовательности: правильные теги `gold_tags` и предсказания `pred_tags`. Сначала проверь, что они одинаковой длины и не пусты. Если это не так, точность посчитать нельзя: у части тегов не будет пары либо делить придётся на ноль. В таком случае подними `ValueError`.

Затем пройди по парам тегов, посчитай точные совпадения и раздели их количество на общее число позиций. Результатом должно быть число типа `float` от `0.0` до `1.0`.

```python
def tag_accuracy(gold_tags, pred_tags):
    # Проверь входные последовательности.
    # Посчитай совпадения на одинаковых позициях.
    # Верни долю совпадений.
    pass
```

Особенно внимательно отнесись к `zip`: сам по себе он молча остановится на более короткой последовательности. Поэтому проверка длины должна происходить до подсчёта, иначе лишние теги незаметно исчезнут и оценка получится недостоверной.

> 🎒 **На пальцах.** Представь застёжку-молнию, у которой каждый зубец слева должен совпасть по цвету с зубцом справа. Ты считаешь совпавшие пары и делишь на число всех пар. Но если одна сторона короче, молния не закрывается целиком: считать только состыкованную часть было бы самообманом. Поэтому сначала сравни длины, а уже потом оценивай совпадения.
