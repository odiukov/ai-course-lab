- concept: token accuracy корректна только для одинаково токенизированных и согласованно размеченных последовательностей
  question: При каком условии позиционное сравнение эталонных и предсказанных POS-тегов даёт корректную token accuracy?
  explanation: Одинаковая позиция должна относиться к одному и тому же токену. Различия в токенизации, порядке токенов или системе тегов делают позиционные совпадения несопоставимыми.
  kind: choice
  options:
    - Последовательности описывают одни и те же токены в одинаковом порядке, а теги принадлежат согласованной системе
    - Обе последовательности содержат одинаковый набор названий тегов, независимо от порядка токенов
    - Предсказанная последовательность не длиннее эталонной
    - Частоты тегов в обеих последовательностях примерно совпадают
  correct: 0
  id: 017-token-accuracy-1
  fingerprint: db0a831c
- concept: token accuracy равна доле позиционных совпадений тегов среди всех выровненных токенов
  question: Эталонная и предсказанная цепочки тегов выровнены и непусты. Заполни знаменатель формулы token accuracy.
  explanation: Числитель считает позиции с совпавшими тегами, а знаменатель — все оцениваемые токены. После проверки выравнивания длины цепочек равны, поэтому допустима длина любой из них.
  kind: cloze
  template: token_accuracy = sum(gold_tags[i] == pred_tags[i] for i in range(len(gold_tags))) / ___
  answer: len(gold_tags)
  accept:
    - len(pred_tags)
  id: 017-token-accuracy-2
  fingerprint: db8c79db
