"""Ai Control Subversion.

Правила:
- используйте только стандартную библиотеку Python;
- файл test_exercise.py не изменяйте.
"""


def audit_count(total_outputs, audit_percent):
    """Верните число выходов, которые человек успеет полностью проверить.

    Неполная проверка не учитывается. Число выходов должно быть неотрицательным,
    а доля аудита — находиться от 0 до 100 процентов.
    """
    raise NotImplementedError


def baseline_outcome(total_outputs, useful_outputs, backdoored_outputs):
    """Оцените Baseline и верните пару (полезность, безопасность).

    Полезность — доля полезных выходов, безопасность — доля выходов без
    бэкдора. Для нулевого числа выходов верните (0.0, 1.0).
    Все количества должны быть допустимыми и неотрицательными.
    """
    raise NotImplementedError


def collusion_adjusted_detection(monitor_detection, collusion):
    """Верните эффективную вероятность обнаружения сговаривающимся монитором.

    monitor_detection задаёт вероятность обнаружения без сговора, а collusion —
    долю атак, которые монитор намеренно пропускает. Обе величины должны быть
    вероятностями от 0 до 1.
    """
    raise NotImplementedError


def trusted_editing_risk(backdoor_rate, trusted_catch_rate, human_audit_rate):
    """Верните ожидаемую долю выпущенных бэкдоров при Trusted Editing.

    Сначала независимый доверенный редактор ловит часть бэкдоров, затем человек
    независимо проверяет указанную долю оставшихся выходов. Все аргументы должны
    быть вероятностями от 0 до 1.
    """
    raise NotImplementedError


def frontier_dominates(candidate, comparison):
    """Определите, доминирует ли первый фронт над вторым по Парето.

    Каждый фронт — список пар (безопасность, полезность). Для каждой точки
    comparison в candidate должна быть точка с той же безопасностью и не меньшей
    полезностью; хотя бы одно улучшение должно быть строгим. Пустой фронт не
    доминирует. Все показатели должны находиться от 0 до 1.
    """
    raise NotImplementedError


def trusted_final_gate(steps):
    """Верните True, если последнее слово в протоколе остаётся за T или H.

    steps — последовательность ролей "U", "T" и "H". Пустая последовательность
    не имеет доверенного шлюза. Неизвестная роль считается ошибкой.
    """
    raise NotImplementedError
